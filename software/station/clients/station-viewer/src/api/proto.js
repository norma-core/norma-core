/*eslint-disable*/
import * as $protobuf from "protobufjs/minimal";

// Common aliases
const $Reader = $protobuf.Reader, $Writer = $protobuf.Writer, $util = $protobuf.util;

// Exported root namespace
const $root = $protobuf.roots["default"] || ($protobuf.roots["default"] = {});

export const commands = $root.commands = (() => {

    /**
     * Namespace commands.
     * @exports commands
     * @namespace
     */
    const commands = {};

    commands.DriverCommand = (function() {

        /**
         * Properties of a DriverCommand.
         * @memberof commands
         * @interface IDriverCommand
         * @property {Uint8Array|null} [commandId] DriverCommand commandId
         * @property {drivers.StationCommandType|null} [type] DriverCommand type
         * @property {Uint8Array|null} [body] DriverCommand body
         */

        /**
         * Constructs a new DriverCommand.
         * @memberof commands
         * @classdesc Represents a DriverCommand.
         * @implements IDriverCommand
         * @constructor
         * @param {commands.IDriverCommand=} [properties] Properties to set
         */
        function DriverCommand(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * DriverCommand commandId.
         * @member {Uint8Array} commandId
         * @memberof commands.DriverCommand
         * @instance
         */
        DriverCommand.prototype.commandId = $util.newBuffer([]);

        /**
         * DriverCommand type.
         * @member {drivers.StationCommandType} type
         * @memberof commands.DriverCommand
         * @instance
         */
        DriverCommand.prototype.type = 0;

        /**
         * DriverCommand body.
         * @member {Uint8Array} body
         * @memberof commands.DriverCommand
         * @instance
         */
        DriverCommand.prototype.body = $util.newBuffer([]);

        /**
         * Creates a new DriverCommand instance using the specified properties.
         * @function create
         * @memberof commands.DriverCommand
         * @static
         * @param {commands.IDriverCommand=} [properties] Properties to set
         * @returns {commands.DriverCommand} DriverCommand instance
         */
        DriverCommand.create = function create(properties) {
            return new DriverCommand(properties);
        };

        /**
         * Encodes the specified DriverCommand message. Does not implicitly {@link commands.DriverCommand.verify|verify} messages.
         * @function encode
         * @memberof commands.DriverCommand
         * @static
         * @param {commands.IDriverCommand} message DriverCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DriverCommand.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.commandId != null && Object.hasOwnProperty.call(message, "commandId"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.commandId);
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.type);
            if (message.body != null && Object.hasOwnProperty.call(message, "body"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.body);
            return writer;
        };

        /**
         * Encodes the specified DriverCommand message, length delimited. Does not implicitly {@link commands.DriverCommand.verify|verify} messages.
         * @function encodeDelimited
         * @memberof commands.DriverCommand
         * @static
         * @param {commands.IDriverCommand} message DriverCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DriverCommand.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a DriverCommand message from the specified reader or buffer.
         * @function decode
         * @memberof commands.DriverCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {commands.DriverCommand} DriverCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DriverCommand.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.commands.DriverCommand();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.commandId = reader.bytes();
                        break;
                    }
                case 2: {
                        message.type = reader.int32();
                        break;
                    }
                case 3: {
                        message.body = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a DriverCommand message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof commands.DriverCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {commands.DriverCommand} DriverCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DriverCommand.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a DriverCommand message.
         * @function verify
         * @memberof commands.DriverCommand
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        DriverCommand.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.commandId != null && message.hasOwnProperty("commandId"))
                if (!(message.commandId && typeof message.commandId.length === "number" || $util.isString(message.commandId)))
                    return "commandId: buffer expected";
            if (message.type != null && message.hasOwnProperty("type"))
                switch (message.type) {
                default:
                    return "type: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                case 5:
                case 6:
                case 7:
                case 8:
                    break;
                }
            if (message.body != null && message.hasOwnProperty("body"))
                if (!(message.body && typeof message.body.length === "number" || $util.isString(message.body)))
                    return "body: buffer expected";
            return null;
        };

        /**
         * Creates a DriverCommand message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof commands.DriverCommand
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {commands.DriverCommand} DriverCommand
         */
        DriverCommand.fromObject = function fromObject(object, long) {
            if (object instanceof $root.commands.DriverCommand)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.commands.DriverCommand();
            if (object.commandId != null)
                if (typeof object.commandId === "string")
                    $util.base64.decode(object.commandId, message.commandId = $util.newBuffer($util.base64.length(object.commandId)), 0);
                else if (object.commandId.length >= 0)
                    message.commandId = object.commandId;
            switch (object.type) {
            default:
                if (typeof object.type === "number") {
                    message.type = object.type;
                    break;
                }
                break;
            case "STC_ST3215_COMMAND":
            case 0:
                message.type = 0;
                break;
            case "STC_MOTOR_MIRRORING_COMMAND":
            case 1:
                message.type = 1;
                break;
            case "STC_INFERENCE_TAG_COMMAND":
            case 2:
                message.type = 2;
                break;
            case "STC_YAHBOOM_DOGZILLA_LITE_COMMAND":
            case 3:
                message.type = 3;
                break;
            case "STC_VESC_TRAMPA_COMMAND":
            case 4:
                message.type = 4;
                break;
            case "STC_ARDUINO_NICLA_SENSE_ENV_COMMAND":
            case 5:
                message.type = 5;
                break;
            case "STC_PWM_OUTPUT_COMMAND":
            case 6:
                message.type = 6;
                break;
            case "STC_USB_VIDEO_COMMAND":
            case 7:
                message.type = 7;
                break;
            case "STC_DFROBOT_RS485_COMMAND":
            case 8:
                message.type = 8;
                break;
            }
            if (object.body != null)
                if (typeof object.body === "string")
                    $util.base64.decode(object.body, message.body = $util.newBuffer($util.base64.length(object.body)), 0);
                else if (object.body.length >= 0)
                    message.body = object.body;
            return message;
        };

        /**
         * Creates a plain object from a DriverCommand message. Also converts values to other types if specified.
         * @function toObject
         * @memberof commands.DriverCommand
         * @static
         * @param {commands.DriverCommand} message DriverCommand
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DriverCommand.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.commandId = "";
                else {
                    object.commandId = [];
                    if (options.bytes !== Array)
                        object.commandId = $util.newBuffer(object.commandId);
                }
                object.type = options.enums === String ? "STC_ST3215_COMMAND" : 0;
                if (options.bytes === String)
                    object.body = "";
                else {
                    object.body = [];
                    if (options.bytes !== Array)
                        object.body = $util.newBuffer(object.body);
                }
            }
            if (message.commandId != null && message.hasOwnProperty("commandId"))
                object.commandId = options.bytes === String ? $util.base64.encode(message.commandId, 0, message.commandId.length) : options.bytes === Array ? Array.prototype.slice.call(message.commandId) : message.commandId;
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = options.enums === String ? $root.drivers.StationCommandType[message.type] === undefined ? message.type : $root.drivers.StationCommandType[message.type] : message.type;
            if (message.body != null && message.hasOwnProperty("body"))
                object.body = options.bytes === String ? $util.base64.encode(message.body, 0, message.body.length) : options.bytes === Array ? Array.prototype.slice.call(message.body) : message.body;
            return object;
        };

        /**
         * Converts this DriverCommand to JSON.
         * @function toJSON
         * @memberof commands.DriverCommand
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DriverCommand.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for DriverCommand
         * @function getTypeUrl
         * @memberof commands.DriverCommand
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        DriverCommand.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/commands.DriverCommand";
        };

        return DriverCommand;
    })();

    commands.StationCommandsPack = (function() {

        /**
         * Properties of a StationCommandsPack.
         * @memberof commands
         * @interface IStationCommandsPack
         * @property {Uint8Array|null} [inferenceStateId] StationCommandsPack inferenceStateId
         * @property {Uint8Array|null} [packId] StationCommandsPack packId
         * @property {Array.<commands.IDriverCommand>|null} [commands] StationCommandsPack commands
         * @property {Array.<string>|null} [tags] StationCommandsPack tags
         */

        /**
         * Constructs a new StationCommandsPack.
         * @memberof commands
         * @classdesc Represents a StationCommandsPack.
         * @implements IStationCommandsPack
         * @constructor
         * @param {commands.IStationCommandsPack=} [properties] Properties to set
         */
        function StationCommandsPack(properties) {
            this.commands = [];
            this.tags = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * StationCommandsPack inferenceStateId.
         * @member {Uint8Array} inferenceStateId
         * @memberof commands.StationCommandsPack
         * @instance
         */
        StationCommandsPack.prototype.inferenceStateId = $util.newBuffer([]);

        /**
         * StationCommandsPack packId.
         * @member {Uint8Array} packId
         * @memberof commands.StationCommandsPack
         * @instance
         */
        StationCommandsPack.prototype.packId = $util.newBuffer([]);

        /**
         * StationCommandsPack commands.
         * @member {Array.<commands.IDriverCommand>} commands
         * @memberof commands.StationCommandsPack
         * @instance
         */
        StationCommandsPack.prototype.commands = $util.emptyArray;

        /**
         * StationCommandsPack tags.
         * @member {Array.<string>} tags
         * @memberof commands.StationCommandsPack
         * @instance
         */
        StationCommandsPack.prototype.tags = $util.emptyArray;

        /**
         * Creates a new StationCommandsPack instance using the specified properties.
         * @function create
         * @memberof commands.StationCommandsPack
         * @static
         * @param {commands.IStationCommandsPack=} [properties] Properties to set
         * @returns {commands.StationCommandsPack} StationCommandsPack instance
         */
        StationCommandsPack.create = function create(properties) {
            return new StationCommandsPack(properties);
        };

        /**
         * Encodes the specified StationCommandsPack message. Does not implicitly {@link commands.StationCommandsPack.verify|verify} messages.
         * @function encode
         * @memberof commands.StationCommandsPack
         * @static
         * @param {commands.IStationCommandsPack} message StationCommandsPack message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StationCommandsPack.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.inferenceStateId != null && Object.hasOwnProperty.call(message, "inferenceStateId"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.inferenceStateId);
            if (message.packId != null && Object.hasOwnProperty.call(message, "packId"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.packId);
            if (message.commands != null && message.commands.length)
                for (let i = 0; i < message.commands.length; ++i)
                    $root.commands.DriverCommand.encode(message.commands[i], writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            if (message.tags != null && message.tags.length)
                for (let i = 0; i < message.tags.length; ++i)
                    writer.uint32(/* id 20, wireType 2 =*/162).string(message.tags[i]);
            return writer;
        };

        /**
         * Encodes the specified StationCommandsPack message, length delimited. Does not implicitly {@link commands.StationCommandsPack.verify|verify} messages.
         * @function encodeDelimited
         * @memberof commands.StationCommandsPack
         * @static
         * @param {commands.IStationCommandsPack} message StationCommandsPack message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StationCommandsPack.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a StationCommandsPack message from the specified reader or buffer.
         * @function decode
         * @memberof commands.StationCommandsPack
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {commands.StationCommandsPack} StationCommandsPack
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StationCommandsPack.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.commands.StationCommandsPack();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.inferenceStateId = reader.bytes();
                        break;
                    }
                case 2: {
                        message.packId = reader.bytes();
                        break;
                    }
                case 10: {
                        if (!(message.commands && message.commands.length))
                            message.commands = [];
                        message.commands.push($root.commands.DriverCommand.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 20: {
                        if (!(message.tags && message.tags.length))
                            message.tags = [];
                        message.tags.push(reader.string());
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a StationCommandsPack message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof commands.StationCommandsPack
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {commands.StationCommandsPack} StationCommandsPack
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StationCommandsPack.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a StationCommandsPack message.
         * @function verify
         * @memberof commands.StationCommandsPack
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        StationCommandsPack.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.inferenceStateId != null && message.hasOwnProperty("inferenceStateId"))
                if (!(message.inferenceStateId && typeof message.inferenceStateId.length === "number" || $util.isString(message.inferenceStateId)))
                    return "inferenceStateId: buffer expected";
            if (message.packId != null && message.hasOwnProperty("packId"))
                if (!(message.packId && typeof message.packId.length === "number" || $util.isString(message.packId)))
                    return "packId: buffer expected";
            if (message.commands != null && message.hasOwnProperty("commands")) {
                if (!Array.isArray(message.commands))
                    return "commands: array expected";
                for (let i = 0; i < message.commands.length; ++i) {
                    let error = $root.commands.DriverCommand.verify(message.commands[i], long + 1);
                    if (error)
                        return "commands." + error;
                }
            }
            if (message.tags != null && message.hasOwnProperty("tags")) {
                if (!Array.isArray(message.tags))
                    return "tags: array expected";
                for (let i = 0; i < message.tags.length; ++i)
                    if (!$util.isString(message.tags[i]))
                        return "tags: string[] expected";
            }
            return null;
        };

        /**
         * Creates a StationCommandsPack message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof commands.StationCommandsPack
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {commands.StationCommandsPack} StationCommandsPack
         */
        StationCommandsPack.fromObject = function fromObject(object, long) {
            if (object instanceof $root.commands.StationCommandsPack)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.commands.StationCommandsPack();
            if (object.inferenceStateId != null)
                if (typeof object.inferenceStateId === "string")
                    $util.base64.decode(object.inferenceStateId, message.inferenceStateId = $util.newBuffer($util.base64.length(object.inferenceStateId)), 0);
                else if (object.inferenceStateId.length >= 0)
                    message.inferenceStateId = object.inferenceStateId;
            if (object.packId != null)
                if (typeof object.packId === "string")
                    $util.base64.decode(object.packId, message.packId = $util.newBuffer($util.base64.length(object.packId)), 0);
                else if (object.packId.length >= 0)
                    message.packId = object.packId;
            if (object.commands) {
                if (!Array.isArray(object.commands))
                    throw TypeError(".commands.StationCommandsPack.commands: array expected");
                message.commands = [];
                for (let i = 0; i < object.commands.length; ++i) {
                    if (typeof object.commands[i] !== "object")
                        throw TypeError(".commands.StationCommandsPack.commands: object expected");
                    message.commands[i] = $root.commands.DriverCommand.fromObject(object.commands[i], long + 1);
                }
            }
            if (object.tags) {
                if (!Array.isArray(object.tags))
                    throw TypeError(".commands.StationCommandsPack.tags: array expected");
                message.tags = [];
                for (let i = 0; i < object.tags.length; ++i)
                    message.tags[i] = String(object.tags[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a StationCommandsPack message. Also converts values to other types if specified.
         * @function toObject
         * @memberof commands.StationCommandsPack
         * @static
         * @param {commands.StationCommandsPack} message StationCommandsPack
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        StationCommandsPack.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.commands = [];
                object.tags = [];
            }
            if (options.defaults) {
                if (options.bytes === String)
                    object.inferenceStateId = "";
                else {
                    object.inferenceStateId = [];
                    if (options.bytes !== Array)
                        object.inferenceStateId = $util.newBuffer(object.inferenceStateId);
                }
                if (options.bytes === String)
                    object.packId = "";
                else {
                    object.packId = [];
                    if (options.bytes !== Array)
                        object.packId = $util.newBuffer(object.packId);
                }
            }
            if (message.inferenceStateId != null && message.hasOwnProperty("inferenceStateId"))
                object.inferenceStateId = options.bytes === String ? $util.base64.encode(message.inferenceStateId, 0, message.inferenceStateId.length) : options.bytes === Array ? Array.prototype.slice.call(message.inferenceStateId) : message.inferenceStateId;
            if (message.packId != null && message.hasOwnProperty("packId"))
                object.packId = options.bytes === String ? $util.base64.encode(message.packId, 0, message.packId.length) : options.bytes === Array ? Array.prototype.slice.call(message.packId) : message.packId;
            if (message.commands && message.commands.length) {
                object.commands = [];
                for (let j = 0; j < message.commands.length; ++j)
                    object.commands[j] = $root.commands.DriverCommand.toObject(message.commands[j], options);
            }
            if (message.tags && message.tags.length) {
                object.tags = [];
                for (let j = 0; j < message.tags.length; ++j)
                    object.tags[j] = message.tags[j];
            }
            return object;
        };

        /**
         * Converts this StationCommandsPack to JSON.
         * @function toJSON
         * @memberof commands.StationCommandsPack
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        StationCommandsPack.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for StationCommandsPack
         * @function getTypeUrl
         * @memberof commands.StationCommandsPack
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        StationCommandsPack.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/commands.StationCommandsPack";
        };

        return StationCommandsPack;
    })();

    return commands;
})();

export const drivers = $root.drivers = (() => {

    /**
     * Namespace drivers.
     * @exports drivers
     * @namespace
     */
    const drivers = {};

    /**
     * QueueDataType enum.
     * @name drivers.QueueDataType
     * @enum {number}
     * @property {number} QDT_SYSTEM=0 QDT_SYSTEM value
     * @property {number} QDT_STATION_COMMANDS=1 QDT_STATION_COMMANDS value
     * @property {number} QDT_STATION_STARTUPS=2 QDT_STATION_STARTUPS value
     * @property {number} QDT_INFERENCE_TAGS_RX=3 QDT_INFERENCE_TAGS_RX value
     * @property {number} QDT_ST3215_SERIAL_TX=10 QDT_ST3215_SERIAL_TX value
     * @property {number} QDT_ST3215_SERIAL_RX=11 QDT_ST3215_SERIAL_RX value
     * @property {number} QDT_ST3215_META=12 QDT_ST3215_META value
     * @property {number} QDT_ST3215_INFERENCE=13 QDT_ST3215_INFERENCE value
     * @property {number} QDT_VESC_TRAMPA_SERIAL_RX=14 QDT_VESC_TRAMPA_SERIAL_RX value
     * @property {number} QDT_VESC_TRAMPA_SERIAL_TX=15 QDT_VESC_TRAMPA_SERIAL_TX value
     * @property {number} QDT_VESC_TRAMPA_INFERENCE=16 QDT_VESC_TRAMPA_INFERENCE value
     * @property {number} QDT_FFMPEG_VIDEO_STREAM_RX=20 QDT_FFMPEG_VIDEO_STREAM_RX value
     * @property {number} QDT_USB_VIDEO_FRAMES=21 QDT_USB_VIDEO_FRAMES value
     * @property {number} QDT_INFERENCE_FRAMES=22 QDT_INFERENCE_FRAMES value
     * @property {number} QDT_HIKMICRO_THERMAL=23 QDT_HIKMICRO_THERMAL value
     * @property {number} QDT_USB_VIDEO_TX=24 QDT_USB_VIDEO_TX value
     * @property {number} QDT_MOTOR_MIRRORING_MODES=30 QDT_MOTOR_MIRRORING_MODES value
     * @property {number} QDT_MOTOR_MIRRORING_RX=32 QDT_MOTOR_MIRRORING_RX value
     * @property {number} QDT_YAHBOOM_DOGZILLA_LITE_SERIAL_TX=40 QDT_YAHBOOM_DOGZILLA_LITE_SERIAL_TX value
     * @property {number} QDT_YAHBOOM_DOGZILLA_LITE_SERIAL_RX=41 QDT_YAHBOOM_DOGZILLA_LITE_SERIAL_RX value
     * @property {number} QDT_YAHBOOM_DOGZILLA_LITE_INFERENCE=42 QDT_YAHBOOM_DOGZILLA_LITE_INFERENCE value
     * @property {number} QDT_ARDUINO_NICLA_SENSE_ENV_TX=50 QDT_ARDUINO_NICLA_SENSE_ENV_TX value
     * @property {number} QDT_ARDUINO_NICLA_SENSE_ENV_RX=51 QDT_ARDUINO_NICLA_SENSE_ENV_RX value
     * @property {number} QDT_INA226_RX=52 QDT_INA226_RX value
     * @property {number} QDT_AIRGRADIENT_OPEN_AIR_O_1PST_RX=53 QDT_AIRGRADIENT_OPEN_AIR_O_1PST_RX value
     * @property {number} QDT_VICTRON_SMARTSOLAR_MPPT_RX=54 QDT_VICTRON_SMARTSOLAR_MPPT_RX value
     * @property {number} QDT_PWM_OUTPUT_TX=55 QDT_PWM_OUTPUT_TX value
     * @property {number} QDT_PWM_OUTPUT_RX=56 QDT_PWM_OUTPUT_RX value
     * @property {number} QDT_DMESG_RX=57 QDT_DMESG_RX value
     * @property {number} QDT_DFROBOT_RS485_RX=58 QDT_DFROBOT_RS485_RX value
     */
    drivers.QueueDataType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "QDT_SYSTEM"] = 0;
        values[valuesById[1] = "QDT_STATION_COMMANDS"] = 1;
        values[valuesById[2] = "QDT_STATION_STARTUPS"] = 2;
        values[valuesById[3] = "QDT_INFERENCE_TAGS_RX"] = 3;
        values[valuesById[10] = "QDT_ST3215_SERIAL_TX"] = 10;
        values[valuesById[11] = "QDT_ST3215_SERIAL_RX"] = 11;
        values[valuesById[12] = "QDT_ST3215_META"] = 12;
        values[valuesById[13] = "QDT_ST3215_INFERENCE"] = 13;
        values[valuesById[14] = "QDT_VESC_TRAMPA_SERIAL_RX"] = 14;
        values[valuesById[15] = "QDT_VESC_TRAMPA_SERIAL_TX"] = 15;
        values[valuesById[16] = "QDT_VESC_TRAMPA_INFERENCE"] = 16;
        values[valuesById[20] = "QDT_FFMPEG_VIDEO_STREAM_RX"] = 20;
        values[valuesById[21] = "QDT_USB_VIDEO_FRAMES"] = 21;
        values[valuesById[22] = "QDT_INFERENCE_FRAMES"] = 22;
        values[valuesById[23] = "QDT_HIKMICRO_THERMAL"] = 23;
        values[valuesById[24] = "QDT_USB_VIDEO_TX"] = 24;
        values[valuesById[30] = "QDT_MOTOR_MIRRORING_MODES"] = 30;
        values[valuesById[32] = "QDT_MOTOR_MIRRORING_RX"] = 32;
        values[valuesById[40] = "QDT_YAHBOOM_DOGZILLA_LITE_SERIAL_TX"] = 40;
        values[valuesById[41] = "QDT_YAHBOOM_DOGZILLA_LITE_SERIAL_RX"] = 41;
        values[valuesById[42] = "QDT_YAHBOOM_DOGZILLA_LITE_INFERENCE"] = 42;
        values[valuesById[50] = "QDT_ARDUINO_NICLA_SENSE_ENV_TX"] = 50;
        values[valuesById[51] = "QDT_ARDUINO_NICLA_SENSE_ENV_RX"] = 51;
        values[valuesById[52] = "QDT_INA226_RX"] = 52;
        values[valuesById[53] = "QDT_AIRGRADIENT_OPEN_AIR_O_1PST_RX"] = 53;
        values[valuesById[54] = "QDT_VICTRON_SMARTSOLAR_MPPT_RX"] = 54;
        values[valuesById[55] = "QDT_PWM_OUTPUT_TX"] = 55;
        values[valuesById[56] = "QDT_PWM_OUTPUT_RX"] = 56;
        values[valuesById[57] = "QDT_DMESG_RX"] = 57;
        values[valuesById[58] = "QDT_DFROBOT_RS485_RX"] = 58;
        return values;
    })();

    /**
     * StationCommandType enum.
     * @name drivers.StationCommandType
     * @enum {number}
     * @property {number} STC_ST3215_COMMAND=0 STC_ST3215_COMMAND value
     * @property {number} STC_MOTOR_MIRRORING_COMMAND=1 STC_MOTOR_MIRRORING_COMMAND value
     * @property {number} STC_INFERENCE_TAG_COMMAND=2 STC_INFERENCE_TAG_COMMAND value
     * @property {number} STC_YAHBOOM_DOGZILLA_LITE_COMMAND=3 STC_YAHBOOM_DOGZILLA_LITE_COMMAND value
     * @property {number} STC_VESC_TRAMPA_COMMAND=4 STC_VESC_TRAMPA_COMMAND value
     * @property {number} STC_ARDUINO_NICLA_SENSE_ENV_COMMAND=5 STC_ARDUINO_NICLA_SENSE_ENV_COMMAND value
     * @property {number} STC_PWM_OUTPUT_COMMAND=6 STC_PWM_OUTPUT_COMMAND value
     * @property {number} STC_USB_VIDEO_COMMAND=7 STC_USB_VIDEO_COMMAND value
     * @property {number} STC_DFROBOT_RS485_COMMAND=8 STC_DFROBOT_RS485_COMMAND value
     */
    drivers.StationCommandType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "STC_ST3215_COMMAND"] = 0;
        values[valuesById[1] = "STC_MOTOR_MIRRORING_COMMAND"] = 1;
        values[valuesById[2] = "STC_INFERENCE_TAG_COMMAND"] = 2;
        values[valuesById[3] = "STC_YAHBOOM_DOGZILLA_LITE_COMMAND"] = 3;
        values[valuesById[4] = "STC_VESC_TRAMPA_COMMAND"] = 4;
        values[valuesById[5] = "STC_ARDUINO_NICLA_SENSE_ENV_COMMAND"] = 5;
        values[valuesById[6] = "STC_PWM_OUTPUT_COMMAND"] = 6;
        values[valuesById[7] = "STC_USB_VIDEO_COMMAND"] = 7;
        values[valuesById[8] = "STC_DFROBOT_RS485_COMMAND"] = 8;
        return values;
    })();

    return drivers;
})();

export const inference = $root.inference = (() => {

    /**
     * Namespace inference.
     * @exports inference
     * @namespace
     */
    const inference = {};

    inference.InferenceRx = (function() {

        /**
         * Properties of an InferenceRx.
         * @memberof inference
         * @interface IInferenceRx
         * @property {Long|null} [localStampNs] InferenceRx localStampNs
         * @property {Long|null} [monotonicStampNs] InferenceRx monotonicStampNs
         * @property {Long|null} [appStartId] InferenceRx appStartId
         * @property {Array.<inference.InferenceRx.IEntry>|null} [entries] InferenceRx entries
         */

        /**
         * Constructs a new InferenceRx.
         * @memberof inference
         * @classdesc Represents an InferenceRx.
         * @implements IInferenceRx
         * @constructor
         * @param {inference.IInferenceRx=} [properties] Properties to set
         */
        function InferenceRx(properties) {
            this.entries = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * InferenceRx localStampNs.
         * @member {Long} localStampNs
         * @memberof inference.InferenceRx
         * @instance
         */
        InferenceRx.prototype.localStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * InferenceRx monotonicStampNs.
         * @member {Long} monotonicStampNs
         * @memberof inference.InferenceRx
         * @instance
         */
        InferenceRx.prototype.monotonicStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * InferenceRx appStartId.
         * @member {Long} appStartId
         * @memberof inference.InferenceRx
         * @instance
         */
        InferenceRx.prototype.appStartId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * InferenceRx entries.
         * @member {Array.<inference.InferenceRx.IEntry>} entries
         * @memberof inference.InferenceRx
         * @instance
         */
        InferenceRx.prototype.entries = $util.emptyArray;

        /**
         * Creates a new InferenceRx instance using the specified properties.
         * @function create
         * @memberof inference.InferenceRx
         * @static
         * @param {inference.IInferenceRx=} [properties] Properties to set
         * @returns {inference.InferenceRx} InferenceRx instance
         */
        InferenceRx.create = function create(properties) {
            return new InferenceRx(properties);
        };

        /**
         * Encodes the specified InferenceRx message. Does not implicitly {@link inference.InferenceRx.verify|verify} messages.
         * @function encode
         * @memberof inference.InferenceRx
         * @static
         * @param {inference.IInferenceRx} message InferenceRx message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        InferenceRx.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.localStampNs != null && Object.hasOwnProperty.call(message, "localStampNs"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.localStampNs);
            if (message.monotonicStampNs != null && Object.hasOwnProperty.call(message, "monotonicStampNs"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.monotonicStampNs);
            if (message.appStartId != null && Object.hasOwnProperty.call(message, "appStartId"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.appStartId);
            if (message.entries != null && message.entries.length)
                for (let i = 0; i < message.entries.length; ++i)
                    $root.inference.InferenceRx.Entry.encode(message.entries[i], writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified InferenceRx message, length delimited. Does not implicitly {@link inference.InferenceRx.verify|verify} messages.
         * @function encodeDelimited
         * @memberof inference.InferenceRx
         * @static
         * @param {inference.IInferenceRx} message InferenceRx message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        InferenceRx.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an InferenceRx message from the specified reader or buffer.
         * @function decode
         * @memberof inference.InferenceRx
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {inference.InferenceRx} InferenceRx
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        InferenceRx.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.inference.InferenceRx();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.localStampNs = reader.uint64();
                        break;
                    }
                case 2: {
                        message.monotonicStampNs = reader.uint64();
                        break;
                    }
                case 3: {
                        message.appStartId = reader.uint64();
                        break;
                    }
                case 10: {
                        if (!(message.entries && message.entries.length))
                            message.entries = [];
                        message.entries.push($root.inference.InferenceRx.Entry.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an InferenceRx message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof inference.InferenceRx
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {inference.InferenceRx} InferenceRx
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        InferenceRx.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an InferenceRx message.
         * @function verify
         * @memberof inference.InferenceRx
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        InferenceRx.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (!$util.isInteger(message.localStampNs) && !(message.localStampNs && $util.isInteger(message.localStampNs.low) && $util.isInteger(message.localStampNs.high)))
                    return "localStampNs: integer|Long expected";
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (!$util.isInteger(message.monotonicStampNs) && !(message.monotonicStampNs && $util.isInteger(message.monotonicStampNs.low) && $util.isInteger(message.monotonicStampNs.high)))
                    return "monotonicStampNs: integer|Long expected";
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (!$util.isInteger(message.appStartId) && !(message.appStartId && $util.isInteger(message.appStartId.low) && $util.isInteger(message.appStartId.high)))
                    return "appStartId: integer|Long expected";
            if (message.entries != null && message.hasOwnProperty("entries")) {
                if (!Array.isArray(message.entries))
                    return "entries: array expected";
                for (let i = 0; i < message.entries.length; ++i) {
                    let error = $root.inference.InferenceRx.Entry.verify(message.entries[i], long + 1);
                    if (error)
                        return "entries." + error;
                }
            }
            return null;
        };

        /**
         * Creates an InferenceRx message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof inference.InferenceRx
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {inference.InferenceRx} InferenceRx
         */
        InferenceRx.fromObject = function fromObject(object, long) {
            if (object instanceof $root.inference.InferenceRx)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.inference.InferenceRx();
            if (object.localStampNs != null)
                if ($util.Long)
                    (message.localStampNs = $util.Long.fromValue(object.localStampNs)).unsigned = true;
                else if (typeof object.localStampNs === "string")
                    message.localStampNs = parseInt(object.localStampNs, 10);
                else if (typeof object.localStampNs === "number")
                    message.localStampNs = object.localStampNs;
                else if (typeof object.localStampNs === "object")
                    message.localStampNs = new $util.LongBits(object.localStampNs.low >>> 0, object.localStampNs.high >>> 0).toNumber(true);
            if (object.monotonicStampNs != null)
                if ($util.Long)
                    (message.monotonicStampNs = $util.Long.fromValue(object.monotonicStampNs)).unsigned = true;
                else if (typeof object.monotonicStampNs === "string")
                    message.monotonicStampNs = parseInt(object.monotonicStampNs, 10);
                else if (typeof object.monotonicStampNs === "number")
                    message.monotonicStampNs = object.monotonicStampNs;
                else if (typeof object.monotonicStampNs === "object")
                    message.monotonicStampNs = new $util.LongBits(object.monotonicStampNs.low >>> 0, object.monotonicStampNs.high >>> 0).toNumber(true);
            if (object.appStartId != null)
                if ($util.Long)
                    (message.appStartId = $util.Long.fromValue(object.appStartId)).unsigned = true;
                else if (typeof object.appStartId === "string")
                    message.appStartId = parseInt(object.appStartId, 10);
                else if (typeof object.appStartId === "number")
                    message.appStartId = object.appStartId;
                else if (typeof object.appStartId === "object")
                    message.appStartId = new $util.LongBits(object.appStartId.low >>> 0, object.appStartId.high >>> 0).toNumber(true);
            if (object.entries) {
                if (!Array.isArray(object.entries))
                    throw TypeError(".inference.InferenceRx.entries: array expected");
                message.entries = [];
                for (let i = 0; i < object.entries.length; ++i) {
                    if (typeof object.entries[i] !== "object")
                        throw TypeError(".inference.InferenceRx.entries: object expected");
                    message.entries[i] = $root.inference.InferenceRx.Entry.fromObject(object.entries[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from an InferenceRx message. Also converts values to other types if specified.
         * @function toObject
         * @memberof inference.InferenceRx
         * @static
         * @param {inference.InferenceRx} message InferenceRx
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        InferenceRx.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.entries = [];
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.localStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.localStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.monotonicStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.monotonicStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.appStartId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.appStartId = options.longs === String ? "0" : 0;
            }
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (typeof message.localStampNs === "number")
                    object.localStampNs = options.longs === String ? String(message.localStampNs) : message.localStampNs;
                else
                    object.localStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.localStampNs) : options.longs === Number ? new $util.LongBits(message.localStampNs.low >>> 0, message.localStampNs.high >>> 0).toNumber(true) : message.localStampNs;
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (typeof message.monotonicStampNs === "number")
                    object.monotonicStampNs = options.longs === String ? String(message.monotonicStampNs) : message.monotonicStampNs;
                else
                    object.monotonicStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.monotonicStampNs) : options.longs === Number ? new $util.LongBits(message.monotonicStampNs.low >>> 0, message.monotonicStampNs.high >>> 0).toNumber(true) : message.monotonicStampNs;
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (typeof message.appStartId === "number")
                    object.appStartId = options.longs === String ? String(message.appStartId) : message.appStartId;
                else
                    object.appStartId = options.longs === String ? $util.Long.prototype.toString.call(message.appStartId) : options.longs === Number ? new $util.LongBits(message.appStartId.low >>> 0, message.appStartId.high >>> 0).toNumber(true) : message.appStartId;
            if (message.entries && message.entries.length) {
                object.entries = [];
                for (let j = 0; j < message.entries.length; ++j)
                    object.entries[j] = $root.inference.InferenceRx.Entry.toObject(message.entries[j], options);
            }
            return object;
        };

        /**
         * Converts this InferenceRx to JSON.
         * @function toJSON
         * @memberof inference.InferenceRx
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        InferenceRx.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for InferenceRx
         * @function getTypeUrl
         * @memberof inference.InferenceRx
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        InferenceRx.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/inference.InferenceRx";
        };

        InferenceRx.Entry = (function() {

            /**
             * Properties of an Entry.
             * @memberof inference.InferenceRx
             * @interface IEntry
             * @property {string|null} [queue] Entry queue
             * @property {Uint8Array|null} [ptr] Entry ptr
             * @property {drivers.QueueDataType|null} [type] Entry type
             */

            /**
             * Constructs a new Entry.
             * @memberof inference.InferenceRx
             * @classdesc Represents an Entry.
             * @implements IEntry
             * @constructor
             * @param {inference.InferenceRx.IEntry=} [properties] Properties to set
             */
            function Entry(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * Entry queue.
             * @member {string} queue
             * @memberof inference.InferenceRx.Entry
             * @instance
             */
            Entry.prototype.queue = "";

            /**
             * Entry ptr.
             * @member {Uint8Array} ptr
             * @memberof inference.InferenceRx.Entry
             * @instance
             */
            Entry.prototype.ptr = $util.newBuffer([]);

            /**
             * Entry type.
             * @member {drivers.QueueDataType} type
             * @memberof inference.InferenceRx.Entry
             * @instance
             */
            Entry.prototype.type = 0;

            /**
             * Creates a new Entry instance using the specified properties.
             * @function create
             * @memberof inference.InferenceRx.Entry
             * @static
             * @param {inference.InferenceRx.IEntry=} [properties] Properties to set
             * @returns {inference.InferenceRx.Entry} Entry instance
             */
            Entry.create = function create(properties) {
                return new Entry(properties);
            };

            /**
             * Encodes the specified Entry message. Does not implicitly {@link inference.InferenceRx.Entry.verify|verify} messages.
             * @function encode
             * @memberof inference.InferenceRx.Entry
             * @static
             * @param {inference.InferenceRx.IEntry} message Entry message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Entry.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.queue != null && Object.hasOwnProperty.call(message, "queue"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.queue);
                if (message.ptr != null && Object.hasOwnProperty.call(message, "ptr"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.ptr);
                if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                    writer.uint32(/* id 3, wireType 0 =*/24).int32(message.type);
                return writer;
            };

            /**
             * Encodes the specified Entry message, length delimited. Does not implicitly {@link inference.InferenceRx.Entry.verify|verify} messages.
             * @function encodeDelimited
             * @memberof inference.InferenceRx.Entry
             * @static
             * @param {inference.InferenceRx.IEntry} message Entry message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Entry.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes an Entry message from the specified reader or buffer.
             * @function decode
             * @memberof inference.InferenceRx.Entry
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {inference.InferenceRx.Entry} Entry
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Entry.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.inference.InferenceRx.Entry();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.queue = reader.string();
                            break;
                        }
                    case 2: {
                            message.ptr = reader.bytes();
                            break;
                        }
                    case 3: {
                            message.type = reader.int32();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes an Entry message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof inference.InferenceRx.Entry
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {inference.InferenceRx.Entry} Entry
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Entry.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an Entry message.
             * @function verify
             * @memberof inference.InferenceRx.Entry
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Entry.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.queue != null && message.hasOwnProperty("queue"))
                    if (!$util.isString(message.queue))
                        return "queue: string expected";
                if (message.ptr != null && message.hasOwnProperty("ptr"))
                    if (!(message.ptr && typeof message.ptr.length === "number" || $util.isString(message.ptr)))
                        return "ptr: buffer expected";
                if (message.type != null && message.hasOwnProperty("type"))
                    switch (message.type) {
                    default:
                        return "type: enum value expected";
                    case 0:
                    case 1:
                    case 2:
                    case 3:
                    case 10:
                    case 11:
                    case 12:
                    case 13:
                    case 14:
                    case 15:
                    case 16:
                    case 20:
                    case 21:
                    case 22:
                    case 23:
                    case 24:
                    case 30:
                    case 32:
                    case 40:
                    case 41:
                    case 42:
                    case 50:
                    case 51:
                    case 52:
                    case 53:
                    case 54:
                    case 55:
                    case 56:
                    case 57:
                    case 58:
                        break;
                    }
                return null;
            };

            /**
             * Creates an Entry message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof inference.InferenceRx.Entry
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {inference.InferenceRx.Entry} Entry
             */
            Entry.fromObject = function fromObject(object, long) {
                if (object instanceof $root.inference.InferenceRx.Entry)
                    return object;
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.inference.InferenceRx.Entry();
                if (object.queue != null)
                    message.queue = String(object.queue);
                if (object.ptr != null)
                    if (typeof object.ptr === "string")
                        $util.base64.decode(object.ptr, message.ptr = $util.newBuffer($util.base64.length(object.ptr)), 0);
                    else if (object.ptr.length >= 0)
                        message.ptr = object.ptr;
                switch (object.type) {
                default:
                    if (typeof object.type === "number") {
                        message.type = object.type;
                        break;
                    }
                    break;
                case "QDT_SYSTEM":
                case 0:
                    message.type = 0;
                    break;
                case "QDT_STATION_COMMANDS":
                case 1:
                    message.type = 1;
                    break;
                case "QDT_STATION_STARTUPS":
                case 2:
                    message.type = 2;
                    break;
                case "QDT_INFERENCE_TAGS_RX":
                case 3:
                    message.type = 3;
                    break;
                case "QDT_ST3215_SERIAL_TX":
                case 10:
                    message.type = 10;
                    break;
                case "QDT_ST3215_SERIAL_RX":
                case 11:
                    message.type = 11;
                    break;
                case "QDT_ST3215_META":
                case 12:
                    message.type = 12;
                    break;
                case "QDT_ST3215_INFERENCE":
                case 13:
                    message.type = 13;
                    break;
                case "QDT_VESC_TRAMPA_SERIAL_RX":
                case 14:
                    message.type = 14;
                    break;
                case "QDT_VESC_TRAMPA_SERIAL_TX":
                case 15:
                    message.type = 15;
                    break;
                case "QDT_VESC_TRAMPA_INFERENCE":
                case 16:
                    message.type = 16;
                    break;
                case "QDT_FFMPEG_VIDEO_STREAM_RX":
                case 20:
                    message.type = 20;
                    break;
                case "QDT_USB_VIDEO_FRAMES":
                case 21:
                    message.type = 21;
                    break;
                case "QDT_INFERENCE_FRAMES":
                case 22:
                    message.type = 22;
                    break;
                case "QDT_HIKMICRO_THERMAL":
                case 23:
                    message.type = 23;
                    break;
                case "QDT_USB_VIDEO_TX":
                case 24:
                    message.type = 24;
                    break;
                case "QDT_MOTOR_MIRRORING_MODES":
                case 30:
                    message.type = 30;
                    break;
                case "QDT_MOTOR_MIRRORING_RX":
                case 32:
                    message.type = 32;
                    break;
                case "QDT_YAHBOOM_DOGZILLA_LITE_SERIAL_TX":
                case 40:
                    message.type = 40;
                    break;
                case "QDT_YAHBOOM_DOGZILLA_LITE_SERIAL_RX":
                case 41:
                    message.type = 41;
                    break;
                case "QDT_YAHBOOM_DOGZILLA_LITE_INFERENCE":
                case 42:
                    message.type = 42;
                    break;
                case "QDT_ARDUINO_NICLA_SENSE_ENV_TX":
                case 50:
                    message.type = 50;
                    break;
                case "QDT_ARDUINO_NICLA_SENSE_ENV_RX":
                case 51:
                    message.type = 51;
                    break;
                case "QDT_INA226_RX":
                case 52:
                    message.type = 52;
                    break;
                case "QDT_AIRGRADIENT_OPEN_AIR_O_1PST_RX":
                case 53:
                    message.type = 53;
                    break;
                case "QDT_VICTRON_SMARTSOLAR_MPPT_RX":
                case 54:
                    message.type = 54;
                    break;
                case "QDT_PWM_OUTPUT_TX":
                case 55:
                    message.type = 55;
                    break;
                case "QDT_PWM_OUTPUT_RX":
                case 56:
                    message.type = 56;
                    break;
                case "QDT_DMESG_RX":
                case 57:
                    message.type = 57;
                    break;
                case "QDT_DFROBOT_RS485_RX":
                case 58:
                    message.type = 58;
                    break;
                }
                return message;
            };

            /**
             * Creates a plain object from an Entry message. Also converts values to other types if specified.
             * @function toObject
             * @memberof inference.InferenceRx.Entry
             * @static
             * @param {inference.InferenceRx.Entry} message Entry
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Entry.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.queue = "";
                    if (options.bytes === String)
                        object.ptr = "";
                    else {
                        object.ptr = [];
                        if (options.bytes !== Array)
                            object.ptr = $util.newBuffer(object.ptr);
                    }
                    object.type = options.enums === String ? "QDT_SYSTEM" : 0;
                }
                if (message.queue != null && message.hasOwnProperty("queue"))
                    object.queue = message.queue;
                if (message.ptr != null && message.hasOwnProperty("ptr"))
                    object.ptr = options.bytes === String ? $util.base64.encode(message.ptr, 0, message.ptr.length) : options.bytes === Array ? Array.prototype.slice.call(message.ptr) : message.ptr;
                if (message.type != null && message.hasOwnProperty("type"))
                    object.type = options.enums === String ? $root.drivers.QueueDataType[message.type] === undefined ? message.type : $root.drivers.QueueDataType[message.type] : message.type;
                return object;
            };

            /**
             * Converts this Entry to JSON.
             * @function toJSON
             * @memberof inference.InferenceRx.Entry
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Entry.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for Entry
             * @function getTypeUrl
             * @memberof inference.InferenceRx.Entry
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            Entry.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/inference.InferenceRx.Entry";
            };

            return Entry;
        })();

        return InferenceRx;
    })();

    return inference;
})();

export const startups = $root.startups = (() => {

    /**
     * Namespace startups.
     * @exports startups
     * @namespace
     */
    const startups = {};

    startups.StationStartup = (function() {

        /**
         * Properties of a StationStartup.
         * @memberof startups
         * @interface IStationStartup
         * @property {Long|null} [monotonicStampNs] StationStartup monotonicStampNs
         * @property {Long|null} [localStampNs] StationStartup localStampNs
         * @property {Long|null} [appStartId] StationStartup appStartId
         * @property {Uint8Array|null} [stationUuid] StationStartup stationUuid
         * @property {string|null} [version] StationStartup version
         * @property {string|null} [gitHash] StationStartup gitHash
         * @property {Uint8Array|null} [inferenceQueuePtr] StationStartup inferenceQueuePtr
         */

        /**
         * Constructs a new StationStartup.
         * @memberof startups
         * @classdesc Represents a StationStartup.
         * @implements IStationStartup
         * @constructor
         * @param {startups.IStationStartup=} [properties] Properties to set
         */
        function StationStartup(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * StationStartup monotonicStampNs.
         * @member {Long} monotonicStampNs
         * @memberof startups.StationStartup
         * @instance
         */
        StationStartup.prototype.monotonicStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * StationStartup localStampNs.
         * @member {Long} localStampNs
         * @memberof startups.StationStartup
         * @instance
         */
        StationStartup.prototype.localStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * StationStartup appStartId.
         * @member {Long} appStartId
         * @memberof startups.StationStartup
         * @instance
         */
        StationStartup.prototype.appStartId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * StationStartup stationUuid.
         * @member {Uint8Array} stationUuid
         * @memberof startups.StationStartup
         * @instance
         */
        StationStartup.prototype.stationUuid = $util.newBuffer([]);

        /**
         * StationStartup version.
         * @member {string} version
         * @memberof startups.StationStartup
         * @instance
         */
        StationStartup.prototype.version = "";

        /**
         * StationStartup gitHash.
         * @member {string} gitHash
         * @memberof startups.StationStartup
         * @instance
         */
        StationStartup.prototype.gitHash = "";

        /**
         * StationStartup inferenceQueuePtr.
         * @member {Uint8Array} inferenceQueuePtr
         * @memberof startups.StationStartup
         * @instance
         */
        StationStartup.prototype.inferenceQueuePtr = $util.newBuffer([]);

        /**
         * Creates a new StationStartup instance using the specified properties.
         * @function create
         * @memberof startups.StationStartup
         * @static
         * @param {startups.IStationStartup=} [properties] Properties to set
         * @returns {startups.StationStartup} StationStartup instance
         */
        StationStartup.create = function create(properties) {
            return new StationStartup(properties);
        };

        /**
         * Encodes the specified StationStartup message. Does not implicitly {@link startups.StationStartup.verify|verify} messages.
         * @function encode
         * @memberof startups.StationStartup
         * @static
         * @param {startups.IStationStartup} message StationStartup message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StationStartup.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.monotonicStampNs != null && Object.hasOwnProperty.call(message, "monotonicStampNs"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.monotonicStampNs);
            if (message.localStampNs != null && Object.hasOwnProperty.call(message, "localStampNs"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.localStampNs);
            if (message.appStartId != null && Object.hasOwnProperty.call(message, "appStartId"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.appStartId);
            if (message.stationUuid != null && Object.hasOwnProperty.call(message, "stationUuid"))
                writer.uint32(/* id 10, wireType 2 =*/82).bytes(message.stationUuid);
            if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                writer.uint32(/* id 11, wireType 2 =*/90).string(message.version);
            if (message.gitHash != null && Object.hasOwnProperty.call(message, "gitHash"))
                writer.uint32(/* id 12, wireType 2 =*/98).string(message.gitHash);
            if (message.inferenceQueuePtr != null && Object.hasOwnProperty.call(message, "inferenceQueuePtr"))
                writer.uint32(/* id 20, wireType 2 =*/162).bytes(message.inferenceQueuePtr);
            return writer;
        };

        /**
         * Encodes the specified StationStartup message, length delimited. Does not implicitly {@link startups.StationStartup.verify|verify} messages.
         * @function encodeDelimited
         * @memberof startups.StationStartup
         * @static
         * @param {startups.IStationStartup} message StationStartup message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StationStartup.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a StationStartup message from the specified reader or buffer.
         * @function decode
         * @memberof startups.StationStartup
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {startups.StationStartup} StationStartup
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StationStartup.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.startups.StationStartup();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.monotonicStampNs = reader.uint64();
                        break;
                    }
                case 2: {
                        message.localStampNs = reader.uint64();
                        break;
                    }
                case 3: {
                        message.appStartId = reader.uint64();
                        break;
                    }
                case 10: {
                        message.stationUuid = reader.bytes();
                        break;
                    }
                case 11: {
                        message.version = reader.string();
                        break;
                    }
                case 12: {
                        message.gitHash = reader.string();
                        break;
                    }
                case 20: {
                        message.inferenceQueuePtr = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a StationStartup message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof startups.StationStartup
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {startups.StationStartup} StationStartup
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StationStartup.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a StationStartup message.
         * @function verify
         * @memberof startups.StationStartup
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        StationStartup.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (!$util.isInteger(message.monotonicStampNs) && !(message.monotonicStampNs && $util.isInteger(message.monotonicStampNs.low) && $util.isInteger(message.monotonicStampNs.high)))
                    return "monotonicStampNs: integer|Long expected";
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (!$util.isInteger(message.localStampNs) && !(message.localStampNs && $util.isInteger(message.localStampNs.low) && $util.isInteger(message.localStampNs.high)))
                    return "localStampNs: integer|Long expected";
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (!$util.isInteger(message.appStartId) && !(message.appStartId && $util.isInteger(message.appStartId.low) && $util.isInteger(message.appStartId.high)))
                    return "appStartId: integer|Long expected";
            if (message.stationUuid != null && message.hasOwnProperty("stationUuid"))
                if (!(message.stationUuid && typeof message.stationUuid.length === "number" || $util.isString(message.stationUuid)))
                    return "stationUuid: buffer expected";
            if (message.version != null && message.hasOwnProperty("version"))
                if (!$util.isString(message.version))
                    return "version: string expected";
            if (message.gitHash != null && message.hasOwnProperty("gitHash"))
                if (!$util.isString(message.gitHash))
                    return "gitHash: string expected";
            if (message.inferenceQueuePtr != null && message.hasOwnProperty("inferenceQueuePtr"))
                if (!(message.inferenceQueuePtr && typeof message.inferenceQueuePtr.length === "number" || $util.isString(message.inferenceQueuePtr)))
                    return "inferenceQueuePtr: buffer expected";
            return null;
        };

        /**
         * Creates a StationStartup message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof startups.StationStartup
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {startups.StationStartup} StationStartup
         */
        StationStartup.fromObject = function fromObject(object, long) {
            if (object instanceof $root.startups.StationStartup)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.startups.StationStartup();
            if (object.monotonicStampNs != null)
                if ($util.Long)
                    (message.monotonicStampNs = $util.Long.fromValue(object.monotonicStampNs)).unsigned = true;
                else if (typeof object.monotonicStampNs === "string")
                    message.monotonicStampNs = parseInt(object.monotonicStampNs, 10);
                else if (typeof object.monotonicStampNs === "number")
                    message.monotonicStampNs = object.monotonicStampNs;
                else if (typeof object.monotonicStampNs === "object")
                    message.monotonicStampNs = new $util.LongBits(object.monotonicStampNs.low >>> 0, object.monotonicStampNs.high >>> 0).toNumber(true);
            if (object.localStampNs != null)
                if ($util.Long)
                    (message.localStampNs = $util.Long.fromValue(object.localStampNs)).unsigned = true;
                else if (typeof object.localStampNs === "string")
                    message.localStampNs = parseInt(object.localStampNs, 10);
                else if (typeof object.localStampNs === "number")
                    message.localStampNs = object.localStampNs;
                else if (typeof object.localStampNs === "object")
                    message.localStampNs = new $util.LongBits(object.localStampNs.low >>> 0, object.localStampNs.high >>> 0).toNumber(true);
            if (object.appStartId != null)
                if ($util.Long)
                    (message.appStartId = $util.Long.fromValue(object.appStartId)).unsigned = true;
                else if (typeof object.appStartId === "string")
                    message.appStartId = parseInt(object.appStartId, 10);
                else if (typeof object.appStartId === "number")
                    message.appStartId = object.appStartId;
                else if (typeof object.appStartId === "object")
                    message.appStartId = new $util.LongBits(object.appStartId.low >>> 0, object.appStartId.high >>> 0).toNumber(true);
            if (object.stationUuid != null)
                if (typeof object.stationUuid === "string")
                    $util.base64.decode(object.stationUuid, message.stationUuid = $util.newBuffer($util.base64.length(object.stationUuid)), 0);
                else if (object.stationUuid.length >= 0)
                    message.stationUuid = object.stationUuid;
            if (object.version != null)
                message.version = String(object.version);
            if (object.gitHash != null)
                message.gitHash = String(object.gitHash);
            if (object.inferenceQueuePtr != null)
                if (typeof object.inferenceQueuePtr === "string")
                    $util.base64.decode(object.inferenceQueuePtr, message.inferenceQueuePtr = $util.newBuffer($util.base64.length(object.inferenceQueuePtr)), 0);
                else if (object.inferenceQueuePtr.length >= 0)
                    message.inferenceQueuePtr = object.inferenceQueuePtr;
            return message;
        };

        /**
         * Creates a plain object from a StationStartup message. Also converts values to other types if specified.
         * @function toObject
         * @memberof startups.StationStartup
         * @static
         * @param {startups.StationStartup} message StationStartup
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        StationStartup.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.monotonicStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.monotonicStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.localStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.localStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.appStartId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.appStartId = options.longs === String ? "0" : 0;
                if (options.bytes === String)
                    object.stationUuid = "";
                else {
                    object.stationUuid = [];
                    if (options.bytes !== Array)
                        object.stationUuid = $util.newBuffer(object.stationUuid);
                }
                object.version = "";
                object.gitHash = "";
                if (options.bytes === String)
                    object.inferenceQueuePtr = "";
                else {
                    object.inferenceQueuePtr = [];
                    if (options.bytes !== Array)
                        object.inferenceQueuePtr = $util.newBuffer(object.inferenceQueuePtr);
                }
            }
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (typeof message.monotonicStampNs === "number")
                    object.monotonicStampNs = options.longs === String ? String(message.monotonicStampNs) : message.monotonicStampNs;
                else
                    object.monotonicStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.monotonicStampNs) : options.longs === Number ? new $util.LongBits(message.monotonicStampNs.low >>> 0, message.monotonicStampNs.high >>> 0).toNumber(true) : message.monotonicStampNs;
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (typeof message.localStampNs === "number")
                    object.localStampNs = options.longs === String ? String(message.localStampNs) : message.localStampNs;
                else
                    object.localStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.localStampNs) : options.longs === Number ? new $util.LongBits(message.localStampNs.low >>> 0, message.localStampNs.high >>> 0).toNumber(true) : message.localStampNs;
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (typeof message.appStartId === "number")
                    object.appStartId = options.longs === String ? String(message.appStartId) : message.appStartId;
                else
                    object.appStartId = options.longs === String ? $util.Long.prototype.toString.call(message.appStartId) : options.longs === Number ? new $util.LongBits(message.appStartId.low >>> 0, message.appStartId.high >>> 0).toNumber(true) : message.appStartId;
            if (message.stationUuid != null && message.hasOwnProperty("stationUuid"))
                object.stationUuid = options.bytes === String ? $util.base64.encode(message.stationUuid, 0, message.stationUuid.length) : options.bytes === Array ? Array.prototype.slice.call(message.stationUuid) : message.stationUuid;
            if (message.version != null && message.hasOwnProperty("version"))
                object.version = message.version;
            if (message.gitHash != null && message.hasOwnProperty("gitHash"))
                object.gitHash = message.gitHash;
            if (message.inferenceQueuePtr != null && message.hasOwnProperty("inferenceQueuePtr"))
                object.inferenceQueuePtr = options.bytes === String ? $util.base64.encode(message.inferenceQueuePtr, 0, message.inferenceQueuePtr.length) : options.bytes === Array ? Array.prototype.slice.call(message.inferenceQueuePtr) : message.inferenceQueuePtr;
            return object;
        };

        /**
         * Converts this StationStartup to JSON.
         * @function toJSON
         * @memberof startups.StationStartup
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        StationStartup.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for StationStartup
         * @function getTypeUrl
         * @memberof startups.StationStartup
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        StationStartup.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/startups.StationStartup";
        };

        return StationStartup;
    })();

    return startups;
})();

export const inference_tags = $root.inference_tags = (() => {

    /**
     * Namespace inference_tags.
     * @exports inference_tags
     * @namespace
     */
    const inference_tags = {};

    /**
     * CommandType enum.
     * @name inference_tags.CommandType
     * @enum {number}
     * @property {number} CT_ADD_TAG=0 CT_ADD_TAG value
     * @property {number} CT_REMOVE_TAG=1 CT_REMOVE_TAG value
     */
    inference_tags.CommandType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "CT_ADD_TAG"] = 0;
        values[valuesById[1] = "CT_REMOVE_TAG"] = 1;
        return values;
    })();

    inference_tags.RxEnvelope = (function() {

        /**
         * Properties of a RxEnvelope.
         * @memberof inference_tags
         * @interface IRxEnvelope
         * @property {Long|null} [monotonicStampNs] RxEnvelope monotonicStampNs
         * @property {Long|null} [localStampNs] RxEnvelope localStampNs
         * @property {Long|null} [appStartId] RxEnvelope appStartId
         * @property {inference_tags.CommandType|null} [type] RxEnvelope type
         * @property {Uint8Array|null} [inferenceQueuePtr] RxEnvelope inferenceQueuePtr
         * @property {string|null} [tag] RxEnvelope tag
         */

        /**
         * Constructs a new RxEnvelope.
         * @memberof inference_tags
         * @classdesc Represents a RxEnvelope.
         * @implements IRxEnvelope
         * @constructor
         * @param {inference_tags.IRxEnvelope=} [properties] Properties to set
         */
        function RxEnvelope(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RxEnvelope monotonicStampNs.
         * @member {Long} monotonicStampNs
         * @memberof inference_tags.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.monotonicStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * RxEnvelope localStampNs.
         * @member {Long} localStampNs
         * @memberof inference_tags.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.localStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * RxEnvelope appStartId.
         * @member {Long} appStartId
         * @memberof inference_tags.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.appStartId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * RxEnvelope type.
         * @member {inference_tags.CommandType} type
         * @memberof inference_tags.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.type = 0;

        /**
         * RxEnvelope inferenceQueuePtr.
         * @member {Uint8Array} inferenceQueuePtr
         * @memberof inference_tags.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.inferenceQueuePtr = $util.newBuffer([]);

        /**
         * RxEnvelope tag.
         * @member {string} tag
         * @memberof inference_tags.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.tag = "";

        /**
         * Creates a new RxEnvelope instance using the specified properties.
         * @function create
         * @memberof inference_tags.RxEnvelope
         * @static
         * @param {inference_tags.IRxEnvelope=} [properties] Properties to set
         * @returns {inference_tags.RxEnvelope} RxEnvelope instance
         */
        RxEnvelope.create = function create(properties) {
            return new RxEnvelope(properties);
        };

        /**
         * Encodes the specified RxEnvelope message. Does not implicitly {@link inference_tags.RxEnvelope.verify|verify} messages.
         * @function encode
         * @memberof inference_tags.RxEnvelope
         * @static
         * @param {inference_tags.IRxEnvelope} message RxEnvelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RxEnvelope.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.monotonicStampNs != null && Object.hasOwnProperty.call(message, "monotonicStampNs"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.monotonicStampNs);
            if (message.localStampNs != null && Object.hasOwnProperty.call(message, "localStampNs"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.localStampNs);
            if (message.appStartId != null && Object.hasOwnProperty.call(message, "appStartId"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.appStartId);
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.type);
            if (message.inferenceQueuePtr != null && Object.hasOwnProperty.call(message, "inferenceQueuePtr"))
                writer.uint32(/* id 11, wireType 2 =*/90).bytes(message.inferenceQueuePtr);
            if (message.tag != null && Object.hasOwnProperty.call(message, "tag"))
                writer.uint32(/* id 12, wireType 2 =*/98).string(message.tag);
            return writer;
        };

        /**
         * Encodes the specified RxEnvelope message, length delimited. Does not implicitly {@link inference_tags.RxEnvelope.verify|verify} messages.
         * @function encodeDelimited
         * @memberof inference_tags.RxEnvelope
         * @static
         * @param {inference_tags.IRxEnvelope} message RxEnvelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RxEnvelope.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RxEnvelope message from the specified reader or buffer.
         * @function decode
         * @memberof inference_tags.RxEnvelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {inference_tags.RxEnvelope} RxEnvelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RxEnvelope.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.inference_tags.RxEnvelope();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.monotonicStampNs = reader.uint64();
                        break;
                    }
                case 2: {
                        message.localStampNs = reader.uint64();
                        break;
                    }
                case 3: {
                        message.appStartId = reader.uint64();
                        break;
                    }
                case 10: {
                        message.type = reader.int32();
                        break;
                    }
                case 11: {
                        message.inferenceQueuePtr = reader.bytes();
                        break;
                    }
                case 12: {
                        message.tag = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RxEnvelope message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof inference_tags.RxEnvelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {inference_tags.RxEnvelope} RxEnvelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RxEnvelope.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RxEnvelope message.
         * @function verify
         * @memberof inference_tags.RxEnvelope
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RxEnvelope.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (!$util.isInteger(message.monotonicStampNs) && !(message.monotonicStampNs && $util.isInteger(message.monotonicStampNs.low) && $util.isInteger(message.monotonicStampNs.high)))
                    return "monotonicStampNs: integer|Long expected";
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (!$util.isInteger(message.localStampNs) && !(message.localStampNs && $util.isInteger(message.localStampNs.low) && $util.isInteger(message.localStampNs.high)))
                    return "localStampNs: integer|Long expected";
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (!$util.isInteger(message.appStartId) && !(message.appStartId && $util.isInteger(message.appStartId.low) && $util.isInteger(message.appStartId.high)))
                    return "appStartId: integer|Long expected";
            if (message.type != null && message.hasOwnProperty("type"))
                switch (message.type) {
                default:
                    return "type: enum value expected";
                case 0:
                case 1:
                    break;
                }
            if (message.inferenceQueuePtr != null && message.hasOwnProperty("inferenceQueuePtr"))
                if (!(message.inferenceQueuePtr && typeof message.inferenceQueuePtr.length === "number" || $util.isString(message.inferenceQueuePtr)))
                    return "inferenceQueuePtr: buffer expected";
            if (message.tag != null && message.hasOwnProperty("tag"))
                if (!$util.isString(message.tag))
                    return "tag: string expected";
            return null;
        };

        /**
         * Creates a RxEnvelope message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof inference_tags.RxEnvelope
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {inference_tags.RxEnvelope} RxEnvelope
         */
        RxEnvelope.fromObject = function fromObject(object, long) {
            if (object instanceof $root.inference_tags.RxEnvelope)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.inference_tags.RxEnvelope();
            if (object.monotonicStampNs != null)
                if ($util.Long)
                    (message.monotonicStampNs = $util.Long.fromValue(object.monotonicStampNs)).unsigned = true;
                else if (typeof object.monotonicStampNs === "string")
                    message.monotonicStampNs = parseInt(object.monotonicStampNs, 10);
                else if (typeof object.monotonicStampNs === "number")
                    message.monotonicStampNs = object.monotonicStampNs;
                else if (typeof object.monotonicStampNs === "object")
                    message.monotonicStampNs = new $util.LongBits(object.monotonicStampNs.low >>> 0, object.monotonicStampNs.high >>> 0).toNumber(true);
            if (object.localStampNs != null)
                if ($util.Long)
                    (message.localStampNs = $util.Long.fromValue(object.localStampNs)).unsigned = true;
                else if (typeof object.localStampNs === "string")
                    message.localStampNs = parseInt(object.localStampNs, 10);
                else if (typeof object.localStampNs === "number")
                    message.localStampNs = object.localStampNs;
                else if (typeof object.localStampNs === "object")
                    message.localStampNs = new $util.LongBits(object.localStampNs.low >>> 0, object.localStampNs.high >>> 0).toNumber(true);
            if (object.appStartId != null)
                if ($util.Long)
                    (message.appStartId = $util.Long.fromValue(object.appStartId)).unsigned = true;
                else if (typeof object.appStartId === "string")
                    message.appStartId = parseInt(object.appStartId, 10);
                else if (typeof object.appStartId === "number")
                    message.appStartId = object.appStartId;
                else if (typeof object.appStartId === "object")
                    message.appStartId = new $util.LongBits(object.appStartId.low >>> 0, object.appStartId.high >>> 0).toNumber(true);
            switch (object.type) {
            default:
                if (typeof object.type === "number") {
                    message.type = object.type;
                    break;
                }
                break;
            case "CT_ADD_TAG":
            case 0:
                message.type = 0;
                break;
            case "CT_REMOVE_TAG":
            case 1:
                message.type = 1;
                break;
            }
            if (object.inferenceQueuePtr != null)
                if (typeof object.inferenceQueuePtr === "string")
                    $util.base64.decode(object.inferenceQueuePtr, message.inferenceQueuePtr = $util.newBuffer($util.base64.length(object.inferenceQueuePtr)), 0);
                else if (object.inferenceQueuePtr.length >= 0)
                    message.inferenceQueuePtr = object.inferenceQueuePtr;
            if (object.tag != null)
                message.tag = String(object.tag);
            return message;
        };

        /**
         * Creates a plain object from a RxEnvelope message. Also converts values to other types if specified.
         * @function toObject
         * @memberof inference_tags.RxEnvelope
         * @static
         * @param {inference_tags.RxEnvelope} message RxEnvelope
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RxEnvelope.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.monotonicStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.monotonicStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.localStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.localStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.appStartId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.appStartId = options.longs === String ? "0" : 0;
                object.type = options.enums === String ? "CT_ADD_TAG" : 0;
                if (options.bytes === String)
                    object.inferenceQueuePtr = "";
                else {
                    object.inferenceQueuePtr = [];
                    if (options.bytes !== Array)
                        object.inferenceQueuePtr = $util.newBuffer(object.inferenceQueuePtr);
                }
                object.tag = "";
            }
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (typeof message.monotonicStampNs === "number")
                    object.monotonicStampNs = options.longs === String ? String(message.monotonicStampNs) : message.monotonicStampNs;
                else
                    object.monotonicStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.monotonicStampNs) : options.longs === Number ? new $util.LongBits(message.monotonicStampNs.low >>> 0, message.monotonicStampNs.high >>> 0).toNumber(true) : message.monotonicStampNs;
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (typeof message.localStampNs === "number")
                    object.localStampNs = options.longs === String ? String(message.localStampNs) : message.localStampNs;
                else
                    object.localStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.localStampNs) : options.longs === Number ? new $util.LongBits(message.localStampNs.low >>> 0, message.localStampNs.high >>> 0).toNumber(true) : message.localStampNs;
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (typeof message.appStartId === "number")
                    object.appStartId = options.longs === String ? String(message.appStartId) : message.appStartId;
                else
                    object.appStartId = options.longs === String ? $util.Long.prototype.toString.call(message.appStartId) : options.longs === Number ? new $util.LongBits(message.appStartId.low >>> 0, message.appStartId.high >>> 0).toNumber(true) : message.appStartId;
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = options.enums === String ? $root.inference_tags.CommandType[message.type] === undefined ? message.type : $root.inference_tags.CommandType[message.type] : message.type;
            if (message.inferenceQueuePtr != null && message.hasOwnProperty("inferenceQueuePtr"))
                object.inferenceQueuePtr = options.bytes === String ? $util.base64.encode(message.inferenceQueuePtr, 0, message.inferenceQueuePtr.length) : options.bytes === Array ? Array.prototype.slice.call(message.inferenceQueuePtr) : message.inferenceQueuePtr;
            if (message.tag != null && message.hasOwnProperty("tag"))
                object.tag = message.tag;
            return object;
        };

        /**
         * Converts this RxEnvelope to JSON.
         * @function toJSON
         * @memberof inference_tags.RxEnvelope
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RxEnvelope.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RxEnvelope
         * @function getTypeUrl
         * @memberof inference_tags.RxEnvelope
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RxEnvelope.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/inference_tags.RxEnvelope";
        };

        return RxEnvelope;
    })();

    inference_tags.Command = (function() {

        /**
         * Properties of a Command.
         * @memberof inference_tags
         * @interface ICommand
         * @property {inference_tags.CommandType|null} [type] Command type
         * @property {Uint8Array|null} [inferenceQueuePtr] Command inferenceQueuePtr
         * @property {string|null} [tag] Command tag
         */

        /**
         * Constructs a new Command.
         * @memberof inference_tags
         * @classdesc Represents a Command.
         * @implements ICommand
         * @constructor
         * @param {inference_tags.ICommand=} [properties] Properties to set
         */
        function Command(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Command type.
         * @member {inference_tags.CommandType} type
         * @memberof inference_tags.Command
         * @instance
         */
        Command.prototype.type = 0;

        /**
         * Command inferenceQueuePtr.
         * @member {Uint8Array} inferenceQueuePtr
         * @memberof inference_tags.Command
         * @instance
         */
        Command.prototype.inferenceQueuePtr = $util.newBuffer([]);

        /**
         * Command tag.
         * @member {string} tag
         * @memberof inference_tags.Command
         * @instance
         */
        Command.prototype.tag = "";

        /**
         * Creates a new Command instance using the specified properties.
         * @function create
         * @memberof inference_tags.Command
         * @static
         * @param {inference_tags.ICommand=} [properties] Properties to set
         * @returns {inference_tags.Command} Command instance
         */
        Command.create = function create(properties) {
            return new Command(properties);
        };

        /**
         * Encodes the specified Command message. Does not implicitly {@link inference_tags.Command.verify|verify} messages.
         * @function encode
         * @memberof inference_tags.Command
         * @static
         * @param {inference_tags.ICommand} message Command message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Command.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.type);
            if (message.inferenceQueuePtr != null && Object.hasOwnProperty.call(message, "inferenceQueuePtr"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.inferenceQueuePtr);
            if (message.tag != null && Object.hasOwnProperty.call(message, "tag"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.tag);
            return writer;
        };

        /**
         * Encodes the specified Command message, length delimited. Does not implicitly {@link inference_tags.Command.verify|verify} messages.
         * @function encodeDelimited
         * @memberof inference_tags.Command
         * @static
         * @param {inference_tags.ICommand} message Command message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Command.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Command message from the specified reader or buffer.
         * @function decode
         * @memberof inference_tags.Command
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {inference_tags.Command} Command
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Command.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.inference_tags.Command();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.type = reader.int32();
                        break;
                    }
                case 2: {
                        message.inferenceQueuePtr = reader.bytes();
                        break;
                    }
                case 3: {
                        message.tag = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Command message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof inference_tags.Command
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {inference_tags.Command} Command
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Command.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Command message.
         * @function verify
         * @memberof inference_tags.Command
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Command.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.type != null && message.hasOwnProperty("type"))
                switch (message.type) {
                default:
                    return "type: enum value expected";
                case 0:
                case 1:
                    break;
                }
            if (message.inferenceQueuePtr != null && message.hasOwnProperty("inferenceQueuePtr"))
                if (!(message.inferenceQueuePtr && typeof message.inferenceQueuePtr.length === "number" || $util.isString(message.inferenceQueuePtr)))
                    return "inferenceQueuePtr: buffer expected";
            if (message.tag != null && message.hasOwnProperty("tag"))
                if (!$util.isString(message.tag))
                    return "tag: string expected";
            return null;
        };

        /**
         * Creates a Command message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof inference_tags.Command
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {inference_tags.Command} Command
         */
        Command.fromObject = function fromObject(object, long) {
            if (object instanceof $root.inference_tags.Command)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.inference_tags.Command();
            switch (object.type) {
            default:
                if (typeof object.type === "number") {
                    message.type = object.type;
                    break;
                }
                break;
            case "CT_ADD_TAG":
            case 0:
                message.type = 0;
                break;
            case "CT_REMOVE_TAG":
            case 1:
                message.type = 1;
                break;
            }
            if (object.inferenceQueuePtr != null)
                if (typeof object.inferenceQueuePtr === "string")
                    $util.base64.decode(object.inferenceQueuePtr, message.inferenceQueuePtr = $util.newBuffer($util.base64.length(object.inferenceQueuePtr)), 0);
                else if (object.inferenceQueuePtr.length >= 0)
                    message.inferenceQueuePtr = object.inferenceQueuePtr;
            if (object.tag != null)
                message.tag = String(object.tag);
            return message;
        };

        /**
         * Creates a plain object from a Command message. Also converts values to other types if specified.
         * @function toObject
         * @memberof inference_tags.Command
         * @static
         * @param {inference_tags.Command} message Command
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Command.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.type = options.enums === String ? "CT_ADD_TAG" : 0;
                if (options.bytes === String)
                    object.inferenceQueuePtr = "";
                else {
                    object.inferenceQueuePtr = [];
                    if (options.bytes !== Array)
                        object.inferenceQueuePtr = $util.newBuffer(object.inferenceQueuePtr);
                }
                object.tag = "";
            }
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = options.enums === String ? $root.inference_tags.CommandType[message.type] === undefined ? message.type : $root.inference_tags.CommandType[message.type] : message.type;
            if (message.inferenceQueuePtr != null && message.hasOwnProperty("inferenceQueuePtr"))
                object.inferenceQueuePtr = options.bytes === String ? $util.base64.encode(message.inferenceQueuePtr, 0, message.inferenceQueuePtr.length) : options.bytes === Array ? Array.prototype.slice.call(message.inferenceQueuePtr) : message.inferenceQueuePtr;
            if (message.tag != null && message.hasOwnProperty("tag"))
                object.tag = message.tag;
            return object;
        };

        /**
         * Converts this Command to JSON.
         * @function toJSON
         * @memberof inference_tags.Command
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Command.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Command
         * @function getTypeUrl
         * @memberof inference_tags.Command
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Command.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/inference_tags.Command";
        };

        return Command;
    })();

    return inference_tags;
})();

export const st3215 = $root.st3215 = (() => {

    /**
     * Namespace st3215.
     * @exports st3215
     * @namespace
     */
    const st3215 = {};

    /**
     * ST3215SignalType enum.
     * @name st3215.ST3215SignalType
     * @enum {number}
     * @property {number} ST3215_SIGNAL_TYPE_UNSPECIFIED=0 ST3215_SIGNAL_TYPE_UNSPECIFIED value
     * @property {number} ST3215_BUS_CONNECT=1 ST3215_BUS_CONNECT value
     * @property {number} ST3215_BUS_DISCONNECT=2 ST3215_BUS_DISCONNECT value
     * @property {number} ST3215_DRIVE_CONNECT=3 ST3215_DRIVE_CONNECT value
     * @property {number} ST3215_DRIVE_DISCONNECT=4 ST3215_DRIVE_DISCONNECT value
     * @property {number} ST3215_DRIVE_STATE=6 ST3215_DRIVE_STATE value
     * @property {number} ST3215_COMMAND=7 ST3215_COMMAND value
     * @property {number} ST3215_COMMAND_SUCCESS=8 ST3215_COMMAND_SUCCESS value
     * @property {number} ST3215_ERROR=9 ST3215_ERROR value
     * @property {number} ST3215_COMMAND_REJECTED=11 ST3215_COMMAND_REJECTED value
     * @property {number} ST3215_COMMAND_FAILED=12 ST3215_COMMAND_FAILED value
     */
    st3215.ST3215SignalType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "ST3215_SIGNAL_TYPE_UNSPECIFIED"] = 0;
        values[valuesById[1] = "ST3215_BUS_CONNECT"] = 1;
        values[valuesById[2] = "ST3215_BUS_DISCONNECT"] = 2;
        values[valuesById[3] = "ST3215_DRIVE_CONNECT"] = 3;
        values[valuesById[4] = "ST3215_DRIVE_DISCONNECT"] = 4;
        values[valuesById[6] = "ST3215_DRIVE_STATE"] = 6;
        values[valuesById[7] = "ST3215_COMMAND"] = 7;
        values[valuesById[8] = "ST3215_COMMAND_SUCCESS"] = 8;
        values[valuesById[9] = "ST3215_ERROR"] = 9;
        values[valuesById[11] = "ST3215_COMMAND_REJECTED"] = 11;
        values[valuesById[12] = "ST3215_COMMAND_FAILED"] = 12;
        return values;
    })();

    st3215.RxEnvelope = (function() {

        /**
         * Properties of a RxEnvelope.
         * @memberof st3215
         * @interface IRxEnvelope
         * @property {Long|null} [monotonicStampNs] RxEnvelope monotonicStampNs
         * @property {Long|null} [localStampNs] RxEnvelope localStampNs
         * @property {Long|null} [appStartId] RxEnvelope appStartId
         * @property {st3215.ST3215SignalType|null} [signalType] RxEnvelope signalType
         * @property {st3215.IST3215Bus|null} [bus] RxEnvelope bus
         * @property {number|null} [motorId] RxEnvelope motorId
         * @property {Uint8Array|null} [data] RxEnvelope data
         * @property {st3215.ITxEnvelope|null} [command] RxEnvelope command
         * @property {st3215.IST3215Error|null} [error] RxEnvelope error
         */

        /**
         * Constructs a new RxEnvelope.
         * @memberof st3215
         * @classdesc Represents a RxEnvelope.
         * @implements IRxEnvelope
         * @constructor
         * @param {st3215.IRxEnvelope=} [properties] Properties to set
         */
        function RxEnvelope(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RxEnvelope monotonicStampNs.
         * @member {Long} monotonicStampNs
         * @memberof st3215.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.monotonicStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * RxEnvelope localStampNs.
         * @member {Long} localStampNs
         * @memberof st3215.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.localStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * RxEnvelope appStartId.
         * @member {Long} appStartId
         * @memberof st3215.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.appStartId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * RxEnvelope signalType.
         * @member {st3215.ST3215SignalType} signalType
         * @memberof st3215.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.signalType = 0;

        /**
         * RxEnvelope bus.
         * @member {st3215.IST3215Bus|null|undefined} bus
         * @memberof st3215.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.bus = null;

        /**
         * RxEnvelope motorId.
         * @member {number} motorId
         * @memberof st3215.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.motorId = 0;

        /**
         * RxEnvelope data.
         * @member {Uint8Array} data
         * @memberof st3215.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.data = $util.newBuffer([]);

        /**
         * RxEnvelope command.
         * @member {st3215.ITxEnvelope|null|undefined} command
         * @memberof st3215.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.command = null;

        /**
         * RxEnvelope error.
         * @member {st3215.IST3215Error|null|undefined} error
         * @memberof st3215.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.error = null;

        /**
         * Creates a new RxEnvelope instance using the specified properties.
         * @function create
         * @memberof st3215.RxEnvelope
         * @static
         * @param {st3215.IRxEnvelope=} [properties] Properties to set
         * @returns {st3215.RxEnvelope} RxEnvelope instance
         */
        RxEnvelope.create = function create(properties) {
            return new RxEnvelope(properties);
        };

        /**
         * Encodes the specified RxEnvelope message. Does not implicitly {@link st3215.RxEnvelope.verify|verify} messages.
         * @function encode
         * @memberof st3215.RxEnvelope
         * @static
         * @param {st3215.IRxEnvelope} message RxEnvelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RxEnvelope.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.monotonicStampNs != null && Object.hasOwnProperty.call(message, "monotonicStampNs"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.monotonicStampNs);
            if (message.localStampNs != null && Object.hasOwnProperty.call(message, "localStampNs"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.localStampNs);
            if (message.appStartId != null && Object.hasOwnProperty.call(message, "appStartId"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.appStartId);
            if (message.signalType != null && Object.hasOwnProperty.call(message, "signalType"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.signalType);
            if (message.bus != null && Object.hasOwnProperty.call(message, "bus"))
                $root.st3215.ST3215Bus.encode(message.bus, writer.uint32(/* id 11, wireType 2 =*/90).fork()).ldelim();
            if (message.motorId != null && Object.hasOwnProperty.call(message, "motorId"))
                writer.uint32(/* id 12, wireType 0 =*/96).uint32(message.motorId);
            if (message.data != null && Object.hasOwnProperty.call(message, "data"))
                writer.uint32(/* id 20, wireType 2 =*/162).bytes(message.data);
            if (message.command != null && Object.hasOwnProperty.call(message, "command"))
                $root.st3215.TxEnvelope.encode(message.command, writer.uint32(/* id 30, wireType 2 =*/242).fork()).ldelim();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.st3215.ST3215Error.encode(message.error, writer.uint32(/* id 50, wireType 2 =*/402).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified RxEnvelope message, length delimited. Does not implicitly {@link st3215.RxEnvelope.verify|verify} messages.
         * @function encodeDelimited
         * @memberof st3215.RxEnvelope
         * @static
         * @param {st3215.IRxEnvelope} message RxEnvelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RxEnvelope.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RxEnvelope message from the specified reader or buffer.
         * @function decode
         * @memberof st3215.RxEnvelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {st3215.RxEnvelope} RxEnvelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RxEnvelope.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.st3215.RxEnvelope();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.monotonicStampNs = reader.uint64();
                        break;
                    }
                case 2: {
                        message.localStampNs = reader.uint64();
                        break;
                    }
                case 3: {
                        message.appStartId = reader.uint64();
                        break;
                    }
                case 10: {
                        message.signalType = reader.int32();
                        break;
                    }
                case 11: {
                        message.bus = $root.st3215.ST3215Bus.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 12: {
                        message.motorId = reader.uint32();
                        break;
                    }
                case 20: {
                        message.data = reader.bytes();
                        break;
                    }
                case 30: {
                        message.command = $root.st3215.TxEnvelope.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 50: {
                        message.error = $root.st3215.ST3215Error.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RxEnvelope message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof st3215.RxEnvelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {st3215.RxEnvelope} RxEnvelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RxEnvelope.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RxEnvelope message.
         * @function verify
         * @memberof st3215.RxEnvelope
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RxEnvelope.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (!$util.isInteger(message.monotonicStampNs) && !(message.monotonicStampNs && $util.isInteger(message.monotonicStampNs.low) && $util.isInteger(message.monotonicStampNs.high)))
                    return "monotonicStampNs: integer|Long expected";
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (!$util.isInteger(message.localStampNs) && !(message.localStampNs && $util.isInteger(message.localStampNs.low) && $util.isInteger(message.localStampNs.high)))
                    return "localStampNs: integer|Long expected";
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (!$util.isInteger(message.appStartId) && !(message.appStartId && $util.isInteger(message.appStartId.low) && $util.isInteger(message.appStartId.high)))
                    return "appStartId: integer|Long expected";
            if (message.signalType != null && message.hasOwnProperty("signalType"))
                switch (message.signalType) {
                default:
                    return "signalType: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                case 6:
                case 7:
                case 8:
                case 9:
                case 11:
                case 12:
                    break;
                }
            if (message.bus != null && message.hasOwnProperty("bus")) {
                let error = $root.st3215.ST3215Bus.verify(message.bus, long + 1);
                if (error)
                    return "bus." + error;
            }
            if (message.motorId != null && message.hasOwnProperty("motorId"))
                if (!$util.isInteger(message.motorId))
                    return "motorId: integer expected";
            if (message.data != null && message.hasOwnProperty("data"))
                if (!(message.data && typeof message.data.length === "number" || $util.isString(message.data)))
                    return "data: buffer expected";
            if (message.command != null && message.hasOwnProperty("command")) {
                let error = $root.st3215.TxEnvelope.verify(message.command, long + 1);
                if (error)
                    return "command." + error;
            }
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.st3215.ST3215Error.verify(message.error, long + 1);
                if (error)
                    return "error." + error;
            }
            return null;
        };

        /**
         * Creates a RxEnvelope message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof st3215.RxEnvelope
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {st3215.RxEnvelope} RxEnvelope
         */
        RxEnvelope.fromObject = function fromObject(object, long) {
            if (object instanceof $root.st3215.RxEnvelope)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.st3215.RxEnvelope();
            if (object.monotonicStampNs != null)
                if ($util.Long)
                    (message.monotonicStampNs = $util.Long.fromValue(object.monotonicStampNs)).unsigned = true;
                else if (typeof object.monotonicStampNs === "string")
                    message.monotonicStampNs = parseInt(object.monotonicStampNs, 10);
                else if (typeof object.monotonicStampNs === "number")
                    message.monotonicStampNs = object.monotonicStampNs;
                else if (typeof object.monotonicStampNs === "object")
                    message.monotonicStampNs = new $util.LongBits(object.monotonicStampNs.low >>> 0, object.monotonicStampNs.high >>> 0).toNumber(true);
            if (object.localStampNs != null)
                if ($util.Long)
                    (message.localStampNs = $util.Long.fromValue(object.localStampNs)).unsigned = true;
                else if (typeof object.localStampNs === "string")
                    message.localStampNs = parseInt(object.localStampNs, 10);
                else if (typeof object.localStampNs === "number")
                    message.localStampNs = object.localStampNs;
                else if (typeof object.localStampNs === "object")
                    message.localStampNs = new $util.LongBits(object.localStampNs.low >>> 0, object.localStampNs.high >>> 0).toNumber(true);
            if (object.appStartId != null)
                if ($util.Long)
                    (message.appStartId = $util.Long.fromValue(object.appStartId)).unsigned = true;
                else if (typeof object.appStartId === "string")
                    message.appStartId = parseInt(object.appStartId, 10);
                else if (typeof object.appStartId === "number")
                    message.appStartId = object.appStartId;
                else if (typeof object.appStartId === "object")
                    message.appStartId = new $util.LongBits(object.appStartId.low >>> 0, object.appStartId.high >>> 0).toNumber(true);
            switch (object.signalType) {
            default:
                if (typeof object.signalType === "number") {
                    message.signalType = object.signalType;
                    break;
                }
                break;
            case "ST3215_SIGNAL_TYPE_UNSPECIFIED":
            case 0:
                message.signalType = 0;
                break;
            case "ST3215_BUS_CONNECT":
            case 1:
                message.signalType = 1;
                break;
            case "ST3215_BUS_DISCONNECT":
            case 2:
                message.signalType = 2;
                break;
            case "ST3215_DRIVE_CONNECT":
            case 3:
                message.signalType = 3;
                break;
            case "ST3215_DRIVE_DISCONNECT":
            case 4:
                message.signalType = 4;
                break;
            case "ST3215_DRIVE_STATE":
            case 6:
                message.signalType = 6;
                break;
            case "ST3215_COMMAND":
            case 7:
                message.signalType = 7;
                break;
            case "ST3215_COMMAND_SUCCESS":
            case 8:
                message.signalType = 8;
                break;
            case "ST3215_ERROR":
            case 9:
                message.signalType = 9;
                break;
            case "ST3215_COMMAND_REJECTED":
            case 11:
                message.signalType = 11;
                break;
            case "ST3215_COMMAND_FAILED":
            case 12:
                message.signalType = 12;
                break;
            }
            if (object.bus != null) {
                if (typeof object.bus !== "object")
                    throw TypeError(".st3215.RxEnvelope.bus: object expected");
                message.bus = $root.st3215.ST3215Bus.fromObject(object.bus, long + 1);
            }
            if (object.motorId != null)
                message.motorId = object.motorId >>> 0;
            if (object.data != null)
                if (typeof object.data === "string")
                    $util.base64.decode(object.data, message.data = $util.newBuffer($util.base64.length(object.data)), 0);
                else if (object.data.length >= 0)
                    message.data = object.data;
            if (object.command != null) {
                if (typeof object.command !== "object")
                    throw TypeError(".st3215.RxEnvelope.command: object expected");
                message.command = $root.st3215.TxEnvelope.fromObject(object.command, long + 1);
            }
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".st3215.RxEnvelope.error: object expected");
                message.error = $root.st3215.ST3215Error.fromObject(object.error, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a RxEnvelope message. Also converts values to other types if specified.
         * @function toObject
         * @memberof st3215.RxEnvelope
         * @static
         * @param {st3215.RxEnvelope} message RxEnvelope
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RxEnvelope.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.monotonicStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.monotonicStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.localStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.localStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.appStartId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.appStartId = options.longs === String ? "0" : 0;
                object.signalType = options.enums === String ? "ST3215_SIGNAL_TYPE_UNSPECIFIED" : 0;
                object.bus = null;
                object.motorId = 0;
                if (options.bytes === String)
                    object.data = "";
                else {
                    object.data = [];
                    if (options.bytes !== Array)
                        object.data = $util.newBuffer(object.data);
                }
                object.command = null;
                object.error = null;
            }
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (typeof message.monotonicStampNs === "number")
                    object.monotonicStampNs = options.longs === String ? String(message.monotonicStampNs) : message.monotonicStampNs;
                else
                    object.monotonicStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.monotonicStampNs) : options.longs === Number ? new $util.LongBits(message.monotonicStampNs.low >>> 0, message.monotonicStampNs.high >>> 0).toNumber(true) : message.monotonicStampNs;
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (typeof message.localStampNs === "number")
                    object.localStampNs = options.longs === String ? String(message.localStampNs) : message.localStampNs;
                else
                    object.localStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.localStampNs) : options.longs === Number ? new $util.LongBits(message.localStampNs.low >>> 0, message.localStampNs.high >>> 0).toNumber(true) : message.localStampNs;
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (typeof message.appStartId === "number")
                    object.appStartId = options.longs === String ? String(message.appStartId) : message.appStartId;
                else
                    object.appStartId = options.longs === String ? $util.Long.prototype.toString.call(message.appStartId) : options.longs === Number ? new $util.LongBits(message.appStartId.low >>> 0, message.appStartId.high >>> 0).toNumber(true) : message.appStartId;
            if (message.signalType != null && message.hasOwnProperty("signalType"))
                object.signalType = options.enums === String ? $root.st3215.ST3215SignalType[message.signalType] === undefined ? message.signalType : $root.st3215.ST3215SignalType[message.signalType] : message.signalType;
            if (message.bus != null && message.hasOwnProperty("bus"))
                object.bus = $root.st3215.ST3215Bus.toObject(message.bus, options);
            if (message.motorId != null && message.hasOwnProperty("motorId"))
                object.motorId = message.motorId;
            if (message.data != null && message.hasOwnProperty("data"))
                object.data = options.bytes === String ? $util.base64.encode(message.data, 0, message.data.length) : options.bytes === Array ? Array.prototype.slice.call(message.data) : message.data;
            if (message.command != null && message.hasOwnProperty("command"))
                object.command = $root.st3215.TxEnvelope.toObject(message.command, options);
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.st3215.ST3215Error.toObject(message.error, options);
            return object;
        };

        /**
         * Converts this RxEnvelope to JSON.
         * @function toJSON
         * @memberof st3215.RxEnvelope
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RxEnvelope.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RxEnvelope
         * @function getTypeUrl
         * @memberof st3215.RxEnvelope
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RxEnvelope.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/st3215.RxEnvelope";
        };

        return RxEnvelope;
    })();

    st3215.TxEnvelope = (function() {

        /**
         * Properties of a TxEnvelope.
         * @memberof st3215
         * @interface ITxEnvelope
         * @property {Long|null} [monotonicStampNs] TxEnvelope monotonicStampNs
         * @property {Long|null} [localStampNs] TxEnvelope localStampNs
         * @property {Long|null} [appStartId] TxEnvelope appStartId
         * @property {string|null} [targetBusSerial] TxEnvelope targetBusSerial
         * @property {Uint8Array|null} [commandId] TxEnvelope commandId
         * @property {st3215.IST3215WriteCommand|null} [write] TxEnvelope write
         * @property {st3215.IST3215RegWriteCommand|null} [regWrite] TxEnvelope regWrite
         * @property {st3215.IST3215ActionCommand|null} [action] TxEnvelope action
         * @property {st3215.IST3215ResetCommand|null} [reset] TxEnvelope reset
         * @property {st3215.IResetCalibrationCommand|null} [resetCalibration] TxEnvelope resetCalibration
         * @property {st3215.IFreezeCalibrationCommand|null} [freezeCalibration] TxEnvelope freezeCalibration
         * @property {st3215.IST3215SyncWriteCommand|null} [syncWrite] TxEnvelope syncWrite
         * @property {st3215.IAutoCalibrateCommand|null} [autoCalibrate] TxEnvelope autoCalibrate
         * @property {st3215.IStopAutoCalibrateCommand|null} [stopAutoCalibrate] TxEnvelope stopAutoCalibrate
         */

        /**
         * Constructs a new TxEnvelope.
         * @memberof st3215
         * @classdesc Represents a TxEnvelope.
         * @implements ITxEnvelope
         * @constructor
         * @param {st3215.ITxEnvelope=} [properties] Properties to set
         */
        function TxEnvelope(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * TxEnvelope monotonicStampNs.
         * @member {Long} monotonicStampNs
         * @memberof st3215.TxEnvelope
         * @instance
         */
        TxEnvelope.prototype.monotonicStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * TxEnvelope localStampNs.
         * @member {Long} localStampNs
         * @memberof st3215.TxEnvelope
         * @instance
         */
        TxEnvelope.prototype.localStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * TxEnvelope appStartId.
         * @member {Long} appStartId
         * @memberof st3215.TxEnvelope
         * @instance
         */
        TxEnvelope.prototype.appStartId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * TxEnvelope targetBusSerial.
         * @member {string} targetBusSerial
         * @memberof st3215.TxEnvelope
         * @instance
         */
        TxEnvelope.prototype.targetBusSerial = "";

        /**
         * TxEnvelope commandId.
         * @member {Uint8Array} commandId
         * @memberof st3215.TxEnvelope
         * @instance
         */
        TxEnvelope.prototype.commandId = $util.newBuffer([]);

        /**
         * TxEnvelope write.
         * @member {st3215.IST3215WriteCommand|null|undefined} write
         * @memberof st3215.TxEnvelope
         * @instance
         */
        TxEnvelope.prototype.write = null;

        /**
         * TxEnvelope regWrite.
         * @member {st3215.IST3215RegWriteCommand|null|undefined} regWrite
         * @memberof st3215.TxEnvelope
         * @instance
         */
        TxEnvelope.prototype.regWrite = null;

        /**
         * TxEnvelope action.
         * @member {st3215.IST3215ActionCommand|null|undefined} action
         * @memberof st3215.TxEnvelope
         * @instance
         */
        TxEnvelope.prototype.action = null;

        /**
         * TxEnvelope reset.
         * @member {st3215.IST3215ResetCommand|null|undefined} reset
         * @memberof st3215.TxEnvelope
         * @instance
         */
        TxEnvelope.prototype.reset = null;

        /**
         * TxEnvelope resetCalibration.
         * @member {st3215.IResetCalibrationCommand|null|undefined} resetCalibration
         * @memberof st3215.TxEnvelope
         * @instance
         */
        TxEnvelope.prototype.resetCalibration = null;

        /**
         * TxEnvelope freezeCalibration.
         * @member {st3215.IFreezeCalibrationCommand|null|undefined} freezeCalibration
         * @memberof st3215.TxEnvelope
         * @instance
         */
        TxEnvelope.prototype.freezeCalibration = null;

        /**
         * TxEnvelope syncWrite.
         * @member {st3215.IST3215SyncWriteCommand|null|undefined} syncWrite
         * @memberof st3215.TxEnvelope
         * @instance
         */
        TxEnvelope.prototype.syncWrite = null;

        /**
         * TxEnvelope autoCalibrate.
         * @member {st3215.IAutoCalibrateCommand|null|undefined} autoCalibrate
         * @memberof st3215.TxEnvelope
         * @instance
         */
        TxEnvelope.prototype.autoCalibrate = null;

        /**
         * TxEnvelope stopAutoCalibrate.
         * @member {st3215.IStopAutoCalibrateCommand|null|undefined} stopAutoCalibrate
         * @memberof st3215.TxEnvelope
         * @instance
         */
        TxEnvelope.prototype.stopAutoCalibrate = null;

        /**
         * Creates a new TxEnvelope instance using the specified properties.
         * @function create
         * @memberof st3215.TxEnvelope
         * @static
         * @param {st3215.ITxEnvelope=} [properties] Properties to set
         * @returns {st3215.TxEnvelope} TxEnvelope instance
         */
        TxEnvelope.create = function create(properties) {
            return new TxEnvelope(properties);
        };

        /**
         * Encodes the specified TxEnvelope message. Does not implicitly {@link st3215.TxEnvelope.verify|verify} messages.
         * @function encode
         * @memberof st3215.TxEnvelope
         * @static
         * @param {st3215.ITxEnvelope} message TxEnvelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TxEnvelope.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.monotonicStampNs != null && Object.hasOwnProperty.call(message, "monotonicStampNs"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.monotonicStampNs);
            if (message.localStampNs != null && Object.hasOwnProperty.call(message, "localStampNs"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.localStampNs);
            if (message.targetBusSerial != null && Object.hasOwnProperty.call(message, "targetBusSerial"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.targetBusSerial);
            if (message.commandId != null && Object.hasOwnProperty.call(message, "commandId"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.commandId);
            if (message.appStartId != null && Object.hasOwnProperty.call(message, "appStartId"))
                writer.uint32(/* id 5, wireType 0 =*/40).uint64(message.appStartId);
            if (message.write != null && Object.hasOwnProperty.call(message, "write"))
                $root.st3215.ST3215WriteCommand.encode(message.write, writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            if (message.regWrite != null && Object.hasOwnProperty.call(message, "regWrite"))
                $root.st3215.ST3215RegWriteCommand.encode(message.regWrite, writer.uint32(/* id 11, wireType 2 =*/90).fork()).ldelim();
            if (message.action != null && Object.hasOwnProperty.call(message, "action"))
                $root.st3215.ST3215ActionCommand.encode(message.action, writer.uint32(/* id 12, wireType 2 =*/98).fork()).ldelim();
            if (message.reset != null && Object.hasOwnProperty.call(message, "reset"))
                $root.st3215.ST3215ResetCommand.encode(message.reset, writer.uint32(/* id 13, wireType 2 =*/106).fork()).ldelim();
            if (message.resetCalibration != null && Object.hasOwnProperty.call(message, "resetCalibration"))
                $root.st3215.ResetCalibrationCommand.encode(message.resetCalibration, writer.uint32(/* id 14, wireType 2 =*/114).fork()).ldelim();
            if (message.freezeCalibration != null && Object.hasOwnProperty.call(message, "freezeCalibration"))
                $root.st3215.FreezeCalibrationCommand.encode(message.freezeCalibration, writer.uint32(/* id 15, wireType 2 =*/122).fork()).ldelim();
            if (message.syncWrite != null && Object.hasOwnProperty.call(message, "syncWrite"))
                $root.st3215.ST3215SyncWriteCommand.encode(message.syncWrite, writer.uint32(/* id 16, wireType 2 =*/130).fork()).ldelim();
            if (message.autoCalibrate != null && Object.hasOwnProperty.call(message, "autoCalibrate"))
                $root.st3215.AutoCalibrateCommand.encode(message.autoCalibrate, writer.uint32(/* id 17, wireType 2 =*/138).fork()).ldelim();
            if (message.stopAutoCalibrate != null && Object.hasOwnProperty.call(message, "stopAutoCalibrate"))
                $root.st3215.StopAutoCalibrateCommand.encode(message.stopAutoCalibrate, writer.uint32(/* id 18, wireType 2 =*/146).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified TxEnvelope message, length delimited. Does not implicitly {@link st3215.TxEnvelope.verify|verify} messages.
         * @function encodeDelimited
         * @memberof st3215.TxEnvelope
         * @static
         * @param {st3215.ITxEnvelope} message TxEnvelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TxEnvelope.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a TxEnvelope message from the specified reader or buffer.
         * @function decode
         * @memberof st3215.TxEnvelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {st3215.TxEnvelope} TxEnvelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TxEnvelope.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.st3215.TxEnvelope();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.monotonicStampNs = reader.uint64();
                        break;
                    }
                case 2: {
                        message.localStampNs = reader.uint64();
                        break;
                    }
                case 5: {
                        message.appStartId = reader.uint64();
                        break;
                    }
                case 3: {
                        message.targetBusSerial = reader.string();
                        break;
                    }
                case 4: {
                        message.commandId = reader.bytes();
                        break;
                    }
                case 10: {
                        message.write = $root.st3215.ST3215WriteCommand.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 11: {
                        message.regWrite = $root.st3215.ST3215RegWriteCommand.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 12: {
                        message.action = $root.st3215.ST3215ActionCommand.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 13: {
                        message.reset = $root.st3215.ST3215ResetCommand.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 14: {
                        message.resetCalibration = $root.st3215.ResetCalibrationCommand.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 15: {
                        message.freezeCalibration = $root.st3215.FreezeCalibrationCommand.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 16: {
                        message.syncWrite = $root.st3215.ST3215SyncWriteCommand.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 17: {
                        message.autoCalibrate = $root.st3215.AutoCalibrateCommand.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 18: {
                        message.stopAutoCalibrate = $root.st3215.StopAutoCalibrateCommand.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a TxEnvelope message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof st3215.TxEnvelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {st3215.TxEnvelope} TxEnvelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TxEnvelope.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TxEnvelope message.
         * @function verify
         * @memberof st3215.TxEnvelope
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TxEnvelope.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (!$util.isInteger(message.monotonicStampNs) && !(message.monotonicStampNs && $util.isInteger(message.monotonicStampNs.low) && $util.isInteger(message.monotonicStampNs.high)))
                    return "monotonicStampNs: integer|Long expected";
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (!$util.isInteger(message.localStampNs) && !(message.localStampNs && $util.isInteger(message.localStampNs.low) && $util.isInteger(message.localStampNs.high)))
                    return "localStampNs: integer|Long expected";
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (!$util.isInteger(message.appStartId) && !(message.appStartId && $util.isInteger(message.appStartId.low) && $util.isInteger(message.appStartId.high)))
                    return "appStartId: integer|Long expected";
            if (message.targetBusSerial != null && message.hasOwnProperty("targetBusSerial"))
                if (!$util.isString(message.targetBusSerial))
                    return "targetBusSerial: string expected";
            if (message.commandId != null && message.hasOwnProperty("commandId"))
                if (!(message.commandId && typeof message.commandId.length === "number" || $util.isString(message.commandId)))
                    return "commandId: buffer expected";
            if (message.write != null && message.hasOwnProperty("write")) {
                let error = $root.st3215.ST3215WriteCommand.verify(message.write, long + 1);
                if (error)
                    return "write." + error;
            }
            if (message.regWrite != null && message.hasOwnProperty("regWrite")) {
                let error = $root.st3215.ST3215RegWriteCommand.verify(message.regWrite, long + 1);
                if (error)
                    return "regWrite." + error;
            }
            if (message.action != null && message.hasOwnProperty("action")) {
                let error = $root.st3215.ST3215ActionCommand.verify(message.action, long + 1);
                if (error)
                    return "action." + error;
            }
            if (message.reset != null && message.hasOwnProperty("reset")) {
                let error = $root.st3215.ST3215ResetCommand.verify(message.reset, long + 1);
                if (error)
                    return "reset." + error;
            }
            if (message.resetCalibration != null && message.hasOwnProperty("resetCalibration")) {
                let error = $root.st3215.ResetCalibrationCommand.verify(message.resetCalibration, long + 1);
                if (error)
                    return "resetCalibration." + error;
            }
            if (message.freezeCalibration != null && message.hasOwnProperty("freezeCalibration")) {
                let error = $root.st3215.FreezeCalibrationCommand.verify(message.freezeCalibration, long + 1);
                if (error)
                    return "freezeCalibration." + error;
            }
            if (message.syncWrite != null && message.hasOwnProperty("syncWrite")) {
                let error = $root.st3215.ST3215SyncWriteCommand.verify(message.syncWrite, long + 1);
                if (error)
                    return "syncWrite." + error;
            }
            if (message.autoCalibrate != null && message.hasOwnProperty("autoCalibrate")) {
                let error = $root.st3215.AutoCalibrateCommand.verify(message.autoCalibrate, long + 1);
                if (error)
                    return "autoCalibrate." + error;
            }
            if (message.stopAutoCalibrate != null && message.hasOwnProperty("stopAutoCalibrate")) {
                let error = $root.st3215.StopAutoCalibrateCommand.verify(message.stopAutoCalibrate, long + 1);
                if (error)
                    return "stopAutoCalibrate." + error;
            }
            return null;
        };

        /**
         * Creates a TxEnvelope message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof st3215.TxEnvelope
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {st3215.TxEnvelope} TxEnvelope
         */
        TxEnvelope.fromObject = function fromObject(object, long) {
            if (object instanceof $root.st3215.TxEnvelope)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.st3215.TxEnvelope();
            if (object.monotonicStampNs != null)
                if ($util.Long)
                    (message.monotonicStampNs = $util.Long.fromValue(object.monotonicStampNs)).unsigned = true;
                else if (typeof object.monotonicStampNs === "string")
                    message.monotonicStampNs = parseInt(object.monotonicStampNs, 10);
                else if (typeof object.monotonicStampNs === "number")
                    message.monotonicStampNs = object.monotonicStampNs;
                else if (typeof object.monotonicStampNs === "object")
                    message.monotonicStampNs = new $util.LongBits(object.monotonicStampNs.low >>> 0, object.monotonicStampNs.high >>> 0).toNumber(true);
            if (object.localStampNs != null)
                if ($util.Long)
                    (message.localStampNs = $util.Long.fromValue(object.localStampNs)).unsigned = true;
                else if (typeof object.localStampNs === "string")
                    message.localStampNs = parseInt(object.localStampNs, 10);
                else if (typeof object.localStampNs === "number")
                    message.localStampNs = object.localStampNs;
                else if (typeof object.localStampNs === "object")
                    message.localStampNs = new $util.LongBits(object.localStampNs.low >>> 0, object.localStampNs.high >>> 0).toNumber(true);
            if (object.appStartId != null)
                if ($util.Long)
                    (message.appStartId = $util.Long.fromValue(object.appStartId)).unsigned = true;
                else if (typeof object.appStartId === "string")
                    message.appStartId = parseInt(object.appStartId, 10);
                else if (typeof object.appStartId === "number")
                    message.appStartId = object.appStartId;
                else if (typeof object.appStartId === "object")
                    message.appStartId = new $util.LongBits(object.appStartId.low >>> 0, object.appStartId.high >>> 0).toNumber(true);
            if (object.targetBusSerial != null)
                message.targetBusSerial = String(object.targetBusSerial);
            if (object.commandId != null)
                if (typeof object.commandId === "string")
                    $util.base64.decode(object.commandId, message.commandId = $util.newBuffer($util.base64.length(object.commandId)), 0);
                else if (object.commandId.length >= 0)
                    message.commandId = object.commandId;
            if (object.write != null) {
                if (typeof object.write !== "object")
                    throw TypeError(".st3215.TxEnvelope.write: object expected");
                message.write = $root.st3215.ST3215WriteCommand.fromObject(object.write, long + 1);
            }
            if (object.regWrite != null) {
                if (typeof object.regWrite !== "object")
                    throw TypeError(".st3215.TxEnvelope.regWrite: object expected");
                message.regWrite = $root.st3215.ST3215RegWriteCommand.fromObject(object.regWrite, long + 1);
            }
            if (object.action != null) {
                if (typeof object.action !== "object")
                    throw TypeError(".st3215.TxEnvelope.action: object expected");
                message.action = $root.st3215.ST3215ActionCommand.fromObject(object.action, long + 1);
            }
            if (object.reset != null) {
                if (typeof object.reset !== "object")
                    throw TypeError(".st3215.TxEnvelope.reset: object expected");
                message.reset = $root.st3215.ST3215ResetCommand.fromObject(object.reset, long + 1);
            }
            if (object.resetCalibration != null) {
                if (typeof object.resetCalibration !== "object")
                    throw TypeError(".st3215.TxEnvelope.resetCalibration: object expected");
                message.resetCalibration = $root.st3215.ResetCalibrationCommand.fromObject(object.resetCalibration, long + 1);
            }
            if (object.freezeCalibration != null) {
                if (typeof object.freezeCalibration !== "object")
                    throw TypeError(".st3215.TxEnvelope.freezeCalibration: object expected");
                message.freezeCalibration = $root.st3215.FreezeCalibrationCommand.fromObject(object.freezeCalibration, long + 1);
            }
            if (object.syncWrite != null) {
                if (typeof object.syncWrite !== "object")
                    throw TypeError(".st3215.TxEnvelope.syncWrite: object expected");
                message.syncWrite = $root.st3215.ST3215SyncWriteCommand.fromObject(object.syncWrite, long + 1);
            }
            if (object.autoCalibrate != null) {
                if (typeof object.autoCalibrate !== "object")
                    throw TypeError(".st3215.TxEnvelope.autoCalibrate: object expected");
                message.autoCalibrate = $root.st3215.AutoCalibrateCommand.fromObject(object.autoCalibrate, long + 1);
            }
            if (object.stopAutoCalibrate != null) {
                if (typeof object.stopAutoCalibrate !== "object")
                    throw TypeError(".st3215.TxEnvelope.stopAutoCalibrate: object expected");
                message.stopAutoCalibrate = $root.st3215.StopAutoCalibrateCommand.fromObject(object.stopAutoCalibrate, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a TxEnvelope message. Also converts values to other types if specified.
         * @function toObject
         * @memberof st3215.TxEnvelope
         * @static
         * @param {st3215.TxEnvelope} message TxEnvelope
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TxEnvelope.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.monotonicStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.monotonicStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.localStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.localStampNs = options.longs === String ? "0" : 0;
                object.targetBusSerial = "";
                if (options.bytes === String)
                    object.commandId = "";
                else {
                    object.commandId = [];
                    if (options.bytes !== Array)
                        object.commandId = $util.newBuffer(object.commandId);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.appStartId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.appStartId = options.longs === String ? "0" : 0;
                object.write = null;
                object.regWrite = null;
                object.action = null;
                object.reset = null;
                object.resetCalibration = null;
                object.freezeCalibration = null;
                object.syncWrite = null;
                object.autoCalibrate = null;
                object.stopAutoCalibrate = null;
            }
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (typeof message.monotonicStampNs === "number")
                    object.monotonicStampNs = options.longs === String ? String(message.monotonicStampNs) : message.monotonicStampNs;
                else
                    object.monotonicStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.monotonicStampNs) : options.longs === Number ? new $util.LongBits(message.monotonicStampNs.low >>> 0, message.monotonicStampNs.high >>> 0).toNumber(true) : message.monotonicStampNs;
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (typeof message.localStampNs === "number")
                    object.localStampNs = options.longs === String ? String(message.localStampNs) : message.localStampNs;
                else
                    object.localStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.localStampNs) : options.longs === Number ? new $util.LongBits(message.localStampNs.low >>> 0, message.localStampNs.high >>> 0).toNumber(true) : message.localStampNs;
            if (message.targetBusSerial != null && message.hasOwnProperty("targetBusSerial"))
                object.targetBusSerial = message.targetBusSerial;
            if (message.commandId != null && message.hasOwnProperty("commandId"))
                object.commandId = options.bytes === String ? $util.base64.encode(message.commandId, 0, message.commandId.length) : options.bytes === Array ? Array.prototype.slice.call(message.commandId) : message.commandId;
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (typeof message.appStartId === "number")
                    object.appStartId = options.longs === String ? String(message.appStartId) : message.appStartId;
                else
                    object.appStartId = options.longs === String ? $util.Long.prototype.toString.call(message.appStartId) : options.longs === Number ? new $util.LongBits(message.appStartId.low >>> 0, message.appStartId.high >>> 0).toNumber(true) : message.appStartId;
            if (message.write != null && message.hasOwnProperty("write"))
                object.write = $root.st3215.ST3215WriteCommand.toObject(message.write, options);
            if (message.regWrite != null && message.hasOwnProperty("regWrite"))
                object.regWrite = $root.st3215.ST3215RegWriteCommand.toObject(message.regWrite, options);
            if (message.action != null && message.hasOwnProperty("action"))
                object.action = $root.st3215.ST3215ActionCommand.toObject(message.action, options);
            if (message.reset != null && message.hasOwnProperty("reset"))
                object.reset = $root.st3215.ST3215ResetCommand.toObject(message.reset, options);
            if (message.resetCalibration != null && message.hasOwnProperty("resetCalibration"))
                object.resetCalibration = $root.st3215.ResetCalibrationCommand.toObject(message.resetCalibration, options);
            if (message.freezeCalibration != null && message.hasOwnProperty("freezeCalibration"))
                object.freezeCalibration = $root.st3215.FreezeCalibrationCommand.toObject(message.freezeCalibration, options);
            if (message.syncWrite != null && message.hasOwnProperty("syncWrite"))
                object.syncWrite = $root.st3215.ST3215SyncWriteCommand.toObject(message.syncWrite, options);
            if (message.autoCalibrate != null && message.hasOwnProperty("autoCalibrate"))
                object.autoCalibrate = $root.st3215.AutoCalibrateCommand.toObject(message.autoCalibrate, options);
            if (message.stopAutoCalibrate != null && message.hasOwnProperty("stopAutoCalibrate"))
                object.stopAutoCalibrate = $root.st3215.StopAutoCalibrateCommand.toObject(message.stopAutoCalibrate, options);
            return object;
        };

        /**
         * Converts this TxEnvelope to JSON.
         * @function toJSON
         * @memberof st3215.TxEnvelope
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TxEnvelope.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for TxEnvelope
         * @function getTypeUrl
         * @memberof st3215.TxEnvelope
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        TxEnvelope.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/st3215.TxEnvelope";
        };

        return TxEnvelope;
    })();

    st3215.MetaEnvelope = (function() {

        /**
         * Properties of a MetaEnvelope.
         * @memberof st3215
         * @interface IMetaEnvelope
         * @property {Long|null} [monotonicStampNs] MetaEnvelope monotonicStampNs
         * @property {Long|null} [localStampNs] MetaEnvelope localStampNs
         * @property {Long|null} [appStartId] MetaEnvelope appStartId
         * @property {st3215.MetaEnvelopeType|null} [type] MetaEnvelope type
         * @property {Uint8Array|null} [rxUintnPtr] MetaEnvelope rxUintnPtr
         * @property {string|null} [busSerial] MetaEnvelope busSerial
         * @property {Array.<st3215.IMotorArc>|null} [arcs] MetaEnvelope arcs
         */

        /**
         * Constructs a new MetaEnvelope.
         * @memberof st3215
         * @classdesc Represents a MetaEnvelope.
         * @implements IMetaEnvelope
         * @constructor
         * @param {st3215.IMetaEnvelope=} [properties] Properties to set
         */
        function MetaEnvelope(properties) {
            this.arcs = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MetaEnvelope monotonicStampNs.
         * @member {Long} monotonicStampNs
         * @memberof st3215.MetaEnvelope
         * @instance
         */
        MetaEnvelope.prototype.monotonicStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * MetaEnvelope localStampNs.
         * @member {Long} localStampNs
         * @memberof st3215.MetaEnvelope
         * @instance
         */
        MetaEnvelope.prototype.localStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * MetaEnvelope appStartId.
         * @member {Long} appStartId
         * @memberof st3215.MetaEnvelope
         * @instance
         */
        MetaEnvelope.prototype.appStartId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * MetaEnvelope type.
         * @member {st3215.MetaEnvelopeType} type
         * @memberof st3215.MetaEnvelope
         * @instance
         */
        MetaEnvelope.prototype.type = 0;

        /**
         * MetaEnvelope rxUintnPtr.
         * @member {Uint8Array} rxUintnPtr
         * @memberof st3215.MetaEnvelope
         * @instance
         */
        MetaEnvelope.prototype.rxUintnPtr = $util.newBuffer([]);

        /**
         * MetaEnvelope busSerial.
         * @member {string} busSerial
         * @memberof st3215.MetaEnvelope
         * @instance
         */
        MetaEnvelope.prototype.busSerial = "";

        /**
         * MetaEnvelope arcs.
         * @member {Array.<st3215.IMotorArc>} arcs
         * @memberof st3215.MetaEnvelope
         * @instance
         */
        MetaEnvelope.prototype.arcs = $util.emptyArray;

        /**
         * Creates a new MetaEnvelope instance using the specified properties.
         * @function create
         * @memberof st3215.MetaEnvelope
         * @static
         * @param {st3215.IMetaEnvelope=} [properties] Properties to set
         * @returns {st3215.MetaEnvelope} MetaEnvelope instance
         */
        MetaEnvelope.create = function create(properties) {
            return new MetaEnvelope(properties);
        };

        /**
         * Encodes the specified MetaEnvelope message. Does not implicitly {@link st3215.MetaEnvelope.verify|verify} messages.
         * @function encode
         * @memberof st3215.MetaEnvelope
         * @static
         * @param {st3215.IMetaEnvelope} message MetaEnvelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MetaEnvelope.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.monotonicStampNs != null && Object.hasOwnProperty.call(message, "monotonicStampNs"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.monotonicStampNs);
            if (message.localStampNs != null && Object.hasOwnProperty.call(message, "localStampNs"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.localStampNs);
            if (message.appStartId != null && Object.hasOwnProperty.call(message, "appStartId"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.appStartId);
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.type);
            if (message.rxUintnPtr != null && Object.hasOwnProperty.call(message, "rxUintnPtr"))
                writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.rxUintnPtr);
            if (message.busSerial != null && Object.hasOwnProperty.call(message, "busSerial"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.busSerial);
            if (message.arcs != null && message.arcs.length)
                for (let i = 0; i < message.arcs.length; ++i)
                    $root.st3215.MotorArc.encode(message.arcs[i], writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified MetaEnvelope message, length delimited. Does not implicitly {@link st3215.MetaEnvelope.verify|verify} messages.
         * @function encodeDelimited
         * @memberof st3215.MetaEnvelope
         * @static
         * @param {st3215.IMetaEnvelope} message MetaEnvelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MetaEnvelope.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a MetaEnvelope message from the specified reader or buffer.
         * @function decode
         * @memberof st3215.MetaEnvelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {st3215.MetaEnvelope} MetaEnvelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MetaEnvelope.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.st3215.MetaEnvelope();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.monotonicStampNs = reader.uint64();
                        break;
                    }
                case 2: {
                        message.localStampNs = reader.uint64();
                        break;
                    }
                case 3: {
                        message.appStartId = reader.uint64();
                        break;
                    }
                case 4: {
                        message.type = reader.int32();
                        break;
                    }
                case 5: {
                        message.rxUintnPtr = reader.bytes();
                        break;
                    }
                case 6: {
                        message.busSerial = reader.string();
                        break;
                    }
                case 10: {
                        if (!(message.arcs && message.arcs.length))
                            message.arcs = [];
                        message.arcs.push($root.st3215.MotorArc.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a MetaEnvelope message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof st3215.MetaEnvelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {st3215.MetaEnvelope} MetaEnvelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MetaEnvelope.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MetaEnvelope message.
         * @function verify
         * @memberof st3215.MetaEnvelope
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MetaEnvelope.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (!$util.isInteger(message.monotonicStampNs) && !(message.monotonicStampNs && $util.isInteger(message.monotonicStampNs.low) && $util.isInteger(message.monotonicStampNs.high)))
                    return "monotonicStampNs: integer|Long expected";
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (!$util.isInteger(message.localStampNs) && !(message.localStampNs && $util.isInteger(message.localStampNs.low) && $util.isInteger(message.localStampNs.high)))
                    return "localStampNs: integer|Long expected";
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (!$util.isInteger(message.appStartId) && !(message.appStartId && $util.isInteger(message.appStartId.low) && $util.isInteger(message.appStartId.high)))
                    return "appStartId: integer|Long expected";
            if (message.type != null && message.hasOwnProperty("type"))
                switch (message.type) {
                default:
                    return "type: enum value expected";
                case 0:
                case 1:
                case 2:
                    break;
                }
            if (message.rxUintnPtr != null && message.hasOwnProperty("rxUintnPtr"))
                if (!(message.rxUintnPtr && typeof message.rxUintnPtr.length === "number" || $util.isString(message.rxUintnPtr)))
                    return "rxUintnPtr: buffer expected";
            if (message.busSerial != null && message.hasOwnProperty("busSerial"))
                if (!$util.isString(message.busSerial))
                    return "busSerial: string expected";
            if (message.arcs != null && message.hasOwnProperty("arcs")) {
                if (!Array.isArray(message.arcs))
                    return "arcs: array expected";
                for (let i = 0; i < message.arcs.length; ++i) {
                    let error = $root.st3215.MotorArc.verify(message.arcs[i], long + 1);
                    if (error)
                        return "arcs." + error;
                }
            }
            return null;
        };

        /**
         * Creates a MetaEnvelope message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof st3215.MetaEnvelope
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {st3215.MetaEnvelope} MetaEnvelope
         */
        MetaEnvelope.fromObject = function fromObject(object, long) {
            if (object instanceof $root.st3215.MetaEnvelope)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.st3215.MetaEnvelope();
            if (object.monotonicStampNs != null)
                if ($util.Long)
                    (message.monotonicStampNs = $util.Long.fromValue(object.monotonicStampNs)).unsigned = true;
                else if (typeof object.monotonicStampNs === "string")
                    message.monotonicStampNs = parseInt(object.monotonicStampNs, 10);
                else if (typeof object.monotonicStampNs === "number")
                    message.monotonicStampNs = object.monotonicStampNs;
                else if (typeof object.monotonicStampNs === "object")
                    message.monotonicStampNs = new $util.LongBits(object.monotonicStampNs.low >>> 0, object.monotonicStampNs.high >>> 0).toNumber(true);
            if (object.localStampNs != null)
                if ($util.Long)
                    (message.localStampNs = $util.Long.fromValue(object.localStampNs)).unsigned = true;
                else if (typeof object.localStampNs === "string")
                    message.localStampNs = parseInt(object.localStampNs, 10);
                else if (typeof object.localStampNs === "number")
                    message.localStampNs = object.localStampNs;
                else if (typeof object.localStampNs === "object")
                    message.localStampNs = new $util.LongBits(object.localStampNs.low >>> 0, object.localStampNs.high >>> 0).toNumber(true);
            if (object.appStartId != null)
                if ($util.Long)
                    (message.appStartId = $util.Long.fromValue(object.appStartId)).unsigned = true;
                else if (typeof object.appStartId === "string")
                    message.appStartId = parseInt(object.appStartId, 10);
                else if (typeof object.appStartId === "number")
                    message.appStartId = object.appStartId;
                else if (typeof object.appStartId === "object")
                    message.appStartId = new $util.LongBits(object.appStartId.low >>> 0, object.appStartId.high >>> 0).toNumber(true);
            switch (object.type) {
            default:
                if (typeof object.type === "number") {
                    message.type = object.type;
                    break;
                }
                break;
            case "MET_UNKNOWN":
            case 0:
                message.type = 0;
                break;
            case "MET_RESET_CALIBRATION":
            case 1:
                message.type = 1;
                break;
            case "MET_FREEZE_CALIBRATION":
            case 2:
                message.type = 2;
                break;
            }
            if (object.rxUintnPtr != null)
                if (typeof object.rxUintnPtr === "string")
                    $util.base64.decode(object.rxUintnPtr, message.rxUintnPtr = $util.newBuffer($util.base64.length(object.rxUintnPtr)), 0);
                else if (object.rxUintnPtr.length >= 0)
                    message.rxUintnPtr = object.rxUintnPtr;
            if (object.busSerial != null)
                message.busSerial = String(object.busSerial);
            if (object.arcs) {
                if (!Array.isArray(object.arcs))
                    throw TypeError(".st3215.MetaEnvelope.arcs: array expected");
                message.arcs = [];
                for (let i = 0; i < object.arcs.length; ++i) {
                    if (typeof object.arcs[i] !== "object")
                        throw TypeError(".st3215.MetaEnvelope.arcs: object expected");
                    message.arcs[i] = $root.st3215.MotorArc.fromObject(object.arcs[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a MetaEnvelope message. Also converts values to other types if specified.
         * @function toObject
         * @memberof st3215.MetaEnvelope
         * @static
         * @param {st3215.MetaEnvelope} message MetaEnvelope
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MetaEnvelope.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.arcs = [];
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.monotonicStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.monotonicStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.localStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.localStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.appStartId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.appStartId = options.longs === String ? "0" : 0;
                object.type = options.enums === String ? "MET_UNKNOWN" : 0;
                if (options.bytes === String)
                    object.rxUintnPtr = "";
                else {
                    object.rxUintnPtr = [];
                    if (options.bytes !== Array)
                        object.rxUintnPtr = $util.newBuffer(object.rxUintnPtr);
                }
                object.busSerial = "";
            }
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (typeof message.monotonicStampNs === "number")
                    object.monotonicStampNs = options.longs === String ? String(message.monotonicStampNs) : message.monotonicStampNs;
                else
                    object.monotonicStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.monotonicStampNs) : options.longs === Number ? new $util.LongBits(message.monotonicStampNs.low >>> 0, message.monotonicStampNs.high >>> 0).toNumber(true) : message.monotonicStampNs;
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (typeof message.localStampNs === "number")
                    object.localStampNs = options.longs === String ? String(message.localStampNs) : message.localStampNs;
                else
                    object.localStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.localStampNs) : options.longs === Number ? new $util.LongBits(message.localStampNs.low >>> 0, message.localStampNs.high >>> 0).toNumber(true) : message.localStampNs;
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (typeof message.appStartId === "number")
                    object.appStartId = options.longs === String ? String(message.appStartId) : message.appStartId;
                else
                    object.appStartId = options.longs === String ? $util.Long.prototype.toString.call(message.appStartId) : options.longs === Number ? new $util.LongBits(message.appStartId.low >>> 0, message.appStartId.high >>> 0).toNumber(true) : message.appStartId;
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = options.enums === String ? $root.st3215.MetaEnvelopeType[message.type] === undefined ? message.type : $root.st3215.MetaEnvelopeType[message.type] : message.type;
            if (message.rxUintnPtr != null && message.hasOwnProperty("rxUintnPtr"))
                object.rxUintnPtr = options.bytes === String ? $util.base64.encode(message.rxUintnPtr, 0, message.rxUintnPtr.length) : options.bytes === Array ? Array.prototype.slice.call(message.rxUintnPtr) : message.rxUintnPtr;
            if (message.busSerial != null && message.hasOwnProperty("busSerial"))
                object.busSerial = message.busSerial;
            if (message.arcs && message.arcs.length) {
                object.arcs = [];
                for (let j = 0; j < message.arcs.length; ++j)
                    object.arcs[j] = $root.st3215.MotorArc.toObject(message.arcs[j], options);
            }
            return object;
        };

        /**
         * Converts this MetaEnvelope to JSON.
         * @function toJSON
         * @memberof st3215.MetaEnvelope
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MetaEnvelope.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MetaEnvelope
         * @function getTypeUrl
         * @memberof st3215.MetaEnvelope
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MetaEnvelope.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/st3215.MetaEnvelope";
        };

        return MetaEnvelope;
    })();

    /**
     * CommandResult enum.
     * @name st3215.CommandResult
     * @enum {number}
     * @property {number} CR_PROCESSING=0 CR_PROCESSING value
     * @property {number} CR_SUCCESS=1 CR_SUCCESS value
     * @property {number} CR_REJECTED=2 CR_REJECTED value
     * @property {number} CR_FAILED=3 CR_FAILED value
     */
    st3215.CommandResult = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "CR_PROCESSING"] = 0;
        values[valuesById[1] = "CR_SUCCESS"] = 1;
        values[valuesById[2] = "CR_REJECTED"] = 2;
        values[valuesById[3] = "CR_FAILED"] = 3;
        return values;
    })();

    st3215.InferenceCommandState = (function() {

        /**
         * Properties of an InferenceCommandState.
         * @memberof st3215
         * @interface IInferenceCommandState
         * @property {st3215.ITxEnvelope|null} [command] InferenceCommandState command
         * @property {st3215.CommandResult|null} [result] InferenceCommandState result
         */

        /**
         * Constructs a new InferenceCommandState.
         * @memberof st3215
         * @classdesc Represents an InferenceCommandState.
         * @implements IInferenceCommandState
         * @constructor
         * @param {st3215.IInferenceCommandState=} [properties] Properties to set
         */
        function InferenceCommandState(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * InferenceCommandState command.
         * @member {st3215.ITxEnvelope|null|undefined} command
         * @memberof st3215.InferenceCommandState
         * @instance
         */
        InferenceCommandState.prototype.command = null;

        /**
         * InferenceCommandState result.
         * @member {st3215.CommandResult} result
         * @memberof st3215.InferenceCommandState
         * @instance
         */
        InferenceCommandState.prototype.result = 0;

        /**
         * Creates a new InferenceCommandState instance using the specified properties.
         * @function create
         * @memberof st3215.InferenceCommandState
         * @static
         * @param {st3215.IInferenceCommandState=} [properties] Properties to set
         * @returns {st3215.InferenceCommandState} InferenceCommandState instance
         */
        InferenceCommandState.create = function create(properties) {
            return new InferenceCommandState(properties);
        };

        /**
         * Encodes the specified InferenceCommandState message. Does not implicitly {@link st3215.InferenceCommandState.verify|verify} messages.
         * @function encode
         * @memberof st3215.InferenceCommandState
         * @static
         * @param {st3215.IInferenceCommandState} message InferenceCommandState message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        InferenceCommandState.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.command != null && Object.hasOwnProperty.call(message, "command"))
                $root.st3215.TxEnvelope.encode(message.command, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.result != null && Object.hasOwnProperty.call(message, "result"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.result);
            return writer;
        };

        /**
         * Encodes the specified InferenceCommandState message, length delimited. Does not implicitly {@link st3215.InferenceCommandState.verify|verify} messages.
         * @function encodeDelimited
         * @memberof st3215.InferenceCommandState
         * @static
         * @param {st3215.IInferenceCommandState} message InferenceCommandState message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        InferenceCommandState.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an InferenceCommandState message from the specified reader or buffer.
         * @function decode
         * @memberof st3215.InferenceCommandState
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {st3215.InferenceCommandState} InferenceCommandState
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        InferenceCommandState.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.st3215.InferenceCommandState();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.command = $root.st3215.TxEnvelope.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 2: {
                        message.result = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an InferenceCommandState message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof st3215.InferenceCommandState
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {st3215.InferenceCommandState} InferenceCommandState
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        InferenceCommandState.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an InferenceCommandState message.
         * @function verify
         * @memberof st3215.InferenceCommandState
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        InferenceCommandState.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.command != null && message.hasOwnProperty("command")) {
                let error = $root.st3215.TxEnvelope.verify(message.command, long + 1);
                if (error)
                    return "command." + error;
            }
            if (message.result != null && message.hasOwnProperty("result"))
                switch (message.result) {
                default:
                    return "result: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                    break;
                }
            return null;
        };

        /**
         * Creates an InferenceCommandState message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof st3215.InferenceCommandState
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {st3215.InferenceCommandState} InferenceCommandState
         */
        InferenceCommandState.fromObject = function fromObject(object, long) {
            if (object instanceof $root.st3215.InferenceCommandState)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.st3215.InferenceCommandState();
            if (object.command != null) {
                if (typeof object.command !== "object")
                    throw TypeError(".st3215.InferenceCommandState.command: object expected");
                message.command = $root.st3215.TxEnvelope.fromObject(object.command, long + 1);
            }
            switch (object.result) {
            default:
                if (typeof object.result === "number") {
                    message.result = object.result;
                    break;
                }
                break;
            case "CR_PROCESSING":
            case 0:
                message.result = 0;
                break;
            case "CR_SUCCESS":
            case 1:
                message.result = 1;
                break;
            case "CR_REJECTED":
            case 2:
                message.result = 2;
                break;
            case "CR_FAILED":
            case 3:
                message.result = 3;
                break;
            }
            return message;
        };

        /**
         * Creates a plain object from an InferenceCommandState message. Also converts values to other types if specified.
         * @function toObject
         * @memberof st3215.InferenceCommandState
         * @static
         * @param {st3215.InferenceCommandState} message InferenceCommandState
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        InferenceCommandState.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.command = null;
                object.result = options.enums === String ? "CR_PROCESSING" : 0;
            }
            if (message.command != null && message.hasOwnProperty("command"))
                object.command = $root.st3215.TxEnvelope.toObject(message.command, options);
            if (message.result != null && message.hasOwnProperty("result"))
                object.result = options.enums === String ? $root.st3215.CommandResult[message.result] === undefined ? message.result : $root.st3215.CommandResult[message.result] : message.result;
            return object;
        };

        /**
         * Converts this InferenceCommandState to JSON.
         * @function toJSON
         * @memberof st3215.InferenceCommandState
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        InferenceCommandState.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for InferenceCommandState
         * @function getTypeUrl
         * @memberof st3215.InferenceCommandState
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        InferenceCommandState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/st3215.InferenceCommandState";
        };

        return InferenceCommandState;
    })();

    st3215.AutoCalibrationState = (function() {

        /**
         * Properties of an AutoCalibrationState.
         * @memberof st3215
         * @interface IAutoCalibrationState
         * @property {st3215.AutoCalibrationState.Status|null} [status] AutoCalibrationState status
         * @property {number|null} [currentStep] AutoCalibrationState currentStep
         * @property {number|null} [totalSteps] AutoCalibrationState totalSteps
         * @property {string|null} [currentPhase] AutoCalibrationState currentPhase
         * @property {string|null} [errorMessage] AutoCalibrationState errorMessage
         */

        /**
         * Constructs a new AutoCalibrationState.
         * @memberof st3215
         * @classdesc Represents an AutoCalibrationState.
         * @implements IAutoCalibrationState
         * @constructor
         * @param {st3215.IAutoCalibrationState=} [properties] Properties to set
         */
        function AutoCalibrationState(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AutoCalibrationState status.
         * @member {st3215.AutoCalibrationState.Status} status
         * @memberof st3215.AutoCalibrationState
         * @instance
         */
        AutoCalibrationState.prototype.status = 0;

        /**
         * AutoCalibrationState currentStep.
         * @member {number} currentStep
         * @memberof st3215.AutoCalibrationState
         * @instance
         */
        AutoCalibrationState.prototype.currentStep = 0;

        /**
         * AutoCalibrationState totalSteps.
         * @member {number} totalSteps
         * @memberof st3215.AutoCalibrationState
         * @instance
         */
        AutoCalibrationState.prototype.totalSteps = 0;

        /**
         * AutoCalibrationState currentPhase.
         * @member {string} currentPhase
         * @memberof st3215.AutoCalibrationState
         * @instance
         */
        AutoCalibrationState.prototype.currentPhase = "";

        /**
         * AutoCalibrationState errorMessage.
         * @member {string} errorMessage
         * @memberof st3215.AutoCalibrationState
         * @instance
         */
        AutoCalibrationState.prototype.errorMessage = "";

        /**
         * Creates a new AutoCalibrationState instance using the specified properties.
         * @function create
         * @memberof st3215.AutoCalibrationState
         * @static
         * @param {st3215.IAutoCalibrationState=} [properties] Properties to set
         * @returns {st3215.AutoCalibrationState} AutoCalibrationState instance
         */
        AutoCalibrationState.create = function create(properties) {
            return new AutoCalibrationState(properties);
        };

        /**
         * Encodes the specified AutoCalibrationState message. Does not implicitly {@link st3215.AutoCalibrationState.verify|verify} messages.
         * @function encode
         * @memberof st3215.AutoCalibrationState
         * @static
         * @param {st3215.IAutoCalibrationState} message AutoCalibrationState message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AutoCalibrationState.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.status);
            if (message.currentStep != null && Object.hasOwnProperty.call(message, "currentStep"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.currentStep);
            if (message.totalSteps != null && Object.hasOwnProperty.call(message, "totalSteps"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.totalSteps);
            if (message.currentPhase != null && Object.hasOwnProperty.call(message, "currentPhase"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.currentPhase);
            if (message.errorMessage != null && Object.hasOwnProperty.call(message, "errorMessage"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.errorMessage);
            return writer;
        };

        /**
         * Encodes the specified AutoCalibrationState message, length delimited. Does not implicitly {@link st3215.AutoCalibrationState.verify|verify} messages.
         * @function encodeDelimited
         * @memberof st3215.AutoCalibrationState
         * @static
         * @param {st3215.IAutoCalibrationState} message AutoCalibrationState message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AutoCalibrationState.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AutoCalibrationState message from the specified reader or buffer.
         * @function decode
         * @memberof st3215.AutoCalibrationState
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {st3215.AutoCalibrationState} AutoCalibrationState
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AutoCalibrationState.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.st3215.AutoCalibrationState();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.status = reader.int32();
                        break;
                    }
                case 2: {
                        message.currentStep = reader.uint32();
                        break;
                    }
                case 3: {
                        message.totalSteps = reader.uint32();
                        break;
                    }
                case 4: {
                        message.currentPhase = reader.string();
                        break;
                    }
                case 5: {
                        message.errorMessage = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AutoCalibrationState message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof st3215.AutoCalibrationState
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {st3215.AutoCalibrationState} AutoCalibrationState
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AutoCalibrationState.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AutoCalibrationState message.
         * @function verify
         * @memberof st3215.AutoCalibrationState
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AutoCalibrationState.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.status != null && message.hasOwnProperty("status"))
                switch (message.status) {
                default:
                    return "status: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                    break;
                }
            if (message.currentStep != null && message.hasOwnProperty("currentStep"))
                if (!$util.isInteger(message.currentStep))
                    return "currentStep: integer expected";
            if (message.totalSteps != null && message.hasOwnProperty("totalSteps"))
                if (!$util.isInteger(message.totalSteps))
                    return "totalSteps: integer expected";
            if (message.currentPhase != null && message.hasOwnProperty("currentPhase"))
                if (!$util.isString(message.currentPhase))
                    return "currentPhase: string expected";
            if (message.errorMessage != null && message.hasOwnProperty("errorMessage"))
                if (!$util.isString(message.errorMessage))
                    return "errorMessage: string expected";
            return null;
        };

        /**
         * Creates an AutoCalibrationState message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof st3215.AutoCalibrationState
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {st3215.AutoCalibrationState} AutoCalibrationState
         */
        AutoCalibrationState.fromObject = function fromObject(object, long) {
            if (object instanceof $root.st3215.AutoCalibrationState)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.st3215.AutoCalibrationState();
            switch (object.status) {
            default:
                if (typeof object.status === "number") {
                    message.status = object.status;
                    break;
                }
                break;
            case "IDLE":
            case 0:
                message.status = 0;
                break;
            case "IN_PROGRESS":
            case 1:
                message.status = 1;
                break;
            case "DONE":
            case 2:
                message.status = 2;
                break;
            case "FAILED":
            case 3:
                message.status = 3;
                break;
            case "STOPPED":
            case 4:
                message.status = 4;
                break;
            }
            if (object.currentStep != null)
                message.currentStep = object.currentStep >>> 0;
            if (object.totalSteps != null)
                message.totalSteps = object.totalSteps >>> 0;
            if (object.currentPhase != null)
                message.currentPhase = String(object.currentPhase);
            if (object.errorMessage != null)
                message.errorMessage = String(object.errorMessage);
            return message;
        };

        /**
         * Creates a plain object from an AutoCalibrationState message. Also converts values to other types if specified.
         * @function toObject
         * @memberof st3215.AutoCalibrationState
         * @static
         * @param {st3215.AutoCalibrationState} message AutoCalibrationState
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AutoCalibrationState.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.status = options.enums === String ? "IDLE" : 0;
                object.currentStep = 0;
                object.totalSteps = 0;
                object.currentPhase = "";
                object.errorMessage = "";
            }
            if (message.status != null && message.hasOwnProperty("status"))
                object.status = options.enums === String ? $root.st3215.AutoCalibrationState.Status[message.status] === undefined ? message.status : $root.st3215.AutoCalibrationState.Status[message.status] : message.status;
            if (message.currentStep != null && message.hasOwnProperty("currentStep"))
                object.currentStep = message.currentStep;
            if (message.totalSteps != null && message.hasOwnProperty("totalSteps"))
                object.totalSteps = message.totalSteps;
            if (message.currentPhase != null && message.hasOwnProperty("currentPhase"))
                object.currentPhase = message.currentPhase;
            if (message.errorMessage != null && message.hasOwnProperty("errorMessage"))
                object.errorMessage = message.errorMessage;
            return object;
        };

        /**
         * Converts this AutoCalibrationState to JSON.
         * @function toJSON
         * @memberof st3215.AutoCalibrationState
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AutoCalibrationState.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AutoCalibrationState
         * @function getTypeUrl
         * @memberof st3215.AutoCalibrationState
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AutoCalibrationState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/st3215.AutoCalibrationState";
        };

        /**
         * Status enum.
         * @name st3215.AutoCalibrationState.Status
         * @enum {number}
         * @property {number} IDLE=0 IDLE value
         * @property {number} IN_PROGRESS=1 IN_PROGRESS value
         * @property {number} DONE=2 DONE value
         * @property {number} FAILED=3 FAILED value
         * @property {number} STOPPED=4 STOPPED value
         */
        AutoCalibrationState.Status = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "IDLE"] = 0;
            values[valuesById[1] = "IN_PROGRESS"] = 1;
            values[valuesById[2] = "DONE"] = 2;
            values[valuesById[3] = "FAILED"] = 3;
            values[valuesById[4] = "STOPPED"] = 4;
            return values;
        })();

        return AutoCalibrationState;
    })();

    st3215.InferenceState = (function() {

        /**
         * Properties of an InferenceState.
         * @memberof st3215
         * @interface IInferenceState
         * @property {Uint8Array|null} [lastInferenceQueuePtr] InferenceState lastInferenceQueuePtr
         * @property {Array.<st3215.InferenceState.IBusState>|null} [buses] InferenceState buses
         */

        /**
         * Constructs a new InferenceState.
         * @memberof st3215
         * @classdesc Represents an InferenceState.
         * @implements IInferenceState
         * @constructor
         * @param {st3215.IInferenceState=} [properties] Properties to set
         */
        function InferenceState(properties) {
            this.buses = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * InferenceState lastInferenceQueuePtr.
         * @member {Uint8Array} lastInferenceQueuePtr
         * @memberof st3215.InferenceState
         * @instance
         */
        InferenceState.prototype.lastInferenceQueuePtr = $util.newBuffer([]);

        /**
         * InferenceState buses.
         * @member {Array.<st3215.InferenceState.IBusState>} buses
         * @memberof st3215.InferenceState
         * @instance
         */
        InferenceState.prototype.buses = $util.emptyArray;

        /**
         * Creates a new InferenceState instance using the specified properties.
         * @function create
         * @memberof st3215.InferenceState
         * @static
         * @param {st3215.IInferenceState=} [properties] Properties to set
         * @returns {st3215.InferenceState} InferenceState instance
         */
        InferenceState.create = function create(properties) {
            return new InferenceState(properties);
        };

        /**
         * Encodes the specified InferenceState message. Does not implicitly {@link st3215.InferenceState.verify|verify} messages.
         * @function encode
         * @memberof st3215.InferenceState
         * @static
         * @param {st3215.IInferenceState} message InferenceState message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        InferenceState.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.lastInferenceQueuePtr != null && Object.hasOwnProperty.call(message, "lastInferenceQueuePtr"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.lastInferenceQueuePtr);
            if (message.buses != null && message.buses.length)
                for (let i = 0; i < message.buses.length; ++i)
                    $root.st3215.InferenceState.BusState.encode(message.buses[i], writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified InferenceState message, length delimited. Does not implicitly {@link st3215.InferenceState.verify|verify} messages.
         * @function encodeDelimited
         * @memberof st3215.InferenceState
         * @static
         * @param {st3215.IInferenceState} message InferenceState message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        InferenceState.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an InferenceState message from the specified reader or buffer.
         * @function decode
         * @memberof st3215.InferenceState
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {st3215.InferenceState} InferenceState
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        InferenceState.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.st3215.InferenceState();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.lastInferenceQueuePtr = reader.bytes();
                        break;
                    }
                case 10: {
                        if (!(message.buses && message.buses.length))
                            message.buses = [];
                        message.buses.push($root.st3215.InferenceState.BusState.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an InferenceState message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof st3215.InferenceState
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {st3215.InferenceState} InferenceState
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        InferenceState.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an InferenceState message.
         * @function verify
         * @memberof st3215.InferenceState
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        InferenceState.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.lastInferenceQueuePtr != null && message.hasOwnProperty("lastInferenceQueuePtr"))
                if (!(message.lastInferenceQueuePtr && typeof message.lastInferenceQueuePtr.length === "number" || $util.isString(message.lastInferenceQueuePtr)))
                    return "lastInferenceQueuePtr: buffer expected";
            if (message.buses != null && message.hasOwnProperty("buses")) {
                if (!Array.isArray(message.buses))
                    return "buses: array expected";
                for (let i = 0; i < message.buses.length; ++i) {
                    let error = $root.st3215.InferenceState.BusState.verify(message.buses[i], long + 1);
                    if (error)
                        return "buses." + error;
                }
            }
            return null;
        };

        /**
         * Creates an InferenceState message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof st3215.InferenceState
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {st3215.InferenceState} InferenceState
         */
        InferenceState.fromObject = function fromObject(object, long) {
            if (object instanceof $root.st3215.InferenceState)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.st3215.InferenceState();
            if (object.lastInferenceQueuePtr != null)
                if (typeof object.lastInferenceQueuePtr === "string")
                    $util.base64.decode(object.lastInferenceQueuePtr, message.lastInferenceQueuePtr = $util.newBuffer($util.base64.length(object.lastInferenceQueuePtr)), 0);
                else if (object.lastInferenceQueuePtr.length >= 0)
                    message.lastInferenceQueuePtr = object.lastInferenceQueuePtr;
            if (object.buses) {
                if (!Array.isArray(object.buses))
                    throw TypeError(".st3215.InferenceState.buses: array expected");
                message.buses = [];
                for (let i = 0; i < object.buses.length; ++i) {
                    if (typeof object.buses[i] !== "object")
                        throw TypeError(".st3215.InferenceState.buses: object expected");
                    message.buses[i] = $root.st3215.InferenceState.BusState.fromObject(object.buses[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from an InferenceState message. Also converts values to other types if specified.
         * @function toObject
         * @memberof st3215.InferenceState
         * @static
         * @param {st3215.InferenceState} message InferenceState
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        InferenceState.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.buses = [];
            if (options.defaults)
                if (options.bytes === String)
                    object.lastInferenceQueuePtr = "";
                else {
                    object.lastInferenceQueuePtr = [];
                    if (options.bytes !== Array)
                        object.lastInferenceQueuePtr = $util.newBuffer(object.lastInferenceQueuePtr);
                }
            if (message.lastInferenceQueuePtr != null && message.hasOwnProperty("lastInferenceQueuePtr"))
                object.lastInferenceQueuePtr = options.bytes === String ? $util.base64.encode(message.lastInferenceQueuePtr, 0, message.lastInferenceQueuePtr.length) : options.bytes === Array ? Array.prototype.slice.call(message.lastInferenceQueuePtr) : message.lastInferenceQueuePtr;
            if (message.buses && message.buses.length) {
                object.buses = [];
                for (let j = 0; j < message.buses.length; ++j)
                    object.buses[j] = $root.st3215.InferenceState.BusState.toObject(message.buses[j], options);
            }
            return object;
        };

        /**
         * Converts this InferenceState to JSON.
         * @function toJSON
         * @memberof st3215.InferenceState
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        InferenceState.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for InferenceState
         * @function getTypeUrl
         * @memberof st3215.InferenceState
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        InferenceState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/st3215.InferenceState";
        };

        InferenceState.MotorState = (function() {

            /**
             * Properties of a MotorState.
             * @memberof st3215.InferenceState
             * @interface IMotorState
             * @property {number|null} [id] MotorState id
             * @property {Uint8Array|null} [rxPointer] MotorState rxPointer
             * @property {Long|null} [monotonicStampNs] MotorState monotonicStampNs
             * @property {Long|null} [systemStampNs] MotorState systemStampNs
             * @property {Long|null} [appStartId] MotorState appStartId
             * @property {Uint8Array|null} [state] MotorState state
             * @property {st3215.IST3215Error|null} [error] MotorState error
             * @property {number|null} [rangeMin] MotorState rangeMin
             * @property {number|null} [rangeMax] MotorState rangeMax
             * @property {boolean|null} [rangeFreezed] MotorState rangeFreezed
             * @property {st3215.IInferenceCommandState|null} [lastCommand] MotorState lastCommand
             */

            /**
             * Constructs a new MotorState.
             * @memberof st3215.InferenceState
             * @classdesc Represents a MotorState.
             * @implements IMotorState
             * @constructor
             * @param {st3215.InferenceState.IMotorState=} [properties] Properties to set
             */
            function MotorState(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * MotorState id.
             * @member {number} id
             * @memberof st3215.InferenceState.MotorState
             * @instance
             */
            MotorState.prototype.id = 0;

            /**
             * MotorState rxPointer.
             * @member {Uint8Array} rxPointer
             * @memberof st3215.InferenceState.MotorState
             * @instance
             */
            MotorState.prototype.rxPointer = $util.newBuffer([]);

            /**
             * MotorState monotonicStampNs.
             * @member {Long} monotonicStampNs
             * @memberof st3215.InferenceState.MotorState
             * @instance
             */
            MotorState.prototype.monotonicStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

            /**
             * MotorState systemStampNs.
             * @member {Long} systemStampNs
             * @memberof st3215.InferenceState.MotorState
             * @instance
             */
            MotorState.prototype.systemStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

            /**
             * MotorState appStartId.
             * @member {Long} appStartId
             * @memberof st3215.InferenceState.MotorState
             * @instance
             */
            MotorState.prototype.appStartId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

            /**
             * MotorState state.
             * @member {Uint8Array} state
             * @memberof st3215.InferenceState.MotorState
             * @instance
             */
            MotorState.prototype.state = $util.newBuffer([]);

            /**
             * MotorState error.
             * @member {st3215.IST3215Error|null|undefined} error
             * @memberof st3215.InferenceState.MotorState
             * @instance
             */
            MotorState.prototype.error = null;

            /**
             * MotorState rangeMin.
             * @member {number} rangeMin
             * @memberof st3215.InferenceState.MotorState
             * @instance
             */
            MotorState.prototype.rangeMin = 0;

            /**
             * MotorState rangeMax.
             * @member {number} rangeMax
             * @memberof st3215.InferenceState.MotorState
             * @instance
             */
            MotorState.prototype.rangeMax = 0;

            /**
             * MotorState rangeFreezed.
             * @member {boolean} rangeFreezed
             * @memberof st3215.InferenceState.MotorState
             * @instance
             */
            MotorState.prototype.rangeFreezed = false;

            /**
             * MotorState lastCommand.
             * @member {st3215.IInferenceCommandState|null|undefined} lastCommand
             * @memberof st3215.InferenceState.MotorState
             * @instance
             */
            MotorState.prototype.lastCommand = null;

            /**
             * Creates a new MotorState instance using the specified properties.
             * @function create
             * @memberof st3215.InferenceState.MotorState
             * @static
             * @param {st3215.InferenceState.IMotorState=} [properties] Properties to set
             * @returns {st3215.InferenceState.MotorState} MotorState instance
             */
            MotorState.create = function create(properties) {
                return new MotorState(properties);
            };

            /**
             * Encodes the specified MotorState message. Does not implicitly {@link st3215.InferenceState.MotorState.verify|verify} messages.
             * @function encode
             * @memberof st3215.InferenceState.MotorState
             * @static
             * @param {st3215.InferenceState.IMotorState} message MotorState message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MotorState.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                    writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.id);
                if (message.rxPointer != null && Object.hasOwnProperty.call(message, "rxPointer"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.rxPointer);
                if (message.monotonicStampNs != null && Object.hasOwnProperty.call(message, "monotonicStampNs"))
                    writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.monotonicStampNs);
                if (message.systemStampNs != null && Object.hasOwnProperty.call(message, "systemStampNs"))
                    writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.systemStampNs);
                if (message.state != null && Object.hasOwnProperty.call(message, "state"))
                    writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.state);
                if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                    $root.st3215.ST3215Error.encode(message.error, writer.uint32(/* id 6, wireType 2 =*/50).fork()).ldelim();
                if (message.appStartId != null && Object.hasOwnProperty.call(message, "appStartId"))
                    writer.uint32(/* id 7, wireType 0 =*/56).uint64(message.appStartId);
                if (message.rangeMin != null && Object.hasOwnProperty.call(message, "rangeMin"))
                    writer.uint32(/* id 10, wireType 0 =*/80).uint32(message.rangeMin);
                if (message.rangeMax != null && Object.hasOwnProperty.call(message, "rangeMax"))
                    writer.uint32(/* id 11, wireType 0 =*/88).uint32(message.rangeMax);
                if (message.lastCommand != null && Object.hasOwnProperty.call(message, "lastCommand"))
                    $root.st3215.InferenceCommandState.encode(message.lastCommand, writer.uint32(/* id 12, wireType 2 =*/98).fork()).ldelim();
                if (message.rangeFreezed != null && Object.hasOwnProperty.call(message, "rangeFreezed"))
                    writer.uint32(/* id 14, wireType 0 =*/112).bool(message.rangeFreezed);
                return writer;
            };

            /**
             * Encodes the specified MotorState message, length delimited. Does not implicitly {@link st3215.InferenceState.MotorState.verify|verify} messages.
             * @function encodeDelimited
             * @memberof st3215.InferenceState.MotorState
             * @static
             * @param {st3215.InferenceState.IMotorState} message MotorState message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MotorState.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a MotorState message from the specified reader or buffer.
             * @function decode
             * @memberof st3215.InferenceState.MotorState
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {st3215.InferenceState.MotorState} MotorState
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MotorState.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.st3215.InferenceState.MotorState();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.id = reader.uint32();
                            break;
                        }
                    case 2: {
                            message.rxPointer = reader.bytes();
                            break;
                        }
                    case 3: {
                            message.monotonicStampNs = reader.uint64();
                            break;
                        }
                    case 4: {
                            message.systemStampNs = reader.uint64();
                            break;
                        }
                    case 7: {
                            message.appStartId = reader.uint64();
                            break;
                        }
                    case 5: {
                            message.state = reader.bytes();
                            break;
                        }
                    case 6: {
                            message.error = $root.st3215.ST3215Error.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 10: {
                            message.rangeMin = reader.uint32();
                            break;
                        }
                    case 11: {
                            message.rangeMax = reader.uint32();
                            break;
                        }
                    case 14: {
                            message.rangeFreezed = reader.bool();
                            break;
                        }
                    case 12: {
                            message.lastCommand = $root.st3215.InferenceCommandState.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a MotorState message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof st3215.InferenceState.MotorState
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {st3215.InferenceState.MotorState} MotorState
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MotorState.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a MotorState message.
             * @function verify
             * @memberof st3215.InferenceState.MotorState
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            MotorState.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.id != null && message.hasOwnProperty("id"))
                    if (!$util.isInteger(message.id))
                        return "id: integer expected";
                if (message.rxPointer != null && message.hasOwnProperty("rxPointer"))
                    if (!(message.rxPointer && typeof message.rxPointer.length === "number" || $util.isString(message.rxPointer)))
                        return "rxPointer: buffer expected";
                if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                    if (!$util.isInteger(message.monotonicStampNs) && !(message.monotonicStampNs && $util.isInteger(message.monotonicStampNs.low) && $util.isInteger(message.monotonicStampNs.high)))
                        return "monotonicStampNs: integer|Long expected";
                if (message.systemStampNs != null && message.hasOwnProperty("systemStampNs"))
                    if (!$util.isInteger(message.systemStampNs) && !(message.systemStampNs && $util.isInteger(message.systemStampNs.low) && $util.isInteger(message.systemStampNs.high)))
                        return "systemStampNs: integer|Long expected";
                if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                    if (!$util.isInteger(message.appStartId) && !(message.appStartId && $util.isInteger(message.appStartId.low) && $util.isInteger(message.appStartId.high)))
                        return "appStartId: integer|Long expected";
                if (message.state != null && message.hasOwnProperty("state"))
                    if (!(message.state && typeof message.state.length === "number" || $util.isString(message.state)))
                        return "state: buffer expected";
                if (message.error != null && message.hasOwnProperty("error")) {
                    let error = $root.st3215.ST3215Error.verify(message.error, long + 1);
                    if (error)
                        return "error." + error;
                }
                if (message.rangeMin != null && message.hasOwnProperty("rangeMin"))
                    if (!$util.isInteger(message.rangeMin))
                        return "rangeMin: integer expected";
                if (message.rangeMax != null && message.hasOwnProperty("rangeMax"))
                    if (!$util.isInteger(message.rangeMax))
                        return "rangeMax: integer expected";
                if (message.rangeFreezed != null && message.hasOwnProperty("rangeFreezed"))
                    if (typeof message.rangeFreezed !== "boolean")
                        return "rangeFreezed: boolean expected";
                if (message.lastCommand != null && message.hasOwnProperty("lastCommand")) {
                    let error = $root.st3215.InferenceCommandState.verify(message.lastCommand, long + 1);
                    if (error)
                        return "lastCommand." + error;
                }
                return null;
            };

            /**
             * Creates a MotorState message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof st3215.InferenceState.MotorState
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {st3215.InferenceState.MotorState} MotorState
             */
            MotorState.fromObject = function fromObject(object, long) {
                if (object instanceof $root.st3215.InferenceState.MotorState)
                    return object;
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.st3215.InferenceState.MotorState();
                if (object.id != null)
                    message.id = object.id >>> 0;
                if (object.rxPointer != null)
                    if (typeof object.rxPointer === "string")
                        $util.base64.decode(object.rxPointer, message.rxPointer = $util.newBuffer($util.base64.length(object.rxPointer)), 0);
                    else if (object.rxPointer.length >= 0)
                        message.rxPointer = object.rxPointer;
                if (object.monotonicStampNs != null)
                    if ($util.Long)
                        (message.monotonicStampNs = $util.Long.fromValue(object.monotonicStampNs)).unsigned = true;
                    else if (typeof object.monotonicStampNs === "string")
                        message.monotonicStampNs = parseInt(object.monotonicStampNs, 10);
                    else if (typeof object.monotonicStampNs === "number")
                        message.monotonicStampNs = object.monotonicStampNs;
                    else if (typeof object.monotonicStampNs === "object")
                        message.monotonicStampNs = new $util.LongBits(object.monotonicStampNs.low >>> 0, object.monotonicStampNs.high >>> 0).toNumber(true);
                if (object.systemStampNs != null)
                    if ($util.Long)
                        (message.systemStampNs = $util.Long.fromValue(object.systemStampNs)).unsigned = true;
                    else if (typeof object.systemStampNs === "string")
                        message.systemStampNs = parseInt(object.systemStampNs, 10);
                    else if (typeof object.systemStampNs === "number")
                        message.systemStampNs = object.systemStampNs;
                    else if (typeof object.systemStampNs === "object")
                        message.systemStampNs = new $util.LongBits(object.systemStampNs.low >>> 0, object.systemStampNs.high >>> 0).toNumber(true);
                if (object.appStartId != null)
                    if ($util.Long)
                        (message.appStartId = $util.Long.fromValue(object.appStartId)).unsigned = true;
                    else if (typeof object.appStartId === "string")
                        message.appStartId = parseInt(object.appStartId, 10);
                    else if (typeof object.appStartId === "number")
                        message.appStartId = object.appStartId;
                    else if (typeof object.appStartId === "object")
                        message.appStartId = new $util.LongBits(object.appStartId.low >>> 0, object.appStartId.high >>> 0).toNumber(true);
                if (object.state != null)
                    if (typeof object.state === "string")
                        $util.base64.decode(object.state, message.state = $util.newBuffer($util.base64.length(object.state)), 0);
                    else if (object.state.length >= 0)
                        message.state = object.state;
                if (object.error != null) {
                    if (typeof object.error !== "object")
                        throw TypeError(".st3215.InferenceState.MotorState.error: object expected");
                    message.error = $root.st3215.ST3215Error.fromObject(object.error, long + 1);
                }
                if (object.rangeMin != null)
                    message.rangeMin = object.rangeMin >>> 0;
                if (object.rangeMax != null)
                    message.rangeMax = object.rangeMax >>> 0;
                if (object.rangeFreezed != null)
                    message.rangeFreezed = Boolean(object.rangeFreezed);
                if (object.lastCommand != null) {
                    if (typeof object.lastCommand !== "object")
                        throw TypeError(".st3215.InferenceState.MotorState.lastCommand: object expected");
                    message.lastCommand = $root.st3215.InferenceCommandState.fromObject(object.lastCommand, long + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a MotorState message. Also converts values to other types if specified.
             * @function toObject
             * @memberof st3215.InferenceState.MotorState
             * @static
             * @param {st3215.InferenceState.MotorState} message MotorState
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            MotorState.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.id = 0;
                    if (options.bytes === String)
                        object.rxPointer = "";
                    else {
                        object.rxPointer = [];
                        if (options.bytes !== Array)
                            object.rxPointer = $util.newBuffer(object.rxPointer);
                    }
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, true);
                        object.monotonicStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                    } else
                        object.monotonicStampNs = options.longs === String ? "0" : 0;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, true);
                        object.systemStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                    } else
                        object.systemStampNs = options.longs === String ? "0" : 0;
                    if (options.bytes === String)
                        object.state = "";
                    else {
                        object.state = [];
                        if (options.bytes !== Array)
                            object.state = $util.newBuffer(object.state);
                    }
                    object.error = null;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, true);
                        object.appStartId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                    } else
                        object.appStartId = options.longs === String ? "0" : 0;
                    object.rangeMin = 0;
                    object.rangeMax = 0;
                    object.lastCommand = null;
                    object.rangeFreezed = false;
                }
                if (message.id != null && message.hasOwnProperty("id"))
                    object.id = message.id;
                if (message.rxPointer != null && message.hasOwnProperty("rxPointer"))
                    object.rxPointer = options.bytes === String ? $util.base64.encode(message.rxPointer, 0, message.rxPointer.length) : options.bytes === Array ? Array.prototype.slice.call(message.rxPointer) : message.rxPointer;
                if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                    if (typeof message.monotonicStampNs === "number")
                        object.monotonicStampNs = options.longs === String ? String(message.monotonicStampNs) : message.monotonicStampNs;
                    else
                        object.monotonicStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.monotonicStampNs) : options.longs === Number ? new $util.LongBits(message.monotonicStampNs.low >>> 0, message.monotonicStampNs.high >>> 0).toNumber(true) : message.monotonicStampNs;
                if (message.systemStampNs != null && message.hasOwnProperty("systemStampNs"))
                    if (typeof message.systemStampNs === "number")
                        object.systemStampNs = options.longs === String ? String(message.systemStampNs) : message.systemStampNs;
                    else
                        object.systemStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.systemStampNs) : options.longs === Number ? new $util.LongBits(message.systemStampNs.low >>> 0, message.systemStampNs.high >>> 0).toNumber(true) : message.systemStampNs;
                if (message.state != null && message.hasOwnProperty("state"))
                    object.state = options.bytes === String ? $util.base64.encode(message.state, 0, message.state.length) : options.bytes === Array ? Array.prototype.slice.call(message.state) : message.state;
                if (message.error != null && message.hasOwnProperty("error"))
                    object.error = $root.st3215.ST3215Error.toObject(message.error, options);
                if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                    if (typeof message.appStartId === "number")
                        object.appStartId = options.longs === String ? String(message.appStartId) : message.appStartId;
                    else
                        object.appStartId = options.longs === String ? $util.Long.prototype.toString.call(message.appStartId) : options.longs === Number ? new $util.LongBits(message.appStartId.low >>> 0, message.appStartId.high >>> 0).toNumber(true) : message.appStartId;
                if (message.rangeMin != null && message.hasOwnProperty("rangeMin"))
                    object.rangeMin = message.rangeMin;
                if (message.rangeMax != null && message.hasOwnProperty("rangeMax"))
                    object.rangeMax = message.rangeMax;
                if (message.lastCommand != null && message.hasOwnProperty("lastCommand"))
                    object.lastCommand = $root.st3215.InferenceCommandState.toObject(message.lastCommand, options);
                if (message.rangeFreezed != null && message.hasOwnProperty("rangeFreezed"))
                    object.rangeFreezed = message.rangeFreezed;
                return object;
            };

            /**
             * Converts this MotorState to JSON.
             * @function toJSON
             * @memberof st3215.InferenceState.MotorState
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            MotorState.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for MotorState
             * @function getTypeUrl
             * @memberof st3215.InferenceState.MotorState
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            MotorState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/st3215.InferenceState.MotorState";
            };

            return MotorState;
        })();

        InferenceState.BusState = (function() {

            /**
             * Properties of a BusState.
             * @memberof st3215.InferenceState
             * @interface IBusState
             * @property {st3215.IST3215Bus|null} [bus] BusState bus
             * @property {Long|null} [monotonicStampNs] BusState monotonicStampNs
             * @property {Long|null} [systemStampNs] BusState systemStampNs
             * @property {Long|null} [appStartId] BusState appStartId
             * @property {Array.<st3215.InferenceState.IMotorState>|null} [motors] BusState motors
             * @property {st3215.IAutoCalibrationState|null} [autoCalibration] BusState autoCalibration
             */

            /**
             * Constructs a new BusState.
             * @memberof st3215.InferenceState
             * @classdesc Represents a BusState.
             * @implements IBusState
             * @constructor
             * @param {st3215.InferenceState.IBusState=} [properties] Properties to set
             */
            function BusState(properties) {
                this.motors = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * BusState bus.
             * @member {st3215.IST3215Bus|null|undefined} bus
             * @memberof st3215.InferenceState.BusState
             * @instance
             */
            BusState.prototype.bus = null;

            /**
             * BusState monotonicStampNs.
             * @member {Long} monotonicStampNs
             * @memberof st3215.InferenceState.BusState
             * @instance
             */
            BusState.prototype.monotonicStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

            /**
             * BusState systemStampNs.
             * @member {Long} systemStampNs
             * @memberof st3215.InferenceState.BusState
             * @instance
             */
            BusState.prototype.systemStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

            /**
             * BusState appStartId.
             * @member {Long} appStartId
             * @memberof st3215.InferenceState.BusState
             * @instance
             */
            BusState.prototype.appStartId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

            /**
             * BusState motors.
             * @member {Array.<st3215.InferenceState.IMotorState>} motors
             * @memberof st3215.InferenceState.BusState
             * @instance
             */
            BusState.prototype.motors = $util.emptyArray;

            /**
             * BusState autoCalibration.
             * @member {st3215.IAutoCalibrationState|null|undefined} autoCalibration
             * @memberof st3215.InferenceState.BusState
             * @instance
             */
            BusState.prototype.autoCalibration = null;

            /**
             * Creates a new BusState instance using the specified properties.
             * @function create
             * @memberof st3215.InferenceState.BusState
             * @static
             * @param {st3215.InferenceState.IBusState=} [properties] Properties to set
             * @returns {st3215.InferenceState.BusState} BusState instance
             */
            BusState.create = function create(properties) {
                return new BusState(properties);
            };

            /**
             * Encodes the specified BusState message. Does not implicitly {@link st3215.InferenceState.BusState.verify|verify} messages.
             * @function encode
             * @memberof st3215.InferenceState.BusState
             * @static
             * @param {st3215.InferenceState.IBusState} message BusState message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BusState.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.bus != null && Object.hasOwnProperty.call(message, "bus"))
                    $root.st3215.ST3215Bus.encode(message.bus, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
                if (message.monotonicStampNs != null && Object.hasOwnProperty.call(message, "monotonicStampNs"))
                    writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.monotonicStampNs);
                if (message.systemStampNs != null && Object.hasOwnProperty.call(message, "systemStampNs"))
                    writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.systemStampNs);
                if (message.appStartId != null && Object.hasOwnProperty.call(message, "appStartId"))
                    writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.appStartId);
                if (message.motors != null && message.motors.length)
                    for (let i = 0; i < message.motors.length; ++i)
                        $root.st3215.InferenceState.MotorState.encode(message.motors[i], writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
                if (message.autoCalibration != null && Object.hasOwnProperty.call(message, "autoCalibration"))
                    $root.st3215.AutoCalibrationState.encode(message.autoCalibration, writer.uint32(/* id 20, wireType 2 =*/162).fork()).ldelim();
                return writer;
            };

            /**
             * Encodes the specified BusState message, length delimited. Does not implicitly {@link st3215.InferenceState.BusState.verify|verify} messages.
             * @function encodeDelimited
             * @memberof st3215.InferenceState.BusState
             * @static
             * @param {st3215.InferenceState.IBusState} message BusState message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BusState.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a BusState message from the specified reader or buffer.
             * @function decode
             * @memberof st3215.InferenceState.BusState
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {st3215.InferenceState.BusState} BusState
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BusState.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.st3215.InferenceState.BusState();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.bus = $root.st3215.ST3215Bus.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 2: {
                            message.monotonicStampNs = reader.uint64();
                            break;
                        }
                    case 3: {
                            message.systemStampNs = reader.uint64();
                            break;
                        }
                    case 4: {
                            message.appStartId = reader.uint64();
                            break;
                        }
                    case 10: {
                            if (!(message.motors && message.motors.length))
                                message.motors = [];
                            message.motors.push($root.st3215.InferenceState.MotorState.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    case 20: {
                            message.autoCalibration = $root.st3215.AutoCalibrationState.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a BusState message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof st3215.InferenceState.BusState
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {st3215.InferenceState.BusState} BusState
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BusState.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a BusState message.
             * @function verify
             * @memberof st3215.InferenceState.BusState
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            BusState.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.bus != null && message.hasOwnProperty("bus")) {
                    let error = $root.st3215.ST3215Bus.verify(message.bus, long + 1);
                    if (error)
                        return "bus." + error;
                }
                if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                    if (!$util.isInteger(message.monotonicStampNs) && !(message.monotonicStampNs && $util.isInteger(message.monotonicStampNs.low) && $util.isInteger(message.monotonicStampNs.high)))
                        return "monotonicStampNs: integer|Long expected";
                if (message.systemStampNs != null && message.hasOwnProperty("systemStampNs"))
                    if (!$util.isInteger(message.systemStampNs) && !(message.systemStampNs && $util.isInteger(message.systemStampNs.low) && $util.isInteger(message.systemStampNs.high)))
                        return "systemStampNs: integer|Long expected";
                if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                    if (!$util.isInteger(message.appStartId) && !(message.appStartId && $util.isInteger(message.appStartId.low) && $util.isInteger(message.appStartId.high)))
                        return "appStartId: integer|Long expected";
                if (message.motors != null && message.hasOwnProperty("motors")) {
                    if (!Array.isArray(message.motors))
                        return "motors: array expected";
                    for (let i = 0; i < message.motors.length; ++i) {
                        let error = $root.st3215.InferenceState.MotorState.verify(message.motors[i], long + 1);
                        if (error)
                            return "motors." + error;
                    }
                }
                if (message.autoCalibration != null && message.hasOwnProperty("autoCalibration")) {
                    let error = $root.st3215.AutoCalibrationState.verify(message.autoCalibration, long + 1);
                    if (error)
                        return "autoCalibration." + error;
                }
                return null;
            };

            /**
             * Creates a BusState message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof st3215.InferenceState.BusState
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {st3215.InferenceState.BusState} BusState
             */
            BusState.fromObject = function fromObject(object, long) {
                if (object instanceof $root.st3215.InferenceState.BusState)
                    return object;
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.st3215.InferenceState.BusState();
                if (object.bus != null) {
                    if (typeof object.bus !== "object")
                        throw TypeError(".st3215.InferenceState.BusState.bus: object expected");
                    message.bus = $root.st3215.ST3215Bus.fromObject(object.bus, long + 1);
                }
                if (object.monotonicStampNs != null)
                    if ($util.Long)
                        (message.monotonicStampNs = $util.Long.fromValue(object.monotonicStampNs)).unsigned = true;
                    else if (typeof object.monotonicStampNs === "string")
                        message.monotonicStampNs = parseInt(object.monotonicStampNs, 10);
                    else if (typeof object.monotonicStampNs === "number")
                        message.monotonicStampNs = object.monotonicStampNs;
                    else if (typeof object.monotonicStampNs === "object")
                        message.monotonicStampNs = new $util.LongBits(object.monotonicStampNs.low >>> 0, object.monotonicStampNs.high >>> 0).toNumber(true);
                if (object.systemStampNs != null)
                    if ($util.Long)
                        (message.systemStampNs = $util.Long.fromValue(object.systemStampNs)).unsigned = true;
                    else if (typeof object.systemStampNs === "string")
                        message.systemStampNs = parseInt(object.systemStampNs, 10);
                    else if (typeof object.systemStampNs === "number")
                        message.systemStampNs = object.systemStampNs;
                    else if (typeof object.systemStampNs === "object")
                        message.systemStampNs = new $util.LongBits(object.systemStampNs.low >>> 0, object.systemStampNs.high >>> 0).toNumber(true);
                if (object.appStartId != null)
                    if ($util.Long)
                        (message.appStartId = $util.Long.fromValue(object.appStartId)).unsigned = true;
                    else if (typeof object.appStartId === "string")
                        message.appStartId = parseInt(object.appStartId, 10);
                    else if (typeof object.appStartId === "number")
                        message.appStartId = object.appStartId;
                    else if (typeof object.appStartId === "object")
                        message.appStartId = new $util.LongBits(object.appStartId.low >>> 0, object.appStartId.high >>> 0).toNumber(true);
                if (object.motors) {
                    if (!Array.isArray(object.motors))
                        throw TypeError(".st3215.InferenceState.BusState.motors: array expected");
                    message.motors = [];
                    for (let i = 0; i < object.motors.length; ++i) {
                        if (typeof object.motors[i] !== "object")
                            throw TypeError(".st3215.InferenceState.BusState.motors: object expected");
                        message.motors[i] = $root.st3215.InferenceState.MotorState.fromObject(object.motors[i], long + 1);
                    }
                }
                if (object.autoCalibration != null) {
                    if (typeof object.autoCalibration !== "object")
                        throw TypeError(".st3215.InferenceState.BusState.autoCalibration: object expected");
                    message.autoCalibration = $root.st3215.AutoCalibrationState.fromObject(object.autoCalibration, long + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a BusState message. Also converts values to other types if specified.
             * @function toObject
             * @memberof st3215.InferenceState.BusState
             * @static
             * @param {st3215.InferenceState.BusState} message BusState
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            BusState.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.motors = [];
                if (options.defaults) {
                    object.bus = null;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, true);
                        object.monotonicStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                    } else
                        object.monotonicStampNs = options.longs === String ? "0" : 0;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, true);
                        object.systemStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                    } else
                        object.systemStampNs = options.longs === String ? "0" : 0;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, true);
                        object.appStartId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                    } else
                        object.appStartId = options.longs === String ? "0" : 0;
                    object.autoCalibration = null;
                }
                if (message.bus != null && message.hasOwnProperty("bus"))
                    object.bus = $root.st3215.ST3215Bus.toObject(message.bus, options);
                if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                    if (typeof message.monotonicStampNs === "number")
                        object.monotonicStampNs = options.longs === String ? String(message.monotonicStampNs) : message.monotonicStampNs;
                    else
                        object.monotonicStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.monotonicStampNs) : options.longs === Number ? new $util.LongBits(message.monotonicStampNs.low >>> 0, message.monotonicStampNs.high >>> 0).toNumber(true) : message.monotonicStampNs;
                if (message.systemStampNs != null && message.hasOwnProperty("systemStampNs"))
                    if (typeof message.systemStampNs === "number")
                        object.systemStampNs = options.longs === String ? String(message.systemStampNs) : message.systemStampNs;
                    else
                        object.systemStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.systemStampNs) : options.longs === Number ? new $util.LongBits(message.systemStampNs.low >>> 0, message.systemStampNs.high >>> 0).toNumber(true) : message.systemStampNs;
                if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                    if (typeof message.appStartId === "number")
                        object.appStartId = options.longs === String ? String(message.appStartId) : message.appStartId;
                    else
                        object.appStartId = options.longs === String ? $util.Long.prototype.toString.call(message.appStartId) : options.longs === Number ? new $util.LongBits(message.appStartId.low >>> 0, message.appStartId.high >>> 0).toNumber(true) : message.appStartId;
                if (message.motors && message.motors.length) {
                    object.motors = [];
                    for (let j = 0; j < message.motors.length; ++j)
                        object.motors[j] = $root.st3215.InferenceState.MotorState.toObject(message.motors[j], options);
                }
                if (message.autoCalibration != null && message.hasOwnProperty("autoCalibration"))
                    object.autoCalibration = $root.st3215.AutoCalibrationState.toObject(message.autoCalibration, options);
                return object;
            };

            /**
             * Converts this BusState to JSON.
             * @function toJSON
             * @memberof st3215.InferenceState.BusState
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            BusState.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for BusState
             * @function getTypeUrl
             * @memberof st3215.InferenceState.BusState
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            BusState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/st3215.InferenceState.BusState";
            };

            return BusState;
        })();

        return InferenceState;
    })();

    st3215.Command = (function() {

        /**
         * Properties of a Command.
         * @memberof st3215
         * @interface ICommand
         * @property {string|null} [targetBusSerial] Command targetBusSerial
         * @property {st3215.IST3215WriteCommand|null} [write] Command write
         * @property {st3215.IST3215RegWriteCommand|null} [regWrite] Command regWrite
         * @property {st3215.IST3215ActionCommand|null} [action] Command action
         * @property {st3215.IST3215ResetCommand|null} [reset] Command reset
         * @property {st3215.IResetCalibrationCommand|null} [resetCalibration] Command resetCalibration
         * @property {st3215.IFreezeCalibrationCommand|null} [freezeCalibration] Command freezeCalibration
         * @property {st3215.IST3215SyncWriteCommand|null} [syncWrite] Command syncWrite
         * @property {st3215.IAutoCalibrateCommand|null} [autoCalibrate] Command autoCalibrate
         * @property {st3215.IStopAutoCalibrateCommand|null} [stopAutoCalibrate] Command stopAutoCalibrate
         */

        /**
         * Constructs a new Command.
         * @memberof st3215
         * @classdesc Represents a Command.
         * @implements ICommand
         * @constructor
         * @param {st3215.ICommand=} [properties] Properties to set
         */
        function Command(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Command targetBusSerial.
         * @member {string} targetBusSerial
         * @memberof st3215.Command
         * @instance
         */
        Command.prototype.targetBusSerial = "";

        /**
         * Command write.
         * @member {st3215.IST3215WriteCommand|null|undefined} write
         * @memberof st3215.Command
         * @instance
         */
        Command.prototype.write = null;

        /**
         * Command regWrite.
         * @member {st3215.IST3215RegWriteCommand|null|undefined} regWrite
         * @memberof st3215.Command
         * @instance
         */
        Command.prototype.regWrite = null;

        /**
         * Command action.
         * @member {st3215.IST3215ActionCommand|null|undefined} action
         * @memberof st3215.Command
         * @instance
         */
        Command.prototype.action = null;

        /**
         * Command reset.
         * @member {st3215.IST3215ResetCommand|null|undefined} reset
         * @memberof st3215.Command
         * @instance
         */
        Command.prototype.reset = null;

        /**
         * Command resetCalibration.
         * @member {st3215.IResetCalibrationCommand|null|undefined} resetCalibration
         * @memberof st3215.Command
         * @instance
         */
        Command.prototype.resetCalibration = null;

        /**
         * Command freezeCalibration.
         * @member {st3215.IFreezeCalibrationCommand|null|undefined} freezeCalibration
         * @memberof st3215.Command
         * @instance
         */
        Command.prototype.freezeCalibration = null;

        /**
         * Command syncWrite.
         * @member {st3215.IST3215SyncWriteCommand|null|undefined} syncWrite
         * @memberof st3215.Command
         * @instance
         */
        Command.prototype.syncWrite = null;

        /**
         * Command autoCalibrate.
         * @member {st3215.IAutoCalibrateCommand|null|undefined} autoCalibrate
         * @memberof st3215.Command
         * @instance
         */
        Command.prototype.autoCalibrate = null;

        /**
         * Command stopAutoCalibrate.
         * @member {st3215.IStopAutoCalibrateCommand|null|undefined} stopAutoCalibrate
         * @memberof st3215.Command
         * @instance
         */
        Command.prototype.stopAutoCalibrate = null;

        /**
         * Creates a new Command instance using the specified properties.
         * @function create
         * @memberof st3215.Command
         * @static
         * @param {st3215.ICommand=} [properties] Properties to set
         * @returns {st3215.Command} Command instance
         */
        Command.create = function create(properties) {
            return new Command(properties);
        };

        /**
         * Encodes the specified Command message. Does not implicitly {@link st3215.Command.verify|verify} messages.
         * @function encode
         * @memberof st3215.Command
         * @static
         * @param {st3215.ICommand} message Command message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Command.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.targetBusSerial != null && Object.hasOwnProperty.call(message, "targetBusSerial"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.targetBusSerial);
            if (message.write != null && Object.hasOwnProperty.call(message, "write"))
                $root.st3215.ST3215WriteCommand.encode(message.write, writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            if (message.regWrite != null && Object.hasOwnProperty.call(message, "regWrite"))
                $root.st3215.ST3215RegWriteCommand.encode(message.regWrite, writer.uint32(/* id 11, wireType 2 =*/90).fork()).ldelim();
            if (message.action != null && Object.hasOwnProperty.call(message, "action"))
                $root.st3215.ST3215ActionCommand.encode(message.action, writer.uint32(/* id 12, wireType 2 =*/98).fork()).ldelim();
            if (message.reset != null && Object.hasOwnProperty.call(message, "reset"))
                $root.st3215.ST3215ResetCommand.encode(message.reset, writer.uint32(/* id 13, wireType 2 =*/106).fork()).ldelim();
            if (message.resetCalibration != null && Object.hasOwnProperty.call(message, "resetCalibration"))
                $root.st3215.ResetCalibrationCommand.encode(message.resetCalibration, writer.uint32(/* id 14, wireType 2 =*/114).fork()).ldelim();
            if (message.freezeCalibration != null && Object.hasOwnProperty.call(message, "freezeCalibration"))
                $root.st3215.FreezeCalibrationCommand.encode(message.freezeCalibration, writer.uint32(/* id 15, wireType 2 =*/122).fork()).ldelim();
            if (message.syncWrite != null && Object.hasOwnProperty.call(message, "syncWrite"))
                $root.st3215.ST3215SyncWriteCommand.encode(message.syncWrite, writer.uint32(/* id 16, wireType 2 =*/130).fork()).ldelim();
            if (message.autoCalibrate != null && Object.hasOwnProperty.call(message, "autoCalibrate"))
                $root.st3215.AutoCalibrateCommand.encode(message.autoCalibrate, writer.uint32(/* id 17, wireType 2 =*/138).fork()).ldelim();
            if (message.stopAutoCalibrate != null && Object.hasOwnProperty.call(message, "stopAutoCalibrate"))
                $root.st3215.StopAutoCalibrateCommand.encode(message.stopAutoCalibrate, writer.uint32(/* id 18, wireType 2 =*/146).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified Command message, length delimited. Does not implicitly {@link st3215.Command.verify|verify} messages.
         * @function encodeDelimited
         * @memberof st3215.Command
         * @static
         * @param {st3215.ICommand} message Command message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Command.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Command message from the specified reader or buffer.
         * @function decode
         * @memberof st3215.Command
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {st3215.Command} Command
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Command.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.st3215.Command();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.targetBusSerial = reader.string();
                        break;
                    }
                case 10: {
                        message.write = $root.st3215.ST3215WriteCommand.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 11: {
                        message.regWrite = $root.st3215.ST3215RegWriteCommand.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 12: {
                        message.action = $root.st3215.ST3215ActionCommand.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 13: {
                        message.reset = $root.st3215.ST3215ResetCommand.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 14: {
                        message.resetCalibration = $root.st3215.ResetCalibrationCommand.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 15: {
                        message.freezeCalibration = $root.st3215.FreezeCalibrationCommand.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 16: {
                        message.syncWrite = $root.st3215.ST3215SyncWriteCommand.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 17: {
                        message.autoCalibrate = $root.st3215.AutoCalibrateCommand.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 18: {
                        message.stopAutoCalibrate = $root.st3215.StopAutoCalibrateCommand.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Command message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof st3215.Command
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {st3215.Command} Command
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Command.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Command message.
         * @function verify
         * @memberof st3215.Command
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Command.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.targetBusSerial != null && message.hasOwnProperty("targetBusSerial"))
                if (!$util.isString(message.targetBusSerial))
                    return "targetBusSerial: string expected";
            if (message.write != null && message.hasOwnProperty("write")) {
                let error = $root.st3215.ST3215WriteCommand.verify(message.write, long + 1);
                if (error)
                    return "write." + error;
            }
            if (message.regWrite != null && message.hasOwnProperty("regWrite")) {
                let error = $root.st3215.ST3215RegWriteCommand.verify(message.regWrite, long + 1);
                if (error)
                    return "regWrite." + error;
            }
            if (message.action != null && message.hasOwnProperty("action")) {
                let error = $root.st3215.ST3215ActionCommand.verify(message.action, long + 1);
                if (error)
                    return "action." + error;
            }
            if (message.reset != null && message.hasOwnProperty("reset")) {
                let error = $root.st3215.ST3215ResetCommand.verify(message.reset, long + 1);
                if (error)
                    return "reset." + error;
            }
            if (message.resetCalibration != null && message.hasOwnProperty("resetCalibration")) {
                let error = $root.st3215.ResetCalibrationCommand.verify(message.resetCalibration, long + 1);
                if (error)
                    return "resetCalibration." + error;
            }
            if (message.freezeCalibration != null && message.hasOwnProperty("freezeCalibration")) {
                let error = $root.st3215.FreezeCalibrationCommand.verify(message.freezeCalibration, long + 1);
                if (error)
                    return "freezeCalibration." + error;
            }
            if (message.syncWrite != null && message.hasOwnProperty("syncWrite")) {
                let error = $root.st3215.ST3215SyncWriteCommand.verify(message.syncWrite, long + 1);
                if (error)
                    return "syncWrite." + error;
            }
            if (message.autoCalibrate != null && message.hasOwnProperty("autoCalibrate")) {
                let error = $root.st3215.AutoCalibrateCommand.verify(message.autoCalibrate, long + 1);
                if (error)
                    return "autoCalibrate." + error;
            }
            if (message.stopAutoCalibrate != null && message.hasOwnProperty("stopAutoCalibrate")) {
                let error = $root.st3215.StopAutoCalibrateCommand.verify(message.stopAutoCalibrate, long + 1);
                if (error)
                    return "stopAutoCalibrate." + error;
            }
            return null;
        };

        /**
         * Creates a Command message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof st3215.Command
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {st3215.Command} Command
         */
        Command.fromObject = function fromObject(object, long) {
            if (object instanceof $root.st3215.Command)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.st3215.Command();
            if (object.targetBusSerial != null)
                message.targetBusSerial = String(object.targetBusSerial);
            if (object.write != null) {
                if (typeof object.write !== "object")
                    throw TypeError(".st3215.Command.write: object expected");
                message.write = $root.st3215.ST3215WriteCommand.fromObject(object.write, long + 1);
            }
            if (object.regWrite != null) {
                if (typeof object.regWrite !== "object")
                    throw TypeError(".st3215.Command.regWrite: object expected");
                message.regWrite = $root.st3215.ST3215RegWriteCommand.fromObject(object.regWrite, long + 1);
            }
            if (object.action != null) {
                if (typeof object.action !== "object")
                    throw TypeError(".st3215.Command.action: object expected");
                message.action = $root.st3215.ST3215ActionCommand.fromObject(object.action, long + 1);
            }
            if (object.reset != null) {
                if (typeof object.reset !== "object")
                    throw TypeError(".st3215.Command.reset: object expected");
                message.reset = $root.st3215.ST3215ResetCommand.fromObject(object.reset, long + 1);
            }
            if (object.resetCalibration != null) {
                if (typeof object.resetCalibration !== "object")
                    throw TypeError(".st3215.Command.resetCalibration: object expected");
                message.resetCalibration = $root.st3215.ResetCalibrationCommand.fromObject(object.resetCalibration, long + 1);
            }
            if (object.freezeCalibration != null) {
                if (typeof object.freezeCalibration !== "object")
                    throw TypeError(".st3215.Command.freezeCalibration: object expected");
                message.freezeCalibration = $root.st3215.FreezeCalibrationCommand.fromObject(object.freezeCalibration, long + 1);
            }
            if (object.syncWrite != null) {
                if (typeof object.syncWrite !== "object")
                    throw TypeError(".st3215.Command.syncWrite: object expected");
                message.syncWrite = $root.st3215.ST3215SyncWriteCommand.fromObject(object.syncWrite, long + 1);
            }
            if (object.autoCalibrate != null) {
                if (typeof object.autoCalibrate !== "object")
                    throw TypeError(".st3215.Command.autoCalibrate: object expected");
                message.autoCalibrate = $root.st3215.AutoCalibrateCommand.fromObject(object.autoCalibrate, long + 1);
            }
            if (object.stopAutoCalibrate != null) {
                if (typeof object.stopAutoCalibrate !== "object")
                    throw TypeError(".st3215.Command.stopAutoCalibrate: object expected");
                message.stopAutoCalibrate = $root.st3215.StopAutoCalibrateCommand.fromObject(object.stopAutoCalibrate, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a Command message. Also converts values to other types if specified.
         * @function toObject
         * @memberof st3215.Command
         * @static
         * @param {st3215.Command} message Command
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Command.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.targetBusSerial = "";
                object.write = null;
                object.regWrite = null;
                object.action = null;
                object.reset = null;
                object.resetCalibration = null;
                object.freezeCalibration = null;
                object.syncWrite = null;
                object.autoCalibrate = null;
                object.stopAutoCalibrate = null;
            }
            if (message.targetBusSerial != null && message.hasOwnProperty("targetBusSerial"))
                object.targetBusSerial = message.targetBusSerial;
            if (message.write != null && message.hasOwnProperty("write"))
                object.write = $root.st3215.ST3215WriteCommand.toObject(message.write, options);
            if (message.regWrite != null && message.hasOwnProperty("regWrite"))
                object.regWrite = $root.st3215.ST3215RegWriteCommand.toObject(message.regWrite, options);
            if (message.action != null && message.hasOwnProperty("action"))
                object.action = $root.st3215.ST3215ActionCommand.toObject(message.action, options);
            if (message.reset != null && message.hasOwnProperty("reset"))
                object.reset = $root.st3215.ST3215ResetCommand.toObject(message.reset, options);
            if (message.resetCalibration != null && message.hasOwnProperty("resetCalibration"))
                object.resetCalibration = $root.st3215.ResetCalibrationCommand.toObject(message.resetCalibration, options);
            if (message.freezeCalibration != null && message.hasOwnProperty("freezeCalibration"))
                object.freezeCalibration = $root.st3215.FreezeCalibrationCommand.toObject(message.freezeCalibration, options);
            if (message.syncWrite != null && message.hasOwnProperty("syncWrite"))
                object.syncWrite = $root.st3215.ST3215SyncWriteCommand.toObject(message.syncWrite, options);
            if (message.autoCalibrate != null && message.hasOwnProperty("autoCalibrate"))
                object.autoCalibrate = $root.st3215.AutoCalibrateCommand.toObject(message.autoCalibrate, options);
            if (message.stopAutoCalibrate != null && message.hasOwnProperty("stopAutoCalibrate"))
                object.stopAutoCalibrate = $root.st3215.StopAutoCalibrateCommand.toObject(message.stopAutoCalibrate, options);
            return object;
        };

        /**
         * Converts this Command to JSON.
         * @function toJSON
         * @memberof st3215.Command
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Command.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Command
         * @function getTypeUrl
         * @memberof st3215.Command
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Command.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/st3215.Command";
        };

        return Command;
    })();

    st3215.ResetCalibrationCommand = (function() {

        /**
         * Properties of a ResetCalibrationCommand.
         * @memberof st3215
         * @interface IResetCalibrationCommand
         * @property {boolean|null} [reset] ResetCalibrationCommand reset
         */

        /**
         * Constructs a new ResetCalibrationCommand.
         * @memberof st3215
         * @classdesc Represents a ResetCalibrationCommand.
         * @implements IResetCalibrationCommand
         * @constructor
         * @param {st3215.IResetCalibrationCommand=} [properties] Properties to set
         */
        function ResetCalibrationCommand(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ResetCalibrationCommand reset.
         * @member {boolean} reset
         * @memberof st3215.ResetCalibrationCommand
         * @instance
         */
        ResetCalibrationCommand.prototype.reset = false;

        /**
         * Creates a new ResetCalibrationCommand instance using the specified properties.
         * @function create
         * @memberof st3215.ResetCalibrationCommand
         * @static
         * @param {st3215.IResetCalibrationCommand=} [properties] Properties to set
         * @returns {st3215.ResetCalibrationCommand} ResetCalibrationCommand instance
         */
        ResetCalibrationCommand.create = function create(properties) {
            return new ResetCalibrationCommand(properties);
        };

        /**
         * Encodes the specified ResetCalibrationCommand message. Does not implicitly {@link st3215.ResetCalibrationCommand.verify|verify} messages.
         * @function encode
         * @memberof st3215.ResetCalibrationCommand
         * @static
         * @param {st3215.IResetCalibrationCommand} message ResetCalibrationCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ResetCalibrationCommand.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.reset != null && Object.hasOwnProperty.call(message, "reset"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.reset);
            return writer;
        };

        /**
         * Encodes the specified ResetCalibrationCommand message, length delimited. Does not implicitly {@link st3215.ResetCalibrationCommand.verify|verify} messages.
         * @function encodeDelimited
         * @memberof st3215.ResetCalibrationCommand
         * @static
         * @param {st3215.IResetCalibrationCommand} message ResetCalibrationCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ResetCalibrationCommand.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ResetCalibrationCommand message from the specified reader or buffer.
         * @function decode
         * @memberof st3215.ResetCalibrationCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {st3215.ResetCalibrationCommand} ResetCalibrationCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ResetCalibrationCommand.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.st3215.ResetCalibrationCommand();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.reset = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ResetCalibrationCommand message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof st3215.ResetCalibrationCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {st3215.ResetCalibrationCommand} ResetCalibrationCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ResetCalibrationCommand.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ResetCalibrationCommand message.
         * @function verify
         * @memberof st3215.ResetCalibrationCommand
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ResetCalibrationCommand.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.reset != null && message.hasOwnProperty("reset"))
                if (typeof message.reset !== "boolean")
                    return "reset: boolean expected";
            return null;
        };

        /**
         * Creates a ResetCalibrationCommand message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof st3215.ResetCalibrationCommand
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {st3215.ResetCalibrationCommand} ResetCalibrationCommand
         */
        ResetCalibrationCommand.fromObject = function fromObject(object, long) {
            if (object instanceof $root.st3215.ResetCalibrationCommand)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.st3215.ResetCalibrationCommand();
            if (object.reset != null)
                message.reset = Boolean(object.reset);
            return message;
        };

        /**
         * Creates a plain object from a ResetCalibrationCommand message. Also converts values to other types if specified.
         * @function toObject
         * @memberof st3215.ResetCalibrationCommand
         * @static
         * @param {st3215.ResetCalibrationCommand} message ResetCalibrationCommand
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ResetCalibrationCommand.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.reset = false;
            if (message.reset != null && message.hasOwnProperty("reset"))
                object.reset = message.reset;
            return object;
        };

        /**
         * Converts this ResetCalibrationCommand to JSON.
         * @function toJSON
         * @memberof st3215.ResetCalibrationCommand
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ResetCalibrationCommand.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ResetCalibrationCommand
         * @function getTypeUrl
         * @memberof st3215.ResetCalibrationCommand
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ResetCalibrationCommand.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/st3215.ResetCalibrationCommand";
        };

        return ResetCalibrationCommand;
    })();

    st3215.FreezeCalibrationCommand = (function() {

        /**
         * Properties of a FreezeCalibrationCommand.
         * @memberof st3215
         * @interface IFreezeCalibrationCommand
         * @property {boolean|null} [freeze] FreezeCalibrationCommand freeze
         * @property {Array.<st3215.IFreezeMotorArc>|null} [arcs] FreezeCalibrationCommand arcs
         */

        /**
         * Constructs a new FreezeCalibrationCommand.
         * @memberof st3215
         * @classdesc Represents a FreezeCalibrationCommand.
         * @implements IFreezeCalibrationCommand
         * @constructor
         * @param {st3215.IFreezeCalibrationCommand=} [properties] Properties to set
         */
        function FreezeCalibrationCommand(properties) {
            this.arcs = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FreezeCalibrationCommand freeze.
         * @member {boolean} freeze
         * @memberof st3215.FreezeCalibrationCommand
         * @instance
         */
        FreezeCalibrationCommand.prototype.freeze = false;

        /**
         * FreezeCalibrationCommand arcs.
         * @member {Array.<st3215.IFreezeMotorArc>} arcs
         * @memberof st3215.FreezeCalibrationCommand
         * @instance
         */
        FreezeCalibrationCommand.prototype.arcs = $util.emptyArray;

        /**
         * Creates a new FreezeCalibrationCommand instance using the specified properties.
         * @function create
         * @memberof st3215.FreezeCalibrationCommand
         * @static
         * @param {st3215.IFreezeCalibrationCommand=} [properties] Properties to set
         * @returns {st3215.FreezeCalibrationCommand} FreezeCalibrationCommand instance
         */
        FreezeCalibrationCommand.create = function create(properties) {
            return new FreezeCalibrationCommand(properties);
        };

        /**
         * Encodes the specified FreezeCalibrationCommand message. Does not implicitly {@link st3215.FreezeCalibrationCommand.verify|verify} messages.
         * @function encode
         * @memberof st3215.FreezeCalibrationCommand
         * @static
         * @param {st3215.IFreezeCalibrationCommand} message FreezeCalibrationCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FreezeCalibrationCommand.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.freeze != null && Object.hasOwnProperty.call(message, "freeze"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.freeze);
            if (message.arcs != null && message.arcs.length)
                for (let i = 0; i < message.arcs.length; ++i)
                    $root.st3215.FreezeMotorArc.encode(message.arcs[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified FreezeCalibrationCommand message, length delimited. Does not implicitly {@link st3215.FreezeCalibrationCommand.verify|verify} messages.
         * @function encodeDelimited
         * @memberof st3215.FreezeCalibrationCommand
         * @static
         * @param {st3215.IFreezeCalibrationCommand} message FreezeCalibrationCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FreezeCalibrationCommand.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FreezeCalibrationCommand message from the specified reader or buffer.
         * @function decode
         * @memberof st3215.FreezeCalibrationCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {st3215.FreezeCalibrationCommand} FreezeCalibrationCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FreezeCalibrationCommand.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.st3215.FreezeCalibrationCommand();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.freeze = reader.bool();
                        break;
                    }
                case 2: {
                        if (!(message.arcs && message.arcs.length))
                            message.arcs = [];
                        message.arcs.push($root.st3215.FreezeMotorArc.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FreezeCalibrationCommand message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof st3215.FreezeCalibrationCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {st3215.FreezeCalibrationCommand} FreezeCalibrationCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FreezeCalibrationCommand.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FreezeCalibrationCommand message.
         * @function verify
         * @memberof st3215.FreezeCalibrationCommand
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FreezeCalibrationCommand.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.freeze != null && message.hasOwnProperty("freeze"))
                if (typeof message.freeze !== "boolean")
                    return "freeze: boolean expected";
            if (message.arcs != null && message.hasOwnProperty("arcs")) {
                if (!Array.isArray(message.arcs))
                    return "arcs: array expected";
                for (let i = 0; i < message.arcs.length; ++i) {
                    let error = $root.st3215.FreezeMotorArc.verify(message.arcs[i], long + 1);
                    if (error)
                        return "arcs." + error;
                }
            }
            return null;
        };

        /**
         * Creates a FreezeCalibrationCommand message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof st3215.FreezeCalibrationCommand
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {st3215.FreezeCalibrationCommand} FreezeCalibrationCommand
         */
        FreezeCalibrationCommand.fromObject = function fromObject(object, long) {
            if (object instanceof $root.st3215.FreezeCalibrationCommand)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.st3215.FreezeCalibrationCommand();
            if (object.freeze != null)
                message.freeze = Boolean(object.freeze);
            if (object.arcs) {
                if (!Array.isArray(object.arcs))
                    throw TypeError(".st3215.FreezeCalibrationCommand.arcs: array expected");
                message.arcs = [];
                for (let i = 0; i < object.arcs.length; ++i) {
                    if (typeof object.arcs[i] !== "object")
                        throw TypeError(".st3215.FreezeCalibrationCommand.arcs: object expected");
                    message.arcs[i] = $root.st3215.FreezeMotorArc.fromObject(object.arcs[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a FreezeCalibrationCommand message. Also converts values to other types if specified.
         * @function toObject
         * @memberof st3215.FreezeCalibrationCommand
         * @static
         * @param {st3215.FreezeCalibrationCommand} message FreezeCalibrationCommand
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FreezeCalibrationCommand.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.arcs = [];
            if (options.defaults)
                object.freeze = false;
            if (message.freeze != null && message.hasOwnProperty("freeze"))
                object.freeze = message.freeze;
            if (message.arcs && message.arcs.length) {
                object.arcs = [];
                for (let j = 0; j < message.arcs.length; ++j)
                    object.arcs[j] = $root.st3215.FreezeMotorArc.toObject(message.arcs[j], options);
            }
            return object;
        };

        /**
         * Converts this FreezeCalibrationCommand to JSON.
         * @function toJSON
         * @memberof st3215.FreezeCalibrationCommand
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FreezeCalibrationCommand.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FreezeCalibrationCommand
         * @function getTypeUrl
         * @memberof st3215.FreezeCalibrationCommand
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FreezeCalibrationCommand.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/st3215.FreezeCalibrationCommand";
        };

        return FreezeCalibrationCommand;
    })();

    st3215.FreezeMotorArc = (function() {

        /**
         * Properties of a FreezeMotorArc.
         * @memberof st3215
         * @interface IFreezeMotorArc
         * @property {number|null} [motorId] FreezeMotorArc motorId
         * @property {number|null} [minAngle] FreezeMotorArc minAngle
         * @property {number|null} [maxAngle] FreezeMotorArc maxAngle
         * @property {number|null} [midpoint] FreezeMotorArc midpoint
         */

        /**
         * Constructs a new FreezeMotorArc.
         * @memberof st3215
         * @classdesc Represents a FreezeMotorArc.
         * @implements IFreezeMotorArc
         * @constructor
         * @param {st3215.IFreezeMotorArc=} [properties] Properties to set
         */
        function FreezeMotorArc(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FreezeMotorArc motorId.
         * @member {number} motorId
         * @memberof st3215.FreezeMotorArc
         * @instance
         */
        FreezeMotorArc.prototype.motorId = 0;

        /**
         * FreezeMotorArc minAngle.
         * @member {number} minAngle
         * @memberof st3215.FreezeMotorArc
         * @instance
         */
        FreezeMotorArc.prototype.minAngle = 0;

        /**
         * FreezeMotorArc maxAngle.
         * @member {number} maxAngle
         * @memberof st3215.FreezeMotorArc
         * @instance
         */
        FreezeMotorArc.prototype.maxAngle = 0;

        /**
         * FreezeMotorArc midpoint.
         * @member {number} midpoint
         * @memberof st3215.FreezeMotorArc
         * @instance
         */
        FreezeMotorArc.prototype.midpoint = 0;

        /**
         * Creates a new FreezeMotorArc instance using the specified properties.
         * @function create
         * @memberof st3215.FreezeMotorArc
         * @static
         * @param {st3215.IFreezeMotorArc=} [properties] Properties to set
         * @returns {st3215.FreezeMotorArc} FreezeMotorArc instance
         */
        FreezeMotorArc.create = function create(properties) {
            return new FreezeMotorArc(properties);
        };

        /**
         * Encodes the specified FreezeMotorArc message. Does not implicitly {@link st3215.FreezeMotorArc.verify|verify} messages.
         * @function encode
         * @memberof st3215.FreezeMotorArc
         * @static
         * @param {st3215.IFreezeMotorArc} message FreezeMotorArc message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FreezeMotorArc.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.motorId != null && Object.hasOwnProperty.call(message, "motorId"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.motorId);
            if (message.minAngle != null && Object.hasOwnProperty.call(message, "minAngle"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.minAngle);
            if (message.maxAngle != null && Object.hasOwnProperty.call(message, "maxAngle"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.maxAngle);
            if (message.midpoint != null && Object.hasOwnProperty.call(message, "midpoint"))
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.midpoint);
            return writer;
        };

        /**
         * Encodes the specified FreezeMotorArc message, length delimited. Does not implicitly {@link st3215.FreezeMotorArc.verify|verify} messages.
         * @function encodeDelimited
         * @memberof st3215.FreezeMotorArc
         * @static
         * @param {st3215.IFreezeMotorArc} message FreezeMotorArc message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FreezeMotorArc.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FreezeMotorArc message from the specified reader or buffer.
         * @function decode
         * @memberof st3215.FreezeMotorArc
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {st3215.FreezeMotorArc} FreezeMotorArc
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FreezeMotorArc.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.st3215.FreezeMotorArc();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.motorId = reader.uint32();
                        break;
                    }
                case 2: {
                        message.minAngle = reader.uint32();
                        break;
                    }
                case 3: {
                        message.maxAngle = reader.uint32();
                        break;
                    }
                case 4: {
                        message.midpoint = reader.uint32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FreezeMotorArc message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof st3215.FreezeMotorArc
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {st3215.FreezeMotorArc} FreezeMotorArc
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FreezeMotorArc.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FreezeMotorArc message.
         * @function verify
         * @memberof st3215.FreezeMotorArc
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FreezeMotorArc.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.motorId != null && message.hasOwnProperty("motorId"))
                if (!$util.isInteger(message.motorId))
                    return "motorId: integer expected";
            if (message.minAngle != null && message.hasOwnProperty("minAngle"))
                if (!$util.isInteger(message.minAngle))
                    return "minAngle: integer expected";
            if (message.maxAngle != null && message.hasOwnProperty("maxAngle"))
                if (!$util.isInteger(message.maxAngle))
                    return "maxAngle: integer expected";
            if (message.midpoint != null && message.hasOwnProperty("midpoint"))
                if (!$util.isInteger(message.midpoint))
                    return "midpoint: integer expected";
            return null;
        };

        /**
         * Creates a FreezeMotorArc message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof st3215.FreezeMotorArc
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {st3215.FreezeMotorArc} FreezeMotorArc
         */
        FreezeMotorArc.fromObject = function fromObject(object, long) {
            if (object instanceof $root.st3215.FreezeMotorArc)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.st3215.FreezeMotorArc();
            if (object.motorId != null)
                message.motorId = object.motorId >>> 0;
            if (object.minAngle != null)
                message.minAngle = object.minAngle >>> 0;
            if (object.maxAngle != null)
                message.maxAngle = object.maxAngle >>> 0;
            if (object.midpoint != null)
                message.midpoint = object.midpoint >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a FreezeMotorArc message. Also converts values to other types if specified.
         * @function toObject
         * @memberof st3215.FreezeMotorArc
         * @static
         * @param {st3215.FreezeMotorArc} message FreezeMotorArc
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FreezeMotorArc.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.motorId = 0;
                object.minAngle = 0;
                object.maxAngle = 0;
                object.midpoint = 0;
            }
            if (message.motorId != null && message.hasOwnProperty("motorId"))
                object.motorId = message.motorId;
            if (message.minAngle != null && message.hasOwnProperty("minAngle"))
                object.minAngle = message.minAngle;
            if (message.maxAngle != null && message.hasOwnProperty("maxAngle"))
                object.maxAngle = message.maxAngle;
            if (message.midpoint != null && message.hasOwnProperty("midpoint"))
                object.midpoint = message.midpoint;
            return object;
        };

        /**
         * Converts this FreezeMotorArc to JSON.
         * @function toJSON
         * @memberof st3215.FreezeMotorArc
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FreezeMotorArc.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FreezeMotorArc
         * @function getTypeUrl
         * @memberof st3215.FreezeMotorArc
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FreezeMotorArc.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/st3215.FreezeMotorArc";
        };

        return FreezeMotorArc;
    })();

    st3215.AutoCalibrateCommand = (function() {

        /**
         * Properties of an AutoCalibrateCommand.
         * @memberof st3215
         * @interface IAutoCalibrateCommand
         * @property {boolean|null} [calibrate] AutoCalibrateCommand calibrate
         */

        /**
         * Constructs a new AutoCalibrateCommand.
         * @memberof st3215
         * @classdesc Represents an AutoCalibrateCommand.
         * @implements IAutoCalibrateCommand
         * @constructor
         * @param {st3215.IAutoCalibrateCommand=} [properties] Properties to set
         */
        function AutoCalibrateCommand(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AutoCalibrateCommand calibrate.
         * @member {boolean} calibrate
         * @memberof st3215.AutoCalibrateCommand
         * @instance
         */
        AutoCalibrateCommand.prototype.calibrate = false;

        /**
         * Creates a new AutoCalibrateCommand instance using the specified properties.
         * @function create
         * @memberof st3215.AutoCalibrateCommand
         * @static
         * @param {st3215.IAutoCalibrateCommand=} [properties] Properties to set
         * @returns {st3215.AutoCalibrateCommand} AutoCalibrateCommand instance
         */
        AutoCalibrateCommand.create = function create(properties) {
            return new AutoCalibrateCommand(properties);
        };

        /**
         * Encodes the specified AutoCalibrateCommand message. Does not implicitly {@link st3215.AutoCalibrateCommand.verify|verify} messages.
         * @function encode
         * @memberof st3215.AutoCalibrateCommand
         * @static
         * @param {st3215.IAutoCalibrateCommand} message AutoCalibrateCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AutoCalibrateCommand.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.calibrate != null && Object.hasOwnProperty.call(message, "calibrate"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.calibrate);
            return writer;
        };

        /**
         * Encodes the specified AutoCalibrateCommand message, length delimited. Does not implicitly {@link st3215.AutoCalibrateCommand.verify|verify} messages.
         * @function encodeDelimited
         * @memberof st3215.AutoCalibrateCommand
         * @static
         * @param {st3215.IAutoCalibrateCommand} message AutoCalibrateCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AutoCalibrateCommand.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AutoCalibrateCommand message from the specified reader or buffer.
         * @function decode
         * @memberof st3215.AutoCalibrateCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {st3215.AutoCalibrateCommand} AutoCalibrateCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AutoCalibrateCommand.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.st3215.AutoCalibrateCommand();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.calibrate = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AutoCalibrateCommand message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof st3215.AutoCalibrateCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {st3215.AutoCalibrateCommand} AutoCalibrateCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AutoCalibrateCommand.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AutoCalibrateCommand message.
         * @function verify
         * @memberof st3215.AutoCalibrateCommand
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AutoCalibrateCommand.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.calibrate != null && message.hasOwnProperty("calibrate"))
                if (typeof message.calibrate !== "boolean")
                    return "calibrate: boolean expected";
            return null;
        };

        /**
         * Creates an AutoCalibrateCommand message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof st3215.AutoCalibrateCommand
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {st3215.AutoCalibrateCommand} AutoCalibrateCommand
         */
        AutoCalibrateCommand.fromObject = function fromObject(object, long) {
            if (object instanceof $root.st3215.AutoCalibrateCommand)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.st3215.AutoCalibrateCommand();
            if (object.calibrate != null)
                message.calibrate = Boolean(object.calibrate);
            return message;
        };

        /**
         * Creates a plain object from an AutoCalibrateCommand message. Also converts values to other types if specified.
         * @function toObject
         * @memberof st3215.AutoCalibrateCommand
         * @static
         * @param {st3215.AutoCalibrateCommand} message AutoCalibrateCommand
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AutoCalibrateCommand.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.calibrate = false;
            if (message.calibrate != null && message.hasOwnProperty("calibrate"))
                object.calibrate = message.calibrate;
            return object;
        };

        /**
         * Converts this AutoCalibrateCommand to JSON.
         * @function toJSON
         * @memberof st3215.AutoCalibrateCommand
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AutoCalibrateCommand.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AutoCalibrateCommand
         * @function getTypeUrl
         * @memberof st3215.AutoCalibrateCommand
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AutoCalibrateCommand.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/st3215.AutoCalibrateCommand";
        };

        return AutoCalibrateCommand;
    })();

    st3215.StopAutoCalibrateCommand = (function() {

        /**
         * Properties of a StopAutoCalibrateCommand.
         * @memberof st3215
         * @interface IStopAutoCalibrateCommand
         * @property {boolean|null} [stop] StopAutoCalibrateCommand stop
         */

        /**
         * Constructs a new StopAutoCalibrateCommand.
         * @memberof st3215
         * @classdesc Represents a StopAutoCalibrateCommand.
         * @implements IStopAutoCalibrateCommand
         * @constructor
         * @param {st3215.IStopAutoCalibrateCommand=} [properties] Properties to set
         */
        function StopAutoCalibrateCommand(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * StopAutoCalibrateCommand stop.
         * @member {boolean} stop
         * @memberof st3215.StopAutoCalibrateCommand
         * @instance
         */
        StopAutoCalibrateCommand.prototype.stop = false;

        /**
         * Creates a new StopAutoCalibrateCommand instance using the specified properties.
         * @function create
         * @memberof st3215.StopAutoCalibrateCommand
         * @static
         * @param {st3215.IStopAutoCalibrateCommand=} [properties] Properties to set
         * @returns {st3215.StopAutoCalibrateCommand} StopAutoCalibrateCommand instance
         */
        StopAutoCalibrateCommand.create = function create(properties) {
            return new StopAutoCalibrateCommand(properties);
        };

        /**
         * Encodes the specified StopAutoCalibrateCommand message. Does not implicitly {@link st3215.StopAutoCalibrateCommand.verify|verify} messages.
         * @function encode
         * @memberof st3215.StopAutoCalibrateCommand
         * @static
         * @param {st3215.IStopAutoCalibrateCommand} message StopAutoCalibrateCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StopAutoCalibrateCommand.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.stop != null && Object.hasOwnProperty.call(message, "stop"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.stop);
            return writer;
        };

        /**
         * Encodes the specified StopAutoCalibrateCommand message, length delimited. Does not implicitly {@link st3215.StopAutoCalibrateCommand.verify|verify} messages.
         * @function encodeDelimited
         * @memberof st3215.StopAutoCalibrateCommand
         * @static
         * @param {st3215.IStopAutoCalibrateCommand} message StopAutoCalibrateCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StopAutoCalibrateCommand.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a StopAutoCalibrateCommand message from the specified reader or buffer.
         * @function decode
         * @memberof st3215.StopAutoCalibrateCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {st3215.StopAutoCalibrateCommand} StopAutoCalibrateCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StopAutoCalibrateCommand.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.st3215.StopAutoCalibrateCommand();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.stop = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a StopAutoCalibrateCommand message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof st3215.StopAutoCalibrateCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {st3215.StopAutoCalibrateCommand} StopAutoCalibrateCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StopAutoCalibrateCommand.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a StopAutoCalibrateCommand message.
         * @function verify
         * @memberof st3215.StopAutoCalibrateCommand
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        StopAutoCalibrateCommand.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.stop != null && message.hasOwnProperty("stop"))
                if (typeof message.stop !== "boolean")
                    return "stop: boolean expected";
            return null;
        };

        /**
         * Creates a StopAutoCalibrateCommand message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof st3215.StopAutoCalibrateCommand
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {st3215.StopAutoCalibrateCommand} StopAutoCalibrateCommand
         */
        StopAutoCalibrateCommand.fromObject = function fromObject(object, long) {
            if (object instanceof $root.st3215.StopAutoCalibrateCommand)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.st3215.StopAutoCalibrateCommand();
            if (object.stop != null)
                message.stop = Boolean(object.stop);
            return message;
        };

        /**
         * Creates a plain object from a StopAutoCalibrateCommand message. Also converts values to other types if specified.
         * @function toObject
         * @memberof st3215.StopAutoCalibrateCommand
         * @static
         * @param {st3215.StopAutoCalibrateCommand} message StopAutoCalibrateCommand
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        StopAutoCalibrateCommand.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.stop = false;
            if (message.stop != null && message.hasOwnProperty("stop"))
                object.stop = message.stop;
            return object;
        };

        /**
         * Converts this StopAutoCalibrateCommand to JSON.
         * @function toJSON
         * @memberof st3215.StopAutoCalibrateCommand
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        StopAutoCalibrateCommand.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for StopAutoCalibrateCommand
         * @function getTypeUrl
         * @memberof st3215.StopAutoCalibrateCommand
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        StopAutoCalibrateCommand.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/st3215.StopAutoCalibrateCommand";
        };

        return StopAutoCalibrateCommand;
    })();

    st3215.ST3215Bus = (function() {

        /**
         * Properties of a ST3215Bus.
         * @memberof st3215
         * @interface IST3215Bus
         * @property {string|null} [portName] ST3215Bus portName
         * @property {number|null} [vid] ST3215Bus vid
         * @property {number|null} [pid] ST3215Bus pid
         * @property {string|null} [serialNumber] ST3215Bus serialNumber
         * @property {string|null} [manufacturer] ST3215Bus manufacturer
         * @property {string|null} [product] ST3215Bus product
         * @property {number|null} [portBaudRate] ST3215Bus portBaudRate
         */

        /**
         * Constructs a new ST3215Bus.
         * @memberof st3215
         * @classdesc Represents a ST3215Bus.
         * @implements IST3215Bus
         * @constructor
         * @param {st3215.IST3215Bus=} [properties] Properties to set
         */
        function ST3215Bus(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ST3215Bus portName.
         * @member {string} portName
         * @memberof st3215.ST3215Bus
         * @instance
         */
        ST3215Bus.prototype.portName = "";

        /**
         * ST3215Bus vid.
         * @member {number} vid
         * @memberof st3215.ST3215Bus
         * @instance
         */
        ST3215Bus.prototype.vid = 0;

        /**
         * ST3215Bus pid.
         * @member {number} pid
         * @memberof st3215.ST3215Bus
         * @instance
         */
        ST3215Bus.prototype.pid = 0;

        /**
         * ST3215Bus serialNumber.
         * @member {string} serialNumber
         * @memberof st3215.ST3215Bus
         * @instance
         */
        ST3215Bus.prototype.serialNumber = "";

        /**
         * ST3215Bus manufacturer.
         * @member {string} manufacturer
         * @memberof st3215.ST3215Bus
         * @instance
         */
        ST3215Bus.prototype.manufacturer = "";

        /**
         * ST3215Bus product.
         * @member {string} product
         * @memberof st3215.ST3215Bus
         * @instance
         */
        ST3215Bus.prototype.product = "";

        /**
         * ST3215Bus portBaudRate.
         * @member {number} portBaudRate
         * @memberof st3215.ST3215Bus
         * @instance
         */
        ST3215Bus.prototype.portBaudRate = 0;

        /**
         * Creates a new ST3215Bus instance using the specified properties.
         * @function create
         * @memberof st3215.ST3215Bus
         * @static
         * @param {st3215.IST3215Bus=} [properties] Properties to set
         * @returns {st3215.ST3215Bus} ST3215Bus instance
         */
        ST3215Bus.create = function create(properties) {
            return new ST3215Bus(properties);
        };

        /**
         * Encodes the specified ST3215Bus message. Does not implicitly {@link st3215.ST3215Bus.verify|verify} messages.
         * @function encode
         * @memberof st3215.ST3215Bus
         * @static
         * @param {st3215.IST3215Bus} message ST3215Bus message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ST3215Bus.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.portName != null && Object.hasOwnProperty.call(message, "portName"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.portName);
            if (message.vid != null && Object.hasOwnProperty.call(message, "vid"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.vid);
            if (message.pid != null && Object.hasOwnProperty.call(message, "pid"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.pid);
            if (message.serialNumber != null && Object.hasOwnProperty.call(message, "serialNumber"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.serialNumber);
            if (message.manufacturer != null && Object.hasOwnProperty.call(message, "manufacturer"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.manufacturer);
            if (message.product != null && Object.hasOwnProperty.call(message, "product"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.product);
            if (message.portBaudRate != null && Object.hasOwnProperty.call(message, "portBaudRate"))
                writer.uint32(/* id 7, wireType 0 =*/56).uint32(message.portBaudRate);
            return writer;
        };

        /**
         * Encodes the specified ST3215Bus message, length delimited. Does not implicitly {@link st3215.ST3215Bus.verify|verify} messages.
         * @function encodeDelimited
         * @memberof st3215.ST3215Bus
         * @static
         * @param {st3215.IST3215Bus} message ST3215Bus message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ST3215Bus.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ST3215Bus message from the specified reader or buffer.
         * @function decode
         * @memberof st3215.ST3215Bus
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {st3215.ST3215Bus} ST3215Bus
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ST3215Bus.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.st3215.ST3215Bus();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.portName = reader.string();
                        break;
                    }
                case 2: {
                        message.vid = reader.uint32();
                        break;
                    }
                case 3: {
                        message.pid = reader.uint32();
                        break;
                    }
                case 4: {
                        message.serialNumber = reader.string();
                        break;
                    }
                case 5: {
                        message.manufacturer = reader.string();
                        break;
                    }
                case 6: {
                        message.product = reader.string();
                        break;
                    }
                case 7: {
                        message.portBaudRate = reader.uint32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ST3215Bus message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof st3215.ST3215Bus
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {st3215.ST3215Bus} ST3215Bus
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ST3215Bus.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ST3215Bus message.
         * @function verify
         * @memberof st3215.ST3215Bus
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ST3215Bus.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.portName != null && message.hasOwnProperty("portName"))
                if (!$util.isString(message.portName))
                    return "portName: string expected";
            if (message.vid != null && message.hasOwnProperty("vid"))
                if (!$util.isInteger(message.vid))
                    return "vid: integer expected";
            if (message.pid != null && message.hasOwnProperty("pid"))
                if (!$util.isInteger(message.pid))
                    return "pid: integer expected";
            if (message.serialNumber != null && message.hasOwnProperty("serialNumber"))
                if (!$util.isString(message.serialNumber))
                    return "serialNumber: string expected";
            if (message.manufacturer != null && message.hasOwnProperty("manufacturer"))
                if (!$util.isString(message.manufacturer))
                    return "manufacturer: string expected";
            if (message.product != null && message.hasOwnProperty("product"))
                if (!$util.isString(message.product))
                    return "product: string expected";
            if (message.portBaudRate != null && message.hasOwnProperty("portBaudRate"))
                if (!$util.isInteger(message.portBaudRate))
                    return "portBaudRate: integer expected";
            return null;
        };

        /**
         * Creates a ST3215Bus message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof st3215.ST3215Bus
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {st3215.ST3215Bus} ST3215Bus
         */
        ST3215Bus.fromObject = function fromObject(object, long) {
            if (object instanceof $root.st3215.ST3215Bus)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.st3215.ST3215Bus();
            if (object.portName != null)
                message.portName = String(object.portName);
            if (object.vid != null)
                message.vid = object.vid >>> 0;
            if (object.pid != null)
                message.pid = object.pid >>> 0;
            if (object.serialNumber != null)
                message.serialNumber = String(object.serialNumber);
            if (object.manufacturer != null)
                message.manufacturer = String(object.manufacturer);
            if (object.product != null)
                message.product = String(object.product);
            if (object.portBaudRate != null)
                message.portBaudRate = object.portBaudRate >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a ST3215Bus message. Also converts values to other types if specified.
         * @function toObject
         * @memberof st3215.ST3215Bus
         * @static
         * @param {st3215.ST3215Bus} message ST3215Bus
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ST3215Bus.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.portName = "";
                object.vid = 0;
                object.pid = 0;
                object.serialNumber = "";
                object.manufacturer = "";
                object.product = "";
                object.portBaudRate = 0;
            }
            if (message.portName != null && message.hasOwnProperty("portName"))
                object.portName = message.portName;
            if (message.vid != null && message.hasOwnProperty("vid"))
                object.vid = message.vid;
            if (message.pid != null && message.hasOwnProperty("pid"))
                object.pid = message.pid;
            if (message.serialNumber != null && message.hasOwnProperty("serialNumber"))
                object.serialNumber = message.serialNumber;
            if (message.manufacturer != null && message.hasOwnProperty("manufacturer"))
                object.manufacturer = message.manufacturer;
            if (message.product != null && message.hasOwnProperty("product"))
                object.product = message.product;
            if (message.portBaudRate != null && message.hasOwnProperty("portBaudRate"))
                object.portBaudRate = message.portBaudRate;
            return object;
        };

        /**
         * Converts this ST3215Bus to JSON.
         * @function toJSON
         * @memberof st3215.ST3215Bus
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ST3215Bus.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ST3215Bus
         * @function getTypeUrl
         * @memberof st3215.ST3215Bus
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ST3215Bus.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/st3215.ST3215Bus";
        };

        return ST3215Bus;
    })();

    st3215.ST3215SignalData = (function() {

        /**
         * Properties of a ST3215SignalData.
         * @memberof st3215
         * @interface IST3215SignalData
         * @property {string|null} [portName] ST3215SignalData portName
         * @property {number|null} [motorId] ST3215SignalData motorId
         * @property {st3215.ST3215SignalType|null} [type] ST3215SignalData type
         * @property {number|null} [baudRate] ST3215SignalData baudRate
         * @property {Uint8Array|null} [data] ST3215SignalData data
         */

        /**
         * Constructs a new ST3215SignalData.
         * @memberof st3215
         * @classdesc Represents a ST3215SignalData.
         * @implements IST3215SignalData
         * @constructor
         * @param {st3215.IST3215SignalData=} [properties] Properties to set
         */
        function ST3215SignalData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ST3215SignalData portName.
         * @member {string} portName
         * @memberof st3215.ST3215SignalData
         * @instance
         */
        ST3215SignalData.prototype.portName = "";

        /**
         * ST3215SignalData motorId.
         * @member {number} motorId
         * @memberof st3215.ST3215SignalData
         * @instance
         */
        ST3215SignalData.prototype.motorId = 0;

        /**
         * ST3215SignalData type.
         * @member {st3215.ST3215SignalType} type
         * @memberof st3215.ST3215SignalData
         * @instance
         */
        ST3215SignalData.prototype.type = 0;

        /**
         * ST3215SignalData baudRate.
         * @member {number} baudRate
         * @memberof st3215.ST3215SignalData
         * @instance
         */
        ST3215SignalData.prototype.baudRate = 0;

        /**
         * ST3215SignalData data.
         * @member {Uint8Array} data
         * @memberof st3215.ST3215SignalData
         * @instance
         */
        ST3215SignalData.prototype.data = $util.newBuffer([]);

        /**
         * Creates a new ST3215SignalData instance using the specified properties.
         * @function create
         * @memberof st3215.ST3215SignalData
         * @static
         * @param {st3215.IST3215SignalData=} [properties] Properties to set
         * @returns {st3215.ST3215SignalData} ST3215SignalData instance
         */
        ST3215SignalData.create = function create(properties) {
            return new ST3215SignalData(properties);
        };

        /**
         * Encodes the specified ST3215SignalData message. Does not implicitly {@link st3215.ST3215SignalData.verify|verify} messages.
         * @function encode
         * @memberof st3215.ST3215SignalData
         * @static
         * @param {st3215.IST3215SignalData} message ST3215SignalData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ST3215SignalData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.portName != null && Object.hasOwnProperty.call(message, "portName"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.portName);
            if (message.motorId != null && Object.hasOwnProperty.call(message, "motorId"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.motorId);
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.type);
            if (message.baudRate != null && Object.hasOwnProperty.call(message, "baudRate"))
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.baudRate);
            if (message.data != null && Object.hasOwnProperty.call(message, "data"))
                writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.data);
            return writer;
        };

        /**
         * Encodes the specified ST3215SignalData message, length delimited. Does not implicitly {@link st3215.ST3215SignalData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof st3215.ST3215SignalData
         * @static
         * @param {st3215.IST3215SignalData} message ST3215SignalData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ST3215SignalData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ST3215SignalData message from the specified reader or buffer.
         * @function decode
         * @memberof st3215.ST3215SignalData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {st3215.ST3215SignalData} ST3215SignalData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ST3215SignalData.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.st3215.ST3215SignalData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.portName = reader.string();
                        break;
                    }
                case 2: {
                        message.motorId = reader.uint32();
                        break;
                    }
                case 3: {
                        message.type = reader.int32();
                        break;
                    }
                case 4: {
                        message.baudRate = reader.uint32();
                        break;
                    }
                case 5: {
                        message.data = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ST3215SignalData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof st3215.ST3215SignalData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {st3215.ST3215SignalData} ST3215SignalData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ST3215SignalData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ST3215SignalData message.
         * @function verify
         * @memberof st3215.ST3215SignalData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ST3215SignalData.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.portName != null && message.hasOwnProperty("portName"))
                if (!$util.isString(message.portName))
                    return "portName: string expected";
            if (message.motorId != null && message.hasOwnProperty("motorId"))
                if (!$util.isInteger(message.motorId))
                    return "motorId: integer expected";
            if (message.type != null && message.hasOwnProperty("type"))
                switch (message.type) {
                default:
                    return "type: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                case 6:
                case 7:
                case 8:
                case 9:
                case 11:
                case 12:
                    break;
                }
            if (message.baudRate != null && message.hasOwnProperty("baudRate"))
                if (!$util.isInteger(message.baudRate))
                    return "baudRate: integer expected";
            if (message.data != null && message.hasOwnProperty("data"))
                if (!(message.data && typeof message.data.length === "number" || $util.isString(message.data)))
                    return "data: buffer expected";
            return null;
        };

        /**
         * Creates a ST3215SignalData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof st3215.ST3215SignalData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {st3215.ST3215SignalData} ST3215SignalData
         */
        ST3215SignalData.fromObject = function fromObject(object, long) {
            if (object instanceof $root.st3215.ST3215SignalData)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.st3215.ST3215SignalData();
            if (object.portName != null)
                message.portName = String(object.portName);
            if (object.motorId != null)
                message.motorId = object.motorId >>> 0;
            switch (object.type) {
            default:
                if (typeof object.type === "number") {
                    message.type = object.type;
                    break;
                }
                break;
            case "ST3215_SIGNAL_TYPE_UNSPECIFIED":
            case 0:
                message.type = 0;
                break;
            case "ST3215_BUS_CONNECT":
            case 1:
                message.type = 1;
                break;
            case "ST3215_BUS_DISCONNECT":
            case 2:
                message.type = 2;
                break;
            case "ST3215_DRIVE_CONNECT":
            case 3:
                message.type = 3;
                break;
            case "ST3215_DRIVE_DISCONNECT":
            case 4:
                message.type = 4;
                break;
            case "ST3215_DRIVE_STATE":
            case 6:
                message.type = 6;
                break;
            case "ST3215_COMMAND":
            case 7:
                message.type = 7;
                break;
            case "ST3215_COMMAND_SUCCESS":
            case 8:
                message.type = 8;
                break;
            case "ST3215_ERROR":
            case 9:
                message.type = 9;
                break;
            case "ST3215_COMMAND_REJECTED":
            case 11:
                message.type = 11;
                break;
            case "ST3215_COMMAND_FAILED":
            case 12:
                message.type = 12;
                break;
            }
            if (object.baudRate != null)
                message.baudRate = object.baudRate >>> 0;
            if (object.data != null)
                if (typeof object.data === "string")
                    $util.base64.decode(object.data, message.data = $util.newBuffer($util.base64.length(object.data)), 0);
                else if (object.data.length >= 0)
                    message.data = object.data;
            return message;
        };

        /**
         * Creates a plain object from a ST3215SignalData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof st3215.ST3215SignalData
         * @static
         * @param {st3215.ST3215SignalData} message ST3215SignalData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ST3215SignalData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.portName = "";
                object.motorId = 0;
                object.type = options.enums === String ? "ST3215_SIGNAL_TYPE_UNSPECIFIED" : 0;
                object.baudRate = 0;
                if (options.bytes === String)
                    object.data = "";
                else {
                    object.data = [];
                    if (options.bytes !== Array)
                        object.data = $util.newBuffer(object.data);
                }
            }
            if (message.portName != null && message.hasOwnProperty("portName"))
                object.portName = message.portName;
            if (message.motorId != null && message.hasOwnProperty("motorId"))
                object.motorId = message.motorId;
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = options.enums === String ? $root.st3215.ST3215SignalType[message.type] === undefined ? message.type : $root.st3215.ST3215SignalType[message.type] : message.type;
            if (message.baudRate != null && message.hasOwnProperty("baudRate"))
                object.baudRate = message.baudRate;
            if (message.data != null && message.hasOwnProperty("data"))
                object.data = options.bytes === String ? $util.base64.encode(message.data, 0, message.data.length) : options.bytes === Array ? Array.prototype.slice.call(message.data) : message.data;
            return object;
        };

        /**
         * Converts this ST3215SignalData to JSON.
         * @function toJSON
         * @memberof st3215.ST3215SignalData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ST3215SignalData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ST3215SignalData
         * @function getTypeUrl
         * @memberof st3215.ST3215SignalData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ST3215SignalData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/st3215.ST3215SignalData";
        };

        return ST3215SignalData;
    })();

    st3215.ST3215WriteCommand = (function() {

        /**
         * Properties of a ST3215WriteCommand.
         * @memberof st3215
         * @interface IST3215WriteCommand
         * @property {number|null} [motorId] ST3215WriteCommand motorId
         * @property {number|null} [address] ST3215WriteCommand address
         * @property {Uint8Array|null} [value] ST3215WriteCommand value
         */

        /**
         * Constructs a new ST3215WriteCommand.
         * @memberof st3215
         * @classdesc Represents a ST3215WriteCommand.
         * @implements IST3215WriteCommand
         * @constructor
         * @param {st3215.IST3215WriteCommand=} [properties] Properties to set
         */
        function ST3215WriteCommand(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ST3215WriteCommand motorId.
         * @member {number} motorId
         * @memberof st3215.ST3215WriteCommand
         * @instance
         */
        ST3215WriteCommand.prototype.motorId = 0;

        /**
         * ST3215WriteCommand address.
         * @member {number} address
         * @memberof st3215.ST3215WriteCommand
         * @instance
         */
        ST3215WriteCommand.prototype.address = 0;

        /**
         * ST3215WriteCommand value.
         * @member {Uint8Array} value
         * @memberof st3215.ST3215WriteCommand
         * @instance
         */
        ST3215WriteCommand.prototype.value = $util.newBuffer([]);

        /**
         * Creates a new ST3215WriteCommand instance using the specified properties.
         * @function create
         * @memberof st3215.ST3215WriteCommand
         * @static
         * @param {st3215.IST3215WriteCommand=} [properties] Properties to set
         * @returns {st3215.ST3215WriteCommand} ST3215WriteCommand instance
         */
        ST3215WriteCommand.create = function create(properties) {
            return new ST3215WriteCommand(properties);
        };

        /**
         * Encodes the specified ST3215WriteCommand message. Does not implicitly {@link st3215.ST3215WriteCommand.verify|verify} messages.
         * @function encode
         * @memberof st3215.ST3215WriteCommand
         * @static
         * @param {st3215.IST3215WriteCommand} message ST3215WriteCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ST3215WriteCommand.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.motorId != null && Object.hasOwnProperty.call(message, "motorId"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.motorId);
            if (message.address != null && Object.hasOwnProperty.call(message, "address"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.address);
            if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.value);
            return writer;
        };

        /**
         * Encodes the specified ST3215WriteCommand message, length delimited. Does not implicitly {@link st3215.ST3215WriteCommand.verify|verify} messages.
         * @function encodeDelimited
         * @memberof st3215.ST3215WriteCommand
         * @static
         * @param {st3215.IST3215WriteCommand} message ST3215WriteCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ST3215WriteCommand.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ST3215WriteCommand message from the specified reader or buffer.
         * @function decode
         * @memberof st3215.ST3215WriteCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {st3215.ST3215WriteCommand} ST3215WriteCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ST3215WriteCommand.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.st3215.ST3215WriteCommand();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.motorId = reader.uint32();
                        break;
                    }
                case 2: {
                        message.address = reader.uint32();
                        break;
                    }
                case 3: {
                        message.value = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ST3215WriteCommand message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof st3215.ST3215WriteCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {st3215.ST3215WriteCommand} ST3215WriteCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ST3215WriteCommand.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ST3215WriteCommand message.
         * @function verify
         * @memberof st3215.ST3215WriteCommand
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ST3215WriteCommand.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.motorId != null && message.hasOwnProperty("motorId"))
                if (!$util.isInteger(message.motorId))
                    return "motorId: integer expected";
            if (message.address != null && message.hasOwnProperty("address"))
                if (!$util.isInteger(message.address))
                    return "address: integer expected";
            if (message.value != null && message.hasOwnProperty("value"))
                if (!(message.value && typeof message.value.length === "number" || $util.isString(message.value)))
                    return "value: buffer expected";
            return null;
        };

        /**
         * Creates a ST3215WriteCommand message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof st3215.ST3215WriteCommand
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {st3215.ST3215WriteCommand} ST3215WriteCommand
         */
        ST3215WriteCommand.fromObject = function fromObject(object, long) {
            if (object instanceof $root.st3215.ST3215WriteCommand)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.st3215.ST3215WriteCommand();
            if (object.motorId != null)
                message.motorId = object.motorId >>> 0;
            if (object.address != null)
                message.address = object.address >>> 0;
            if (object.value != null)
                if (typeof object.value === "string")
                    $util.base64.decode(object.value, message.value = $util.newBuffer($util.base64.length(object.value)), 0);
                else if (object.value.length >= 0)
                    message.value = object.value;
            return message;
        };

        /**
         * Creates a plain object from a ST3215WriteCommand message. Also converts values to other types if specified.
         * @function toObject
         * @memberof st3215.ST3215WriteCommand
         * @static
         * @param {st3215.ST3215WriteCommand} message ST3215WriteCommand
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ST3215WriteCommand.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.motorId = 0;
                object.address = 0;
                if (options.bytes === String)
                    object.value = "";
                else {
                    object.value = [];
                    if (options.bytes !== Array)
                        object.value = $util.newBuffer(object.value);
                }
            }
            if (message.motorId != null && message.hasOwnProperty("motorId"))
                object.motorId = message.motorId;
            if (message.address != null && message.hasOwnProperty("address"))
                object.address = message.address;
            if (message.value != null && message.hasOwnProperty("value"))
                object.value = options.bytes === String ? $util.base64.encode(message.value, 0, message.value.length) : options.bytes === Array ? Array.prototype.slice.call(message.value) : message.value;
            return object;
        };

        /**
         * Converts this ST3215WriteCommand to JSON.
         * @function toJSON
         * @memberof st3215.ST3215WriteCommand
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ST3215WriteCommand.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ST3215WriteCommand
         * @function getTypeUrl
         * @memberof st3215.ST3215WriteCommand
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ST3215WriteCommand.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/st3215.ST3215WriteCommand";
        };

        return ST3215WriteCommand;
    })();

    st3215.ST3215RegWriteCommand = (function() {

        /**
         * Properties of a ST3215RegWriteCommand.
         * @memberof st3215
         * @interface IST3215RegWriteCommand
         * @property {number|null} [motorId] ST3215RegWriteCommand motorId
         * @property {number|null} [address] ST3215RegWriteCommand address
         * @property {Uint8Array|null} [value] ST3215RegWriteCommand value
         */

        /**
         * Constructs a new ST3215RegWriteCommand.
         * @memberof st3215
         * @classdesc Represents a ST3215RegWriteCommand.
         * @implements IST3215RegWriteCommand
         * @constructor
         * @param {st3215.IST3215RegWriteCommand=} [properties] Properties to set
         */
        function ST3215RegWriteCommand(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ST3215RegWriteCommand motorId.
         * @member {number} motorId
         * @memberof st3215.ST3215RegWriteCommand
         * @instance
         */
        ST3215RegWriteCommand.prototype.motorId = 0;

        /**
         * ST3215RegWriteCommand address.
         * @member {number} address
         * @memberof st3215.ST3215RegWriteCommand
         * @instance
         */
        ST3215RegWriteCommand.prototype.address = 0;

        /**
         * ST3215RegWriteCommand value.
         * @member {Uint8Array} value
         * @memberof st3215.ST3215RegWriteCommand
         * @instance
         */
        ST3215RegWriteCommand.prototype.value = $util.newBuffer([]);

        /**
         * Creates a new ST3215RegWriteCommand instance using the specified properties.
         * @function create
         * @memberof st3215.ST3215RegWriteCommand
         * @static
         * @param {st3215.IST3215RegWriteCommand=} [properties] Properties to set
         * @returns {st3215.ST3215RegWriteCommand} ST3215RegWriteCommand instance
         */
        ST3215RegWriteCommand.create = function create(properties) {
            return new ST3215RegWriteCommand(properties);
        };

        /**
         * Encodes the specified ST3215RegWriteCommand message. Does not implicitly {@link st3215.ST3215RegWriteCommand.verify|verify} messages.
         * @function encode
         * @memberof st3215.ST3215RegWriteCommand
         * @static
         * @param {st3215.IST3215RegWriteCommand} message ST3215RegWriteCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ST3215RegWriteCommand.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.motorId != null && Object.hasOwnProperty.call(message, "motorId"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.motorId);
            if (message.address != null && Object.hasOwnProperty.call(message, "address"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.address);
            if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.value);
            return writer;
        };

        /**
         * Encodes the specified ST3215RegWriteCommand message, length delimited. Does not implicitly {@link st3215.ST3215RegWriteCommand.verify|verify} messages.
         * @function encodeDelimited
         * @memberof st3215.ST3215RegWriteCommand
         * @static
         * @param {st3215.IST3215RegWriteCommand} message ST3215RegWriteCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ST3215RegWriteCommand.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ST3215RegWriteCommand message from the specified reader or buffer.
         * @function decode
         * @memberof st3215.ST3215RegWriteCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {st3215.ST3215RegWriteCommand} ST3215RegWriteCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ST3215RegWriteCommand.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.st3215.ST3215RegWriteCommand();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.motorId = reader.uint32();
                        break;
                    }
                case 2: {
                        message.address = reader.uint32();
                        break;
                    }
                case 3: {
                        message.value = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ST3215RegWriteCommand message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof st3215.ST3215RegWriteCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {st3215.ST3215RegWriteCommand} ST3215RegWriteCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ST3215RegWriteCommand.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ST3215RegWriteCommand message.
         * @function verify
         * @memberof st3215.ST3215RegWriteCommand
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ST3215RegWriteCommand.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.motorId != null && message.hasOwnProperty("motorId"))
                if (!$util.isInteger(message.motorId))
                    return "motorId: integer expected";
            if (message.address != null && message.hasOwnProperty("address"))
                if (!$util.isInteger(message.address))
                    return "address: integer expected";
            if (message.value != null && message.hasOwnProperty("value"))
                if (!(message.value && typeof message.value.length === "number" || $util.isString(message.value)))
                    return "value: buffer expected";
            return null;
        };

        /**
         * Creates a ST3215RegWriteCommand message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof st3215.ST3215RegWriteCommand
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {st3215.ST3215RegWriteCommand} ST3215RegWriteCommand
         */
        ST3215RegWriteCommand.fromObject = function fromObject(object, long) {
            if (object instanceof $root.st3215.ST3215RegWriteCommand)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.st3215.ST3215RegWriteCommand();
            if (object.motorId != null)
                message.motorId = object.motorId >>> 0;
            if (object.address != null)
                message.address = object.address >>> 0;
            if (object.value != null)
                if (typeof object.value === "string")
                    $util.base64.decode(object.value, message.value = $util.newBuffer($util.base64.length(object.value)), 0);
                else if (object.value.length >= 0)
                    message.value = object.value;
            return message;
        };

        /**
         * Creates a plain object from a ST3215RegWriteCommand message. Also converts values to other types if specified.
         * @function toObject
         * @memberof st3215.ST3215RegWriteCommand
         * @static
         * @param {st3215.ST3215RegWriteCommand} message ST3215RegWriteCommand
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ST3215RegWriteCommand.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.motorId = 0;
                object.address = 0;
                if (options.bytes === String)
                    object.value = "";
                else {
                    object.value = [];
                    if (options.bytes !== Array)
                        object.value = $util.newBuffer(object.value);
                }
            }
            if (message.motorId != null && message.hasOwnProperty("motorId"))
                object.motorId = message.motorId;
            if (message.address != null && message.hasOwnProperty("address"))
                object.address = message.address;
            if (message.value != null && message.hasOwnProperty("value"))
                object.value = options.bytes === String ? $util.base64.encode(message.value, 0, message.value.length) : options.bytes === Array ? Array.prototype.slice.call(message.value) : message.value;
            return object;
        };

        /**
         * Converts this ST3215RegWriteCommand to JSON.
         * @function toJSON
         * @memberof st3215.ST3215RegWriteCommand
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ST3215RegWriteCommand.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ST3215RegWriteCommand
         * @function getTypeUrl
         * @memberof st3215.ST3215RegWriteCommand
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ST3215RegWriteCommand.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/st3215.ST3215RegWriteCommand";
        };

        return ST3215RegWriteCommand;
    })();

    st3215.ST3215ActionCommand = (function() {

        /**
         * Properties of a ST3215ActionCommand.
         * @memberof st3215
         * @interface IST3215ActionCommand
         * @property {number|null} [motorId] ST3215ActionCommand motorId
         */

        /**
         * Constructs a new ST3215ActionCommand.
         * @memberof st3215
         * @classdesc Represents a ST3215ActionCommand.
         * @implements IST3215ActionCommand
         * @constructor
         * @param {st3215.IST3215ActionCommand=} [properties] Properties to set
         */
        function ST3215ActionCommand(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ST3215ActionCommand motorId.
         * @member {number} motorId
         * @memberof st3215.ST3215ActionCommand
         * @instance
         */
        ST3215ActionCommand.prototype.motorId = 0;

        /**
         * Creates a new ST3215ActionCommand instance using the specified properties.
         * @function create
         * @memberof st3215.ST3215ActionCommand
         * @static
         * @param {st3215.IST3215ActionCommand=} [properties] Properties to set
         * @returns {st3215.ST3215ActionCommand} ST3215ActionCommand instance
         */
        ST3215ActionCommand.create = function create(properties) {
            return new ST3215ActionCommand(properties);
        };

        /**
         * Encodes the specified ST3215ActionCommand message. Does not implicitly {@link st3215.ST3215ActionCommand.verify|verify} messages.
         * @function encode
         * @memberof st3215.ST3215ActionCommand
         * @static
         * @param {st3215.IST3215ActionCommand} message ST3215ActionCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ST3215ActionCommand.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.motorId != null && Object.hasOwnProperty.call(message, "motorId"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.motorId);
            return writer;
        };

        /**
         * Encodes the specified ST3215ActionCommand message, length delimited. Does not implicitly {@link st3215.ST3215ActionCommand.verify|verify} messages.
         * @function encodeDelimited
         * @memberof st3215.ST3215ActionCommand
         * @static
         * @param {st3215.IST3215ActionCommand} message ST3215ActionCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ST3215ActionCommand.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ST3215ActionCommand message from the specified reader or buffer.
         * @function decode
         * @memberof st3215.ST3215ActionCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {st3215.ST3215ActionCommand} ST3215ActionCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ST3215ActionCommand.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.st3215.ST3215ActionCommand();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.motorId = reader.uint32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ST3215ActionCommand message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof st3215.ST3215ActionCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {st3215.ST3215ActionCommand} ST3215ActionCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ST3215ActionCommand.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ST3215ActionCommand message.
         * @function verify
         * @memberof st3215.ST3215ActionCommand
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ST3215ActionCommand.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.motorId != null && message.hasOwnProperty("motorId"))
                if (!$util.isInteger(message.motorId))
                    return "motorId: integer expected";
            return null;
        };

        /**
         * Creates a ST3215ActionCommand message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof st3215.ST3215ActionCommand
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {st3215.ST3215ActionCommand} ST3215ActionCommand
         */
        ST3215ActionCommand.fromObject = function fromObject(object, long) {
            if (object instanceof $root.st3215.ST3215ActionCommand)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.st3215.ST3215ActionCommand();
            if (object.motorId != null)
                message.motorId = object.motorId >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a ST3215ActionCommand message. Also converts values to other types if specified.
         * @function toObject
         * @memberof st3215.ST3215ActionCommand
         * @static
         * @param {st3215.ST3215ActionCommand} message ST3215ActionCommand
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ST3215ActionCommand.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.motorId = 0;
            if (message.motorId != null && message.hasOwnProperty("motorId"))
                object.motorId = message.motorId;
            return object;
        };

        /**
         * Converts this ST3215ActionCommand to JSON.
         * @function toJSON
         * @memberof st3215.ST3215ActionCommand
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ST3215ActionCommand.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ST3215ActionCommand
         * @function getTypeUrl
         * @memberof st3215.ST3215ActionCommand
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ST3215ActionCommand.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/st3215.ST3215ActionCommand";
        };

        return ST3215ActionCommand;
    })();

    st3215.ST3215SyncWriteCommand = (function() {

        /**
         * Properties of a ST3215SyncWriteCommand.
         * @memberof st3215
         * @interface IST3215SyncWriteCommand
         * @property {number|null} [address] ST3215SyncWriteCommand address
         * @property {Array.<st3215.ST3215SyncWriteCommand.IMotorWrite>|null} [motors] ST3215SyncWriteCommand motors
         */

        /**
         * Constructs a new ST3215SyncWriteCommand.
         * @memberof st3215
         * @classdesc Represents a ST3215SyncWriteCommand.
         * @implements IST3215SyncWriteCommand
         * @constructor
         * @param {st3215.IST3215SyncWriteCommand=} [properties] Properties to set
         */
        function ST3215SyncWriteCommand(properties) {
            this.motors = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ST3215SyncWriteCommand address.
         * @member {number} address
         * @memberof st3215.ST3215SyncWriteCommand
         * @instance
         */
        ST3215SyncWriteCommand.prototype.address = 0;

        /**
         * ST3215SyncWriteCommand motors.
         * @member {Array.<st3215.ST3215SyncWriteCommand.IMotorWrite>} motors
         * @memberof st3215.ST3215SyncWriteCommand
         * @instance
         */
        ST3215SyncWriteCommand.prototype.motors = $util.emptyArray;

        /**
         * Creates a new ST3215SyncWriteCommand instance using the specified properties.
         * @function create
         * @memberof st3215.ST3215SyncWriteCommand
         * @static
         * @param {st3215.IST3215SyncWriteCommand=} [properties] Properties to set
         * @returns {st3215.ST3215SyncWriteCommand} ST3215SyncWriteCommand instance
         */
        ST3215SyncWriteCommand.create = function create(properties) {
            return new ST3215SyncWriteCommand(properties);
        };

        /**
         * Encodes the specified ST3215SyncWriteCommand message. Does not implicitly {@link st3215.ST3215SyncWriteCommand.verify|verify} messages.
         * @function encode
         * @memberof st3215.ST3215SyncWriteCommand
         * @static
         * @param {st3215.IST3215SyncWriteCommand} message ST3215SyncWriteCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ST3215SyncWriteCommand.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.address != null && Object.hasOwnProperty.call(message, "address"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.address);
            if (message.motors != null && message.motors.length)
                for (let i = 0; i < message.motors.length; ++i)
                    $root.st3215.ST3215SyncWriteCommand.MotorWrite.encode(message.motors[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified ST3215SyncWriteCommand message, length delimited. Does not implicitly {@link st3215.ST3215SyncWriteCommand.verify|verify} messages.
         * @function encodeDelimited
         * @memberof st3215.ST3215SyncWriteCommand
         * @static
         * @param {st3215.IST3215SyncWriteCommand} message ST3215SyncWriteCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ST3215SyncWriteCommand.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ST3215SyncWriteCommand message from the specified reader or buffer.
         * @function decode
         * @memberof st3215.ST3215SyncWriteCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {st3215.ST3215SyncWriteCommand} ST3215SyncWriteCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ST3215SyncWriteCommand.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.st3215.ST3215SyncWriteCommand();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.address = reader.uint32();
                        break;
                    }
                case 2: {
                        if (!(message.motors && message.motors.length))
                            message.motors = [];
                        message.motors.push($root.st3215.ST3215SyncWriteCommand.MotorWrite.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ST3215SyncWriteCommand message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof st3215.ST3215SyncWriteCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {st3215.ST3215SyncWriteCommand} ST3215SyncWriteCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ST3215SyncWriteCommand.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ST3215SyncWriteCommand message.
         * @function verify
         * @memberof st3215.ST3215SyncWriteCommand
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ST3215SyncWriteCommand.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.address != null && message.hasOwnProperty("address"))
                if (!$util.isInteger(message.address))
                    return "address: integer expected";
            if (message.motors != null && message.hasOwnProperty("motors")) {
                if (!Array.isArray(message.motors))
                    return "motors: array expected";
                for (let i = 0; i < message.motors.length; ++i) {
                    let error = $root.st3215.ST3215SyncWriteCommand.MotorWrite.verify(message.motors[i], long + 1);
                    if (error)
                        return "motors." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ST3215SyncWriteCommand message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof st3215.ST3215SyncWriteCommand
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {st3215.ST3215SyncWriteCommand} ST3215SyncWriteCommand
         */
        ST3215SyncWriteCommand.fromObject = function fromObject(object, long) {
            if (object instanceof $root.st3215.ST3215SyncWriteCommand)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.st3215.ST3215SyncWriteCommand();
            if (object.address != null)
                message.address = object.address >>> 0;
            if (object.motors) {
                if (!Array.isArray(object.motors))
                    throw TypeError(".st3215.ST3215SyncWriteCommand.motors: array expected");
                message.motors = [];
                for (let i = 0; i < object.motors.length; ++i) {
                    if (typeof object.motors[i] !== "object")
                        throw TypeError(".st3215.ST3215SyncWriteCommand.motors: object expected");
                    message.motors[i] = $root.st3215.ST3215SyncWriteCommand.MotorWrite.fromObject(object.motors[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ST3215SyncWriteCommand message. Also converts values to other types if specified.
         * @function toObject
         * @memberof st3215.ST3215SyncWriteCommand
         * @static
         * @param {st3215.ST3215SyncWriteCommand} message ST3215SyncWriteCommand
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ST3215SyncWriteCommand.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.motors = [];
            if (options.defaults)
                object.address = 0;
            if (message.address != null && message.hasOwnProperty("address"))
                object.address = message.address;
            if (message.motors && message.motors.length) {
                object.motors = [];
                for (let j = 0; j < message.motors.length; ++j)
                    object.motors[j] = $root.st3215.ST3215SyncWriteCommand.MotorWrite.toObject(message.motors[j], options);
            }
            return object;
        };

        /**
         * Converts this ST3215SyncWriteCommand to JSON.
         * @function toJSON
         * @memberof st3215.ST3215SyncWriteCommand
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ST3215SyncWriteCommand.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ST3215SyncWriteCommand
         * @function getTypeUrl
         * @memberof st3215.ST3215SyncWriteCommand
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ST3215SyncWriteCommand.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/st3215.ST3215SyncWriteCommand";
        };

        ST3215SyncWriteCommand.MotorWrite = (function() {

            /**
             * Properties of a MotorWrite.
             * @memberof st3215.ST3215SyncWriteCommand
             * @interface IMotorWrite
             * @property {number|null} [motorId] MotorWrite motorId
             * @property {Uint8Array|null} [value] MotorWrite value
             */

            /**
             * Constructs a new MotorWrite.
             * @memberof st3215.ST3215SyncWriteCommand
             * @classdesc Represents a MotorWrite.
             * @implements IMotorWrite
             * @constructor
             * @param {st3215.ST3215SyncWriteCommand.IMotorWrite=} [properties] Properties to set
             */
            function MotorWrite(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * MotorWrite motorId.
             * @member {number} motorId
             * @memberof st3215.ST3215SyncWriteCommand.MotorWrite
             * @instance
             */
            MotorWrite.prototype.motorId = 0;

            /**
             * MotorWrite value.
             * @member {Uint8Array} value
             * @memberof st3215.ST3215SyncWriteCommand.MotorWrite
             * @instance
             */
            MotorWrite.prototype.value = $util.newBuffer([]);

            /**
             * Creates a new MotorWrite instance using the specified properties.
             * @function create
             * @memberof st3215.ST3215SyncWriteCommand.MotorWrite
             * @static
             * @param {st3215.ST3215SyncWriteCommand.IMotorWrite=} [properties] Properties to set
             * @returns {st3215.ST3215SyncWriteCommand.MotorWrite} MotorWrite instance
             */
            MotorWrite.create = function create(properties) {
                return new MotorWrite(properties);
            };

            /**
             * Encodes the specified MotorWrite message. Does not implicitly {@link st3215.ST3215SyncWriteCommand.MotorWrite.verify|verify} messages.
             * @function encode
             * @memberof st3215.ST3215SyncWriteCommand.MotorWrite
             * @static
             * @param {st3215.ST3215SyncWriteCommand.IMotorWrite} message MotorWrite message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MotorWrite.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.motorId != null && Object.hasOwnProperty.call(message, "motorId"))
                    writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.motorId);
                if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.value);
                return writer;
            };

            /**
             * Encodes the specified MotorWrite message, length delimited. Does not implicitly {@link st3215.ST3215SyncWriteCommand.MotorWrite.verify|verify} messages.
             * @function encodeDelimited
             * @memberof st3215.ST3215SyncWriteCommand.MotorWrite
             * @static
             * @param {st3215.ST3215SyncWriteCommand.IMotorWrite} message MotorWrite message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MotorWrite.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a MotorWrite message from the specified reader or buffer.
             * @function decode
             * @memberof st3215.ST3215SyncWriteCommand.MotorWrite
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {st3215.ST3215SyncWriteCommand.MotorWrite} MotorWrite
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MotorWrite.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.st3215.ST3215SyncWriteCommand.MotorWrite();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.motorId = reader.uint32();
                            break;
                        }
                    case 2: {
                            message.value = reader.bytes();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a MotorWrite message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof st3215.ST3215SyncWriteCommand.MotorWrite
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {st3215.ST3215SyncWriteCommand.MotorWrite} MotorWrite
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MotorWrite.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a MotorWrite message.
             * @function verify
             * @memberof st3215.ST3215SyncWriteCommand.MotorWrite
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            MotorWrite.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.motorId != null && message.hasOwnProperty("motorId"))
                    if (!$util.isInteger(message.motorId))
                        return "motorId: integer expected";
                if (message.value != null && message.hasOwnProperty("value"))
                    if (!(message.value && typeof message.value.length === "number" || $util.isString(message.value)))
                        return "value: buffer expected";
                return null;
            };

            /**
             * Creates a MotorWrite message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof st3215.ST3215SyncWriteCommand.MotorWrite
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {st3215.ST3215SyncWriteCommand.MotorWrite} MotorWrite
             */
            MotorWrite.fromObject = function fromObject(object, long) {
                if (object instanceof $root.st3215.ST3215SyncWriteCommand.MotorWrite)
                    return object;
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.st3215.ST3215SyncWriteCommand.MotorWrite();
                if (object.motorId != null)
                    message.motorId = object.motorId >>> 0;
                if (object.value != null)
                    if (typeof object.value === "string")
                        $util.base64.decode(object.value, message.value = $util.newBuffer($util.base64.length(object.value)), 0);
                    else if (object.value.length >= 0)
                        message.value = object.value;
                return message;
            };

            /**
             * Creates a plain object from a MotorWrite message. Also converts values to other types if specified.
             * @function toObject
             * @memberof st3215.ST3215SyncWriteCommand.MotorWrite
             * @static
             * @param {st3215.ST3215SyncWriteCommand.MotorWrite} message MotorWrite
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            MotorWrite.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.motorId = 0;
                    if (options.bytes === String)
                        object.value = "";
                    else {
                        object.value = [];
                        if (options.bytes !== Array)
                            object.value = $util.newBuffer(object.value);
                    }
                }
                if (message.motorId != null && message.hasOwnProperty("motorId"))
                    object.motorId = message.motorId;
                if (message.value != null && message.hasOwnProperty("value"))
                    object.value = options.bytes === String ? $util.base64.encode(message.value, 0, message.value.length) : options.bytes === Array ? Array.prototype.slice.call(message.value) : message.value;
                return object;
            };

            /**
             * Converts this MotorWrite to JSON.
             * @function toJSON
             * @memberof st3215.ST3215SyncWriteCommand.MotorWrite
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            MotorWrite.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for MotorWrite
             * @function getTypeUrl
             * @memberof st3215.ST3215SyncWriteCommand.MotorWrite
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            MotorWrite.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/st3215.ST3215SyncWriteCommand.MotorWrite";
            };

            return MotorWrite;
        })();

        return ST3215SyncWriteCommand;
    })();

    st3215.ST3215ResetCommand = (function() {

        /**
         * Properties of a ST3215ResetCommand.
         * @memberof st3215
         * @interface IST3215ResetCommand
         * @property {string|null} [portName] ST3215ResetCommand portName
         * @property {number|null} [motorId] ST3215ResetCommand motorId
         */

        /**
         * Constructs a new ST3215ResetCommand.
         * @memberof st3215
         * @classdesc Represents a ST3215ResetCommand.
         * @implements IST3215ResetCommand
         * @constructor
         * @param {st3215.IST3215ResetCommand=} [properties] Properties to set
         */
        function ST3215ResetCommand(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ST3215ResetCommand portName.
         * @member {string} portName
         * @memberof st3215.ST3215ResetCommand
         * @instance
         */
        ST3215ResetCommand.prototype.portName = "";

        /**
         * ST3215ResetCommand motorId.
         * @member {number} motorId
         * @memberof st3215.ST3215ResetCommand
         * @instance
         */
        ST3215ResetCommand.prototype.motorId = 0;

        /**
         * Creates a new ST3215ResetCommand instance using the specified properties.
         * @function create
         * @memberof st3215.ST3215ResetCommand
         * @static
         * @param {st3215.IST3215ResetCommand=} [properties] Properties to set
         * @returns {st3215.ST3215ResetCommand} ST3215ResetCommand instance
         */
        ST3215ResetCommand.create = function create(properties) {
            return new ST3215ResetCommand(properties);
        };

        /**
         * Encodes the specified ST3215ResetCommand message. Does not implicitly {@link st3215.ST3215ResetCommand.verify|verify} messages.
         * @function encode
         * @memberof st3215.ST3215ResetCommand
         * @static
         * @param {st3215.IST3215ResetCommand} message ST3215ResetCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ST3215ResetCommand.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.portName != null && Object.hasOwnProperty.call(message, "portName"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.portName);
            if (message.motorId != null && Object.hasOwnProperty.call(message, "motorId"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.motorId);
            return writer;
        };

        /**
         * Encodes the specified ST3215ResetCommand message, length delimited. Does not implicitly {@link st3215.ST3215ResetCommand.verify|verify} messages.
         * @function encodeDelimited
         * @memberof st3215.ST3215ResetCommand
         * @static
         * @param {st3215.IST3215ResetCommand} message ST3215ResetCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ST3215ResetCommand.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ST3215ResetCommand message from the specified reader or buffer.
         * @function decode
         * @memberof st3215.ST3215ResetCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {st3215.ST3215ResetCommand} ST3215ResetCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ST3215ResetCommand.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.st3215.ST3215ResetCommand();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.portName = reader.string();
                        break;
                    }
                case 2: {
                        message.motorId = reader.uint32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ST3215ResetCommand message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof st3215.ST3215ResetCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {st3215.ST3215ResetCommand} ST3215ResetCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ST3215ResetCommand.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ST3215ResetCommand message.
         * @function verify
         * @memberof st3215.ST3215ResetCommand
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ST3215ResetCommand.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.portName != null && message.hasOwnProperty("portName"))
                if (!$util.isString(message.portName))
                    return "portName: string expected";
            if (message.motorId != null && message.hasOwnProperty("motorId"))
                if (!$util.isInteger(message.motorId))
                    return "motorId: integer expected";
            return null;
        };

        /**
         * Creates a ST3215ResetCommand message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof st3215.ST3215ResetCommand
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {st3215.ST3215ResetCommand} ST3215ResetCommand
         */
        ST3215ResetCommand.fromObject = function fromObject(object, long) {
            if (object instanceof $root.st3215.ST3215ResetCommand)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.st3215.ST3215ResetCommand();
            if (object.portName != null)
                message.portName = String(object.portName);
            if (object.motorId != null)
                message.motorId = object.motorId >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a ST3215ResetCommand message. Also converts values to other types if specified.
         * @function toObject
         * @memberof st3215.ST3215ResetCommand
         * @static
         * @param {st3215.ST3215ResetCommand} message ST3215ResetCommand
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ST3215ResetCommand.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.portName = "";
                object.motorId = 0;
            }
            if (message.portName != null && message.hasOwnProperty("portName"))
                object.portName = message.portName;
            if (message.motorId != null && message.hasOwnProperty("motorId"))
                object.motorId = message.motorId;
            return object;
        };

        /**
         * Converts this ST3215ResetCommand to JSON.
         * @function toJSON
         * @memberof st3215.ST3215ResetCommand
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ST3215ResetCommand.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ST3215ResetCommand
         * @function getTypeUrl
         * @memberof st3215.ST3215ResetCommand
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ST3215ResetCommand.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/st3215.ST3215ResetCommand";
        };

        return ST3215ResetCommand;
    })();

    /**
     * MetaEnvelopeType enum.
     * @name st3215.MetaEnvelopeType
     * @enum {number}
     * @property {number} MET_UNKNOWN=0 MET_UNKNOWN value
     * @property {number} MET_RESET_CALIBRATION=1 MET_RESET_CALIBRATION value
     * @property {number} MET_FREEZE_CALIBRATION=2 MET_FREEZE_CALIBRATION value
     */
    st3215.MetaEnvelopeType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "MET_UNKNOWN"] = 0;
        values[valuesById[1] = "MET_RESET_CALIBRATION"] = 1;
        values[valuesById[2] = "MET_FREEZE_CALIBRATION"] = 2;
        return values;
    })();

    st3215.MotorArc = (function() {

        /**
         * Properties of a MotorArc.
         * @memberof st3215
         * @interface IMotorArc
         * @property {number|null} [motorId] MotorArc motorId
         * @property {number|null} [minAngle] MotorArc minAngle
         * @property {number|null} [maxAngle] MotorArc maxAngle
         * @property {boolean|null} [rangeFreezed] MotorArc rangeFreezed
         * @property {Array.<number>|null} [positions] MotorArc positions
         */

        /**
         * Constructs a new MotorArc.
         * @memberof st3215
         * @classdesc Represents a MotorArc.
         * @implements IMotorArc
         * @constructor
         * @param {st3215.IMotorArc=} [properties] Properties to set
         */
        function MotorArc(properties) {
            this.positions = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MotorArc motorId.
         * @member {number} motorId
         * @memberof st3215.MotorArc
         * @instance
         */
        MotorArc.prototype.motorId = 0;

        /**
         * MotorArc minAngle.
         * @member {number} minAngle
         * @memberof st3215.MotorArc
         * @instance
         */
        MotorArc.prototype.minAngle = 0;

        /**
         * MotorArc maxAngle.
         * @member {number} maxAngle
         * @memberof st3215.MotorArc
         * @instance
         */
        MotorArc.prototype.maxAngle = 0;

        /**
         * MotorArc rangeFreezed.
         * @member {boolean} rangeFreezed
         * @memberof st3215.MotorArc
         * @instance
         */
        MotorArc.prototype.rangeFreezed = false;

        /**
         * MotorArc positions.
         * @member {Array.<number>} positions
         * @memberof st3215.MotorArc
         * @instance
         */
        MotorArc.prototype.positions = $util.emptyArray;

        /**
         * Creates a new MotorArc instance using the specified properties.
         * @function create
         * @memberof st3215.MotorArc
         * @static
         * @param {st3215.IMotorArc=} [properties] Properties to set
         * @returns {st3215.MotorArc} MotorArc instance
         */
        MotorArc.create = function create(properties) {
            return new MotorArc(properties);
        };

        /**
         * Encodes the specified MotorArc message. Does not implicitly {@link st3215.MotorArc.verify|verify} messages.
         * @function encode
         * @memberof st3215.MotorArc
         * @static
         * @param {st3215.IMotorArc} message MotorArc message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MotorArc.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.motorId != null && Object.hasOwnProperty.call(message, "motorId"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.motorId);
            if (message.minAngle != null && Object.hasOwnProperty.call(message, "minAngle"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.minAngle);
            if (message.maxAngle != null && Object.hasOwnProperty.call(message, "maxAngle"))
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.maxAngle);
            if (message.rangeFreezed != null && Object.hasOwnProperty.call(message, "rangeFreezed"))
                writer.uint32(/* id 5, wireType 0 =*/40).bool(message.rangeFreezed);
            if (message.positions != null && message.positions.length) {
                writer.uint32(/* id 10, wireType 2 =*/82).fork();
                for (let i = 0; i < message.positions.length; ++i)
                    writer.uint32(message.positions[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified MotorArc message, length delimited. Does not implicitly {@link st3215.MotorArc.verify|verify} messages.
         * @function encodeDelimited
         * @memberof st3215.MotorArc
         * @static
         * @param {st3215.IMotorArc} message MotorArc message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MotorArc.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a MotorArc message from the specified reader or buffer.
         * @function decode
         * @memberof st3215.MotorArc
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {st3215.MotorArc} MotorArc
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MotorArc.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.st3215.MotorArc();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 2: {
                        message.motorId = reader.uint32();
                        break;
                    }
                case 3: {
                        message.minAngle = reader.uint32();
                        break;
                    }
                case 4: {
                        message.maxAngle = reader.uint32();
                        break;
                    }
                case 5: {
                        message.rangeFreezed = reader.bool();
                        break;
                    }
                case 10: {
                        if (!(message.positions && message.positions.length))
                            message.positions = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.positions.push(reader.uint32());
                        } else
                            message.positions.push(reader.uint32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a MotorArc message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof st3215.MotorArc
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {st3215.MotorArc} MotorArc
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MotorArc.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MotorArc message.
         * @function verify
         * @memberof st3215.MotorArc
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MotorArc.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.motorId != null && message.hasOwnProperty("motorId"))
                if (!$util.isInteger(message.motorId))
                    return "motorId: integer expected";
            if (message.minAngle != null && message.hasOwnProperty("minAngle"))
                if (!$util.isInteger(message.minAngle))
                    return "minAngle: integer expected";
            if (message.maxAngle != null && message.hasOwnProperty("maxAngle"))
                if (!$util.isInteger(message.maxAngle))
                    return "maxAngle: integer expected";
            if (message.rangeFreezed != null && message.hasOwnProperty("rangeFreezed"))
                if (typeof message.rangeFreezed !== "boolean")
                    return "rangeFreezed: boolean expected";
            if (message.positions != null && message.hasOwnProperty("positions")) {
                if (!Array.isArray(message.positions))
                    return "positions: array expected";
                for (let i = 0; i < message.positions.length; ++i)
                    if (!$util.isInteger(message.positions[i]))
                        return "positions: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a MotorArc message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof st3215.MotorArc
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {st3215.MotorArc} MotorArc
         */
        MotorArc.fromObject = function fromObject(object, long) {
            if (object instanceof $root.st3215.MotorArc)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.st3215.MotorArc();
            if (object.motorId != null)
                message.motorId = object.motorId >>> 0;
            if (object.minAngle != null)
                message.minAngle = object.minAngle >>> 0;
            if (object.maxAngle != null)
                message.maxAngle = object.maxAngle >>> 0;
            if (object.rangeFreezed != null)
                message.rangeFreezed = Boolean(object.rangeFreezed);
            if (object.positions) {
                if (!Array.isArray(object.positions))
                    throw TypeError(".st3215.MotorArc.positions: array expected");
                message.positions = [];
                for (let i = 0; i < object.positions.length; ++i)
                    message.positions[i] = object.positions[i] >>> 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a MotorArc message. Also converts values to other types if specified.
         * @function toObject
         * @memberof st3215.MotorArc
         * @static
         * @param {st3215.MotorArc} message MotorArc
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MotorArc.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.positions = [];
            if (options.defaults) {
                object.motorId = 0;
                object.minAngle = 0;
                object.maxAngle = 0;
                object.rangeFreezed = false;
            }
            if (message.motorId != null && message.hasOwnProperty("motorId"))
                object.motorId = message.motorId;
            if (message.minAngle != null && message.hasOwnProperty("minAngle"))
                object.minAngle = message.minAngle;
            if (message.maxAngle != null && message.hasOwnProperty("maxAngle"))
                object.maxAngle = message.maxAngle;
            if (message.rangeFreezed != null && message.hasOwnProperty("rangeFreezed"))
                object.rangeFreezed = message.rangeFreezed;
            if (message.positions && message.positions.length) {
                object.positions = [];
                for (let j = 0; j < message.positions.length; ++j)
                    object.positions[j] = message.positions[j];
            }
            return object;
        };

        /**
         * Converts this MotorArc to JSON.
         * @function toJSON
         * @memberof st3215.MotorArc
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MotorArc.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MotorArc
         * @function getTypeUrl
         * @memberof st3215.MotorArc
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MotorArc.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/st3215.MotorArc";
        };

        return MotorArc;
    })();

    st3215.ST3215Error = (function() {

        /**
         * Properties of a ST3215Error.
         * @memberof st3215
         * @interface IST3215Error
         * @property {st3215.ST3215Error.ST3215ErrorKind|null} [kind] ST3215Error kind
         * @property {Uint8Array|null} [commandPacket] ST3215Error commandPacket
         * @property {Uint8Array|null} [responsePacket] ST3215Error responsePacket
         * @property {string|null} [description] ST3215Error description
         * @property {Array.<st3215.ST3215Error.ServoErrorType>|null} [servo] ST3215Error servo
         */

        /**
         * Constructs a new ST3215Error.
         * @memberof st3215
         * @classdesc Represents a ST3215Error.
         * @implements IST3215Error
         * @constructor
         * @param {st3215.IST3215Error=} [properties] Properties to set
         */
        function ST3215Error(properties) {
            this.servo = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ST3215Error kind.
         * @member {st3215.ST3215Error.ST3215ErrorKind} kind
         * @memberof st3215.ST3215Error
         * @instance
         */
        ST3215Error.prototype.kind = 0;

        /**
         * ST3215Error commandPacket.
         * @member {Uint8Array} commandPacket
         * @memberof st3215.ST3215Error
         * @instance
         */
        ST3215Error.prototype.commandPacket = $util.newBuffer([]);

        /**
         * ST3215Error responsePacket.
         * @member {Uint8Array} responsePacket
         * @memberof st3215.ST3215Error
         * @instance
         */
        ST3215Error.prototype.responsePacket = $util.newBuffer([]);

        /**
         * ST3215Error description.
         * @member {string} description
         * @memberof st3215.ST3215Error
         * @instance
         */
        ST3215Error.prototype.description = "";

        /**
         * ST3215Error servo.
         * @member {Array.<st3215.ST3215Error.ServoErrorType>} servo
         * @memberof st3215.ST3215Error
         * @instance
         */
        ST3215Error.prototype.servo = $util.emptyArray;

        /**
         * Creates a new ST3215Error instance using the specified properties.
         * @function create
         * @memberof st3215.ST3215Error
         * @static
         * @param {st3215.IST3215Error=} [properties] Properties to set
         * @returns {st3215.ST3215Error} ST3215Error instance
         */
        ST3215Error.create = function create(properties) {
            return new ST3215Error(properties);
        };

        /**
         * Encodes the specified ST3215Error message. Does not implicitly {@link st3215.ST3215Error.verify|verify} messages.
         * @function encode
         * @memberof st3215.ST3215Error
         * @static
         * @param {st3215.IST3215Error} message ST3215Error message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ST3215Error.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.kind != null && Object.hasOwnProperty.call(message, "kind"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.kind);
            if (message.commandPacket != null && Object.hasOwnProperty.call(message, "commandPacket"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.commandPacket);
            if (message.responsePacket != null && Object.hasOwnProperty.call(message, "responsePacket"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.responsePacket);
            if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.description);
            if (message.servo != null && message.servo.length) {
                writer.uint32(/* id 10, wireType 2 =*/82).fork();
                for (let i = 0; i < message.servo.length; ++i)
                    writer.int32(message.servo[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified ST3215Error message, length delimited. Does not implicitly {@link st3215.ST3215Error.verify|verify} messages.
         * @function encodeDelimited
         * @memberof st3215.ST3215Error
         * @static
         * @param {st3215.IST3215Error} message ST3215Error message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ST3215Error.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ST3215Error message from the specified reader or buffer.
         * @function decode
         * @memberof st3215.ST3215Error
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {st3215.ST3215Error} ST3215Error
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ST3215Error.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.st3215.ST3215Error();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.kind = reader.int32();
                        break;
                    }
                case 2: {
                        message.commandPacket = reader.bytes();
                        break;
                    }
                case 3: {
                        message.responsePacket = reader.bytes();
                        break;
                    }
                case 5: {
                        message.description = reader.string();
                        break;
                    }
                case 10: {
                        if (!(message.servo && message.servo.length))
                            message.servo = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.servo.push(reader.int32());
                        } else
                            message.servo.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ST3215Error message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof st3215.ST3215Error
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {st3215.ST3215Error} ST3215Error
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ST3215Error.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ST3215Error message.
         * @function verify
         * @memberof st3215.ST3215Error
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ST3215Error.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.kind != null && message.hasOwnProperty("kind"))
                switch (message.kind) {
                default:
                    return "kind: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                case 5:
                case 6:
                    break;
                }
            if (message.commandPacket != null && message.hasOwnProperty("commandPacket"))
                if (!(message.commandPacket && typeof message.commandPacket.length === "number" || $util.isString(message.commandPacket)))
                    return "commandPacket: buffer expected";
            if (message.responsePacket != null && message.hasOwnProperty("responsePacket"))
                if (!(message.responsePacket && typeof message.responsePacket.length === "number" || $util.isString(message.responsePacket)))
                    return "responsePacket: buffer expected";
            if (message.description != null && message.hasOwnProperty("description"))
                if (!$util.isString(message.description))
                    return "description: string expected";
            if (message.servo != null && message.hasOwnProperty("servo")) {
                if (!Array.isArray(message.servo))
                    return "servo: array expected";
                for (let i = 0; i < message.servo.length; ++i)
                    switch (message.servo[i]) {
                    default:
                        return "servo: enum value[] expected";
                    case 0:
                    case 1:
                    case 2:
                    case 3:
                    case 4:
                    case 5:
                    case 6:
                        break;
                    }
            }
            return null;
        };

        /**
         * Creates a ST3215Error message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof st3215.ST3215Error
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {st3215.ST3215Error} ST3215Error
         */
        ST3215Error.fromObject = function fromObject(object, long) {
            if (object instanceof $root.st3215.ST3215Error)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.st3215.ST3215Error();
            switch (object.kind) {
            default:
                if (typeof object.kind === "number") {
                    message.kind = object.kind;
                    break;
                }
                break;
            case "SEK_IO":
            case 0:
                message.kind = 0;
                break;
            case "SEK_INVALID_HEADER":
            case 1:
                message.kind = 1;
                break;
            case "SEK_INVALID_CHECKSUM":
            case 2:
                message.kind = 2;
                break;
            case "SEK_SERVO_ERROR":
            case 3:
                message.kind = 3;
                break;
            case "SEK_MOTOR_ID_ERROR":
            case 4:
                message.kind = 4;
                break;
            case "SEK_INVALID_DATA":
            case 5:
                message.kind = 5;
                break;
            case "SEK_TIMEOUT":
            case 6:
                message.kind = 6;
                break;
            }
            if (object.commandPacket != null)
                if (typeof object.commandPacket === "string")
                    $util.base64.decode(object.commandPacket, message.commandPacket = $util.newBuffer($util.base64.length(object.commandPacket)), 0);
                else if (object.commandPacket.length >= 0)
                    message.commandPacket = object.commandPacket;
            if (object.responsePacket != null)
                if (typeof object.responsePacket === "string")
                    $util.base64.decode(object.responsePacket, message.responsePacket = $util.newBuffer($util.base64.length(object.responsePacket)), 0);
                else if (object.responsePacket.length >= 0)
                    message.responsePacket = object.responsePacket;
            if (object.description != null)
                message.description = String(object.description);
            if (object.servo) {
                if (!Array.isArray(object.servo))
                    throw TypeError(".st3215.ST3215Error.servo: array expected");
                message.servo = [];
                for (let i = 0; i < object.servo.length; ++i)
                    switch (object.servo[i]) {
                    default:
                        if (typeof object.servo[i] === "number") {
                            message.servo[i] = object.servo[i];
                            break;
                        }
                    case "SET_INSTRUCTION":
                    case 0:
                        message.servo[i] = 0;
                        break;
                    case "SET_OVERLOAD":
                    case 1:
                        message.servo[i] = 1;
                        break;
                    case "SET_CHECKSUM":
                    case 2:
                        message.servo[i] = 2;
                        break;
                    case "SET_RANGE":
                    case 3:
                        message.servo[i] = 3;
                        break;
                    case "SET_OVERHEAT":
                    case 4:
                        message.servo[i] = 4;
                        break;
                    case "SET_ANGLE_LIMIT":
                    case 5:
                        message.servo[i] = 5;
                        break;
                    case "SET_VOLTAGE":
                    case 6:
                        message.servo[i] = 6;
                        break;
                    }
            }
            return message;
        };

        /**
         * Creates a plain object from a ST3215Error message. Also converts values to other types if specified.
         * @function toObject
         * @memberof st3215.ST3215Error
         * @static
         * @param {st3215.ST3215Error} message ST3215Error
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ST3215Error.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.servo = [];
            if (options.defaults) {
                object.kind = options.enums === String ? "SEK_IO" : 0;
                if (options.bytes === String)
                    object.commandPacket = "";
                else {
                    object.commandPacket = [];
                    if (options.bytes !== Array)
                        object.commandPacket = $util.newBuffer(object.commandPacket);
                }
                if (options.bytes === String)
                    object.responsePacket = "";
                else {
                    object.responsePacket = [];
                    if (options.bytes !== Array)
                        object.responsePacket = $util.newBuffer(object.responsePacket);
                }
                object.description = "";
            }
            if (message.kind != null && message.hasOwnProperty("kind"))
                object.kind = options.enums === String ? $root.st3215.ST3215Error.ST3215ErrorKind[message.kind] === undefined ? message.kind : $root.st3215.ST3215Error.ST3215ErrorKind[message.kind] : message.kind;
            if (message.commandPacket != null && message.hasOwnProperty("commandPacket"))
                object.commandPacket = options.bytes === String ? $util.base64.encode(message.commandPacket, 0, message.commandPacket.length) : options.bytes === Array ? Array.prototype.slice.call(message.commandPacket) : message.commandPacket;
            if (message.responsePacket != null && message.hasOwnProperty("responsePacket"))
                object.responsePacket = options.bytes === String ? $util.base64.encode(message.responsePacket, 0, message.responsePacket.length) : options.bytes === Array ? Array.prototype.slice.call(message.responsePacket) : message.responsePacket;
            if (message.description != null && message.hasOwnProperty("description"))
                object.description = message.description;
            if (message.servo && message.servo.length) {
                object.servo = [];
                for (let j = 0; j < message.servo.length; ++j)
                    object.servo[j] = options.enums === String ? $root.st3215.ST3215Error.ServoErrorType[message.servo[j]] === undefined ? message.servo[j] : $root.st3215.ST3215Error.ServoErrorType[message.servo[j]] : message.servo[j];
            }
            return object;
        };

        /**
         * Converts this ST3215Error to JSON.
         * @function toJSON
         * @memberof st3215.ST3215Error
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ST3215Error.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ST3215Error
         * @function getTypeUrl
         * @memberof st3215.ST3215Error
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ST3215Error.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/st3215.ST3215Error";
        };

        /**
         * ServoErrorType enum.
         * @name st3215.ST3215Error.ServoErrorType
         * @enum {number}
         * @property {number} SET_INSTRUCTION=0 SET_INSTRUCTION value
         * @property {number} SET_OVERLOAD=1 SET_OVERLOAD value
         * @property {number} SET_CHECKSUM=2 SET_CHECKSUM value
         * @property {number} SET_RANGE=3 SET_RANGE value
         * @property {number} SET_OVERHEAT=4 SET_OVERHEAT value
         * @property {number} SET_ANGLE_LIMIT=5 SET_ANGLE_LIMIT value
         * @property {number} SET_VOLTAGE=6 SET_VOLTAGE value
         */
        ST3215Error.ServoErrorType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "SET_INSTRUCTION"] = 0;
            values[valuesById[1] = "SET_OVERLOAD"] = 1;
            values[valuesById[2] = "SET_CHECKSUM"] = 2;
            values[valuesById[3] = "SET_RANGE"] = 3;
            values[valuesById[4] = "SET_OVERHEAT"] = 4;
            values[valuesById[5] = "SET_ANGLE_LIMIT"] = 5;
            values[valuesById[6] = "SET_VOLTAGE"] = 6;
            return values;
        })();

        /**
         * ST3215ErrorKind enum.
         * @name st3215.ST3215Error.ST3215ErrorKind
         * @enum {number}
         * @property {number} SEK_IO=0 SEK_IO value
         * @property {number} SEK_INVALID_HEADER=1 SEK_INVALID_HEADER value
         * @property {number} SEK_INVALID_CHECKSUM=2 SEK_INVALID_CHECKSUM value
         * @property {number} SEK_SERVO_ERROR=3 SEK_SERVO_ERROR value
         * @property {number} SEK_MOTOR_ID_ERROR=4 SEK_MOTOR_ID_ERROR value
         * @property {number} SEK_INVALID_DATA=5 SEK_INVALID_DATA value
         * @property {number} SEK_TIMEOUT=6 SEK_TIMEOUT value
         */
        ST3215Error.ST3215ErrorKind = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "SEK_IO"] = 0;
            values[valuesById[1] = "SEK_INVALID_HEADER"] = 1;
            values[valuesById[2] = "SEK_INVALID_CHECKSUM"] = 2;
            values[valuesById[3] = "SEK_SERVO_ERROR"] = 3;
            values[valuesById[4] = "SEK_MOTOR_ID_ERROR"] = 4;
            values[valuesById[5] = "SEK_INVALID_DATA"] = 5;
            values[valuesById[6] = "SEK_TIMEOUT"] = 6;
            return values;
        })();

        return ST3215Error;
    })();

    return st3215;
})();

export const vesc_trampa = $root.vesc_trampa = (() => {

    /**
     * Namespace vesc_trampa.
     * @exports vesc_trampa
     * @namespace
     */
    const vesc_trampa = {};

    /**
     * VescTrampaSignalType enum.
     * @name vesc_trampa.VescTrampaSignalType
     * @enum {number}
     * @property {number} VESC_TRAMPA_SIGNAL_TYPE_UNSPECIFIED=0 VESC_TRAMPA_SIGNAL_TYPE_UNSPECIFIED value
     * @property {number} VESC_TRAMPA_BOARD_CONNECT=1 VESC_TRAMPA_BOARD_CONNECT value
     * @property {number} VESC_TRAMPA_BOARD_DISCONNECT=2 VESC_TRAMPA_BOARD_DISCONNECT value
     * @property {number} VESC_TRAMPA_BOARD_PACKET=3 VESC_TRAMPA_BOARD_PACKET value
     * @property {number} VESC_TRAMPA_COMMAND=4 VESC_TRAMPA_COMMAND value
     * @property {number} VESC_TRAMPA_COMMAND_SUCCESS=5 VESC_TRAMPA_COMMAND_SUCCESS value
     * @property {number} VESC_TRAMPA_COMMAND_REJECTED=6 VESC_TRAMPA_COMMAND_REJECTED value
     * @property {number} VESC_TRAMPA_COMMAND_FAILED=7 VESC_TRAMPA_COMMAND_FAILED value
     */
    vesc_trampa.VescTrampaSignalType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "VESC_TRAMPA_SIGNAL_TYPE_UNSPECIFIED"] = 0;
        values[valuesById[1] = "VESC_TRAMPA_BOARD_CONNECT"] = 1;
        values[valuesById[2] = "VESC_TRAMPA_BOARD_DISCONNECT"] = 2;
        values[valuesById[3] = "VESC_TRAMPA_BOARD_PACKET"] = 3;
        values[valuesById[4] = "VESC_TRAMPA_COMMAND"] = 4;
        values[valuesById[5] = "VESC_TRAMPA_COMMAND_SUCCESS"] = 5;
        values[valuesById[6] = "VESC_TRAMPA_COMMAND_REJECTED"] = 6;
        values[valuesById[7] = "VESC_TRAMPA_COMMAND_FAILED"] = 7;
        return values;
    })();

    vesc_trampa.RxEnvelope = (function() {

        /**
         * Properties of a RxEnvelope.
         * @memberof vesc_trampa
         * @interface IRxEnvelope
         * @property {Long|null} [monotonicStampNs] RxEnvelope monotonicStampNs
         * @property {Long|null} [localStampNs] RxEnvelope localStampNs
         * @property {Long|null} [appStartId] RxEnvelope appStartId
         * @property {vesc_trampa.VescTrampaSignalType|null} [signalType] RxEnvelope signalType
         * @property {vesc_trampa.IVescTrampaBoard|null} [board] RxEnvelope board
         * @property {vesc_trampa.IVescTrampaBoardPacket|null} [boardPacket] RxEnvelope boardPacket
         * @property {vesc_trampa.ITxEnvelope|null} [command] RxEnvelope command
         * @property {string|null} [error] RxEnvelope error
         */

        /**
         * Constructs a new RxEnvelope.
         * @memberof vesc_trampa
         * @classdesc Represents a RxEnvelope.
         * @implements IRxEnvelope
         * @constructor
         * @param {vesc_trampa.IRxEnvelope=} [properties] Properties to set
         */
        function RxEnvelope(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RxEnvelope monotonicStampNs.
         * @member {Long} monotonicStampNs
         * @memberof vesc_trampa.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.monotonicStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * RxEnvelope localStampNs.
         * @member {Long} localStampNs
         * @memberof vesc_trampa.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.localStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * RxEnvelope appStartId.
         * @member {Long} appStartId
         * @memberof vesc_trampa.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.appStartId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * RxEnvelope signalType.
         * @member {vesc_trampa.VescTrampaSignalType} signalType
         * @memberof vesc_trampa.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.signalType = 0;

        /**
         * RxEnvelope board.
         * @member {vesc_trampa.IVescTrampaBoard|null|undefined} board
         * @memberof vesc_trampa.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.board = null;

        /**
         * RxEnvelope boardPacket.
         * @member {vesc_trampa.IVescTrampaBoardPacket|null|undefined} boardPacket
         * @memberof vesc_trampa.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.boardPacket = null;

        /**
         * RxEnvelope command.
         * @member {vesc_trampa.ITxEnvelope|null|undefined} command
         * @memberof vesc_trampa.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.command = null;

        /**
         * RxEnvelope error.
         * @member {string} error
         * @memberof vesc_trampa.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.error = "";

        /**
         * Creates a new RxEnvelope instance using the specified properties.
         * @function create
         * @memberof vesc_trampa.RxEnvelope
         * @static
         * @param {vesc_trampa.IRxEnvelope=} [properties] Properties to set
         * @returns {vesc_trampa.RxEnvelope} RxEnvelope instance
         */
        RxEnvelope.create = function create(properties) {
            return new RxEnvelope(properties);
        };

        /**
         * Encodes the specified RxEnvelope message. Does not implicitly {@link vesc_trampa.RxEnvelope.verify|verify} messages.
         * @function encode
         * @memberof vesc_trampa.RxEnvelope
         * @static
         * @param {vesc_trampa.IRxEnvelope} message RxEnvelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RxEnvelope.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.monotonicStampNs != null && Object.hasOwnProperty.call(message, "monotonicStampNs"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.monotonicStampNs);
            if (message.localStampNs != null && Object.hasOwnProperty.call(message, "localStampNs"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.localStampNs);
            if (message.appStartId != null && Object.hasOwnProperty.call(message, "appStartId"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.appStartId);
            if (message.signalType != null && Object.hasOwnProperty.call(message, "signalType"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.signalType);
            if (message.board != null && Object.hasOwnProperty.call(message, "board"))
                $root.vesc_trampa.VescTrampaBoard.encode(message.board, writer.uint32(/* id 11, wireType 2 =*/90).fork()).ldelim();
            if (message.boardPacket != null && Object.hasOwnProperty.call(message, "boardPacket"))
                $root.vesc_trampa.VescTrampaBoardPacket.encode(message.boardPacket, writer.uint32(/* id 20, wireType 2 =*/162).fork()).ldelim();
            if (message.command != null && Object.hasOwnProperty.call(message, "command"))
                $root.vesc_trampa.TxEnvelope.encode(message.command, writer.uint32(/* id 30, wireType 2 =*/242).fork()).ldelim();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                writer.uint32(/* id 50, wireType 2 =*/402).string(message.error);
            return writer;
        };

        /**
         * Encodes the specified RxEnvelope message, length delimited. Does not implicitly {@link vesc_trampa.RxEnvelope.verify|verify} messages.
         * @function encodeDelimited
         * @memberof vesc_trampa.RxEnvelope
         * @static
         * @param {vesc_trampa.IRxEnvelope} message RxEnvelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RxEnvelope.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RxEnvelope message from the specified reader or buffer.
         * @function decode
         * @memberof vesc_trampa.RxEnvelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {vesc_trampa.RxEnvelope} RxEnvelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RxEnvelope.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.vesc_trampa.RxEnvelope();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.monotonicStampNs = reader.uint64();
                        break;
                    }
                case 2: {
                        message.localStampNs = reader.uint64();
                        break;
                    }
                case 3: {
                        message.appStartId = reader.uint64();
                        break;
                    }
                case 10: {
                        message.signalType = reader.int32();
                        break;
                    }
                case 11: {
                        message.board = $root.vesc_trampa.VescTrampaBoard.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 20: {
                        message.boardPacket = $root.vesc_trampa.VescTrampaBoardPacket.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 30: {
                        message.command = $root.vesc_trampa.TxEnvelope.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 50: {
                        message.error = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RxEnvelope message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof vesc_trampa.RxEnvelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {vesc_trampa.RxEnvelope} RxEnvelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RxEnvelope.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RxEnvelope message.
         * @function verify
         * @memberof vesc_trampa.RxEnvelope
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RxEnvelope.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (!$util.isInteger(message.monotonicStampNs) && !(message.monotonicStampNs && $util.isInteger(message.monotonicStampNs.low) && $util.isInteger(message.monotonicStampNs.high)))
                    return "monotonicStampNs: integer|Long expected";
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (!$util.isInteger(message.localStampNs) && !(message.localStampNs && $util.isInteger(message.localStampNs.low) && $util.isInteger(message.localStampNs.high)))
                    return "localStampNs: integer|Long expected";
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (!$util.isInteger(message.appStartId) && !(message.appStartId && $util.isInteger(message.appStartId.low) && $util.isInteger(message.appStartId.high)))
                    return "appStartId: integer|Long expected";
            if (message.signalType != null && message.hasOwnProperty("signalType"))
                switch (message.signalType) {
                default:
                    return "signalType: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                case 5:
                case 6:
                case 7:
                    break;
                }
            if (message.board != null && message.hasOwnProperty("board")) {
                let error = $root.vesc_trampa.VescTrampaBoard.verify(message.board, long + 1);
                if (error)
                    return "board." + error;
            }
            if (message.boardPacket != null && message.hasOwnProperty("boardPacket")) {
                let error = $root.vesc_trampa.VescTrampaBoardPacket.verify(message.boardPacket, long + 1);
                if (error)
                    return "boardPacket." + error;
            }
            if (message.command != null && message.hasOwnProperty("command")) {
                let error = $root.vesc_trampa.TxEnvelope.verify(message.command, long + 1);
                if (error)
                    return "command." + error;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                if (!$util.isString(message.error))
                    return "error: string expected";
            return null;
        };

        /**
         * Creates a RxEnvelope message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof vesc_trampa.RxEnvelope
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {vesc_trampa.RxEnvelope} RxEnvelope
         */
        RxEnvelope.fromObject = function fromObject(object, long) {
            if (object instanceof $root.vesc_trampa.RxEnvelope)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.vesc_trampa.RxEnvelope();
            if (object.monotonicStampNs != null)
                if ($util.Long)
                    (message.monotonicStampNs = $util.Long.fromValue(object.monotonicStampNs)).unsigned = true;
                else if (typeof object.monotonicStampNs === "string")
                    message.monotonicStampNs = parseInt(object.monotonicStampNs, 10);
                else if (typeof object.monotonicStampNs === "number")
                    message.monotonicStampNs = object.monotonicStampNs;
                else if (typeof object.monotonicStampNs === "object")
                    message.monotonicStampNs = new $util.LongBits(object.monotonicStampNs.low >>> 0, object.monotonicStampNs.high >>> 0).toNumber(true);
            if (object.localStampNs != null)
                if ($util.Long)
                    (message.localStampNs = $util.Long.fromValue(object.localStampNs)).unsigned = true;
                else if (typeof object.localStampNs === "string")
                    message.localStampNs = parseInt(object.localStampNs, 10);
                else if (typeof object.localStampNs === "number")
                    message.localStampNs = object.localStampNs;
                else if (typeof object.localStampNs === "object")
                    message.localStampNs = new $util.LongBits(object.localStampNs.low >>> 0, object.localStampNs.high >>> 0).toNumber(true);
            if (object.appStartId != null)
                if ($util.Long)
                    (message.appStartId = $util.Long.fromValue(object.appStartId)).unsigned = true;
                else if (typeof object.appStartId === "string")
                    message.appStartId = parseInt(object.appStartId, 10);
                else if (typeof object.appStartId === "number")
                    message.appStartId = object.appStartId;
                else if (typeof object.appStartId === "object")
                    message.appStartId = new $util.LongBits(object.appStartId.low >>> 0, object.appStartId.high >>> 0).toNumber(true);
            switch (object.signalType) {
            default:
                if (typeof object.signalType === "number") {
                    message.signalType = object.signalType;
                    break;
                }
                break;
            case "VESC_TRAMPA_SIGNAL_TYPE_UNSPECIFIED":
            case 0:
                message.signalType = 0;
                break;
            case "VESC_TRAMPA_BOARD_CONNECT":
            case 1:
                message.signalType = 1;
                break;
            case "VESC_TRAMPA_BOARD_DISCONNECT":
            case 2:
                message.signalType = 2;
                break;
            case "VESC_TRAMPA_BOARD_PACKET":
            case 3:
                message.signalType = 3;
                break;
            case "VESC_TRAMPA_COMMAND":
            case 4:
                message.signalType = 4;
                break;
            case "VESC_TRAMPA_COMMAND_SUCCESS":
            case 5:
                message.signalType = 5;
                break;
            case "VESC_TRAMPA_COMMAND_REJECTED":
            case 6:
                message.signalType = 6;
                break;
            case "VESC_TRAMPA_COMMAND_FAILED":
            case 7:
                message.signalType = 7;
                break;
            }
            if (object.board != null) {
                if (typeof object.board !== "object")
                    throw TypeError(".vesc_trampa.RxEnvelope.board: object expected");
                message.board = $root.vesc_trampa.VescTrampaBoard.fromObject(object.board, long + 1);
            }
            if (object.boardPacket != null) {
                if (typeof object.boardPacket !== "object")
                    throw TypeError(".vesc_trampa.RxEnvelope.boardPacket: object expected");
                message.boardPacket = $root.vesc_trampa.VescTrampaBoardPacket.fromObject(object.boardPacket, long + 1);
            }
            if (object.command != null) {
                if (typeof object.command !== "object")
                    throw TypeError(".vesc_trampa.RxEnvelope.command: object expected");
                message.command = $root.vesc_trampa.TxEnvelope.fromObject(object.command, long + 1);
            }
            if (object.error != null)
                message.error = String(object.error);
            return message;
        };

        /**
         * Creates a plain object from a RxEnvelope message. Also converts values to other types if specified.
         * @function toObject
         * @memberof vesc_trampa.RxEnvelope
         * @static
         * @param {vesc_trampa.RxEnvelope} message RxEnvelope
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RxEnvelope.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.monotonicStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.monotonicStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.localStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.localStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.appStartId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.appStartId = options.longs === String ? "0" : 0;
                object.signalType = options.enums === String ? "VESC_TRAMPA_SIGNAL_TYPE_UNSPECIFIED" : 0;
                object.board = null;
                object.boardPacket = null;
                object.command = null;
                object.error = "";
            }
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (typeof message.monotonicStampNs === "number")
                    object.monotonicStampNs = options.longs === String ? String(message.monotonicStampNs) : message.monotonicStampNs;
                else
                    object.monotonicStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.monotonicStampNs) : options.longs === Number ? new $util.LongBits(message.monotonicStampNs.low >>> 0, message.monotonicStampNs.high >>> 0).toNumber(true) : message.monotonicStampNs;
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (typeof message.localStampNs === "number")
                    object.localStampNs = options.longs === String ? String(message.localStampNs) : message.localStampNs;
                else
                    object.localStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.localStampNs) : options.longs === Number ? new $util.LongBits(message.localStampNs.low >>> 0, message.localStampNs.high >>> 0).toNumber(true) : message.localStampNs;
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (typeof message.appStartId === "number")
                    object.appStartId = options.longs === String ? String(message.appStartId) : message.appStartId;
                else
                    object.appStartId = options.longs === String ? $util.Long.prototype.toString.call(message.appStartId) : options.longs === Number ? new $util.LongBits(message.appStartId.low >>> 0, message.appStartId.high >>> 0).toNumber(true) : message.appStartId;
            if (message.signalType != null && message.hasOwnProperty("signalType"))
                object.signalType = options.enums === String ? $root.vesc_trampa.VescTrampaSignalType[message.signalType] === undefined ? message.signalType : $root.vesc_trampa.VescTrampaSignalType[message.signalType] : message.signalType;
            if (message.board != null && message.hasOwnProperty("board"))
                object.board = $root.vesc_trampa.VescTrampaBoard.toObject(message.board, options);
            if (message.boardPacket != null && message.hasOwnProperty("boardPacket"))
                object.boardPacket = $root.vesc_trampa.VescTrampaBoardPacket.toObject(message.boardPacket, options);
            if (message.command != null && message.hasOwnProperty("command"))
                object.command = $root.vesc_trampa.TxEnvelope.toObject(message.command, options);
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = message.error;
            return object;
        };

        /**
         * Converts this RxEnvelope to JSON.
         * @function toJSON
         * @memberof vesc_trampa.RxEnvelope
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RxEnvelope.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RxEnvelope
         * @function getTypeUrl
         * @memberof vesc_trampa.RxEnvelope
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RxEnvelope.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/vesc_trampa.RxEnvelope";
        };

        return RxEnvelope;
    })();

    vesc_trampa.TxEnvelope = (function() {

        /**
         * Properties of a TxEnvelope.
         * @memberof vesc_trampa
         * @interface ITxEnvelope
         * @property {Long|null} [monotonicStampNs] TxEnvelope monotonicStampNs
         * @property {Long|null} [localStampNs] TxEnvelope localStampNs
         * @property {Long|null} [appStartId] TxEnvelope appStartId
         * @property {Uint8Array|null} [targetBoardUuid] TxEnvelope targetBoardUuid
         * @property {Uint8Array|null} [commandId] TxEnvelope commandId
         * @property {vesc_trampa.IVescTrampaMotorModeCommand|null} [motorMode] TxEnvelope motorMode
         * @property {Array.<vesc_trampa.IVescTrampaBoardCommand>|null} [boardCommands] TxEnvelope boardCommands
         */

        /**
         * Constructs a new TxEnvelope.
         * @memberof vesc_trampa
         * @classdesc Represents a TxEnvelope.
         * @implements ITxEnvelope
         * @constructor
         * @param {vesc_trampa.ITxEnvelope=} [properties] Properties to set
         */
        function TxEnvelope(properties) {
            this.boardCommands = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * TxEnvelope monotonicStampNs.
         * @member {Long} monotonicStampNs
         * @memberof vesc_trampa.TxEnvelope
         * @instance
         */
        TxEnvelope.prototype.monotonicStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * TxEnvelope localStampNs.
         * @member {Long} localStampNs
         * @memberof vesc_trampa.TxEnvelope
         * @instance
         */
        TxEnvelope.prototype.localStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * TxEnvelope appStartId.
         * @member {Long} appStartId
         * @memberof vesc_trampa.TxEnvelope
         * @instance
         */
        TxEnvelope.prototype.appStartId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * TxEnvelope targetBoardUuid.
         * @member {Uint8Array} targetBoardUuid
         * @memberof vesc_trampa.TxEnvelope
         * @instance
         */
        TxEnvelope.prototype.targetBoardUuid = $util.newBuffer([]);

        /**
         * TxEnvelope commandId.
         * @member {Uint8Array} commandId
         * @memberof vesc_trampa.TxEnvelope
         * @instance
         */
        TxEnvelope.prototype.commandId = $util.newBuffer([]);

        /**
         * TxEnvelope motorMode.
         * @member {vesc_trampa.IVescTrampaMotorModeCommand|null|undefined} motorMode
         * @memberof vesc_trampa.TxEnvelope
         * @instance
         */
        TxEnvelope.prototype.motorMode = null;

        /**
         * TxEnvelope boardCommands.
         * @member {Array.<vesc_trampa.IVescTrampaBoardCommand>} boardCommands
         * @memberof vesc_trampa.TxEnvelope
         * @instance
         */
        TxEnvelope.prototype.boardCommands = $util.emptyArray;

        /**
         * Creates a new TxEnvelope instance using the specified properties.
         * @function create
         * @memberof vesc_trampa.TxEnvelope
         * @static
         * @param {vesc_trampa.ITxEnvelope=} [properties] Properties to set
         * @returns {vesc_trampa.TxEnvelope} TxEnvelope instance
         */
        TxEnvelope.create = function create(properties) {
            return new TxEnvelope(properties);
        };

        /**
         * Encodes the specified TxEnvelope message. Does not implicitly {@link vesc_trampa.TxEnvelope.verify|verify} messages.
         * @function encode
         * @memberof vesc_trampa.TxEnvelope
         * @static
         * @param {vesc_trampa.ITxEnvelope} message TxEnvelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TxEnvelope.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.monotonicStampNs != null && Object.hasOwnProperty.call(message, "monotonicStampNs"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.monotonicStampNs);
            if (message.localStampNs != null && Object.hasOwnProperty.call(message, "localStampNs"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.localStampNs);
            if (message.targetBoardUuid != null && Object.hasOwnProperty.call(message, "targetBoardUuid"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.targetBoardUuid);
            if (message.commandId != null && Object.hasOwnProperty.call(message, "commandId"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.commandId);
            if (message.appStartId != null && Object.hasOwnProperty.call(message, "appStartId"))
                writer.uint32(/* id 5, wireType 0 =*/40).uint64(message.appStartId);
            if (message.motorMode != null && Object.hasOwnProperty.call(message, "motorMode"))
                $root.vesc_trampa.VescTrampaMotorModeCommand.encode(message.motorMode, writer.uint32(/* id 11, wireType 2 =*/90).fork()).ldelim();
            if (message.boardCommands != null && message.boardCommands.length)
                for (let i = 0; i < message.boardCommands.length; ++i)
                    $root.vesc_trampa.VescTrampaBoardCommand.encode(message.boardCommands[i], writer.uint32(/* id 12, wireType 2 =*/98).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified TxEnvelope message, length delimited. Does not implicitly {@link vesc_trampa.TxEnvelope.verify|verify} messages.
         * @function encodeDelimited
         * @memberof vesc_trampa.TxEnvelope
         * @static
         * @param {vesc_trampa.ITxEnvelope} message TxEnvelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TxEnvelope.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a TxEnvelope message from the specified reader or buffer.
         * @function decode
         * @memberof vesc_trampa.TxEnvelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {vesc_trampa.TxEnvelope} TxEnvelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TxEnvelope.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.vesc_trampa.TxEnvelope();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.monotonicStampNs = reader.uint64();
                        break;
                    }
                case 2: {
                        message.localStampNs = reader.uint64();
                        break;
                    }
                case 5: {
                        message.appStartId = reader.uint64();
                        break;
                    }
                case 3: {
                        message.targetBoardUuid = reader.bytes();
                        break;
                    }
                case 4: {
                        message.commandId = reader.bytes();
                        break;
                    }
                case 11: {
                        message.motorMode = $root.vesc_trampa.VescTrampaMotorModeCommand.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 12: {
                        if (!(message.boardCommands && message.boardCommands.length))
                            message.boardCommands = [];
                        message.boardCommands.push($root.vesc_trampa.VescTrampaBoardCommand.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a TxEnvelope message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof vesc_trampa.TxEnvelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {vesc_trampa.TxEnvelope} TxEnvelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TxEnvelope.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TxEnvelope message.
         * @function verify
         * @memberof vesc_trampa.TxEnvelope
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TxEnvelope.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (!$util.isInteger(message.monotonicStampNs) && !(message.monotonicStampNs && $util.isInteger(message.monotonicStampNs.low) && $util.isInteger(message.monotonicStampNs.high)))
                    return "monotonicStampNs: integer|Long expected";
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (!$util.isInteger(message.localStampNs) && !(message.localStampNs && $util.isInteger(message.localStampNs.low) && $util.isInteger(message.localStampNs.high)))
                    return "localStampNs: integer|Long expected";
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (!$util.isInteger(message.appStartId) && !(message.appStartId && $util.isInteger(message.appStartId.low) && $util.isInteger(message.appStartId.high)))
                    return "appStartId: integer|Long expected";
            if (message.targetBoardUuid != null && message.hasOwnProperty("targetBoardUuid"))
                if (!(message.targetBoardUuid && typeof message.targetBoardUuid.length === "number" || $util.isString(message.targetBoardUuid)))
                    return "targetBoardUuid: buffer expected";
            if (message.commandId != null && message.hasOwnProperty("commandId"))
                if (!(message.commandId && typeof message.commandId.length === "number" || $util.isString(message.commandId)))
                    return "commandId: buffer expected";
            if (message.motorMode != null && message.hasOwnProperty("motorMode")) {
                let error = $root.vesc_trampa.VescTrampaMotorModeCommand.verify(message.motorMode, long + 1);
                if (error)
                    return "motorMode." + error;
            }
            if (message.boardCommands != null && message.hasOwnProperty("boardCommands")) {
                if (!Array.isArray(message.boardCommands))
                    return "boardCommands: array expected";
                for (let i = 0; i < message.boardCommands.length; ++i) {
                    let error = $root.vesc_trampa.VescTrampaBoardCommand.verify(message.boardCommands[i], long + 1);
                    if (error)
                        return "boardCommands." + error;
                }
            }
            return null;
        };

        /**
         * Creates a TxEnvelope message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof vesc_trampa.TxEnvelope
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {vesc_trampa.TxEnvelope} TxEnvelope
         */
        TxEnvelope.fromObject = function fromObject(object, long) {
            if (object instanceof $root.vesc_trampa.TxEnvelope)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.vesc_trampa.TxEnvelope();
            if (object.monotonicStampNs != null)
                if ($util.Long)
                    (message.monotonicStampNs = $util.Long.fromValue(object.monotonicStampNs)).unsigned = true;
                else if (typeof object.monotonicStampNs === "string")
                    message.monotonicStampNs = parseInt(object.monotonicStampNs, 10);
                else if (typeof object.monotonicStampNs === "number")
                    message.monotonicStampNs = object.monotonicStampNs;
                else if (typeof object.monotonicStampNs === "object")
                    message.monotonicStampNs = new $util.LongBits(object.monotonicStampNs.low >>> 0, object.monotonicStampNs.high >>> 0).toNumber(true);
            if (object.localStampNs != null)
                if ($util.Long)
                    (message.localStampNs = $util.Long.fromValue(object.localStampNs)).unsigned = true;
                else if (typeof object.localStampNs === "string")
                    message.localStampNs = parseInt(object.localStampNs, 10);
                else if (typeof object.localStampNs === "number")
                    message.localStampNs = object.localStampNs;
                else if (typeof object.localStampNs === "object")
                    message.localStampNs = new $util.LongBits(object.localStampNs.low >>> 0, object.localStampNs.high >>> 0).toNumber(true);
            if (object.appStartId != null)
                if ($util.Long)
                    (message.appStartId = $util.Long.fromValue(object.appStartId)).unsigned = true;
                else if (typeof object.appStartId === "string")
                    message.appStartId = parseInt(object.appStartId, 10);
                else if (typeof object.appStartId === "number")
                    message.appStartId = object.appStartId;
                else if (typeof object.appStartId === "object")
                    message.appStartId = new $util.LongBits(object.appStartId.low >>> 0, object.appStartId.high >>> 0).toNumber(true);
            if (object.targetBoardUuid != null)
                if (typeof object.targetBoardUuid === "string")
                    $util.base64.decode(object.targetBoardUuid, message.targetBoardUuid = $util.newBuffer($util.base64.length(object.targetBoardUuid)), 0);
                else if (object.targetBoardUuid.length >= 0)
                    message.targetBoardUuid = object.targetBoardUuid;
            if (object.commandId != null)
                if (typeof object.commandId === "string")
                    $util.base64.decode(object.commandId, message.commandId = $util.newBuffer($util.base64.length(object.commandId)), 0);
                else if (object.commandId.length >= 0)
                    message.commandId = object.commandId;
            if (object.motorMode != null) {
                if (typeof object.motorMode !== "object")
                    throw TypeError(".vesc_trampa.TxEnvelope.motorMode: object expected");
                message.motorMode = $root.vesc_trampa.VescTrampaMotorModeCommand.fromObject(object.motorMode, long + 1);
            }
            if (object.boardCommands) {
                if (!Array.isArray(object.boardCommands))
                    throw TypeError(".vesc_trampa.TxEnvelope.boardCommands: array expected");
                message.boardCommands = [];
                for (let i = 0; i < object.boardCommands.length; ++i) {
                    if (typeof object.boardCommands[i] !== "object")
                        throw TypeError(".vesc_trampa.TxEnvelope.boardCommands: object expected");
                    message.boardCommands[i] = $root.vesc_trampa.VescTrampaBoardCommand.fromObject(object.boardCommands[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a TxEnvelope message. Also converts values to other types if specified.
         * @function toObject
         * @memberof vesc_trampa.TxEnvelope
         * @static
         * @param {vesc_trampa.TxEnvelope} message TxEnvelope
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TxEnvelope.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.boardCommands = [];
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.monotonicStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.monotonicStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.localStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.localStampNs = options.longs === String ? "0" : 0;
                if (options.bytes === String)
                    object.targetBoardUuid = "";
                else {
                    object.targetBoardUuid = [];
                    if (options.bytes !== Array)
                        object.targetBoardUuid = $util.newBuffer(object.targetBoardUuid);
                }
                if (options.bytes === String)
                    object.commandId = "";
                else {
                    object.commandId = [];
                    if (options.bytes !== Array)
                        object.commandId = $util.newBuffer(object.commandId);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.appStartId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.appStartId = options.longs === String ? "0" : 0;
                object.motorMode = null;
            }
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (typeof message.monotonicStampNs === "number")
                    object.monotonicStampNs = options.longs === String ? String(message.monotonicStampNs) : message.monotonicStampNs;
                else
                    object.monotonicStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.monotonicStampNs) : options.longs === Number ? new $util.LongBits(message.monotonicStampNs.low >>> 0, message.monotonicStampNs.high >>> 0).toNumber(true) : message.monotonicStampNs;
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (typeof message.localStampNs === "number")
                    object.localStampNs = options.longs === String ? String(message.localStampNs) : message.localStampNs;
                else
                    object.localStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.localStampNs) : options.longs === Number ? new $util.LongBits(message.localStampNs.low >>> 0, message.localStampNs.high >>> 0).toNumber(true) : message.localStampNs;
            if (message.targetBoardUuid != null && message.hasOwnProperty("targetBoardUuid"))
                object.targetBoardUuid = options.bytes === String ? $util.base64.encode(message.targetBoardUuid, 0, message.targetBoardUuid.length) : options.bytes === Array ? Array.prototype.slice.call(message.targetBoardUuid) : message.targetBoardUuid;
            if (message.commandId != null && message.hasOwnProperty("commandId"))
                object.commandId = options.bytes === String ? $util.base64.encode(message.commandId, 0, message.commandId.length) : options.bytes === Array ? Array.prototype.slice.call(message.commandId) : message.commandId;
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (typeof message.appStartId === "number")
                    object.appStartId = options.longs === String ? String(message.appStartId) : message.appStartId;
                else
                    object.appStartId = options.longs === String ? $util.Long.prototype.toString.call(message.appStartId) : options.longs === Number ? new $util.LongBits(message.appStartId.low >>> 0, message.appStartId.high >>> 0).toNumber(true) : message.appStartId;
            if (message.motorMode != null && message.hasOwnProperty("motorMode"))
                object.motorMode = $root.vesc_trampa.VescTrampaMotorModeCommand.toObject(message.motorMode, options);
            if (message.boardCommands && message.boardCommands.length) {
                object.boardCommands = [];
                for (let j = 0; j < message.boardCommands.length; ++j)
                    object.boardCommands[j] = $root.vesc_trampa.VescTrampaBoardCommand.toObject(message.boardCommands[j], options);
            }
            return object;
        };

        /**
         * Converts this TxEnvelope to JSON.
         * @function toJSON
         * @memberof vesc_trampa.TxEnvelope
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TxEnvelope.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for TxEnvelope
         * @function getTypeUrl
         * @memberof vesc_trampa.TxEnvelope
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        TxEnvelope.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/vesc_trampa.TxEnvelope";
        };

        return TxEnvelope;
    })();

    vesc_trampa.Command = (function() {

        /**
         * Properties of a Command.
         * @memberof vesc_trampa
         * @interface ICommand
         * @property {Uint8Array|null} [targetBoardUuid] Command targetBoardUuid
         * @property {vesc_trampa.IVescTrampaMotorModeCommand|null} [motorMode] Command motorMode
         * @property {Array.<vesc_trampa.IVescTrampaBoardCommand>|null} [boardCommands] Command boardCommands
         */

        /**
         * Constructs a new Command.
         * @memberof vesc_trampa
         * @classdesc Represents a Command.
         * @implements ICommand
         * @constructor
         * @param {vesc_trampa.ICommand=} [properties] Properties to set
         */
        function Command(properties) {
            this.boardCommands = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Command targetBoardUuid.
         * @member {Uint8Array} targetBoardUuid
         * @memberof vesc_trampa.Command
         * @instance
         */
        Command.prototype.targetBoardUuid = $util.newBuffer([]);

        /**
         * Command motorMode.
         * @member {vesc_trampa.IVescTrampaMotorModeCommand|null|undefined} motorMode
         * @memberof vesc_trampa.Command
         * @instance
         */
        Command.prototype.motorMode = null;

        /**
         * Command boardCommands.
         * @member {Array.<vesc_trampa.IVescTrampaBoardCommand>} boardCommands
         * @memberof vesc_trampa.Command
         * @instance
         */
        Command.prototype.boardCommands = $util.emptyArray;

        /**
         * Creates a new Command instance using the specified properties.
         * @function create
         * @memberof vesc_trampa.Command
         * @static
         * @param {vesc_trampa.ICommand=} [properties] Properties to set
         * @returns {vesc_trampa.Command} Command instance
         */
        Command.create = function create(properties) {
            return new Command(properties);
        };

        /**
         * Encodes the specified Command message. Does not implicitly {@link vesc_trampa.Command.verify|verify} messages.
         * @function encode
         * @memberof vesc_trampa.Command
         * @static
         * @param {vesc_trampa.ICommand} message Command message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Command.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.targetBoardUuid != null && Object.hasOwnProperty.call(message, "targetBoardUuid"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.targetBoardUuid);
            if (message.motorMode != null && Object.hasOwnProperty.call(message, "motorMode"))
                $root.vesc_trampa.VescTrampaMotorModeCommand.encode(message.motorMode, writer.uint32(/* id 11, wireType 2 =*/90).fork()).ldelim();
            if (message.boardCommands != null && message.boardCommands.length)
                for (let i = 0; i < message.boardCommands.length; ++i)
                    $root.vesc_trampa.VescTrampaBoardCommand.encode(message.boardCommands[i], writer.uint32(/* id 12, wireType 2 =*/98).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified Command message, length delimited. Does not implicitly {@link vesc_trampa.Command.verify|verify} messages.
         * @function encodeDelimited
         * @memberof vesc_trampa.Command
         * @static
         * @param {vesc_trampa.ICommand} message Command message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Command.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Command message from the specified reader or buffer.
         * @function decode
         * @memberof vesc_trampa.Command
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {vesc_trampa.Command} Command
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Command.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.vesc_trampa.Command();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.targetBoardUuid = reader.bytes();
                        break;
                    }
                case 11: {
                        message.motorMode = $root.vesc_trampa.VescTrampaMotorModeCommand.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 12: {
                        if (!(message.boardCommands && message.boardCommands.length))
                            message.boardCommands = [];
                        message.boardCommands.push($root.vesc_trampa.VescTrampaBoardCommand.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Command message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof vesc_trampa.Command
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {vesc_trampa.Command} Command
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Command.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Command message.
         * @function verify
         * @memberof vesc_trampa.Command
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Command.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.targetBoardUuid != null && message.hasOwnProperty("targetBoardUuid"))
                if (!(message.targetBoardUuid && typeof message.targetBoardUuid.length === "number" || $util.isString(message.targetBoardUuid)))
                    return "targetBoardUuid: buffer expected";
            if (message.motorMode != null && message.hasOwnProperty("motorMode")) {
                let error = $root.vesc_trampa.VescTrampaMotorModeCommand.verify(message.motorMode, long + 1);
                if (error)
                    return "motorMode." + error;
            }
            if (message.boardCommands != null && message.hasOwnProperty("boardCommands")) {
                if (!Array.isArray(message.boardCommands))
                    return "boardCommands: array expected";
                for (let i = 0; i < message.boardCommands.length; ++i) {
                    let error = $root.vesc_trampa.VescTrampaBoardCommand.verify(message.boardCommands[i], long + 1);
                    if (error)
                        return "boardCommands." + error;
                }
            }
            return null;
        };

        /**
         * Creates a Command message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof vesc_trampa.Command
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {vesc_trampa.Command} Command
         */
        Command.fromObject = function fromObject(object, long) {
            if (object instanceof $root.vesc_trampa.Command)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.vesc_trampa.Command();
            if (object.targetBoardUuid != null)
                if (typeof object.targetBoardUuid === "string")
                    $util.base64.decode(object.targetBoardUuid, message.targetBoardUuid = $util.newBuffer($util.base64.length(object.targetBoardUuid)), 0);
                else if (object.targetBoardUuid.length >= 0)
                    message.targetBoardUuid = object.targetBoardUuid;
            if (object.motorMode != null) {
                if (typeof object.motorMode !== "object")
                    throw TypeError(".vesc_trampa.Command.motorMode: object expected");
                message.motorMode = $root.vesc_trampa.VescTrampaMotorModeCommand.fromObject(object.motorMode, long + 1);
            }
            if (object.boardCommands) {
                if (!Array.isArray(object.boardCommands))
                    throw TypeError(".vesc_trampa.Command.boardCommands: array expected");
                message.boardCommands = [];
                for (let i = 0; i < object.boardCommands.length; ++i) {
                    if (typeof object.boardCommands[i] !== "object")
                        throw TypeError(".vesc_trampa.Command.boardCommands: object expected");
                    message.boardCommands[i] = $root.vesc_trampa.VescTrampaBoardCommand.fromObject(object.boardCommands[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a Command message. Also converts values to other types if specified.
         * @function toObject
         * @memberof vesc_trampa.Command
         * @static
         * @param {vesc_trampa.Command} message Command
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Command.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.boardCommands = [];
            if (options.defaults) {
                if (options.bytes === String)
                    object.targetBoardUuid = "";
                else {
                    object.targetBoardUuid = [];
                    if (options.bytes !== Array)
                        object.targetBoardUuid = $util.newBuffer(object.targetBoardUuid);
                }
                object.motorMode = null;
            }
            if (message.targetBoardUuid != null && message.hasOwnProperty("targetBoardUuid"))
                object.targetBoardUuid = options.bytes === String ? $util.base64.encode(message.targetBoardUuid, 0, message.targetBoardUuid.length) : options.bytes === Array ? Array.prototype.slice.call(message.targetBoardUuid) : message.targetBoardUuid;
            if (message.motorMode != null && message.hasOwnProperty("motorMode"))
                object.motorMode = $root.vesc_trampa.VescTrampaMotorModeCommand.toObject(message.motorMode, options);
            if (message.boardCommands && message.boardCommands.length) {
                object.boardCommands = [];
                for (let j = 0; j < message.boardCommands.length; ++j)
                    object.boardCommands[j] = $root.vesc_trampa.VescTrampaBoardCommand.toObject(message.boardCommands[j], options);
            }
            return object;
        };

        /**
         * Converts this Command to JSON.
         * @function toJSON
         * @memberof vesc_trampa.Command
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Command.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Command
         * @function getTypeUrl
         * @memberof vesc_trampa.Command
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Command.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/vesc_trampa.Command";
        };

        return Command;
    })();

    vesc_trampa.VescTrampaBoardCommand = (function() {

        /**
         * Properties of a VescTrampaBoardCommand.
         * @memberof vesc_trampa
         * @interface IVescTrampaBoardCommand
         * @property {Uint8Array|null} [payload] VescTrampaBoardCommand payload
         * @property {boolean|null} [responseExpected] VescTrampaBoardCommand responseExpected
         * @property {number|null} [durationMs] VescTrampaBoardCommand durationMs
         */

        /**
         * Constructs a new VescTrampaBoardCommand.
         * @memberof vesc_trampa
         * @classdesc Represents a VescTrampaBoardCommand.
         * @implements IVescTrampaBoardCommand
         * @constructor
         * @param {vesc_trampa.IVescTrampaBoardCommand=} [properties] Properties to set
         */
        function VescTrampaBoardCommand(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * VescTrampaBoardCommand payload.
         * @member {Uint8Array} payload
         * @memberof vesc_trampa.VescTrampaBoardCommand
         * @instance
         */
        VescTrampaBoardCommand.prototype.payload = $util.newBuffer([]);

        /**
         * VescTrampaBoardCommand responseExpected.
         * @member {boolean} responseExpected
         * @memberof vesc_trampa.VescTrampaBoardCommand
         * @instance
         */
        VescTrampaBoardCommand.prototype.responseExpected = false;

        /**
         * VescTrampaBoardCommand durationMs.
         * @member {number} durationMs
         * @memberof vesc_trampa.VescTrampaBoardCommand
         * @instance
         */
        VescTrampaBoardCommand.prototype.durationMs = 0;

        /**
         * Creates a new VescTrampaBoardCommand instance using the specified properties.
         * @function create
         * @memberof vesc_trampa.VescTrampaBoardCommand
         * @static
         * @param {vesc_trampa.IVescTrampaBoardCommand=} [properties] Properties to set
         * @returns {vesc_trampa.VescTrampaBoardCommand} VescTrampaBoardCommand instance
         */
        VescTrampaBoardCommand.create = function create(properties) {
            return new VescTrampaBoardCommand(properties);
        };

        /**
         * Encodes the specified VescTrampaBoardCommand message. Does not implicitly {@link vesc_trampa.VescTrampaBoardCommand.verify|verify} messages.
         * @function encode
         * @memberof vesc_trampa.VescTrampaBoardCommand
         * @static
         * @param {vesc_trampa.IVescTrampaBoardCommand} message VescTrampaBoardCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        VescTrampaBoardCommand.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.payload != null && Object.hasOwnProperty.call(message, "payload"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.payload);
            if (message.responseExpected != null && Object.hasOwnProperty.call(message, "responseExpected"))
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.responseExpected);
            if (message.durationMs != null && Object.hasOwnProperty.call(message, "durationMs"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.durationMs);
            return writer;
        };

        /**
         * Encodes the specified VescTrampaBoardCommand message, length delimited. Does not implicitly {@link vesc_trampa.VescTrampaBoardCommand.verify|verify} messages.
         * @function encodeDelimited
         * @memberof vesc_trampa.VescTrampaBoardCommand
         * @static
         * @param {vesc_trampa.IVescTrampaBoardCommand} message VescTrampaBoardCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        VescTrampaBoardCommand.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a VescTrampaBoardCommand message from the specified reader or buffer.
         * @function decode
         * @memberof vesc_trampa.VescTrampaBoardCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {vesc_trampa.VescTrampaBoardCommand} VescTrampaBoardCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        VescTrampaBoardCommand.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.vesc_trampa.VescTrampaBoardCommand();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.payload = reader.bytes();
                        break;
                    }
                case 2: {
                        message.responseExpected = reader.bool();
                        break;
                    }
                case 3: {
                        message.durationMs = reader.uint32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a VescTrampaBoardCommand message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof vesc_trampa.VescTrampaBoardCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {vesc_trampa.VescTrampaBoardCommand} VescTrampaBoardCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        VescTrampaBoardCommand.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a VescTrampaBoardCommand message.
         * @function verify
         * @memberof vesc_trampa.VescTrampaBoardCommand
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        VescTrampaBoardCommand.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.payload != null && message.hasOwnProperty("payload"))
                if (!(message.payload && typeof message.payload.length === "number" || $util.isString(message.payload)))
                    return "payload: buffer expected";
            if (message.responseExpected != null && message.hasOwnProperty("responseExpected"))
                if (typeof message.responseExpected !== "boolean")
                    return "responseExpected: boolean expected";
            if (message.durationMs != null && message.hasOwnProperty("durationMs"))
                if (!$util.isInteger(message.durationMs))
                    return "durationMs: integer expected";
            return null;
        };

        /**
         * Creates a VescTrampaBoardCommand message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof vesc_trampa.VescTrampaBoardCommand
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {vesc_trampa.VescTrampaBoardCommand} VescTrampaBoardCommand
         */
        VescTrampaBoardCommand.fromObject = function fromObject(object, long) {
            if (object instanceof $root.vesc_trampa.VescTrampaBoardCommand)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.vesc_trampa.VescTrampaBoardCommand();
            if (object.payload != null)
                if (typeof object.payload === "string")
                    $util.base64.decode(object.payload, message.payload = $util.newBuffer($util.base64.length(object.payload)), 0);
                else if (object.payload.length >= 0)
                    message.payload = object.payload;
            if (object.responseExpected != null)
                message.responseExpected = Boolean(object.responseExpected);
            if (object.durationMs != null)
                message.durationMs = object.durationMs >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a VescTrampaBoardCommand message. Also converts values to other types if specified.
         * @function toObject
         * @memberof vesc_trampa.VescTrampaBoardCommand
         * @static
         * @param {vesc_trampa.VescTrampaBoardCommand} message VescTrampaBoardCommand
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        VescTrampaBoardCommand.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.payload = "";
                else {
                    object.payload = [];
                    if (options.bytes !== Array)
                        object.payload = $util.newBuffer(object.payload);
                }
                object.responseExpected = false;
                object.durationMs = 0;
            }
            if (message.payload != null && message.hasOwnProperty("payload"))
                object.payload = options.bytes === String ? $util.base64.encode(message.payload, 0, message.payload.length) : options.bytes === Array ? Array.prototype.slice.call(message.payload) : message.payload;
            if (message.responseExpected != null && message.hasOwnProperty("responseExpected"))
                object.responseExpected = message.responseExpected;
            if (message.durationMs != null && message.hasOwnProperty("durationMs"))
                object.durationMs = message.durationMs;
            return object;
        };

        /**
         * Converts this VescTrampaBoardCommand to JSON.
         * @function toJSON
         * @memberof vesc_trampa.VescTrampaBoardCommand
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        VescTrampaBoardCommand.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for VescTrampaBoardCommand
         * @function getTypeUrl
         * @memberof vesc_trampa.VescTrampaBoardCommand
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        VescTrampaBoardCommand.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/vesc_trampa.VescTrampaBoardCommand";
        };

        return VescTrampaBoardCommand;
    })();

    /**
     * VescTrampaMotorMode enum.
     * @name vesc_trampa.VescTrampaMotorMode
     * @enum {number}
     * @property {number} VESC_TRAMPA_MOTOR_MODE_UNSPECIFIED=0 VESC_TRAMPA_MOTOR_MODE_UNSPECIFIED value
     * @property {number} VESC_TRAMPA_MOTOR_MODE_HOLD=1 VESC_TRAMPA_MOTOR_MODE_HOLD value
     */
    vesc_trampa.VescTrampaMotorMode = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "VESC_TRAMPA_MOTOR_MODE_UNSPECIFIED"] = 0;
        values[valuesById[1] = "VESC_TRAMPA_MOTOR_MODE_HOLD"] = 1;
        return values;
    })();

    vesc_trampa.VescTrampaMotorModeCommand = (function() {

        /**
         * Properties of a VescTrampaMotorModeCommand.
         * @memberof vesc_trampa
         * @interface IVescTrampaMotorModeCommand
         * @property {vesc_trampa.VescTrampaMotorMode|null} [mode] VescTrampaMotorModeCommand mode
         */

        /**
         * Constructs a new VescTrampaMotorModeCommand.
         * @memberof vesc_trampa
         * @classdesc Represents a VescTrampaMotorModeCommand.
         * @implements IVescTrampaMotorModeCommand
         * @constructor
         * @param {vesc_trampa.IVescTrampaMotorModeCommand=} [properties] Properties to set
         */
        function VescTrampaMotorModeCommand(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * VescTrampaMotorModeCommand mode.
         * @member {vesc_trampa.VescTrampaMotorMode} mode
         * @memberof vesc_trampa.VescTrampaMotorModeCommand
         * @instance
         */
        VescTrampaMotorModeCommand.prototype.mode = 0;

        /**
         * Creates a new VescTrampaMotorModeCommand instance using the specified properties.
         * @function create
         * @memberof vesc_trampa.VescTrampaMotorModeCommand
         * @static
         * @param {vesc_trampa.IVescTrampaMotorModeCommand=} [properties] Properties to set
         * @returns {vesc_trampa.VescTrampaMotorModeCommand} VescTrampaMotorModeCommand instance
         */
        VescTrampaMotorModeCommand.create = function create(properties) {
            return new VescTrampaMotorModeCommand(properties);
        };

        /**
         * Encodes the specified VescTrampaMotorModeCommand message. Does not implicitly {@link vesc_trampa.VescTrampaMotorModeCommand.verify|verify} messages.
         * @function encode
         * @memberof vesc_trampa.VescTrampaMotorModeCommand
         * @static
         * @param {vesc_trampa.IVescTrampaMotorModeCommand} message VescTrampaMotorModeCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        VescTrampaMotorModeCommand.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.mode);
            return writer;
        };

        /**
         * Encodes the specified VescTrampaMotorModeCommand message, length delimited. Does not implicitly {@link vesc_trampa.VescTrampaMotorModeCommand.verify|verify} messages.
         * @function encodeDelimited
         * @memberof vesc_trampa.VescTrampaMotorModeCommand
         * @static
         * @param {vesc_trampa.IVescTrampaMotorModeCommand} message VescTrampaMotorModeCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        VescTrampaMotorModeCommand.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a VescTrampaMotorModeCommand message from the specified reader or buffer.
         * @function decode
         * @memberof vesc_trampa.VescTrampaMotorModeCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {vesc_trampa.VescTrampaMotorModeCommand} VescTrampaMotorModeCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        VescTrampaMotorModeCommand.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.vesc_trampa.VescTrampaMotorModeCommand();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.mode = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a VescTrampaMotorModeCommand message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof vesc_trampa.VescTrampaMotorModeCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {vesc_trampa.VescTrampaMotorModeCommand} VescTrampaMotorModeCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        VescTrampaMotorModeCommand.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a VescTrampaMotorModeCommand message.
         * @function verify
         * @memberof vesc_trampa.VescTrampaMotorModeCommand
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        VescTrampaMotorModeCommand.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.mode != null && message.hasOwnProperty("mode"))
                switch (message.mode) {
                default:
                    return "mode: enum value expected";
                case 0:
                case 1:
                    break;
                }
            return null;
        };

        /**
         * Creates a VescTrampaMotorModeCommand message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof vesc_trampa.VescTrampaMotorModeCommand
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {vesc_trampa.VescTrampaMotorModeCommand} VescTrampaMotorModeCommand
         */
        VescTrampaMotorModeCommand.fromObject = function fromObject(object, long) {
            if (object instanceof $root.vesc_trampa.VescTrampaMotorModeCommand)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.vesc_trampa.VescTrampaMotorModeCommand();
            switch (object.mode) {
            default:
                if (typeof object.mode === "number") {
                    message.mode = object.mode;
                    break;
                }
                break;
            case "VESC_TRAMPA_MOTOR_MODE_UNSPECIFIED":
            case 0:
                message.mode = 0;
                break;
            case "VESC_TRAMPA_MOTOR_MODE_HOLD":
            case 1:
                message.mode = 1;
                break;
            }
            return message;
        };

        /**
         * Creates a plain object from a VescTrampaMotorModeCommand message. Also converts values to other types if specified.
         * @function toObject
         * @memberof vesc_trampa.VescTrampaMotorModeCommand
         * @static
         * @param {vesc_trampa.VescTrampaMotorModeCommand} message VescTrampaMotorModeCommand
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        VescTrampaMotorModeCommand.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.mode = options.enums === String ? "VESC_TRAMPA_MOTOR_MODE_UNSPECIFIED" : 0;
            if (message.mode != null && message.hasOwnProperty("mode"))
                object.mode = options.enums === String ? $root.vesc_trampa.VescTrampaMotorMode[message.mode] === undefined ? message.mode : $root.vesc_trampa.VescTrampaMotorMode[message.mode] : message.mode;
            return object;
        };

        /**
         * Converts this VescTrampaMotorModeCommand to JSON.
         * @function toJSON
         * @memberof vesc_trampa.VescTrampaMotorModeCommand
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        VescTrampaMotorModeCommand.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for VescTrampaMotorModeCommand
         * @function getTypeUrl
         * @memberof vesc_trampa.VescTrampaMotorModeCommand
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        VescTrampaMotorModeCommand.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/vesc_trampa.VescTrampaMotorModeCommand";
        };

        return VescTrampaMotorModeCommand;
    })();

    vesc_trampa.VescTrampaBoardPacket = (function() {

        /**
         * Properties of a VescTrampaBoardPacket.
         * @memberof vesc_trampa
         * @interface IVescTrampaBoardPacket
         * @property {number|null} [startByte] VescTrampaBoardPacket startByte
         * @property {number|null} [payloadLen] VescTrampaBoardPacket payloadLen
         * @property {number|null} [commandId] VescTrampaBoardPacket commandId
         * @property {Uint8Array|null} [payload] VescTrampaBoardPacket payload
         * @property {number|null} [crc] VescTrampaBoardPacket crc
         * @property {number|null} [endByte] VescTrampaBoardPacket endByte
         */

        /**
         * Constructs a new VescTrampaBoardPacket.
         * @memberof vesc_trampa
         * @classdesc Represents a VescTrampaBoardPacket.
         * @implements IVescTrampaBoardPacket
         * @constructor
         * @param {vesc_trampa.IVescTrampaBoardPacket=} [properties] Properties to set
         */
        function VescTrampaBoardPacket(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * VescTrampaBoardPacket startByte.
         * @member {number} startByte
         * @memberof vesc_trampa.VescTrampaBoardPacket
         * @instance
         */
        VescTrampaBoardPacket.prototype.startByte = 0;

        /**
         * VescTrampaBoardPacket payloadLen.
         * @member {number} payloadLen
         * @memberof vesc_trampa.VescTrampaBoardPacket
         * @instance
         */
        VescTrampaBoardPacket.prototype.payloadLen = 0;

        /**
         * VescTrampaBoardPacket commandId.
         * @member {number} commandId
         * @memberof vesc_trampa.VescTrampaBoardPacket
         * @instance
         */
        VescTrampaBoardPacket.prototype.commandId = 0;

        /**
         * VescTrampaBoardPacket payload.
         * @member {Uint8Array} payload
         * @memberof vesc_trampa.VescTrampaBoardPacket
         * @instance
         */
        VescTrampaBoardPacket.prototype.payload = $util.newBuffer([]);

        /**
         * VescTrampaBoardPacket crc.
         * @member {number} crc
         * @memberof vesc_trampa.VescTrampaBoardPacket
         * @instance
         */
        VescTrampaBoardPacket.prototype.crc = 0;

        /**
         * VescTrampaBoardPacket endByte.
         * @member {number} endByte
         * @memberof vesc_trampa.VescTrampaBoardPacket
         * @instance
         */
        VescTrampaBoardPacket.prototype.endByte = 0;

        /**
         * Creates a new VescTrampaBoardPacket instance using the specified properties.
         * @function create
         * @memberof vesc_trampa.VescTrampaBoardPacket
         * @static
         * @param {vesc_trampa.IVescTrampaBoardPacket=} [properties] Properties to set
         * @returns {vesc_trampa.VescTrampaBoardPacket} VescTrampaBoardPacket instance
         */
        VescTrampaBoardPacket.create = function create(properties) {
            return new VescTrampaBoardPacket(properties);
        };

        /**
         * Encodes the specified VescTrampaBoardPacket message. Does not implicitly {@link vesc_trampa.VescTrampaBoardPacket.verify|verify} messages.
         * @function encode
         * @memberof vesc_trampa.VescTrampaBoardPacket
         * @static
         * @param {vesc_trampa.IVescTrampaBoardPacket} message VescTrampaBoardPacket message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        VescTrampaBoardPacket.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.startByte != null && Object.hasOwnProperty.call(message, "startByte"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.startByte);
            if (message.payloadLen != null && Object.hasOwnProperty.call(message, "payloadLen"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.payloadLen);
            if (message.commandId != null && Object.hasOwnProperty.call(message, "commandId"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.commandId);
            if (message.payload != null && Object.hasOwnProperty.call(message, "payload"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.payload);
            if (message.crc != null && Object.hasOwnProperty.call(message, "crc"))
                writer.uint32(/* id 5, wireType 0 =*/40).uint32(message.crc);
            if (message.endByte != null && Object.hasOwnProperty.call(message, "endByte"))
                writer.uint32(/* id 6, wireType 0 =*/48).uint32(message.endByte);
            return writer;
        };

        /**
         * Encodes the specified VescTrampaBoardPacket message, length delimited. Does not implicitly {@link vesc_trampa.VescTrampaBoardPacket.verify|verify} messages.
         * @function encodeDelimited
         * @memberof vesc_trampa.VescTrampaBoardPacket
         * @static
         * @param {vesc_trampa.IVescTrampaBoardPacket} message VescTrampaBoardPacket message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        VescTrampaBoardPacket.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a VescTrampaBoardPacket message from the specified reader or buffer.
         * @function decode
         * @memberof vesc_trampa.VescTrampaBoardPacket
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {vesc_trampa.VescTrampaBoardPacket} VescTrampaBoardPacket
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        VescTrampaBoardPacket.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.vesc_trampa.VescTrampaBoardPacket();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.startByte = reader.uint32();
                        break;
                    }
                case 2: {
                        message.payloadLen = reader.uint32();
                        break;
                    }
                case 3: {
                        message.commandId = reader.uint32();
                        break;
                    }
                case 4: {
                        message.payload = reader.bytes();
                        break;
                    }
                case 5: {
                        message.crc = reader.uint32();
                        break;
                    }
                case 6: {
                        message.endByte = reader.uint32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a VescTrampaBoardPacket message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof vesc_trampa.VescTrampaBoardPacket
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {vesc_trampa.VescTrampaBoardPacket} VescTrampaBoardPacket
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        VescTrampaBoardPacket.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a VescTrampaBoardPacket message.
         * @function verify
         * @memberof vesc_trampa.VescTrampaBoardPacket
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        VescTrampaBoardPacket.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.startByte != null && message.hasOwnProperty("startByte"))
                if (!$util.isInteger(message.startByte))
                    return "startByte: integer expected";
            if (message.payloadLen != null && message.hasOwnProperty("payloadLen"))
                if (!$util.isInteger(message.payloadLen))
                    return "payloadLen: integer expected";
            if (message.commandId != null && message.hasOwnProperty("commandId"))
                if (!$util.isInteger(message.commandId))
                    return "commandId: integer expected";
            if (message.payload != null && message.hasOwnProperty("payload"))
                if (!(message.payload && typeof message.payload.length === "number" || $util.isString(message.payload)))
                    return "payload: buffer expected";
            if (message.crc != null && message.hasOwnProperty("crc"))
                if (!$util.isInteger(message.crc))
                    return "crc: integer expected";
            if (message.endByte != null && message.hasOwnProperty("endByte"))
                if (!$util.isInteger(message.endByte))
                    return "endByte: integer expected";
            return null;
        };

        /**
         * Creates a VescTrampaBoardPacket message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof vesc_trampa.VescTrampaBoardPacket
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {vesc_trampa.VescTrampaBoardPacket} VescTrampaBoardPacket
         */
        VescTrampaBoardPacket.fromObject = function fromObject(object, long) {
            if (object instanceof $root.vesc_trampa.VescTrampaBoardPacket)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.vesc_trampa.VescTrampaBoardPacket();
            if (object.startByte != null)
                message.startByte = object.startByte >>> 0;
            if (object.payloadLen != null)
                message.payloadLen = object.payloadLen >>> 0;
            if (object.commandId != null)
                message.commandId = object.commandId >>> 0;
            if (object.payload != null)
                if (typeof object.payload === "string")
                    $util.base64.decode(object.payload, message.payload = $util.newBuffer($util.base64.length(object.payload)), 0);
                else if (object.payload.length >= 0)
                    message.payload = object.payload;
            if (object.crc != null)
                message.crc = object.crc >>> 0;
            if (object.endByte != null)
                message.endByte = object.endByte >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a VescTrampaBoardPacket message. Also converts values to other types if specified.
         * @function toObject
         * @memberof vesc_trampa.VescTrampaBoardPacket
         * @static
         * @param {vesc_trampa.VescTrampaBoardPacket} message VescTrampaBoardPacket
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        VescTrampaBoardPacket.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.startByte = 0;
                object.payloadLen = 0;
                object.commandId = 0;
                if (options.bytes === String)
                    object.payload = "";
                else {
                    object.payload = [];
                    if (options.bytes !== Array)
                        object.payload = $util.newBuffer(object.payload);
                }
                object.crc = 0;
                object.endByte = 0;
            }
            if (message.startByte != null && message.hasOwnProperty("startByte"))
                object.startByte = message.startByte;
            if (message.payloadLen != null && message.hasOwnProperty("payloadLen"))
                object.payloadLen = message.payloadLen;
            if (message.commandId != null && message.hasOwnProperty("commandId"))
                object.commandId = message.commandId;
            if (message.payload != null && message.hasOwnProperty("payload"))
                object.payload = options.bytes === String ? $util.base64.encode(message.payload, 0, message.payload.length) : options.bytes === Array ? Array.prototype.slice.call(message.payload) : message.payload;
            if (message.crc != null && message.hasOwnProperty("crc"))
                object.crc = message.crc;
            if (message.endByte != null && message.hasOwnProperty("endByte"))
                object.endByte = message.endByte;
            return object;
        };

        /**
         * Converts this VescTrampaBoardPacket to JSON.
         * @function toJSON
         * @memberof vesc_trampa.VescTrampaBoardPacket
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        VescTrampaBoardPacket.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for VescTrampaBoardPacket
         * @function getTypeUrl
         * @memberof vesc_trampa.VescTrampaBoardPacket
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        VescTrampaBoardPacket.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/vesc_trampa.VescTrampaBoardPacket";
        };

        return VescTrampaBoardPacket;
    })();

    vesc_trampa.InferenceState = (function() {

        /**
         * Properties of an InferenceState.
         * @memberof vesc_trampa
         * @interface IInferenceState
         * @property {Uint8Array|null} [lastInferenceQueuePtr] InferenceState lastInferenceQueuePtr
         * @property {Array.<vesc_trampa.InferenceState.IBoardState>|null} [boards] InferenceState boards
         */

        /**
         * Constructs a new InferenceState.
         * @memberof vesc_trampa
         * @classdesc Represents an InferenceState.
         * @implements IInferenceState
         * @constructor
         * @param {vesc_trampa.IInferenceState=} [properties] Properties to set
         */
        function InferenceState(properties) {
            this.boards = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * InferenceState lastInferenceQueuePtr.
         * @member {Uint8Array} lastInferenceQueuePtr
         * @memberof vesc_trampa.InferenceState
         * @instance
         */
        InferenceState.prototype.lastInferenceQueuePtr = $util.newBuffer([]);

        /**
         * InferenceState boards.
         * @member {Array.<vesc_trampa.InferenceState.IBoardState>} boards
         * @memberof vesc_trampa.InferenceState
         * @instance
         */
        InferenceState.prototype.boards = $util.emptyArray;

        /**
         * Creates a new InferenceState instance using the specified properties.
         * @function create
         * @memberof vesc_trampa.InferenceState
         * @static
         * @param {vesc_trampa.IInferenceState=} [properties] Properties to set
         * @returns {vesc_trampa.InferenceState} InferenceState instance
         */
        InferenceState.create = function create(properties) {
            return new InferenceState(properties);
        };

        /**
         * Encodes the specified InferenceState message. Does not implicitly {@link vesc_trampa.InferenceState.verify|verify} messages.
         * @function encode
         * @memberof vesc_trampa.InferenceState
         * @static
         * @param {vesc_trampa.IInferenceState} message InferenceState message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        InferenceState.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.lastInferenceQueuePtr != null && Object.hasOwnProperty.call(message, "lastInferenceQueuePtr"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.lastInferenceQueuePtr);
            if (message.boards != null && message.boards.length)
                for (let i = 0; i < message.boards.length; ++i)
                    $root.vesc_trampa.InferenceState.BoardState.encode(message.boards[i], writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified InferenceState message, length delimited. Does not implicitly {@link vesc_trampa.InferenceState.verify|verify} messages.
         * @function encodeDelimited
         * @memberof vesc_trampa.InferenceState
         * @static
         * @param {vesc_trampa.IInferenceState} message InferenceState message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        InferenceState.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an InferenceState message from the specified reader or buffer.
         * @function decode
         * @memberof vesc_trampa.InferenceState
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {vesc_trampa.InferenceState} InferenceState
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        InferenceState.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.vesc_trampa.InferenceState();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.lastInferenceQueuePtr = reader.bytes();
                        break;
                    }
                case 10: {
                        if (!(message.boards && message.boards.length))
                            message.boards = [];
                        message.boards.push($root.vesc_trampa.InferenceState.BoardState.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an InferenceState message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof vesc_trampa.InferenceState
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {vesc_trampa.InferenceState} InferenceState
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        InferenceState.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an InferenceState message.
         * @function verify
         * @memberof vesc_trampa.InferenceState
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        InferenceState.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.lastInferenceQueuePtr != null && message.hasOwnProperty("lastInferenceQueuePtr"))
                if (!(message.lastInferenceQueuePtr && typeof message.lastInferenceQueuePtr.length === "number" || $util.isString(message.lastInferenceQueuePtr)))
                    return "lastInferenceQueuePtr: buffer expected";
            if (message.boards != null && message.hasOwnProperty("boards")) {
                if (!Array.isArray(message.boards))
                    return "boards: array expected";
                for (let i = 0; i < message.boards.length; ++i) {
                    let error = $root.vesc_trampa.InferenceState.BoardState.verify(message.boards[i], long + 1);
                    if (error)
                        return "boards." + error;
                }
            }
            return null;
        };

        /**
         * Creates an InferenceState message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof vesc_trampa.InferenceState
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {vesc_trampa.InferenceState} InferenceState
         */
        InferenceState.fromObject = function fromObject(object, long) {
            if (object instanceof $root.vesc_trampa.InferenceState)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.vesc_trampa.InferenceState();
            if (object.lastInferenceQueuePtr != null)
                if (typeof object.lastInferenceQueuePtr === "string")
                    $util.base64.decode(object.lastInferenceQueuePtr, message.lastInferenceQueuePtr = $util.newBuffer($util.base64.length(object.lastInferenceQueuePtr)), 0);
                else if (object.lastInferenceQueuePtr.length >= 0)
                    message.lastInferenceQueuePtr = object.lastInferenceQueuePtr;
            if (object.boards) {
                if (!Array.isArray(object.boards))
                    throw TypeError(".vesc_trampa.InferenceState.boards: array expected");
                message.boards = [];
                for (let i = 0; i < object.boards.length; ++i) {
                    if (typeof object.boards[i] !== "object")
                        throw TypeError(".vesc_trampa.InferenceState.boards: object expected");
                    message.boards[i] = $root.vesc_trampa.InferenceState.BoardState.fromObject(object.boards[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from an InferenceState message. Also converts values to other types if specified.
         * @function toObject
         * @memberof vesc_trampa.InferenceState
         * @static
         * @param {vesc_trampa.InferenceState} message InferenceState
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        InferenceState.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.boards = [];
            if (options.defaults)
                if (options.bytes === String)
                    object.lastInferenceQueuePtr = "";
                else {
                    object.lastInferenceQueuePtr = [];
                    if (options.bytes !== Array)
                        object.lastInferenceQueuePtr = $util.newBuffer(object.lastInferenceQueuePtr);
                }
            if (message.lastInferenceQueuePtr != null && message.hasOwnProperty("lastInferenceQueuePtr"))
                object.lastInferenceQueuePtr = options.bytes === String ? $util.base64.encode(message.lastInferenceQueuePtr, 0, message.lastInferenceQueuePtr.length) : options.bytes === Array ? Array.prototype.slice.call(message.lastInferenceQueuePtr) : message.lastInferenceQueuePtr;
            if (message.boards && message.boards.length) {
                object.boards = [];
                for (let j = 0; j < message.boards.length; ++j)
                    object.boards[j] = $root.vesc_trampa.InferenceState.BoardState.toObject(message.boards[j], options);
            }
            return object;
        };

        /**
         * Converts this InferenceState to JSON.
         * @function toJSON
         * @memberof vesc_trampa.InferenceState
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        InferenceState.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for InferenceState
         * @function getTypeUrl
         * @memberof vesc_trampa.InferenceState
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        InferenceState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/vesc_trampa.InferenceState";
        };

        InferenceState.BoardState = (function() {

            /**
             * Properties of a BoardState.
             * @memberof vesc_trampa.InferenceState
             * @interface IBoardState
             * @property {vesc_trampa.IVescTrampaBoard|null} [board] BoardState board
             * @property {vesc_trampa.VescTrampaMotorMode|null} [motorMode] BoardState motorMode
             * @property {Long|null} [monotonicStampNs] BoardState monotonicStampNs
             * @property {Long|null} [localStampNs] BoardState localStampNs
             * @property {Long|null} [appStartId] BoardState appStartId
             * @property {Uint8Array|null} [valuesPayload] BoardState valuesPayload
             * @property {Uint8Array|null} [valuesRxPointer] BoardState valuesRxPointer
             * @property {Long|null} [valuesMonotonicStampNs] BoardState valuesMonotonicStampNs
             * @property {Long|null} [valuesLocalStampNs] BoardState valuesLocalStampNs
             * @property {Long|null} [valuesAppStartId] BoardState valuesAppStartId
             */

            /**
             * Constructs a new BoardState.
             * @memberof vesc_trampa.InferenceState
             * @classdesc Represents a BoardState.
             * @implements IBoardState
             * @constructor
             * @param {vesc_trampa.InferenceState.IBoardState=} [properties] Properties to set
             */
            function BoardState(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * BoardState board.
             * @member {vesc_trampa.IVescTrampaBoard|null|undefined} board
             * @memberof vesc_trampa.InferenceState.BoardState
             * @instance
             */
            BoardState.prototype.board = null;

            /**
             * BoardState motorMode.
             * @member {vesc_trampa.VescTrampaMotorMode} motorMode
             * @memberof vesc_trampa.InferenceState.BoardState
             * @instance
             */
            BoardState.prototype.motorMode = 0;

            /**
             * BoardState monotonicStampNs.
             * @member {Long} monotonicStampNs
             * @memberof vesc_trampa.InferenceState.BoardState
             * @instance
             */
            BoardState.prototype.monotonicStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

            /**
             * BoardState localStampNs.
             * @member {Long} localStampNs
             * @memberof vesc_trampa.InferenceState.BoardState
             * @instance
             */
            BoardState.prototype.localStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

            /**
             * BoardState appStartId.
             * @member {Long} appStartId
             * @memberof vesc_trampa.InferenceState.BoardState
             * @instance
             */
            BoardState.prototype.appStartId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

            /**
             * BoardState valuesPayload.
             * @member {Uint8Array} valuesPayload
             * @memberof vesc_trampa.InferenceState.BoardState
             * @instance
             */
            BoardState.prototype.valuesPayload = $util.newBuffer([]);

            /**
             * BoardState valuesRxPointer.
             * @member {Uint8Array} valuesRxPointer
             * @memberof vesc_trampa.InferenceState.BoardState
             * @instance
             */
            BoardState.prototype.valuesRxPointer = $util.newBuffer([]);

            /**
             * BoardState valuesMonotonicStampNs.
             * @member {Long} valuesMonotonicStampNs
             * @memberof vesc_trampa.InferenceState.BoardState
             * @instance
             */
            BoardState.prototype.valuesMonotonicStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

            /**
             * BoardState valuesLocalStampNs.
             * @member {Long} valuesLocalStampNs
             * @memberof vesc_trampa.InferenceState.BoardState
             * @instance
             */
            BoardState.prototype.valuesLocalStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

            /**
             * BoardState valuesAppStartId.
             * @member {Long} valuesAppStartId
             * @memberof vesc_trampa.InferenceState.BoardState
             * @instance
             */
            BoardState.prototype.valuesAppStartId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

            /**
             * Creates a new BoardState instance using the specified properties.
             * @function create
             * @memberof vesc_trampa.InferenceState.BoardState
             * @static
             * @param {vesc_trampa.InferenceState.IBoardState=} [properties] Properties to set
             * @returns {vesc_trampa.InferenceState.BoardState} BoardState instance
             */
            BoardState.create = function create(properties) {
                return new BoardState(properties);
            };

            /**
             * Encodes the specified BoardState message. Does not implicitly {@link vesc_trampa.InferenceState.BoardState.verify|verify} messages.
             * @function encode
             * @memberof vesc_trampa.InferenceState.BoardState
             * @static
             * @param {vesc_trampa.InferenceState.IBoardState} message BoardState message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BoardState.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.board != null && Object.hasOwnProperty.call(message, "board"))
                    $root.vesc_trampa.VescTrampaBoard.encode(message.board, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
                if (message.motorMode != null && Object.hasOwnProperty.call(message, "motorMode"))
                    writer.uint32(/* id 2, wireType 0 =*/16).int32(message.motorMode);
                if (message.monotonicStampNs != null && Object.hasOwnProperty.call(message, "monotonicStampNs"))
                    writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.monotonicStampNs);
                if (message.localStampNs != null && Object.hasOwnProperty.call(message, "localStampNs"))
                    writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.localStampNs);
                if (message.appStartId != null && Object.hasOwnProperty.call(message, "appStartId"))
                    writer.uint32(/* id 5, wireType 0 =*/40).uint64(message.appStartId);
                if (message.valuesPayload != null && Object.hasOwnProperty.call(message, "valuesPayload"))
                    writer.uint32(/* id 10, wireType 2 =*/82).bytes(message.valuesPayload);
                if (message.valuesRxPointer != null && Object.hasOwnProperty.call(message, "valuesRxPointer"))
                    writer.uint32(/* id 11, wireType 2 =*/90).bytes(message.valuesRxPointer);
                if (message.valuesMonotonicStampNs != null && Object.hasOwnProperty.call(message, "valuesMonotonicStampNs"))
                    writer.uint32(/* id 12, wireType 0 =*/96).uint64(message.valuesMonotonicStampNs);
                if (message.valuesLocalStampNs != null && Object.hasOwnProperty.call(message, "valuesLocalStampNs"))
                    writer.uint32(/* id 13, wireType 0 =*/104).uint64(message.valuesLocalStampNs);
                if (message.valuesAppStartId != null && Object.hasOwnProperty.call(message, "valuesAppStartId"))
                    writer.uint32(/* id 14, wireType 0 =*/112).uint64(message.valuesAppStartId);
                return writer;
            };

            /**
             * Encodes the specified BoardState message, length delimited. Does not implicitly {@link vesc_trampa.InferenceState.BoardState.verify|verify} messages.
             * @function encodeDelimited
             * @memberof vesc_trampa.InferenceState.BoardState
             * @static
             * @param {vesc_trampa.InferenceState.IBoardState} message BoardState message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BoardState.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a BoardState message from the specified reader or buffer.
             * @function decode
             * @memberof vesc_trampa.InferenceState.BoardState
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {vesc_trampa.InferenceState.BoardState} BoardState
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BoardState.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.vesc_trampa.InferenceState.BoardState();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.board = $root.vesc_trampa.VescTrampaBoard.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 2: {
                            message.motorMode = reader.int32();
                            break;
                        }
                    case 3: {
                            message.monotonicStampNs = reader.uint64();
                            break;
                        }
                    case 4: {
                            message.localStampNs = reader.uint64();
                            break;
                        }
                    case 5: {
                            message.appStartId = reader.uint64();
                            break;
                        }
                    case 10: {
                            message.valuesPayload = reader.bytes();
                            break;
                        }
                    case 11: {
                            message.valuesRxPointer = reader.bytes();
                            break;
                        }
                    case 12: {
                            message.valuesMonotonicStampNs = reader.uint64();
                            break;
                        }
                    case 13: {
                            message.valuesLocalStampNs = reader.uint64();
                            break;
                        }
                    case 14: {
                            message.valuesAppStartId = reader.uint64();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a BoardState message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof vesc_trampa.InferenceState.BoardState
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {vesc_trampa.InferenceState.BoardState} BoardState
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BoardState.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a BoardState message.
             * @function verify
             * @memberof vesc_trampa.InferenceState.BoardState
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            BoardState.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.board != null && message.hasOwnProperty("board")) {
                    let error = $root.vesc_trampa.VescTrampaBoard.verify(message.board, long + 1);
                    if (error)
                        return "board." + error;
                }
                if (message.motorMode != null && message.hasOwnProperty("motorMode"))
                    switch (message.motorMode) {
                    default:
                        return "motorMode: enum value expected";
                    case 0:
                    case 1:
                        break;
                    }
                if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                    if (!$util.isInteger(message.monotonicStampNs) && !(message.monotonicStampNs && $util.isInteger(message.monotonicStampNs.low) && $util.isInteger(message.monotonicStampNs.high)))
                        return "monotonicStampNs: integer|Long expected";
                if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                    if (!$util.isInteger(message.localStampNs) && !(message.localStampNs && $util.isInteger(message.localStampNs.low) && $util.isInteger(message.localStampNs.high)))
                        return "localStampNs: integer|Long expected";
                if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                    if (!$util.isInteger(message.appStartId) && !(message.appStartId && $util.isInteger(message.appStartId.low) && $util.isInteger(message.appStartId.high)))
                        return "appStartId: integer|Long expected";
                if (message.valuesPayload != null && message.hasOwnProperty("valuesPayload"))
                    if (!(message.valuesPayload && typeof message.valuesPayload.length === "number" || $util.isString(message.valuesPayload)))
                        return "valuesPayload: buffer expected";
                if (message.valuesRxPointer != null && message.hasOwnProperty("valuesRxPointer"))
                    if (!(message.valuesRxPointer && typeof message.valuesRxPointer.length === "number" || $util.isString(message.valuesRxPointer)))
                        return "valuesRxPointer: buffer expected";
                if (message.valuesMonotonicStampNs != null && message.hasOwnProperty("valuesMonotonicStampNs"))
                    if (!$util.isInteger(message.valuesMonotonicStampNs) && !(message.valuesMonotonicStampNs && $util.isInteger(message.valuesMonotonicStampNs.low) && $util.isInteger(message.valuesMonotonicStampNs.high)))
                        return "valuesMonotonicStampNs: integer|Long expected";
                if (message.valuesLocalStampNs != null && message.hasOwnProperty("valuesLocalStampNs"))
                    if (!$util.isInteger(message.valuesLocalStampNs) && !(message.valuesLocalStampNs && $util.isInteger(message.valuesLocalStampNs.low) && $util.isInteger(message.valuesLocalStampNs.high)))
                        return "valuesLocalStampNs: integer|Long expected";
                if (message.valuesAppStartId != null && message.hasOwnProperty("valuesAppStartId"))
                    if (!$util.isInteger(message.valuesAppStartId) && !(message.valuesAppStartId && $util.isInteger(message.valuesAppStartId.low) && $util.isInteger(message.valuesAppStartId.high)))
                        return "valuesAppStartId: integer|Long expected";
                return null;
            };

            /**
             * Creates a BoardState message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof vesc_trampa.InferenceState.BoardState
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {vesc_trampa.InferenceState.BoardState} BoardState
             */
            BoardState.fromObject = function fromObject(object, long) {
                if (object instanceof $root.vesc_trampa.InferenceState.BoardState)
                    return object;
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.vesc_trampa.InferenceState.BoardState();
                if (object.board != null) {
                    if (typeof object.board !== "object")
                        throw TypeError(".vesc_trampa.InferenceState.BoardState.board: object expected");
                    message.board = $root.vesc_trampa.VescTrampaBoard.fromObject(object.board, long + 1);
                }
                switch (object.motorMode) {
                default:
                    if (typeof object.motorMode === "number") {
                        message.motorMode = object.motorMode;
                        break;
                    }
                    break;
                case "VESC_TRAMPA_MOTOR_MODE_UNSPECIFIED":
                case 0:
                    message.motorMode = 0;
                    break;
                case "VESC_TRAMPA_MOTOR_MODE_HOLD":
                case 1:
                    message.motorMode = 1;
                    break;
                }
                if (object.monotonicStampNs != null)
                    if ($util.Long)
                        (message.monotonicStampNs = $util.Long.fromValue(object.monotonicStampNs)).unsigned = true;
                    else if (typeof object.monotonicStampNs === "string")
                        message.monotonicStampNs = parseInt(object.monotonicStampNs, 10);
                    else if (typeof object.monotonicStampNs === "number")
                        message.monotonicStampNs = object.monotonicStampNs;
                    else if (typeof object.monotonicStampNs === "object")
                        message.monotonicStampNs = new $util.LongBits(object.monotonicStampNs.low >>> 0, object.monotonicStampNs.high >>> 0).toNumber(true);
                if (object.localStampNs != null)
                    if ($util.Long)
                        (message.localStampNs = $util.Long.fromValue(object.localStampNs)).unsigned = true;
                    else if (typeof object.localStampNs === "string")
                        message.localStampNs = parseInt(object.localStampNs, 10);
                    else if (typeof object.localStampNs === "number")
                        message.localStampNs = object.localStampNs;
                    else if (typeof object.localStampNs === "object")
                        message.localStampNs = new $util.LongBits(object.localStampNs.low >>> 0, object.localStampNs.high >>> 0).toNumber(true);
                if (object.appStartId != null)
                    if ($util.Long)
                        (message.appStartId = $util.Long.fromValue(object.appStartId)).unsigned = true;
                    else if (typeof object.appStartId === "string")
                        message.appStartId = parseInt(object.appStartId, 10);
                    else if (typeof object.appStartId === "number")
                        message.appStartId = object.appStartId;
                    else if (typeof object.appStartId === "object")
                        message.appStartId = new $util.LongBits(object.appStartId.low >>> 0, object.appStartId.high >>> 0).toNumber(true);
                if (object.valuesPayload != null)
                    if (typeof object.valuesPayload === "string")
                        $util.base64.decode(object.valuesPayload, message.valuesPayload = $util.newBuffer($util.base64.length(object.valuesPayload)), 0);
                    else if (object.valuesPayload.length >= 0)
                        message.valuesPayload = object.valuesPayload;
                if (object.valuesRxPointer != null)
                    if (typeof object.valuesRxPointer === "string")
                        $util.base64.decode(object.valuesRxPointer, message.valuesRxPointer = $util.newBuffer($util.base64.length(object.valuesRxPointer)), 0);
                    else if (object.valuesRxPointer.length >= 0)
                        message.valuesRxPointer = object.valuesRxPointer;
                if (object.valuesMonotonicStampNs != null)
                    if ($util.Long)
                        (message.valuesMonotonicStampNs = $util.Long.fromValue(object.valuesMonotonicStampNs)).unsigned = true;
                    else if (typeof object.valuesMonotonicStampNs === "string")
                        message.valuesMonotonicStampNs = parseInt(object.valuesMonotonicStampNs, 10);
                    else if (typeof object.valuesMonotonicStampNs === "number")
                        message.valuesMonotonicStampNs = object.valuesMonotonicStampNs;
                    else if (typeof object.valuesMonotonicStampNs === "object")
                        message.valuesMonotonicStampNs = new $util.LongBits(object.valuesMonotonicStampNs.low >>> 0, object.valuesMonotonicStampNs.high >>> 0).toNumber(true);
                if (object.valuesLocalStampNs != null)
                    if ($util.Long)
                        (message.valuesLocalStampNs = $util.Long.fromValue(object.valuesLocalStampNs)).unsigned = true;
                    else if (typeof object.valuesLocalStampNs === "string")
                        message.valuesLocalStampNs = parseInt(object.valuesLocalStampNs, 10);
                    else if (typeof object.valuesLocalStampNs === "number")
                        message.valuesLocalStampNs = object.valuesLocalStampNs;
                    else if (typeof object.valuesLocalStampNs === "object")
                        message.valuesLocalStampNs = new $util.LongBits(object.valuesLocalStampNs.low >>> 0, object.valuesLocalStampNs.high >>> 0).toNumber(true);
                if (object.valuesAppStartId != null)
                    if ($util.Long)
                        (message.valuesAppStartId = $util.Long.fromValue(object.valuesAppStartId)).unsigned = true;
                    else if (typeof object.valuesAppStartId === "string")
                        message.valuesAppStartId = parseInt(object.valuesAppStartId, 10);
                    else if (typeof object.valuesAppStartId === "number")
                        message.valuesAppStartId = object.valuesAppStartId;
                    else if (typeof object.valuesAppStartId === "object")
                        message.valuesAppStartId = new $util.LongBits(object.valuesAppStartId.low >>> 0, object.valuesAppStartId.high >>> 0).toNumber(true);
                return message;
            };

            /**
             * Creates a plain object from a BoardState message. Also converts values to other types if specified.
             * @function toObject
             * @memberof vesc_trampa.InferenceState.BoardState
             * @static
             * @param {vesc_trampa.InferenceState.BoardState} message BoardState
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            BoardState.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.board = null;
                    object.motorMode = options.enums === String ? "VESC_TRAMPA_MOTOR_MODE_UNSPECIFIED" : 0;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, true);
                        object.monotonicStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                    } else
                        object.monotonicStampNs = options.longs === String ? "0" : 0;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, true);
                        object.localStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                    } else
                        object.localStampNs = options.longs === String ? "0" : 0;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, true);
                        object.appStartId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                    } else
                        object.appStartId = options.longs === String ? "0" : 0;
                    if (options.bytes === String)
                        object.valuesPayload = "";
                    else {
                        object.valuesPayload = [];
                        if (options.bytes !== Array)
                            object.valuesPayload = $util.newBuffer(object.valuesPayload);
                    }
                    if (options.bytes === String)
                        object.valuesRxPointer = "";
                    else {
                        object.valuesRxPointer = [];
                        if (options.bytes !== Array)
                            object.valuesRxPointer = $util.newBuffer(object.valuesRxPointer);
                    }
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, true);
                        object.valuesMonotonicStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                    } else
                        object.valuesMonotonicStampNs = options.longs === String ? "0" : 0;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, true);
                        object.valuesLocalStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                    } else
                        object.valuesLocalStampNs = options.longs === String ? "0" : 0;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, true);
                        object.valuesAppStartId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                    } else
                        object.valuesAppStartId = options.longs === String ? "0" : 0;
                }
                if (message.board != null && message.hasOwnProperty("board"))
                    object.board = $root.vesc_trampa.VescTrampaBoard.toObject(message.board, options);
                if (message.motorMode != null && message.hasOwnProperty("motorMode"))
                    object.motorMode = options.enums === String ? $root.vesc_trampa.VescTrampaMotorMode[message.motorMode] === undefined ? message.motorMode : $root.vesc_trampa.VescTrampaMotorMode[message.motorMode] : message.motorMode;
                if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                    if (typeof message.monotonicStampNs === "number")
                        object.monotonicStampNs = options.longs === String ? String(message.monotonicStampNs) : message.monotonicStampNs;
                    else
                        object.monotonicStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.monotonicStampNs) : options.longs === Number ? new $util.LongBits(message.monotonicStampNs.low >>> 0, message.monotonicStampNs.high >>> 0).toNumber(true) : message.monotonicStampNs;
                if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                    if (typeof message.localStampNs === "number")
                        object.localStampNs = options.longs === String ? String(message.localStampNs) : message.localStampNs;
                    else
                        object.localStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.localStampNs) : options.longs === Number ? new $util.LongBits(message.localStampNs.low >>> 0, message.localStampNs.high >>> 0).toNumber(true) : message.localStampNs;
                if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                    if (typeof message.appStartId === "number")
                        object.appStartId = options.longs === String ? String(message.appStartId) : message.appStartId;
                    else
                        object.appStartId = options.longs === String ? $util.Long.prototype.toString.call(message.appStartId) : options.longs === Number ? new $util.LongBits(message.appStartId.low >>> 0, message.appStartId.high >>> 0).toNumber(true) : message.appStartId;
                if (message.valuesPayload != null && message.hasOwnProperty("valuesPayload"))
                    object.valuesPayload = options.bytes === String ? $util.base64.encode(message.valuesPayload, 0, message.valuesPayload.length) : options.bytes === Array ? Array.prototype.slice.call(message.valuesPayload) : message.valuesPayload;
                if (message.valuesRxPointer != null && message.hasOwnProperty("valuesRxPointer"))
                    object.valuesRxPointer = options.bytes === String ? $util.base64.encode(message.valuesRxPointer, 0, message.valuesRxPointer.length) : options.bytes === Array ? Array.prototype.slice.call(message.valuesRxPointer) : message.valuesRxPointer;
                if (message.valuesMonotonicStampNs != null && message.hasOwnProperty("valuesMonotonicStampNs"))
                    if (typeof message.valuesMonotonicStampNs === "number")
                        object.valuesMonotonicStampNs = options.longs === String ? String(message.valuesMonotonicStampNs) : message.valuesMonotonicStampNs;
                    else
                        object.valuesMonotonicStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.valuesMonotonicStampNs) : options.longs === Number ? new $util.LongBits(message.valuesMonotonicStampNs.low >>> 0, message.valuesMonotonicStampNs.high >>> 0).toNumber(true) : message.valuesMonotonicStampNs;
                if (message.valuesLocalStampNs != null && message.hasOwnProperty("valuesLocalStampNs"))
                    if (typeof message.valuesLocalStampNs === "number")
                        object.valuesLocalStampNs = options.longs === String ? String(message.valuesLocalStampNs) : message.valuesLocalStampNs;
                    else
                        object.valuesLocalStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.valuesLocalStampNs) : options.longs === Number ? new $util.LongBits(message.valuesLocalStampNs.low >>> 0, message.valuesLocalStampNs.high >>> 0).toNumber(true) : message.valuesLocalStampNs;
                if (message.valuesAppStartId != null && message.hasOwnProperty("valuesAppStartId"))
                    if (typeof message.valuesAppStartId === "number")
                        object.valuesAppStartId = options.longs === String ? String(message.valuesAppStartId) : message.valuesAppStartId;
                    else
                        object.valuesAppStartId = options.longs === String ? $util.Long.prototype.toString.call(message.valuesAppStartId) : options.longs === Number ? new $util.LongBits(message.valuesAppStartId.low >>> 0, message.valuesAppStartId.high >>> 0).toNumber(true) : message.valuesAppStartId;
                return object;
            };

            /**
             * Converts this BoardState to JSON.
             * @function toJSON
             * @memberof vesc_trampa.InferenceState.BoardState
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            BoardState.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for BoardState
             * @function getTypeUrl
             * @memberof vesc_trampa.InferenceState.BoardState
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            BoardState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/vesc_trampa.InferenceState.BoardState";
            };

            return BoardState;
        })();

        return InferenceState;
    })();

    vesc_trampa.VescTrampaBoard = (function() {

        /**
         * Properties of a VescTrampaBoard.
         * @memberof vesc_trampa
         * @interface IVescTrampaBoard
         * @property {string|null} [portName] VescTrampaBoard portName
         * @property {number|null} [vid] VescTrampaBoard vid
         * @property {number|null} [pid] VescTrampaBoard pid
         * @property {string|null} [serialNumber] VescTrampaBoard serialNumber
         * @property {string|null} [manufacturer] VescTrampaBoard manufacturer
         * @property {string|null} [product] VescTrampaBoard product
         * @property {number|null} [portBaudRate] VescTrampaBoard portBaudRate
         * @property {number|null} [firmwareMajor] VescTrampaBoard firmwareMajor
         * @property {number|null} [firmwareMinor] VescTrampaBoard firmwareMinor
         * @property {string|null} [hardwareName] VescTrampaBoard hardwareName
         * @property {Uint8Array|null} [uuid] VescTrampaBoard uuid
         * @property {boolean|null} [pairingDone] VescTrampaBoard pairingDone
         * @property {number|null} [testVersionNumber] VescTrampaBoard testVersionNumber
         * @property {number|null} [hardwareType] VescTrampaBoard hardwareType
         * @property {number|null} [customConfigCount] VescTrampaBoard customConfigCount
         * @property {boolean|null} [hasPhaseFilters] VescTrampaBoard hasPhaseFilters
         * @property {number|null} [qmlHw] VescTrampaBoard qmlHw
         * @property {number|null} [qmlApp] VescTrampaBoard qmlApp
         * @property {number|null} [nrfFlags] VescTrampaBoard nrfFlags
         * @property {string|null} [firmwareName] VescTrampaBoard firmwareName
         * @property {number|null} [hardwareConfigCrc] VescTrampaBoard hardwareConfigCrc
         * @property {Uint8Array|null} [firmwareInfoExtraBytes] VescTrampaBoard firmwareInfoExtraBytes
         * @property {Uint8Array|null} [firmwareInfoRawPayload] VescTrampaBoard firmwareInfoRawPayload
         */

        /**
         * Constructs a new VescTrampaBoard.
         * @memberof vesc_trampa
         * @classdesc Represents a VescTrampaBoard.
         * @implements IVescTrampaBoard
         * @constructor
         * @param {vesc_trampa.IVescTrampaBoard=} [properties] Properties to set
         */
        function VescTrampaBoard(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * VescTrampaBoard portName.
         * @member {string} portName
         * @memberof vesc_trampa.VescTrampaBoard
         * @instance
         */
        VescTrampaBoard.prototype.portName = "";

        /**
         * VescTrampaBoard vid.
         * @member {number} vid
         * @memberof vesc_trampa.VescTrampaBoard
         * @instance
         */
        VescTrampaBoard.prototype.vid = 0;

        /**
         * VescTrampaBoard pid.
         * @member {number} pid
         * @memberof vesc_trampa.VescTrampaBoard
         * @instance
         */
        VescTrampaBoard.prototype.pid = 0;

        /**
         * VescTrampaBoard serialNumber.
         * @member {string} serialNumber
         * @memberof vesc_trampa.VescTrampaBoard
         * @instance
         */
        VescTrampaBoard.prototype.serialNumber = "";

        /**
         * VescTrampaBoard manufacturer.
         * @member {string} manufacturer
         * @memberof vesc_trampa.VescTrampaBoard
         * @instance
         */
        VescTrampaBoard.prototype.manufacturer = "";

        /**
         * VescTrampaBoard product.
         * @member {string} product
         * @memberof vesc_trampa.VescTrampaBoard
         * @instance
         */
        VescTrampaBoard.prototype.product = "";

        /**
         * VescTrampaBoard portBaudRate.
         * @member {number} portBaudRate
         * @memberof vesc_trampa.VescTrampaBoard
         * @instance
         */
        VescTrampaBoard.prototype.portBaudRate = 0;

        /**
         * VescTrampaBoard firmwareMajor.
         * @member {number} firmwareMajor
         * @memberof vesc_trampa.VescTrampaBoard
         * @instance
         */
        VescTrampaBoard.prototype.firmwareMajor = 0;

        /**
         * VescTrampaBoard firmwareMinor.
         * @member {number} firmwareMinor
         * @memberof vesc_trampa.VescTrampaBoard
         * @instance
         */
        VescTrampaBoard.prototype.firmwareMinor = 0;

        /**
         * VescTrampaBoard hardwareName.
         * @member {string} hardwareName
         * @memberof vesc_trampa.VescTrampaBoard
         * @instance
         */
        VescTrampaBoard.prototype.hardwareName = "";

        /**
         * VescTrampaBoard uuid.
         * @member {Uint8Array} uuid
         * @memberof vesc_trampa.VescTrampaBoard
         * @instance
         */
        VescTrampaBoard.prototype.uuid = $util.newBuffer([]);

        /**
         * VescTrampaBoard pairingDone.
         * @member {boolean} pairingDone
         * @memberof vesc_trampa.VescTrampaBoard
         * @instance
         */
        VescTrampaBoard.prototype.pairingDone = false;

        /**
         * VescTrampaBoard testVersionNumber.
         * @member {number} testVersionNumber
         * @memberof vesc_trampa.VescTrampaBoard
         * @instance
         */
        VescTrampaBoard.prototype.testVersionNumber = 0;

        /**
         * VescTrampaBoard hardwareType.
         * @member {number} hardwareType
         * @memberof vesc_trampa.VescTrampaBoard
         * @instance
         */
        VescTrampaBoard.prototype.hardwareType = 0;

        /**
         * VescTrampaBoard customConfigCount.
         * @member {number} customConfigCount
         * @memberof vesc_trampa.VescTrampaBoard
         * @instance
         */
        VescTrampaBoard.prototype.customConfigCount = 0;

        /**
         * VescTrampaBoard hasPhaseFilters.
         * @member {boolean} hasPhaseFilters
         * @memberof vesc_trampa.VescTrampaBoard
         * @instance
         */
        VescTrampaBoard.prototype.hasPhaseFilters = false;

        /**
         * VescTrampaBoard qmlHw.
         * @member {number} qmlHw
         * @memberof vesc_trampa.VescTrampaBoard
         * @instance
         */
        VescTrampaBoard.prototype.qmlHw = 0;

        /**
         * VescTrampaBoard qmlApp.
         * @member {number} qmlApp
         * @memberof vesc_trampa.VescTrampaBoard
         * @instance
         */
        VescTrampaBoard.prototype.qmlApp = 0;

        /**
         * VescTrampaBoard nrfFlags.
         * @member {number} nrfFlags
         * @memberof vesc_trampa.VescTrampaBoard
         * @instance
         */
        VescTrampaBoard.prototype.nrfFlags = 0;

        /**
         * VescTrampaBoard firmwareName.
         * @member {string} firmwareName
         * @memberof vesc_trampa.VescTrampaBoard
         * @instance
         */
        VescTrampaBoard.prototype.firmwareName = "";

        /**
         * VescTrampaBoard hardwareConfigCrc.
         * @member {number} hardwareConfigCrc
         * @memberof vesc_trampa.VescTrampaBoard
         * @instance
         */
        VescTrampaBoard.prototype.hardwareConfigCrc = 0;

        /**
         * VescTrampaBoard firmwareInfoExtraBytes.
         * @member {Uint8Array} firmwareInfoExtraBytes
         * @memberof vesc_trampa.VescTrampaBoard
         * @instance
         */
        VescTrampaBoard.prototype.firmwareInfoExtraBytes = $util.newBuffer([]);

        /**
         * VescTrampaBoard firmwareInfoRawPayload.
         * @member {Uint8Array} firmwareInfoRawPayload
         * @memberof vesc_trampa.VescTrampaBoard
         * @instance
         */
        VescTrampaBoard.prototype.firmwareInfoRawPayload = $util.newBuffer([]);

        /**
         * Creates a new VescTrampaBoard instance using the specified properties.
         * @function create
         * @memberof vesc_trampa.VescTrampaBoard
         * @static
         * @param {vesc_trampa.IVescTrampaBoard=} [properties] Properties to set
         * @returns {vesc_trampa.VescTrampaBoard} VescTrampaBoard instance
         */
        VescTrampaBoard.create = function create(properties) {
            return new VescTrampaBoard(properties);
        };

        /**
         * Encodes the specified VescTrampaBoard message. Does not implicitly {@link vesc_trampa.VescTrampaBoard.verify|verify} messages.
         * @function encode
         * @memberof vesc_trampa.VescTrampaBoard
         * @static
         * @param {vesc_trampa.IVescTrampaBoard} message VescTrampaBoard message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        VescTrampaBoard.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.portName != null && Object.hasOwnProperty.call(message, "portName"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.portName);
            if (message.vid != null && Object.hasOwnProperty.call(message, "vid"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.vid);
            if (message.pid != null && Object.hasOwnProperty.call(message, "pid"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.pid);
            if (message.serialNumber != null && Object.hasOwnProperty.call(message, "serialNumber"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.serialNumber);
            if (message.manufacturer != null && Object.hasOwnProperty.call(message, "manufacturer"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.manufacturer);
            if (message.product != null && Object.hasOwnProperty.call(message, "product"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.product);
            if (message.portBaudRate != null && Object.hasOwnProperty.call(message, "portBaudRate"))
                writer.uint32(/* id 7, wireType 0 =*/56).uint32(message.portBaudRate);
            if (message.firmwareMajor != null && Object.hasOwnProperty.call(message, "firmwareMajor"))
                writer.uint32(/* id 20, wireType 0 =*/160).uint32(message.firmwareMajor);
            if (message.firmwareMinor != null && Object.hasOwnProperty.call(message, "firmwareMinor"))
                writer.uint32(/* id 21, wireType 0 =*/168).uint32(message.firmwareMinor);
            if (message.hardwareName != null && Object.hasOwnProperty.call(message, "hardwareName"))
                writer.uint32(/* id 22, wireType 2 =*/178).string(message.hardwareName);
            if (message.uuid != null && Object.hasOwnProperty.call(message, "uuid"))
                writer.uint32(/* id 23, wireType 2 =*/186).bytes(message.uuid);
            if (message.pairingDone != null && Object.hasOwnProperty.call(message, "pairingDone"))
                writer.uint32(/* id 24, wireType 0 =*/192).bool(message.pairingDone);
            if (message.testVersionNumber != null && Object.hasOwnProperty.call(message, "testVersionNumber"))
                writer.uint32(/* id 25, wireType 0 =*/200).uint32(message.testVersionNumber);
            if (message.hardwareType != null && Object.hasOwnProperty.call(message, "hardwareType"))
                writer.uint32(/* id 26, wireType 0 =*/208).uint32(message.hardwareType);
            if (message.customConfigCount != null && Object.hasOwnProperty.call(message, "customConfigCount"))
                writer.uint32(/* id 27, wireType 0 =*/216).uint32(message.customConfigCount);
            if (message.hasPhaseFilters != null && Object.hasOwnProperty.call(message, "hasPhaseFilters"))
                writer.uint32(/* id 28, wireType 0 =*/224).bool(message.hasPhaseFilters);
            if (message.qmlHw != null && Object.hasOwnProperty.call(message, "qmlHw"))
                writer.uint32(/* id 29, wireType 0 =*/232).uint32(message.qmlHw);
            if (message.qmlApp != null && Object.hasOwnProperty.call(message, "qmlApp"))
                writer.uint32(/* id 30, wireType 0 =*/240).uint32(message.qmlApp);
            if (message.nrfFlags != null && Object.hasOwnProperty.call(message, "nrfFlags"))
                writer.uint32(/* id 31, wireType 0 =*/248).uint32(message.nrfFlags);
            if (message.firmwareName != null && Object.hasOwnProperty.call(message, "firmwareName"))
                writer.uint32(/* id 32, wireType 2 =*/258).string(message.firmwareName);
            if (message.hardwareConfigCrc != null && Object.hasOwnProperty.call(message, "hardwareConfigCrc"))
                writer.uint32(/* id 33, wireType 0 =*/264).uint32(message.hardwareConfigCrc);
            if (message.firmwareInfoExtraBytes != null && Object.hasOwnProperty.call(message, "firmwareInfoExtraBytes"))
                writer.uint32(/* id 34, wireType 2 =*/274).bytes(message.firmwareInfoExtraBytes);
            if (message.firmwareInfoRawPayload != null && Object.hasOwnProperty.call(message, "firmwareInfoRawPayload"))
                writer.uint32(/* id 35, wireType 2 =*/282).bytes(message.firmwareInfoRawPayload);
            return writer;
        };

        /**
         * Encodes the specified VescTrampaBoard message, length delimited. Does not implicitly {@link vesc_trampa.VescTrampaBoard.verify|verify} messages.
         * @function encodeDelimited
         * @memberof vesc_trampa.VescTrampaBoard
         * @static
         * @param {vesc_trampa.IVescTrampaBoard} message VescTrampaBoard message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        VescTrampaBoard.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a VescTrampaBoard message from the specified reader or buffer.
         * @function decode
         * @memberof vesc_trampa.VescTrampaBoard
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {vesc_trampa.VescTrampaBoard} VescTrampaBoard
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        VescTrampaBoard.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.vesc_trampa.VescTrampaBoard();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.portName = reader.string();
                        break;
                    }
                case 2: {
                        message.vid = reader.uint32();
                        break;
                    }
                case 3: {
                        message.pid = reader.uint32();
                        break;
                    }
                case 4: {
                        message.serialNumber = reader.string();
                        break;
                    }
                case 5: {
                        message.manufacturer = reader.string();
                        break;
                    }
                case 6: {
                        message.product = reader.string();
                        break;
                    }
                case 7: {
                        message.portBaudRate = reader.uint32();
                        break;
                    }
                case 20: {
                        message.firmwareMajor = reader.uint32();
                        break;
                    }
                case 21: {
                        message.firmwareMinor = reader.uint32();
                        break;
                    }
                case 22: {
                        message.hardwareName = reader.string();
                        break;
                    }
                case 23: {
                        message.uuid = reader.bytes();
                        break;
                    }
                case 24: {
                        message.pairingDone = reader.bool();
                        break;
                    }
                case 25: {
                        message.testVersionNumber = reader.uint32();
                        break;
                    }
                case 26: {
                        message.hardwareType = reader.uint32();
                        break;
                    }
                case 27: {
                        message.customConfigCount = reader.uint32();
                        break;
                    }
                case 28: {
                        message.hasPhaseFilters = reader.bool();
                        break;
                    }
                case 29: {
                        message.qmlHw = reader.uint32();
                        break;
                    }
                case 30: {
                        message.qmlApp = reader.uint32();
                        break;
                    }
                case 31: {
                        message.nrfFlags = reader.uint32();
                        break;
                    }
                case 32: {
                        message.firmwareName = reader.string();
                        break;
                    }
                case 33: {
                        message.hardwareConfigCrc = reader.uint32();
                        break;
                    }
                case 34: {
                        message.firmwareInfoExtraBytes = reader.bytes();
                        break;
                    }
                case 35: {
                        message.firmwareInfoRawPayload = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a VescTrampaBoard message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof vesc_trampa.VescTrampaBoard
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {vesc_trampa.VescTrampaBoard} VescTrampaBoard
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        VescTrampaBoard.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a VescTrampaBoard message.
         * @function verify
         * @memberof vesc_trampa.VescTrampaBoard
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        VescTrampaBoard.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.portName != null && message.hasOwnProperty("portName"))
                if (!$util.isString(message.portName))
                    return "portName: string expected";
            if (message.vid != null && message.hasOwnProperty("vid"))
                if (!$util.isInteger(message.vid))
                    return "vid: integer expected";
            if (message.pid != null && message.hasOwnProperty("pid"))
                if (!$util.isInteger(message.pid))
                    return "pid: integer expected";
            if (message.serialNumber != null && message.hasOwnProperty("serialNumber"))
                if (!$util.isString(message.serialNumber))
                    return "serialNumber: string expected";
            if (message.manufacturer != null && message.hasOwnProperty("manufacturer"))
                if (!$util.isString(message.manufacturer))
                    return "manufacturer: string expected";
            if (message.product != null && message.hasOwnProperty("product"))
                if (!$util.isString(message.product))
                    return "product: string expected";
            if (message.portBaudRate != null && message.hasOwnProperty("portBaudRate"))
                if (!$util.isInteger(message.portBaudRate))
                    return "portBaudRate: integer expected";
            if (message.firmwareMajor != null && message.hasOwnProperty("firmwareMajor"))
                if (!$util.isInteger(message.firmwareMajor))
                    return "firmwareMajor: integer expected";
            if (message.firmwareMinor != null && message.hasOwnProperty("firmwareMinor"))
                if (!$util.isInteger(message.firmwareMinor))
                    return "firmwareMinor: integer expected";
            if (message.hardwareName != null && message.hasOwnProperty("hardwareName"))
                if (!$util.isString(message.hardwareName))
                    return "hardwareName: string expected";
            if (message.uuid != null && message.hasOwnProperty("uuid"))
                if (!(message.uuid && typeof message.uuid.length === "number" || $util.isString(message.uuid)))
                    return "uuid: buffer expected";
            if (message.pairingDone != null && message.hasOwnProperty("pairingDone"))
                if (typeof message.pairingDone !== "boolean")
                    return "pairingDone: boolean expected";
            if (message.testVersionNumber != null && message.hasOwnProperty("testVersionNumber"))
                if (!$util.isInteger(message.testVersionNumber))
                    return "testVersionNumber: integer expected";
            if (message.hardwareType != null && message.hasOwnProperty("hardwareType"))
                if (!$util.isInteger(message.hardwareType))
                    return "hardwareType: integer expected";
            if (message.customConfigCount != null && message.hasOwnProperty("customConfigCount"))
                if (!$util.isInteger(message.customConfigCount))
                    return "customConfigCount: integer expected";
            if (message.hasPhaseFilters != null && message.hasOwnProperty("hasPhaseFilters"))
                if (typeof message.hasPhaseFilters !== "boolean")
                    return "hasPhaseFilters: boolean expected";
            if (message.qmlHw != null && message.hasOwnProperty("qmlHw"))
                if (!$util.isInteger(message.qmlHw))
                    return "qmlHw: integer expected";
            if (message.qmlApp != null && message.hasOwnProperty("qmlApp"))
                if (!$util.isInteger(message.qmlApp))
                    return "qmlApp: integer expected";
            if (message.nrfFlags != null && message.hasOwnProperty("nrfFlags"))
                if (!$util.isInteger(message.nrfFlags))
                    return "nrfFlags: integer expected";
            if (message.firmwareName != null && message.hasOwnProperty("firmwareName"))
                if (!$util.isString(message.firmwareName))
                    return "firmwareName: string expected";
            if (message.hardwareConfigCrc != null && message.hasOwnProperty("hardwareConfigCrc"))
                if (!$util.isInteger(message.hardwareConfigCrc))
                    return "hardwareConfigCrc: integer expected";
            if (message.firmwareInfoExtraBytes != null && message.hasOwnProperty("firmwareInfoExtraBytes"))
                if (!(message.firmwareInfoExtraBytes && typeof message.firmwareInfoExtraBytes.length === "number" || $util.isString(message.firmwareInfoExtraBytes)))
                    return "firmwareInfoExtraBytes: buffer expected";
            if (message.firmwareInfoRawPayload != null && message.hasOwnProperty("firmwareInfoRawPayload"))
                if (!(message.firmwareInfoRawPayload && typeof message.firmwareInfoRawPayload.length === "number" || $util.isString(message.firmwareInfoRawPayload)))
                    return "firmwareInfoRawPayload: buffer expected";
            return null;
        };

        /**
         * Creates a VescTrampaBoard message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof vesc_trampa.VescTrampaBoard
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {vesc_trampa.VescTrampaBoard} VescTrampaBoard
         */
        VescTrampaBoard.fromObject = function fromObject(object, long) {
            if (object instanceof $root.vesc_trampa.VescTrampaBoard)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.vesc_trampa.VescTrampaBoard();
            if (object.portName != null)
                message.portName = String(object.portName);
            if (object.vid != null)
                message.vid = object.vid >>> 0;
            if (object.pid != null)
                message.pid = object.pid >>> 0;
            if (object.serialNumber != null)
                message.serialNumber = String(object.serialNumber);
            if (object.manufacturer != null)
                message.manufacturer = String(object.manufacturer);
            if (object.product != null)
                message.product = String(object.product);
            if (object.portBaudRate != null)
                message.portBaudRate = object.portBaudRate >>> 0;
            if (object.firmwareMajor != null)
                message.firmwareMajor = object.firmwareMajor >>> 0;
            if (object.firmwareMinor != null)
                message.firmwareMinor = object.firmwareMinor >>> 0;
            if (object.hardwareName != null)
                message.hardwareName = String(object.hardwareName);
            if (object.uuid != null)
                if (typeof object.uuid === "string")
                    $util.base64.decode(object.uuid, message.uuid = $util.newBuffer($util.base64.length(object.uuid)), 0);
                else if (object.uuid.length >= 0)
                    message.uuid = object.uuid;
            if (object.pairingDone != null)
                message.pairingDone = Boolean(object.pairingDone);
            if (object.testVersionNumber != null)
                message.testVersionNumber = object.testVersionNumber >>> 0;
            if (object.hardwareType != null)
                message.hardwareType = object.hardwareType >>> 0;
            if (object.customConfigCount != null)
                message.customConfigCount = object.customConfigCount >>> 0;
            if (object.hasPhaseFilters != null)
                message.hasPhaseFilters = Boolean(object.hasPhaseFilters);
            if (object.qmlHw != null)
                message.qmlHw = object.qmlHw >>> 0;
            if (object.qmlApp != null)
                message.qmlApp = object.qmlApp >>> 0;
            if (object.nrfFlags != null)
                message.nrfFlags = object.nrfFlags >>> 0;
            if (object.firmwareName != null)
                message.firmwareName = String(object.firmwareName);
            if (object.hardwareConfigCrc != null)
                message.hardwareConfigCrc = object.hardwareConfigCrc >>> 0;
            if (object.firmwareInfoExtraBytes != null)
                if (typeof object.firmwareInfoExtraBytes === "string")
                    $util.base64.decode(object.firmwareInfoExtraBytes, message.firmwareInfoExtraBytes = $util.newBuffer($util.base64.length(object.firmwareInfoExtraBytes)), 0);
                else if (object.firmwareInfoExtraBytes.length >= 0)
                    message.firmwareInfoExtraBytes = object.firmwareInfoExtraBytes;
            if (object.firmwareInfoRawPayload != null)
                if (typeof object.firmwareInfoRawPayload === "string")
                    $util.base64.decode(object.firmwareInfoRawPayload, message.firmwareInfoRawPayload = $util.newBuffer($util.base64.length(object.firmwareInfoRawPayload)), 0);
                else if (object.firmwareInfoRawPayload.length >= 0)
                    message.firmwareInfoRawPayload = object.firmwareInfoRawPayload;
            return message;
        };

        /**
         * Creates a plain object from a VescTrampaBoard message. Also converts values to other types if specified.
         * @function toObject
         * @memberof vesc_trampa.VescTrampaBoard
         * @static
         * @param {vesc_trampa.VescTrampaBoard} message VescTrampaBoard
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        VescTrampaBoard.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.portName = "";
                object.vid = 0;
                object.pid = 0;
                object.serialNumber = "";
                object.manufacturer = "";
                object.product = "";
                object.portBaudRate = 0;
                object.firmwareMajor = 0;
                object.firmwareMinor = 0;
                object.hardwareName = "";
                if (options.bytes === String)
                    object.uuid = "";
                else {
                    object.uuid = [];
                    if (options.bytes !== Array)
                        object.uuid = $util.newBuffer(object.uuid);
                }
                object.pairingDone = false;
                object.testVersionNumber = 0;
                object.hardwareType = 0;
                object.customConfigCount = 0;
                object.hasPhaseFilters = false;
                object.qmlHw = 0;
                object.qmlApp = 0;
                object.nrfFlags = 0;
                object.firmwareName = "";
                object.hardwareConfigCrc = 0;
                if (options.bytes === String)
                    object.firmwareInfoExtraBytes = "";
                else {
                    object.firmwareInfoExtraBytes = [];
                    if (options.bytes !== Array)
                        object.firmwareInfoExtraBytes = $util.newBuffer(object.firmwareInfoExtraBytes);
                }
                if (options.bytes === String)
                    object.firmwareInfoRawPayload = "";
                else {
                    object.firmwareInfoRawPayload = [];
                    if (options.bytes !== Array)
                        object.firmwareInfoRawPayload = $util.newBuffer(object.firmwareInfoRawPayload);
                }
            }
            if (message.portName != null && message.hasOwnProperty("portName"))
                object.portName = message.portName;
            if (message.vid != null && message.hasOwnProperty("vid"))
                object.vid = message.vid;
            if (message.pid != null && message.hasOwnProperty("pid"))
                object.pid = message.pid;
            if (message.serialNumber != null && message.hasOwnProperty("serialNumber"))
                object.serialNumber = message.serialNumber;
            if (message.manufacturer != null && message.hasOwnProperty("manufacturer"))
                object.manufacturer = message.manufacturer;
            if (message.product != null && message.hasOwnProperty("product"))
                object.product = message.product;
            if (message.portBaudRate != null && message.hasOwnProperty("portBaudRate"))
                object.portBaudRate = message.portBaudRate;
            if (message.firmwareMajor != null && message.hasOwnProperty("firmwareMajor"))
                object.firmwareMajor = message.firmwareMajor;
            if (message.firmwareMinor != null && message.hasOwnProperty("firmwareMinor"))
                object.firmwareMinor = message.firmwareMinor;
            if (message.hardwareName != null && message.hasOwnProperty("hardwareName"))
                object.hardwareName = message.hardwareName;
            if (message.uuid != null && message.hasOwnProperty("uuid"))
                object.uuid = options.bytes === String ? $util.base64.encode(message.uuid, 0, message.uuid.length) : options.bytes === Array ? Array.prototype.slice.call(message.uuid) : message.uuid;
            if (message.pairingDone != null && message.hasOwnProperty("pairingDone"))
                object.pairingDone = message.pairingDone;
            if (message.testVersionNumber != null && message.hasOwnProperty("testVersionNumber"))
                object.testVersionNumber = message.testVersionNumber;
            if (message.hardwareType != null && message.hasOwnProperty("hardwareType"))
                object.hardwareType = message.hardwareType;
            if (message.customConfigCount != null && message.hasOwnProperty("customConfigCount"))
                object.customConfigCount = message.customConfigCount;
            if (message.hasPhaseFilters != null && message.hasOwnProperty("hasPhaseFilters"))
                object.hasPhaseFilters = message.hasPhaseFilters;
            if (message.qmlHw != null && message.hasOwnProperty("qmlHw"))
                object.qmlHw = message.qmlHw;
            if (message.qmlApp != null && message.hasOwnProperty("qmlApp"))
                object.qmlApp = message.qmlApp;
            if (message.nrfFlags != null && message.hasOwnProperty("nrfFlags"))
                object.nrfFlags = message.nrfFlags;
            if (message.firmwareName != null && message.hasOwnProperty("firmwareName"))
                object.firmwareName = message.firmwareName;
            if (message.hardwareConfigCrc != null && message.hasOwnProperty("hardwareConfigCrc"))
                object.hardwareConfigCrc = message.hardwareConfigCrc;
            if (message.firmwareInfoExtraBytes != null && message.hasOwnProperty("firmwareInfoExtraBytes"))
                object.firmwareInfoExtraBytes = options.bytes === String ? $util.base64.encode(message.firmwareInfoExtraBytes, 0, message.firmwareInfoExtraBytes.length) : options.bytes === Array ? Array.prototype.slice.call(message.firmwareInfoExtraBytes) : message.firmwareInfoExtraBytes;
            if (message.firmwareInfoRawPayload != null && message.hasOwnProperty("firmwareInfoRawPayload"))
                object.firmwareInfoRawPayload = options.bytes === String ? $util.base64.encode(message.firmwareInfoRawPayload, 0, message.firmwareInfoRawPayload.length) : options.bytes === Array ? Array.prototype.slice.call(message.firmwareInfoRawPayload) : message.firmwareInfoRawPayload;
            return object;
        };

        /**
         * Converts this VescTrampaBoard to JSON.
         * @function toJSON
         * @memberof vesc_trampa.VescTrampaBoard
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        VescTrampaBoard.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for VescTrampaBoard
         * @function getTypeUrl
         * @memberof vesc_trampa.VescTrampaBoard
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        VescTrampaBoard.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/vesc_trampa.VescTrampaBoard";
        };

        return VescTrampaBoard;
    })();

    return vesc_trampa;
})();

export const usbvideo = $root.usbvideo = (() => {

    /**
     * Namespace usbvideo.
     * @exports usbvideo
     * @namespace
     */
    const usbvideo = {};

    /**
     * RxEnvelopeType enum.
     * @name usbvideo.RxEnvelopeType
     * @enum {number}
     * @property {number} ET_FRAMES=0 ET_FRAMES value
     * @property {number} ET_DEVICE_CONNECTED=1 ET_DEVICE_CONNECTED value
     * @property {number} ET_DEVICE_RECORDING_START=3 ET_DEVICE_RECORDING_START value
     * @property {number} ET_DEVICE_RECORDING_END=4 ET_DEVICE_RECORDING_END value
     * @property {number} ET_DEVICE_DISCONNECTED=5 ET_DEVICE_DISCONNECTED value
     * @property {number} ET_ERROR=6 ET_ERROR value
     * @property {number} ET_COMMAND=7 ET_COMMAND value
     * @property {number} ET_COMMAND_SUCCESS=8 ET_COMMAND_SUCCESS value
     * @property {number} ET_COMMAND_REJECTED=9 ET_COMMAND_REJECTED value
     * @property {number} ET_COMMAND_FAILED=10 ET_COMMAND_FAILED value
     */
    usbvideo.RxEnvelopeType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "ET_FRAMES"] = 0;
        values[valuesById[1] = "ET_DEVICE_CONNECTED"] = 1;
        values[valuesById[3] = "ET_DEVICE_RECORDING_START"] = 3;
        values[valuesById[4] = "ET_DEVICE_RECORDING_END"] = 4;
        values[valuesById[5] = "ET_DEVICE_DISCONNECTED"] = 5;
        values[valuesById[6] = "ET_ERROR"] = 6;
        values[valuesById[7] = "ET_COMMAND"] = 7;
        values[valuesById[8] = "ET_COMMAND_SUCCESS"] = 8;
        values[valuesById[9] = "ET_COMMAND_REJECTED"] = 9;
        values[valuesById[10] = "ET_COMMAND_FAILED"] = 10;
        return values;
    })();

    usbvideo.RxEnvelope = (function() {

        /**
         * Properties of a RxEnvelope.
         * @memberof usbvideo
         * @interface IRxEnvelope
         * @property {usbvideo.RxEnvelopeType|null} [type] RxEnvelope type
         * @property {frame.IFrameStamp|null} [stamp] RxEnvelope stamp
         * @property {usbvideo.ICamera|null} [camera] RxEnvelope camera
         * @property {Array.<usbvideo.ICameraFormat>|null} [formats] RxEnvelope formats
         * @property {string|null} [error] RxEnvelope error
         * @property {Uint8Array|null} [lastInferenceQueuePtr] RxEnvelope lastInferenceQueuePtr
         * @property {frame.IFramesPack|null} [frames] RxEnvelope frames
         * @property {usbvideo.ITxEnvelope|null} [command] RxEnvelope command
         */

        /**
         * Constructs a new RxEnvelope.
         * @memberof usbvideo
         * @classdesc Represents a RxEnvelope.
         * @implements IRxEnvelope
         * @constructor
         * @param {usbvideo.IRxEnvelope=} [properties] Properties to set
         */
        function RxEnvelope(properties) {
            this.formats = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RxEnvelope type.
         * @member {usbvideo.RxEnvelopeType} type
         * @memberof usbvideo.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.type = 0;

        /**
         * RxEnvelope stamp.
         * @member {frame.IFrameStamp|null|undefined} stamp
         * @memberof usbvideo.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.stamp = null;

        /**
         * RxEnvelope camera.
         * @member {usbvideo.ICamera|null|undefined} camera
         * @memberof usbvideo.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.camera = null;

        /**
         * RxEnvelope formats.
         * @member {Array.<usbvideo.ICameraFormat>} formats
         * @memberof usbvideo.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.formats = $util.emptyArray;

        /**
         * RxEnvelope error.
         * @member {string} error
         * @memberof usbvideo.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.error = "";

        /**
         * RxEnvelope lastInferenceQueuePtr.
         * @member {Uint8Array} lastInferenceQueuePtr
         * @memberof usbvideo.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.lastInferenceQueuePtr = $util.newBuffer([]);

        /**
         * RxEnvelope frames.
         * @member {frame.IFramesPack|null|undefined} frames
         * @memberof usbvideo.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.frames = null;

        /**
         * RxEnvelope command.
         * @member {usbvideo.ITxEnvelope|null|undefined} command
         * @memberof usbvideo.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.command = null;

        /**
         * Creates a new RxEnvelope instance using the specified properties.
         * @function create
         * @memberof usbvideo.RxEnvelope
         * @static
         * @param {usbvideo.IRxEnvelope=} [properties] Properties to set
         * @returns {usbvideo.RxEnvelope} RxEnvelope instance
         */
        RxEnvelope.create = function create(properties) {
            return new RxEnvelope(properties);
        };

        /**
         * Encodes the specified RxEnvelope message. Does not implicitly {@link usbvideo.RxEnvelope.verify|verify} messages.
         * @function encode
         * @memberof usbvideo.RxEnvelope
         * @static
         * @param {usbvideo.IRxEnvelope} message RxEnvelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RxEnvelope.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.type);
            if (message.stamp != null && Object.hasOwnProperty.call(message, "stamp"))
                $root.frame.FrameStamp.encode(message.stamp, writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.camera != null && Object.hasOwnProperty.call(message, "camera"))
                $root.usbvideo.Camera.encode(message.camera, writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            if (message.formats != null && message.formats.length)
                for (let i = 0; i < message.formats.length; ++i)
                    $root.usbvideo.CameraFormat.encode(message.formats[i], writer.uint32(/* id 6, wireType 2 =*/50).fork()).ldelim();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.error);
            if (message.lastInferenceQueuePtr != null && Object.hasOwnProperty.call(message, "lastInferenceQueuePtr"))
                writer.uint32(/* id 8, wireType 2 =*/66).bytes(message.lastInferenceQueuePtr);
            if (message.frames != null && Object.hasOwnProperty.call(message, "frames"))
                $root.frame.FramesPack.encode(message.frames, writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            if (message.command != null && Object.hasOwnProperty.call(message, "command"))
                $root.usbvideo.TxEnvelope.encode(message.command, writer.uint32(/* id 20, wireType 2 =*/162).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified RxEnvelope message, length delimited. Does not implicitly {@link usbvideo.RxEnvelope.verify|verify} messages.
         * @function encodeDelimited
         * @memberof usbvideo.RxEnvelope
         * @static
         * @param {usbvideo.IRxEnvelope} message RxEnvelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RxEnvelope.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RxEnvelope message from the specified reader or buffer.
         * @function decode
         * @memberof usbvideo.RxEnvelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {usbvideo.RxEnvelope} RxEnvelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RxEnvelope.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.usbvideo.RxEnvelope();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.type = reader.int32();
                        break;
                    }
                case 2: {
                        message.stamp = $root.frame.FrameStamp.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 5: {
                        message.camera = $root.usbvideo.Camera.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 6: {
                        if (!(message.formats && message.formats.length))
                            message.formats = [];
                        message.formats.push($root.usbvideo.CameraFormat.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 7: {
                        message.error = reader.string();
                        break;
                    }
                case 8: {
                        message.lastInferenceQueuePtr = reader.bytes();
                        break;
                    }
                case 10: {
                        message.frames = $root.frame.FramesPack.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 20: {
                        message.command = $root.usbvideo.TxEnvelope.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RxEnvelope message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof usbvideo.RxEnvelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {usbvideo.RxEnvelope} RxEnvelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RxEnvelope.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RxEnvelope message.
         * @function verify
         * @memberof usbvideo.RxEnvelope
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RxEnvelope.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.type != null && message.hasOwnProperty("type"))
                switch (message.type) {
                default:
                    return "type: enum value expected";
                case 0:
                case 1:
                case 3:
                case 4:
                case 5:
                case 6:
                case 7:
                case 8:
                case 9:
                case 10:
                    break;
                }
            if (message.stamp != null && message.hasOwnProperty("stamp")) {
                let error = $root.frame.FrameStamp.verify(message.stamp, long + 1);
                if (error)
                    return "stamp." + error;
            }
            if (message.camera != null && message.hasOwnProperty("camera")) {
                let error = $root.usbvideo.Camera.verify(message.camera, long + 1);
                if (error)
                    return "camera." + error;
            }
            if (message.formats != null && message.hasOwnProperty("formats")) {
                if (!Array.isArray(message.formats))
                    return "formats: array expected";
                for (let i = 0; i < message.formats.length; ++i) {
                    let error = $root.usbvideo.CameraFormat.verify(message.formats[i], long + 1);
                    if (error)
                        return "formats." + error;
                }
            }
            if (message.error != null && message.hasOwnProperty("error"))
                if (!$util.isString(message.error))
                    return "error: string expected";
            if (message.lastInferenceQueuePtr != null && message.hasOwnProperty("lastInferenceQueuePtr"))
                if (!(message.lastInferenceQueuePtr && typeof message.lastInferenceQueuePtr.length === "number" || $util.isString(message.lastInferenceQueuePtr)))
                    return "lastInferenceQueuePtr: buffer expected";
            if (message.frames != null && message.hasOwnProperty("frames")) {
                let error = $root.frame.FramesPack.verify(message.frames, long + 1);
                if (error)
                    return "frames." + error;
            }
            if (message.command != null && message.hasOwnProperty("command")) {
                let error = $root.usbvideo.TxEnvelope.verify(message.command, long + 1);
                if (error)
                    return "command." + error;
            }
            return null;
        };

        /**
         * Creates a RxEnvelope message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof usbvideo.RxEnvelope
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {usbvideo.RxEnvelope} RxEnvelope
         */
        RxEnvelope.fromObject = function fromObject(object, long) {
            if (object instanceof $root.usbvideo.RxEnvelope)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.usbvideo.RxEnvelope();
            switch (object.type) {
            default:
                if (typeof object.type === "number") {
                    message.type = object.type;
                    break;
                }
                break;
            case "ET_FRAMES":
            case 0:
                message.type = 0;
                break;
            case "ET_DEVICE_CONNECTED":
            case 1:
                message.type = 1;
                break;
            case "ET_DEVICE_RECORDING_START":
            case 3:
                message.type = 3;
                break;
            case "ET_DEVICE_RECORDING_END":
            case 4:
                message.type = 4;
                break;
            case "ET_DEVICE_DISCONNECTED":
            case 5:
                message.type = 5;
                break;
            case "ET_ERROR":
            case 6:
                message.type = 6;
                break;
            case "ET_COMMAND":
            case 7:
                message.type = 7;
                break;
            case "ET_COMMAND_SUCCESS":
            case 8:
                message.type = 8;
                break;
            case "ET_COMMAND_REJECTED":
            case 9:
                message.type = 9;
                break;
            case "ET_COMMAND_FAILED":
            case 10:
                message.type = 10;
                break;
            }
            if (object.stamp != null) {
                if (typeof object.stamp !== "object")
                    throw TypeError(".usbvideo.RxEnvelope.stamp: object expected");
                message.stamp = $root.frame.FrameStamp.fromObject(object.stamp, long + 1);
            }
            if (object.camera != null) {
                if (typeof object.camera !== "object")
                    throw TypeError(".usbvideo.RxEnvelope.camera: object expected");
                message.camera = $root.usbvideo.Camera.fromObject(object.camera, long + 1);
            }
            if (object.formats) {
                if (!Array.isArray(object.formats))
                    throw TypeError(".usbvideo.RxEnvelope.formats: array expected");
                message.formats = [];
                for (let i = 0; i < object.formats.length; ++i) {
                    if (typeof object.formats[i] !== "object")
                        throw TypeError(".usbvideo.RxEnvelope.formats: object expected");
                    message.formats[i] = $root.usbvideo.CameraFormat.fromObject(object.formats[i], long + 1);
                }
            }
            if (object.error != null)
                message.error = String(object.error);
            if (object.lastInferenceQueuePtr != null)
                if (typeof object.lastInferenceQueuePtr === "string")
                    $util.base64.decode(object.lastInferenceQueuePtr, message.lastInferenceQueuePtr = $util.newBuffer($util.base64.length(object.lastInferenceQueuePtr)), 0);
                else if (object.lastInferenceQueuePtr.length >= 0)
                    message.lastInferenceQueuePtr = object.lastInferenceQueuePtr;
            if (object.frames != null) {
                if (typeof object.frames !== "object")
                    throw TypeError(".usbvideo.RxEnvelope.frames: object expected");
                message.frames = $root.frame.FramesPack.fromObject(object.frames, long + 1);
            }
            if (object.command != null) {
                if (typeof object.command !== "object")
                    throw TypeError(".usbvideo.RxEnvelope.command: object expected");
                message.command = $root.usbvideo.TxEnvelope.fromObject(object.command, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a RxEnvelope message. Also converts values to other types if specified.
         * @function toObject
         * @memberof usbvideo.RxEnvelope
         * @static
         * @param {usbvideo.RxEnvelope} message RxEnvelope
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RxEnvelope.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.formats = [];
            if (options.defaults) {
                object.type = options.enums === String ? "ET_FRAMES" : 0;
                object.stamp = null;
                object.camera = null;
                object.error = "";
                if (options.bytes === String)
                    object.lastInferenceQueuePtr = "";
                else {
                    object.lastInferenceQueuePtr = [];
                    if (options.bytes !== Array)
                        object.lastInferenceQueuePtr = $util.newBuffer(object.lastInferenceQueuePtr);
                }
                object.frames = null;
                object.command = null;
            }
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = options.enums === String ? $root.usbvideo.RxEnvelopeType[message.type] === undefined ? message.type : $root.usbvideo.RxEnvelopeType[message.type] : message.type;
            if (message.stamp != null && message.hasOwnProperty("stamp"))
                object.stamp = $root.frame.FrameStamp.toObject(message.stamp, options);
            if (message.camera != null && message.hasOwnProperty("camera"))
                object.camera = $root.usbvideo.Camera.toObject(message.camera, options);
            if (message.formats && message.formats.length) {
                object.formats = [];
                for (let j = 0; j < message.formats.length; ++j)
                    object.formats[j] = $root.usbvideo.CameraFormat.toObject(message.formats[j], options);
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = message.error;
            if (message.lastInferenceQueuePtr != null && message.hasOwnProperty("lastInferenceQueuePtr"))
                object.lastInferenceQueuePtr = options.bytes === String ? $util.base64.encode(message.lastInferenceQueuePtr, 0, message.lastInferenceQueuePtr.length) : options.bytes === Array ? Array.prototype.slice.call(message.lastInferenceQueuePtr) : message.lastInferenceQueuePtr;
            if (message.frames != null && message.hasOwnProperty("frames"))
                object.frames = $root.frame.FramesPack.toObject(message.frames, options);
            if (message.command != null && message.hasOwnProperty("command"))
                object.command = $root.usbvideo.TxEnvelope.toObject(message.command, options);
            return object;
        };

        /**
         * Converts this RxEnvelope to JSON.
         * @function toJSON
         * @memberof usbvideo.RxEnvelope
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RxEnvelope.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RxEnvelope
         * @function getTypeUrl
         * @memberof usbvideo.RxEnvelope
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RxEnvelope.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/usbvideo.RxEnvelope";
        };

        return RxEnvelope;
    })();

    /**
     * SetFormatMode enum.
     * @name usbvideo.SetFormatMode
     * @enum {number}
     * @property {number} SET_FORMAT_MODE_AUTO=0 SET_FORMAT_MODE_AUTO value
     * @property {number} SET_FORMAT_MODE_MANUAL=1 SET_FORMAT_MODE_MANUAL value
     * @property {number} SET_FORMAT_MODE_NONE=2 SET_FORMAT_MODE_NONE value
     */
    usbvideo.SetFormatMode = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "SET_FORMAT_MODE_AUTO"] = 0;
        values[valuesById[1] = "SET_FORMAT_MODE_MANUAL"] = 1;
        values[valuesById[2] = "SET_FORMAT_MODE_NONE"] = 2;
        return values;
    })();

    usbvideo.SetFormatCommand = (function() {

        /**
         * Properties of a SetFormatCommand.
         * @memberof usbvideo
         * @interface ISetFormatCommand
         * @property {usbvideo.SetFormatMode|null} [mode] SetFormatCommand mode
         * @property {usbvideo.ICameraFormat|null} [format] SetFormatCommand format
         */

        /**
         * Constructs a new SetFormatCommand.
         * @memberof usbvideo
         * @classdesc Represents a SetFormatCommand.
         * @implements ISetFormatCommand
         * @constructor
         * @param {usbvideo.ISetFormatCommand=} [properties] Properties to set
         */
        function SetFormatCommand(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SetFormatCommand mode.
         * @member {usbvideo.SetFormatMode} mode
         * @memberof usbvideo.SetFormatCommand
         * @instance
         */
        SetFormatCommand.prototype.mode = 0;

        /**
         * SetFormatCommand format.
         * @member {usbvideo.ICameraFormat|null|undefined} format
         * @memberof usbvideo.SetFormatCommand
         * @instance
         */
        SetFormatCommand.prototype.format = null;

        /**
         * Creates a new SetFormatCommand instance using the specified properties.
         * @function create
         * @memberof usbvideo.SetFormatCommand
         * @static
         * @param {usbvideo.ISetFormatCommand=} [properties] Properties to set
         * @returns {usbvideo.SetFormatCommand} SetFormatCommand instance
         */
        SetFormatCommand.create = function create(properties) {
            return new SetFormatCommand(properties);
        };

        /**
         * Encodes the specified SetFormatCommand message. Does not implicitly {@link usbvideo.SetFormatCommand.verify|verify} messages.
         * @function encode
         * @memberof usbvideo.SetFormatCommand
         * @static
         * @param {usbvideo.ISetFormatCommand} message SetFormatCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SetFormatCommand.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.mode);
            if (message.format != null && Object.hasOwnProperty.call(message, "format"))
                $root.usbvideo.CameraFormat.encode(message.format, writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified SetFormatCommand message, length delimited. Does not implicitly {@link usbvideo.SetFormatCommand.verify|verify} messages.
         * @function encodeDelimited
         * @memberof usbvideo.SetFormatCommand
         * @static
         * @param {usbvideo.ISetFormatCommand} message SetFormatCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SetFormatCommand.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SetFormatCommand message from the specified reader or buffer.
         * @function decode
         * @memberof usbvideo.SetFormatCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {usbvideo.SetFormatCommand} SetFormatCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SetFormatCommand.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.usbvideo.SetFormatCommand();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.mode = reader.int32();
                        break;
                    }
                case 2: {
                        message.format = $root.usbvideo.CameraFormat.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SetFormatCommand message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof usbvideo.SetFormatCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {usbvideo.SetFormatCommand} SetFormatCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SetFormatCommand.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SetFormatCommand message.
         * @function verify
         * @memberof usbvideo.SetFormatCommand
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SetFormatCommand.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.mode != null && message.hasOwnProperty("mode"))
                switch (message.mode) {
                default:
                    return "mode: enum value expected";
                case 0:
                case 1:
                case 2:
                    break;
                }
            if (message.format != null && message.hasOwnProperty("format")) {
                let error = $root.usbvideo.CameraFormat.verify(message.format, long + 1);
                if (error)
                    return "format." + error;
            }
            return null;
        };

        /**
         * Creates a SetFormatCommand message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof usbvideo.SetFormatCommand
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {usbvideo.SetFormatCommand} SetFormatCommand
         */
        SetFormatCommand.fromObject = function fromObject(object, long) {
            if (object instanceof $root.usbvideo.SetFormatCommand)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.usbvideo.SetFormatCommand();
            switch (object.mode) {
            default:
                if (typeof object.mode === "number") {
                    message.mode = object.mode;
                    break;
                }
                break;
            case "SET_FORMAT_MODE_AUTO":
            case 0:
                message.mode = 0;
                break;
            case "SET_FORMAT_MODE_MANUAL":
            case 1:
                message.mode = 1;
                break;
            case "SET_FORMAT_MODE_NONE":
            case 2:
                message.mode = 2;
                break;
            }
            if (object.format != null) {
                if (typeof object.format !== "object")
                    throw TypeError(".usbvideo.SetFormatCommand.format: object expected");
                message.format = $root.usbvideo.CameraFormat.fromObject(object.format, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a SetFormatCommand message. Also converts values to other types if specified.
         * @function toObject
         * @memberof usbvideo.SetFormatCommand
         * @static
         * @param {usbvideo.SetFormatCommand} message SetFormatCommand
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SetFormatCommand.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.mode = options.enums === String ? "SET_FORMAT_MODE_AUTO" : 0;
                object.format = null;
            }
            if (message.mode != null && message.hasOwnProperty("mode"))
                object.mode = options.enums === String ? $root.usbvideo.SetFormatMode[message.mode] === undefined ? message.mode : $root.usbvideo.SetFormatMode[message.mode] : message.mode;
            if (message.format != null && message.hasOwnProperty("format"))
                object.format = $root.usbvideo.CameraFormat.toObject(message.format, options);
            return object;
        };

        /**
         * Converts this SetFormatCommand to JSON.
         * @function toJSON
         * @memberof usbvideo.SetFormatCommand
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SetFormatCommand.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SetFormatCommand
         * @function getTypeUrl
         * @memberof usbvideo.SetFormatCommand
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SetFormatCommand.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/usbvideo.SetFormatCommand";
        };

        return SetFormatCommand;
    })();

    usbvideo.Command = (function() {

        /**
         * Properties of a Command.
         * @memberof usbvideo
         * @interface ICommand
         * @property {string|null} [targetCameraUniqueId] Command targetCameraUniqueId
         * @property {usbvideo.ISetFormatCommand|null} [setFormat] Command setFormat
         */

        /**
         * Constructs a new Command.
         * @memberof usbvideo
         * @classdesc Represents a Command.
         * @implements ICommand
         * @constructor
         * @param {usbvideo.ICommand=} [properties] Properties to set
         */
        function Command(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Command targetCameraUniqueId.
         * @member {string} targetCameraUniqueId
         * @memberof usbvideo.Command
         * @instance
         */
        Command.prototype.targetCameraUniqueId = "";

        /**
         * Command setFormat.
         * @member {usbvideo.ISetFormatCommand|null|undefined} setFormat
         * @memberof usbvideo.Command
         * @instance
         */
        Command.prototype.setFormat = null;

        /**
         * Creates a new Command instance using the specified properties.
         * @function create
         * @memberof usbvideo.Command
         * @static
         * @param {usbvideo.ICommand=} [properties] Properties to set
         * @returns {usbvideo.Command} Command instance
         */
        Command.create = function create(properties) {
            return new Command(properties);
        };

        /**
         * Encodes the specified Command message. Does not implicitly {@link usbvideo.Command.verify|verify} messages.
         * @function encode
         * @memberof usbvideo.Command
         * @static
         * @param {usbvideo.ICommand} message Command message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Command.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.targetCameraUniqueId != null && Object.hasOwnProperty.call(message, "targetCameraUniqueId"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.targetCameraUniqueId);
            if (message.setFormat != null && Object.hasOwnProperty.call(message, "setFormat"))
                $root.usbvideo.SetFormatCommand.encode(message.setFormat, writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified Command message, length delimited. Does not implicitly {@link usbvideo.Command.verify|verify} messages.
         * @function encodeDelimited
         * @memberof usbvideo.Command
         * @static
         * @param {usbvideo.ICommand} message Command message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Command.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Command message from the specified reader or buffer.
         * @function decode
         * @memberof usbvideo.Command
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {usbvideo.Command} Command
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Command.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.usbvideo.Command();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.targetCameraUniqueId = reader.string();
                        break;
                    }
                case 10: {
                        message.setFormat = $root.usbvideo.SetFormatCommand.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Command message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof usbvideo.Command
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {usbvideo.Command} Command
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Command.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Command message.
         * @function verify
         * @memberof usbvideo.Command
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Command.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.targetCameraUniqueId != null && message.hasOwnProperty("targetCameraUniqueId"))
                if (!$util.isString(message.targetCameraUniqueId))
                    return "targetCameraUniqueId: string expected";
            if (message.setFormat != null && message.hasOwnProperty("setFormat")) {
                let error = $root.usbvideo.SetFormatCommand.verify(message.setFormat, long + 1);
                if (error)
                    return "setFormat." + error;
            }
            return null;
        };

        /**
         * Creates a Command message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof usbvideo.Command
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {usbvideo.Command} Command
         */
        Command.fromObject = function fromObject(object, long) {
            if (object instanceof $root.usbvideo.Command)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.usbvideo.Command();
            if (object.targetCameraUniqueId != null)
                message.targetCameraUniqueId = String(object.targetCameraUniqueId);
            if (object.setFormat != null) {
                if (typeof object.setFormat !== "object")
                    throw TypeError(".usbvideo.Command.setFormat: object expected");
                message.setFormat = $root.usbvideo.SetFormatCommand.fromObject(object.setFormat, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a Command message. Also converts values to other types if specified.
         * @function toObject
         * @memberof usbvideo.Command
         * @static
         * @param {usbvideo.Command} message Command
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Command.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.targetCameraUniqueId = "";
                object.setFormat = null;
            }
            if (message.targetCameraUniqueId != null && message.hasOwnProperty("targetCameraUniqueId"))
                object.targetCameraUniqueId = message.targetCameraUniqueId;
            if (message.setFormat != null && message.hasOwnProperty("setFormat"))
                object.setFormat = $root.usbvideo.SetFormatCommand.toObject(message.setFormat, options);
            return object;
        };

        /**
         * Converts this Command to JSON.
         * @function toJSON
         * @memberof usbvideo.Command
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Command.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Command
         * @function getTypeUrl
         * @memberof usbvideo.Command
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Command.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/usbvideo.Command";
        };

        return Command;
    })();

    usbvideo.TxEnvelope = (function() {

        /**
         * Properties of a TxEnvelope.
         * @memberof usbvideo
         * @interface ITxEnvelope
         * @property {Long|null} [monotonicStampNs] TxEnvelope monotonicStampNs
         * @property {Long|null} [localStampNs] TxEnvelope localStampNs
         * @property {Long|null} [appStartId] TxEnvelope appStartId
         * @property {Uint8Array|null} [commandId] TxEnvelope commandId
         * @property {string|null} [targetCameraUniqueId] TxEnvelope targetCameraUniqueId
         * @property {usbvideo.ICommand|null} [command] TxEnvelope command
         */

        /**
         * Constructs a new TxEnvelope.
         * @memberof usbvideo
         * @classdesc Represents a TxEnvelope.
         * @implements ITxEnvelope
         * @constructor
         * @param {usbvideo.ITxEnvelope=} [properties] Properties to set
         */
        function TxEnvelope(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * TxEnvelope monotonicStampNs.
         * @member {Long} monotonicStampNs
         * @memberof usbvideo.TxEnvelope
         * @instance
         */
        TxEnvelope.prototype.monotonicStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * TxEnvelope localStampNs.
         * @member {Long} localStampNs
         * @memberof usbvideo.TxEnvelope
         * @instance
         */
        TxEnvelope.prototype.localStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * TxEnvelope appStartId.
         * @member {Long} appStartId
         * @memberof usbvideo.TxEnvelope
         * @instance
         */
        TxEnvelope.prototype.appStartId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * TxEnvelope commandId.
         * @member {Uint8Array} commandId
         * @memberof usbvideo.TxEnvelope
         * @instance
         */
        TxEnvelope.prototype.commandId = $util.newBuffer([]);

        /**
         * TxEnvelope targetCameraUniqueId.
         * @member {string} targetCameraUniqueId
         * @memberof usbvideo.TxEnvelope
         * @instance
         */
        TxEnvelope.prototype.targetCameraUniqueId = "";

        /**
         * TxEnvelope command.
         * @member {usbvideo.ICommand|null|undefined} command
         * @memberof usbvideo.TxEnvelope
         * @instance
         */
        TxEnvelope.prototype.command = null;

        /**
         * Creates a new TxEnvelope instance using the specified properties.
         * @function create
         * @memberof usbvideo.TxEnvelope
         * @static
         * @param {usbvideo.ITxEnvelope=} [properties] Properties to set
         * @returns {usbvideo.TxEnvelope} TxEnvelope instance
         */
        TxEnvelope.create = function create(properties) {
            return new TxEnvelope(properties);
        };

        /**
         * Encodes the specified TxEnvelope message. Does not implicitly {@link usbvideo.TxEnvelope.verify|verify} messages.
         * @function encode
         * @memberof usbvideo.TxEnvelope
         * @static
         * @param {usbvideo.ITxEnvelope} message TxEnvelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TxEnvelope.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.monotonicStampNs != null && Object.hasOwnProperty.call(message, "monotonicStampNs"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.monotonicStampNs);
            if (message.localStampNs != null && Object.hasOwnProperty.call(message, "localStampNs"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.localStampNs);
            if (message.appStartId != null && Object.hasOwnProperty.call(message, "appStartId"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.appStartId);
            if (message.commandId != null && Object.hasOwnProperty.call(message, "commandId"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.commandId);
            if (message.targetCameraUniqueId != null && Object.hasOwnProperty.call(message, "targetCameraUniqueId"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.targetCameraUniqueId);
            if (message.command != null && Object.hasOwnProperty.call(message, "command"))
                $root.usbvideo.Command.encode(message.command, writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified TxEnvelope message, length delimited. Does not implicitly {@link usbvideo.TxEnvelope.verify|verify} messages.
         * @function encodeDelimited
         * @memberof usbvideo.TxEnvelope
         * @static
         * @param {usbvideo.ITxEnvelope} message TxEnvelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TxEnvelope.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a TxEnvelope message from the specified reader or buffer.
         * @function decode
         * @memberof usbvideo.TxEnvelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {usbvideo.TxEnvelope} TxEnvelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TxEnvelope.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.usbvideo.TxEnvelope();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.monotonicStampNs = reader.uint64();
                        break;
                    }
                case 2: {
                        message.localStampNs = reader.uint64();
                        break;
                    }
                case 3: {
                        message.appStartId = reader.uint64();
                        break;
                    }
                case 4: {
                        message.commandId = reader.bytes();
                        break;
                    }
                case 5: {
                        message.targetCameraUniqueId = reader.string();
                        break;
                    }
                case 10: {
                        message.command = $root.usbvideo.Command.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a TxEnvelope message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof usbvideo.TxEnvelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {usbvideo.TxEnvelope} TxEnvelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TxEnvelope.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TxEnvelope message.
         * @function verify
         * @memberof usbvideo.TxEnvelope
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TxEnvelope.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (!$util.isInteger(message.monotonicStampNs) && !(message.monotonicStampNs && $util.isInteger(message.monotonicStampNs.low) && $util.isInteger(message.monotonicStampNs.high)))
                    return "monotonicStampNs: integer|Long expected";
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (!$util.isInteger(message.localStampNs) && !(message.localStampNs && $util.isInteger(message.localStampNs.low) && $util.isInteger(message.localStampNs.high)))
                    return "localStampNs: integer|Long expected";
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (!$util.isInteger(message.appStartId) && !(message.appStartId && $util.isInteger(message.appStartId.low) && $util.isInteger(message.appStartId.high)))
                    return "appStartId: integer|Long expected";
            if (message.commandId != null && message.hasOwnProperty("commandId"))
                if (!(message.commandId && typeof message.commandId.length === "number" || $util.isString(message.commandId)))
                    return "commandId: buffer expected";
            if (message.targetCameraUniqueId != null && message.hasOwnProperty("targetCameraUniqueId"))
                if (!$util.isString(message.targetCameraUniqueId))
                    return "targetCameraUniqueId: string expected";
            if (message.command != null && message.hasOwnProperty("command")) {
                let error = $root.usbvideo.Command.verify(message.command, long + 1);
                if (error)
                    return "command." + error;
            }
            return null;
        };

        /**
         * Creates a TxEnvelope message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof usbvideo.TxEnvelope
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {usbvideo.TxEnvelope} TxEnvelope
         */
        TxEnvelope.fromObject = function fromObject(object, long) {
            if (object instanceof $root.usbvideo.TxEnvelope)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.usbvideo.TxEnvelope();
            if (object.monotonicStampNs != null)
                if ($util.Long)
                    (message.monotonicStampNs = $util.Long.fromValue(object.monotonicStampNs)).unsigned = true;
                else if (typeof object.monotonicStampNs === "string")
                    message.monotonicStampNs = parseInt(object.monotonicStampNs, 10);
                else if (typeof object.monotonicStampNs === "number")
                    message.monotonicStampNs = object.monotonicStampNs;
                else if (typeof object.monotonicStampNs === "object")
                    message.monotonicStampNs = new $util.LongBits(object.monotonicStampNs.low >>> 0, object.monotonicStampNs.high >>> 0).toNumber(true);
            if (object.localStampNs != null)
                if ($util.Long)
                    (message.localStampNs = $util.Long.fromValue(object.localStampNs)).unsigned = true;
                else if (typeof object.localStampNs === "string")
                    message.localStampNs = parseInt(object.localStampNs, 10);
                else if (typeof object.localStampNs === "number")
                    message.localStampNs = object.localStampNs;
                else if (typeof object.localStampNs === "object")
                    message.localStampNs = new $util.LongBits(object.localStampNs.low >>> 0, object.localStampNs.high >>> 0).toNumber(true);
            if (object.appStartId != null)
                if ($util.Long)
                    (message.appStartId = $util.Long.fromValue(object.appStartId)).unsigned = true;
                else if (typeof object.appStartId === "string")
                    message.appStartId = parseInt(object.appStartId, 10);
                else if (typeof object.appStartId === "number")
                    message.appStartId = object.appStartId;
                else if (typeof object.appStartId === "object")
                    message.appStartId = new $util.LongBits(object.appStartId.low >>> 0, object.appStartId.high >>> 0).toNumber(true);
            if (object.commandId != null)
                if (typeof object.commandId === "string")
                    $util.base64.decode(object.commandId, message.commandId = $util.newBuffer($util.base64.length(object.commandId)), 0);
                else if (object.commandId.length >= 0)
                    message.commandId = object.commandId;
            if (object.targetCameraUniqueId != null)
                message.targetCameraUniqueId = String(object.targetCameraUniqueId);
            if (object.command != null) {
                if (typeof object.command !== "object")
                    throw TypeError(".usbvideo.TxEnvelope.command: object expected");
                message.command = $root.usbvideo.Command.fromObject(object.command, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a TxEnvelope message. Also converts values to other types if specified.
         * @function toObject
         * @memberof usbvideo.TxEnvelope
         * @static
         * @param {usbvideo.TxEnvelope} message TxEnvelope
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TxEnvelope.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.monotonicStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.monotonicStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.localStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.localStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.appStartId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.appStartId = options.longs === String ? "0" : 0;
                if (options.bytes === String)
                    object.commandId = "";
                else {
                    object.commandId = [];
                    if (options.bytes !== Array)
                        object.commandId = $util.newBuffer(object.commandId);
                }
                object.targetCameraUniqueId = "";
                object.command = null;
            }
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (typeof message.monotonicStampNs === "number")
                    object.monotonicStampNs = options.longs === String ? String(message.monotonicStampNs) : message.monotonicStampNs;
                else
                    object.monotonicStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.monotonicStampNs) : options.longs === Number ? new $util.LongBits(message.monotonicStampNs.low >>> 0, message.monotonicStampNs.high >>> 0).toNumber(true) : message.monotonicStampNs;
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (typeof message.localStampNs === "number")
                    object.localStampNs = options.longs === String ? String(message.localStampNs) : message.localStampNs;
                else
                    object.localStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.localStampNs) : options.longs === Number ? new $util.LongBits(message.localStampNs.low >>> 0, message.localStampNs.high >>> 0).toNumber(true) : message.localStampNs;
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (typeof message.appStartId === "number")
                    object.appStartId = options.longs === String ? String(message.appStartId) : message.appStartId;
                else
                    object.appStartId = options.longs === String ? $util.Long.prototype.toString.call(message.appStartId) : options.longs === Number ? new $util.LongBits(message.appStartId.low >>> 0, message.appStartId.high >>> 0).toNumber(true) : message.appStartId;
            if (message.commandId != null && message.hasOwnProperty("commandId"))
                object.commandId = options.bytes === String ? $util.base64.encode(message.commandId, 0, message.commandId.length) : options.bytes === Array ? Array.prototype.slice.call(message.commandId) : message.commandId;
            if (message.targetCameraUniqueId != null && message.hasOwnProperty("targetCameraUniqueId"))
                object.targetCameraUniqueId = message.targetCameraUniqueId;
            if (message.command != null && message.hasOwnProperty("command"))
                object.command = $root.usbvideo.Command.toObject(message.command, options);
            return object;
        };

        /**
         * Converts this TxEnvelope to JSON.
         * @function toJSON
         * @memberof usbvideo.TxEnvelope
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TxEnvelope.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for TxEnvelope
         * @function getTypeUrl
         * @memberof usbvideo.TxEnvelope
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        TxEnvelope.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/usbvideo.TxEnvelope";
        };

        return TxEnvelope;
    })();

    usbvideo.Camera = (function() {

        /**
         * Properties of a Camera.
         * @memberof usbvideo
         * @interface ICamera
         * @property {number|null} [vendorId] Camera vendorId
         * @property {number|null} [productId] Camera productId
         * @property {string|null} [serialNumber] Camera serialNumber
         * @property {string|null} [manufacturer] Camera manufacturer
         * @property {string|null} [product] Camera product
         * @property {number|null} [busNumber] Camera busNumber
         * @property {number|null} [deviceNumber] Camera deviceNumber
         * @property {string|null} [uniqueId] Camera uniqueId
         */

        /**
         * Constructs a new Camera.
         * @memberof usbvideo
         * @classdesc Represents a Camera.
         * @implements ICamera
         * @constructor
         * @param {usbvideo.ICamera=} [properties] Properties to set
         */
        function Camera(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Camera vendorId.
         * @member {number} vendorId
         * @memberof usbvideo.Camera
         * @instance
         */
        Camera.prototype.vendorId = 0;

        /**
         * Camera productId.
         * @member {number} productId
         * @memberof usbvideo.Camera
         * @instance
         */
        Camera.prototype.productId = 0;

        /**
         * Camera serialNumber.
         * @member {string} serialNumber
         * @memberof usbvideo.Camera
         * @instance
         */
        Camera.prototype.serialNumber = "";

        /**
         * Camera manufacturer.
         * @member {string} manufacturer
         * @memberof usbvideo.Camera
         * @instance
         */
        Camera.prototype.manufacturer = "";

        /**
         * Camera product.
         * @member {string} product
         * @memberof usbvideo.Camera
         * @instance
         */
        Camera.prototype.product = "";

        /**
         * Camera busNumber.
         * @member {number} busNumber
         * @memberof usbvideo.Camera
         * @instance
         */
        Camera.prototype.busNumber = 0;

        /**
         * Camera deviceNumber.
         * @member {number} deviceNumber
         * @memberof usbvideo.Camera
         * @instance
         */
        Camera.prototype.deviceNumber = 0;

        /**
         * Camera uniqueId.
         * @member {string} uniqueId
         * @memberof usbvideo.Camera
         * @instance
         */
        Camera.prototype.uniqueId = "";

        /**
         * Creates a new Camera instance using the specified properties.
         * @function create
         * @memberof usbvideo.Camera
         * @static
         * @param {usbvideo.ICamera=} [properties] Properties to set
         * @returns {usbvideo.Camera} Camera instance
         */
        Camera.create = function create(properties) {
            return new Camera(properties);
        };

        /**
         * Encodes the specified Camera message. Does not implicitly {@link usbvideo.Camera.verify|verify} messages.
         * @function encode
         * @memberof usbvideo.Camera
         * @static
         * @param {usbvideo.ICamera} message Camera message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Camera.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.vendorId != null && Object.hasOwnProperty.call(message, "vendorId"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.vendorId);
            if (message.productId != null && Object.hasOwnProperty.call(message, "productId"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.productId);
            if (message.serialNumber != null && Object.hasOwnProperty.call(message, "serialNumber"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.serialNumber);
            if (message.manufacturer != null && Object.hasOwnProperty.call(message, "manufacturer"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.manufacturer);
            if (message.product != null && Object.hasOwnProperty.call(message, "product"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.product);
            if (message.busNumber != null && Object.hasOwnProperty.call(message, "busNumber"))
                writer.uint32(/* id 6, wireType 0 =*/48).uint32(message.busNumber);
            if (message.deviceNumber != null && Object.hasOwnProperty.call(message, "deviceNumber"))
                writer.uint32(/* id 7, wireType 0 =*/56).uint32(message.deviceNumber);
            if (message.uniqueId != null && Object.hasOwnProperty.call(message, "uniqueId"))
                writer.uint32(/* id 8, wireType 2 =*/66).string(message.uniqueId);
            return writer;
        };

        /**
         * Encodes the specified Camera message, length delimited. Does not implicitly {@link usbvideo.Camera.verify|verify} messages.
         * @function encodeDelimited
         * @memberof usbvideo.Camera
         * @static
         * @param {usbvideo.ICamera} message Camera message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Camera.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Camera message from the specified reader or buffer.
         * @function decode
         * @memberof usbvideo.Camera
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {usbvideo.Camera} Camera
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Camera.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.usbvideo.Camera();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.vendorId = reader.uint32();
                        break;
                    }
                case 2: {
                        message.productId = reader.uint32();
                        break;
                    }
                case 3: {
                        message.serialNumber = reader.string();
                        break;
                    }
                case 4: {
                        message.manufacturer = reader.string();
                        break;
                    }
                case 5: {
                        message.product = reader.string();
                        break;
                    }
                case 6: {
                        message.busNumber = reader.uint32();
                        break;
                    }
                case 7: {
                        message.deviceNumber = reader.uint32();
                        break;
                    }
                case 8: {
                        message.uniqueId = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Camera message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof usbvideo.Camera
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {usbvideo.Camera} Camera
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Camera.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Camera message.
         * @function verify
         * @memberof usbvideo.Camera
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Camera.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.vendorId != null && message.hasOwnProperty("vendorId"))
                if (!$util.isInteger(message.vendorId))
                    return "vendorId: integer expected";
            if (message.productId != null && message.hasOwnProperty("productId"))
                if (!$util.isInteger(message.productId))
                    return "productId: integer expected";
            if (message.serialNumber != null && message.hasOwnProperty("serialNumber"))
                if (!$util.isString(message.serialNumber))
                    return "serialNumber: string expected";
            if (message.manufacturer != null && message.hasOwnProperty("manufacturer"))
                if (!$util.isString(message.manufacturer))
                    return "manufacturer: string expected";
            if (message.product != null && message.hasOwnProperty("product"))
                if (!$util.isString(message.product))
                    return "product: string expected";
            if (message.busNumber != null && message.hasOwnProperty("busNumber"))
                if (!$util.isInteger(message.busNumber))
                    return "busNumber: integer expected";
            if (message.deviceNumber != null && message.hasOwnProperty("deviceNumber"))
                if (!$util.isInteger(message.deviceNumber))
                    return "deviceNumber: integer expected";
            if (message.uniqueId != null && message.hasOwnProperty("uniqueId"))
                if (!$util.isString(message.uniqueId))
                    return "uniqueId: string expected";
            return null;
        };

        /**
         * Creates a Camera message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof usbvideo.Camera
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {usbvideo.Camera} Camera
         */
        Camera.fromObject = function fromObject(object, long) {
            if (object instanceof $root.usbvideo.Camera)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.usbvideo.Camera();
            if (object.vendorId != null)
                message.vendorId = object.vendorId >>> 0;
            if (object.productId != null)
                message.productId = object.productId >>> 0;
            if (object.serialNumber != null)
                message.serialNumber = String(object.serialNumber);
            if (object.manufacturer != null)
                message.manufacturer = String(object.manufacturer);
            if (object.product != null)
                message.product = String(object.product);
            if (object.busNumber != null)
                message.busNumber = object.busNumber >>> 0;
            if (object.deviceNumber != null)
                message.deviceNumber = object.deviceNumber >>> 0;
            if (object.uniqueId != null)
                message.uniqueId = String(object.uniqueId);
            return message;
        };

        /**
         * Creates a plain object from a Camera message. Also converts values to other types if specified.
         * @function toObject
         * @memberof usbvideo.Camera
         * @static
         * @param {usbvideo.Camera} message Camera
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Camera.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.vendorId = 0;
                object.productId = 0;
                object.serialNumber = "";
                object.manufacturer = "";
                object.product = "";
                object.busNumber = 0;
                object.deviceNumber = 0;
                object.uniqueId = "";
            }
            if (message.vendorId != null && message.hasOwnProperty("vendorId"))
                object.vendorId = message.vendorId;
            if (message.productId != null && message.hasOwnProperty("productId"))
                object.productId = message.productId;
            if (message.serialNumber != null && message.hasOwnProperty("serialNumber"))
                object.serialNumber = message.serialNumber;
            if (message.manufacturer != null && message.hasOwnProperty("manufacturer"))
                object.manufacturer = message.manufacturer;
            if (message.product != null && message.hasOwnProperty("product"))
                object.product = message.product;
            if (message.busNumber != null && message.hasOwnProperty("busNumber"))
                object.busNumber = message.busNumber;
            if (message.deviceNumber != null && message.hasOwnProperty("deviceNumber"))
                object.deviceNumber = message.deviceNumber;
            if (message.uniqueId != null && message.hasOwnProperty("uniqueId"))
                object.uniqueId = message.uniqueId;
            return object;
        };

        /**
         * Converts this Camera to JSON.
         * @function toJSON
         * @memberof usbvideo.Camera
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Camera.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Camera
         * @function getTypeUrl
         * @memberof usbvideo.Camera
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Camera.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/usbvideo.Camera";
        };

        return Camera;
    })();

    usbvideo.CameraFormat = (function() {

        /**
         * Properties of a CameraFormat.
         * @memberof usbvideo
         * @interface ICameraFormat
         * @property {number|null} [fourcc] CameraFormat fourcc
         * @property {number|null} [index] CameraFormat index
         * @property {number|null} [width] CameraFormat width
         * @property {number|null} [height] CameraFormat height
         * @property {number|null} [framesPerSecond] CameraFormat framesPerSecond
         * @property {Uint8Array|null} [guid] CameraFormat guid
         * @property {number|null} [frameIndex] CameraFormat frameIndex
         */

        /**
         * Constructs a new CameraFormat.
         * @memberof usbvideo
         * @classdesc Represents a CameraFormat.
         * @implements ICameraFormat
         * @constructor
         * @param {usbvideo.ICameraFormat=} [properties] Properties to set
         */
        function CameraFormat(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CameraFormat fourcc.
         * @member {number} fourcc
         * @memberof usbvideo.CameraFormat
         * @instance
         */
        CameraFormat.prototype.fourcc = 0;

        /**
         * CameraFormat index.
         * @member {number} index
         * @memberof usbvideo.CameraFormat
         * @instance
         */
        CameraFormat.prototype.index = 0;

        /**
         * CameraFormat width.
         * @member {number} width
         * @memberof usbvideo.CameraFormat
         * @instance
         */
        CameraFormat.prototype.width = 0;

        /**
         * CameraFormat height.
         * @member {number} height
         * @memberof usbvideo.CameraFormat
         * @instance
         */
        CameraFormat.prototype.height = 0;

        /**
         * CameraFormat framesPerSecond.
         * @member {number} framesPerSecond
         * @memberof usbvideo.CameraFormat
         * @instance
         */
        CameraFormat.prototype.framesPerSecond = 0;

        /**
         * CameraFormat guid.
         * @member {Uint8Array} guid
         * @memberof usbvideo.CameraFormat
         * @instance
         */
        CameraFormat.prototype.guid = $util.newBuffer([]);

        /**
         * CameraFormat frameIndex.
         * @member {number} frameIndex
         * @memberof usbvideo.CameraFormat
         * @instance
         */
        CameraFormat.prototype.frameIndex = 0;

        /**
         * Creates a new CameraFormat instance using the specified properties.
         * @function create
         * @memberof usbvideo.CameraFormat
         * @static
         * @param {usbvideo.ICameraFormat=} [properties] Properties to set
         * @returns {usbvideo.CameraFormat} CameraFormat instance
         */
        CameraFormat.create = function create(properties) {
            return new CameraFormat(properties);
        };

        /**
         * Encodes the specified CameraFormat message. Does not implicitly {@link usbvideo.CameraFormat.verify|verify} messages.
         * @function encode
         * @memberof usbvideo.CameraFormat
         * @static
         * @param {usbvideo.ICameraFormat} message CameraFormat message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CameraFormat.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.fourcc != null && Object.hasOwnProperty.call(message, "fourcc"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.fourcc);
            if (message.index != null && Object.hasOwnProperty.call(message, "index"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.index);
            if (message.width != null && Object.hasOwnProperty.call(message, "width"))
                writer.uint32(/* id 5, wireType 0 =*/40).uint32(message.width);
            if (message.height != null && Object.hasOwnProperty.call(message, "height"))
                writer.uint32(/* id 6, wireType 0 =*/48).uint32(message.height);
            if (message.framesPerSecond != null && Object.hasOwnProperty.call(message, "framesPerSecond"))
                writer.uint32(/* id 7, wireType 5 =*/61).float(message.framesPerSecond);
            if (message.guid != null && Object.hasOwnProperty.call(message, "guid"))
                writer.uint32(/* id 8, wireType 2 =*/66).bytes(message.guid);
            if (message.frameIndex != null && Object.hasOwnProperty.call(message, "frameIndex"))
                writer.uint32(/* id 9, wireType 0 =*/72).uint32(message.frameIndex);
            return writer;
        };

        /**
         * Encodes the specified CameraFormat message, length delimited. Does not implicitly {@link usbvideo.CameraFormat.verify|verify} messages.
         * @function encodeDelimited
         * @memberof usbvideo.CameraFormat
         * @static
         * @param {usbvideo.ICameraFormat} message CameraFormat message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CameraFormat.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CameraFormat message from the specified reader or buffer.
         * @function decode
         * @memberof usbvideo.CameraFormat
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {usbvideo.CameraFormat} CameraFormat
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CameraFormat.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.usbvideo.CameraFormat();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.fourcc = reader.uint32();
                        break;
                    }
                case 2: {
                        message.index = reader.uint32();
                        break;
                    }
                case 5: {
                        message.width = reader.uint32();
                        break;
                    }
                case 6: {
                        message.height = reader.uint32();
                        break;
                    }
                case 7: {
                        message.framesPerSecond = reader.float();
                        break;
                    }
                case 8: {
                        message.guid = reader.bytes();
                        break;
                    }
                case 9: {
                        message.frameIndex = reader.uint32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CameraFormat message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof usbvideo.CameraFormat
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {usbvideo.CameraFormat} CameraFormat
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CameraFormat.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CameraFormat message.
         * @function verify
         * @memberof usbvideo.CameraFormat
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CameraFormat.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.fourcc != null && message.hasOwnProperty("fourcc"))
                if (!$util.isInteger(message.fourcc))
                    return "fourcc: integer expected";
            if (message.index != null && message.hasOwnProperty("index"))
                if (!$util.isInteger(message.index))
                    return "index: integer expected";
            if (message.width != null && message.hasOwnProperty("width"))
                if (!$util.isInteger(message.width))
                    return "width: integer expected";
            if (message.height != null && message.hasOwnProperty("height"))
                if (!$util.isInteger(message.height))
                    return "height: integer expected";
            if (message.framesPerSecond != null && message.hasOwnProperty("framesPerSecond"))
                if (typeof message.framesPerSecond !== "number")
                    return "framesPerSecond: number expected";
            if (message.guid != null && message.hasOwnProperty("guid"))
                if (!(message.guid && typeof message.guid.length === "number" || $util.isString(message.guid)))
                    return "guid: buffer expected";
            if (message.frameIndex != null && message.hasOwnProperty("frameIndex"))
                if (!$util.isInteger(message.frameIndex))
                    return "frameIndex: integer expected";
            return null;
        };

        /**
         * Creates a CameraFormat message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof usbvideo.CameraFormat
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {usbvideo.CameraFormat} CameraFormat
         */
        CameraFormat.fromObject = function fromObject(object, long) {
            if (object instanceof $root.usbvideo.CameraFormat)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.usbvideo.CameraFormat();
            if (object.fourcc != null)
                message.fourcc = object.fourcc >>> 0;
            if (object.index != null)
                message.index = object.index >>> 0;
            if (object.width != null)
                message.width = object.width >>> 0;
            if (object.height != null)
                message.height = object.height >>> 0;
            if (object.framesPerSecond != null)
                message.framesPerSecond = Number(object.framesPerSecond);
            if (object.guid != null)
                if (typeof object.guid === "string")
                    $util.base64.decode(object.guid, message.guid = $util.newBuffer($util.base64.length(object.guid)), 0);
                else if (object.guid.length >= 0)
                    message.guid = object.guid;
            if (object.frameIndex != null)
                message.frameIndex = object.frameIndex >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a CameraFormat message. Also converts values to other types if specified.
         * @function toObject
         * @memberof usbvideo.CameraFormat
         * @static
         * @param {usbvideo.CameraFormat} message CameraFormat
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CameraFormat.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.fourcc = 0;
                object.index = 0;
                object.width = 0;
                object.height = 0;
                object.framesPerSecond = 0;
                if (options.bytes === String)
                    object.guid = "";
                else {
                    object.guid = [];
                    if (options.bytes !== Array)
                        object.guid = $util.newBuffer(object.guid);
                }
                object.frameIndex = 0;
            }
            if (message.fourcc != null && message.hasOwnProperty("fourcc"))
                object.fourcc = message.fourcc;
            if (message.index != null && message.hasOwnProperty("index"))
                object.index = message.index;
            if (message.width != null && message.hasOwnProperty("width"))
                object.width = message.width;
            if (message.height != null && message.hasOwnProperty("height"))
                object.height = message.height;
            if (message.framesPerSecond != null && message.hasOwnProperty("framesPerSecond"))
                object.framesPerSecond = options.json && !isFinite(message.framesPerSecond) ? String(message.framesPerSecond) : message.framesPerSecond;
            if (message.guid != null && message.hasOwnProperty("guid"))
                object.guid = options.bytes === String ? $util.base64.encode(message.guid, 0, message.guid.length) : options.bytes === Array ? Array.prototype.slice.call(message.guid) : message.guid;
            if (message.frameIndex != null && message.hasOwnProperty("frameIndex"))
                object.frameIndex = message.frameIndex;
            return object;
        };

        /**
         * Converts this CameraFormat to JSON.
         * @function toJSON
         * @memberof usbvideo.CameraFormat
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CameraFormat.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CameraFormat
         * @function getTypeUrl
         * @memberof usbvideo.CameraFormat
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CameraFormat.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/usbvideo.CameraFormat";
        };

        return CameraFormat;
    })();

    return usbvideo;
})();

export const frame = $root.frame = (() => {

    /**
     * Namespace frame.
     * @exports frame
     * @namespace
     */
    const frame = {};

    frame.FramesPack = (function() {

        /**
         * Properties of a FramesPack.
         * @memberof frame
         * @interface IFramesPack
         * @property {frame.IFrameFormat|null} [format] FramesPack format
         * @property {Array.<frame.IFrameStamp>|null} [stamps] FramesPack stamps
         * @property {Uint8Array|null} [linearData] FramesPack linearData
         * @property {Array.<Uint8Array>|null} [framesData] FramesPack framesData
         */

        /**
         * Constructs a new FramesPack.
         * @memberof frame
         * @classdesc Represents a FramesPack.
         * @implements IFramesPack
         * @constructor
         * @param {frame.IFramesPack=} [properties] Properties to set
         */
        function FramesPack(properties) {
            this.stamps = [];
            this.framesData = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FramesPack format.
         * @member {frame.IFrameFormat|null|undefined} format
         * @memberof frame.FramesPack
         * @instance
         */
        FramesPack.prototype.format = null;

        /**
         * FramesPack stamps.
         * @member {Array.<frame.IFrameStamp>} stamps
         * @memberof frame.FramesPack
         * @instance
         */
        FramesPack.prototype.stamps = $util.emptyArray;

        /**
         * FramesPack linearData.
         * @member {Uint8Array} linearData
         * @memberof frame.FramesPack
         * @instance
         */
        FramesPack.prototype.linearData = $util.newBuffer([]);

        /**
         * FramesPack framesData.
         * @member {Array.<Uint8Array>} framesData
         * @memberof frame.FramesPack
         * @instance
         */
        FramesPack.prototype.framesData = $util.emptyArray;

        /**
         * Creates a new FramesPack instance using the specified properties.
         * @function create
         * @memberof frame.FramesPack
         * @static
         * @param {frame.IFramesPack=} [properties] Properties to set
         * @returns {frame.FramesPack} FramesPack instance
         */
        FramesPack.create = function create(properties) {
            return new FramesPack(properties);
        };

        /**
         * Encodes the specified FramesPack message. Does not implicitly {@link frame.FramesPack.verify|verify} messages.
         * @function encode
         * @memberof frame.FramesPack
         * @static
         * @param {frame.IFramesPack} message FramesPack message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FramesPack.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.format != null && Object.hasOwnProperty.call(message, "format"))
                $root.frame.FrameFormat.encode(message.format, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.stamps != null && message.stamps.length)
                for (let i = 0; i < message.stamps.length; ++i)
                    $root.frame.FrameStamp.encode(message.stamps[i], writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            if (message.linearData != null && Object.hasOwnProperty.call(message, "linearData"))
                writer.uint32(/* id 11, wireType 2 =*/90).bytes(message.linearData);
            if (message.framesData != null && message.framesData.length)
                for (let i = 0; i < message.framesData.length; ++i)
                    writer.uint32(/* id 12, wireType 2 =*/98).bytes(message.framesData[i]);
            return writer;
        };

        /**
         * Encodes the specified FramesPack message, length delimited. Does not implicitly {@link frame.FramesPack.verify|verify} messages.
         * @function encodeDelimited
         * @memberof frame.FramesPack
         * @static
         * @param {frame.IFramesPack} message FramesPack message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FramesPack.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FramesPack message from the specified reader or buffer.
         * @function decode
         * @memberof frame.FramesPack
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {frame.FramesPack} FramesPack
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FramesPack.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.frame.FramesPack();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.format = $root.frame.FrameFormat.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 10: {
                        if (!(message.stamps && message.stamps.length))
                            message.stamps = [];
                        message.stamps.push($root.frame.FrameStamp.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 11: {
                        message.linearData = reader.bytes();
                        break;
                    }
                case 12: {
                        if (!(message.framesData && message.framesData.length))
                            message.framesData = [];
                        message.framesData.push(reader.bytes());
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FramesPack message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof frame.FramesPack
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {frame.FramesPack} FramesPack
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FramesPack.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FramesPack message.
         * @function verify
         * @memberof frame.FramesPack
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FramesPack.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.format != null && message.hasOwnProperty("format")) {
                let error = $root.frame.FrameFormat.verify(message.format, long + 1);
                if (error)
                    return "format." + error;
            }
            if (message.stamps != null && message.hasOwnProperty("stamps")) {
                if (!Array.isArray(message.stamps))
                    return "stamps: array expected";
                for (let i = 0; i < message.stamps.length; ++i) {
                    let error = $root.frame.FrameStamp.verify(message.stamps[i], long + 1);
                    if (error)
                        return "stamps." + error;
                }
            }
            if (message.linearData != null && message.hasOwnProperty("linearData"))
                if (!(message.linearData && typeof message.linearData.length === "number" || $util.isString(message.linearData)))
                    return "linearData: buffer expected";
            if (message.framesData != null && message.hasOwnProperty("framesData")) {
                if (!Array.isArray(message.framesData))
                    return "framesData: array expected";
                for (let i = 0; i < message.framesData.length; ++i)
                    if (!(message.framesData[i] && typeof message.framesData[i].length === "number" || $util.isString(message.framesData[i])))
                        return "framesData: buffer[] expected";
            }
            return null;
        };

        /**
         * Creates a FramesPack message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof frame.FramesPack
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {frame.FramesPack} FramesPack
         */
        FramesPack.fromObject = function fromObject(object, long) {
            if (object instanceof $root.frame.FramesPack)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.frame.FramesPack();
            if (object.format != null) {
                if (typeof object.format !== "object")
                    throw TypeError(".frame.FramesPack.format: object expected");
                message.format = $root.frame.FrameFormat.fromObject(object.format, long + 1);
            }
            if (object.stamps) {
                if (!Array.isArray(object.stamps))
                    throw TypeError(".frame.FramesPack.stamps: array expected");
                message.stamps = [];
                for (let i = 0; i < object.stamps.length; ++i) {
                    if (typeof object.stamps[i] !== "object")
                        throw TypeError(".frame.FramesPack.stamps: object expected");
                    message.stamps[i] = $root.frame.FrameStamp.fromObject(object.stamps[i], long + 1);
                }
            }
            if (object.linearData != null)
                if (typeof object.linearData === "string")
                    $util.base64.decode(object.linearData, message.linearData = $util.newBuffer($util.base64.length(object.linearData)), 0);
                else if (object.linearData.length >= 0)
                    message.linearData = object.linearData;
            if (object.framesData) {
                if (!Array.isArray(object.framesData))
                    throw TypeError(".frame.FramesPack.framesData: array expected");
                message.framesData = [];
                for (let i = 0; i < object.framesData.length; ++i)
                    if (typeof object.framesData[i] === "string")
                        $util.base64.decode(object.framesData[i], message.framesData[i] = $util.newBuffer($util.base64.length(object.framesData[i])), 0);
                    else if (object.framesData[i].length >= 0)
                        message.framesData[i] = object.framesData[i];
            }
            return message;
        };

        /**
         * Creates a plain object from a FramesPack message. Also converts values to other types if specified.
         * @function toObject
         * @memberof frame.FramesPack
         * @static
         * @param {frame.FramesPack} message FramesPack
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FramesPack.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.stamps = [];
                object.framesData = [];
            }
            if (options.defaults) {
                object.format = null;
                if (options.bytes === String)
                    object.linearData = "";
                else {
                    object.linearData = [];
                    if (options.bytes !== Array)
                        object.linearData = $util.newBuffer(object.linearData);
                }
            }
            if (message.format != null && message.hasOwnProperty("format"))
                object.format = $root.frame.FrameFormat.toObject(message.format, options);
            if (message.stamps && message.stamps.length) {
                object.stamps = [];
                for (let j = 0; j < message.stamps.length; ++j)
                    object.stamps[j] = $root.frame.FrameStamp.toObject(message.stamps[j], options);
            }
            if (message.linearData != null && message.hasOwnProperty("linearData"))
                object.linearData = options.bytes === String ? $util.base64.encode(message.linearData, 0, message.linearData.length) : options.bytes === Array ? Array.prototype.slice.call(message.linearData) : message.linearData;
            if (message.framesData && message.framesData.length) {
                object.framesData = [];
                for (let j = 0; j < message.framesData.length; ++j)
                    object.framesData[j] = options.bytes === String ? $util.base64.encode(message.framesData[j], 0, message.framesData[j].length) : options.bytes === Array ? Array.prototype.slice.call(message.framesData[j]) : message.framesData[j];
            }
            return object;
        };

        /**
         * Converts this FramesPack to JSON.
         * @function toJSON
         * @memberof frame.FramesPack
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FramesPack.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FramesPack
         * @function getTypeUrl
         * @memberof frame.FramesPack
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FramesPack.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/frame.FramesPack";
        };

        return FramesPack;
    })();

    frame.FrameStamp = (function() {

        /**
         * Properties of a FrameStamp.
         * @memberof frame
         * @interface IFrameStamp
         * @property {Long|null} [monotonicStampNs] FrameStamp monotonicStampNs
         * @property {Long|null} [localStampNs] FrameStamp localStampNs
         * @property {Long|null} [appStartId] FrameStamp appStartId
         * @property {Long|null} [index] FrameStamp index
         */

        /**
         * Constructs a new FrameStamp.
         * @memberof frame
         * @classdesc Represents a FrameStamp.
         * @implements IFrameStamp
         * @constructor
         * @param {frame.IFrameStamp=} [properties] Properties to set
         */
        function FrameStamp(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FrameStamp monotonicStampNs.
         * @member {Long} monotonicStampNs
         * @memberof frame.FrameStamp
         * @instance
         */
        FrameStamp.prototype.monotonicStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * FrameStamp localStampNs.
         * @member {Long} localStampNs
         * @memberof frame.FrameStamp
         * @instance
         */
        FrameStamp.prototype.localStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * FrameStamp appStartId.
         * @member {Long} appStartId
         * @memberof frame.FrameStamp
         * @instance
         */
        FrameStamp.prototype.appStartId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * FrameStamp index.
         * @member {Long} index
         * @memberof frame.FrameStamp
         * @instance
         */
        FrameStamp.prototype.index = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new FrameStamp instance using the specified properties.
         * @function create
         * @memberof frame.FrameStamp
         * @static
         * @param {frame.IFrameStamp=} [properties] Properties to set
         * @returns {frame.FrameStamp} FrameStamp instance
         */
        FrameStamp.create = function create(properties) {
            return new FrameStamp(properties);
        };

        /**
         * Encodes the specified FrameStamp message. Does not implicitly {@link frame.FrameStamp.verify|verify} messages.
         * @function encode
         * @memberof frame.FrameStamp
         * @static
         * @param {frame.IFrameStamp} message FrameStamp message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FrameStamp.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.monotonicStampNs != null && Object.hasOwnProperty.call(message, "monotonicStampNs"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.monotonicStampNs);
            if (message.localStampNs != null && Object.hasOwnProperty.call(message, "localStampNs"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.localStampNs);
            if (message.appStartId != null && Object.hasOwnProperty.call(message, "appStartId"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.appStartId);
            if (message.index != null && Object.hasOwnProperty.call(message, "index"))
                writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.index);
            return writer;
        };

        /**
         * Encodes the specified FrameStamp message, length delimited. Does not implicitly {@link frame.FrameStamp.verify|verify} messages.
         * @function encodeDelimited
         * @memberof frame.FrameStamp
         * @static
         * @param {frame.IFrameStamp} message FrameStamp message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FrameStamp.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FrameStamp message from the specified reader or buffer.
         * @function decode
         * @memberof frame.FrameStamp
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {frame.FrameStamp} FrameStamp
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FrameStamp.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.frame.FrameStamp();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.monotonicStampNs = reader.uint64();
                        break;
                    }
                case 2: {
                        message.localStampNs = reader.uint64();
                        break;
                    }
                case 3: {
                        message.appStartId = reader.uint64();
                        break;
                    }
                case 4: {
                        message.index = reader.uint64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FrameStamp message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof frame.FrameStamp
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {frame.FrameStamp} FrameStamp
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FrameStamp.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FrameStamp message.
         * @function verify
         * @memberof frame.FrameStamp
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FrameStamp.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (!$util.isInteger(message.monotonicStampNs) && !(message.monotonicStampNs && $util.isInteger(message.monotonicStampNs.low) && $util.isInteger(message.monotonicStampNs.high)))
                    return "monotonicStampNs: integer|Long expected";
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (!$util.isInteger(message.localStampNs) && !(message.localStampNs && $util.isInteger(message.localStampNs.low) && $util.isInteger(message.localStampNs.high)))
                    return "localStampNs: integer|Long expected";
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (!$util.isInteger(message.appStartId) && !(message.appStartId && $util.isInteger(message.appStartId.low) && $util.isInteger(message.appStartId.high)))
                    return "appStartId: integer|Long expected";
            if (message.index != null && message.hasOwnProperty("index"))
                if (!$util.isInteger(message.index) && !(message.index && $util.isInteger(message.index.low) && $util.isInteger(message.index.high)))
                    return "index: integer|Long expected";
            return null;
        };

        /**
         * Creates a FrameStamp message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof frame.FrameStamp
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {frame.FrameStamp} FrameStamp
         */
        FrameStamp.fromObject = function fromObject(object, long) {
            if (object instanceof $root.frame.FrameStamp)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.frame.FrameStamp();
            if (object.monotonicStampNs != null)
                if ($util.Long)
                    (message.monotonicStampNs = $util.Long.fromValue(object.monotonicStampNs)).unsigned = true;
                else if (typeof object.monotonicStampNs === "string")
                    message.monotonicStampNs = parseInt(object.monotonicStampNs, 10);
                else if (typeof object.monotonicStampNs === "number")
                    message.monotonicStampNs = object.monotonicStampNs;
                else if (typeof object.monotonicStampNs === "object")
                    message.monotonicStampNs = new $util.LongBits(object.monotonicStampNs.low >>> 0, object.monotonicStampNs.high >>> 0).toNumber(true);
            if (object.localStampNs != null)
                if ($util.Long)
                    (message.localStampNs = $util.Long.fromValue(object.localStampNs)).unsigned = true;
                else if (typeof object.localStampNs === "string")
                    message.localStampNs = parseInt(object.localStampNs, 10);
                else if (typeof object.localStampNs === "number")
                    message.localStampNs = object.localStampNs;
                else if (typeof object.localStampNs === "object")
                    message.localStampNs = new $util.LongBits(object.localStampNs.low >>> 0, object.localStampNs.high >>> 0).toNumber(true);
            if (object.appStartId != null)
                if ($util.Long)
                    (message.appStartId = $util.Long.fromValue(object.appStartId)).unsigned = true;
                else if (typeof object.appStartId === "string")
                    message.appStartId = parseInt(object.appStartId, 10);
                else if (typeof object.appStartId === "number")
                    message.appStartId = object.appStartId;
                else if (typeof object.appStartId === "object")
                    message.appStartId = new $util.LongBits(object.appStartId.low >>> 0, object.appStartId.high >>> 0).toNumber(true);
            if (object.index != null)
                if ($util.Long)
                    (message.index = $util.Long.fromValue(object.index)).unsigned = true;
                else if (typeof object.index === "string")
                    message.index = parseInt(object.index, 10);
                else if (typeof object.index === "number")
                    message.index = object.index;
                else if (typeof object.index === "object")
                    message.index = new $util.LongBits(object.index.low >>> 0, object.index.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a FrameStamp message. Also converts values to other types if specified.
         * @function toObject
         * @memberof frame.FrameStamp
         * @static
         * @param {frame.FrameStamp} message FrameStamp
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FrameStamp.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.monotonicStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.monotonicStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.localStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.localStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.appStartId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.appStartId = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.index = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.index = options.longs === String ? "0" : 0;
            }
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (typeof message.monotonicStampNs === "number")
                    object.monotonicStampNs = options.longs === String ? String(message.monotonicStampNs) : message.monotonicStampNs;
                else
                    object.monotonicStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.monotonicStampNs) : options.longs === Number ? new $util.LongBits(message.monotonicStampNs.low >>> 0, message.monotonicStampNs.high >>> 0).toNumber(true) : message.monotonicStampNs;
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (typeof message.localStampNs === "number")
                    object.localStampNs = options.longs === String ? String(message.localStampNs) : message.localStampNs;
                else
                    object.localStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.localStampNs) : options.longs === Number ? new $util.LongBits(message.localStampNs.low >>> 0, message.localStampNs.high >>> 0).toNumber(true) : message.localStampNs;
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (typeof message.appStartId === "number")
                    object.appStartId = options.longs === String ? String(message.appStartId) : message.appStartId;
                else
                    object.appStartId = options.longs === String ? $util.Long.prototype.toString.call(message.appStartId) : options.longs === Number ? new $util.LongBits(message.appStartId.low >>> 0, message.appStartId.high >>> 0).toNumber(true) : message.appStartId;
            if (message.index != null && message.hasOwnProperty("index"))
                if (typeof message.index === "number")
                    object.index = options.longs === String ? String(message.index) : message.index;
                else
                    object.index = options.longs === String ? $util.Long.prototype.toString.call(message.index) : options.longs === Number ? new $util.LongBits(message.index.low >>> 0, message.index.high >>> 0).toNumber(true) : message.index;
            return object;
        };

        /**
         * Converts this FrameStamp to JSON.
         * @function toJSON
         * @memberof frame.FrameStamp
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FrameStamp.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FrameStamp
         * @function getTypeUrl
         * @memberof frame.FrameStamp
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FrameStamp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/frame.FrameStamp";
        };

        return FrameStamp;
    })();

    /**
     * FrameFormatKind enum.
     * @name frame.FrameFormatKind
     * @enum {number}
     * @property {number} FF_NCHW=0 FF_NCHW value
     * @property {number} FF_JPEG=1 FF_JPEG value
     */
    frame.FrameFormatKind = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "FF_NCHW"] = 0;
        values[valuesById[1] = "FF_JPEG"] = 1;
        return values;
    })();

    frame.FrameFormat = (function() {

        /**
         * Properties of a FrameFormat.
         * @memberof frame
         * @interface IFrameFormat
         * @property {number|null} [width] FrameFormat width
         * @property {number|null} [height] FrameFormat height
         * @property {frame.FrameFormatKind|null} [kind] FrameFormat kind
         */

        /**
         * Constructs a new FrameFormat.
         * @memberof frame
         * @classdesc Represents a FrameFormat.
         * @implements IFrameFormat
         * @constructor
         * @param {frame.IFrameFormat=} [properties] Properties to set
         */
        function FrameFormat(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FrameFormat width.
         * @member {number} width
         * @memberof frame.FrameFormat
         * @instance
         */
        FrameFormat.prototype.width = 0;

        /**
         * FrameFormat height.
         * @member {number} height
         * @memberof frame.FrameFormat
         * @instance
         */
        FrameFormat.prototype.height = 0;

        /**
         * FrameFormat kind.
         * @member {frame.FrameFormatKind} kind
         * @memberof frame.FrameFormat
         * @instance
         */
        FrameFormat.prototype.kind = 0;

        /**
         * Creates a new FrameFormat instance using the specified properties.
         * @function create
         * @memberof frame.FrameFormat
         * @static
         * @param {frame.IFrameFormat=} [properties] Properties to set
         * @returns {frame.FrameFormat} FrameFormat instance
         */
        FrameFormat.create = function create(properties) {
            return new FrameFormat(properties);
        };

        /**
         * Encodes the specified FrameFormat message. Does not implicitly {@link frame.FrameFormat.verify|verify} messages.
         * @function encode
         * @memberof frame.FrameFormat
         * @static
         * @param {frame.IFrameFormat} message FrameFormat message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FrameFormat.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.width != null && Object.hasOwnProperty.call(message, "width"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.width);
            if (message.height != null && Object.hasOwnProperty.call(message, "height"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.height);
            if (message.kind != null && Object.hasOwnProperty.call(message, "kind"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.kind);
            return writer;
        };

        /**
         * Encodes the specified FrameFormat message, length delimited. Does not implicitly {@link frame.FrameFormat.verify|verify} messages.
         * @function encodeDelimited
         * @memberof frame.FrameFormat
         * @static
         * @param {frame.IFrameFormat} message FrameFormat message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FrameFormat.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FrameFormat message from the specified reader or buffer.
         * @function decode
         * @memberof frame.FrameFormat
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {frame.FrameFormat} FrameFormat
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FrameFormat.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.frame.FrameFormat();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.width = reader.uint32();
                        break;
                    }
                case 2: {
                        message.height = reader.uint32();
                        break;
                    }
                case 10: {
                        message.kind = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FrameFormat message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof frame.FrameFormat
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {frame.FrameFormat} FrameFormat
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FrameFormat.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FrameFormat message.
         * @function verify
         * @memberof frame.FrameFormat
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FrameFormat.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.width != null && message.hasOwnProperty("width"))
                if (!$util.isInteger(message.width))
                    return "width: integer expected";
            if (message.height != null && message.hasOwnProperty("height"))
                if (!$util.isInteger(message.height))
                    return "height: integer expected";
            if (message.kind != null && message.hasOwnProperty("kind"))
                switch (message.kind) {
                default:
                    return "kind: enum value expected";
                case 0:
                case 1:
                    break;
                }
            return null;
        };

        /**
         * Creates a FrameFormat message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof frame.FrameFormat
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {frame.FrameFormat} FrameFormat
         */
        FrameFormat.fromObject = function fromObject(object, long) {
            if (object instanceof $root.frame.FrameFormat)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.frame.FrameFormat();
            if (object.width != null)
                message.width = object.width >>> 0;
            if (object.height != null)
                message.height = object.height >>> 0;
            switch (object.kind) {
            default:
                if (typeof object.kind === "number") {
                    message.kind = object.kind;
                    break;
                }
                break;
            case "FF_NCHW":
            case 0:
                message.kind = 0;
                break;
            case "FF_JPEG":
            case 1:
                message.kind = 1;
                break;
            }
            return message;
        };

        /**
         * Creates a plain object from a FrameFormat message. Also converts values to other types if specified.
         * @function toObject
         * @memberof frame.FrameFormat
         * @static
         * @param {frame.FrameFormat} message FrameFormat
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FrameFormat.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.width = 0;
                object.height = 0;
                object.kind = options.enums === String ? "FF_NCHW" : 0;
            }
            if (message.width != null && message.hasOwnProperty("width"))
                object.width = message.width;
            if (message.height != null && message.hasOwnProperty("height"))
                object.height = message.height;
            if (message.kind != null && message.hasOwnProperty("kind"))
                object.kind = options.enums === String ? $root.frame.FrameFormatKind[message.kind] === undefined ? message.kind : $root.frame.FrameFormatKind[message.kind] : message.kind;
            return object;
        };

        /**
         * Converts this FrameFormat to JSON.
         * @function toJSON
         * @memberof frame.FrameFormat
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FrameFormat.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FrameFormat
         * @function getTypeUrl
         * @memberof frame.FrameFormat
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FrameFormat.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/frame.FrameFormat";
        };

        return FrameFormat;
    })();

    return frame;
})();

export const motors_mirroring = $root.motors_mirroring = (() => {

    /**
     * Namespace motors_mirroring.
     * @exports motors_mirroring
     * @namespace
     */
    const motors_mirroring = {};

    /**
     * BusType enum.
     * @name motors_mirroring.BusType
     * @enum {number}
     * @property {number} MBT_ST3215=0 MBT_ST3215 value
     */
    motors_mirroring.BusType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "MBT_ST3215"] = 0;
        return values;
    })();

    /**
     * BusMode enum.
     * @name motors_mirroring.BusMode
     * @enum {number}
     * @property {number} BR_UNKNOWN=0 BR_UNKNOWN value
     * @property {number} BR_LEADER=1 BR_LEADER value
     * @property {number} BR_FOLLOWER=2 BR_FOLLOWER value
     */
    motors_mirroring.BusMode = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "BR_UNKNOWN"] = 0;
        values[valuesById[1] = "BR_LEADER"] = 1;
        values[valuesById[2] = "BR_FOLLOWER"] = 2;
        return values;
    })();

    motors_mirroring.MirroringBus = (function() {

        /**
         * Properties of a MirroringBus.
         * @memberof motors_mirroring
         * @interface IMirroringBus
         * @property {motors_mirroring.BusType|null} [type] MirroringBus type
         * @property {string|null} [uniqueId] MirroringBus uniqueId
         */

        /**
         * Constructs a new MirroringBus.
         * @memberof motors_mirroring
         * @classdesc Represents a MirroringBus.
         * @implements IMirroringBus
         * @constructor
         * @param {motors_mirroring.IMirroringBus=} [properties] Properties to set
         */
        function MirroringBus(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MirroringBus type.
         * @member {motors_mirroring.BusType} type
         * @memberof motors_mirroring.MirroringBus
         * @instance
         */
        MirroringBus.prototype.type = 0;

        /**
         * MirroringBus uniqueId.
         * @member {string} uniqueId
         * @memberof motors_mirroring.MirroringBus
         * @instance
         */
        MirroringBus.prototype.uniqueId = "";

        /**
         * Creates a new MirroringBus instance using the specified properties.
         * @function create
         * @memberof motors_mirroring.MirroringBus
         * @static
         * @param {motors_mirroring.IMirroringBus=} [properties] Properties to set
         * @returns {motors_mirroring.MirroringBus} MirroringBus instance
         */
        MirroringBus.create = function create(properties) {
            return new MirroringBus(properties);
        };

        /**
         * Encodes the specified MirroringBus message. Does not implicitly {@link motors_mirroring.MirroringBus.verify|verify} messages.
         * @function encode
         * @memberof motors_mirroring.MirroringBus
         * @static
         * @param {motors_mirroring.IMirroringBus} message MirroringBus message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MirroringBus.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.type);
            if (message.uniqueId != null && Object.hasOwnProperty.call(message, "uniqueId"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.uniqueId);
            return writer;
        };

        /**
         * Encodes the specified MirroringBus message, length delimited. Does not implicitly {@link motors_mirroring.MirroringBus.verify|verify} messages.
         * @function encodeDelimited
         * @memberof motors_mirroring.MirroringBus
         * @static
         * @param {motors_mirroring.IMirroringBus} message MirroringBus message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MirroringBus.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a MirroringBus message from the specified reader or buffer.
         * @function decode
         * @memberof motors_mirroring.MirroringBus
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {motors_mirroring.MirroringBus} MirroringBus
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MirroringBus.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.motors_mirroring.MirroringBus();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.type = reader.int32();
                        break;
                    }
                case 2: {
                        message.uniqueId = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a MirroringBus message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof motors_mirroring.MirroringBus
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {motors_mirroring.MirroringBus} MirroringBus
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MirroringBus.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MirroringBus message.
         * @function verify
         * @memberof motors_mirroring.MirroringBus
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MirroringBus.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.type != null && message.hasOwnProperty("type"))
                switch (message.type) {
                default:
                    return "type: enum value expected";
                case 0:
                    break;
                }
            if (message.uniqueId != null && message.hasOwnProperty("uniqueId"))
                if (!$util.isString(message.uniqueId))
                    return "uniqueId: string expected";
            return null;
        };

        /**
         * Creates a MirroringBus message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof motors_mirroring.MirroringBus
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {motors_mirroring.MirroringBus} MirroringBus
         */
        MirroringBus.fromObject = function fromObject(object, long) {
            if (object instanceof $root.motors_mirroring.MirroringBus)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.motors_mirroring.MirroringBus();
            switch (object.type) {
            default:
                if (typeof object.type === "number") {
                    message.type = object.type;
                    break;
                }
                break;
            case "MBT_ST3215":
            case 0:
                message.type = 0;
                break;
            }
            if (object.uniqueId != null)
                message.uniqueId = String(object.uniqueId);
            return message;
        };

        /**
         * Creates a plain object from a MirroringBus message. Also converts values to other types if specified.
         * @function toObject
         * @memberof motors_mirroring.MirroringBus
         * @static
         * @param {motors_mirroring.MirroringBus} message MirroringBus
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MirroringBus.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.type = options.enums === String ? "MBT_ST3215" : 0;
                object.uniqueId = "";
            }
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = options.enums === String ? $root.motors_mirroring.BusType[message.type] === undefined ? message.type : $root.motors_mirroring.BusType[message.type] : message.type;
            if (message.uniqueId != null && message.hasOwnProperty("uniqueId"))
                object.uniqueId = message.uniqueId;
            return object;
        };

        /**
         * Converts this MirroringBus to JSON.
         * @function toJSON
         * @memberof motors_mirroring.MirroringBus
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MirroringBus.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MirroringBus
         * @function getTypeUrl
         * @memberof motors_mirroring.MirroringBus
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MirroringBus.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/motors_mirroring.MirroringBus";
        };

        return MirroringBus;
    })();

    motors_mirroring.ModeEnvelope = (function() {

        /**
         * Properties of a ModeEnvelope.
         * @memberof motors_mirroring
         * @interface IModeEnvelope
         * @property {Long|null} [monotonicStampNs] ModeEnvelope monotonicStampNs
         * @property {Long|null} [localStampNs] ModeEnvelope localStampNs
         * @property {Long|null} [appStartId] ModeEnvelope appStartId
         * @property {motors_mirroring.IMirroringBus|null} [bus] ModeEnvelope bus
         * @property {motors_mirroring.BusMode|null} [mode] ModeEnvelope mode
         */

        /**
         * Constructs a new ModeEnvelope.
         * @memberof motors_mirroring
         * @classdesc Represents a ModeEnvelope.
         * @implements IModeEnvelope
         * @constructor
         * @param {motors_mirroring.IModeEnvelope=} [properties] Properties to set
         */
        function ModeEnvelope(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ModeEnvelope monotonicStampNs.
         * @member {Long} monotonicStampNs
         * @memberof motors_mirroring.ModeEnvelope
         * @instance
         */
        ModeEnvelope.prototype.monotonicStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ModeEnvelope localStampNs.
         * @member {Long} localStampNs
         * @memberof motors_mirroring.ModeEnvelope
         * @instance
         */
        ModeEnvelope.prototype.localStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ModeEnvelope appStartId.
         * @member {Long} appStartId
         * @memberof motors_mirroring.ModeEnvelope
         * @instance
         */
        ModeEnvelope.prototype.appStartId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ModeEnvelope bus.
         * @member {motors_mirroring.IMirroringBus|null|undefined} bus
         * @memberof motors_mirroring.ModeEnvelope
         * @instance
         */
        ModeEnvelope.prototype.bus = null;

        /**
         * ModeEnvelope mode.
         * @member {motors_mirroring.BusMode} mode
         * @memberof motors_mirroring.ModeEnvelope
         * @instance
         */
        ModeEnvelope.prototype.mode = 0;

        /**
         * Creates a new ModeEnvelope instance using the specified properties.
         * @function create
         * @memberof motors_mirroring.ModeEnvelope
         * @static
         * @param {motors_mirroring.IModeEnvelope=} [properties] Properties to set
         * @returns {motors_mirroring.ModeEnvelope} ModeEnvelope instance
         */
        ModeEnvelope.create = function create(properties) {
            return new ModeEnvelope(properties);
        };

        /**
         * Encodes the specified ModeEnvelope message. Does not implicitly {@link motors_mirroring.ModeEnvelope.verify|verify} messages.
         * @function encode
         * @memberof motors_mirroring.ModeEnvelope
         * @static
         * @param {motors_mirroring.IModeEnvelope} message ModeEnvelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ModeEnvelope.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.monotonicStampNs != null && Object.hasOwnProperty.call(message, "monotonicStampNs"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.monotonicStampNs);
            if (message.localStampNs != null && Object.hasOwnProperty.call(message, "localStampNs"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.localStampNs);
            if (message.appStartId != null && Object.hasOwnProperty.call(message, "appStartId"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.appStartId);
            if (message.bus != null && Object.hasOwnProperty.call(message, "bus"))
                $root.motors_mirroring.MirroringBus.encode(message.bus, writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.mode);
            return writer;
        };

        /**
         * Encodes the specified ModeEnvelope message, length delimited. Does not implicitly {@link motors_mirroring.ModeEnvelope.verify|verify} messages.
         * @function encodeDelimited
         * @memberof motors_mirroring.ModeEnvelope
         * @static
         * @param {motors_mirroring.IModeEnvelope} message ModeEnvelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ModeEnvelope.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ModeEnvelope message from the specified reader or buffer.
         * @function decode
         * @memberof motors_mirroring.ModeEnvelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {motors_mirroring.ModeEnvelope} ModeEnvelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ModeEnvelope.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.motors_mirroring.ModeEnvelope();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.monotonicStampNs = reader.uint64();
                        break;
                    }
                case 2: {
                        message.localStampNs = reader.uint64();
                        break;
                    }
                case 3: {
                        message.appStartId = reader.uint64();
                        break;
                    }
                case 4: {
                        message.bus = $root.motors_mirroring.MirroringBus.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 5: {
                        message.mode = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ModeEnvelope message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof motors_mirroring.ModeEnvelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {motors_mirroring.ModeEnvelope} ModeEnvelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ModeEnvelope.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ModeEnvelope message.
         * @function verify
         * @memberof motors_mirroring.ModeEnvelope
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ModeEnvelope.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (!$util.isInteger(message.monotonicStampNs) && !(message.monotonicStampNs && $util.isInteger(message.monotonicStampNs.low) && $util.isInteger(message.monotonicStampNs.high)))
                    return "monotonicStampNs: integer|Long expected";
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (!$util.isInteger(message.localStampNs) && !(message.localStampNs && $util.isInteger(message.localStampNs.low) && $util.isInteger(message.localStampNs.high)))
                    return "localStampNs: integer|Long expected";
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (!$util.isInteger(message.appStartId) && !(message.appStartId && $util.isInteger(message.appStartId.low) && $util.isInteger(message.appStartId.high)))
                    return "appStartId: integer|Long expected";
            if (message.bus != null && message.hasOwnProperty("bus")) {
                let error = $root.motors_mirroring.MirroringBus.verify(message.bus, long + 1);
                if (error)
                    return "bus." + error;
            }
            if (message.mode != null && message.hasOwnProperty("mode"))
                switch (message.mode) {
                default:
                    return "mode: enum value expected";
                case 0:
                case 1:
                case 2:
                    break;
                }
            return null;
        };

        /**
         * Creates a ModeEnvelope message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof motors_mirroring.ModeEnvelope
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {motors_mirroring.ModeEnvelope} ModeEnvelope
         */
        ModeEnvelope.fromObject = function fromObject(object, long) {
            if (object instanceof $root.motors_mirroring.ModeEnvelope)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.motors_mirroring.ModeEnvelope();
            if (object.monotonicStampNs != null)
                if ($util.Long)
                    (message.monotonicStampNs = $util.Long.fromValue(object.monotonicStampNs)).unsigned = true;
                else if (typeof object.monotonicStampNs === "string")
                    message.monotonicStampNs = parseInt(object.monotonicStampNs, 10);
                else if (typeof object.monotonicStampNs === "number")
                    message.monotonicStampNs = object.monotonicStampNs;
                else if (typeof object.monotonicStampNs === "object")
                    message.monotonicStampNs = new $util.LongBits(object.monotonicStampNs.low >>> 0, object.monotonicStampNs.high >>> 0).toNumber(true);
            if (object.localStampNs != null)
                if ($util.Long)
                    (message.localStampNs = $util.Long.fromValue(object.localStampNs)).unsigned = true;
                else if (typeof object.localStampNs === "string")
                    message.localStampNs = parseInt(object.localStampNs, 10);
                else if (typeof object.localStampNs === "number")
                    message.localStampNs = object.localStampNs;
                else if (typeof object.localStampNs === "object")
                    message.localStampNs = new $util.LongBits(object.localStampNs.low >>> 0, object.localStampNs.high >>> 0).toNumber(true);
            if (object.appStartId != null)
                if ($util.Long)
                    (message.appStartId = $util.Long.fromValue(object.appStartId)).unsigned = true;
                else if (typeof object.appStartId === "string")
                    message.appStartId = parseInt(object.appStartId, 10);
                else if (typeof object.appStartId === "number")
                    message.appStartId = object.appStartId;
                else if (typeof object.appStartId === "object")
                    message.appStartId = new $util.LongBits(object.appStartId.low >>> 0, object.appStartId.high >>> 0).toNumber(true);
            if (object.bus != null) {
                if (typeof object.bus !== "object")
                    throw TypeError(".motors_mirroring.ModeEnvelope.bus: object expected");
                message.bus = $root.motors_mirroring.MirroringBus.fromObject(object.bus, long + 1);
            }
            switch (object.mode) {
            default:
                if (typeof object.mode === "number") {
                    message.mode = object.mode;
                    break;
                }
                break;
            case "BR_UNKNOWN":
            case 0:
                message.mode = 0;
                break;
            case "BR_LEADER":
            case 1:
                message.mode = 1;
                break;
            case "BR_FOLLOWER":
            case 2:
                message.mode = 2;
                break;
            }
            return message;
        };

        /**
         * Creates a plain object from a ModeEnvelope message. Also converts values to other types if specified.
         * @function toObject
         * @memberof motors_mirroring.ModeEnvelope
         * @static
         * @param {motors_mirroring.ModeEnvelope} message ModeEnvelope
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ModeEnvelope.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.monotonicStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.monotonicStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.localStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.localStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.appStartId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.appStartId = options.longs === String ? "0" : 0;
                object.bus = null;
                object.mode = options.enums === String ? "BR_UNKNOWN" : 0;
            }
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (typeof message.monotonicStampNs === "number")
                    object.monotonicStampNs = options.longs === String ? String(message.monotonicStampNs) : message.monotonicStampNs;
                else
                    object.monotonicStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.monotonicStampNs) : options.longs === Number ? new $util.LongBits(message.monotonicStampNs.low >>> 0, message.monotonicStampNs.high >>> 0).toNumber(true) : message.monotonicStampNs;
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (typeof message.localStampNs === "number")
                    object.localStampNs = options.longs === String ? String(message.localStampNs) : message.localStampNs;
                else
                    object.localStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.localStampNs) : options.longs === Number ? new $util.LongBits(message.localStampNs.low >>> 0, message.localStampNs.high >>> 0).toNumber(true) : message.localStampNs;
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (typeof message.appStartId === "number")
                    object.appStartId = options.longs === String ? String(message.appStartId) : message.appStartId;
                else
                    object.appStartId = options.longs === String ? $util.Long.prototype.toString.call(message.appStartId) : options.longs === Number ? new $util.LongBits(message.appStartId.low >>> 0, message.appStartId.high >>> 0).toNumber(true) : message.appStartId;
            if (message.bus != null && message.hasOwnProperty("bus"))
                object.bus = $root.motors_mirroring.MirroringBus.toObject(message.bus, options);
            if (message.mode != null && message.hasOwnProperty("mode"))
                object.mode = options.enums === String ? $root.motors_mirroring.BusMode[message.mode] === undefined ? message.mode : $root.motors_mirroring.BusMode[message.mode] : message.mode;
            return object;
        };

        /**
         * Converts this ModeEnvelope to JSON.
         * @function toJSON
         * @memberof motors_mirroring.ModeEnvelope
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ModeEnvelope.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ModeEnvelope
         * @function getTypeUrl
         * @memberof motors_mirroring.ModeEnvelope
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ModeEnvelope.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/motors_mirroring.ModeEnvelope";
        };

        return ModeEnvelope;
    })();

    /**
     * CommandType enum.
     * @name motors_mirroring.CommandType
     * @enum {number}
     * @property {number} CT_START_MIRROR=0 CT_START_MIRROR value
     * @property {number} CT_STOP_MIRROR=1 CT_STOP_MIRROR value
     */
    motors_mirroring.CommandType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "CT_START_MIRROR"] = 0;
        values[valuesById[1] = "CT_STOP_MIRROR"] = 1;
        return values;
    })();

    motors_mirroring.Command = (function() {

        /**
         * Properties of a Command.
         * @memberof motors_mirroring
         * @interface ICommand
         * @property {motors_mirroring.CommandType|null} [type] Command type
         * @property {motors_mirroring.IMirroringBus|null} [source] Command source
         * @property {Array.<motors_mirroring.IMirroringBus>|null} [targets] Command targets
         */

        /**
         * Constructs a new Command.
         * @memberof motors_mirroring
         * @classdesc Represents a Command.
         * @implements ICommand
         * @constructor
         * @param {motors_mirroring.ICommand=} [properties] Properties to set
         */
        function Command(properties) {
            this.targets = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Command type.
         * @member {motors_mirroring.CommandType} type
         * @memberof motors_mirroring.Command
         * @instance
         */
        Command.prototype.type = 0;

        /**
         * Command source.
         * @member {motors_mirroring.IMirroringBus|null|undefined} source
         * @memberof motors_mirroring.Command
         * @instance
         */
        Command.prototype.source = null;

        /**
         * Command targets.
         * @member {Array.<motors_mirroring.IMirroringBus>} targets
         * @memberof motors_mirroring.Command
         * @instance
         */
        Command.prototype.targets = $util.emptyArray;

        /**
         * Creates a new Command instance using the specified properties.
         * @function create
         * @memberof motors_mirroring.Command
         * @static
         * @param {motors_mirroring.ICommand=} [properties] Properties to set
         * @returns {motors_mirroring.Command} Command instance
         */
        Command.create = function create(properties) {
            return new Command(properties);
        };

        /**
         * Encodes the specified Command message. Does not implicitly {@link motors_mirroring.Command.verify|verify} messages.
         * @function encode
         * @memberof motors_mirroring.Command
         * @static
         * @param {motors_mirroring.ICommand} message Command message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Command.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.type);
            if (message.source != null && Object.hasOwnProperty.call(message, "source"))
                $root.motors_mirroring.MirroringBus.encode(message.source, writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.targets != null && message.targets.length)
                for (let i = 0; i < message.targets.length; ++i)
                    $root.motors_mirroring.MirroringBus.encode(message.targets[i], writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified Command message, length delimited. Does not implicitly {@link motors_mirroring.Command.verify|verify} messages.
         * @function encodeDelimited
         * @memberof motors_mirroring.Command
         * @static
         * @param {motors_mirroring.ICommand} message Command message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Command.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Command message from the specified reader or buffer.
         * @function decode
         * @memberof motors_mirroring.Command
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {motors_mirroring.Command} Command
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Command.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.motors_mirroring.Command();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.type = reader.int32();
                        break;
                    }
                case 2: {
                        message.source = $root.motors_mirroring.MirroringBus.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 10: {
                        if (!(message.targets && message.targets.length))
                            message.targets = [];
                        message.targets.push($root.motors_mirroring.MirroringBus.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Command message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof motors_mirroring.Command
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {motors_mirroring.Command} Command
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Command.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Command message.
         * @function verify
         * @memberof motors_mirroring.Command
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Command.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.type != null && message.hasOwnProperty("type"))
                switch (message.type) {
                default:
                    return "type: enum value expected";
                case 0:
                case 1:
                    break;
                }
            if (message.source != null && message.hasOwnProperty("source")) {
                let error = $root.motors_mirroring.MirroringBus.verify(message.source, long + 1);
                if (error)
                    return "source." + error;
            }
            if (message.targets != null && message.hasOwnProperty("targets")) {
                if (!Array.isArray(message.targets))
                    return "targets: array expected";
                for (let i = 0; i < message.targets.length; ++i) {
                    let error = $root.motors_mirroring.MirroringBus.verify(message.targets[i], long + 1);
                    if (error)
                        return "targets." + error;
                }
            }
            return null;
        };

        /**
         * Creates a Command message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof motors_mirroring.Command
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {motors_mirroring.Command} Command
         */
        Command.fromObject = function fromObject(object, long) {
            if (object instanceof $root.motors_mirroring.Command)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.motors_mirroring.Command();
            switch (object.type) {
            default:
                if (typeof object.type === "number") {
                    message.type = object.type;
                    break;
                }
                break;
            case "CT_START_MIRROR":
            case 0:
                message.type = 0;
                break;
            case "CT_STOP_MIRROR":
            case 1:
                message.type = 1;
                break;
            }
            if (object.source != null) {
                if (typeof object.source !== "object")
                    throw TypeError(".motors_mirroring.Command.source: object expected");
                message.source = $root.motors_mirroring.MirroringBus.fromObject(object.source, long + 1);
            }
            if (object.targets) {
                if (!Array.isArray(object.targets))
                    throw TypeError(".motors_mirroring.Command.targets: array expected");
                message.targets = [];
                for (let i = 0; i < object.targets.length; ++i) {
                    if (typeof object.targets[i] !== "object")
                        throw TypeError(".motors_mirroring.Command.targets: object expected");
                    message.targets[i] = $root.motors_mirroring.MirroringBus.fromObject(object.targets[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a Command message. Also converts values to other types if specified.
         * @function toObject
         * @memberof motors_mirroring.Command
         * @static
         * @param {motors_mirroring.Command} message Command
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Command.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.targets = [];
            if (options.defaults) {
                object.type = options.enums === String ? "CT_START_MIRROR" : 0;
                object.source = null;
            }
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = options.enums === String ? $root.motors_mirroring.CommandType[message.type] === undefined ? message.type : $root.motors_mirroring.CommandType[message.type] : message.type;
            if (message.source != null && message.hasOwnProperty("source"))
                object.source = $root.motors_mirroring.MirroringBus.toObject(message.source, options);
            if (message.targets && message.targets.length) {
                object.targets = [];
                for (let j = 0; j < message.targets.length; ++j)
                    object.targets[j] = $root.motors_mirroring.MirroringBus.toObject(message.targets[j], options);
            }
            return object;
        };

        /**
         * Converts this Command to JSON.
         * @function toJSON
         * @memberof motors_mirroring.Command
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Command.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Command
         * @function getTypeUrl
         * @memberof motors_mirroring.Command
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Command.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/motors_mirroring.Command";
        };

        return Command;
    })();

    motors_mirroring.TxEnvelope = (function() {

        /**
         * Properties of a TxEnvelope.
         * @memberof motors_mirroring
         * @interface ITxEnvelope
         * @property {Long|null} [monotonicStampNs] TxEnvelope monotonicStampNs
         * @property {Long|null} [localStampNs] TxEnvelope localStampNs
         * @property {Long|null} [appStartId] TxEnvelope appStartId
         * @property {motors_mirroring.ICommand|null} [command] TxEnvelope command
         */

        /**
         * Constructs a new TxEnvelope.
         * @memberof motors_mirroring
         * @classdesc Represents a TxEnvelope.
         * @implements ITxEnvelope
         * @constructor
         * @param {motors_mirroring.ITxEnvelope=} [properties] Properties to set
         */
        function TxEnvelope(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * TxEnvelope monotonicStampNs.
         * @member {Long} monotonicStampNs
         * @memberof motors_mirroring.TxEnvelope
         * @instance
         */
        TxEnvelope.prototype.monotonicStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * TxEnvelope localStampNs.
         * @member {Long} localStampNs
         * @memberof motors_mirroring.TxEnvelope
         * @instance
         */
        TxEnvelope.prototype.localStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * TxEnvelope appStartId.
         * @member {Long} appStartId
         * @memberof motors_mirroring.TxEnvelope
         * @instance
         */
        TxEnvelope.prototype.appStartId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * TxEnvelope command.
         * @member {motors_mirroring.ICommand|null|undefined} command
         * @memberof motors_mirroring.TxEnvelope
         * @instance
         */
        TxEnvelope.prototype.command = null;

        /**
         * Creates a new TxEnvelope instance using the specified properties.
         * @function create
         * @memberof motors_mirroring.TxEnvelope
         * @static
         * @param {motors_mirroring.ITxEnvelope=} [properties] Properties to set
         * @returns {motors_mirroring.TxEnvelope} TxEnvelope instance
         */
        TxEnvelope.create = function create(properties) {
            return new TxEnvelope(properties);
        };

        /**
         * Encodes the specified TxEnvelope message. Does not implicitly {@link motors_mirroring.TxEnvelope.verify|verify} messages.
         * @function encode
         * @memberof motors_mirroring.TxEnvelope
         * @static
         * @param {motors_mirroring.ITxEnvelope} message TxEnvelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TxEnvelope.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.monotonicStampNs != null && Object.hasOwnProperty.call(message, "monotonicStampNs"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.monotonicStampNs);
            if (message.localStampNs != null && Object.hasOwnProperty.call(message, "localStampNs"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.localStampNs);
            if (message.appStartId != null && Object.hasOwnProperty.call(message, "appStartId"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.appStartId);
            if (message.command != null && Object.hasOwnProperty.call(message, "command"))
                $root.motors_mirroring.Command.encode(message.command, writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified TxEnvelope message, length delimited. Does not implicitly {@link motors_mirroring.TxEnvelope.verify|verify} messages.
         * @function encodeDelimited
         * @memberof motors_mirroring.TxEnvelope
         * @static
         * @param {motors_mirroring.ITxEnvelope} message TxEnvelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TxEnvelope.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a TxEnvelope message from the specified reader or buffer.
         * @function decode
         * @memberof motors_mirroring.TxEnvelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {motors_mirroring.TxEnvelope} TxEnvelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TxEnvelope.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.motors_mirroring.TxEnvelope();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.monotonicStampNs = reader.uint64();
                        break;
                    }
                case 2: {
                        message.localStampNs = reader.uint64();
                        break;
                    }
                case 3: {
                        message.appStartId = reader.uint64();
                        break;
                    }
                case 10: {
                        message.command = $root.motors_mirroring.Command.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a TxEnvelope message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof motors_mirroring.TxEnvelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {motors_mirroring.TxEnvelope} TxEnvelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TxEnvelope.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TxEnvelope message.
         * @function verify
         * @memberof motors_mirroring.TxEnvelope
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TxEnvelope.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (!$util.isInteger(message.monotonicStampNs) && !(message.monotonicStampNs && $util.isInteger(message.monotonicStampNs.low) && $util.isInteger(message.monotonicStampNs.high)))
                    return "monotonicStampNs: integer|Long expected";
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (!$util.isInteger(message.localStampNs) && !(message.localStampNs && $util.isInteger(message.localStampNs.low) && $util.isInteger(message.localStampNs.high)))
                    return "localStampNs: integer|Long expected";
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (!$util.isInteger(message.appStartId) && !(message.appStartId && $util.isInteger(message.appStartId.low) && $util.isInteger(message.appStartId.high)))
                    return "appStartId: integer|Long expected";
            if (message.command != null && message.hasOwnProperty("command")) {
                let error = $root.motors_mirroring.Command.verify(message.command, long + 1);
                if (error)
                    return "command." + error;
            }
            return null;
        };

        /**
         * Creates a TxEnvelope message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof motors_mirroring.TxEnvelope
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {motors_mirroring.TxEnvelope} TxEnvelope
         */
        TxEnvelope.fromObject = function fromObject(object, long) {
            if (object instanceof $root.motors_mirroring.TxEnvelope)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.motors_mirroring.TxEnvelope();
            if (object.monotonicStampNs != null)
                if ($util.Long)
                    (message.monotonicStampNs = $util.Long.fromValue(object.monotonicStampNs)).unsigned = true;
                else if (typeof object.monotonicStampNs === "string")
                    message.monotonicStampNs = parseInt(object.monotonicStampNs, 10);
                else if (typeof object.monotonicStampNs === "number")
                    message.monotonicStampNs = object.monotonicStampNs;
                else if (typeof object.monotonicStampNs === "object")
                    message.monotonicStampNs = new $util.LongBits(object.monotonicStampNs.low >>> 0, object.monotonicStampNs.high >>> 0).toNumber(true);
            if (object.localStampNs != null)
                if ($util.Long)
                    (message.localStampNs = $util.Long.fromValue(object.localStampNs)).unsigned = true;
                else if (typeof object.localStampNs === "string")
                    message.localStampNs = parseInt(object.localStampNs, 10);
                else if (typeof object.localStampNs === "number")
                    message.localStampNs = object.localStampNs;
                else if (typeof object.localStampNs === "object")
                    message.localStampNs = new $util.LongBits(object.localStampNs.low >>> 0, object.localStampNs.high >>> 0).toNumber(true);
            if (object.appStartId != null)
                if ($util.Long)
                    (message.appStartId = $util.Long.fromValue(object.appStartId)).unsigned = true;
                else if (typeof object.appStartId === "string")
                    message.appStartId = parseInt(object.appStartId, 10);
                else if (typeof object.appStartId === "number")
                    message.appStartId = object.appStartId;
                else if (typeof object.appStartId === "object")
                    message.appStartId = new $util.LongBits(object.appStartId.low >>> 0, object.appStartId.high >>> 0).toNumber(true);
            if (object.command != null) {
                if (typeof object.command !== "object")
                    throw TypeError(".motors_mirroring.TxEnvelope.command: object expected");
                message.command = $root.motors_mirroring.Command.fromObject(object.command, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a TxEnvelope message. Also converts values to other types if specified.
         * @function toObject
         * @memberof motors_mirroring.TxEnvelope
         * @static
         * @param {motors_mirroring.TxEnvelope} message TxEnvelope
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TxEnvelope.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.monotonicStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.monotonicStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.localStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.localStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.appStartId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.appStartId = options.longs === String ? "0" : 0;
                object.command = null;
            }
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (typeof message.monotonicStampNs === "number")
                    object.monotonicStampNs = options.longs === String ? String(message.monotonicStampNs) : message.monotonicStampNs;
                else
                    object.monotonicStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.monotonicStampNs) : options.longs === Number ? new $util.LongBits(message.monotonicStampNs.low >>> 0, message.monotonicStampNs.high >>> 0).toNumber(true) : message.monotonicStampNs;
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (typeof message.localStampNs === "number")
                    object.localStampNs = options.longs === String ? String(message.localStampNs) : message.localStampNs;
                else
                    object.localStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.localStampNs) : options.longs === Number ? new $util.LongBits(message.localStampNs.low >>> 0, message.localStampNs.high >>> 0).toNumber(true) : message.localStampNs;
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (typeof message.appStartId === "number")
                    object.appStartId = options.longs === String ? String(message.appStartId) : message.appStartId;
                else
                    object.appStartId = options.longs === String ? $util.Long.prototype.toString.call(message.appStartId) : options.longs === Number ? new $util.LongBits(message.appStartId.low >>> 0, message.appStartId.high >>> 0).toNumber(true) : message.appStartId;
            if (message.command != null && message.hasOwnProperty("command"))
                object.command = $root.motors_mirroring.Command.toObject(message.command, options);
            return object;
        };

        /**
         * Converts this TxEnvelope to JSON.
         * @function toJSON
         * @memberof motors_mirroring.TxEnvelope
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TxEnvelope.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for TxEnvelope
         * @function getTypeUrl
         * @memberof motors_mirroring.TxEnvelope
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        TxEnvelope.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/motors_mirroring.TxEnvelope";
        };

        return TxEnvelope;
    })();

    motors_mirroring.RxEnvelope = (function() {

        /**
         * Properties of a RxEnvelope.
         * @memberof motors_mirroring
         * @interface IRxEnvelope
         * @property {Long|null} [monotonicStampNs] RxEnvelope monotonicStampNs
         * @property {Long|null} [localStampNs] RxEnvelope localStampNs
         * @property {Long|null} [appStartId] RxEnvelope appStartId
         * @property {motors_mirroring.IInferenceState|null} [state] RxEnvelope state
         * @property {motors_mirroring.ICommand|null} [command] RxEnvelope command
         */

        /**
         * Constructs a new RxEnvelope.
         * @memberof motors_mirroring
         * @classdesc Represents a RxEnvelope.
         * @implements IRxEnvelope
         * @constructor
         * @param {motors_mirroring.IRxEnvelope=} [properties] Properties to set
         */
        function RxEnvelope(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RxEnvelope monotonicStampNs.
         * @member {Long} monotonicStampNs
         * @memberof motors_mirroring.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.monotonicStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * RxEnvelope localStampNs.
         * @member {Long} localStampNs
         * @memberof motors_mirroring.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.localStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * RxEnvelope appStartId.
         * @member {Long} appStartId
         * @memberof motors_mirroring.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.appStartId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * RxEnvelope state.
         * @member {motors_mirroring.IInferenceState|null|undefined} state
         * @memberof motors_mirroring.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.state = null;

        /**
         * RxEnvelope command.
         * @member {motors_mirroring.ICommand|null|undefined} command
         * @memberof motors_mirroring.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.command = null;

        /**
         * Creates a new RxEnvelope instance using the specified properties.
         * @function create
         * @memberof motors_mirroring.RxEnvelope
         * @static
         * @param {motors_mirroring.IRxEnvelope=} [properties] Properties to set
         * @returns {motors_mirroring.RxEnvelope} RxEnvelope instance
         */
        RxEnvelope.create = function create(properties) {
            return new RxEnvelope(properties);
        };

        /**
         * Encodes the specified RxEnvelope message. Does not implicitly {@link motors_mirroring.RxEnvelope.verify|verify} messages.
         * @function encode
         * @memberof motors_mirroring.RxEnvelope
         * @static
         * @param {motors_mirroring.IRxEnvelope} message RxEnvelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RxEnvelope.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.monotonicStampNs != null && Object.hasOwnProperty.call(message, "monotonicStampNs"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.monotonicStampNs);
            if (message.localStampNs != null && Object.hasOwnProperty.call(message, "localStampNs"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.localStampNs);
            if (message.appStartId != null && Object.hasOwnProperty.call(message, "appStartId"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.appStartId);
            if (message.state != null && Object.hasOwnProperty.call(message, "state"))
                $root.motors_mirroring.InferenceState.encode(message.state, writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            if (message.command != null && Object.hasOwnProperty.call(message, "command"))
                $root.motors_mirroring.Command.encode(message.command, writer.uint32(/* id 11, wireType 2 =*/90).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified RxEnvelope message, length delimited. Does not implicitly {@link motors_mirroring.RxEnvelope.verify|verify} messages.
         * @function encodeDelimited
         * @memberof motors_mirroring.RxEnvelope
         * @static
         * @param {motors_mirroring.IRxEnvelope} message RxEnvelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RxEnvelope.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RxEnvelope message from the specified reader or buffer.
         * @function decode
         * @memberof motors_mirroring.RxEnvelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {motors_mirroring.RxEnvelope} RxEnvelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RxEnvelope.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.motors_mirroring.RxEnvelope();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.monotonicStampNs = reader.uint64();
                        break;
                    }
                case 2: {
                        message.localStampNs = reader.uint64();
                        break;
                    }
                case 3: {
                        message.appStartId = reader.uint64();
                        break;
                    }
                case 10: {
                        message.state = $root.motors_mirroring.InferenceState.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 11: {
                        message.command = $root.motors_mirroring.Command.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RxEnvelope message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof motors_mirroring.RxEnvelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {motors_mirroring.RxEnvelope} RxEnvelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RxEnvelope.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RxEnvelope message.
         * @function verify
         * @memberof motors_mirroring.RxEnvelope
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RxEnvelope.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (!$util.isInteger(message.monotonicStampNs) && !(message.monotonicStampNs && $util.isInteger(message.monotonicStampNs.low) && $util.isInteger(message.monotonicStampNs.high)))
                    return "monotonicStampNs: integer|Long expected";
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (!$util.isInteger(message.localStampNs) && !(message.localStampNs && $util.isInteger(message.localStampNs.low) && $util.isInteger(message.localStampNs.high)))
                    return "localStampNs: integer|Long expected";
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (!$util.isInteger(message.appStartId) && !(message.appStartId && $util.isInteger(message.appStartId.low) && $util.isInteger(message.appStartId.high)))
                    return "appStartId: integer|Long expected";
            if (message.state != null && message.hasOwnProperty("state")) {
                let error = $root.motors_mirroring.InferenceState.verify(message.state, long + 1);
                if (error)
                    return "state." + error;
            }
            if (message.command != null && message.hasOwnProperty("command")) {
                let error = $root.motors_mirroring.Command.verify(message.command, long + 1);
                if (error)
                    return "command." + error;
            }
            return null;
        };

        /**
         * Creates a RxEnvelope message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof motors_mirroring.RxEnvelope
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {motors_mirroring.RxEnvelope} RxEnvelope
         */
        RxEnvelope.fromObject = function fromObject(object, long) {
            if (object instanceof $root.motors_mirroring.RxEnvelope)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.motors_mirroring.RxEnvelope();
            if (object.monotonicStampNs != null)
                if ($util.Long)
                    (message.monotonicStampNs = $util.Long.fromValue(object.monotonicStampNs)).unsigned = true;
                else if (typeof object.monotonicStampNs === "string")
                    message.monotonicStampNs = parseInt(object.monotonicStampNs, 10);
                else if (typeof object.monotonicStampNs === "number")
                    message.monotonicStampNs = object.monotonicStampNs;
                else if (typeof object.monotonicStampNs === "object")
                    message.monotonicStampNs = new $util.LongBits(object.monotonicStampNs.low >>> 0, object.monotonicStampNs.high >>> 0).toNumber(true);
            if (object.localStampNs != null)
                if ($util.Long)
                    (message.localStampNs = $util.Long.fromValue(object.localStampNs)).unsigned = true;
                else if (typeof object.localStampNs === "string")
                    message.localStampNs = parseInt(object.localStampNs, 10);
                else if (typeof object.localStampNs === "number")
                    message.localStampNs = object.localStampNs;
                else if (typeof object.localStampNs === "object")
                    message.localStampNs = new $util.LongBits(object.localStampNs.low >>> 0, object.localStampNs.high >>> 0).toNumber(true);
            if (object.appStartId != null)
                if ($util.Long)
                    (message.appStartId = $util.Long.fromValue(object.appStartId)).unsigned = true;
                else if (typeof object.appStartId === "string")
                    message.appStartId = parseInt(object.appStartId, 10);
                else if (typeof object.appStartId === "number")
                    message.appStartId = object.appStartId;
                else if (typeof object.appStartId === "object")
                    message.appStartId = new $util.LongBits(object.appStartId.low >>> 0, object.appStartId.high >>> 0).toNumber(true);
            if (object.state != null) {
                if (typeof object.state !== "object")
                    throw TypeError(".motors_mirroring.RxEnvelope.state: object expected");
                message.state = $root.motors_mirroring.InferenceState.fromObject(object.state, long + 1);
            }
            if (object.command != null) {
                if (typeof object.command !== "object")
                    throw TypeError(".motors_mirroring.RxEnvelope.command: object expected");
                message.command = $root.motors_mirroring.Command.fromObject(object.command, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a RxEnvelope message. Also converts values to other types if specified.
         * @function toObject
         * @memberof motors_mirroring.RxEnvelope
         * @static
         * @param {motors_mirroring.RxEnvelope} message RxEnvelope
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RxEnvelope.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.monotonicStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.monotonicStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.localStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.localStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.appStartId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.appStartId = options.longs === String ? "0" : 0;
                object.state = null;
                object.command = null;
            }
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (typeof message.monotonicStampNs === "number")
                    object.monotonicStampNs = options.longs === String ? String(message.monotonicStampNs) : message.monotonicStampNs;
                else
                    object.monotonicStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.monotonicStampNs) : options.longs === Number ? new $util.LongBits(message.monotonicStampNs.low >>> 0, message.monotonicStampNs.high >>> 0).toNumber(true) : message.monotonicStampNs;
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (typeof message.localStampNs === "number")
                    object.localStampNs = options.longs === String ? String(message.localStampNs) : message.localStampNs;
                else
                    object.localStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.localStampNs) : options.longs === Number ? new $util.LongBits(message.localStampNs.low >>> 0, message.localStampNs.high >>> 0).toNumber(true) : message.localStampNs;
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (typeof message.appStartId === "number")
                    object.appStartId = options.longs === String ? String(message.appStartId) : message.appStartId;
                else
                    object.appStartId = options.longs === String ? $util.Long.prototype.toString.call(message.appStartId) : options.longs === Number ? new $util.LongBits(message.appStartId.low >>> 0, message.appStartId.high >>> 0).toNumber(true) : message.appStartId;
            if (message.state != null && message.hasOwnProperty("state"))
                object.state = $root.motors_mirroring.InferenceState.toObject(message.state, options);
            if (message.command != null && message.hasOwnProperty("command"))
                object.command = $root.motors_mirroring.Command.toObject(message.command, options);
            return object;
        };

        /**
         * Converts this RxEnvelope to JSON.
         * @function toJSON
         * @memberof motors_mirroring.RxEnvelope
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RxEnvelope.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RxEnvelope
         * @function getTypeUrl
         * @memberof motors_mirroring.RxEnvelope
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RxEnvelope.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/motors_mirroring.RxEnvelope";
        };

        return RxEnvelope;
    })();

    motors_mirroring.InferenceState = (function() {

        /**
         * Properties of an InferenceState.
         * @memberof motors_mirroring
         * @interface IInferenceState
         * @property {Array.<motors_mirroring.InferenceState.IBus>|null} [modes] InferenceState modes
         * @property {Array.<motors_mirroring.InferenceState.IMirroring>|null} [mirroring] InferenceState mirroring
         */

        /**
         * Constructs a new InferenceState.
         * @memberof motors_mirroring
         * @classdesc Represents an InferenceState.
         * @implements IInferenceState
         * @constructor
         * @param {motors_mirroring.IInferenceState=} [properties] Properties to set
         */
        function InferenceState(properties) {
            this.modes = [];
            this.mirroring = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * InferenceState modes.
         * @member {Array.<motors_mirroring.InferenceState.IBus>} modes
         * @memberof motors_mirroring.InferenceState
         * @instance
         */
        InferenceState.prototype.modes = $util.emptyArray;

        /**
         * InferenceState mirroring.
         * @member {Array.<motors_mirroring.InferenceState.IMirroring>} mirroring
         * @memberof motors_mirroring.InferenceState
         * @instance
         */
        InferenceState.prototype.mirroring = $util.emptyArray;

        /**
         * Creates a new InferenceState instance using the specified properties.
         * @function create
         * @memberof motors_mirroring.InferenceState
         * @static
         * @param {motors_mirroring.IInferenceState=} [properties] Properties to set
         * @returns {motors_mirroring.InferenceState} InferenceState instance
         */
        InferenceState.create = function create(properties) {
            return new InferenceState(properties);
        };

        /**
         * Encodes the specified InferenceState message. Does not implicitly {@link motors_mirroring.InferenceState.verify|verify} messages.
         * @function encode
         * @memberof motors_mirroring.InferenceState
         * @static
         * @param {motors_mirroring.IInferenceState} message InferenceState message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        InferenceState.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.modes != null && message.modes.length)
                for (let i = 0; i < message.modes.length; ++i)
                    $root.motors_mirroring.InferenceState.Bus.encode(message.modes[i], writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            if (message.mirroring != null && message.mirroring.length)
                for (let i = 0; i < message.mirroring.length; ++i)
                    $root.motors_mirroring.InferenceState.Mirroring.encode(message.mirroring[i], writer.uint32(/* id 11, wireType 2 =*/90).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified InferenceState message, length delimited. Does not implicitly {@link motors_mirroring.InferenceState.verify|verify} messages.
         * @function encodeDelimited
         * @memberof motors_mirroring.InferenceState
         * @static
         * @param {motors_mirroring.IInferenceState} message InferenceState message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        InferenceState.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an InferenceState message from the specified reader or buffer.
         * @function decode
         * @memberof motors_mirroring.InferenceState
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {motors_mirroring.InferenceState} InferenceState
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        InferenceState.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.motors_mirroring.InferenceState();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 10: {
                        if (!(message.modes && message.modes.length))
                            message.modes = [];
                        message.modes.push($root.motors_mirroring.InferenceState.Bus.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 11: {
                        if (!(message.mirroring && message.mirroring.length))
                            message.mirroring = [];
                        message.mirroring.push($root.motors_mirroring.InferenceState.Mirroring.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an InferenceState message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof motors_mirroring.InferenceState
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {motors_mirroring.InferenceState} InferenceState
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        InferenceState.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an InferenceState message.
         * @function verify
         * @memberof motors_mirroring.InferenceState
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        InferenceState.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.modes != null && message.hasOwnProperty("modes")) {
                if (!Array.isArray(message.modes))
                    return "modes: array expected";
                for (let i = 0; i < message.modes.length; ++i) {
                    let error = $root.motors_mirroring.InferenceState.Bus.verify(message.modes[i], long + 1);
                    if (error)
                        return "modes." + error;
                }
            }
            if (message.mirroring != null && message.hasOwnProperty("mirroring")) {
                if (!Array.isArray(message.mirroring))
                    return "mirroring: array expected";
                for (let i = 0; i < message.mirroring.length; ++i) {
                    let error = $root.motors_mirroring.InferenceState.Mirroring.verify(message.mirroring[i], long + 1);
                    if (error)
                        return "mirroring." + error;
                }
            }
            return null;
        };

        /**
         * Creates an InferenceState message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof motors_mirroring.InferenceState
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {motors_mirroring.InferenceState} InferenceState
         */
        InferenceState.fromObject = function fromObject(object, long) {
            if (object instanceof $root.motors_mirroring.InferenceState)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.motors_mirroring.InferenceState();
            if (object.modes) {
                if (!Array.isArray(object.modes))
                    throw TypeError(".motors_mirroring.InferenceState.modes: array expected");
                message.modes = [];
                for (let i = 0; i < object.modes.length; ++i) {
                    if (typeof object.modes[i] !== "object")
                        throw TypeError(".motors_mirroring.InferenceState.modes: object expected");
                    message.modes[i] = $root.motors_mirroring.InferenceState.Bus.fromObject(object.modes[i], long + 1);
                }
            }
            if (object.mirroring) {
                if (!Array.isArray(object.mirroring))
                    throw TypeError(".motors_mirroring.InferenceState.mirroring: array expected");
                message.mirroring = [];
                for (let i = 0; i < object.mirroring.length; ++i) {
                    if (typeof object.mirroring[i] !== "object")
                        throw TypeError(".motors_mirroring.InferenceState.mirroring: object expected");
                    message.mirroring[i] = $root.motors_mirroring.InferenceState.Mirroring.fromObject(object.mirroring[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from an InferenceState message. Also converts values to other types if specified.
         * @function toObject
         * @memberof motors_mirroring.InferenceState
         * @static
         * @param {motors_mirroring.InferenceState} message InferenceState
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        InferenceState.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.modes = [];
                object.mirroring = [];
            }
            if (message.modes && message.modes.length) {
                object.modes = [];
                for (let j = 0; j < message.modes.length; ++j)
                    object.modes[j] = $root.motors_mirroring.InferenceState.Bus.toObject(message.modes[j], options);
            }
            if (message.mirroring && message.mirroring.length) {
                object.mirroring = [];
                for (let j = 0; j < message.mirroring.length; ++j)
                    object.mirroring[j] = $root.motors_mirroring.InferenceState.Mirroring.toObject(message.mirroring[j], options);
            }
            return object;
        };

        /**
         * Converts this InferenceState to JSON.
         * @function toJSON
         * @memberof motors_mirroring.InferenceState
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        InferenceState.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for InferenceState
         * @function getTypeUrl
         * @memberof motors_mirroring.InferenceState
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        InferenceState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/motors_mirroring.InferenceState";
        };

        InferenceState.Bus = (function() {

            /**
             * Properties of a Bus.
             * @memberof motors_mirroring.InferenceState
             * @interface IBus
             * @property {motors_mirroring.IMirroringBus|null} [id] Bus id
             * @property {motors_mirroring.BusMode|null} [mode] Bus mode
             */

            /**
             * Constructs a new Bus.
             * @memberof motors_mirroring.InferenceState
             * @classdesc Represents a Bus.
             * @implements IBus
             * @constructor
             * @param {motors_mirroring.InferenceState.IBus=} [properties] Properties to set
             */
            function Bus(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * Bus id.
             * @member {motors_mirroring.IMirroringBus|null|undefined} id
             * @memberof motors_mirroring.InferenceState.Bus
             * @instance
             */
            Bus.prototype.id = null;

            /**
             * Bus mode.
             * @member {motors_mirroring.BusMode} mode
             * @memberof motors_mirroring.InferenceState.Bus
             * @instance
             */
            Bus.prototype.mode = 0;

            /**
             * Creates a new Bus instance using the specified properties.
             * @function create
             * @memberof motors_mirroring.InferenceState.Bus
             * @static
             * @param {motors_mirroring.InferenceState.IBus=} [properties] Properties to set
             * @returns {motors_mirroring.InferenceState.Bus} Bus instance
             */
            Bus.create = function create(properties) {
                return new Bus(properties);
            };

            /**
             * Encodes the specified Bus message. Does not implicitly {@link motors_mirroring.InferenceState.Bus.verify|verify} messages.
             * @function encode
             * @memberof motors_mirroring.InferenceState.Bus
             * @static
             * @param {motors_mirroring.InferenceState.IBus} message Bus message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Bus.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                    $root.motors_mirroring.MirroringBus.encode(message.id, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
                if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
                    writer.uint32(/* id 2, wireType 0 =*/16).int32(message.mode);
                return writer;
            };

            /**
             * Encodes the specified Bus message, length delimited. Does not implicitly {@link motors_mirroring.InferenceState.Bus.verify|verify} messages.
             * @function encodeDelimited
             * @memberof motors_mirroring.InferenceState.Bus
             * @static
             * @param {motors_mirroring.InferenceState.IBus} message Bus message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Bus.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a Bus message from the specified reader or buffer.
             * @function decode
             * @memberof motors_mirroring.InferenceState.Bus
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {motors_mirroring.InferenceState.Bus} Bus
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Bus.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.motors_mirroring.InferenceState.Bus();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.id = $root.motors_mirroring.MirroringBus.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 2: {
                            message.mode = reader.int32();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a Bus message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof motors_mirroring.InferenceState.Bus
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {motors_mirroring.InferenceState.Bus} Bus
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Bus.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Bus message.
             * @function verify
             * @memberof motors_mirroring.InferenceState.Bus
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Bus.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.id != null && message.hasOwnProperty("id")) {
                    let error = $root.motors_mirroring.MirroringBus.verify(message.id, long + 1);
                    if (error)
                        return "id." + error;
                }
                if (message.mode != null && message.hasOwnProperty("mode"))
                    switch (message.mode) {
                    default:
                        return "mode: enum value expected";
                    case 0:
                    case 1:
                    case 2:
                        break;
                    }
                return null;
            };

            /**
             * Creates a Bus message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof motors_mirroring.InferenceState.Bus
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {motors_mirroring.InferenceState.Bus} Bus
             */
            Bus.fromObject = function fromObject(object, long) {
                if (object instanceof $root.motors_mirroring.InferenceState.Bus)
                    return object;
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.motors_mirroring.InferenceState.Bus();
                if (object.id != null) {
                    if (typeof object.id !== "object")
                        throw TypeError(".motors_mirroring.InferenceState.Bus.id: object expected");
                    message.id = $root.motors_mirroring.MirroringBus.fromObject(object.id, long + 1);
                }
                switch (object.mode) {
                default:
                    if (typeof object.mode === "number") {
                        message.mode = object.mode;
                        break;
                    }
                    break;
                case "BR_UNKNOWN":
                case 0:
                    message.mode = 0;
                    break;
                case "BR_LEADER":
                case 1:
                    message.mode = 1;
                    break;
                case "BR_FOLLOWER":
                case 2:
                    message.mode = 2;
                    break;
                }
                return message;
            };

            /**
             * Creates a plain object from a Bus message. Also converts values to other types if specified.
             * @function toObject
             * @memberof motors_mirroring.InferenceState.Bus
             * @static
             * @param {motors_mirroring.InferenceState.Bus} message Bus
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Bus.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.id = null;
                    object.mode = options.enums === String ? "BR_UNKNOWN" : 0;
                }
                if (message.id != null && message.hasOwnProperty("id"))
                    object.id = $root.motors_mirroring.MirroringBus.toObject(message.id, options);
                if (message.mode != null && message.hasOwnProperty("mode"))
                    object.mode = options.enums === String ? $root.motors_mirroring.BusMode[message.mode] === undefined ? message.mode : $root.motors_mirroring.BusMode[message.mode] : message.mode;
                return object;
            };

            /**
             * Converts this Bus to JSON.
             * @function toJSON
             * @memberof motors_mirroring.InferenceState.Bus
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Bus.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for Bus
             * @function getTypeUrl
             * @memberof motors_mirroring.InferenceState.Bus
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            Bus.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/motors_mirroring.InferenceState.Bus";
            };

            return Bus;
        })();

        InferenceState.Mirroring = (function() {

            /**
             * Properties of a Mirroring.
             * @memberof motors_mirroring.InferenceState
             * @interface IMirroring
             * @property {motors_mirroring.InferenceState.IBus|null} [source] Mirroring source
             * @property {Array.<motors_mirroring.InferenceState.IBus>|null} [targets] Mirroring targets
             */

            /**
             * Constructs a new Mirroring.
             * @memberof motors_mirroring.InferenceState
             * @classdesc Represents a Mirroring.
             * @implements IMirroring
             * @constructor
             * @param {motors_mirroring.InferenceState.IMirroring=} [properties] Properties to set
             */
            function Mirroring(properties) {
                this.targets = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * Mirroring source.
             * @member {motors_mirroring.InferenceState.IBus|null|undefined} source
             * @memberof motors_mirroring.InferenceState.Mirroring
             * @instance
             */
            Mirroring.prototype.source = null;

            /**
             * Mirroring targets.
             * @member {Array.<motors_mirroring.InferenceState.IBus>} targets
             * @memberof motors_mirroring.InferenceState.Mirroring
             * @instance
             */
            Mirroring.prototype.targets = $util.emptyArray;

            /**
             * Creates a new Mirroring instance using the specified properties.
             * @function create
             * @memberof motors_mirroring.InferenceState.Mirroring
             * @static
             * @param {motors_mirroring.InferenceState.IMirroring=} [properties] Properties to set
             * @returns {motors_mirroring.InferenceState.Mirroring} Mirroring instance
             */
            Mirroring.create = function create(properties) {
                return new Mirroring(properties);
            };

            /**
             * Encodes the specified Mirroring message. Does not implicitly {@link motors_mirroring.InferenceState.Mirroring.verify|verify} messages.
             * @function encode
             * @memberof motors_mirroring.InferenceState.Mirroring
             * @static
             * @param {motors_mirroring.InferenceState.IMirroring} message Mirroring message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Mirroring.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.source != null && Object.hasOwnProperty.call(message, "source"))
                    $root.motors_mirroring.InferenceState.Bus.encode(message.source, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
                if (message.targets != null && message.targets.length)
                    for (let i = 0; i < message.targets.length; ++i)
                        $root.motors_mirroring.InferenceState.Bus.encode(message.targets[i], writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
                return writer;
            };

            /**
             * Encodes the specified Mirroring message, length delimited. Does not implicitly {@link motors_mirroring.InferenceState.Mirroring.verify|verify} messages.
             * @function encodeDelimited
             * @memberof motors_mirroring.InferenceState.Mirroring
             * @static
             * @param {motors_mirroring.InferenceState.IMirroring} message Mirroring message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Mirroring.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a Mirroring message from the specified reader or buffer.
             * @function decode
             * @memberof motors_mirroring.InferenceState.Mirroring
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {motors_mirroring.InferenceState.Mirroring} Mirroring
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Mirroring.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.motors_mirroring.InferenceState.Mirroring();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.source = $root.motors_mirroring.InferenceState.Bus.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 10: {
                            if (!(message.targets && message.targets.length))
                                message.targets = [];
                            message.targets.push($root.motors_mirroring.InferenceState.Bus.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a Mirroring message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof motors_mirroring.InferenceState.Mirroring
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {motors_mirroring.InferenceState.Mirroring} Mirroring
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Mirroring.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Mirroring message.
             * @function verify
             * @memberof motors_mirroring.InferenceState.Mirroring
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Mirroring.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.source != null && message.hasOwnProperty("source")) {
                    let error = $root.motors_mirroring.InferenceState.Bus.verify(message.source, long + 1);
                    if (error)
                        return "source." + error;
                }
                if (message.targets != null && message.hasOwnProperty("targets")) {
                    if (!Array.isArray(message.targets))
                        return "targets: array expected";
                    for (let i = 0; i < message.targets.length; ++i) {
                        let error = $root.motors_mirroring.InferenceState.Bus.verify(message.targets[i], long + 1);
                        if (error)
                            return "targets." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a Mirroring message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof motors_mirroring.InferenceState.Mirroring
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {motors_mirroring.InferenceState.Mirroring} Mirroring
             */
            Mirroring.fromObject = function fromObject(object, long) {
                if (object instanceof $root.motors_mirroring.InferenceState.Mirroring)
                    return object;
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.motors_mirroring.InferenceState.Mirroring();
                if (object.source != null) {
                    if (typeof object.source !== "object")
                        throw TypeError(".motors_mirroring.InferenceState.Mirroring.source: object expected");
                    message.source = $root.motors_mirroring.InferenceState.Bus.fromObject(object.source, long + 1);
                }
                if (object.targets) {
                    if (!Array.isArray(object.targets))
                        throw TypeError(".motors_mirroring.InferenceState.Mirroring.targets: array expected");
                    message.targets = [];
                    for (let i = 0; i < object.targets.length; ++i) {
                        if (typeof object.targets[i] !== "object")
                            throw TypeError(".motors_mirroring.InferenceState.Mirroring.targets: object expected");
                        message.targets[i] = $root.motors_mirroring.InferenceState.Bus.fromObject(object.targets[i], long + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a Mirroring message. Also converts values to other types if specified.
             * @function toObject
             * @memberof motors_mirroring.InferenceState.Mirroring
             * @static
             * @param {motors_mirroring.InferenceState.Mirroring} message Mirroring
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Mirroring.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.targets = [];
                if (options.defaults)
                    object.source = null;
                if (message.source != null && message.hasOwnProperty("source"))
                    object.source = $root.motors_mirroring.InferenceState.Bus.toObject(message.source, options);
                if (message.targets && message.targets.length) {
                    object.targets = [];
                    for (let j = 0; j < message.targets.length; ++j)
                        object.targets[j] = $root.motors_mirroring.InferenceState.Bus.toObject(message.targets[j], options);
                }
                return object;
            };

            /**
             * Converts this Mirroring to JSON.
             * @function toJSON
             * @memberof motors_mirroring.InferenceState.Mirroring
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Mirroring.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for Mirroring
             * @function getTypeUrl
             * @memberof motors_mirroring.InferenceState.Mirroring
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            Mirroring.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/motors_mirroring.InferenceState.Mirroring";
            };

            return Mirroring;
        })();

        return InferenceState;
    })();

    return motors_mirroring;
})();

export const yahboom_dogzilla_lite = $root.yahboom_dogzilla_lite = (() => {

    /**
     * Namespace yahboom_dogzilla_lite.
     * @exports yahboom_dogzilla_lite
     * @namespace
     */
    const yahboom_dogzilla_lite = {};

    /**
     * YahboomDogzillaLiteSignalType enum.
     * @name yahboom_dogzilla_lite.YahboomDogzillaLiteSignalType
     * @enum {number}
     * @property {number} YAHBOOM_DOGZILLA_LITE_SIGNAL_TYPE_UNSPECIFIED=0 YAHBOOM_DOGZILLA_LITE_SIGNAL_TYPE_UNSPECIFIED value
     * @property {number} YAHBOOM_DOGZILLA_LITE_CONNECTED=1 YAHBOOM_DOGZILLA_LITE_CONNECTED value
     * @property {number} YAHBOOM_DOGZILLA_LITE_DISCONNECTED=2 YAHBOOM_DOGZILLA_LITE_DISCONNECTED value
     * @property {number} YAHBOOM_DOGZILLA_LITE_STATUS_UPDATE=3 YAHBOOM_DOGZILLA_LITE_STATUS_UPDATE value
     * @property {number} YAHBOOM_DOGZILLA_LITE_COMMAND=4 YAHBOOM_DOGZILLA_LITE_COMMAND value
     * @property {number} YAHBOOM_DOGZILLA_LITE_COMMAND_SUCCESS=5 YAHBOOM_DOGZILLA_LITE_COMMAND_SUCCESS value
     * @property {number} YAHBOOM_DOGZILLA_LITE_COMMAND_FAILED=6 YAHBOOM_DOGZILLA_LITE_COMMAND_FAILED value
     * @property {number} YAHBOOM_DOGZILLA_LITE_ERROR=7 YAHBOOM_DOGZILLA_LITE_ERROR value
     */
    yahboom_dogzilla_lite.YahboomDogzillaLiteSignalType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "YAHBOOM_DOGZILLA_LITE_SIGNAL_TYPE_UNSPECIFIED"] = 0;
        values[valuesById[1] = "YAHBOOM_DOGZILLA_LITE_CONNECTED"] = 1;
        values[valuesById[2] = "YAHBOOM_DOGZILLA_LITE_DISCONNECTED"] = 2;
        values[valuesById[3] = "YAHBOOM_DOGZILLA_LITE_STATUS_UPDATE"] = 3;
        values[valuesById[4] = "YAHBOOM_DOGZILLA_LITE_COMMAND"] = 4;
        values[valuesById[5] = "YAHBOOM_DOGZILLA_LITE_COMMAND_SUCCESS"] = 5;
        values[valuesById[6] = "YAHBOOM_DOGZILLA_LITE_COMMAND_FAILED"] = 6;
        values[valuesById[7] = "YAHBOOM_DOGZILLA_LITE_ERROR"] = 7;
        return values;
    })();

    /**
     * YahboomDogzillaLiteModel enum.
     * @name yahboom_dogzilla_lite.YahboomDogzillaLiteModel
     * @enum {number}
     * @property {number} YAHBOOM_DOGZILLA_LITE_MODEL_UNKNOWN=0 YAHBOOM_DOGZILLA_LITE_MODEL_UNKNOWN value
     * @property {number} YAHBOOM_DOGZILLA_LITE_MINI=1 YAHBOOM_DOGZILLA_LITE_MINI value
     * @property {number} YAHBOOM_DOGZILLA_LITE=2 YAHBOOM_DOGZILLA_LITE value
     * @property {number} YAHBOOM_DOGZILLA_LITE_PRO=3 YAHBOOM_DOGZILLA_LITE_PRO value
     * @property {number} YAHBOOM_DOGZILLA_LITE_RIDER=4 YAHBOOM_DOGZILLA_LITE_RIDER value
     */
    yahboom_dogzilla_lite.YahboomDogzillaLiteModel = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "YAHBOOM_DOGZILLA_LITE_MODEL_UNKNOWN"] = 0;
        values[valuesById[1] = "YAHBOOM_DOGZILLA_LITE_MINI"] = 1;
        values[valuesById[2] = "YAHBOOM_DOGZILLA_LITE"] = 2;
        values[valuesById[3] = "YAHBOOM_DOGZILLA_LITE_PRO"] = 3;
        values[valuesById[4] = "YAHBOOM_DOGZILLA_LITE_RIDER"] = 4;
        return values;
    })();

    /**
     * GaitType enum.
     * @name yahboom_dogzilla_lite.GaitType
     * @enum {number}
     * @property {number} GAIT_TROT=0 GAIT_TROT value
     * @property {number} GAIT_WALK=1 GAIT_WALK value
     * @property {number} GAIT_HIGH_WALK=2 GAIT_HIGH_WALK value
     * @property {number} GAIT_MICRO_TROT=3 GAIT_MICRO_TROT value
     */
    yahboom_dogzilla_lite.GaitType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "GAIT_TROT"] = 0;
        values[valuesById[1] = "GAIT_WALK"] = 1;
        values[valuesById[2] = "GAIT_HIGH_WALK"] = 2;
        values[valuesById[3] = "GAIT_MICRO_TROT"] = 3;
        return values;
    })();

    /**
     * PerformanceMode enum.
     * @name yahboom_dogzilla_lite.PerformanceMode
     * @enum {number}
     * @property {number} PERFORMANCE_NORMAL_CONTROL=0 PERFORMANCE_NORMAL_CONTROL value
     * @property {number} PERFORMANCE_CYCLE_ACTION=1 PERFORMANCE_CYCLE_ACTION value
     */
    yahboom_dogzilla_lite.PerformanceMode = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "PERFORMANCE_NORMAL_CONTROL"] = 0;
        values[valuesById[1] = "PERFORMANCE_CYCLE_ACTION"] = 1;
        return values;
    })();

    /**
     * ImuMode enum.
     * @name yahboom_dogzilla_lite.ImuMode
     * @enum {number}
     * @property {number} IMU_DISABLED=0 IMU_DISABLED value
     * @property {number} IMU_SELF_STABILIZE=1 IMU_SELF_STABILIZE value
     */
    yahboom_dogzilla_lite.ImuMode = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "IMU_DISABLED"] = 0;
        values[valuesById[1] = "IMU_SELF_STABILIZE"] = 1;
        return values;
    })();

    /**
     * ActionType enum.
     * @name yahboom_dogzilla_lite.ActionType
     * @enum {number}
     * @property {number} ACTION_UNSPECIFIED=0 ACTION_UNSPECIFIED value
     * @property {number} ACTION_LIE_DOWN=1 ACTION_LIE_DOWN value
     * @property {number} ACTION_STAND_UP=2 ACTION_STAND_UP value
     * @property {number} ACTION_CRAWL_FORWARD=3 ACTION_CRAWL_FORWARD value
     * @property {number} ACTION_TURN_AROUND=4 ACTION_TURN_AROUND value
     * @property {number} ACTION_MARCH_IN_PLACE=5 ACTION_MARCH_IN_PLACE value
     * @property {number} ACTION_SQUAT=6 ACTION_SQUAT value
     * @property {number} ACTION_ROLL=7 ACTION_ROLL value
     * @property {number} ACTION_PITCH=8 ACTION_PITCH value
     * @property {number} ACTION_YAW=9 ACTION_YAW value
     * @property {number} ACTION_THREE_AXIS_ROTATION=10 ACTION_THREE_AXIS_ROTATION value
     * @property {number} ACTION_PEE=11 ACTION_PEE value
     * @property {number} ACTION_SIT_DOWN=12 ACTION_SIT_DOWN value
     * @property {number} ACTION_WAVE=13 ACTION_WAVE value
     * @property {number} ACTION_STRETCH=14 ACTION_STRETCH value
     * @property {number} ACTION_WAVE2=15 ACTION_WAVE2 value
     * @property {number} ACTION_SWAY=16 ACTION_SWAY value
     * @property {number} ACTION_BEG_FOR_FOOD=17 ACTION_BEG_FOR_FOOD value
     * @property {number} ACTION_FIND_FOOD=18 ACTION_FIND_FOOD value
     * @property {number} ACTION_HANDSHAKE=19 ACTION_HANDSHAKE value
     * @property {number} ACTION_ARM_DEMO=20 ACTION_ARM_DEMO value
     * @property {number} ACTION_PUSHUPS=21 ACTION_PUSHUPS value
     * @property {number} ACTION_PITCH_YAW_ROTATION=22 ACTION_PITCH_YAW_ROTATION value
     * @property {number} ACTION_UP_DOWN_ROTATION=23 ACTION_UP_DOWN_ROTATION value
     * @property {number} ACTION_FORWARD_BACKWARD_ROTATION=24 ACTION_FORWARD_BACKWARD_ROTATION value
     * @property {number} ACTION_RESTORE_DEFAULT=255 ACTION_RESTORE_DEFAULT value
     */
    yahboom_dogzilla_lite.ActionType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "ACTION_UNSPECIFIED"] = 0;
        values[valuesById[1] = "ACTION_LIE_DOWN"] = 1;
        values[valuesById[2] = "ACTION_STAND_UP"] = 2;
        values[valuesById[3] = "ACTION_CRAWL_FORWARD"] = 3;
        values[valuesById[4] = "ACTION_TURN_AROUND"] = 4;
        values[valuesById[5] = "ACTION_MARCH_IN_PLACE"] = 5;
        values[valuesById[6] = "ACTION_SQUAT"] = 6;
        values[valuesById[7] = "ACTION_ROLL"] = 7;
        values[valuesById[8] = "ACTION_PITCH"] = 8;
        values[valuesById[9] = "ACTION_YAW"] = 9;
        values[valuesById[10] = "ACTION_THREE_AXIS_ROTATION"] = 10;
        values[valuesById[11] = "ACTION_PEE"] = 11;
        values[valuesById[12] = "ACTION_SIT_DOWN"] = 12;
        values[valuesById[13] = "ACTION_WAVE"] = 13;
        values[valuesById[14] = "ACTION_STRETCH"] = 14;
        values[valuesById[15] = "ACTION_WAVE2"] = 15;
        values[valuesById[16] = "ACTION_SWAY"] = 16;
        values[valuesById[17] = "ACTION_BEG_FOR_FOOD"] = 17;
        values[valuesById[18] = "ACTION_FIND_FOOD"] = 18;
        values[valuesById[19] = "ACTION_HANDSHAKE"] = 19;
        values[valuesById[20] = "ACTION_ARM_DEMO"] = 20;
        values[valuesById[21] = "ACTION_PUSHUPS"] = 21;
        values[valuesById[22] = "ACTION_PITCH_YAW_ROTATION"] = 22;
        values[valuesById[23] = "ACTION_UP_DOWN_ROTATION"] = 23;
        values[valuesById[24] = "ACTION_FORWARD_BACKWARD_ROTATION"] = 24;
        values[valuesById[255] = "ACTION_RESTORE_DEFAULT"] = 255;
        return values;
    })();

    yahboom_dogzilla_lite.YahboomDogzillaLiteDevice = (function() {

        /**
         * Properties of a YahboomDogzillaLiteDevice.
         * @memberof yahboom_dogzilla_lite
         * @interface IYahboomDogzillaLiteDevice
         * @property {string|null} [portName] YahboomDogzillaLiteDevice portName
         * @property {number|null} [baudRate] YahboomDogzillaLiteDevice baudRate
         * @property {string|null} [serialNumber] YahboomDogzillaLiteDevice serialNumber
         * @property {string|null} [firmwareVersion] YahboomDogzillaLiteDevice firmwareVersion
         * @property {yahboom_dogzilla_lite.YahboomDogzillaLiteModel|null} [model] YahboomDogzillaLiteDevice model
         * @property {number|null} [vid] YahboomDogzillaLiteDevice vid
         * @property {number|null} [pid] YahboomDogzillaLiteDevice pid
         * @property {string|null} [manufacturer] YahboomDogzillaLiteDevice manufacturer
         * @property {string|null} [product] YahboomDogzillaLiteDevice product
         */

        /**
         * Constructs a new YahboomDogzillaLiteDevice.
         * @memberof yahboom_dogzilla_lite
         * @classdesc Represents a YahboomDogzillaLiteDevice.
         * @implements IYahboomDogzillaLiteDevice
         * @constructor
         * @param {yahboom_dogzilla_lite.IYahboomDogzillaLiteDevice=} [properties] Properties to set
         */
        function YahboomDogzillaLiteDevice(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * YahboomDogzillaLiteDevice portName.
         * @member {string} portName
         * @memberof yahboom_dogzilla_lite.YahboomDogzillaLiteDevice
         * @instance
         */
        YahboomDogzillaLiteDevice.prototype.portName = "";

        /**
         * YahboomDogzillaLiteDevice baudRate.
         * @member {number} baudRate
         * @memberof yahboom_dogzilla_lite.YahboomDogzillaLiteDevice
         * @instance
         */
        YahboomDogzillaLiteDevice.prototype.baudRate = 0;

        /**
         * YahboomDogzillaLiteDevice serialNumber.
         * @member {string} serialNumber
         * @memberof yahboom_dogzilla_lite.YahboomDogzillaLiteDevice
         * @instance
         */
        YahboomDogzillaLiteDevice.prototype.serialNumber = "";

        /**
         * YahboomDogzillaLiteDevice firmwareVersion.
         * @member {string} firmwareVersion
         * @memberof yahboom_dogzilla_lite.YahboomDogzillaLiteDevice
         * @instance
         */
        YahboomDogzillaLiteDevice.prototype.firmwareVersion = "";

        /**
         * YahboomDogzillaLiteDevice model.
         * @member {yahboom_dogzilla_lite.YahboomDogzillaLiteModel} model
         * @memberof yahboom_dogzilla_lite.YahboomDogzillaLiteDevice
         * @instance
         */
        YahboomDogzillaLiteDevice.prototype.model = 0;

        /**
         * YahboomDogzillaLiteDevice vid.
         * @member {number} vid
         * @memberof yahboom_dogzilla_lite.YahboomDogzillaLiteDevice
         * @instance
         */
        YahboomDogzillaLiteDevice.prototype.vid = 0;

        /**
         * YahboomDogzillaLiteDevice pid.
         * @member {number} pid
         * @memberof yahboom_dogzilla_lite.YahboomDogzillaLiteDevice
         * @instance
         */
        YahboomDogzillaLiteDevice.prototype.pid = 0;

        /**
         * YahboomDogzillaLiteDevice manufacturer.
         * @member {string} manufacturer
         * @memberof yahboom_dogzilla_lite.YahboomDogzillaLiteDevice
         * @instance
         */
        YahboomDogzillaLiteDevice.prototype.manufacturer = "";

        /**
         * YahboomDogzillaLiteDevice product.
         * @member {string} product
         * @memberof yahboom_dogzilla_lite.YahboomDogzillaLiteDevice
         * @instance
         */
        YahboomDogzillaLiteDevice.prototype.product = "";

        /**
         * Creates a new YahboomDogzillaLiteDevice instance using the specified properties.
         * @function create
         * @memberof yahboom_dogzilla_lite.YahboomDogzillaLiteDevice
         * @static
         * @param {yahboom_dogzilla_lite.IYahboomDogzillaLiteDevice=} [properties] Properties to set
         * @returns {yahboom_dogzilla_lite.YahboomDogzillaLiteDevice} YahboomDogzillaLiteDevice instance
         */
        YahboomDogzillaLiteDevice.create = function create(properties) {
            return new YahboomDogzillaLiteDevice(properties);
        };

        /**
         * Encodes the specified YahboomDogzillaLiteDevice message. Does not implicitly {@link yahboom_dogzilla_lite.YahboomDogzillaLiteDevice.verify|verify} messages.
         * @function encode
         * @memberof yahboom_dogzilla_lite.YahboomDogzillaLiteDevice
         * @static
         * @param {yahboom_dogzilla_lite.IYahboomDogzillaLiteDevice} message YahboomDogzillaLiteDevice message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        YahboomDogzillaLiteDevice.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.portName != null && Object.hasOwnProperty.call(message, "portName"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.portName);
            if (message.baudRate != null && Object.hasOwnProperty.call(message, "baudRate"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.baudRate);
            if (message.serialNumber != null && Object.hasOwnProperty.call(message, "serialNumber"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.serialNumber);
            if (message.firmwareVersion != null && Object.hasOwnProperty.call(message, "firmwareVersion"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.firmwareVersion);
            if (message.model != null && Object.hasOwnProperty.call(message, "model"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.model);
            if (message.vid != null && Object.hasOwnProperty.call(message, "vid"))
                writer.uint32(/* id 10, wireType 0 =*/80).uint32(message.vid);
            if (message.pid != null && Object.hasOwnProperty.call(message, "pid"))
                writer.uint32(/* id 11, wireType 0 =*/88).uint32(message.pid);
            if (message.manufacturer != null && Object.hasOwnProperty.call(message, "manufacturer"))
                writer.uint32(/* id 12, wireType 2 =*/98).string(message.manufacturer);
            if (message.product != null && Object.hasOwnProperty.call(message, "product"))
                writer.uint32(/* id 13, wireType 2 =*/106).string(message.product);
            return writer;
        };

        /**
         * Encodes the specified YahboomDogzillaLiteDevice message, length delimited. Does not implicitly {@link yahboom_dogzilla_lite.YahboomDogzillaLiteDevice.verify|verify} messages.
         * @function encodeDelimited
         * @memberof yahboom_dogzilla_lite.YahboomDogzillaLiteDevice
         * @static
         * @param {yahboom_dogzilla_lite.IYahboomDogzillaLiteDevice} message YahboomDogzillaLiteDevice message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        YahboomDogzillaLiteDevice.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a YahboomDogzillaLiteDevice message from the specified reader or buffer.
         * @function decode
         * @memberof yahboom_dogzilla_lite.YahboomDogzillaLiteDevice
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {yahboom_dogzilla_lite.YahboomDogzillaLiteDevice} YahboomDogzillaLiteDevice
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        YahboomDogzillaLiteDevice.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.yahboom_dogzilla_lite.YahboomDogzillaLiteDevice();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.portName = reader.string();
                        break;
                    }
                case 2: {
                        message.baudRate = reader.uint32();
                        break;
                    }
                case 3: {
                        message.serialNumber = reader.string();
                        break;
                    }
                case 4: {
                        message.firmwareVersion = reader.string();
                        break;
                    }
                case 5: {
                        message.model = reader.int32();
                        break;
                    }
                case 10: {
                        message.vid = reader.uint32();
                        break;
                    }
                case 11: {
                        message.pid = reader.uint32();
                        break;
                    }
                case 12: {
                        message.manufacturer = reader.string();
                        break;
                    }
                case 13: {
                        message.product = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a YahboomDogzillaLiteDevice message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof yahboom_dogzilla_lite.YahboomDogzillaLiteDevice
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {yahboom_dogzilla_lite.YahboomDogzillaLiteDevice} YahboomDogzillaLiteDevice
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        YahboomDogzillaLiteDevice.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a YahboomDogzillaLiteDevice message.
         * @function verify
         * @memberof yahboom_dogzilla_lite.YahboomDogzillaLiteDevice
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        YahboomDogzillaLiteDevice.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.portName != null && message.hasOwnProperty("portName"))
                if (!$util.isString(message.portName))
                    return "portName: string expected";
            if (message.baudRate != null && message.hasOwnProperty("baudRate"))
                if (!$util.isInteger(message.baudRate))
                    return "baudRate: integer expected";
            if (message.serialNumber != null && message.hasOwnProperty("serialNumber"))
                if (!$util.isString(message.serialNumber))
                    return "serialNumber: string expected";
            if (message.firmwareVersion != null && message.hasOwnProperty("firmwareVersion"))
                if (!$util.isString(message.firmwareVersion))
                    return "firmwareVersion: string expected";
            if (message.model != null && message.hasOwnProperty("model"))
                switch (message.model) {
                default:
                    return "model: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                    break;
                }
            if (message.vid != null && message.hasOwnProperty("vid"))
                if (!$util.isInteger(message.vid))
                    return "vid: integer expected";
            if (message.pid != null && message.hasOwnProperty("pid"))
                if (!$util.isInteger(message.pid))
                    return "pid: integer expected";
            if (message.manufacturer != null && message.hasOwnProperty("manufacturer"))
                if (!$util.isString(message.manufacturer))
                    return "manufacturer: string expected";
            if (message.product != null && message.hasOwnProperty("product"))
                if (!$util.isString(message.product))
                    return "product: string expected";
            return null;
        };

        /**
         * Creates a YahboomDogzillaLiteDevice message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof yahboom_dogzilla_lite.YahboomDogzillaLiteDevice
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {yahboom_dogzilla_lite.YahboomDogzillaLiteDevice} YahboomDogzillaLiteDevice
         */
        YahboomDogzillaLiteDevice.fromObject = function fromObject(object, long) {
            if (object instanceof $root.yahboom_dogzilla_lite.YahboomDogzillaLiteDevice)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.yahboom_dogzilla_lite.YahboomDogzillaLiteDevice();
            if (object.portName != null)
                message.portName = String(object.portName);
            if (object.baudRate != null)
                message.baudRate = object.baudRate >>> 0;
            if (object.serialNumber != null)
                message.serialNumber = String(object.serialNumber);
            if (object.firmwareVersion != null)
                message.firmwareVersion = String(object.firmwareVersion);
            switch (object.model) {
            default:
                if (typeof object.model === "number") {
                    message.model = object.model;
                    break;
                }
                break;
            case "YAHBOOM_DOGZILLA_LITE_MODEL_UNKNOWN":
            case 0:
                message.model = 0;
                break;
            case "YAHBOOM_DOGZILLA_LITE_MINI":
            case 1:
                message.model = 1;
                break;
            case "YAHBOOM_DOGZILLA_LITE":
            case 2:
                message.model = 2;
                break;
            case "YAHBOOM_DOGZILLA_LITE_PRO":
            case 3:
                message.model = 3;
                break;
            case "YAHBOOM_DOGZILLA_LITE_RIDER":
            case 4:
                message.model = 4;
                break;
            }
            if (object.vid != null)
                message.vid = object.vid >>> 0;
            if (object.pid != null)
                message.pid = object.pid >>> 0;
            if (object.manufacturer != null)
                message.manufacturer = String(object.manufacturer);
            if (object.product != null)
                message.product = String(object.product);
            return message;
        };

        /**
         * Creates a plain object from a YahboomDogzillaLiteDevice message. Also converts values to other types if specified.
         * @function toObject
         * @memberof yahboom_dogzilla_lite.YahboomDogzillaLiteDevice
         * @static
         * @param {yahboom_dogzilla_lite.YahboomDogzillaLiteDevice} message YahboomDogzillaLiteDevice
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        YahboomDogzillaLiteDevice.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.portName = "";
                object.baudRate = 0;
                object.serialNumber = "";
                object.firmwareVersion = "";
                object.model = options.enums === String ? "YAHBOOM_DOGZILLA_LITE_MODEL_UNKNOWN" : 0;
                object.vid = 0;
                object.pid = 0;
                object.manufacturer = "";
                object.product = "";
            }
            if (message.portName != null && message.hasOwnProperty("portName"))
                object.portName = message.portName;
            if (message.baudRate != null && message.hasOwnProperty("baudRate"))
                object.baudRate = message.baudRate;
            if (message.serialNumber != null && message.hasOwnProperty("serialNumber"))
                object.serialNumber = message.serialNumber;
            if (message.firmwareVersion != null && message.hasOwnProperty("firmwareVersion"))
                object.firmwareVersion = message.firmwareVersion;
            if (message.model != null && message.hasOwnProperty("model"))
                object.model = options.enums === String ? $root.yahboom_dogzilla_lite.YahboomDogzillaLiteModel[message.model] === undefined ? message.model : $root.yahboom_dogzilla_lite.YahboomDogzillaLiteModel[message.model] : message.model;
            if (message.vid != null && message.hasOwnProperty("vid"))
                object.vid = message.vid;
            if (message.pid != null && message.hasOwnProperty("pid"))
                object.pid = message.pid;
            if (message.manufacturer != null && message.hasOwnProperty("manufacturer"))
                object.manufacturer = message.manufacturer;
            if (message.product != null && message.hasOwnProperty("product"))
                object.product = message.product;
            return object;
        };

        /**
         * Converts this YahboomDogzillaLiteDevice to JSON.
         * @function toJSON
         * @memberof yahboom_dogzilla_lite.YahboomDogzillaLiteDevice
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        YahboomDogzillaLiteDevice.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for YahboomDogzillaLiteDevice
         * @function getTypeUrl
         * @memberof yahboom_dogzilla_lite.YahboomDogzillaLiteDevice
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        YahboomDogzillaLiteDevice.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/yahboom_dogzilla_lite.YahboomDogzillaLiteDevice";
        };

        return YahboomDogzillaLiteDevice;
    })();

    yahboom_dogzilla_lite.ImuOrientation = (function() {

        /**
         * Properties of an ImuOrientation.
         * @memberof yahboom_dogzilla_lite
         * @interface IImuOrientation
         * @property {number|null} [roll] ImuOrientation roll
         * @property {number|null} [pitch] ImuOrientation pitch
         * @property {number|null} [yaw] ImuOrientation yaw
         */

        /**
         * Constructs a new ImuOrientation.
         * @memberof yahboom_dogzilla_lite
         * @classdesc Represents an ImuOrientation.
         * @implements IImuOrientation
         * @constructor
         * @param {yahboom_dogzilla_lite.IImuOrientation=} [properties] Properties to set
         */
        function ImuOrientation(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ImuOrientation roll.
         * @member {number} roll
         * @memberof yahboom_dogzilla_lite.ImuOrientation
         * @instance
         */
        ImuOrientation.prototype.roll = 0;

        /**
         * ImuOrientation pitch.
         * @member {number} pitch
         * @memberof yahboom_dogzilla_lite.ImuOrientation
         * @instance
         */
        ImuOrientation.prototype.pitch = 0;

        /**
         * ImuOrientation yaw.
         * @member {number} yaw
         * @memberof yahboom_dogzilla_lite.ImuOrientation
         * @instance
         */
        ImuOrientation.prototype.yaw = 0;

        /**
         * Creates a new ImuOrientation instance using the specified properties.
         * @function create
         * @memberof yahboom_dogzilla_lite.ImuOrientation
         * @static
         * @param {yahboom_dogzilla_lite.IImuOrientation=} [properties] Properties to set
         * @returns {yahboom_dogzilla_lite.ImuOrientation} ImuOrientation instance
         */
        ImuOrientation.create = function create(properties) {
            return new ImuOrientation(properties);
        };

        /**
         * Encodes the specified ImuOrientation message. Does not implicitly {@link yahboom_dogzilla_lite.ImuOrientation.verify|verify} messages.
         * @function encode
         * @memberof yahboom_dogzilla_lite.ImuOrientation
         * @static
         * @param {yahboom_dogzilla_lite.IImuOrientation} message ImuOrientation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ImuOrientation.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.roll != null && Object.hasOwnProperty.call(message, "roll"))
                writer.uint32(/* id 1, wireType 5 =*/13).float(message.roll);
            if (message.pitch != null && Object.hasOwnProperty.call(message, "pitch"))
                writer.uint32(/* id 2, wireType 5 =*/21).float(message.pitch);
            if (message.yaw != null && Object.hasOwnProperty.call(message, "yaw"))
                writer.uint32(/* id 3, wireType 5 =*/29).float(message.yaw);
            return writer;
        };

        /**
         * Encodes the specified ImuOrientation message, length delimited. Does not implicitly {@link yahboom_dogzilla_lite.ImuOrientation.verify|verify} messages.
         * @function encodeDelimited
         * @memberof yahboom_dogzilla_lite.ImuOrientation
         * @static
         * @param {yahboom_dogzilla_lite.IImuOrientation} message ImuOrientation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ImuOrientation.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an ImuOrientation message from the specified reader or buffer.
         * @function decode
         * @memberof yahboom_dogzilla_lite.ImuOrientation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {yahboom_dogzilla_lite.ImuOrientation} ImuOrientation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ImuOrientation.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.yahboom_dogzilla_lite.ImuOrientation();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.roll = reader.float();
                        break;
                    }
                case 2: {
                        message.pitch = reader.float();
                        break;
                    }
                case 3: {
                        message.yaw = reader.float();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an ImuOrientation message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof yahboom_dogzilla_lite.ImuOrientation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {yahboom_dogzilla_lite.ImuOrientation} ImuOrientation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ImuOrientation.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an ImuOrientation message.
         * @function verify
         * @memberof yahboom_dogzilla_lite.ImuOrientation
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ImuOrientation.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.roll != null && message.hasOwnProperty("roll"))
                if (typeof message.roll !== "number")
                    return "roll: number expected";
            if (message.pitch != null && message.hasOwnProperty("pitch"))
                if (typeof message.pitch !== "number")
                    return "pitch: number expected";
            if (message.yaw != null && message.hasOwnProperty("yaw"))
                if (typeof message.yaw !== "number")
                    return "yaw: number expected";
            return null;
        };

        /**
         * Creates an ImuOrientation message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof yahboom_dogzilla_lite.ImuOrientation
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {yahboom_dogzilla_lite.ImuOrientation} ImuOrientation
         */
        ImuOrientation.fromObject = function fromObject(object, long) {
            if (object instanceof $root.yahboom_dogzilla_lite.ImuOrientation)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.yahboom_dogzilla_lite.ImuOrientation();
            if (object.roll != null)
                message.roll = Number(object.roll);
            if (object.pitch != null)
                message.pitch = Number(object.pitch);
            if (object.yaw != null)
                message.yaw = Number(object.yaw);
            return message;
        };

        /**
         * Creates a plain object from an ImuOrientation message. Also converts values to other types if specified.
         * @function toObject
         * @memberof yahboom_dogzilla_lite.ImuOrientation
         * @static
         * @param {yahboom_dogzilla_lite.ImuOrientation} message ImuOrientation
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ImuOrientation.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.roll = 0;
                object.pitch = 0;
                object.yaw = 0;
            }
            if (message.roll != null && message.hasOwnProperty("roll"))
                object.roll = options.json && !isFinite(message.roll) ? String(message.roll) : message.roll;
            if (message.pitch != null && message.hasOwnProperty("pitch"))
                object.pitch = options.json && !isFinite(message.pitch) ? String(message.pitch) : message.pitch;
            if (message.yaw != null && message.hasOwnProperty("yaw"))
                object.yaw = options.json && !isFinite(message.yaw) ? String(message.yaw) : message.yaw;
            return object;
        };

        /**
         * Converts this ImuOrientation to JSON.
         * @function toJSON
         * @memberof yahboom_dogzilla_lite.ImuOrientation
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ImuOrientation.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ImuOrientation
         * @function getTypeUrl
         * @memberof yahboom_dogzilla_lite.ImuOrientation
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ImuOrientation.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/yahboom_dogzilla_lite.ImuOrientation";
        };

        return ImuOrientation;
    })();

    yahboom_dogzilla_lite.Acceleration = (function() {

        /**
         * Properties of an Acceleration.
         * @memberof yahboom_dogzilla_lite
         * @interface IAcceleration
         * @property {number|null} [x] Acceleration x
         * @property {number|null} [y] Acceleration y
         * @property {number|null} [z] Acceleration z
         */

        /**
         * Constructs a new Acceleration.
         * @memberof yahboom_dogzilla_lite
         * @classdesc Represents an Acceleration.
         * @implements IAcceleration
         * @constructor
         * @param {yahboom_dogzilla_lite.IAcceleration=} [properties] Properties to set
         */
        function Acceleration(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Acceleration x.
         * @member {number} x
         * @memberof yahboom_dogzilla_lite.Acceleration
         * @instance
         */
        Acceleration.prototype.x = 0;

        /**
         * Acceleration y.
         * @member {number} y
         * @memberof yahboom_dogzilla_lite.Acceleration
         * @instance
         */
        Acceleration.prototype.y = 0;

        /**
         * Acceleration z.
         * @member {number} z
         * @memberof yahboom_dogzilla_lite.Acceleration
         * @instance
         */
        Acceleration.prototype.z = 0;

        /**
         * Creates a new Acceleration instance using the specified properties.
         * @function create
         * @memberof yahboom_dogzilla_lite.Acceleration
         * @static
         * @param {yahboom_dogzilla_lite.IAcceleration=} [properties] Properties to set
         * @returns {yahboom_dogzilla_lite.Acceleration} Acceleration instance
         */
        Acceleration.create = function create(properties) {
            return new Acceleration(properties);
        };

        /**
         * Encodes the specified Acceleration message. Does not implicitly {@link yahboom_dogzilla_lite.Acceleration.verify|verify} messages.
         * @function encode
         * @memberof yahboom_dogzilla_lite.Acceleration
         * @static
         * @param {yahboom_dogzilla_lite.IAcceleration} message Acceleration message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Acceleration.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.x != null && Object.hasOwnProperty.call(message, "x"))
                writer.uint32(/* id 1, wireType 5 =*/13).float(message.x);
            if (message.y != null && Object.hasOwnProperty.call(message, "y"))
                writer.uint32(/* id 2, wireType 5 =*/21).float(message.y);
            if (message.z != null && Object.hasOwnProperty.call(message, "z"))
                writer.uint32(/* id 3, wireType 5 =*/29).float(message.z);
            return writer;
        };

        /**
         * Encodes the specified Acceleration message, length delimited. Does not implicitly {@link yahboom_dogzilla_lite.Acceleration.verify|verify} messages.
         * @function encodeDelimited
         * @memberof yahboom_dogzilla_lite.Acceleration
         * @static
         * @param {yahboom_dogzilla_lite.IAcceleration} message Acceleration message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Acceleration.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an Acceleration message from the specified reader or buffer.
         * @function decode
         * @memberof yahboom_dogzilla_lite.Acceleration
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {yahboom_dogzilla_lite.Acceleration} Acceleration
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Acceleration.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.yahboom_dogzilla_lite.Acceleration();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.x = reader.float();
                        break;
                    }
                case 2: {
                        message.y = reader.float();
                        break;
                    }
                case 3: {
                        message.z = reader.float();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an Acceleration message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof yahboom_dogzilla_lite.Acceleration
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {yahboom_dogzilla_lite.Acceleration} Acceleration
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Acceleration.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an Acceleration message.
         * @function verify
         * @memberof yahboom_dogzilla_lite.Acceleration
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Acceleration.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.x != null && message.hasOwnProperty("x"))
                if (typeof message.x !== "number")
                    return "x: number expected";
            if (message.y != null && message.hasOwnProperty("y"))
                if (typeof message.y !== "number")
                    return "y: number expected";
            if (message.z != null && message.hasOwnProperty("z"))
                if (typeof message.z !== "number")
                    return "z: number expected";
            return null;
        };

        /**
         * Creates an Acceleration message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof yahboom_dogzilla_lite.Acceleration
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {yahboom_dogzilla_lite.Acceleration} Acceleration
         */
        Acceleration.fromObject = function fromObject(object, long) {
            if (object instanceof $root.yahboom_dogzilla_lite.Acceleration)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.yahboom_dogzilla_lite.Acceleration();
            if (object.x != null)
                message.x = Number(object.x);
            if (object.y != null)
                message.y = Number(object.y);
            if (object.z != null)
                message.z = Number(object.z);
            return message;
        };

        /**
         * Creates a plain object from an Acceleration message. Also converts values to other types if specified.
         * @function toObject
         * @memberof yahboom_dogzilla_lite.Acceleration
         * @static
         * @param {yahboom_dogzilla_lite.Acceleration} message Acceleration
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Acceleration.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.x = 0;
                object.y = 0;
                object.z = 0;
            }
            if (message.x != null && message.hasOwnProperty("x"))
                object.x = options.json && !isFinite(message.x) ? String(message.x) : message.x;
            if (message.y != null && message.hasOwnProperty("y"))
                object.y = options.json && !isFinite(message.y) ? String(message.y) : message.y;
            if (message.z != null && message.hasOwnProperty("z"))
                object.z = options.json && !isFinite(message.z) ? String(message.z) : message.z;
            return object;
        };

        /**
         * Converts this Acceleration to JSON.
         * @function toJSON
         * @memberof yahboom_dogzilla_lite.Acceleration
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Acceleration.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Acceleration
         * @function getTypeUrl
         * @memberof yahboom_dogzilla_lite.Acceleration
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Acceleration.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/yahboom_dogzilla_lite.Acceleration";
        };

        return Acceleration;
    })();

    yahboom_dogzilla_lite.YahboomDogzillaLiteStatus = (function() {

        /**
         * Properties of a YahboomDogzillaLiteStatus.
         * @memberof yahboom_dogzilla_lite
         * @interface IYahboomDogzillaLiteStatus
         * @property {number|null} [batteryLevel] YahboomDogzillaLiteStatus batteryLevel
         * @property {yahboom_dogzilla_lite.YahboomDogzillaLiteModel|null} [model] YahboomDogzillaLiteStatus model
         * @property {string|null} [firmwareVersion] YahboomDogzillaLiteStatus firmwareVersion
         * @property {Array.<number>|null} [servoPositions] YahboomDogzillaLiteStatus servoPositions
         * @property {yahboom_dogzilla_lite.IImuOrientation|null} [orientation] YahboomDogzillaLiteStatus orientation
         * @property {yahboom_dogzilla_lite.IAcceleration|null} [acceleration] YahboomDogzillaLiteStatus acceleration
         * @property {number|null} [legServoSpeed] YahboomDogzillaLiteStatus legServoSpeed
         * @property {number|null} [armServoSpeed] YahboomDogzillaLiteStatus armServoSpeed
         * @property {Array.<number>|null} [servoAngles] YahboomDogzillaLiteStatus servoAngles
         */

        /**
         * Constructs a new YahboomDogzillaLiteStatus.
         * @memberof yahboom_dogzilla_lite
         * @classdesc Represents a YahboomDogzillaLiteStatus.
         * @implements IYahboomDogzillaLiteStatus
         * @constructor
         * @param {yahboom_dogzilla_lite.IYahboomDogzillaLiteStatus=} [properties] Properties to set
         */
        function YahboomDogzillaLiteStatus(properties) {
            this.servoPositions = [];
            this.servoAngles = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * YahboomDogzillaLiteStatus batteryLevel.
         * @member {number} batteryLevel
         * @memberof yahboom_dogzilla_lite.YahboomDogzillaLiteStatus
         * @instance
         */
        YahboomDogzillaLiteStatus.prototype.batteryLevel = 0;

        /**
         * YahboomDogzillaLiteStatus model.
         * @member {yahboom_dogzilla_lite.YahboomDogzillaLiteModel} model
         * @memberof yahboom_dogzilla_lite.YahboomDogzillaLiteStatus
         * @instance
         */
        YahboomDogzillaLiteStatus.prototype.model = 0;

        /**
         * YahboomDogzillaLiteStatus firmwareVersion.
         * @member {string} firmwareVersion
         * @memberof yahboom_dogzilla_lite.YahboomDogzillaLiteStatus
         * @instance
         */
        YahboomDogzillaLiteStatus.prototype.firmwareVersion = "";

        /**
         * YahboomDogzillaLiteStatus servoPositions.
         * @member {Array.<number>} servoPositions
         * @memberof yahboom_dogzilla_lite.YahboomDogzillaLiteStatus
         * @instance
         */
        YahboomDogzillaLiteStatus.prototype.servoPositions = $util.emptyArray;

        /**
         * YahboomDogzillaLiteStatus orientation.
         * @member {yahboom_dogzilla_lite.IImuOrientation|null|undefined} orientation
         * @memberof yahboom_dogzilla_lite.YahboomDogzillaLiteStatus
         * @instance
         */
        YahboomDogzillaLiteStatus.prototype.orientation = null;

        /**
         * YahboomDogzillaLiteStatus acceleration.
         * @member {yahboom_dogzilla_lite.IAcceleration|null|undefined} acceleration
         * @memberof yahboom_dogzilla_lite.YahboomDogzillaLiteStatus
         * @instance
         */
        YahboomDogzillaLiteStatus.prototype.acceleration = null;

        /**
         * YahboomDogzillaLiteStatus legServoSpeed.
         * @member {number} legServoSpeed
         * @memberof yahboom_dogzilla_lite.YahboomDogzillaLiteStatus
         * @instance
         */
        YahboomDogzillaLiteStatus.prototype.legServoSpeed = 0;

        /**
         * YahboomDogzillaLiteStatus armServoSpeed.
         * @member {number} armServoSpeed
         * @memberof yahboom_dogzilla_lite.YahboomDogzillaLiteStatus
         * @instance
         */
        YahboomDogzillaLiteStatus.prototype.armServoSpeed = 0;

        /**
         * YahboomDogzillaLiteStatus servoAngles.
         * @member {Array.<number>} servoAngles
         * @memberof yahboom_dogzilla_lite.YahboomDogzillaLiteStatus
         * @instance
         */
        YahboomDogzillaLiteStatus.prototype.servoAngles = $util.emptyArray;

        /**
         * Creates a new YahboomDogzillaLiteStatus instance using the specified properties.
         * @function create
         * @memberof yahboom_dogzilla_lite.YahboomDogzillaLiteStatus
         * @static
         * @param {yahboom_dogzilla_lite.IYahboomDogzillaLiteStatus=} [properties] Properties to set
         * @returns {yahboom_dogzilla_lite.YahboomDogzillaLiteStatus} YahboomDogzillaLiteStatus instance
         */
        YahboomDogzillaLiteStatus.create = function create(properties) {
            return new YahboomDogzillaLiteStatus(properties);
        };

        /**
         * Encodes the specified YahboomDogzillaLiteStatus message. Does not implicitly {@link yahboom_dogzilla_lite.YahboomDogzillaLiteStatus.verify|verify} messages.
         * @function encode
         * @memberof yahboom_dogzilla_lite.YahboomDogzillaLiteStatus
         * @static
         * @param {yahboom_dogzilla_lite.IYahboomDogzillaLiteStatus} message YahboomDogzillaLiteStatus message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        YahboomDogzillaLiteStatus.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.batteryLevel != null && Object.hasOwnProperty.call(message, "batteryLevel"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.batteryLevel);
            if (message.model != null && Object.hasOwnProperty.call(message, "model"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.model);
            if (message.firmwareVersion != null && Object.hasOwnProperty.call(message, "firmwareVersion"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.firmwareVersion);
            if (message.servoPositions != null && message.servoPositions.length) {
                writer.uint32(/* id 10, wireType 2 =*/82).fork();
                for (let i = 0; i < message.servoPositions.length; ++i)
                    writer.uint32(message.servoPositions[i]);
                writer.ldelim();
            }
            if (message.orientation != null && Object.hasOwnProperty.call(message, "orientation"))
                $root.yahboom_dogzilla_lite.ImuOrientation.encode(message.orientation, writer.uint32(/* id 11, wireType 2 =*/90).fork()).ldelim();
            if (message.acceleration != null && Object.hasOwnProperty.call(message, "acceleration"))
                $root.yahboom_dogzilla_lite.Acceleration.encode(message.acceleration, writer.uint32(/* id 12, wireType 2 =*/98).fork()).ldelim();
            if (message.legServoSpeed != null && Object.hasOwnProperty.call(message, "legServoSpeed"))
                writer.uint32(/* id 13, wireType 0 =*/104).uint32(message.legServoSpeed);
            if (message.armServoSpeed != null && Object.hasOwnProperty.call(message, "armServoSpeed"))
                writer.uint32(/* id 14, wireType 0 =*/112).uint32(message.armServoSpeed);
            if (message.servoAngles != null && message.servoAngles.length) {
                writer.uint32(/* id 15, wireType 2 =*/122).fork();
                for (let i = 0; i < message.servoAngles.length; ++i)
                    writer.float(message.servoAngles[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified YahboomDogzillaLiteStatus message, length delimited. Does not implicitly {@link yahboom_dogzilla_lite.YahboomDogzillaLiteStatus.verify|verify} messages.
         * @function encodeDelimited
         * @memberof yahboom_dogzilla_lite.YahboomDogzillaLiteStatus
         * @static
         * @param {yahboom_dogzilla_lite.IYahboomDogzillaLiteStatus} message YahboomDogzillaLiteStatus message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        YahboomDogzillaLiteStatus.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a YahboomDogzillaLiteStatus message from the specified reader or buffer.
         * @function decode
         * @memberof yahboom_dogzilla_lite.YahboomDogzillaLiteStatus
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {yahboom_dogzilla_lite.YahboomDogzillaLiteStatus} YahboomDogzillaLiteStatus
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        YahboomDogzillaLiteStatus.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.yahboom_dogzilla_lite.YahboomDogzillaLiteStatus();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.batteryLevel = reader.uint32();
                        break;
                    }
                case 2: {
                        message.model = reader.int32();
                        break;
                    }
                case 3: {
                        message.firmwareVersion = reader.string();
                        break;
                    }
                case 10: {
                        if (!(message.servoPositions && message.servoPositions.length))
                            message.servoPositions = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.servoPositions.push(reader.uint32());
                        } else
                            message.servoPositions.push(reader.uint32());
                        break;
                    }
                case 11: {
                        message.orientation = $root.yahboom_dogzilla_lite.ImuOrientation.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 12: {
                        message.acceleration = $root.yahboom_dogzilla_lite.Acceleration.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 13: {
                        message.legServoSpeed = reader.uint32();
                        break;
                    }
                case 14: {
                        message.armServoSpeed = reader.uint32();
                        break;
                    }
                case 15: {
                        if (!(message.servoAngles && message.servoAngles.length))
                            message.servoAngles = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.servoAngles.push(reader.float());
                        } else
                            message.servoAngles.push(reader.float());
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a YahboomDogzillaLiteStatus message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof yahboom_dogzilla_lite.YahboomDogzillaLiteStatus
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {yahboom_dogzilla_lite.YahboomDogzillaLiteStatus} YahboomDogzillaLiteStatus
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        YahboomDogzillaLiteStatus.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a YahboomDogzillaLiteStatus message.
         * @function verify
         * @memberof yahboom_dogzilla_lite.YahboomDogzillaLiteStatus
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        YahboomDogzillaLiteStatus.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.batteryLevel != null && message.hasOwnProperty("batteryLevel"))
                if (!$util.isInteger(message.batteryLevel))
                    return "batteryLevel: integer expected";
            if (message.model != null && message.hasOwnProperty("model"))
                switch (message.model) {
                default:
                    return "model: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                    break;
                }
            if (message.firmwareVersion != null && message.hasOwnProperty("firmwareVersion"))
                if (!$util.isString(message.firmwareVersion))
                    return "firmwareVersion: string expected";
            if (message.servoPositions != null && message.hasOwnProperty("servoPositions")) {
                if (!Array.isArray(message.servoPositions))
                    return "servoPositions: array expected";
                for (let i = 0; i < message.servoPositions.length; ++i)
                    if (!$util.isInteger(message.servoPositions[i]))
                        return "servoPositions: integer[] expected";
            }
            if (message.orientation != null && message.hasOwnProperty("orientation")) {
                let error = $root.yahboom_dogzilla_lite.ImuOrientation.verify(message.orientation, long + 1);
                if (error)
                    return "orientation." + error;
            }
            if (message.acceleration != null && message.hasOwnProperty("acceleration")) {
                let error = $root.yahboom_dogzilla_lite.Acceleration.verify(message.acceleration, long + 1);
                if (error)
                    return "acceleration." + error;
            }
            if (message.legServoSpeed != null && message.hasOwnProperty("legServoSpeed"))
                if (!$util.isInteger(message.legServoSpeed))
                    return "legServoSpeed: integer expected";
            if (message.armServoSpeed != null && message.hasOwnProperty("armServoSpeed"))
                if (!$util.isInteger(message.armServoSpeed))
                    return "armServoSpeed: integer expected";
            if (message.servoAngles != null && message.hasOwnProperty("servoAngles")) {
                if (!Array.isArray(message.servoAngles))
                    return "servoAngles: array expected";
                for (let i = 0; i < message.servoAngles.length; ++i)
                    if (typeof message.servoAngles[i] !== "number")
                        return "servoAngles: number[] expected";
            }
            return null;
        };

        /**
         * Creates a YahboomDogzillaLiteStatus message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof yahboom_dogzilla_lite.YahboomDogzillaLiteStatus
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {yahboom_dogzilla_lite.YahboomDogzillaLiteStatus} YahboomDogzillaLiteStatus
         */
        YahboomDogzillaLiteStatus.fromObject = function fromObject(object, long) {
            if (object instanceof $root.yahboom_dogzilla_lite.YahboomDogzillaLiteStatus)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.yahboom_dogzilla_lite.YahboomDogzillaLiteStatus();
            if (object.batteryLevel != null)
                message.batteryLevel = object.batteryLevel >>> 0;
            switch (object.model) {
            default:
                if (typeof object.model === "number") {
                    message.model = object.model;
                    break;
                }
                break;
            case "YAHBOOM_DOGZILLA_LITE_MODEL_UNKNOWN":
            case 0:
                message.model = 0;
                break;
            case "YAHBOOM_DOGZILLA_LITE_MINI":
            case 1:
                message.model = 1;
                break;
            case "YAHBOOM_DOGZILLA_LITE":
            case 2:
                message.model = 2;
                break;
            case "YAHBOOM_DOGZILLA_LITE_PRO":
            case 3:
                message.model = 3;
                break;
            case "YAHBOOM_DOGZILLA_LITE_RIDER":
            case 4:
                message.model = 4;
                break;
            }
            if (object.firmwareVersion != null)
                message.firmwareVersion = String(object.firmwareVersion);
            if (object.servoPositions) {
                if (!Array.isArray(object.servoPositions))
                    throw TypeError(".yahboom_dogzilla_lite.YahboomDogzillaLiteStatus.servoPositions: array expected");
                message.servoPositions = [];
                for (let i = 0; i < object.servoPositions.length; ++i)
                    message.servoPositions[i] = object.servoPositions[i] >>> 0;
            }
            if (object.orientation != null) {
                if (typeof object.orientation !== "object")
                    throw TypeError(".yahboom_dogzilla_lite.YahboomDogzillaLiteStatus.orientation: object expected");
                message.orientation = $root.yahboom_dogzilla_lite.ImuOrientation.fromObject(object.orientation, long + 1);
            }
            if (object.acceleration != null) {
                if (typeof object.acceleration !== "object")
                    throw TypeError(".yahboom_dogzilla_lite.YahboomDogzillaLiteStatus.acceleration: object expected");
                message.acceleration = $root.yahboom_dogzilla_lite.Acceleration.fromObject(object.acceleration, long + 1);
            }
            if (object.legServoSpeed != null)
                message.legServoSpeed = object.legServoSpeed >>> 0;
            if (object.armServoSpeed != null)
                message.armServoSpeed = object.armServoSpeed >>> 0;
            if (object.servoAngles) {
                if (!Array.isArray(object.servoAngles))
                    throw TypeError(".yahboom_dogzilla_lite.YahboomDogzillaLiteStatus.servoAngles: array expected");
                message.servoAngles = [];
                for (let i = 0; i < object.servoAngles.length; ++i)
                    message.servoAngles[i] = Number(object.servoAngles[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a YahboomDogzillaLiteStatus message. Also converts values to other types if specified.
         * @function toObject
         * @memberof yahboom_dogzilla_lite.YahboomDogzillaLiteStatus
         * @static
         * @param {yahboom_dogzilla_lite.YahboomDogzillaLiteStatus} message YahboomDogzillaLiteStatus
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        YahboomDogzillaLiteStatus.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.servoPositions = [];
                object.servoAngles = [];
            }
            if (options.defaults) {
                object.batteryLevel = 0;
                object.model = options.enums === String ? "YAHBOOM_DOGZILLA_LITE_MODEL_UNKNOWN" : 0;
                object.firmwareVersion = "";
                object.orientation = null;
                object.acceleration = null;
                object.legServoSpeed = 0;
                object.armServoSpeed = 0;
            }
            if (message.batteryLevel != null && message.hasOwnProperty("batteryLevel"))
                object.batteryLevel = message.batteryLevel;
            if (message.model != null && message.hasOwnProperty("model"))
                object.model = options.enums === String ? $root.yahboom_dogzilla_lite.YahboomDogzillaLiteModel[message.model] === undefined ? message.model : $root.yahboom_dogzilla_lite.YahboomDogzillaLiteModel[message.model] : message.model;
            if (message.firmwareVersion != null && message.hasOwnProperty("firmwareVersion"))
                object.firmwareVersion = message.firmwareVersion;
            if (message.servoPositions && message.servoPositions.length) {
                object.servoPositions = [];
                for (let j = 0; j < message.servoPositions.length; ++j)
                    object.servoPositions[j] = message.servoPositions[j];
            }
            if (message.orientation != null && message.hasOwnProperty("orientation"))
                object.orientation = $root.yahboom_dogzilla_lite.ImuOrientation.toObject(message.orientation, options);
            if (message.acceleration != null && message.hasOwnProperty("acceleration"))
                object.acceleration = $root.yahboom_dogzilla_lite.Acceleration.toObject(message.acceleration, options);
            if (message.legServoSpeed != null && message.hasOwnProperty("legServoSpeed"))
                object.legServoSpeed = message.legServoSpeed;
            if (message.armServoSpeed != null && message.hasOwnProperty("armServoSpeed"))
                object.armServoSpeed = message.armServoSpeed;
            if (message.servoAngles && message.servoAngles.length) {
                object.servoAngles = [];
                for (let j = 0; j < message.servoAngles.length; ++j)
                    object.servoAngles[j] = options.json && !isFinite(message.servoAngles[j]) ? String(message.servoAngles[j]) : message.servoAngles[j];
            }
            return object;
        };

        /**
         * Converts this YahboomDogzillaLiteStatus to JSON.
         * @function toJSON
         * @memberof yahboom_dogzilla_lite.YahboomDogzillaLiteStatus
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        YahboomDogzillaLiteStatus.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for YahboomDogzillaLiteStatus
         * @function getTypeUrl
         * @memberof yahboom_dogzilla_lite.YahboomDogzillaLiteStatus
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        YahboomDogzillaLiteStatus.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/yahboom_dogzilla_lite.YahboomDogzillaLiteStatus";
        };

        return YahboomDogzillaLiteStatus;
    })();

    yahboom_dogzilla_lite.ServoCommand = (function() {

        /**
         * Properties of a ServoCommand.
         * @memberof yahboom_dogzilla_lite
         * @interface IServoCommand
         * @property {number|null} [servoId] ServoCommand servoId
         * @property {number|null} [position] ServoCommand position
         */

        /**
         * Constructs a new ServoCommand.
         * @memberof yahboom_dogzilla_lite
         * @classdesc Represents a ServoCommand.
         * @implements IServoCommand
         * @constructor
         * @param {yahboom_dogzilla_lite.IServoCommand=} [properties] Properties to set
         */
        function ServoCommand(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ServoCommand servoId.
         * @member {number} servoId
         * @memberof yahboom_dogzilla_lite.ServoCommand
         * @instance
         */
        ServoCommand.prototype.servoId = 0;

        /**
         * ServoCommand position.
         * @member {number} position
         * @memberof yahboom_dogzilla_lite.ServoCommand
         * @instance
         */
        ServoCommand.prototype.position = 0;

        /**
         * Creates a new ServoCommand instance using the specified properties.
         * @function create
         * @memberof yahboom_dogzilla_lite.ServoCommand
         * @static
         * @param {yahboom_dogzilla_lite.IServoCommand=} [properties] Properties to set
         * @returns {yahboom_dogzilla_lite.ServoCommand} ServoCommand instance
         */
        ServoCommand.create = function create(properties) {
            return new ServoCommand(properties);
        };

        /**
         * Encodes the specified ServoCommand message. Does not implicitly {@link yahboom_dogzilla_lite.ServoCommand.verify|verify} messages.
         * @function encode
         * @memberof yahboom_dogzilla_lite.ServoCommand
         * @static
         * @param {yahboom_dogzilla_lite.IServoCommand} message ServoCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ServoCommand.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.servoId != null && Object.hasOwnProperty.call(message, "servoId"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.servoId);
            if (message.position != null && Object.hasOwnProperty.call(message, "position"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.position);
            return writer;
        };

        /**
         * Encodes the specified ServoCommand message, length delimited. Does not implicitly {@link yahboom_dogzilla_lite.ServoCommand.verify|verify} messages.
         * @function encodeDelimited
         * @memberof yahboom_dogzilla_lite.ServoCommand
         * @static
         * @param {yahboom_dogzilla_lite.IServoCommand} message ServoCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ServoCommand.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ServoCommand message from the specified reader or buffer.
         * @function decode
         * @memberof yahboom_dogzilla_lite.ServoCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {yahboom_dogzilla_lite.ServoCommand} ServoCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ServoCommand.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.yahboom_dogzilla_lite.ServoCommand();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.servoId = reader.uint32();
                        break;
                    }
                case 2: {
                        message.position = reader.uint32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ServoCommand message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof yahboom_dogzilla_lite.ServoCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {yahboom_dogzilla_lite.ServoCommand} ServoCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ServoCommand.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ServoCommand message.
         * @function verify
         * @memberof yahboom_dogzilla_lite.ServoCommand
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ServoCommand.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.servoId != null && message.hasOwnProperty("servoId"))
                if (!$util.isInteger(message.servoId))
                    return "servoId: integer expected";
            if (message.position != null && message.hasOwnProperty("position"))
                if (!$util.isInteger(message.position))
                    return "position: integer expected";
            return null;
        };

        /**
         * Creates a ServoCommand message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof yahboom_dogzilla_lite.ServoCommand
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {yahboom_dogzilla_lite.ServoCommand} ServoCommand
         */
        ServoCommand.fromObject = function fromObject(object, long) {
            if (object instanceof $root.yahboom_dogzilla_lite.ServoCommand)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.yahboom_dogzilla_lite.ServoCommand();
            if (object.servoId != null)
                message.servoId = object.servoId >>> 0;
            if (object.position != null)
                message.position = object.position >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a ServoCommand message. Also converts values to other types if specified.
         * @function toObject
         * @memberof yahboom_dogzilla_lite.ServoCommand
         * @static
         * @param {yahboom_dogzilla_lite.ServoCommand} message ServoCommand
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ServoCommand.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.servoId = 0;
                object.position = 0;
            }
            if (message.servoId != null && message.hasOwnProperty("servoId"))
                object.servoId = message.servoId;
            if (message.position != null && message.hasOwnProperty("position"))
                object.position = message.position;
            return object;
        };

        /**
         * Converts this ServoCommand to JSON.
         * @function toJSON
         * @memberof yahboom_dogzilla_lite.ServoCommand
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ServoCommand.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ServoCommand
         * @function getTypeUrl
         * @memberof yahboom_dogzilla_lite.ServoCommand
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ServoCommand.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/yahboom_dogzilla_lite.ServoCommand";
        };

        return ServoCommand;
    })();

    yahboom_dogzilla_lite.ServoSpeedCommand = (function() {

        /**
         * Properties of a ServoSpeedCommand.
         * @memberof yahboom_dogzilla_lite
         * @interface IServoSpeedCommand
         * @property {number|null} [bodyServoSpeed] ServoSpeedCommand bodyServoSpeed
         * @property {number|null} [armServoSpeed] ServoSpeedCommand armServoSpeed
         */

        /**
         * Constructs a new ServoSpeedCommand.
         * @memberof yahboom_dogzilla_lite
         * @classdesc Represents a ServoSpeedCommand.
         * @implements IServoSpeedCommand
         * @constructor
         * @param {yahboom_dogzilla_lite.IServoSpeedCommand=} [properties] Properties to set
         */
        function ServoSpeedCommand(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ServoSpeedCommand bodyServoSpeed.
         * @member {number|null|undefined} bodyServoSpeed
         * @memberof yahboom_dogzilla_lite.ServoSpeedCommand
         * @instance
         */
        ServoSpeedCommand.prototype.bodyServoSpeed = null;

        /**
         * ServoSpeedCommand armServoSpeed.
         * @member {number|null|undefined} armServoSpeed
         * @memberof yahboom_dogzilla_lite.ServoSpeedCommand
         * @instance
         */
        ServoSpeedCommand.prototype.armServoSpeed = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * ServoSpeedCommand bodySpeed.
         * @member {"bodyServoSpeed"|undefined} bodySpeed
         * @memberof yahboom_dogzilla_lite.ServoSpeedCommand
         * @instance
         */
        Object.defineProperty(ServoSpeedCommand.prototype, "bodySpeed", {
            get: $util.oneOfGetter($oneOfFields = ["bodyServoSpeed"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * ServoSpeedCommand armSpeed.
         * @member {"armServoSpeed"|undefined} armSpeed
         * @memberof yahboom_dogzilla_lite.ServoSpeedCommand
         * @instance
         */
        Object.defineProperty(ServoSpeedCommand.prototype, "armSpeed", {
            get: $util.oneOfGetter($oneOfFields = ["armServoSpeed"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new ServoSpeedCommand instance using the specified properties.
         * @function create
         * @memberof yahboom_dogzilla_lite.ServoSpeedCommand
         * @static
         * @param {yahboom_dogzilla_lite.IServoSpeedCommand=} [properties] Properties to set
         * @returns {yahboom_dogzilla_lite.ServoSpeedCommand} ServoSpeedCommand instance
         */
        ServoSpeedCommand.create = function create(properties) {
            return new ServoSpeedCommand(properties);
        };

        /**
         * Encodes the specified ServoSpeedCommand message. Does not implicitly {@link yahboom_dogzilla_lite.ServoSpeedCommand.verify|verify} messages.
         * @function encode
         * @memberof yahboom_dogzilla_lite.ServoSpeedCommand
         * @static
         * @param {yahboom_dogzilla_lite.IServoSpeedCommand} message ServoSpeedCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ServoSpeedCommand.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.bodyServoSpeed != null && Object.hasOwnProperty.call(message, "bodyServoSpeed"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.bodyServoSpeed);
            if (message.armServoSpeed != null && Object.hasOwnProperty.call(message, "armServoSpeed"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.armServoSpeed);
            return writer;
        };

        /**
         * Encodes the specified ServoSpeedCommand message, length delimited. Does not implicitly {@link yahboom_dogzilla_lite.ServoSpeedCommand.verify|verify} messages.
         * @function encodeDelimited
         * @memberof yahboom_dogzilla_lite.ServoSpeedCommand
         * @static
         * @param {yahboom_dogzilla_lite.IServoSpeedCommand} message ServoSpeedCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ServoSpeedCommand.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ServoSpeedCommand message from the specified reader or buffer.
         * @function decode
         * @memberof yahboom_dogzilla_lite.ServoSpeedCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {yahboom_dogzilla_lite.ServoSpeedCommand} ServoSpeedCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ServoSpeedCommand.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.yahboom_dogzilla_lite.ServoSpeedCommand();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.bodyServoSpeed = reader.uint32();
                        break;
                    }
                case 2: {
                        message.armServoSpeed = reader.uint32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ServoSpeedCommand message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof yahboom_dogzilla_lite.ServoSpeedCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {yahboom_dogzilla_lite.ServoSpeedCommand} ServoSpeedCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ServoSpeedCommand.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ServoSpeedCommand message.
         * @function verify
         * @memberof yahboom_dogzilla_lite.ServoSpeedCommand
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ServoSpeedCommand.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            let properties = {};
            if (message.bodyServoSpeed != null && message.hasOwnProperty("bodyServoSpeed")) {
                properties.bodySpeed = 1;
                if (!$util.isInteger(message.bodyServoSpeed))
                    return "bodyServoSpeed: integer expected";
            }
            if (message.armServoSpeed != null && message.hasOwnProperty("armServoSpeed")) {
                properties.armSpeed = 1;
                if (!$util.isInteger(message.armServoSpeed))
                    return "armServoSpeed: integer expected";
            }
            return null;
        };

        /**
         * Creates a ServoSpeedCommand message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof yahboom_dogzilla_lite.ServoSpeedCommand
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {yahboom_dogzilla_lite.ServoSpeedCommand} ServoSpeedCommand
         */
        ServoSpeedCommand.fromObject = function fromObject(object, long) {
            if (object instanceof $root.yahboom_dogzilla_lite.ServoSpeedCommand)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.yahboom_dogzilla_lite.ServoSpeedCommand();
            if (object.bodyServoSpeed != null)
                message.bodyServoSpeed = object.bodyServoSpeed >>> 0;
            if (object.armServoSpeed != null)
                message.armServoSpeed = object.armServoSpeed >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a ServoSpeedCommand message. Also converts values to other types if specified.
         * @function toObject
         * @memberof yahboom_dogzilla_lite.ServoSpeedCommand
         * @static
         * @param {yahboom_dogzilla_lite.ServoSpeedCommand} message ServoSpeedCommand
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ServoSpeedCommand.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (message.bodyServoSpeed != null && message.hasOwnProperty("bodyServoSpeed")) {
                object.bodyServoSpeed = message.bodyServoSpeed;
                if (options.oneofs)
                    object.bodySpeed = "bodyServoSpeed";
            }
            if (message.armServoSpeed != null && message.hasOwnProperty("armServoSpeed")) {
                object.armServoSpeed = message.armServoSpeed;
                if (options.oneofs)
                    object.armSpeed = "armServoSpeed";
            }
            return object;
        };

        /**
         * Converts this ServoSpeedCommand to JSON.
         * @function toJSON
         * @memberof yahboom_dogzilla_lite.ServoSpeedCommand
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ServoSpeedCommand.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ServoSpeedCommand
         * @function getTypeUrl
         * @memberof yahboom_dogzilla_lite.ServoSpeedCommand
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ServoSpeedCommand.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/yahboom_dogzilla_lite.ServoSpeedCommand";
        };

        return ServoSpeedCommand;
    })();

    yahboom_dogzilla_lite.CalibrationCommand = (function() {

        /**
         * Properties of a CalibrationCommand.
         * @memberof yahboom_dogzilla_lite
         * @interface ICalibrationCommand
         * @property {boolean|null} [enterCalibrationMode] CalibrationCommand enterCalibrationMode
         * @property {boolean|null} [setOrigin] CalibrationCommand setOrigin
         * @property {boolean|null} [servoCentering] CalibrationCommand servoCentering
         * @property {boolean|null} [gyroCalibration] CalibrationCommand gyroCalibration
         */

        /**
         * Constructs a new CalibrationCommand.
         * @memberof yahboom_dogzilla_lite
         * @classdesc Represents a CalibrationCommand.
         * @implements ICalibrationCommand
         * @constructor
         * @param {yahboom_dogzilla_lite.ICalibrationCommand=} [properties] Properties to set
         */
        function CalibrationCommand(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CalibrationCommand enterCalibrationMode.
         * @member {boolean} enterCalibrationMode
         * @memberof yahboom_dogzilla_lite.CalibrationCommand
         * @instance
         */
        CalibrationCommand.prototype.enterCalibrationMode = false;

        /**
         * CalibrationCommand setOrigin.
         * @member {boolean} setOrigin
         * @memberof yahboom_dogzilla_lite.CalibrationCommand
         * @instance
         */
        CalibrationCommand.prototype.setOrigin = false;

        /**
         * CalibrationCommand servoCentering.
         * @member {boolean} servoCentering
         * @memberof yahboom_dogzilla_lite.CalibrationCommand
         * @instance
         */
        CalibrationCommand.prototype.servoCentering = false;

        /**
         * CalibrationCommand gyroCalibration.
         * @member {boolean} gyroCalibration
         * @memberof yahboom_dogzilla_lite.CalibrationCommand
         * @instance
         */
        CalibrationCommand.prototype.gyroCalibration = false;

        /**
         * Creates a new CalibrationCommand instance using the specified properties.
         * @function create
         * @memberof yahboom_dogzilla_lite.CalibrationCommand
         * @static
         * @param {yahboom_dogzilla_lite.ICalibrationCommand=} [properties] Properties to set
         * @returns {yahboom_dogzilla_lite.CalibrationCommand} CalibrationCommand instance
         */
        CalibrationCommand.create = function create(properties) {
            return new CalibrationCommand(properties);
        };

        /**
         * Encodes the specified CalibrationCommand message. Does not implicitly {@link yahboom_dogzilla_lite.CalibrationCommand.verify|verify} messages.
         * @function encode
         * @memberof yahboom_dogzilla_lite.CalibrationCommand
         * @static
         * @param {yahboom_dogzilla_lite.ICalibrationCommand} message CalibrationCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CalibrationCommand.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.enterCalibrationMode != null && Object.hasOwnProperty.call(message, "enterCalibrationMode"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.enterCalibrationMode);
            if (message.setOrigin != null && Object.hasOwnProperty.call(message, "setOrigin"))
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.setOrigin);
            if (message.servoCentering != null && Object.hasOwnProperty.call(message, "servoCentering"))
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.servoCentering);
            if (message.gyroCalibration != null && Object.hasOwnProperty.call(message, "gyroCalibration"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.gyroCalibration);
            return writer;
        };

        /**
         * Encodes the specified CalibrationCommand message, length delimited. Does not implicitly {@link yahboom_dogzilla_lite.CalibrationCommand.verify|verify} messages.
         * @function encodeDelimited
         * @memberof yahboom_dogzilla_lite.CalibrationCommand
         * @static
         * @param {yahboom_dogzilla_lite.ICalibrationCommand} message CalibrationCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CalibrationCommand.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CalibrationCommand message from the specified reader or buffer.
         * @function decode
         * @memberof yahboom_dogzilla_lite.CalibrationCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {yahboom_dogzilla_lite.CalibrationCommand} CalibrationCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CalibrationCommand.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.yahboom_dogzilla_lite.CalibrationCommand();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.enterCalibrationMode = reader.bool();
                        break;
                    }
                case 2: {
                        message.setOrigin = reader.bool();
                        break;
                    }
                case 3: {
                        message.servoCentering = reader.bool();
                        break;
                    }
                case 4: {
                        message.gyroCalibration = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CalibrationCommand message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof yahboom_dogzilla_lite.CalibrationCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {yahboom_dogzilla_lite.CalibrationCommand} CalibrationCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CalibrationCommand.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CalibrationCommand message.
         * @function verify
         * @memberof yahboom_dogzilla_lite.CalibrationCommand
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CalibrationCommand.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.enterCalibrationMode != null && message.hasOwnProperty("enterCalibrationMode"))
                if (typeof message.enterCalibrationMode !== "boolean")
                    return "enterCalibrationMode: boolean expected";
            if (message.setOrigin != null && message.hasOwnProperty("setOrigin"))
                if (typeof message.setOrigin !== "boolean")
                    return "setOrigin: boolean expected";
            if (message.servoCentering != null && message.hasOwnProperty("servoCentering"))
                if (typeof message.servoCentering !== "boolean")
                    return "servoCentering: boolean expected";
            if (message.gyroCalibration != null && message.hasOwnProperty("gyroCalibration"))
                if (typeof message.gyroCalibration !== "boolean")
                    return "gyroCalibration: boolean expected";
            return null;
        };

        /**
         * Creates a CalibrationCommand message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof yahboom_dogzilla_lite.CalibrationCommand
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {yahboom_dogzilla_lite.CalibrationCommand} CalibrationCommand
         */
        CalibrationCommand.fromObject = function fromObject(object, long) {
            if (object instanceof $root.yahboom_dogzilla_lite.CalibrationCommand)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.yahboom_dogzilla_lite.CalibrationCommand();
            if (object.enterCalibrationMode != null)
                message.enterCalibrationMode = Boolean(object.enterCalibrationMode);
            if (object.setOrigin != null)
                message.setOrigin = Boolean(object.setOrigin);
            if (object.servoCentering != null)
                message.servoCentering = Boolean(object.servoCentering);
            if (object.gyroCalibration != null)
                message.gyroCalibration = Boolean(object.gyroCalibration);
            return message;
        };

        /**
         * Creates a plain object from a CalibrationCommand message. Also converts values to other types if specified.
         * @function toObject
         * @memberof yahboom_dogzilla_lite.CalibrationCommand
         * @static
         * @param {yahboom_dogzilla_lite.CalibrationCommand} message CalibrationCommand
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CalibrationCommand.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.enterCalibrationMode = false;
                object.setOrigin = false;
                object.servoCentering = false;
                object.gyroCalibration = false;
            }
            if (message.enterCalibrationMode != null && message.hasOwnProperty("enterCalibrationMode"))
                object.enterCalibrationMode = message.enterCalibrationMode;
            if (message.setOrigin != null && message.hasOwnProperty("setOrigin"))
                object.setOrigin = message.setOrigin;
            if (message.servoCentering != null && message.hasOwnProperty("servoCentering"))
                object.servoCentering = message.servoCentering;
            if (message.gyroCalibration != null && message.hasOwnProperty("gyroCalibration"))
                object.gyroCalibration = message.gyroCalibration;
            return object;
        };

        /**
         * Converts this CalibrationCommand to JSON.
         * @function toJSON
         * @memberof yahboom_dogzilla_lite.CalibrationCommand
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CalibrationCommand.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CalibrationCommand
         * @function getTypeUrl
         * @memberof yahboom_dogzilla_lite.CalibrationCommand
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CalibrationCommand.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/yahboom_dogzilla_lite.CalibrationCommand";
        };

        return CalibrationCommand;
    })();

    yahboom_dogzilla_lite.ArmCommand = (function() {

        /**
         * Properties of an ArmCommand.
         * @memberof yahboom_dogzilla_lite
         * @interface IArmCommand
         * @property {number|null} [gripperStatus] ArmCommand gripperStatus
         * @property {number|null} [gripperX] ArmCommand gripperX
         * @property {number|null} [gripperZ] ArmCommand gripperZ
         * @property {number|null} [polarAngle] ArmCommand polarAngle
         * @property {number|null} [polarRadius] ArmCommand polarRadius
         * @property {boolean|null} [stabilityMode] ArmCommand stabilityMode
         */

        /**
         * Constructs a new ArmCommand.
         * @memberof yahboom_dogzilla_lite
         * @classdesc Represents an ArmCommand.
         * @implements IArmCommand
         * @constructor
         * @param {yahboom_dogzilla_lite.IArmCommand=} [properties] Properties to set
         */
        function ArmCommand(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ArmCommand gripperStatus.
         * @member {number} gripperStatus
         * @memberof yahboom_dogzilla_lite.ArmCommand
         * @instance
         */
        ArmCommand.prototype.gripperStatus = 0;

        /**
         * ArmCommand gripperX.
         * @member {number} gripperX
         * @memberof yahboom_dogzilla_lite.ArmCommand
         * @instance
         */
        ArmCommand.prototype.gripperX = 0;

        /**
         * ArmCommand gripperZ.
         * @member {number} gripperZ
         * @memberof yahboom_dogzilla_lite.ArmCommand
         * @instance
         */
        ArmCommand.prototype.gripperZ = 0;

        /**
         * ArmCommand polarAngle.
         * @member {number} polarAngle
         * @memberof yahboom_dogzilla_lite.ArmCommand
         * @instance
         */
        ArmCommand.prototype.polarAngle = 0;

        /**
         * ArmCommand polarRadius.
         * @member {number} polarRadius
         * @memberof yahboom_dogzilla_lite.ArmCommand
         * @instance
         */
        ArmCommand.prototype.polarRadius = 0;

        /**
         * ArmCommand stabilityMode.
         * @member {boolean} stabilityMode
         * @memberof yahboom_dogzilla_lite.ArmCommand
         * @instance
         */
        ArmCommand.prototype.stabilityMode = false;

        /**
         * Creates a new ArmCommand instance using the specified properties.
         * @function create
         * @memberof yahboom_dogzilla_lite.ArmCommand
         * @static
         * @param {yahboom_dogzilla_lite.IArmCommand=} [properties] Properties to set
         * @returns {yahboom_dogzilla_lite.ArmCommand} ArmCommand instance
         */
        ArmCommand.create = function create(properties) {
            return new ArmCommand(properties);
        };

        /**
         * Encodes the specified ArmCommand message. Does not implicitly {@link yahboom_dogzilla_lite.ArmCommand.verify|verify} messages.
         * @function encode
         * @memberof yahboom_dogzilla_lite.ArmCommand
         * @static
         * @param {yahboom_dogzilla_lite.IArmCommand} message ArmCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ArmCommand.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.gripperStatus != null && Object.hasOwnProperty.call(message, "gripperStatus"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.gripperStatus);
            if (message.gripperX != null && Object.hasOwnProperty.call(message, "gripperX"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.gripperX);
            if (message.gripperZ != null && Object.hasOwnProperty.call(message, "gripperZ"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.gripperZ);
            if (message.polarAngle != null && Object.hasOwnProperty.call(message, "polarAngle"))
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.polarAngle);
            if (message.polarRadius != null && Object.hasOwnProperty.call(message, "polarRadius"))
                writer.uint32(/* id 5, wireType 0 =*/40).uint32(message.polarRadius);
            if (message.stabilityMode != null && Object.hasOwnProperty.call(message, "stabilityMode"))
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.stabilityMode);
            return writer;
        };

        /**
         * Encodes the specified ArmCommand message, length delimited. Does not implicitly {@link yahboom_dogzilla_lite.ArmCommand.verify|verify} messages.
         * @function encodeDelimited
         * @memberof yahboom_dogzilla_lite.ArmCommand
         * @static
         * @param {yahboom_dogzilla_lite.IArmCommand} message ArmCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ArmCommand.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an ArmCommand message from the specified reader or buffer.
         * @function decode
         * @memberof yahboom_dogzilla_lite.ArmCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {yahboom_dogzilla_lite.ArmCommand} ArmCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ArmCommand.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.yahboom_dogzilla_lite.ArmCommand();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.gripperStatus = reader.uint32();
                        break;
                    }
                case 2: {
                        message.gripperX = reader.uint32();
                        break;
                    }
                case 3: {
                        message.gripperZ = reader.uint32();
                        break;
                    }
                case 4: {
                        message.polarAngle = reader.uint32();
                        break;
                    }
                case 5: {
                        message.polarRadius = reader.uint32();
                        break;
                    }
                case 6: {
                        message.stabilityMode = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an ArmCommand message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof yahboom_dogzilla_lite.ArmCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {yahboom_dogzilla_lite.ArmCommand} ArmCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ArmCommand.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an ArmCommand message.
         * @function verify
         * @memberof yahboom_dogzilla_lite.ArmCommand
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ArmCommand.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.gripperStatus != null && message.hasOwnProperty("gripperStatus"))
                if (!$util.isInteger(message.gripperStatus))
                    return "gripperStatus: integer expected";
            if (message.gripperX != null && message.hasOwnProperty("gripperX"))
                if (!$util.isInteger(message.gripperX))
                    return "gripperX: integer expected";
            if (message.gripperZ != null && message.hasOwnProperty("gripperZ"))
                if (!$util.isInteger(message.gripperZ))
                    return "gripperZ: integer expected";
            if (message.polarAngle != null && message.hasOwnProperty("polarAngle"))
                if (!$util.isInteger(message.polarAngle))
                    return "polarAngle: integer expected";
            if (message.polarRadius != null && message.hasOwnProperty("polarRadius"))
                if (!$util.isInteger(message.polarRadius))
                    return "polarRadius: integer expected";
            if (message.stabilityMode != null && message.hasOwnProperty("stabilityMode"))
                if (typeof message.stabilityMode !== "boolean")
                    return "stabilityMode: boolean expected";
            return null;
        };

        /**
         * Creates an ArmCommand message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof yahboom_dogzilla_lite.ArmCommand
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {yahboom_dogzilla_lite.ArmCommand} ArmCommand
         */
        ArmCommand.fromObject = function fromObject(object, long) {
            if (object instanceof $root.yahboom_dogzilla_lite.ArmCommand)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.yahboom_dogzilla_lite.ArmCommand();
            if (object.gripperStatus != null)
                message.gripperStatus = object.gripperStatus >>> 0;
            if (object.gripperX != null)
                message.gripperX = object.gripperX >>> 0;
            if (object.gripperZ != null)
                message.gripperZ = object.gripperZ >>> 0;
            if (object.polarAngle != null)
                message.polarAngle = object.polarAngle >>> 0;
            if (object.polarRadius != null)
                message.polarRadius = object.polarRadius >>> 0;
            if (object.stabilityMode != null)
                message.stabilityMode = Boolean(object.stabilityMode);
            return message;
        };

        /**
         * Creates a plain object from an ArmCommand message. Also converts values to other types if specified.
         * @function toObject
         * @memberof yahboom_dogzilla_lite.ArmCommand
         * @static
         * @param {yahboom_dogzilla_lite.ArmCommand} message ArmCommand
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ArmCommand.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.gripperStatus = 0;
                object.gripperX = 0;
                object.gripperZ = 0;
                object.polarAngle = 0;
                object.polarRadius = 0;
                object.stabilityMode = false;
            }
            if (message.gripperStatus != null && message.hasOwnProperty("gripperStatus"))
                object.gripperStatus = message.gripperStatus;
            if (message.gripperX != null && message.hasOwnProperty("gripperX"))
                object.gripperX = message.gripperX;
            if (message.gripperZ != null && message.hasOwnProperty("gripperZ"))
                object.gripperZ = message.gripperZ;
            if (message.polarAngle != null && message.hasOwnProperty("polarAngle"))
                object.polarAngle = message.polarAngle;
            if (message.polarRadius != null && message.hasOwnProperty("polarRadius"))
                object.polarRadius = message.polarRadius;
            if (message.stabilityMode != null && message.hasOwnProperty("stabilityMode"))
                object.stabilityMode = message.stabilityMode;
            return object;
        };

        /**
         * Converts this ArmCommand to JSON.
         * @function toJSON
         * @memberof yahboom_dogzilla_lite.ArmCommand
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ArmCommand.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ArmCommand
         * @function getTypeUrl
         * @memberof yahboom_dogzilla_lite.ArmCommand
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ArmCommand.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/yahboom_dogzilla_lite.ArmCommand";
        };

        return ArmCommand;
    })();

    yahboom_dogzilla_lite.IoCommand = (function() {

        /**
         * Properties of an IoCommand.
         * @memberof yahboom_dogzilla_lite
         * @interface IIoCommand
         * @property {boolean|null} [power_5vOutput] IoCommand power_5vOutput
         * @property {boolean|null} [digitalIo] IoCommand digitalIo
         */

        /**
         * Constructs a new IoCommand.
         * @memberof yahboom_dogzilla_lite
         * @classdesc Represents an IoCommand.
         * @implements IIoCommand
         * @constructor
         * @param {yahboom_dogzilla_lite.IIoCommand=} [properties] Properties to set
         */
        function IoCommand(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * IoCommand power_5vOutput.
         * @member {boolean} power_5vOutput
         * @memberof yahboom_dogzilla_lite.IoCommand
         * @instance
         */
        IoCommand.prototype.power_5vOutput = false;

        /**
         * IoCommand digitalIo.
         * @member {boolean} digitalIo
         * @memberof yahboom_dogzilla_lite.IoCommand
         * @instance
         */
        IoCommand.prototype.digitalIo = false;

        /**
         * Creates a new IoCommand instance using the specified properties.
         * @function create
         * @memberof yahboom_dogzilla_lite.IoCommand
         * @static
         * @param {yahboom_dogzilla_lite.IIoCommand=} [properties] Properties to set
         * @returns {yahboom_dogzilla_lite.IoCommand} IoCommand instance
         */
        IoCommand.create = function create(properties) {
            return new IoCommand(properties);
        };

        /**
         * Encodes the specified IoCommand message. Does not implicitly {@link yahboom_dogzilla_lite.IoCommand.verify|verify} messages.
         * @function encode
         * @memberof yahboom_dogzilla_lite.IoCommand
         * @static
         * @param {yahboom_dogzilla_lite.IIoCommand} message IoCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        IoCommand.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.power_5vOutput != null && Object.hasOwnProperty.call(message, "power_5vOutput"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.power_5vOutput);
            if (message.digitalIo != null && Object.hasOwnProperty.call(message, "digitalIo"))
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.digitalIo);
            return writer;
        };

        /**
         * Encodes the specified IoCommand message, length delimited. Does not implicitly {@link yahboom_dogzilla_lite.IoCommand.verify|verify} messages.
         * @function encodeDelimited
         * @memberof yahboom_dogzilla_lite.IoCommand
         * @static
         * @param {yahboom_dogzilla_lite.IIoCommand} message IoCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        IoCommand.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an IoCommand message from the specified reader or buffer.
         * @function decode
         * @memberof yahboom_dogzilla_lite.IoCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {yahboom_dogzilla_lite.IoCommand} IoCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        IoCommand.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.yahboom_dogzilla_lite.IoCommand();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.power_5vOutput = reader.bool();
                        break;
                    }
                case 2: {
                        message.digitalIo = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an IoCommand message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof yahboom_dogzilla_lite.IoCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {yahboom_dogzilla_lite.IoCommand} IoCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        IoCommand.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an IoCommand message.
         * @function verify
         * @memberof yahboom_dogzilla_lite.IoCommand
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        IoCommand.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.power_5vOutput != null && message.hasOwnProperty("power_5vOutput"))
                if (typeof message.power_5vOutput !== "boolean")
                    return "power_5vOutput: boolean expected";
            if (message.digitalIo != null && message.hasOwnProperty("digitalIo"))
                if (typeof message.digitalIo !== "boolean")
                    return "digitalIo: boolean expected";
            return null;
        };

        /**
         * Creates an IoCommand message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof yahboom_dogzilla_lite.IoCommand
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {yahboom_dogzilla_lite.IoCommand} IoCommand
         */
        IoCommand.fromObject = function fromObject(object, long) {
            if (object instanceof $root.yahboom_dogzilla_lite.IoCommand)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.yahboom_dogzilla_lite.IoCommand();
            if (object.power_5vOutput != null)
                message.power_5vOutput = Boolean(object.power_5vOutput);
            if (object.digitalIo != null)
                message.digitalIo = Boolean(object.digitalIo);
            return message;
        };

        /**
         * Creates a plain object from an IoCommand message. Also converts values to other types if specified.
         * @function toObject
         * @memberof yahboom_dogzilla_lite.IoCommand
         * @static
         * @param {yahboom_dogzilla_lite.IoCommand} message IoCommand
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        IoCommand.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.power_5vOutput = false;
                object.digitalIo = false;
            }
            if (message.power_5vOutput != null && message.hasOwnProperty("power_5vOutput"))
                object.power_5vOutput = message.power_5vOutput;
            if (message.digitalIo != null && message.hasOwnProperty("digitalIo"))
                object.digitalIo = message.digitalIo;
            return object;
        };

        /**
         * Converts this IoCommand to JSON.
         * @function toJSON
         * @memberof yahboom_dogzilla_lite.IoCommand
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        IoCommand.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for IoCommand
         * @function getTypeUrl
         * @memberof yahboom_dogzilla_lite.IoCommand
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        IoCommand.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/yahboom_dogzilla_lite.IoCommand";
        };

        return IoCommand;
    })();

    yahboom_dogzilla_lite.LedCommand = (function() {

        /**
         * Properties of a LedCommand.
         * @memberof yahboom_dogzilla_lite
         * @interface ILedCommand
         * @property {number|null} [ledIndex] LedCommand ledIndex
         * @property {Uint8Array|null} [rgbBytes] LedCommand rgbBytes
         */

        /**
         * Constructs a new LedCommand.
         * @memberof yahboom_dogzilla_lite
         * @classdesc Represents a LedCommand.
         * @implements ILedCommand
         * @constructor
         * @param {yahboom_dogzilla_lite.ILedCommand=} [properties] Properties to set
         */
        function LedCommand(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * LedCommand ledIndex.
         * @member {number} ledIndex
         * @memberof yahboom_dogzilla_lite.LedCommand
         * @instance
         */
        LedCommand.prototype.ledIndex = 0;

        /**
         * LedCommand rgbBytes.
         * @member {Uint8Array} rgbBytes
         * @memberof yahboom_dogzilla_lite.LedCommand
         * @instance
         */
        LedCommand.prototype.rgbBytes = $util.newBuffer([]);

        /**
         * Creates a new LedCommand instance using the specified properties.
         * @function create
         * @memberof yahboom_dogzilla_lite.LedCommand
         * @static
         * @param {yahboom_dogzilla_lite.ILedCommand=} [properties] Properties to set
         * @returns {yahboom_dogzilla_lite.LedCommand} LedCommand instance
         */
        LedCommand.create = function create(properties) {
            return new LedCommand(properties);
        };

        /**
         * Encodes the specified LedCommand message. Does not implicitly {@link yahboom_dogzilla_lite.LedCommand.verify|verify} messages.
         * @function encode
         * @memberof yahboom_dogzilla_lite.LedCommand
         * @static
         * @param {yahboom_dogzilla_lite.ILedCommand} message LedCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LedCommand.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ledIndex != null && Object.hasOwnProperty.call(message, "ledIndex"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.ledIndex);
            if (message.rgbBytes != null && Object.hasOwnProperty.call(message, "rgbBytes"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.rgbBytes);
            return writer;
        };

        /**
         * Encodes the specified LedCommand message, length delimited. Does not implicitly {@link yahboom_dogzilla_lite.LedCommand.verify|verify} messages.
         * @function encodeDelimited
         * @memberof yahboom_dogzilla_lite.LedCommand
         * @static
         * @param {yahboom_dogzilla_lite.ILedCommand} message LedCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LedCommand.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a LedCommand message from the specified reader or buffer.
         * @function decode
         * @memberof yahboom_dogzilla_lite.LedCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {yahboom_dogzilla_lite.LedCommand} LedCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LedCommand.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.yahboom_dogzilla_lite.LedCommand();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ledIndex = reader.uint32();
                        break;
                    }
                case 2: {
                        message.rgbBytes = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a LedCommand message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof yahboom_dogzilla_lite.LedCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {yahboom_dogzilla_lite.LedCommand} LedCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LedCommand.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LedCommand message.
         * @function verify
         * @memberof yahboom_dogzilla_lite.LedCommand
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LedCommand.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.ledIndex != null && message.hasOwnProperty("ledIndex"))
                if (!$util.isInteger(message.ledIndex))
                    return "ledIndex: integer expected";
            if (message.rgbBytes != null && message.hasOwnProperty("rgbBytes"))
                if (!(message.rgbBytes && typeof message.rgbBytes.length === "number" || $util.isString(message.rgbBytes)))
                    return "rgbBytes: buffer expected";
            return null;
        };

        /**
         * Creates a LedCommand message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof yahboom_dogzilla_lite.LedCommand
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {yahboom_dogzilla_lite.LedCommand} LedCommand
         */
        LedCommand.fromObject = function fromObject(object, long) {
            if (object instanceof $root.yahboom_dogzilla_lite.LedCommand)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.yahboom_dogzilla_lite.LedCommand();
            if (object.ledIndex != null)
                message.ledIndex = object.ledIndex >>> 0;
            if (object.rgbBytes != null)
                if (typeof object.rgbBytes === "string")
                    $util.base64.decode(object.rgbBytes, message.rgbBytes = $util.newBuffer($util.base64.length(object.rgbBytes)), 0);
                else if (object.rgbBytes.length >= 0)
                    message.rgbBytes = object.rgbBytes;
            return message;
        };

        /**
         * Creates a plain object from a LedCommand message. Also converts values to other types if specified.
         * @function toObject
         * @memberof yahboom_dogzilla_lite.LedCommand
         * @static
         * @param {yahboom_dogzilla_lite.LedCommand} message LedCommand
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LedCommand.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.ledIndex = 0;
                if (options.bytes === String)
                    object.rgbBytes = "";
                else {
                    object.rgbBytes = [];
                    if (options.bytes !== Array)
                        object.rgbBytes = $util.newBuffer(object.rgbBytes);
                }
            }
            if (message.ledIndex != null && message.hasOwnProperty("ledIndex"))
                object.ledIndex = message.ledIndex;
            if (message.rgbBytes != null && message.hasOwnProperty("rgbBytes"))
                object.rgbBytes = options.bytes === String ? $util.base64.encode(message.rgbBytes, 0, message.rgbBytes.length) : options.bytes === Array ? Array.prototype.slice.call(message.rgbBytes) : message.rgbBytes;
            return object;
        };

        /**
         * Converts this LedCommand to JSON.
         * @function toJSON
         * @memberof yahboom_dogzilla_lite.LedCommand
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LedCommand.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for LedCommand
         * @function getTypeUrl
         * @memberof yahboom_dogzilla_lite.LedCommand
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        LedCommand.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/yahboom_dogzilla_lite.LedCommand";
        };

        return LedCommand;
    })();

    yahboom_dogzilla_lite.ActionCommand = (function() {

        /**
         * Properties of an ActionCommand.
         * @memberof yahboom_dogzilla_lite
         * @interface IActionCommand
         * @property {yahboom_dogzilla_lite.ActionType|null} [action] ActionCommand action
         */

        /**
         * Constructs a new ActionCommand.
         * @memberof yahboom_dogzilla_lite
         * @classdesc Represents an ActionCommand.
         * @implements IActionCommand
         * @constructor
         * @param {yahboom_dogzilla_lite.IActionCommand=} [properties] Properties to set
         */
        function ActionCommand(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ActionCommand action.
         * @member {yahboom_dogzilla_lite.ActionType} action
         * @memberof yahboom_dogzilla_lite.ActionCommand
         * @instance
         */
        ActionCommand.prototype.action = 0;

        /**
         * Creates a new ActionCommand instance using the specified properties.
         * @function create
         * @memberof yahboom_dogzilla_lite.ActionCommand
         * @static
         * @param {yahboom_dogzilla_lite.IActionCommand=} [properties] Properties to set
         * @returns {yahboom_dogzilla_lite.ActionCommand} ActionCommand instance
         */
        ActionCommand.create = function create(properties) {
            return new ActionCommand(properties);
        };

        /**
         * Encodes the specified ActionCommand message. Does not implicitly {@link yahboom_dogzilla_lite.ActionCommand.verify|verify} messages.
         * @function encode
         * @memberof yahboom_dogzilla_lite.ActionCommand
         * @static
         * @param {yahboom_dogzilla_lite.IActionCommand} message ActionCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ActionCommand.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.action != null && Object.hasOwnProperty.call(message, "action"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.action);
            return writer;
        };

        /**
         * Encodes the specified ActionCommand message, length delimited. Does not implicitly {@link yahboom_dogzilla_lite.ActionCommand.verify|verify} messages.
         * @function encodeDelimited
         * @memberof yahboom_dogzilla_lite.ActionCommand
         * @static
         * @param {yahboom_dogzilla_lite.IActionCommand} message ActionCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ActionCommand.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an ActionCommand message from the specified reader or buffer.
         * @function decode
         * @memberof yahboom_dogzilla_lite.ActionCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {yahboom_dogzilla_lite.ActionCommand} ActionCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ActionCommand.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.yahboom_dogzilla_lite.ActionCommand();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.action = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an ActionCommand message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof yahboom_dogzilla_lite.ActionCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {yahboom_dogzilla_lite.ActionCommand} ActionCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ActionCommand.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an ActionCommand message.
         * @function verify
         * @memberof yahboom_dogzilla_lite.ActionCommand
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ActionCommand.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.action != null && message.hasOwnProperty("action"))
                switch (message.action) {
                default:
                    return "action: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                case 5:
                case 6:
                case 7:
                case 8:
                case 9:
                case 10:
                case 11:
                case 12:
                case 13:
                case 14:
                case 15:
                case 16:
                case 17:
                case 18:
                case 19:
                case 20:
                case 21:
                case 22:
                case 23:
                case 24:
                case 255:
                    break;
                }
            return null;
        };

        /**
         * Creates an ActionCommand message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof yahboom_dogzilla_lite.ActionCommand
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {yahboom_dogzilla_lite.ActionCommand} ActionCommand
         */
        ActionCommand.fromObject = function fromObject(object, long) {
            if (object instanceof $root.yahboom_dogzilla_lite.ActionCommand)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.yahboom_dogzilla_lite.ActionCommand();
            switch (object.action) {
            default:
                if (typeof object.action === "number") {
                    message.action = object.action;
                    break;
                }
                break;
            case "ACTION_UNSPECIFIED":
            case 0:
                message.action = 0;
                break;
            case "ACTION_LIE_DOWN":
            case 1:
                message.action = 1;
                break;
            case "ACTION_STAND_UP":
            case 2:
                message.action = 2;
                break;
            case "ACTION_CRAWL_FORWARD":
            case 3:
                message.action = 3;
                break;
            case "ACTION_TURN_AROUND":
            case 4:
                message.action = 4;
                break;
            case "ACTION_MARCH_IN_PLACE":
            case 5:
                message.action = 5;
                break;
            case "ACTION_SQUAT":
            case 6:
                message.action = 6;
                break;
            case "ACTION_ROLL":
            case 7:
                message.action = 7;
                break;
            case "ACTION_PITCH":
            case 8:
                message.action = 8;
                break;
            case "ACTION_YAW":
            case 9:
                message.action = 9;
                break;
            case "ACTION_THREE_AXIS_ROTATION":
            case 10:
                message.action = 10;
                break;
            case "ACTION_PEE":
            case 11:
                message.action = 11;
                break;
            case "ACTION_SIT_DOWN":
            case 12:
                message.action = 12;
                break;
            case "ACTION_WAVE":
            case 13:
                message.action = 13;
                break;
            case "ACTION_STRETCH":
            case 14:
                message.action = 14;
                break;
            case "ACTION_WAVE2":
            case 15:
                message.action = 15;
                break;
            case "ACTION_SWAY":
            case 16:
                message.action = 16;
                break;
            case "ACTION_BEG_FOR_FOOD":
            case 17:
                message.action = 17;
                break;
            case "ACTION_FIND_FOOD":
            case 18:
                message.action = 18;
                break;
            case "ACTION_HANDSHAKE":
            case 19:
                message.action = 19;
                break;
            case "ACTION_ARM_DEMO":
            case 20:
                message.action = 20;
                break;
            case "ACTION_PUSHUPS":
            case 21:
                message.action = 21;
                break;
            case "ACTION_PITCH_YAW_ROTATION":
            case 22:
                message.action = 22;
                break;
            case "ACTION_UP_DOWN_ROTATION":
            case 23:
                message.action = 23;
                break;
            case "ACTION_FORWARD_BACKWARD_ROTATION":
            case 24:
                message.action = 24;
                break;
            case "ACTION_RESTORE_DEFAULT":
            case 255:
                message.action = 255;
                break;
            }
            return message;
        };

        /**
         * Creates a plain object from an ActionCommand message. Also converts values to other types if specified.
         * @function toObject
         * @memberof yahboom_dogzilla_lite.ActionCommand
         * @static
         * @param {yahboom_dogzilla_lite.ActionCommand} message ActionCommand
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ActionCommand.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.action = options.enums === String ? "ACTION_UNSPECIFIED" : 0;
            if (message.action != null && message.hasOwnProperty("action"))
                object.action = options.enums === String ? $root.yahboom_dogzilla_lite.ActionType[message.action] === undefined ? message.action : $root.yahboom_dogzilla_lite.ActionType[message.action] : message.action;
            return object;
        };

        /**
         * Converts this ActionCommand to JSON.
         * @function toJSON
         * @memberof yahboom_dogzilla_lite.ActionCommand
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ActionCommand.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ActionCommand
         * @function getTypeUrl
         * @memberof yahboom_dogzilla_lite.ActionCommand
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ActionCommand.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/yahboom_dogzilla_lite.ActionCommand";
        };

        return ActionCommand;
    })();

    yahboom_dogzilla_lite.MovementCommand = (function() {

        /**
         * Properties of a MovementCommand.
         * @memberof yahboom_dogzilla_lite
         * @interface IMovementCommand
         * @property {number|null} [moveX] MovementCommand moveX
         * @property {number|null} [moveY] MovementCommand moveY
         * @property {number|null} [moveYaw] MovementCommand moveYaw
         */

        /**
         * Constructs a new MovementCommand.
         * @memberof yahboom_dogzilla_lite
         * @classdesc Represents a MovementCommand.
         * @implements IMovementCommand
         * @constructor
         * @param {yahboom_dogzilla_lite.IMovementCommand=} [properties] Properties to set
         */
        function MovementCommand(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MovementCommand moveX.
         * @member {number} moveX
         * @memberof yahboom_dogzilla_lite.MovementCommand
         * @instance
         */
        MovementCommand.prototype.moveX = 0;

        /**
         * MovementCommand moveY.
         * @member {number} moveY
         * @memberof yahboom_dogzilla_lite.MovementCommand
         * @instance
         */
        MovementCommand.prototype.moveY = 0;

        /**
         * MovementCommand moveYaw.
         * @member {number} moveYaw
         * @memberof yahboom_dogzilla_lite.MovementCommand
         * @instance
         */
        MovementCommand.prototype.moveYaw = 0;

        /**
         * Creates a new MovementCommand instance using the specified properties.
         * @function create
         * @memberof yahboom_dogzilla_lite.MovementCommand
         * @static
         * @param {yahboom_dogzilla_lite.IMovementCommand=} [properties] Properties to set
         * @returns {yahboom_dogzilla_lite.MovementCommand} MovementCommand instance
         */
        MovementCommand.create = function create(properties) {
            return new MovementCommand(properties);
        };

        /**
         * Encodes the specified MovementCommand message. Does not implicitly {@link yahboom_dogzilla_lite.MovementCommand.verify|verify} messages.
         * @function encode
         * @memberof yahboom_dogzilla_lite.MovementCommand
         * @static
         * @param {yahboom_dogzilla_lite.IMovementCommand} message MovementCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MovementCommand.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.moveX != null && Object.hasOwnProperty.call(message, "moveX"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.moveX);
            if (message.moveY != null && Object.hasOwnProperty.call(message, "moveY"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.moveY);
            if (message.moveYaw != null && Object.hasOwnProperty.call(message, "moveYaw"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.moveYaw);
            return writer;
        };

        /**
         * Encodes the specified MovementCommand message, length delimited. Does not implicitly {@link yahboom_dogzilla_lite.MovementCommand.verify|verify} messages.
         * @function encodeDelimited
         * @memberof yahboom_dogzilla_lite.MovementCommand
         * @static
         * @param {yahboom_dogzilla_lite.IMovementCommand} message MovementCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MovementCommand.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a MovementCommand message from the specified reader or buffer.
         * @function decode
         * @memberof yahboom_dogzilla_lite.MovementCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {yahboom_dogzilla_lite.MovementCommand} MovementCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MovementCommand.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.yahboom_dogzilla_lite.MovementCommand();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.moveX = reader.uint32();
                        break;
                    }
                case 2: {
                        message.moveY = reader.uint32();
                        break;
                    }
                case 3: {
                        message.moveYaw = reader.uint32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a MovementCommand message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof yahboom_dogzilla_lite.MovementCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {yahboom_dogzilla_lite.MovementCommand} MovementCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MovementCommand.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MovementCommand message.
         * @function verify
         * @memberof yahboom_dogzilla_lite.MovementCommand
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MovementCommand.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.moveX != null && message.hasOwnProperty("moveX"))
                if (!$util.isInteger(message.moveX))
                    return "moveX: integer expected";
            if (message.moveY != null && message.hasOwnProperty("moveY"))
                if (!$util.isInteger(message.moveY))
                    return "moveY: integer expected";
            if (message.moveYaw != null && message.hasOwnProperty("moveYaw"))
                if (!$util.isInteger(message.moveYaw))
                    return "moveYaw: integer expected";
            return null;
        };

        /**
         * Creates a MovementCommand message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof yahboom_dogzilla_lite.MovementCommand
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {yahboom_dogzilla_lite.MovementCommand} MovementCommand
         */
        MovementCommand.fromObject = function fromObject(object, long) {
            if (object instanceof $root.yahboom_dogzilla_lite.MovementCommand)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.yahboom_dogzilla_lite.MovementCommand();
            if (object.moveX != null)
                message.moveX = object.moveX >>> 0;
            if (object.moveY != null)
                message.moveY = object.moveY >>> 0;
            if (object.moveYaw != null)
                message.moveYaw = object.moveYaw >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a MovementCommand message. Also converts values to other types if specified.
         * @function toObject
         * @memberof yahboom_dogzilla_lite.MovementCommand
         * @static
         * @param {yahboom_dogzilla_lite.MovementCommand} message MovementCommand
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MovementCommand.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.moveX = 0;
                object.moveY = 0;
                object.moveYaw = 0;
            }
            if (message.moveX != null && message.hasOwnProperty("moveX"))
                object.moveX = message.moveX;
            if (message.moveY != null && message.hasOwnProperty("moveY"))
                object.moveY = message.moveY;
            if (message.moveYaw != null && message.hasOwnProperty("moveYaw"))
                object.moveYaw = message.moveYaw;
            return object;
        };

        /**
         * Converts this MovementCommand to JSON.
         * @function toJSON
         * @memberof yahboom_dogzilla_lite.MovementCommand
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MovementCommand.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MovementCommand
         * @function getTypeUrl
         * @memberof yahboom_dogzilla_lite.MovementCommand
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MovementCommand.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/yahboom_dogzilla_lite.MovementCommand";
        };

        return MovementCommand;
    })();

    yahboom_dogzilla_lite.ConfigCommand = (function() {

        /**
         * Properties of a ConfigCommand.
         * @memberof yahboom_dogzilla_lite
         * @interface IConfigCommand
         * @property {yahboom_dogzilla_lite.PerformanceMode|null} [performanceMode] ConfigCommand performanceMode
         * @property {yahboom_dogzilla_lite.GaitType|null} [gait] ConfigCommand gait
         * @property {yahboom_dogzilla_lite.ImuMode|null} [imuMode] ConfigCommand imuMode
         * @property {boolean|null} [enableFeedback] ConfigCommand enableFeedback
         * @property {string|null} [bluetoothName] ConfigCommand bluetoothName
         */

        /**
         * Constructs a new ConfigCommand.
         * @memberof yahboom_dogzilla_lite
         * @classdesc Represents a ConfigCommand.
         * @implements IConfigCommand
         * @constructor
         * @param {yahboom_dogzilla_lite.IConfigCommand=} [properties] Properties to set
         */
        function ConfigCommand(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ConfigCommand performanceMode.
         * @member {yahboom_dogzilla_lite.PerformanceMode} performanceMode
         * @memberof yahboom_dogzilla_lite.ConfigCommand
         * @instance
         */
        ConfigCommand.prototype.performanceMode = 0;

        /**
         * ConfigCommand gait.
         * @member {yahboom_dogzilla_lite.GaitType} gait
         * @memberof yahboom_dogzilla_lite.ConfigCommand
         * @instance
         */
        ConfigCommand.prototype.gait = 0;

        /**
         * ConfigCommand imuMode.
         * @member {yahboom_dogzilla_lite.ImuMode} imuMode
         * @memberof yahboom_dogzilla_lite.ConfigCommand
         * @instance
         */
        ConfigCommand.prototype.imuMode = 0;

        /**
         * ConfigCommand enableFeedback.
         * @member {boolean} enableFeedback
         * @memberof yahboom_dogzilla_lite.ConfigCommand
         * @instance
         */
        ConfigCommand.prototype.enableFeedback = false;

        /**
         * ConfigCommand bluetoothName.
         * @member {string} bluetoothName
         * @memberof yahboom_dogzilla_lite.ConfigCommand
         * @instance
         */
        ConfigCommand.prototype.bluetoothName = "";

        /**
         * Creates a new ConfigCommand instance using the specified properties.
         * @function create
         * @memberof yahboom_dogzilla_lite.ConfigCommand
         * @static
         * @param {yahboom_dogzilla_lite.IConfigCommand=} [properties] Properties to set
         * @returns {yahboom_dogzilla_lite.ConfigCommand} ConfigCommand instance
         */
        ConfigCommand.create = function create(properties) {
            return new ConfigCommand(properties);
        };

        /**
         * Encodes the specified ConfigCommand message. Does not implicitly {@link yahboom_dogzilla_lite.ConfigCommand.verify|verify} messages.
         * @function encode
         * @memberof yahboom_dogzilla_lite.ConfigCommand
         * @static
         * @param {yahboom_dogzilla_lite.IConfigCommand} message ConfigCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ConfigCommand.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.performanceMode != null && Object.hasOwnProperty.call(message, "performanceMode"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.performanceMode);
            if (message.gait != null && Object.hasOwnProperty.call(message, "gait"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.gait);
            if (message.imuMode != null && Object.hasOwnProperty.call(message, "imuMode"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.imuMode);
            if (message.enableFeedback != null && Object.hasOwnProperty.call(message, "enableFeedback"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.enableFeedback);
            if (message.bluetoothName != null && Object.hasOwnProperty.call(message, "bluetoothName"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.bluetoothName);
            return writer;
        };

        /**
         * Encodes the specified ConfigCommand message, length delimited. Does not implicitly {@link yahboom_dogzilla_lite.ConfigCommand.verify|verify} messages.
         * @function encodeDelimited
         * @memberof yahboom_dogzilla_lite.ConfigCommand
         * @static
         * @param {yahboom_dogzilla_lite.IConfigCommand} message ConfigCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ConfigCommand.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ConfigCommand message from the specified reader or buffer.
         * @function decode
         * @memberof yahboom_dogzilla_lite.ConfigCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {yahboom_dogzilla_lite.ConfigCommand} ConfigCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ConfigCommand.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.yahboom_dogzilla_lite.ConfigCommand();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.performanceMode = reader.int32();
                        break;
                    }
                case 2: {
                        message.gait = reader.int32();
                        break;
                    }
                case 3: {
                        message.imuMode = reader.int32();
                        break;
                    }
                case 4: {
                        message.enableFeedback = reader.bool();
                        break;
                    }
                case 5: {
                        message.bluetoothName = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ConfigCommand message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof yahboom_dogzilla_lite.ConfigCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {yahboom_dogzilla_lite.ConfigCommand} ConfigCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ConfigCommand.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ConfigCommand message.
         * @function verify
         * @memberof yahboom_dogzilla_lite.ConfigCommand
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ConfigCommand.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.performanceMode != null && message.hasOwnProperty("performanceMode"))
                switch (message.performanceMode) {
                default:
                    return "performanceMode: enum value expected";
                case 0:
                case 1:
                    break;
                }
            if (message.gait != null && message.hasOwnProperty("gait"))
                switch (message.gait) {
                default:
                    return "gait: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                    break;
                }
            if (message.imuMode != null && message.hasOwnProperty("imuMode"))
                switch (message.imuMode) {
                default:
                    return "imuMode: enum value expected";
                case 0:
                case 1:
                    break;
                }
            if (message.enableFeedback != null && message.hasOwnProperty("enableFeedback"))
                if (typeof message.enableFeedback !== "boolean")
                    return "enableFeedback: boolean expected";
            if (message.bluetoothName != null && message.hasOwnProperty("bluetoothName"))
                if (!$util.isString(message.bluetoothName))
                    return "bluetoothName: string expected";
            return null;
        };

        /**
         * Creates a ConfigCommand message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof yahboom_dogzilla_lite.ConfigCommand
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {yahboom_dogzilla_lite.ConfigCommand} ConfigCommand
         */
        ConfigCommand.fromObject = function fromObject(object, long) {
            if (object instanceof $root.yahboom_dogzilla_lite.ConfigCommand)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.yahboom_dogzilla_lite.ConfigCommand();
            switch (object.performanceMode) {
            default:
                if (typeof object.performanceMode === "number") {
                    message.performanceMode = object.performanceMode;
                    break;
                }
                break;
            case "PERFORMANCE_NORMAL_CONTROL":
            case 0:
                message.performanceMode = 0;
                break;
            case "PERFORMANCE_CYCLE_ACTION":
            case 1:
                message.performanceMode = 1;
                break;
            }
            switch (object.gait) {
            default:
                if (typeof object.gait === "number") {
                    message.gait = object.gait;
                    break;
                }
                break;
            case "GAIT_TROT":
            case 0:
                message.gait = 0;
                break;
            case "GAIT_WALK":
            case 1:
                message.gait = 1;
                break;
            case "GAIT_HIGH_WALK":
            case 2:
                message.gait = 2;
                break;
            case "GAIT_MICRO_TROT":
            case 3:
                message.gait = 3;
                break;
            }
            switch (object.imuMode) {
            default:
                if (typeof object.imuMode === "number") {
                    message.imuMode = object.imuMode;
                    break;
                }
                break;
            case "IMU_DISABLED":
            case 0:
                message.imuMode = 0;
                break;
            case "IMU_SELF_STABILIZE":
            case 1:
                message.imuMode = 1;
                break;
            }
            if (object.enableFeedback != null)
                message.enableFeedback = Boolean(object.enableFeedback);
            if (object.bluetoothName != null)
                message.bluetoothName = String(object.bluetoothName);
            return message;
        };

        /**
         * Creates a plain object from a ConfigCommand message. Also converts values to other types if specified.
         * @function toObject
         * @memberof yahboom_dogzilla_lite.ConfigCommand
         * @static
         * @param {yahboom_dogzilla_lite.ConfigCommand} message ConfigCommand
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ConfigCommand.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.performanceMode = options.enums === String ? "PERFORMANCE_NORMAL_CONTROL" : 0;
                object.gait = options.enums === String ? "GAIT_TROT" : 0;
                object.imuMode = options.enums === String ? "IMU_DISABLED" : 0;
                object.enableFeedback = false;
                object.bluetoothName = "";
            }
            if (message.performanceMode != null && message.hasOwnProperty("performanceMode"))
                object.performanceMode = options.enums === String ? $root.yahboom_dogzilla_lite.PerformanceMode[message.performanceMode] === undefined ? message.performanceMode : $root.yahboom_dogzilla_lite.PerformanceMode[message.performanceMode] : message.performanceMode;
            if (message.gait != null && message.hasOwnProperty("gait"))
                object.gait = options.enums === String ? $root.yahboom_dogzilla_lite.GaitType[message.gait] === undefined ? message.gait : $root.yahboom_dogzilla_lite.GaitType[message.gait] : message.gait;
            if (message.imuMode != null && message.hasOwnProperty("imuMode"))
                object.imuMode = options.enums === String ? $root.yahboom_dogzilla_lite.ImuMode[message.imuMode] === undefined ? message.imuMode : $root.yahboom_dogzilla_lite.ImuMode[message.imuMode] : message.imuMode;
            if (message.enableFeedback != null && message.hasOwnProperty("enableFeedback"))
                object.enableFeedback = message.enableFeedback;
            if (message.bluetoothName != null && message.hasOwnProperty("bluetoothName"))
                object.bluetoothName = message.bluetoothName;
            return object;
        };

        /**
         * Converts this ConfigCommand to JSON.
         * @function toJSON
         * @memberof yahboom_dogzilla_lite.ConfigCommand
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ConfigCommand.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ConfigCommand
         * @function getTypeUrl
         * @memberof yahboom_dogzilla_lite.ConfigCommand
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ConfigCommand.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/yahboom_dogzilla_lite.ConfigCommand";
        };

        return ConfigCommand;
    })();

    yahboom_dogzilla_lite.Command = (function() {

        /**
         * Properties of a Command.
         * @memberof yahboom_dogzilla_lite
         * @interface ICommand
         * @property {string|null} [targetDeviceSerial] Command targetDeviceSerial
         * @property {yahboom_dogzilla_lite.IServoCommand|null} [servo] Command servo
         * @property {yahboom_dogzilla_lite.IServoSpeedCommand|null} [servoSpeed] Command servoSpeed
         * @property {yahboom_dogzilla_lite.ICalibrationCommand|null} [calibration] Command calibration
         * @property {yahboom_dogzilla_lite.IArmCommand|null} [arm] Command arm
         * @property {yahboom_dogzilla_lite.IIoCommand|null} [io] Command io
         * @property {yahboom_dogzilla_lite.IConfigCommand|null} [config] Command config
         * @property {yahboom_dogzilla_lite.ILedCommand|null} [led] Command led
         * @property {yahboom_dogzilla_lite.IActionCommand|null} [action] Command action
         * @property {yahboom_dogzilla_lite.IMovementCommand|null} [movement] Command movement
         */

        /**
         * Constructs a new Command.
         * @memberof yahboom_dogzilla_lite
         * @classdesc Represents a Command.
         * @implements ICommand
         * @constructor
         * @param {yahboom_dogzilla_lite.ICommand=} [properties] Properties to set
         */
        function Command(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Command targetDeviceSerial.
         * @member {string} targetDeviceSerial
         * @memberof yahboom_dogzilla_lite.Command
         * @instance
         */
        Command.prototype.targetDeviceSerial = "";

        /**
         * Command servo.
         * @member {yahboom_dogzilla_lite.IServoCommand|null|undefined} servo
         * @memberof yahboom_dogzilla_lite.Command
         * @instance
         */
        Command.prototype.servo = null;

        /**
         * Command servoSpeed.
         * @member {yahboom_dogzilla_lite.IServoSpeedCommand|null|undefined} servoSpeed
         * @memberof yahboom_dogzilla_lite.Command
         * @instance
         */
        Command.prototype.servoSpeed = null;

        /**
         * Command calibration.
         * @member {yahboom_dogzilla_lite.ICalibrationCommand|null|undefined} calibration
         * @memberof yahboom_dogzilla_lite.Command
         * @instance
         */
        Command.prototype.calibration = null;

        /**
         * Command arm.
         * @member {yahboom_dogzilla_lite.IArmCommand|null|undefined} arm
         * @memberof yahboom_dogzilla_lite.Command
         * @instance
         */
        Command.prototype.arm = null;

        /**
         * Command io.
         * @member {yahboom_dogzilla_lite.IIoCommand|null|undefined} io
         * @memberof yahboom_dogzilla_lite.Command
         * @instance
         */
        Command.prototype.io = null;

        /**
         * Command config.
         * @member {yahboom_dogzilla_lite.IConfigCommand|null|undefined} config
         * @memberof yahboom_dogzilla_lite.Command
         * @instance
         */
        Command.prototype.config = null;

        /**
         * Command led.
         * @member {yahboom_dogzilla_lite.ILedCommand|null|undefined} led
         * @memberof yahboom_dogzilla_lite.Command
         * @instance
         */
        Command.prototype.led = null;

        /**
         * Command action.
         * @member {yahboom_dogzilla_lite.IActionCommand|null|undefined} action
         * @memberof yahboom_dogzilla_lite.Command
         * @instance
         */
        Command.prototype.action = null;

        /**
         * Command movement.
         * @member {yahboom_dogzilla_lite.IMovementCommand|null|undefined} movement
         * @memberof yahboom_dogzilla_lite.Command
         * @instance
         */
        Command.prototype.movement = null;

        /**
         * Creates a new Command instance using the specified properties.
         * @function create
         * @memberof yahboom_dogzilla_lite.Command
         * @static
         * @param {yahboom_dogzilla_lite.ICommand=} [properties] Properties to set
         * @returns {yahboom_dogzilla_lite.Command} Command instance
         */
        Command.create = function create(properties) {
            return new Command(properties);
        };

        /**
         * Encodes the specified Command message. Does not implicitly {@link yahboom_dogzilla_lite.Command.verify|verify} messages.
         * @function encode
         * @memberof yahboom_dogzilla_lite.Command
         * @static
         * @param {yahboom_dogzilla_lite.ICommand} message Command message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Command.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.targetDeviceSerial != null && Object.hasOwnProperty.call(message, "targetDeviceSerial"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.targetDeviceSerial);
            if (message.servo != null && Object.hasOwnProperty.call(message, "servo"))
                $root.yahboom_dogzilla_lite.ServoCommand.encode(message.servo, writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            if (message.servoSpeed != null && Object.hasOwnProperty.call(message, "servoSpeed"))
                $root.yahboom_dogzilla_lite.ServoSpeedCommand.encode(message.servoSpeed, writer.uint32(/* id 11, wireType 2 =*/90).fork()).ldelim();
            if (message.calibration != null && Object.hasOwnProperty.call(message, "calibration"))
                $root.yahboom_dogzilla_lite.CalibrationCommand.encode(message.calibration, writer.uint32(/* id 12, wireType 2 =*/98).fork()).ldelim();
            if (message.arm != null && Object.hasOwnProperty.call(message, "arm"))
                $root.yahboom_dogzilla_lite.ArmCommand.encode(message.arm, writer.uint32(/* id 13, wireType 2 =*/106).fork()).ldelim();
            if (message.io != null && Object.hasOwnProperty.call(message, "io"))
                $root.yahboom_dogzilla_lite.IoCommand.encode(message.io, writer.uint32(/* id 14, wireType 2 =*/114).fork()).ldelim();
            if (message.config != null && Object.hasOwnProperty.call(message, "config"))
                $root.yahboom_dogzilla_lite.ConfigCommand.encode(message.config, writer.uint32(/* id 15, wireType 2 =*/122).fork()).ldelim();
            if (message.led != null && Object.hasOwnProperty.call(message, "led"))
                $root.yahboom_dogzilla_lite.LedCommand.encode(message.led, writer.uint32(/* id 16, wireType 2 =*/130).fork()).ldelim();
            if (message.action != null && Object.hasOwnProperty.call(message, "action"))
                $root.yahboom_dogzilla_lite.ActionCommand.encode(message.action, writer.uint32(/* id 17, wireType 2 =*/138).fork()).ldelim();
            if (message.movement != null && Object.hasOwnProperty.call(message, "movement"))
                $root.yahboom_dogzilla_lite.MovementCommand.encode(message.movement, writer.uint32(/* id 18, wireType 2 =*/146).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified Command message, length delimited. Does not implicitly {@link yahboom_dogzilla_lite.Command.verify|verify} messages.
         * @function encodeDelimited
         * @memberof yahboom_dogzilla_lite.Command
         * @static
         * @param {yahboom_dogzilla_lite.ICommand} message Command message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Command.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Command message from the specified reader or buffer.
         * @function decode
         * @memberof yahboom_dogzilla_lite.Command
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {yahboom_dogzilla_lite.Command} Command
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Command.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.yahboom_dogzilla_lite.Command();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.targetDeviceSerial = reader.string();
                        break;
                    }
                case 10: {
                        message.servo = $root.yahboom_dogzilla_lite.ServoCommand.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 11: {
                        message.servoSpeed = $root.yahboom_dogzilla_lite.ServoSpeedCommand.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 12: {
                        message.calibration = $root.yahboom_dogzilla_lite.CalibrationCommand.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 13: {
                        message.arm = $root.yahboom_dogzilla_lite.ArmCommand.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 14: {
                        message.io = $root.yahboom_dogzilla_lite.IoCommand.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 15: {
                        message.config = $root.yahboom_dogzilla_lite.ConfigCommand.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 16: {
                        message.led = $root.yahboom_dogzilla_lite.LedCommand.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 17: {
                        message.action = $root.yahboom_dogzilla_lite.ActionCommand.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 18: {
                        message.movement = $root.yahboom_dogzilla_lite.MovementCommand.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Command message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof yahboom_dogzilla_lite.Command
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {yahboom_dogzilla_lite.Command} Command
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Command.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Command message.
         * @function verify
         * @memberof yahboom_dogzilla_lite.Command
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Command.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.targetDeviceSerial != null && message.hasOwnProperty("targetDeviceSerial"))
                if (!$util.isString(message.targetDeviceSerial))
                    return "targetDeviceSerial: string expected";
            if (message.servo != null && message.hasOwnProperty("servo")) {
                let error = $root.yahboom_dogzilla_lite.ServoCommand.verify(message.servo, long + 1);
                if (error)
                    return "servo." + error;
            }
            if (message.servoSpeed != null && message.hasOwnProperty("servoSpeed")) {
                let error = $root.yahboom_dogzilla_lite.ServoSpeedCommand.verify(message.servoSpeed, long + 1);
                if (error)
                    return "servoSpeed." + error;
            }
            if (message.calibration != null && message.hasOwnProperty("calibration")) {
                let error = $root.yahboom_dogzilla_lite.CalibrationCommand.verify(message.calibration, long + 1);
                if (error)
                    return "calibration." + error;
            }
            if (message.arm != null && message.hasOwnProperty("arm")) {
                let error = $root.yahboom_dogzilla_lite.ArmCommand.verify(message.arm, long + 1);
                if (error)
                    return "arm." + error;
            }
            if (message.io != null && message.hasOwnProperty("io")) {
                let error = $root.yahboom_dogzilla_lite.IoCommand.verify(message.io, long + 1);
                if (error)
                    return "io." + error;
            }
            if (message.config != null && message.hasOwnProperty("config")) {
                let error = $root.yahboom_dogzilla_lite.ConfigCommand.verify(message.config, long + 1);
                if (error)
                    return "config." + error;
            }
            if (message.led != null && message.hasOwnProperty("led")) {
                let error = $root.yahboom_dogzilla_lite.LedCommand.verify(message.led, long + 1);
                if (error)
                    return "led." + error;
            }
            if (message.action != null && message.hasOwnProperty("action")) {
                let error = $root.yahboom_dogzilla_lite.ActionCommand.verify(message.action, long + 1);
                if (error)
                    return "action." + error;
            }
            if (message.movement != null && message.hasOwnProperty("movement")) {
                let error = $root.yahboom_dogzilla_lite.MovementCommand.verify(message.movement, long + 1);
                if (error)
                    return "movement." + error;
            }
            return null;
        };

        /**
         * Creates a Command message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof yahboom_dogzilla_lite.Command
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {yahboom_dogzilla_lite.Command} Command
         */
        Command.fromObject = function fromObject(object, long) {
            if (object instanceof $root.yahboom_dogzilla_lite.Command)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.yahboom_dogzilla_lite.Command();
            if (object.targetDeviceSerial != null)
                message.targetDeviceSerial = String(object.targetDeviceSerial);
            if (object.servo != null) {
                if (typeof object.servo !== "object")
                    throw TypeError(".yahboom_dogzilla_lite.Command.servo: object expected");
                message.servo = $root.yahboom_dogzilla_lite.ServoCommand.fromObject(object.servo, long + 1);
            }
            if (object.servoSpeed != null) {
                if (typeof object.servoSpeed !== "object")
                    throw TypeError(".yahboom_dogzilla_lite.Command.servoSpeed: object expected");
                message.servoSpeed = $root.yahboom_dogzilla_lite.ServoSpeedCommand.fromObject(object.servoSpeed, long + 1);
            }
            if (object.calibration != null) {
                if (typeof object.calibration !== "object")
                    throw TypeError(".yahboom_dogzilla_lite.Command.calibration: object expected");
                message.calibration = $root.yahboom_dogzilla_lite.CalibrationCommand.fromObject(object.calibration, long + 1);
            }
            if (object.arm != null) {
                if (typeof object.arm !== "object")
                    throw TypeError(".yahboom_dogzilla_lite.Command.arm: object expected");
                message.arm = $root.yahboom_dogzilla_lite.ArmCommand.fromObject(object.arm, long + 1);
            }
            if (object.io != null) {
                if (typeof object.io !== "object")
                    throw TypeError(".yahboom_dogzilla_lite.Command.io: object expected");
                message.io = $root.yahboom_dogzilla_lite.IoCommand.fromObject(object.io, long + 1);
            }
            if (object.config != null) {
                if (typeof object.config !== "object")
                    throw TypeError(".yahboom_dogzilla_lite.Command.config: object expected");
                message.config = $root.yahboom_dogzilla_lite.ConfigCommand.fromObject(object.config, long + 1);
            }
            if (object.led != null) {
                if (typeof object.led !== "object")
                    throw TypeError(".yahboom_dogzilla_lite.Command.led: object expected");
                message.led = $root.yahboom_dogzilla_lite.LedCommand.fromObject(object.led, long + 1);
            }
            if (object.action != null) {
                if (typeof object.action !== "object")
                    throw TypeError(".yahboom_dogzilla_lite.Command.action: object expected");
                message.action = $root.yahboom_dogzilla_lite.ActionCommand.fromObject(object.action, long + 1);
            }
            if (object.movement != null) {
                if (typeof object.movement !== "object")
                    throw TypeError(".yahboom_dogzilla_lite.Command.movement: object expected");
                message.movement = $root.yahboom_dogzilla_lite.MovementCommand.fromObject(object.movement, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a Command message. Also converts values to other types if specified.
         * @function toObject
         * @memberof yahboom_dogzilla_lite.Command
         * @static
         * @param {yahboom_dogzilla_lite.Command} message Command
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Command.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.targetDeviceSerial = "";
                object.servo = null;
                object.servoSpeed = null;
                object.calibration = null;
                object.arm = null;
                object.io = null;
                object.config = null;
                object.led = null;
                object.action = null;
                object.movement = null;
            }
            if (message.targetDeviceSerial != null && message.hasOwnProperty("targetDeviceSerial"))
                object.targetDeviceSerial = message.targetDeviceSerial;
            if (message.servo != null && message.hasOwnProperty("servo"))
                object.servo = $root.yahboom_dogzilla_lite.ServoCommand.toObject(message.servo, options);
            if (message.servoSpeed != null && message.hasOwnProperty("servoSpeed"))
                object.servoSpeed = $root.yahboom_dogzilla_lite.ServoSpeedCommand.toObject(message.servoSpeed, options);
            if (message.calibration != null && message.hasOwnProperty("calibration"))
                object.calibration = $root.yahboom_dogzilla_lite.CalibrationCommand.toObject(message.calibration, options);
            if (message.arm != null && message.hasOwnProperty("arm"))
                object.arm = $root.yahboom_dogzilla_lite.ArmCommand.toObject(message.arm, options);
            if (message.io != null && message.hasOwnProperty("io"))
                object.io = $root.yahboom_dogzilla_lite.IoCommand.toObject(message.io, options);
            if (message.config != null && message.hasOwnProperty("config"))
                object.config = $root.yahboom_dogzilla_lite.ConfigCommand.toObject(message.config, options);
            if (message.led != null && message.hasOwnProperty("led"))
                object.led = $root.yahboom_dogzilla_lite.LedCommand.toObject(message.led, options);
            if (message.action != null && message.hasOwnProperty("action"))
                object.action = $root.yahboom_dogzilla_lite.ActionCommand.toObject(message.action, options);
            if (message.movement != null && message.hasOwnProperty("movement"))
                object.movement = $root.yahboom_dogzilla_lite.MovementCommand.toObject(message.movement, options);
            return object;
        };

        /**
         * Converts this Command to JSON.
         * @function toJSON
         * @memberof yahboom_dogzilla_lite.Command
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Command.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Command
         * @function getTypeUrl
         * @memberof yahboom_dogzilla_lite.Command
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Command.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/yahboom_dogzilla_lite.Command";
        };

        return Command;
    })();

    yahboom_dogzilla_lite.TxEnvelope = (function() {

        /**
         * Properties of a TxEnvelope.
         * @memberof yahboom_dogzilla_lite
         * @interface ITxEnvelope
         * @property {Long|null} [monotonicStampNs] TxEnvelope monotonicStampNs
         * @property {Long|null} [localStampNs] TxEnvelope localStampNs
         * @property {Long|null} [appStartId] TxEnvelope appStartId
         * @property {Uint8Array|null} [commandId] TxEnvelope commandId
         * @property {string|null} [targetDeviceSerial] TxEnvelope targetDeviceSerial
         * @property {yahboom_dogzilla_lite.ICommand|null} [command] TxEnvelope command
         */

        /**
         * Constructs a new TxEnvelope.
         * @memberof yahboom_dogzilla_lite
         * @classdesc Represents a TxEnvelope.
         * @implements ITxEnvelope
         * @constructor
         * @param {yahboom_dogzilla_lite.ITxEnvelope=} [properties] Properties to set
         */
        function TxEnvelope(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * TxEnvelope monotonicStampNs.
         * @member {Long} monotonicStampNs
         * @memberof yahboom_dogzilla_lite.TxEnvelope
         * @instance
         */
        TxEnvelope.prototype.monotonicStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * TxEnvelope localStampNs.
         * @member {Long} localStampNs
         * @memberof yahboom_dogzilla_lite.TxEnvelope
         * @instance
         */
        TxEnvelope.prototype.localStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * TxEnvelope appStartId.
         * @member {Long} appStartId
         * @memberof yahboom_dogzilla_lite.TxEnvelope
         * @instance
         */
        TxEnvelope.prototype.appStartId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * TxEnvelope commandId.
         * @member {Uint8Array} commandId
         * @memberof yahboom_dogzilla_lite.TxEnvelope
         * @instance
         */
        TxEnvelope.prototype.commandId = $util.newBuffer([]);

        /**
         * TxEnvelope targetDeviceSerial.
         * @member {string} targetDeviceSerial
         * @memberof yahboom_dogzilla_lite.TxEnvelope
         * @instance
         */
        TxEnvelope.prototype.targetDeviceSerial = "";

        /**
         * TxEnvelope command.
         * @member {yahboom_dogzilla_lite.ICommand|null|undefined} command
         * @memberof yahboom_dogzilla_lite.TxEnvelope
         * @instance
         */
        TxEnvelope.prototype.command = null;

        /**
         * Creates a new TxEnvelope instance using the specified properties.
         * @function create
         * @memberof yahboom_dogzilla_lite.TxEnvelope
         * @static
         * @param {yahboom_dogzilla_lite.ITxEnvelope=} [properties] Properties to set
         * @returns {yahboom_dogzilla_lite.TxEnvelope} TxEnvelope instance
         */
        TxEnvelope.create = function create(properties) {
            return new TxEnvelope(properties);
        };

        /**
         * Encodes the specified TxEnvelope message. Does not implicitly {@link yahboom_dogzilla_lite.TxEnvelope.verify|verify} messages.
         * @function encode
         * @memberof yahboom_dogzilla_lite.TxEnvelope
         * @static
         * @param {yahboom_dogzilla_lite.ITxEnvelope} message TxEnvelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TxEnvelope.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.monotonicStampNs != null && Object.hasOwnProperty.call(message, "monotonicStampNs"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.monotonicStampNs);
            if (message.localStampNs != null && Object.hasOwnProperty.call(message, "localStampNs"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.localStampNs);
            if (message.appStartId != null && Object.hasOwnProperty.call(message, "appStartId"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.appStartId);
            if (message.commandId != null && Object.hasOwnProperty.call(message, "commandId"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.commandId);
            if (message.targetDeviceSerial != null && Object.hasOwnProperty.call(message, "targetDeviceSerial"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.targetDeviceSerial);
            if (message.command != null && Object.hasOwnProperty.call(message, "command"))
                $root.yahboom_dogzilla_lite.Command.encode(message.command, writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified TxEnvelope message, length delimited. Does not implicitly {@link yahboom_dogzilla_lite.TxEnvelope.verify|verify} messages.
         * @function encodeDelimited
         * @memberof yahboom_dogzilla_lite.TxEnvelope
         * @static
         * @param {yahboom_dogzilla_lite.ITxEnvelope} message TxEnvelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TxEnvelope.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a TxEnvelope message from the specified reader or buffer.
         * @function decode
         * @memberof yahboom_dogzilla_lite.TxEnvelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {yahboom_dogzilla_lite.TxEnvelope} TxEnvelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TxEnvelope.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.yahboom_dogzilla_lite.TxEnvelope();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.monotonicStampNs = reader.uint64();
                        break;
                    }
                case 2: {
                        message.localStampNs = reader.uint64();
                        break;
                    }
                case 3: {
                        message.appStartId = reader.uint64();
                        break;
                    }
                case 4: {
                        message.commandId = reader.bytes();
                        break;
                    }
                case 5: {
                        message.targetDeviceSerial = reader.string();
                        break;
                    }
                case 10: {
                        message.command = $root.yahboom_dogzilla_lite.Command.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a TxEnvelope message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof yahboom_dogzilla_lite.TxEnvelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {yahboom_dogzilla_lite.TxEnvelope} TxEnvelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TxEnvelope.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TxEnvelope message.
         * @function verify
         * @memberof yahboom_dogzilla_lite.TxEnvelope
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TxEnvelope.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (!$util.isInteger(message.monotonicStampNs) && !(message.monotonicStampNs && $util.isInteger(message.monotonicStampNs.low) && $util.isInteger(message.monotonicStampNs.high)))
                    return "monotonicStampNs: integer|Long expected";
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (!$util.isInteger(message.localStampNs) && !(message.localStampNs && $util.isInteger(message.localStampNs.low) && $util.isInteger(message.localStampNs.high)))
                    return "localStampNs: integer|Long expected";
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (!$util.isInteger(message.appStartId) && !(message.appStartId && $util.isInteger(message.appStartId.low) && $util.isInteger(message.appStartId.high)))
                    return "appStartId: integer|Long expected";
            if (message.commandId != null && message.hasOwnProperty("commandId"))
                if (!(message.commandId && typeof message.commandId.length === "number" || $util.isString(message.commandId)))
                    return "commandId: buffer expected";
            if (message.targetDeviceSerial != null && message.hasOwnProperty("targetDeviceSerial"))
                if (!$util.isString(message.targetDeviceSerial))
                    return "targetDeviceSerial: string expected";
            if (message.command != null && message.hasOwnProperty("command")) {
                let error = $root.yahboom_dogzilla_lite.Command.verify(message.command, long + 1);
                if (error)
                    return "command." + error;
            }
            return null;
        };

        /**
         * Creates a TxEnvelope message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof yahboom_dogzilla_lite.TxEnvelope
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {yahboom_dogzilla_lite.TxEnvelope} TxEnvelope
         */
        TxEnvelope.fromObject = function fromObject(object, long) {
            if (object instanceof $root.yahboom_dogzilla_lite.TxEnvelope)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.yahboom_dogzilla_lite.TxEnvelope();
            if (object.monotonicStampNs != null)
                if ($util.Long)
                    (message.monotonicStampNs = $util.Long.fromValue(object.monotonicStampNs)).unsigned = true;
                else if (typeof object.monotonicStampNs === "string")
                    message.monotonicStampNs = parseInt(object.monotonicStampNs, 10);
                else if (typeof object.monotonicStampNs === "number")
                    message.monotonicStampNs = object.monotonicStampNs;
                else if (typeof object.monotonicStampNs === "object")
                    message.monotonicStampNs = new $util.LongBits(object.monotonicStampNs.low >>> 0, object.monotonicStampNs.high >>> 0).toNumber(true);
            if (object.localStampNs != null)
                if ($util.Long)
                    (message.localStampNs = $util.Long.fromValue(object.localStampNs)).unsigned = true;
                else if (typeof object.localStampNs === "string")
                    message.localStampNs = parseInt(object.localStampNs, 10);
                else if (typeof object.localStampNs === "number")
                    message.localStampNs = object.localStampNs;
                else if (typeof object.localStampNs === "object")
                    message.localStampNs = new $util.LongBits(object.localStampNs.low >>> 0, object.localStampNs.high >>> 0).toNumber(true);
            if (object.appStartId != null)
                if ($util.Long)
                    (message.appStartId = $util.Long.fromValue(object.appStartId)).unsigned = true;
                else if (typeof object.appStartId === "string")
                    message.appStartId = parseInt(object.appStartId, 10);
                else if (typeof object.appStartId === "number")
                    message.appStartId = object.appStartId;
                else if (typeof object.appStartId === "object")
                    message.appStartId = new $util.LongBits(object.appStartId.low >>> 0, object.appStartId.high >>> 0).toNumber(true);
            if (object.commandId != null)
                if (typeof object.commandId === "string")
                    $util.base64.decode(object.commandId, message.commandId = $util.newBuffer($util.base64.length(object.commandId)), 0);
                else if (object.commandId.length >= 0)
                    message.commandId = object.commandId;
            if (object.targetDeviceSerial != null)
                message.targetDeviceSerial = String(object.targetDeviceSerial);
            if (object.command != null) {
                if (typeof object.command !== "object")
                    throw TypeError(".yahboom_dogzilla_lite.TxEnvelope.command: object expected");
                message.command = $root.yahboom_dogzilla_lite.Command.fromObject(object.command, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a TxEnvelope message. Also converts values to other types if specified.
         * @function toObject
         * @memberof yahboom_dogzilla_lite.TxEnvelope
         * @static
         * @param {yahboom_dogzilla_lite.TxEnvelope} message TxEnvelope
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TxEnvelope.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.monotonicStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.monotonicStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.localStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.localStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.appStartId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.appStartId = options.longs === String ? "0" : 0;
                if (options.bytes === String)
                    object.commandId = "";
                else {
                    object.commandId = [];
                    if (options.bytes !== Array)
                        object.commandId = $util.newBuffer(object.commandId);
                }
                object.targetDeviceSerial = "";
                object.command = null;
            }
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (typeof message.monotonicStampNs === "number")
                    object.monotonicStampNs = options.longs === String ? String(message.monotonicStampNs) : message.monotonicStampNs;
                else
                    object.monotonicStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.monotonicStampNs) : options.longs === Number ? new $util.LongBits(message.monotonicStampNs.low >>> 0, message.monotonicStampNs.high >>> 0).toNumber(true) : message.monotonicStampNs;
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (typeof message.localStampNs === "number")
                    object.localStampNs = options.longs === String ? String(message.localStampNs) : message.localStampNs;
                else
                    object.localStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.localStampNs) : options.longs === Number ? new $util.LongBits(message.localStampNs.low >>> 0, message.localStampNs.high >>> 0).toNumber(true) : message.localStampNs;
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (typeof message.appStartId === "number")
                    object.appStartId = options.longs === String ? String(message.appStartId) : message.appStartId;
                else
                    object.appStartId = options.longs === String ? $util.Long.prototype.toString.call(message.appStartId) : options.longs === Number ? new $util.LongBits(message.appStartId.low >>> 0, message.appStartId.high >>> 0).toNumber(true) : message.appStartId;
            if (message.commandId != null && message.hasOwnProperty("commandId"))
                object.commandId = options.bytes === String ? $util.base64.encode(message.commandId, 0, message.commandId.length) : options.bytes === Array ? Array.prototype.slice.call(message.commandId) : message.commandId;
            if (message.targetDeviceSerial != null && message.hasOwnProperty("targetDeviceSerial"))
                object.targetDeviceSerial = message.targetDeviceSerial;
            if (message.command != null && message.hasOwnProperty("command"))
                object.command = $root.yahboom_dogzilla_lite.Command.toObject(message.command, options);
            return object;
        };

        /**
         * Converts this TxEnvelope to JSON.
         * @function toJSON
         * @memberof yahboom_dogzilla_lite.TxEnvelope
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TxEnvelope.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for TxEnvelope
         * @function getTypeUrl
         * @memberof yahboom_dogzilla_lite.TxEnvelope
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        TxEnvelope.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/yahboom_dogzilla_lite.TxEnvelope";
        };

        return TxEnvelope;
    })();

    yahboom_dogzilla_lite.RxEnvelope = (function() {

        /**
         * Properties of a RxEnvelope.
         * @memberof yahboom_dogzilla_lite
         * @interface IRxEnvelope
         * @property {Long|null} [monotonicStampNs] RxEnvelope monotonicStampNs
         * @property {Long|null} [localStampNs] RxEnvelope localStampNs
         * @property {Long|null} [appStartId] RxEnvelope appStartId
         * @property {yahboom_dogzilla_lite.YahboomDogzillaLiteSignalType|null} [signalType] RxEnvelope signalType
         * @property {yahboom_dogzilla_lite.IYahboomDogzillaLiteDevice|null} [device] RxEnvelope device
         * @property {yahboom_dogzilla_lite.IYahboomDogzillaLiteStatus|null} [status] RxEnvelope status
         * @property {Uint8Array|null} [data] RxEnvelope data
         * @property {yahboom_dogzilla_lite.ITxEnvelope|null} [command] RxEnvelope command
         * @property {string|null} [errorMessage] RxEnvelope errorMessage
         */

        /**
         * Constructs a new RxEnvelope.
         * @memberof yahboom_dogzilla_lite
         * @classdesc Represents a RxEnvelope.
         * @implements IRxEnvelope
         * @constructor
         * @param {yahboom_dogzilla_lite.IRxEnvelope=} [properties] Properties to set
         */
        function RxEnvelope(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RxEnvelope monotonicStampNs.
         * @member {Long} monotonicStampNs
         * @memberof yahboom_dogzilla_lite.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.monotonicStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * RxEnvelope localStampNs.
         * @member {Long} localStampNs
         * @memberof yahboom_dogzilla_lite.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.localStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * RxEnvelope appStartId.
         * @member {Long} appStartId
         * @memberof yahboom_dogzilla_lite.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.appStartId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * RxEnvelope signalType.
         * @member {yahboom_dogzilla_lite.YahboomDogzillaLiteSignalType} signalType
         * @memberof yahboom_dogzilla_lite.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.signalType = 0;

        /**
         * RxEnvelope device.
         * @member {yahboom_dogzilla_lite.IYahboomDogzillaLiteDevice|null|undefined} device
         * @memberof yahboom_dogzilla_lite.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.device = null;

        /**
         * RxEnvelope status.
         * @member {yahboom_dogzilla_lite.IYahboomDogzillaLiteStatus|null|undefined} status
         * @memberof yahboom_dogzilla_lite.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.status = null;

        /**
         * RxEnvelope data.
         * @member {Uint8Array} data
         * @memberof yahboom_dogzilla_lite.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.data = $util.newBuffer([]);

        /**
         * RxEnvelope command.
         * @member {yahboom_dogzilla_lite.ITxEnvelope|null|undefined} command
         * @memberof yahboom_dogzilla_lite.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.command = null;

        /**
         * RxEnvelope errorMessage.
         * @member {string} errorMessage
         * @memberof yahboom_dogzilla_lite.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.errorMessage = "";

        /**
         * Creates a new RxEnvelope instance using the specified properties.
         * @function create
         * @memberof yahboom_dogzilla_lite.RxEnvelope
         * @static
         * @param {yahboom_dogzilla_lite.IRxEnvelope=} [properties] Properties to set
         * @returns {yahboom_dogzilla_lite.RxEnvelope} RxEnvelope instance
         */
        RxEnvelope.create = function create(properties) {
            return new RxEnvelope(properties);
        };

        /**
         * Encodes the specified RxEnvelope message. Does not implicitly {@link yahboom_dogzilla_lite.RxEnvelope.verify|verify} messages.
         * @function encode
         * @memberof yahboom_dogzilla_lite.RxEnvelope
         * @static
         * @param {yahboom_dogzilla_lite.IRxEnvelope} message RxEnvelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RxEnvelope.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.monotonicStampNs != null && Object.hasOwnProperty.call(message, "monotonicStampNs"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.monotonicStampNs);
            if (message.localStampNs != null && Object.hasOwnProperty.call(message, "localStampNs"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.localStampNs);
            if (message.appStartId != null && Object.hasOwnProperty.call(message, "appStartId"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.appStartId);
            if (message.signalType != null && Object.hasOwnProperty.call(message, "signalType"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.signalType);
            if (message.device != null && Object.hasOwnProperty.call(message, "device"))
                $root.yahboom_dogzilla_lite.YahboomDogzillaLiteDevice.encode(message.device, writer.uint32(/* id 11, wireType 2 =*/90).fork()).ldelim();
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                $root.yahboom_dogzilla_lite.YahboomDogzillaLiteStatus.encode(message.status, writer.uint32(/* id 12, wireType 2 =*/98).fork()).ldelim();
            if (message.data != null && Object.hasOwnProperty.call(message, "data"))
                writer.uint32(/* id 20, wireType 2 =*/162).bytes(message.data);
            if (message.command != null && Object.hasOwnProperty.call(message, "command"))
                $root.yahboom_dogzilla_lite.TxEnvelope.encode(message.command, writer.uint32(/* id 30, wireType 2 =*/242).fork()).ldelim();
            if (message.errorMessage != null && Object.hasOwnProperty.call(message, "errorMessage"))
                writer.uint32(/* id 50, wireType 2 =*/402).string(message.errorMessage);
            return writer;
        };

        /**
         * Encodes the specified RxEnvelope message, length delimited. Does not implicitly {@link yahboom_dogzilla_lite.RxEnvelope.verify|verify} messages.
         * @function encodeDelimited
         * @memberof yahboom_dogzilla_lite.RxEnvelope
         * @static
         * @param {yahboom_dogzilla_lite.IRxEnvelope} message RxEnvelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RxEnvelope.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RxEnvelope message from the specified reader or buffer.
         * @function decode
         * @memberof yahboom_dogzilla_lite.RxEnvelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {yahboom_dogzilla_lite.RxEnvelope} RxEnvelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RxEnvelope.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.yahboom_dogzilla_lite.RxEnvelope();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.monotonicStampNs = reader.uint64();
                        break;
                    }
                case 2: {
                        message.localStampNs = reader.uint64();
                        break;
                    }
                case 3: {
                        message.appStartId = reader.uint64();
                        break;
                    }
                case 10: {
                        message.signalType = reader.int32();
                        break;
                    }
                case 11: {
                        message.device = $root.yahboom_dogzilla_lite.YahboomDogzillaLiteDevice.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 12: {
                        message.status = $root.yahboom_dogzilla_lite.YahboomDogzillaLiteStatus.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 20: {
                        message.data = reader.bytes();
                        break;
                    }
                case 30: {
                        message.command = $root.yahboom_dogzilla_lite.TxEnvelope.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 50: {
                        message.errorMessage = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RxEnvelope message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof yahboom_dogzilla_lite.RxEnvelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {yahboom_dogzilla_lite.RxEnvelope} RxEnvelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RxEnvelope.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RxEnvelope message.
         * @function verify
         * @memberof yahboom_dogzilla_lite.RxEnvelope
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RxEnvelope.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (!$util.isInteger(message.monotonicStampNs) && !(message.monotonicStampNs && $util.isInteger(message.monotonicStampNs.low) && $util.isInteger(message.monotonicStampNs.high)))
                    return "monotonicStampNs: integer|Long expected";
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (!$util.isInteger(message.localStampNs) && !(message.localStampNs && $util.isInteger(message.localStampNs.low) && $util.isInteger(message.localStampNs.high)))
                    return "localStampNs: integer|Long expected";
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (!$util.isInteger(message.appStartId) && !(message.appStartId && $util.isInteger(message.appStartId.low) && $util.isInteger(message.appStartId.high)))
                    return "appStartId: integer|Long expected";
            if (message.signalType != null && message.hasOwnProperty("signalType"))
                switch (message.signalType) {
                default:
                    return "signalType: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                case 5:
                case 6:
                case 7:
                    break;
                }
            if (message.device != null && message.hasOwnProperty("device")) {
                let error = $root.yahboom_dogzilla_lite.YahboomDogzillaLiteDevice.verify(message.device, long + 1);
                if (error)
                    return "device." + error;
            }
            if (message.status != null && message.hasOwnProperty("status")) {
                let error = $root.yahboom_dogzilla_lite.YahboomDogzillaLiteStatus.verify(message.status, long + 1);
                if (error)
                    return "status." + error;
            }
            if (message.data != null && message.hasOwnProperty("data"))
                if (!(message.data && typeof message.data.length === "number" || $util.isString(message.data)))
                    return "data: buffer expected";
            if (message.command != null && message.hasOwnProperty("command")) {
                let error = $root.yahboom_dogzilla_lite.TxEnvelope.verify(message.command, long + 1);
                if (error)
                    return "command." + error;
            }
            if (message.errorMessage != null && message.hasOwnProperty("errorMessage"))
                if (!$util.isString(message.errorMessage))
                    return "errorMessage: string expected";
            return null;
        };

        /**
         * Creates a RxEnvelope message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof yahboom_dogzilla_lite.RxEnvelope
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {yahboom_dogzilla_lite.RxEnvelope} RxEnvelope
         */
        RxEnvelope.fromObject = function fromObject(object, long) {
            if (object instanceof $root.yahboom_dogzilla_lite.RxEnvelope)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.yahboom_dogzilla_lite.RxEnvelope();
            if (object.monotonicStampNs != null)
                if ($util.Long)
                    (message.monotonicStampNs = $util.Long.fromValue(object.monotonicStampNs)).unsigned = true;
                else if (typeof object.monotonicStampNs === "string")
                    message.monotonicStampNs = parseInt(object.monotonicStampNs, 10);
                else if (typeof object.monotonicStampNs === "number")
                    message.monotonicStampNs = object.monotonicStampNs;
                else if (typeof object.monotonicStampNs === "object")
                    message.monotonicStampNs = new $util.LongBits(object.monotonicStampNs.low >>> 0, object.monotonicStampNs.high >>> 0).toNumber(true);
            if (object.localStampNs != null)
                if ($util.Long)
                    (message.localStampNs = $util.Long.fromValue(object.localStampNs)).unsigned = true;
                else if (typeof object.localStampNs === "string")
                    message.localStampNs = parseInt(object.localStampNs, 10);
                else if (typeof object.localStampNs === "number")
                    message.localStampNs = object.localStampNs;
                else if (typeof object.localStampNs === "object")
                    message.localStampNs = new $util.LongBits(object.localStampNs.low >>> 0, object.localStampNs.high >>> 0).toNumber(true);
            if (object.appStartId != null)
                if ($util.Long)
                    (message.appStartId = $util.Long.fromValue(object.appStartId)).unsigned = true;
                else if (typeof object.appStartId === "string")
                    message.appStartId = parseInt(object.appStartId, 10);
                else if (typeof object.appStartId === "number")
                    message.appStartId = object.appStartId;
                else if (typeof object.appStartId === "object")
                    message.appStartId = new $util.LongBits(object.appStartId.low >>> 0, object.appStartId.high >>> 0).toNumber(true);
            switch (object.signalType) {
            default:
                if (typeof object.signalType === "number") {
                    message.signalType = object.signalType;
                    break;
                }
                break;
            case "YAHBOOM_DOGZILLA_LITE_SIGNAL_TYPE_UNSPECIFIED":
            case 0:
                message.signalType = 0;
                break;
            case "YAHBOOM_DOGZILLA_LITE_CONNECTED":
            case 1:
                message.signalType = 1;
                break;
            case "YAHBOOM_DOGZILLA_LITE_DISCONNECTED":
            case 2:
                message.signalType = 2;
                break;
            case "YAHBOOM_DOGZILLA_LITE_STATUS_UPDATE":
            case 3:
                message.signalType = 3;
                break;
            case "YAHBOOM_DOGZILLA_LITE_COMMAND":
            case 4:
                message.signalType = 4;
                break;
            case "YAHBOOM_DOGZILLA_LITE_COMMAND_SUCCESS":
            case 5:
                message.signalType = 5;
                break;
            case "YAHBOOM_DOGZILLA_LITE_COMMAND_FAILED":
            case 6:
                message.signalType = 6;
                break;
            case "YAHBOOM_DOGZILLA_LITE_ERROR":
            case 7:
                message.signalType = 7;
                break;
            }
            if (object.device != null) {
                if (typeof object.device !== "object")
                    throw TypeError(".yahboom_dogzilla_lite.RxEnvelope.device: object expected");
                message.device = $root.yahboom_dogzilla_lite.YahboomDogzillaLiteDevice.fromObject(object.device, long + 1);
            }
            if (object.status != null) {
                if (typeof object.status !== "object")
                    throw TypeError(".yahboom_dogzilla_lite.RxEnvelope.status: object expected");
                message.status = $root.yahboom_dogzilla_lite.YahboomDogzillaLiteStatus.fromObject(object.status, long + 1);
            }
            if (object.data != null)
                if (typeof object.data === "string")
                    $util.base64.decode(object.data, message.data = $util.newBuffer($util.base64.length(object.data)), 0);
                else if (object.data.length >= 0)
                    message.data = object.data;
            if (object.command != null) {
                if (typeof object.command !== "object")
                    throw TypeError(".yahboom_dogzilla_lite.RxEnvelope.command: object expected");
                message.command = $root.yahboom_dogzilla_lite.TxEnvelope.fromObject(object.command, long + 1);
            }
            if (object.errorMessage != null)
                message.errorMessage = String(object.errorMessage);
            return message;
        };

        /**
         * Creates a plain object from a RxEnvelope message. Also converts values to other types if specified.
         * @function toObject
         * @memberof yahboom_dogzilla_lite.RxEnvelope
         * @static
         * @param {yahboom_dogzilla_lite.RxEnvelope} message RxEnvelope
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RxEnvelope.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.monotonicStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.monotonicStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.localStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.localStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.appStartId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.appStartId = options.longs === String ? "0" : 0;
                object.signalType = options.enums === String ? "YAHBOOM_DOGZILLA_LITE_SIGNAL_TYPE_UNSPECIFIED" : 0;
                object.device = null;
                object.status = null;
                if (options.bytes === String)
                    object.data = "";
                else {
                    object.data = [];
                    if (options.bytes !== Array)
                        object.data = $util.newBuffer(object.data);
                }
                object.command = null;
                object.errorMessage = "";
            }
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (typeof message.monotonicStampNs === "number")
                    object.monotonicStampNs = options.longs === String ? String(message.monotonicStampNs) : message.monotonicStampNs;
                else
                    object.monotonicStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.monotonicStampNs) : options.longs === Number ? new $util.LongBits(message.monotonicStampNs.low >>> 0, message.monotonicStampNs.high >>> 0).toNumber(true) : message.monotonicStampNs;
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (typeof message.localStampNs === "number")
                    object.localStampNs = options.longs === String ? String(message.localStampNs) : message.localStampNs;
                else
                    object.localStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.localStampNs) : options.longs === Number ? new $util.LongBits(message.localStampNs.low >>> 0, message.localStampNs.high >>> 0).toNumber(true) : message.localStampNs;
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (typeof message.appStartId === "number")
                    object.appStartId = options.longs === String ? String(message.appStartId) : message.appStartId;
                else
                    object.appStartId = options.longs === String ? $util.Long.prototype.toString.call(message.appStartId) : options.longs === Number ? new $util.LongBits(message.appStartId.low >>> 0, message.appStartId.high >>> 0).toNumber(true) : message.appStartId;
            if (message.signalType != null && message.hasOwnProperty("signalType"))
                object.signalType = options.enums === String ? $root.yahboom_dogzilla_lite.YahboomDogzillaLiteSignalType[message.signalType] === undefined ? message.signalType : $root.yahboom_dogzilla_lite.YahboomDogzillaLiteSignalType[message.signalType] : message.signalType;
            if (message.device != null && message.hasOwnProperty("device"))
                object.device = $root.yahboom_dogzilla_lite.YahboomDogzillaLiteDevice.toObject(message.device, options);
            if (message.status != null && message.hasOwnProperty("status"))
                object.status = $root.yahboom_dogzilla_lite.YahboomDogzillaLiteStatus.toObject(message.status, options);
            if (message.data != null && message.hasOwnProperty("data"))
                object.data = options.bytes === String ? $util.base64.encode(message.data, 0, message.data.length) : options.bytes === Array ? Array.prototype.slice.call(message.data) : message.data;
            if (message.command != null && message.hasOwnProperty("command"))
                object.command = $root.yahboom_dogzilla_lite.TxEnvelope.toObject(message.command, options);
            if (message.errorMessage != null && message.hasOwnProperty("errorMessage"))
                object.errorMessage = message.errorMessage;
            return object;
        };

        /**
         * Converts this RxEnvelope to JSON.
         * @function toJSON
         * @memberof yahboom_dogzilla_lite.RxEnvelope
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RxEnvelope.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RxEnvelope
         * @function getTypeUrl
         * @memberof yahboom_dogzilla_lite.RxEnvelope
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RxEnvelope.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/yahboom_dogzilla_lite.RxEnvelope";
        };

        return RxEnvelope;
    })();

    yahboom_dogzilla_lite.InferenceState = (function() {

        /**
         * Properties of an InferenceState.
         * @memberof yahboom_dogzilla_lite
         * @interface IInferenceState
         * @property {Uint8Array|null} [lastInferenceQueuePtr] InferenceState lastInferenceQueuePtr
         * @property {Array.<yahboom_dogzilla_lite.InferenceState.IDeviceState>|null} [devices] InferenceState devices
         */

        /**
         * Constructs a new InferenceState.
         * @memberof yahboom_dogzilla_lite
         * @classdesc Represents an InferenceState.
         * @implements IInferenceState
         * @constructor
         * @param {yahboom_dogzilla_lite.IInferenceState=} [properties] Properties to set
         */
        function InferenceState(properties) {
            this.devices = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * InferenceState lastInferenceQueuePtr.
         * @member {Uint8Array} lastInferenceQueuePtr
         * @memberof yahboom_dogzilla_lite.InferenceState
         * @instance
         */
        InferenceState.prototype.lastInferenceQueuePtr = $util.newBuffer([]);

        /**
         * InferenceState devices.
         * @member {Array.<yahboom_dogzilla_lite.InferenceState.IDeviceState>} devices
         * @memberof yahboom_dogzilla_lite.InferenceState
         * @instance
         */
        InferenceState.prototype.devices = $util.emptyArray;

        /**
         * Creates a new InferenceState instance using the specified properties.
         * @function create
         * @memberof yahboom_dogzilla_lite.InferenceState
         * @static
         * @param {yahboom_dogzilla_lite.IInferenceState=} [properties] Properties to set
         * @returns {yahboom_dogzilla_lite.InferenceState} InferenceState instance
         */
        InferenceState.create = function create(properties) {
            return new InferenceState(properties);
        };

        /**
         * Encodes the specified InferenceState message. Does not implicitly {@link yahboom_dogzilla_lite.InferenceState.verify|verify} messages.
         * @function encode
         * @memberof yahboom_dogzilla_lite.InferenceState
         * @static
         * @param {yahboom_dogzilla_lite.IInferenceState} message InferenceState message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        InferenceState.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.lastInferenceQueuePtr != null && Object.hasOwnProperty.call(message, "lastInferenceQueuePtr"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.lastInferenceQueuePtr);
            if (message.devices != null && message.devices.length)
                for (let i = 0; i < message.devices.length; ++i)
                    $root.yahboom_dogzilla_lite.InferenceState.DeviceState.encode(message.devices[i], writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified InferenceState message, length delimited. Does not implicitly {@link yahboom_dogzilla_lite.InferenceState.verify|verify} messages.
         * @function encodeDelimited
         * @memberof yahboom_dogzilla_lite.InferenceState
         * @static
         * @param {yahboom_dogzilla_lite.IInferenceState} message InferenceState message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        InferenceState.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an InferenceState message from the specified reader or buffer.
         * @function decode
         * @memberof yahboom_dogzilla_lite.InferenceState
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {yahboom_dogzilla_lite.InferenceState} InferenceState
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        InferenceState.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.yahboom_dogzilla_lite.InferenceState();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.lastInferenceQueuePtr = reader.bytes();
                        break;
                    }
                case 10: {
                        if (!(message.devices && message.devices.length))
                            message.devices = [];
                        message.devices.push($root.yahboom_dogzilla_lite.InferenceState.DeviceState.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an InferenceState message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof yahboom_dogzilla_lite.InferenceState
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {yahboom_dogzilla_lite.InferenceState} InferenceState
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        InferenceState.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an InferenceState message.
         * @function verify
         * @memberof yahboom_dogzilla_lite.InferenceState
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        InferenceState.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.lastInferenceQueuePtr != null && message.hasOwnProperty("lastInferenceQueuePtr"))
                if (!(message.lastInferenceQueuePtr && typeof message.lastInferenceQueuePtr.length === "number" || $util.isString(message.lastInferenceQueuePtr)))
                    return "lastInferenceQueuePtr: buffer expected";
            if (message.devices != null && message.hasOwnProperty("devices")) {
                if (!Array.isArray(message.devices))
                    return "devices: array expected";
                for (let i = 0; i < message.devices.length; ++i) {
                    let error = $root.yahboom_dogzilla_lite.InferenceState.DeviceState.verify(message.devices[i], long + 1);
                    if (error)
                        return "devices." + error;
                }
            }
            return null;
        };

        /**
         * Creates an InferenceState message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof yahboom_dogzilla_lite.InferenceState
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {yahboom_dogzilla_lite.InferenceState} InferenceState
         */
        InferenceState.fromObject = function fromObject(object, long) {
            if (object instanceof $root.yahboom_dogzilla_lite.InferenceState)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.yahboom_dogzilla_lite.InferenceState();
            if (object.lastInferenceQueuePtr != null)
                if (typeof object.lastInferenceQueuePtr === "string")
                    $util.base64.decode(object.lastInferenceQueuePtr, message.lastInferenceQueuePtr = $util.newBuffer($util.base64.length(object.lastInferenceQueuePtr)), 0);
                else if (object.lastInferenceQueuePtr.length >= 0)
                    message.lastInferenceQueuePtr = object.lastInferenceQueuePtr;
            if (object.devices) {
                if (!Array.isArray(object.devices))
                    throw TypeError(".yahboom_dogzilla_lite.InferenceState.devices: array expected");
                message.devices = [];
                for (let i = 0; i < object.devices.length; ++i) {
                    if (typeof object.devices[i] !== "object")
                        throw TypeError(".yahboom_dogzilla_lite.InferenceState.devices: object expected");
                    message.devices[i] = $root.yahboom_dogzilla_lite.InferenceState.DeviceState.fromObject(object.devices[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from an InferenceState message. Also converts values to other types if specified.
         * @function toObject
         * @memberof yahboom_dogzilla_lite.InferenceState
         * @static
         * @param {yahboom_dogzilla_lite.InferenceState} message InferenceState
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        InferenceState.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.devices = [];
            if (options.defaults)
                if (options.bytes === String)
                    object.lastInferenceQueuePtr = "";
                else {
                    object.lastInferenceQueuePtr = [];
                    if (options.bytes !== Array)
                        object.lastInferenceQueuePtr = $util.newBuffer(object.lastInferenceQueuePtr);
                }
            if (message.lastInferenceQueuePtr != null && message.hasOwnProperty("lastInferenceQueuePtr"))
                object.lastInferenceQueuePtr = options.bytes === String ? $util.base64.encode(message.lastInferenceQueuePtr, 0, message.lastInferenceQueuePtr.length) : options.bytes === Array ? Array.prototype.slice.call(message.lastInferenceQueuePtr) : message.lastInferenceQueuePtr;
            if (message.devices && message.devices.length) {
                object.devices = [];
                for (let j = 0; j < message.devices.length; ++j)
                    object.devices[j] = $root.yahboom_dogzilla_lite.InferenceState.DeviceState.toObject(message.devices[j], options);
            }
            return object;
        };

        /**
         * Converts this InferenceState to JSON.
         * @function toJSON
         * @memberof yahboom_dogzilla_lite.InferenceState
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        InferenceState.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for InferenceState
         * @function getTypeUrl
         * @memberof yahboom_dogzilla_lite.InferenceState
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        InferenceState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/yahboom_dogzilla_lite.InferenceState";
        };

        InferenceState.DeviceState = (function() {

            /**
             * Properties of a DeviceState.
             * @memberof yahboom_dogzilla_lite.InferenceState
             * @interface IDeviceState
             * @property {yahboom_dogzilla_lite.IYahboomDogzillaLiteDevice|null} [device] DeviceState device
             * @property {yahboom_dogzilla_lite.IYahboomDogzillaLiteStatus|null} [status] DeviceState status
             * @property {Long|null} [monotonicStampNs] DeviceState monotonicStampNs
             * @property {Long|null} [systemStampNs] DeviceState systemStampNs
             * @property {boolean|null} [isConnected] DeviceState isConnected
             */

            /**
             * Constructs a new DeviceState.
             * @memberof yahboom_dogzilla_lite.InferenceState
             * @classdesc Represents a DeviceState.
             * @implements IDeviceState
             * @constructor
             * @param {yahboom_dogzilla_lite.InferenceState.IDeviceState=} [properties] Properties to set
             */
            function DeviceState(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * DeviceState device.
             * @member {yahboom_dogzilla_lite.IYahboomDogzillaLiteDevice|null|undefined} device
             * @memberof yahboom_dogzilla_lite.InferenceState.DeviceState
             * @instance
             */
            DeviceState.prototype.device = null;

            /**
             * DeviceState status.
             * @member {yahboom_dogzilla_lite.IYahboomDogzillaLiteStatus|null|undefined} status
             * @memberof yahboom_dogzilla_lite.InferenceState.DeviceState
             * @instance
             */
            DeviceState.prototype.status = null;

            /**
             * DeviceState monotonicStampNs.
             * @member {Long} monotonicStampNs
             * @memberof yahboom_dogzilla_lite.InferenceState.DeviceState
             * @instance
             */
            DeviceState.prototype.monotonicStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

            /**
             * DeviceState systemStampNs.
             * @member {Long} systemStampNs
             * @memberof yahboom_dogzilla_lite.InferenceState.DeviceState
             * @instance
             */
            DeviceState.prototype.systemStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

            /**
             * DeviceState isConnected.
             * @member {boolean} isConnected
             * @memberof yahboom_dogzilla_lite.InferenceState.DeviceState
             * @instance
             */
            DeviceState.prototype.isConnected = false;

            /**
             * Creates a new DeviceState instance using the specified properties.
             * @function create
             * @memberof yahboom_dogzilla_lite.InferenceState.DeviceState
             * @static
             * @param {yahboom_dogzilla_lite.InferenceState.IDeviceState=} [properties] Properties to set
             * @returns {yahboom_dogzilla_lite.InferenceState.DeviceState} DeviceState instance
             */
            DeviceState.create = function create(properties) {
                return new DeviceState(properties);
            };

            /**
             * Encodes the specified DeviceState message. Does not implicitly {@link yahboom_dogzilla_lite.InferenceState.DeviceState.verify|verify} messages.
             * @function encode
             * @memberof yahboom_dogzilla_lite.InferenceState.DeviceState
             * @static
             * @param {yahboom_dogzilla_lite.InferenceState.IDeviceState} message DeviceState message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DeviceState.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.device != null && Object.hasOwnProperty.call(message, "device"))
                    $root.yahboom_dogzilla_lite.YahboomDogzillaLiteDevice.encode(message.device, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
                if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                    $root.yahboom_dogzilla_lite.YahboomDogzillaLiteStatus.encode(message.status, writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
                if (message.monotonicStampNs != null && Object.hasOwnProperty.call(message, "monotonicStampNs"))
                    writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.monotonicStampNs);
                if (message.systemStampNs != null && Object.hasOwnProperty.call(message, "systemStampNs"))
                    writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.systemStampNs);
                if (message.isConnected != null && Object.hasOwnProperty.call(message, "isConnected"))
                    writer.uint32(/* id 5, wireType 0 =*/40).bool(message.isConnected);
                return writer;
            };

            /**
             * Encodes the specified DeviceState message, length delimited. Does not implicitly {@link yahboom_dogzilla_lite.InferenceState.DeviceState.verify|verify} messages.
             * @function encodeDelimited
             * @memberof yahboom_dogzilla_lite.InferenceState.DeviceState
             * @static
             * @param {yahboom_dogzilla_lite.InferenceState.IDeviceState} message DeviceState message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DeviceState.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a DeviceState message from the specified reader or buffer.
             * @function decode
             * @memberof yahboom_dogzilla_lite.InferenceState.DeviceState
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {yahboom_dogzilla_lite.InferenceState.DeviceState} DeviceState
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DeviceState.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.yahboom_dogzilla_lite.InferenceState.DeviceState();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.device = $root.yahboom_dogzilla_lite.YahboomDogzillaLiteDevice.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 2: {
                            message.status = $root.yahboom_dogzilla_lite.YahboomDogzillaLiteStatus.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 3: {
                            message.monotonicStampNs = reader.uint64();
                            break;
                        }
                    case 4: {
                            message.systemStampNs = reader.uint64();
                            break;
                        }
                    case 5: {
                            message.isConnected = reader.bool();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a DeviceState message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof yahboom_dogzilla_lite.InferenceState.DeviceState
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {yahboom_dogzilla_lite.InferenceState.DeviceState} DeviceState
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DeviceState.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a DeviceState message.
             * @function verify
             * @memberof yahboom_dogzilla_lite.InferenceState.DeviceState
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            DeviceState.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.device != null && message.hasOwnProperty("device")) {
                    let error = $root.yahboom_dogzilla_lite.YahboomDogzillaLiteDevice.verify(message.device, long + 1);
                    if (error)
                        return "device." + error;
                }
                if (message.status != null && message.hasOwnProperty("status")) {
                    let error = $root.yahboom_dogzilla_lite.YahboomDogzillaLiteStatus.verify(message.status, long + 1);
                    if (error)
                        return "status." + error;
                }
                if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                    if (!$util.isInteger(message.monotonicStampNs) && !(message.monotonicStampNs && $util.isInteger(message.monotonicStampNs.low) && $util.isInteger(message.monotonicStampNs.high)))
                        return "monotonicStampNs: integer|Long expected";
                if (message.systemStampNs != null && message.hasOwnProperty("systemStampNs"))
                    if (!$util.isInteger(message.systemStampNs) && !(message.systemStampNs && $util.isInteger(message.systemStampNs.low) && $util.isInteger(message.systemStampNs.high)))
                        return "systemStampNs: integer|Long expected";
                if (message.isConnected != null && message.hasOwnProperty("isConnected"))
                    if (typeof message.isConnected !== "boolean")
                        return "isConnected: boolean expected";
                return null;
            };

            /**
             * Creates a DeviceState message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof yahboom_dogzilla_lite.InferenceState.DeviceState
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {yahboom_dogzilla_lite.InferenceState.DeviceState} DeviceState
             */
            DeviceState.fromObject = function fromObject(object, long) {
                if (object instanceof $root.yahboom_dogzilla_lite.InferenceState.DeviceState)
                    return object;
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.yahboom_dogzilla_lite.InferenceState.DeviceState();
                if (object.device != null) {
                    if (typeof object.device !== "object")
                        throw TypeError(".yahboom_dogzilla_lite.InferenceState.DeviceState.device: object expected");
                    message.device = $root.yahboom_dogzilla_lite.YahboomDogzillaLiteDevice.fromObject(object.device, long + 1);
                }
                if (object.status != null) {
                    if (typeof object.status !== "object")
                        throw TypeError(".yahboom_dogzilla_lite.InferenceState.DeviceState.status: object expected");
                    message.status = $root.yahboom_dogzilla_lite.YahboomDogzillaLiteStatus.fromObject(object.status, long + 1);
                }
                if (object.monotonicStampNs != null)
                    if ($util.Long)
                        (message.monotonicStampNs = $util.Long.fromValue(object.monotonicStampNs)).unsigned = true;
                    else if (typeof object.monotonicStampNs === "string")
                        message.monotonicStampNs = parseInt(object.monotonicStampNs, 10);
                    else if (typeof object.monotonicStampNs === "number")
                        message.monotonicStampNs = object.monotonicStampNs;
                    else if (typeof object.monotonicStampNs === "object")
                        message.monotonicStampNs = new $util.LongBits(object.monotonicStampNs.low >>> 0, object.monotonicStampNs.high >>> 0).toNumber(true);
                if (object.systemStampNs != null)
                    if ($util.Long)
                        (message.systemStampNs = $util.Long.fromValue(object.systemStampNs)).unsigned = true;
                    else if (typeof object.systemStampNs === "string")
                        message.systemStampNs = parseInt(object.systemStampNs, 10);
                    else if (typeof object.systemStampNs === "number")
                        message.systemStampNs = object.systemStampNs;
                    else if (typeof object.systemStampNs === "object")
                        message.systemStampNs = new $util.LongBits(object.systemStampNs.low >>> 0, object.systemStampNs.high >>> 0).toNumber(true);
                if (object.isConnected != null)
                    message.isConnected = Boolean(object.isConnected);
                return message;
            };

            /**
             * Creates a plain object from a DeviceState message. Also converts values to other types if specified.
             * @function toObject
             * @memberof yahboom_dogzilla_lite.InferenceState.DeviceState
             * @static
             * @param {yahboom_dogzilla_lite.InferenceState.DeviceState} message DeviceState
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            DeviceState.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.device = null;
                    object.status = null;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, true);
                        object.monotonicStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                    } else
                        object.monotonicStampNs = options.longs === String ? "0" : 0;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, true);
                        object.systemStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                    } else
                        object.systemStampNs = options.longs === String ? "0" : 0;
                    object.isConnected = false;
                }
                if (message.device != null && message.hasOwnProperty("device"))
                    object.device = $root.yahboom_dogzilla_lite.YahboomDogzillaLiteDevice.toObject(message.device, options);
                if (message.status != null && message.hasOwnProperty("status"))
                    object.status = $root.yahboom_dogzilla_lite.YahboomDogzillaLiteStatus.toObject(message.status, options);
                if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                    if (typeof message.monotonicStampNs === "number")
                        object.monotonicStampNs = options.longs === String ? String(message.monotonicStampNs) : message.monotonicStampNs;
                    else
                        object.monotonicStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.monotonicStampNs) : options.longs === Number ? new $util.LongBits(message.monotonicStampNs.low >>> 0, message.monotonicStampNs.high >>> 0).toNumber(true) : message.monotonicStampNs;
                if (message.systemStampNs != null && message.hasOwnProperty("systemStampNs"))
                    if (typeof message.systemStampNs === "number")
                        object.systemStampNs = options.longs === String ? String(message.systemStampNs) : message.systemStampNs;
                    else
                        object.systemStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.systemStampNs) : options.longs === Number ? new $util.LongBits(message.systemStampNs.low >>> 0, message.systemStampNs.high >>> 0).toNumber(true) : message.systemStampNs;
                if (message.isConnected != null && message.hasOwnProperty("isConnected"))
                    object.isConnected = message.isConnected;
                return object;
            };

            /**
             * Converts this DeviceState to JSON.
             * @function toJSON
             * @memberof yahboom_dogzilla_lite.InferenceState.DeviceState
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            DeviceState.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for DeviceState
             * @function getTypeUrl
             * @memberof yahboom_dogzilla_lite.InferenceState.DeviceState
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            DeviceState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/yahboom_dogzilla_lite.InferenceState.DeviceState";
            };

            return DeviceState;
        })();

        return InferenceState;
    })();

    /**
     * CommandResult enum.
     * @name yahboom_dogzilla_lite.CommandResult
     * @enum {number}
     * @property {number} CR_PROCESSING=0 CR_PROCESSING value
     * @property {number} CR_SUCCESS=1 CR_SUCCESS value
     * @property {number} CR_FAILED=2 CR_FAILED value
     */
    yahboom_dogzilla_lite.CommandResult = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "CR_PROCESSING"] = 0;
        values[valuesById[1] = "CR_SUCCESS"] = 1;
        values[valuesById[2] = "CR_FAILED"] = 2;
        return values;
    })();

    return yahboom_dogzilla_lite;
})();

export const sysinfo = $root.sysinfo = (() => {

    /**
     * Namespace sysinfo.
     * @exports sysinfo
     * @namespace
     */
    const sysinfo = {};

    sysinfo.Envelope = (function() {

        /**
         * Properties of an Envelope.
         * @memberof sysinfo
         * @interface IEnvelope
         * @property {Long|null} [monotonicStampNs] Envelope monotonicStampNs
         * @property {Long|null} [localStampNs] Envelope localStampNs
         * @property {Long|null} [appStartId] Envelope appStartId
         * @property {sysinfo.IEnvelopeData|null} [data] Envelope data
         */

        /**
         * Constructs a new Envelope.
         * @memberof sysinfo
         * @classdesc Represents an Envelope.
         * @implements IEnvelope
         * @constructor
         * @param {sysinfo.IEnvelope=} [properties] Properties to set
         */
        function Envelope(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Envelope monotonicStampNs.
         * @member {Long} monotonicStampNs
         * @memberof sysinfo.Envelope
         * @instance
         */
        Envelope.prototype.monotonicStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Envelope localStampNs.
         * @member {Long} localStampNs
         * @memberof sysinfo.Envelope
         * @instance
         */
        Envelope.prototype.localStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Envelope appStartId.
         * @member {Long} appStartId
         * @memberof sysinfo.Envelope
         * @instance
         */
        Envelope.prototype.appStartId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Envelope data.
         * @member {sysinfo.IEnvelopeData|null|undefined} data
         * @memberof sysinfo.Envelope
         * @instance
         */
        Envelope.prototype.data = null;

        /**
         * Creates a new Envelope instance using the specified properties.
         * @function create
         * @memberof sysinfo.Envelope
         * @static
         * @param {sysinfo.IEnvelope=} [properties] Properties to set
         * @returns {sysinfo.Envelope} Envelope instance
         */
        Envelope.create = function create(properties) {
            return new Envelope(properties);
        };

        /**
         * Encodes the specified Envelope message. Does not implicitly {@link sysinfo.Envelope.verify|verify} messages.
         * @function encode
         * @memberof sysinfo.Envelope
         * @static
         * @param {sysinfo.IEnvelope} message Envelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Envelope.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.monotonicStampNs != null && Object.hasOwnProperty.call(message, "monotonicStampNs"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.monotonicStampNs);
            if (message.localStampNs != null && Object.hasOwnProperty.call(message, "localStampNs"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.localStampNs);
            if (message.appStartId != null && Object.hasOwnProperty.call(message, "appStartId"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.appStartId);
            if (message.data != null && Object.hasOwnProperty.call(message, "data"))
                $root.sysinfo.EnvelopeData.encode(message.data, writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified Envelope message, length delimited. Does not implicitly {@link sysinfo.Envelope.verify|verify} messages.
         * @function encodeDelimited
         * @memberof sysinfo.Envelope
         * @static
         * @param {sysinfo.IEnvelope} message Envelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Envelope.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an Envelope message from the specified reader or buffer.
         * @function decode
         * @memberof sysinfo.Envelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {sysinfo.Envelope} Envelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Envelope.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.sysinfo.Envelope();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.monotonicStampNs = reader.uint64();
                        break;
                    }
                case 2: {
                        message.localStampNs = reader.uint64();
                        break;
                    }
                case 3: {
                        message.appStartId = reader.uint64();
                        break;
                    }
                case 10: {
                        message.data = $root.sysinfo.EnvelopeData.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an Envelope message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof sysinfo.Envelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {sysinfo.Envelope} Envelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Envelope.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an Envelope message.
         * @function verify
         * @memberof sysinfo.Envelope
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Envelope.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (!$util.isInteger(message.monotonicStampNs) && !(message.monotonicStampNs && $util.isInteger(message.monotonicStampNs.low) && $util.isInteger(message.monotonicStampNs.high)))
                    return "monotonicStampNs: integer|Long expected";
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (!$util.isInteger(message.localStampNs) && !(message.localStampNs && $util.isInteger(message.localStampNs.low) && $util.isInteger(message.localStampNs.high)))
                    return "localStampNs: integer|Long expected";
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (!$util.isInteger(message.appStartId) && !(message.appStartId && $util.isInteger(message.appStartId.low) && $util.isInteger(message.appStartId.high)))
                    return "appStartId: integer|Long expected";
            if (message.data != null && message.hasOwnProperty("data")) {
                let error = $root.sysinfo.EnvelopeData.verify(message.data, long + 1);
                if (error)
                    return "data." + error;
            }
            return null;
        };

        /**
         * Creates an Envelope message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof sysinfo.Envelope
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {sysinfo.Envelope} Envelope
         */
        Envelope.fromObject = function fromObject(object, long) {
            if (object instanceof $root.sysinfo.Envelope)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.sysinfo.Envelope();
            if (object.monotonicStampNs != null)
                if ($util.Long)
                    (message.monotonicStampNs = $util.Long.fromValue(object.monotonicStampNs)).unsigned = true;
                else if (typeof object.monotonicStampNs === "string")
                    message.monotonicStampNs = parseInt(object.monotonicStampNs, 10);
                else if (typeof object.monotonicStampNs === "number")
                    message.monotonicStampNs = object.monotonicStampNs;
                else if (typeof object.monotonicStampNs === "object")
                    message.monotonicStampNs = new $util.LongBits(object.monotonicStampNs.low >>> 0, object.monotonicStampNs.high >>> 0).toNumber(true);
            if (object.localStampNs != null)
                if ($util.Long)
                    (message.localStampNs = $util.Long.fromValue(object.localStampNs)).unsigned = true;
                else if (typeof object.localStampNs === "string")
                    message.localStampNs = parseInt(object.localStampNs, 10);
                else if (typeof object.localStampNs === "number")
                    message.localStampNs = object.localStampNs;
                else if (typeof object.localStampNs === "object")
                    message.localStampNs = new $util.LongBits(object.localStampNs.low >>> 0, object.localStampNs.high >>> 0).toNumber(true);
            if (object.appStartId != null)
                if ($util.Long)
                    (message.appStartId = $util.Long.fromValue(object.appStartId)).unsigned = true;
                else if (typeof object.appStartId === "string")
                    message.appStartId = parseInt(object.appStartId, 10);
                else if (typeof object.appStartId === "number")
                    message.appStartId = object.appStartId;
                else if (typeof object.appStartId === "object")
                    message.appStartId = new $util.LongBits(object.appStartId.low >>> 0, object.appStartId.high >>> 0).toNumber(true);
            if (object.data != null) {
                if (typeof object.data !== "object")
                    throw TypeError(".sysinfo.Envelope.data: object expected");
                message.data = $root.sysinfo.EnvelopeData.fromObject(object.data, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from an Envelope message. Also converts values to other types if specified.
         * @function toObject
         * @memberof sysinfo.Envelope
         * @static
         * @param {sysinfo.Envelope} message Envelope
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Envelope.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.monotonicStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.monotonicStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.localStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.localStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.appStartId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.appStartId = options.longs === String ? "0" : 0;
                object.data = null;
            }
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (typeof message.monotonicStampNs === "number")
                    object.monotonicStampNs = options.longs === String ? String(message.monotonicStampNs) : message.monotonicStampNs;
                else
                    object.monotonicStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.monotonicStampNs) : options.longs === Number ? new $util.LongBits(message.monotonicStampNs.low >>> 0, message.monotonicStampNs.high >>> 0).toNumber(true) : message.monotonicStampNs;
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (typeof message.localStampNs === "number")
                    object.localStampNs = options.longs === String ? String(message.localStampNs) : message.localStampNs;
                else
                    object.localStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.localStampNs) : options.longs === Number ? new $util.LongBits(message.localStampNs.low >>> 0, message.localStampNs.high >>> 0).toNumber(true) : message.localStampNs;
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (typeof message.appStartId === "number")
                    object.appStartId = options.longs === String ? String(message.appStartId) : message.appStartId;
                else
                    object.appStartId = options.longs === String ? $util.Long.prototype.toString.call(message.appStartId) : options.longs === Number ? new $util.LongBits(message.appStartId.low >>> 0, message.appStartId.high >>> 0).toNumber(true) : message.appStartId;
            if (message.data != null && message.hasOwnProperty("data"))
                object.data = $root.sysinfo.EnvelopeData.toObject(message.data, options);
            return object;
        };

        /**
         * Converts this Envelope to JSON.
         * @function toJSON
         * @memberof sysinfo.Envelope
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Envelope.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Envelope
         * @function getTypeUrl
         * @memberof sysinfo.Envelope
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Envelope.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/sysinfo.Envelope";
        };

        return Envelope;
    })();

    sysinfo.InferenceStatePtr = (function() {

        /**
         * Properties of an InferenceStatePtr.
         * @memberof sysinfo
         * @interface IInferenceStatePtr
         * @property {Array.<Uint8Array>|null} [rxPtrs] InferenceStatePtr rxPtrs
         */

        /**
         * Constructs a new InferenceStatePtr.
         * @memberof sysinfo
         * @classdesc Represents an InferenceStatePtr.
         * @implements IInferenceStatePtr
         * @constructor
         * @param {sysinfo.IInferenceStatePtr=} [properties] Properties to set
         */
        function InferenceStatePtr(properties) {
            this.rxPtrs = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * InferenceStatePtr rxPtrs.
         * @member {Array.<Uint8Array>} rxPtrs
         * @memberof sysinfo.InferenceStatePtr
         * @instance
         */
        InferenceStatePtr.prototype.rxPtrs = $util.emptyArray;

        /**
         * Creates a new InferenceStatePtr instance using the specified properties.
         * @function create
         * @memberof sysinfo.InferenceStatePtr
         * @static
         * @param {sysinfo.IInferenceStatePtr=} [properties] Properties to set
         * @returns {sysinfo.InferenceStatePtr} InferenceStatePtr instance
         */
        InferenceStatePtr.create = function create(properties) {
            return new InferenceStatePtr(properties);
        };

        /**
         * Encodes the specified InferenceStatePtr message. Does not implicitly {@link sysinfo.InferenceStatePtr.verify|verify} messages.
         * @function encode
         * @memberof sysinfo.InferenceStatePtr
         * @static
         * @param {sysinfo.IInferenceStatePtr} message InferenceStatePtr message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        InferenceStatePtr.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.rxPtrs != null && message.rxPtrs.length)
                for (let i = 0; i < message.rxPtrs.length; ++i)
                    writer.uint32(/* id 10, wireType 2 =*/82).bytes(message.rxPtrs[i]);
            return writer;
        };

        /**
         * Encodes the specified InferenceStatePtr message, length delimited. Does not implicitly {@link sysinfo.InferenceStatePtr.verify|verify} messages.
         * @function encodeDelimited
         * @memberof sysinfo.InferenceStatePtr
         * @static
         * @param {sysinfo.IInferenceStatePtr} message InferenceStatePtr message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        InferenceStatePtr.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an InferenceStatePtr message from the specified reader or buffer.
         * @function decode
         * @memberof sysinfo.InferenceStatePtr
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {sysinfo.InferenceStatePtr} InferenceStatePtr
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        InferenceStatePtr.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.sysinfo.InferenceStatePtr();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 10: {
                        if (!(message.rxPtrs && message.rxPtrs.length))
                            message.rxPtrs = [];
                        message.rxPtrs.push(reader.bytes());
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an InferenceStatePtr message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof sysinfo.InferenceStatePtr
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {sysinfo.InferenceStatePtr} InferenceStatePtr
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        InferenceStatePtr.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an InferenceStatePtr message.
         * @function verify
         * @memberof sysinfo.InferenceStatePtr
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        InferenceStatePtr.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.rxPtrs != null && message.hasOwnProperty("rxPtrs")) {
                if (!Array.isArray(message.rxPtrs))
                    return "rxPtrs: array expected";
                for (let i = 0; i < message.rxPtrs.length; ++i)
                    if (!(message.rxPtrs[i] && typeof message.rxPtrs[i].length === "number" || $util.isString(message.rxPtrs[i])))
                        return "rxPtrs: buffer[] expected";
            }
            return null;
        };

        /**
         * Creates an InferenceStatePtr message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof sysinfo.InferenceStatePtr
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {sysinfo.InferenceStatePtr} InferenceStatePtr
         */
        InferenceStatePtr.fromObject = function fromObject(object, long) {
            if (object instanceof $root.sysinfo.InferenceStatePtr)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.sysinfo.InferenceStatePtr();
            if (object.rxPtrs) {
                if (!Array.isArray(object.rxPtrs))
                    throw TypeError(".sysinfo.InferenceStatePtr.rxPtrs: array expected");
                message.rxPtrs = [];
                for (let i = 0; i < object.rxPtrs.length; ++i)
                    if (typeof object.rxPtrs[i] === "string")
                        $util.base64.decode(object.rxPtrs[i], message.rxPtrs[i] = $util.newBuffer($util.base64.length(object.rxPtrs[i])), 0);
                    else if (object.rxPtrs[i].length >= 0)
                        message.rxPtrs[i] = object.rxPtrs[i];
            }
            return message;
        };

        /**
         * Creates a plain object from an InferenceStatePtr message. Also converts values to other types if specified.
         * @function toObject
         * @memberof sysinfo.InferenceStatePtr
         * @static
         * @param {sysinfo.InferenceStatePtr} message InferenceStatePtr
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        InferenceStatePtr.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.rxPtrs = [];
            if (message.rxPtrs && message.rxPtrs.length) {
                object.rxPtrs = [];
                for (let j = 0; j < message.rxPtrs.length; ++j)
                    object.rxPtrs[j] = options.bytes === String ? $util.base64.encode(message.rxPtrs[j], 0, message.rxPtrs[j].length) : options.bytes === Array ? Array.prototype.slice.call(message.rxPtrs[j]) : message.rxPtrs[j];
            }
            return object;
        };

        /**
         * Converts this InferenceStatePtr to JSON.
         * @function toJSON
         * @memberof sysinfo.InferenceStatePtr
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        InferenceStatePtr.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for InferenceStatePtr
         * @function getTypeUrl
         * @memberof sysinfo.InferenceStatePtr
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        InferenceStatePtr.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/sysinfo.InferenceStatePtr";
        };

        return InferenceStatePtr;
    })();

    sysinfo.EnvelopeData = (function() {

        /**
         * Properties of an EnvelopeData.
         * @memberof sysinfo
         * @interface IEnvelopeData
         * @property {sysinfo.IOsInfo|null} [os] EnvelopeData os
         * @property {sysinfo.ITimeInfo|null} [time] EnvelopeData time
         * @property {sysinfo.IMemory|null} [memory] EnvelopeData memory
         * @property {sysinfo.IMotherboard|null} [motherboard] EnvelopeData motherboard
         * @property {string|null} [hostname] EnvelopeData hostname
         * @property {string|null} [cpuArch] EnvelopeData cpuArch
         * @property {Long|null} [physicalCoreCount] EnvelopeData physicalCoreCount
         * @property {string|null} [name] EnvelopeData name
         * @property {string|null} [uniqueId] EnvelopeData uniqueId
         * @property {Array.<sysinfo.IUser>|null} [users] EnvelopeData users
         * @property {Array.<sysinfo.ICPU>|null} [cpu] EnvelopeData cpu
         * @property {Array.<sysinfo.IDisk>|null} [disks] EnvelopeData disks
         * @property {Array.<sysinfo.INetwork>|null} [networks] EnvelopeData networks
         * @property {Array.<sysinfo.IProcessInfo>|null} [processes] EnvelopeData processes
         * @property {Array.<sysinfo.ITemperatureSensor>|null} [temperatures] EnvelopeData temperatures
         * @property {Array.<sysinfo.IPowerSource>|null} [powerSources] EnvelopeData powerSources
         * @property {sysinfo.IThrottlingState|null} [throttling] EnvelopeData throttling
         * @property {Array.<sysinfo.ICellularModem>|null} [cellularModems] EnvelopeData cellularModems
         */

        /**
         * Constructs a new EnvelopeData.
         * @memberof sysinfo
         * @classdesc Represents an EnvelopeData.
         * @implements IEnvelopeData
         * @constructor
         * @param {sysinfo.IEnvelopeData=} [properties] Properties to set
         */
        function EnvelopeData(properties) {
            this.users = [];
            this.cpu = [];
            this.disks = [];
            this.networks = [];
            this.processes = [];
            this.temperatures = [];
            this.powerSources = [];
            this.cellularModems = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * EnvelopeData os.
         * @member {sysinfo.IOsInfo|null|undefined} os
         * @memberof sysinfo.EnvelopeData
         * @instance
         */
        EnvelopeData.prototype.os = null;

        /**
         * EnvelopeData time.
         * @member {sysinfo.ITimeInfo|null|undefined} time
         * @memberof sysinfo.EnvelopeData
         * @instance
         */
        EnvelopeData.prototype.time = null;

        /**
         * EnvelopeData memory.
         * @member {sysinfo.IMemory|null|undefined} memory
         * @memberof sysinfo.EnvelopeData
         * @instance
         */
        EnvelopeData.prototype.memory = null;

        /**
         * EnvelopeData motherboard.
         * @member {sysinfo.IMotherboard|null|undefined} motherboard
         * @memberof sysinfo.EnvelopeData
         * @instance
         */
        EnvelopeData.prototype.motherboard = null;

        /**
         * EnvelopeData hostname.
         * @member {string} hostname
         * @memberof sysinfo.EnvelopeData
         * @instance
         */
        EnvelopeData.prototype.hostname = "";

        /**
         * EnvelopeData cpuArch.
         * @member {string} cpuArch
         * @memberof sysinfo.EnvelopeData
         * @instance
         */
        EnvelopeData.prototype.cpuArch = "";

        /**
         * EnvelopeData physicalCoreCount.
         * @member {Long} physicalCoreCount
         * @memberof sysinfo.EnvelopeData
         * @instance
         */
        EnvelopeData.prototype.physicalCoreCount = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * EnvelopeData name.
         * @member {string} name
         * @memberof sysinfo.EnvelopeData
         * @instance
         */
        EnvelopeData.prototype.name = "";

        /**
         * EnvelopeData uniqueId.
         * @member {string} uniqueId
         * @memberof sysinfo.EnvelopeData
         * @instance
         */
        EnvelopeData.prototype.uniqueId = "";

        /**
         * EnvelopeData users.
         * @member {Array.<sysinfo.IUser>} users
         * @memberof sysinfo.EnvelopeData
         * @instance
         */
        EnvelopeData.prototype.users = $util.emptyArray;

        /**
         * EnvelopeData cpu.
         * @member {Array.<sysinfo.ICPU>} cpu
         * @memberof sysinfo.EnvelopeData
         * @instance
         */
        EnvelopeData.prototype.cpu = $util.emptyArray;

        /**
         * EnvelopeData disks.
         * @member {Array.<sysinfo.IDisk>} disks
         * @memberof sysinfo.EnvelopeData
         * @instance
         */
        EnvelopeData.prototype.disks = $util.emptyArray;

        /**
         * EnvelopeData networks.
         * @member {Array.<sysinfo.INetwork>} networks
         * @memberof sysinfo.EnvelopeData
         * @instance
         */
        EnvelopeData.prototype.networks = $util.emptyArray;

        /**
         * EnvelopeData processes.
         * @member {Array.<sysinfo.IProcessInfo>} processes
         * @memberof sysinfo.EnvelopeData
         * @instance
         */
        EnvelopeData.prototype.processes = $util.emptyArray;

        /**
         * EnvelopeData temperatures.
         * @member {Array.<sysinfo.ITemperatureSensor>} temperatures
         * @memberof sysinfo.EnvelopeData
         * @instance
         */
        EnvelopeData.prototype.temperatures = $util.emptyArray;

        /**
         * EnvelopeData powerSources.
         * @member {Array.<sysinfo.IPowerSource>} powerSources
         * @memberof sysinfo.EnvelopeData
         * @instance
         */
        EnvelopeData.prototype.powerSources = $util.emptyArray;

        /**
         * EnvelopeData throttling.
         * @member {sysinfo.IThrottlingState|null|undefined} throttling
         * @memberof sysinfo.EnvelopeData
         * @instance
         */
        EnvelopeData.prototype.throttling = null;

        /**
         * EnvelopeData cellularModems.
         * @member {Array.<sysinfo.ICellularModem>} cellularModems
         * @memberof sysinfo.EnvelopeData
         * @instance
         */
        EnvelopeData.prototype.cellularModems = $util.emptyArray;

        /**
         * Creates a new EnvelopeData instance using the specified properties.
         * @function create
         * @memberof sysinfo.EnvelopeData
         * @static
         * @param {sysinfo.IEnvelopeData=} [properties] Properties to set
         * @returns {sysinfo.EnvelopeData} EnvelopeData instance
         */
        EnvelopeData.create = function create(properties) {
            return new EnvelopeData(properties);
        };

        /**
         * Encodes the specified EnvelopeData message. Does not implicitly {@link sysinfo.EnvelopeData.verify|verify} messages.
         * @function encode
         * @memberof sysinfo.EnvelopeData
         * @static
         * @param {sysinfo.IEnvelopeData} message EnvelopeData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EnvelopeData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.os != null && Object.hasOwnProperty.call(message, "os"))
                $root.sysinfo.OsInfo.encode(message.os, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.time != null && Object.hasOwnProperty.call(message, "time"))
                $root.sysinfo.TimeInfo.encode(message.time, writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.memory != null && Object.hasOwnProperty.call(message, "memory"))
                $root.sysinfo.Memory.encode(message.memory, writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.motherboard != null && Object.hasOwnProperty.call(message, "motherboard"))
                $root.sysinfo.Motherboard.encode(message.motherboard, writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.hostname != null && Object.hasOwnProperty.call(message, "hostname"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.hostname);
            if (message.cpuArch != null && Object.hasOwnProperty.call(message, "cpuArch"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.cpuArch);
            if (message.physicalCoreCount != null && Object.hasOwnProperty.call(message, "physicalCoreCount"))
                writer.uint32(/* id 7, wireType 0 =*/56).uint64(message.physicalCoreCount);
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                writer.uint32(/* id 8, wireType 2 =*/66).string(message.name);
            if (message.uniqueId != null && Object.hasOwnProperty.call(message, "uniqueId"))
                writer.uint32(/* id 9, wireType 2 =*/74).string(message.uniqueId);
            if (message.users != null && message.users.length)
                for (let i = 0; i < message.users.length; ++i)
                    $root.sysinfo.User.encode(message.users[i], writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            if (message.cpu != null && message.cpu.length)
                for (let i = 0; i < message.cpu.length; ++i)
                    $root.sysinfo.CPU.encode(message.cpu[i], writer.uint32(/* id 11, wireType 2 =*/90).fork()).ldelim();
            if (message.disks != null && message.disks.length)
                for (let i = 0; i < message.disks.length; ++i)
                    $root.sysinfo.Disk.encode(message.disks[i], writer.uint32(/* id 12, wireType 2 =*/98).fork()).ldelim();
            if (message.networks != null && message.networks.length)
                for (let i = 0; i < message.networks.length; ++i)
                    $root.sysinfo.Network.encode(message.networks[i], writer.uint32(/* id 13, wireType 2 =*/106).fork()).ldelim();
            if (message.processes != null && message.processes.length)
                for (let i = 0; i < message.processes.length; ++i)
                    $root.sysinfo.ProcessInfo.encode(message.processes[i], writer.uint32(/* id 15, wireType 2 =*/122).fork()).ldelim();
            if (message.temperatures != null && message.temperatures.length)
                for (let i = 0; i < message.temperatures.length; ++i)
                    $root.sysinfo.TemperatureSensor.encode(message.temperatures[i], writer.uint32(/* id 20, wireType 2 =*/162).fork()).ldelim();
            if (message.powerSources != null && message.powerSources.length)
                for (let i = 0; i < message.powerSources.length; ++i)
                    $root.sysinfo.PowerSource.encode(message.powerSources[i], writer.uint32(/* id 21, wireType 2 =*/170).fork()).ldelim();
            if (message.throttling != null && Object.hasOwnProperty.call(message, "throttling"))
                $root.sysinfo.ThrottlingState.encode(message.throttling, writer.uint32(/* id 22, wireType 2 =*/178).fork()).ldelim();
            if (message.cellularModems != null && message.cellularModems.length)
                for (let i = 0; i < message.cellularModems.length; ++i)
                    $root.sysinfo.CellularModem.encode(message.cellularModems[i], writer.uint32(/* id 30, wireType 2 =*/242).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified EnvelopeData message, length delimited. Does not implicitly {@link sysinfo.EnvelopeData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof sysinfo.EnvelopeData
         * @static
         * @param {sysinfo.IEnvelopeData} message EnvelopeData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EnvelopeData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an EnvelopeData message from the specified reader or buffer.
         * @function decode
         * @memberof sysinfo.EnvelopeData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {sysinfo.EnvelopeData} EnvelopeData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EnvelopeData.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.sysinfo.EnvelopeData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.os = $root.sysinfo.OsInfo.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 2: {
                        message.time = $root.sysinfo.TimeInfo.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 3: {
                        message.memory = $root.sysinfo.Memory.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 4: {
                        message.motherboard = $root.sysinfo.Motherboard.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 5: {
                        message.hostname = reader.string();
                        break;
                    }
                case 6: {
                        message.cpuArch = reader.string();
                        break;
                    }
                case 7: {
                        message.physicalCoreCount = reader.uint64();
                        break;
                    }
                case 8: {
                        message.name = reader.string();
                        break;
                    }
                case 9: {
                        message.uniqueId = reader.string();
                        break;
                    }
                case 10: {
                        if (!(message.users && message.users.length))
                            message.users = [];
                        message.users.push($root.sysinfo.User.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 11: {
                        if (!(message.cpu && message.cpu.length))
                            message.cpu = [];
                        message.cpu.push($root.sysinfo.CPU.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 12: {
                        if (!(message.disks && message.disks.length))
                            message.disks = [];
                        message.disks.push($root.sysinfo.Disk.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 13: {
                        if (!(message.networks && message.networks.length))
                            message.networks = [];
                        message.networks.push($root.sysinfo.Network.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 15: {
                        if (!(message.processes && message.processes.length))
                            message.processes = [];
                        message.processes.push($root.sysinfo.ProcessInfo.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 20: {
                        if (!(message.temperatures && message.temperatures.length))
                            message.temperatures = [];
                        message.temperatures.push($root.sysinfo.TemperatureSensor.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 21: {
                        if (!(message.powerSources && message.powerSources.length))
                            message.powerSources = [];
                        message.powerSources.push($root.sysinfo.PowerSource.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 22: {
                        message.throttling = $root.sysinfo.ThrottlingState.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 30: {
                        if (!(message.cellularModems && message.cellularModems.length))
                            message.cellularModems = [];
                        message.cellularModems.push($root.sysinfo.CellularModem.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an EnvelopeData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof sysinfo.EnvelopeData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {sysinfo.EnvelopeData} EnvelopeData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EnvelopeData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an EnvelopeData message.
         * @function verify
         * @memberof sysinfo.EnvelopeData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        EnvelopeData.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.os != null && message.hasOwnProperty("os")) {
                let error = $root.sysinfo.OsInfo.verify(message.os, long + 1);
                if (error)
                    return "os." + error;
            }
            if (message.time != null && message.hasOwnProperty("time")) {
                let error = $root.sysinfo.TimeInfo.verify(message.time, long + 1);
                if (error)
                    return "time." + error;
            }
            if (message.memory != null && message.hasOwnProperty("memory")) {
                let error = $root.sysinfo.Memory.verify(message.memory, long + 1);
                if (error)
                    return "memory." + error;
            }
            if (message.motherboard != null && message.hasOwnProperty("motherboard")) {
                let error = $root.sysinfo.Motherboard.verify(message.motherboard, long + 1);
                if (error)
                    return "motherboard." + error;
            }
            if (message.hostname != null && message.hasOwnProperty("hostname"))
                if (!$util.isString(message.hostname))
                    return "hostname: string expected";
            if (message.cpuArch != null && message.hasOwnProperty("cpuArch"))
                if (!$util.isString(message.cpuArch))
                    return "cpuArch: string expected";
            if (message.physicalCoreCount != null && message.hasOwnProperty("physicalCoreCount"))
                if (!$util.isInteger(message.physicalCoreCount) && !(message.physicalCoreCount && $util.isInteger(message.physicalCoreCount.low) && $util.isInteger(message.physicalCoreCount.high)))
                    return "physicalCoreCount: integer|Long expected";
            if (message.name != null && message.hasOwnProperty("name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.uniqueId != null && message.hasOwnProperty("uniqueId"))
                if (!$util.isString(message.uniqueId))
                    return "uniqueId: string expected";
            if (message.users != null && message.hasOwnProperty("users")) {
                if (!Array.isArray(message.users))
                    return "users: array expected";
                for (let i = 0; i < message.users.length; ++i) {
                    let error = $root.sysinfo.User.verify(message.users[i], long + 1);
                    if (error)
                        return "users." + error;
                }
            }
            if (message.cpu != null && message.hasOwnProperty("cpu")) {
                if (!Array.isArray(message.cpu))
                    return "cpu: array expected";
                for (let i = 0; i < message.cpu.length; ++i) {
                    let error = $root.sysinfo.CPU.verify(message.cpu[i], long + 1);
                    if (error)
                        return "cpu." + error;
                }
            }
            if (message.disks != null && message.hasOwnProperty("disks")) {
                if (!Array.isArray(message.disks))
                    return "disks: array expected";
                for (let i = 0; i < message.disks.length; ++i) {
                    let error = $root.sysinfo.Disk.verify(message.disks[i], long + 1);
                    if (error)
                        return "disks." + error;
                }
            }
            if (message.networks != null && message.hasOwnProperty("networks")) {
                if (!Array.isArray(message.networks))
                    return "networks: array expected";
                for (let i = 0; i < message.networks.length; ++i) {
                    let error = $root.sysinfo.Network.verify(message.networks[i], long + 1);
                    if (error)
                        return "networks." + error;
                }
            }
            if (message.processes != null && message.hasOwnProperty("processes")) {
                if (!Array.isArray(message.processes))
                    return "processes: array expected";
                for (let i = 0; i < message.processes.length; ++i) {
                    let error = $root.sysinfo.ProcessInfo.verify(message.processes[i], long + 1);
                    if (error)
                        return "processes." + error;
                }
            }
            if (message.temperatures != null && message.hasOwnProperty("temperatures")) {
                if (!Array.isArray(message.temperatures))
                    return "temperatures: array expected";
                for (let i = 0; i < message.temperatures.length; ++i) {
                    let error = $root.sysinfo.TemperatureSensor.verify(message.temperatures[i], long + 1);
                    if (error)
                        return "temperatures." + error;
                }
            }
            if (message.powerSources != null && message.hasOwnProperty("powerSources")) {
                if (!Array.isArray(message.powerSources))
                    return "powerSources: array expected";
                for (let i = 0; i < message.powerSources.length; ++i) {
                    let error = $root.sysinfo.PowerSource.verify(message.powerSources[i], long + 1);
                    if (error)
                        return "powerSources." + error;
                }
            }
            if (message.throttling != null && message.hasOwnProperty("throttling")) {
                let error = $root.sysinfo.ThrottlingState.verify(message.throttling, long + 1);
                if (error)
                    return "throttling." + error;
            }
            if (message.cellularModems != null && message.hasOwnProperty("cellularModems")) {
                if (!Array.isArray(message.cellularModems))
                    return "cellularModems: array expected";
                for (let i = 0; i < message.cellularModems.length; ++i) {
                    let error = $root.sysinfo.CellularModem.verify(message.cellularModems[i], long + 1);
                    if (error)
                        return "cellularModems." + error;
                }
            }
            return null;
        };

        /**
         * Creates an EnvelopeData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof sysinfo.EnvelopeData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {sysinfo.EnvelopeData} EnvelopeData
         */
        EnvelopeData.fromObject = function fromObject(object, long) {
            if (object instanceof $root.sysinfo.EnvelopeData)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.sysinfo.EnvelopeData();
            if (object.os != null) {
                if (typeof object.os !== "object")
                    throw TypeError(".sysinfo.EnvelopeData.os: object expected");
                message.os = $root.sysinfo.OsInfo.fromObject(object.os, long + 1);
            }
            if (object.time != null) {
                if (typeof object.time !== "object")
                    throw TypeError(".sysinfo.EnvelopeData.time: object expected");
                message.time = $root.sysinfo.TimeInfo.fromObject(object.time, long + 1);
            }
            if (object.memory != null) {
                if (typeof object.memory !== "object")
                    throw TypeError(".sysinfo.EnvelopeData.memory: object expected");
                message.memory = $root.sysinfo.Memory.fromObject(object.memory, long + 1);
            }
            if (object.motherboard != null) {
                if (typeof object.motherboard !== "object")
                    throw TypeError(".sysinfo.EnvelopeData.motherboard: object expected");
                message.motherboard = $root.sysinfo.Motherboard.fromObject(object.motherboard, long + 1);
            }
            if (object.hostname != null)
                message.hostname = String(object.hostname);
            if (object.cpuArch != null)
                message.cpuArch = String(object.cpuArch);
            if (object.physicalCoreCount != null)
                if ($util.Long)
                    (message.physicalCoreCount = $util.Long.fromValue(object.physicalCoreCount)).unsigned = true;
                else if (typeof object.physicalCoreCount === "string")
                    message.physicalCoreCount = parseInt(object.physicalCoreCount, 10);
                else if (typeof object.physicalCoreCount === "number")
                    message.physicalCoreCount = object.physicalCoreCount;
                else if (typeof object.physicalCoreCount === "object")
                    message.physicalCoreCount = new $util.LongBits(object.physicalCoreCount.low >>> 0, object.physicalCoreCount.high >>> 0).toNumber(true);
            if (object.name != null)
                message.name = String(object.name);
            if (object.uniqueId != null)
                message.uniqueId = String(object.uniqueId);
            if (object.users) {
                if (!Array.isArray(object.users))
                    throw TypeError(".sysinfo.EnvelopeData.users: array expected");
                message.users = [];
                for (let i = 0; i < object.users.length; ++i) {
                    if (typeof object.users[i] !== "object")
                        throw TypeError(".sysinfo.EnvelopeData.users: object expected");
                    message.users[i] = $root.sysinfo.User.fromObject(object.users[i], long + 1);
                }
            }
            if (object.cpu) {
                if (!Array.isArray(object.cpu))
                    throw TypeError(".sysinfo.EnvelopeData.cpu: array expected");
                message.cpu = [];
                for (let i = 0; i < object.cpu.length; ++i) {
                    if (typeof object.cpu[i] !== "object")
                        throw TypeError(".sysinfo.EnvelopeData.cpu: object expected");
                    message.cpu[i] = $root.sysinfo.CPU.fromObject(object.cpu[i], long + 1);
                }
            }
            if (object.disks) {
                if (!Array.isArray(object.disks))
                    throw TypeError(".sysinfo.EnvelopeData.disks: array expected");
                message.disks = [];
                for (let i = 0; i < object.disks.length; ++i) {
                    if (typeof object.disks[i] !== "object")
                        throw TypeError(".sysinfo.EnvelopeData.disks: object expected");
                    message.disks[i] = $root.sysinfo.Disk.fromObject(object.disks[i], long + 1);
                }
            }
            if (object.networks) {
                if (!Array.isArray(object.networks))
                    throw TypeError(".sysinfo.EnvelopeData.networks: array expected");
                message.networks = [];
                for (let i = 0; i < object.networks.length; ++i) {
                    if (typeof object.networks[i] !== "object")
                        throw TypeError(".sysinfo.EnvelopeData.networks: object expected");
                    message.networks[i] = $root.sysinfo.Network.fromObject(object.networks[i], long + 1);
                }
            }
            if (object.processes) {
                if (!Array.isArray(object.processes))
                    throw TypeError(".sysinfo.EnvelopeData.processes: array expected");
                message.processes = [];
                for (let i = 0; i < object.processes.length; ++i) {
                    if (typeof object.processes[i] !== "object")
                        throw TypeError(".sysinfo.EnvelopeData.processes: object expected");
                    message.processes[i] = $root.sysinfo.ProcessInfo.fromObject(object.processes[i], long + 1);
                }
            }
            if (object.temperatures) {
                if (!Array.isArray(object.temperatures))
                    throw TypeError(".sysinfo.EnvelopeData.temperatures: array expected");
                message.temperatures = [];
                for (let i = 0; i < object.temperatures.length; ++i) {
                    if (typeof object.temperatures[i] !== "object")
                        throw TypeError(".sysinfo.EnvelopeData.temperatures: object expected");
                    message.temperatures[i] = $root.sysinfo.TemperatureSensor.fromObject(object.temperatures[i], long + 1);
                }
            }
            if (object.powerSources) {
                if (!Array.isArray(object.powerSources))
                    throw TypeError(".sysinfo.EnvelopeData.powerSources: array expected");
                message.powerSources = [];
                for (let i = 0; i < object.powerSources.length; ++i) {
                    if (typeof object.powerSources[i] !== "object")
                        throw TypeError(".sysinfo.EnvelopeData.powerSources: object expected");
                    message.powerSources[i] = $root.sysinfo.PowerSource.fromObject(object.powerSources[i], long + 1);
                }
            }
            if (object.throttling != null) {
                if (typeof object.throttling !== "object")
                    throw TypeError(".sysinfo.EnvelopeData.throttling: object expected");
                message.throttling = $root.sysinfo.ThrottlingState.fromObject(object.throttling, long + 1);
            }
            if (object.cellularModems) {
                if (!Array.isArray(object.cellularModems))
                    throw TypeError(".sysinfo.EnvelopeData.cellularModems: array expected");
                message.cellularModems = [];
                for (let i = 0; i < object.cellularModems.length; ++i) {
                    if (typeof object.cellularModems[i] !== "object")
                        throw TypeError(".sysinfo.EnvelopeData.cellularModems: object expected");
                    message.cellularModems[i] = $root.sysinfo.CellularModem.fromObject(object.cellularModems[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from an EnvelopeData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof sysinfo.EnvelopeData
         * @static
         * @param {sysinfo.EnvelopeData} message EnvelopeData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        EnvelopeData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.users = [];
                object.cpu = [];
                object.disks = [];
                object.networks = [];
                object.processes = [];
                object.temperatures = [];
                object.powerSources = [];
                object.cellularModems = [];
            }
            if (options.defaults) {
                object.os = null;
                object.time = null;
                object.memory = null;
                object.motherboard = null;
                object.hostname = "";
                object.cpuArch = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.physicalCoreCount = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.physicalCoreCount = options.longs === String ? "0" : 0;
                object.name = "";
                object.uniqueId = "";
                object.throttling = null;
            }
            if (message.os != null && message.hasOwnProperty("os"))
                object.os = $root.sysinfo.OsInfo.toObject(message.os, options);
            if (message.time != null && message.hasOwnProperty("time"))
                object.time = $root.sysinfo.TimeInfo.toObject(message.time, options);
            if (message.memory != null && message.hasOwnProperty("memory"))
                object.memory = $root.sysinfo.Memory.toObject(message.memory, options);
            if (message.motherboard != null && message.hasOwnProperty("motherboard"))
                object.motherboard = $root.sysinfo.Motherboard.toObject(message.motherboard, options);
            if (message.hostname != null && message.hasOwnProperty("hostname"))
                object.hostname = message.hostname;
            if (message.cpuArch != null && message.hasOwnProperty("cpuArch"))
                object.cpuArch = message.cpuArch;
            if (message.physicalCoreCount != null && message.hasOwnProperty("physicalCoreCount"))
                if (typeof message.physicalCoreCount === "number")
                    object.physicalCoreCount = options.longs === String ? String(message.physicalCoreCount) : message.physicalCoreCount;
                else
                    object.physicalCoreCount = options.longs === String ? $util.Long.prototype.toString.call(message.physicalCoreCount) : options.longs === Number ? new $util.LongBits(message.physicalCoreCount.low >>> 0, message.physicalCoreCount.high >>> 0).toNumber(true) : message.physicalCoreCount;
            if (message.name != null && message.hasOwnProperty("name"))
                object.name = message.name;
            if (message.uniqueId != null && message.hasOwnProperty("uniqueId"))
                object.uniqueId = message.uniqueId;
            if (message.users && message.users.length) {
                object.users = [];
                for (let j = 0; j < message.users.length; ++j)
                    object.users[j] = $root.sysinfo.User.toObject(message.users[j], options);
            }
            if (message.cpu && message.cpu.length) {
                object.cpu = [];
                for (let j = 0; j < message.cpu.length; ++j)
                    object.cpu[j] = $root.sysinfo.CPU.toObject(message.cpu[j], options);
            }
            if (message.disks && message.disks.length) {
                object.disks = [];
                for (let j = 0; j < message.disks.length; ++j)
                    object.disks[j] = $root.sysinfo.Disk.toObject(message.disks[j], options);
            }
            if (message.networks && message.networks.length) {
                object.networks = [];
                for (let j = 0; j < message.networks.length; ++j)
                    object.networks[j] = $root.sysinfo.Network.toObject(message.networks[j], options);
            }
            if (message.processes && message.processes.length) {
                object.processes = [];
                for (let j = 0; j < message.processes.length; ++j)
                    object.processes[j] = $root.sysinfo.ProcessInfo.toObject(message.processes[j], options);
            }
            if (message.temperatures && message.temperatures.length) {
                object.temperatures = [];
                for (let j = 0; j < message.temperatures.length; ++j)
                    object.temperatures[j] = $root.sysinfo.TemperatureSensor.toObject(message.temperatures[j], options);
            }
            if (message.powerSources && message.powerSources.length) {
                object.powerSources = [];
                for (let j = 0; j < message.powerSources.length; ++j)
                    object.powerSources[j] = $root.sysinfo.PowerSource.toObject(message.powerSources[j], options);
            }
            if (message.throttling != null && message.hasOwnProperty("throttling"))
                object.throttling = $root.sysinfo.ThrottlingState.toObject(message.throttling, options);
            if (message.cellularModems && message.cellularModems.length) {
                object.cellularModems = [];
                for (let j = 0; j < message.cellularModems.length; ++j)
                    object.cellularModems[j] = $root.sysinfo.CellularModem.toObject(message.cellularModems[j], options);
            }
            return object;
        };

        /**
         * Converts this EnvelopeData to JSON.
         * @function toJSON
         * @memberof sysinfo.EnvelopeData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        EnvelopeData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for EnvelopeData
         * @function getTypeUrl
         * @memberof sysinfo.EnvelopeData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        EnvelopeData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/sysinfo.EnvelopeData";
        };

        return EnvelopeData;
    })();

    sysinfo.OsInfo = (function() {

        /**
         * Properties of an OsInfo.
         * @memberof sysinfo
         * @interface IOsInfo
         * @property {string|null} [name] OsInfo name
         * @property {string|null} [release] OsInfo release
         * @property {string|null} [kernelVersion] OsInfo kernelVersion
         */

        /**
         * Constructs a new OsInfo.
         * @memberof sysinfo
         * @classdesc Represents an OsInfo.
         * @implements IOsInfo
         * @constructor
         * @param {sysinfo.IOsInfo=} [properties] Properties to set
         */
        function OsInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * OsInfo name.
         * @member {string} name
         * @memberof sysinfo.OsInfo
         * @instance
         */
        OsInfo.prototype.name = "";

        /**
         * OsInfo release.
         * @member {string} release
         * @memberof sysinfo.OsInfo
         * @instance
         */
        OsInfo.prototype.release = "";

        /**
         * OsInfo kernelVersion.
         * @member {string} kernelVersion
         * @memberof sysinfo.OsInfo
         * @instance
         */
        OsInfo.prototype.kernelVersion = "";

        /**
         * Creates a new OsInfo instance using the specified properties.
         * @function create
         * @memberof sysinfo.OsInfo
         * @static
         * @param {sysinfo.IOsInfo=} [properties] Properties to set
         * @returns {sysinfo.OsInfo} OsInfo instance
         */
        OsInfo.create = function create(properties) {
            return new OsInfo(properties);
        };

        /**
         * Encodes the specified OsInfo message. Does not implicitly {@link sysinfo.OsInfo.verify|verify} messages.
         * @function encode
         * @memberof sysinfo.OsInfo
         * @static
         * @param {sysinfo.IOsInfo} message OsInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        OsInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
            if (message.release != null && Object.hasOwnProperty.call(message, "release"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.release);
            if (message.kernelVersion != null && Object.hasOwnProperty.call(message, "kernelVersion"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.kernelVersion);
            return writer;
        };

        /**
         * Encodes the specified OsInfo message, length delimited. Does not implicitly {@link sysinfo.OsInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof sysinfo.OsInfo
         * @static
         * @param {sysinfo.IOsInfo} message OsInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        OsInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an OsInfo message from the specified reader or buffer.
         * @function decode
         * @memberof sysinfo.OsInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {sysinfo.OsInfo} OsInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        OsInfo.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.sysinfo.OsInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.name = reader.string();
                        break;
                    }
                case 2: {
                        message.release = reader.string();
                        break;
                    }
                case 3: {
                        message.kernelVersion = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an OsInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof sysinfo.OsInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {sysinfo.OsInfo} OsInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        OsInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an OsInfo message.
         * @function verify
         * @memberof sysinfo.OsInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        OsInfo.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.name != null && message.hasOwnProperty("name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.release != null && message.hasOwnProperty("release"))
                if (!$util.isString(message.release))
                    return "release: string expected";
            if (message.kernelVersion != null && message.hasOwnProperty("kernelVersion"))
                if (!$util.isString(message.kernelVersion))
                    return "kernelVersion: string expected";
            return null;
        };

        /**
         * Creates an OsInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof sysinfo.OsInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {sysinfo.OsInfo} OsInfo
         */
        OsInfo.fromObject = function fromObject(object, long) {
            if (object instanceof $root.sysinfo.OsInfo)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.sysinfo.OsInfo();
            if (object.name != null)
                message.name = String(object.name);
            if (object.release != null)
                message.release = String(object.release);
            if (object.kernelVersion != null)
                message.kernelVersion = String(object.kernelVersion);
            return message;
        };

        /**
         * Creates a plain object from an OsInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof sysinfo.OsInfo
         * @static
         * @param {sysinfo.OsInfo} message OsInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        OsInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.name = "";
                object.release = "";
                object.kernelVersion = "";
            }
            if (message.name != null && message.hasOwnProperty("name"))
                object.name = message.name;
            if (message.release != null && message.hasOwnProperty("release"))
                object.release = message.release;
            if (message.kernelVersion != null && message.hasOwnProperty("kernelVersion"))
                object.kernelVersion = message.kernelVersion;
            return object;
        };

        /**
         * Converts this OsInfo to JSON.
         * @function toJSON
         * @memberof sysinfo.OsInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        OsInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for OsInfo
         * @function getTypeUrl
         * @memberof sysinfo.OsInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        OsInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/sysinfo.OsInfo";
        };

        return OsInfo;
    })();

    sysinfo.TimeInfo = (function() {

        /**
         * Properties of a TimeInfo.
         * @memberof sysinfo
         * @interface ITimeInfo
         * @property {Long|null} [utcOffsetSeconds] TimeInfo utcOffsetSeconds
         */

        /**
         * Constructs a new TimeInfo.
         * @memberof sysinfo
         * @classdesc Represents a TimeInfo.
         * @implements ITimeInfo
         * @constructor
         * @param {sysinfo.ITimeInfo=} [properties] Properties to set
         */
        function TimeInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * TimeInfo utcOffsetSeconds.
         * @member {Long} utcOffsetSeconds
         * @memberof sysinfo.TimeInfo
         * @instance
         */
        TimeInfo.prototype.utcOffsetSeconds = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new TimeInfo instance using the specified properties.
         * @function create
         * @memberof sysinfo.TimeInfo
         * @static
         * @param {sysinfo.ITimeInfo=} [properties] Properties to set
         * @returns {sysinfo.TimeInfo} TimeInfo instance
         */
        TimeInfo.create = function create(properties) {
            return new TimeInfo(properties);
        };

        /**
         * Encodes the specified TimeInfo message. Does not implicitly {@link sysinfo.TimeInfo.verify|verify} messages.
         * @function encode
         * @memberof sysinfo.TimeInfo
         * @static
         * @param {sysinfo.ITimeInfo} message TimeInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TimeInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.utcOffsetSeconds != null && Object.hasOwnProperty.call(message, "utcOffsetSeconds"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.utcOffsetSeconds);
            return writer;
        };

        /**
         * Encodes the specified TimeInfo message, length delimited. Does not implicitly {@link sysinfo.TimeInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof sysinfo.TimeInfo
         * @static
         * @param {sysinfo.ITimeInfo} message TimeInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TimeInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a TimeInfo message from the specified reader or buffer.
         * @function decode
         * @memberof sysinfo.TimeInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {sysinfo.TimeInfo} TimeInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TimeInfo.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.sysinfo.TimeInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.utcOffsetSeconds = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a TimeInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof sysinfo.TimeInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {sysinfo.TimeInfo} TimeInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TimeInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TimeInfo message.
         * @function verify
         * @memberof sysinfo.TimeInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TimeInfo.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.utcOffsetSeconds != null && message.hasOwnProperty("utcOffsetSeconds"))
                if (!$util.isInteger(message.utcOffsetSeconds) && !(message.utcOffsetSeconds && $util.isInteger(message.utcOffsetSeconds.low) && $util.isInteger(message.utcOffsetSeconds.high)))
                    return "utcOffsetSeconds: integer|Long expected";
            return null;
        };

        /**
         * Creates a TimeInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof sysinfo.TimeInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {sysinfo.TimeInfo} TimeInfo
         */
        TimeInfo.fromObject = function fromObject(object, long) {
            if (object instanceof $root.sysinfo.TimeInfo)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.sysinfo.TimeInfo();
            if (object.utcOffsetSeconds != null)
                if ($util.Long)
                    (message.utcOffsetSeconds = $util.Long.fromValue(object.utcOffsetSeconds)).unsigned = false;
                else if (typeof object.utcOffsetSeconds === "string")
                    message.utcOffsetSeconds = parseInt(object.utcOffsetSeconds, 10);
                else if (typeof object.utcOffsetSeconds === "number")
                    message.utcOffsetSeconds = object.utcOffsetSeconds;
                else if (typeof object.utcOffsetSeconds === "object")
                    message.utcOffsetSeconds = new $util.LongBits(object.utcOffsetSeconds.low >>> 0, object.utcOffsetSeconds.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a TimeInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof sysinfo.TimeInfo
         * @static
         * @param {sysinfo.TimeInfo} message TimeInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TimeInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.utcOffsetSeconds = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.utcOffsetSeconds = options.longs === String ? "0" : 0;
            if (message.utcOffsetSeconds != null && message.hasOwnProperty("utcOffsetSeconds"))
                if (typeof message.utcOffsetSeconds === "number")
                    object.utcOffsetSeconds = options.longs === String ? String(message.utcOffsetSeconds) : message.utcOffsetSeconds;
                else
                    object.utcOffsetSeconds = options.longs === String ? $util.Long.prototype.toString.call(message.utcOffsetSeconds) : options.longs === Number ? new $util.LongBits(message.utcOffsetSeconds.low >>> 0, message.utcOffsetSeconds.high >>> 0).toNumber() : message.utcOffsetSeconds;
            return object;
        };

        /**
         * Converts this TimeInfo to JSON.
         * @function toJSON
         * @memberof sysinfo.TimeInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TimeInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for TimeInfo
         * @function getTypeUrl
         * @memberof sysinfo.TimeInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        TimeInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/sysinfo.TimeInfo";
        };

        return TimeInfo;
    })();

    sysinfo.Memory = (function() {

        /**
         * Properties of a Memory.
         * @memberof sysinfo
         * @interface IMemory
         * @property {Long|null} [totalBytes] Memory totalBytes
         * @property {Long|null} [usedBytes] Memory usedBytes
         * @property {Long|null} [totalSwapBytes] Memory totalSwapBytes
         * @property {Long|null} [usedSwapBytes] Memory usedSwapBytes
         */

        /**
         * Constructs a new Memory.
         * @memberof sysinfo
         * @classdesc Represents a Memory.
         * @implements IMemory
         * @constructor
         * @param {sysinfo.IMemory=} [properties] Properties to set
         */
        function Memory(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Memory totalBytes.
         * @member {Long} totalBytes
         * @memberof sysinfo.Memory
         * @instance
         */
        Memory.prototype.totalBytes = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Memory usedBytes.
         * @member {Long} usedBytes
         * @memberof sysinfo.Memory
         * @instance
         */
        Memory.prototype.usedBytes = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Memory totalSwapBytes.
         * @member {Long} totalSwapBytes
         * @memberof sysinfo.Memory
         * @instance
         */
        Memory.prototype.totalSwapBytes = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Memory usedSwapBytes.
         * @member {Long} usedSwapBytes
         * @memberof sysinfo.Memory
         * @instance
         */
        Memory.prototype.usedSwapBytes = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new Memory instance using the specified properties.
         * @function create
         * @memberof sysinfo.Memory
         * @static
         * @param {sysinfo.IMemory=} [properties] Properties to set
         * @returns {sysinfo.Memory} Memory instance
         */
        Memory.create = function create(properties) {
            return new Memory(properties);
        };

        /**
         * Encodes the specified Memory message. Does not implicitly {@link sysinfo.Memory.verify|verify} messages.
         * @function encode
         * @memberof sysinfo.Memory
         * @static
         * @param {sysinfo.IMemory} message Memory message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Memory.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.totalBytes != null && Object.hasOwnProperty.call(message, "totalBytes"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.totalBytes);
            if (message.usedBytes != null && Object.hasOwnProperty.call(message, "usedBytes"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.usedBytes);
            if (message.totalSwapBytes != null && Object.hasOwnProperty.call(message, "totalSwapBytes"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.totalSwapBytes);
            if (message.usedSwapBytes != null && Object.hasOwnProperty.call(message, "usedSwapBytes"))
                writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.usedSwapBytes);
            return writer;
        };

        /**
         * Encodes the specified Memory message, length delimited. Does not implicitly {@link sysinfo.Memory.verify|verify} messages.
         * @function encodeDelimited
         * @memberof sysinfo.Memory
         * @static
         * @param {sysinfo.IMemory} message Memory message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Memory.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Memory message from the specified reader or buffer.
         * @function decode
         * @memberof sysinfo.Memory
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {sysinfo.Memory} Memory
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Memory.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.sysinfo.Memory();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.totalBytes = reader.uint64();
                        break;
                    }
                case 2: {
                        message.usedBytes = reader.uint64();
                        break;
                    }
                case 3: {
                        message.totalSwapBytes = reader.uint64();
                        break;
                    }
                case 4: {
                        message.usedSwapBytes = reader.uint64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Memory message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof sysinfo.Memory
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {sysinfo.Memory} Memory
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Memory.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Memory message.
         * @function verify
         * @memberof sysinfo.Memory
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Memory.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.totalBytes != null && message.hasOwnProperty("totalBytes"))
                if (!$util.isInteger(message.totalBytes) && !(message.totalBytes && $util.isInteger(message.totalBytes.low) && $util.isInteger(message.totalBytes.high)))
                    return "totalBytes: integer|Long expected";
            if (message.usedBytes != null && message.hasOwnProperty("usedBytes"))
                if (!$util.isInteger(message.usedBytes) && !(message.usedBytes && $util.isInteger(message.usedBytes.low) && $util.isInteger(message.usedBytes.high)))
                    return "usedBytes: integer|Long expected";
            if (message.totalSwapBytes != null && message.hasOwnProperty("totalSwapBytes"))
                if (!$util.isInteger(message.totalSwapBytes) && !(message.totalSwapBytes && $util.isInteger(message.totalSwapBytes.low) && $util.isInteger(message.totalSwapBytes.high)))
                    return "totalSwapBytes: integer|Long expected";
            if (message.usedSwapBytes != null && message.hasOwnProperty("usedSwapBytes"))
                if (!$util.isInteger(message.usedSwapBytes) && !(message.usedSwapBytes && $util.isInteger(message.usedSwapBytes.low) && $util.isInteger(message.usedSwapBytes.high)))
                    return "usedSwapBytes: integer|Long expected";
            return null;
        };

        /**
         * Creates a Memory message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof sysinfo.Memory
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {sysinfo.Memory} Memory
         */
        Memory.fromObject = function fromObject(object, long) {
            if (object instanceof $root.sysinfo.Memory)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.sysinfo.Memory();
            if (object.totalBytes != null)
                if ($util.Long)
                    (message.totalBytes = $util.Long.fromValue(object.totalBytes)).unsigned = true;
                else if (typeof object.totalBytes === "string")
                    message.totalBytes = parseInt(object.totalBytes, 10);
                else if (typeof object.totalBytes === "number")
                    message.totalBytes = object.totalBytes;
                else if (typeof object.totalBytes === "object")
                    message.totalBytes = new $util.LongBits(object.totalBytes.low >>> 0, object.totalBytes.high >>> 0).toNumber(true);
            if (object.usedBytes != null)
                if ($util.Long)
                    (message.usedBytes = $util.Long.fromValue(object.usedBytes)).unsigned = true;
                else if (typeof object.usedBytes === "string")
                    message.usedBytes = parseInt(object.usedBytes, 10);
                else if (typeof object.usedBytes === "number")
                    message.usedBytes = object.usedBytes;
                else if (typeof object.usedBytes === "object")
                    message.usedBytes = new $util.LongBits(object.usedBytes.low >>> 0, object.usedBytes.high >>> 0).toNumber(true);
            if (object.totalSwapBytes != null)
                if ($util.Long)
                    (message.totalSwapBytes = $util.Long.fromValue(object.totalSwapBytes)).unsigned = true;
                else if (typeof object.totalSwapBytes === "string")
                    message.totalSwapBytes = parseInt(object.totalSwapBytes, 10);
                else if (typeof object.totalSwapBytes === "number")
                    message.totalSwapBytes = object.totalSwapBytes;
                else if (typeof object.totalSwapBytes === "object")
                    message.totalSwapBytes = new $util.LongBits(object.totalSwapBytes.low >>> 0, object.totalSwapBytes.high >>> 0).toNumber(true);
            if (object.usedSwapBytes != null)
                if ($util.Long)
                    (message.usedSwapBytes = $util.Long.fromValue(object.usedSwapBytes)).unsigned = true;
                else if (typeof object.usedSwapBytes === "string")
                    message.usedSwapBytes = parseInt(object.usedSwapBytes, 10);
                else if (typeof object.usedSwapBytes === "number")
                    message.usedSwapBytes = object.usedSwapBytes;
                else if (typeof object.usedSwapBytes === "object")
                    message.usedSwapBytes = new $util.LongBits(object.usedSwapBytes.low >>> 0, object.usedSwapBytes.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a Memory message. Also converts values to other types if specified.
         * @function toObject
         * @memberof sysinfo.Memory
         * @static
         * @param {sysinfo.Memory} message Memory
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Memory.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.totalBytes = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.totalBytes = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.usedBytes = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.usedBytes = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.totalSwapBytes = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.totalSwapBytes = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.usedSwapBytes = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.usedSwapBytes = options.longs === String ? "0" : 0;
            }
            if (message.totalBytes != null && message.hasOwnProperty("totalBytes"))
                if (typeof message.totalBytes === "number")
                    object.totalBytes = options.longs === String ? String(message.totalBytes) : message.totalBytes;
                else
                    object.totalBytes = options.longs === String ? $util.Long.prototype.toString.call(message.totalBytes) : options.longs === Number ? new $util.LongBits(message.totalBytes.low >>> 0, message.totalBytes.high >>> 0).toNumber(true) : message.totalBytes;
            if (message.usedBytes != null && message.hasOwnProperty("usedBytes"))
                if (typeof message.usedBytes === "number")
                    object.usedBytes = options.longs === String ? String(message.usedBytes) : message.usedBytes;
                else
                    object.usedBytes = options.longs === String ? $util.Long.prototype.toString.call(message.usedBytes) : options.longs === Number ? new $util.LongBits(message.usedBytes.low >>> 0, message.usedBytes.high >>> 0).toNumber(true) : message.usedBytes;
            if (message.totalSwapBytes != null && message.hasOwnProperty("totalSwapBytes"))
                if (typeof message.totalSwapBytes === "number")
                    object.totalSwapBytes = options.longs === String ? String(message.totalSwapBytes) : message.totalSwapBytes;
                else
                    object.totalSwapBytes = options.longs === String ? $util.Long.prototype.toString.call(message.totalSwapBytes) : options.longs === Number ? new $util.LongBits(message.totalSwapBytes.low >>> 0, message.totalSwapBytes.high >>> 0).toNumber(true) : message.totalSwapBytes;
            if (message.usedSwapBytes != null && message.hasOwnProperty("usedSwapBytes"))
                if (typeof message.usedSwapBytes === "number")
                    object.usedSwapBytes = options.longs === String ? String(message.usedSwapBytes) : message.usedSwapBytes;
                else
                    object.usedSwapBytes = options.longs === String ? $util.Long.prototype.toString.call(message.usedSwapBytes) : options.longs === Number ? new $util.LongBits(message.usedSwapBytes.low >>> 0, message.usedSwapBytes.high >>> 0).toNumber(true) : message.usedSwapBytes;
            return object;
        };

        /**
         * Converts this Memory to JSON.
         * @function toJSON
         * @memberof sysinfo.Memory
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Memory.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Memory
         * @function getTypeUrl
         * @memberof sysinfo.Memory
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Memory.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/sysinfo.Memory";
        };

        return Memory;
    })();

    sysinfo.CPU = (function() {

        /**
         * Properties of a CPU.
         * @memberof sysinfo
         * @interface ICPU
         * @property {string|null} [name] CPU name
         * @property {string|null} [vendorId] CPU vendorId
         * @property {string|null} [brand] CPU brand
         * @property {Long|null} [frequency] CPU frequency
         * @property {number|null} [usage] CPU usage
         */

        /**
         * Constructs a new CPU.
         * @memberof sysinfo
         * @classdesc Represents a CPU.
         * @implements ICPU
         * @constructor
         * @param {sysinfo.ICPU=} [properties] Properties to set
         */
        function CPU(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CPU name.
         * @member {string} name
         * @memberof sysinfo.CPU
         * @instance
         */
        CPU.prototype.name = "";

        /**
         * CPU vendorId.
         * @member {string} vendorId
         * @memberof sysinfo.CPU
         * @instance
         */
        CPU.prototype.vendorId = "";

        /**
         * CPU brand.
         * @member {string} brand
         * @memberof sysinfo.CPU
         * @instance
         */
        CPU.prototype.brand = "";

        /**
         * CPU frequency.
         * @member {Long} frequency
         * @memberof sysinfo.CPU
         * @instance
         */
        CPU.prototype.frequency = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * CPU usage.
         * @member {number} usage
         * @memberof sysinfo.CPU
         * @instance
         */
        CPU.prototype.usage = 0;

        /**
         * Creates a new CPU instance using the specified properties.
         * @function create
         * @memberof sysinfo.CPU
         * @static
         * @param {sysinfo.ICPU=} [properties] Properties to set
         * @returns {sysinfo.CPU} CPU instance
         */
        CPU.create = function create(properties) {
            return new CPU(properties);
        };

        /**
         * Encodes the specified CPU message. Does not implicitly {@link sysinfo.CPU.verify|verify} messages.
         * @function encode
         * @memberof sysinfo.CPU
         * @static
         * @param {sysinfo.ICPU} message CPU message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CPU.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
            if (message.vendorId != null && Object.hasOwnProperty.call(message, "vendorId"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.vendorId);
            if (message.brand != null && Object.hasOwnProperty.call(message, "brand"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.brand);
            if (message.frequency != null && Object.hasOwnProperty.call(message, "frequency"))
                writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.frequency);
            if (message.usage != null && Object.hasOwnProperty.call(message, "usage"))
                writer.uint32(/* id 10, wireType 5 =*/85).float(message.usage);
            return writer;
        };

        /**
         * Encodes the specified CPU message, length delimited. Does not implicitly {@link sysinfo.CPU.verify|verify} messages.
         * @function encodeDelimited
         * @memberof sysinfo.CPU
         * @static
         * @param {sysinfo.ICPU} message CPU message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CPU.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CPU message from the specified reader or buffer.
         * @function decode
         * @memberof sysinfo.CPU
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {sysinfo.CPU} CPU
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CPU.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.sysinfo.CPU();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.name = reader.string();
                        break;
                    }
                case 2: {
                        message.vendorId = reader.string();
                        break;
                    }
                case 3: {
                        message.brand = reader.string();
                        break;
                    }
                case 4: {
                        message.frequency = reader.uint64();
                        break;
                    }
                case 10: {
                        message.usage = reader.float();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CPU message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof sysinfo.CPU
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {sysinfo.CPU} CPU
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CPU.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CPU message.
         * @function verify
         * @memberof sysinfo.CPU
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CPU.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.name != null && message.hasOwnProperty("name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.vendorId != null && message.hasOwnProperty("vendorId"))
                if (!$util.isString(message.vendorId))
                    return "vendorId: string expected";
            if (message.brand != null && message.hasOwnProperty("brand"))
                if (!$util.isString(message.brand))
                    return "brand: string expected";
            if (message.frequency != null && message.hasOwnProperty("frequency"))
                if (!$util.isInteger(message.frequency) && !(message.frequency && $util.isInteger(message.frequency.low) && $util.isInteger(message.frequency.high)))
                    return "frequency: integer|Long expected";
            if (message.usage != null && message.hasOwnProperty("usage"))
                if (typeof message.usage !== "number")
                    return "usage: number expected";
            return null;
        };

        /**
         * Creates a CPU message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof sysinfo.CPU
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {sysinfo.CPU} CPU
         */
        CPU.fromObject = function fromObject(object, long) {
            if (object instanceof $root.sysinfo.CPU)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.sysinfo.CPU();
            if (object.name != null)
                message.name = String(object.name);
            if (object.vendorId != null)
                message.vendorId = String(object.vendorId);
            if (object.brand != null)
                message.brand = String(object.brand);
            if (object.frequency != null)
                if ($util.Long)
                    (message.frequency = $util.Long.fromValue(object.frequency)).unsigned = true;
                else if (typeof object.frequency === "string")
                    message.frequency = parseInt(object.frequency, 10);
                else if (typeof object.frequency === "number")
                    message.frequency = object.frequency;
                else if (typeof object.frequency === "object")
                    message.frequency = new $util.LongBits(object.frequency.low >>> 0, object.frequency.high >>> 0).toNumber(true);
            if (object.usage != null)
                message.usage = Number(object.usage);
            return message;
        };

        /**
         * Creates a plain object from a CPU message. Also converts values to other types if specified.
         * @function toObject
         * @memberof sysinfo.CPU
         * @static
         * @param {sysinfo.CPU} message CPU
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CPU.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.name = "";
                object.vendorId = "";
                object.brand = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.frequency = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.frequency = options.longs === String ? "0" : 0;
                object.usage = 0;
            }
            if (message.name != null && message.hasOwnProperty("name"))
                object.name = message.name;
            if (message.vendorId != null && message.hasOwnProperty("vendorId"))
                object.vendorId = message.vendorId;
            if (message.brand != null && message.hasOwnProperty("brand"))
                object.brand = message.brand;
            if (message.frequency != null && message.hasOwnProperty("frequency"))
                if (typeof message.frequency === "number")
                    object.frequency = options.longs === String ? String(message.frequency) : message.frequency;
                else
                    object.frequency = options.longs === String ? $util.Long.prototype.toString.call(message.frequency) : options.longs === Number ? new $util.LongBits(message.frequency.low >>> 0, message.frequency.high >>> 0).toNumber(true) : message.frequency;
            if (message.usage != null && message.hasOwnProperty("usage"))
                object.usage = options.json && !isFinite(message.usage) ? String(message.usage) : message.usage;
            return object;
        };

        /**
         * Converts this CPU to JSON.
         * @function toJSON
         * @memberof sysinfo.CPU
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CPU.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CPU
         * @function getTypeUrl
         * @memberof sysinfo.CPU
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CPU.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/sysinfo.CPU";
        };

        return CPU;
    })();

    sysinfo.Disk = (function() {

        /**
         * Properties of a Disk.
         * @memberof sysinfo
         * @interface IDisk
         * @property {string|null} [kind] Disk kind
         * @property {string|null} [fs] Disk fs
         * @property {string|null} [name] Disk name
         * @property {string|null} [mountPoint] Disk mountPoint
         * @property {boolean|null} [removable] Disk removable
         * @property {boolean|null} [readOnly] Disk readOnly
         * @property {Long|null} [totalSpaceBytes] Disk totalSpaceBytes
         * @property {Long|null} [availableSpaceBytes] Disk availableSpaceBytes
         * @property {Long|null} [totalReadBytes] Disk totalReadBytes
         * @property {Long|null} [totalWrittenBytes] Disk totalWrittenBytes
         */

        /**
         * Constructs a new Disk.
         * @memberof sysinfo
         * @classdesc Represents a Disk.
         * @implements IDisk
         * @constructor
         * @param {sysinfo.IDisk=} [properties] Properties to set
         */
        function Disk(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Disk kind.
         * @member {string} kind
         * @memberof sysinfo.Disk
         * @instance
         */
        Disk.prototype.kind = "";

        /**
         * Disk fs.
         * @member {string} fs
         * @memberof sysinfo.Disk
         * @instance
         */
        Disk.prototype.fs = "";

        /**
         * Disk name.
         * @member {string} name
         * @memberof sysinfo.Disk
         * @instance
         */
        Disk.prototype.name = "";

        /**
         * Disk mountPoint.
         * @member {string} mountPoint
         * @memberof sysinfo.Disk
         * @instance
         */
        Disk.prototype.mountPoint = "";

        /**
         * Disk removable.
         * @member {boolean} removable
         * @memberof sysinfo.Disk
         * @instance
         */
        Disk.prototype.removable = false;

        /**
         * Disk readOnly.
         * @member {boolean} readOnly
         * @memberof sysinfo.Disk
         * @instance
         */
        Disk.prototype.readOnly = false;

        /**
         * Disk totalSpaceBytes.
         * @member {Long} totalSpaceBytes
         * @memberof sysinfo.Disk
         * @instance
         */
        Disk.prototype.totalSpaceBytes = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Disk availableSpaceBytes.
         * @member {Long} availableSpaceBytes
         * @memberof sysinfo.Disk
         * @instance
         */
        Disk.prototype.availableSpaceBytes = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Disk totalReadBytes.
         * @member {Long} totalReadBytes
         * @memberof sysinfo.Disk
         * @instance
         */
        Disk.prototype.totalReadBytes = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Disk totalWrittenBytes.
         * @member {Long} totalWrittenBytes
         * @memberof sysinfo.Disk
         * @instance
         */
        Disk.prototype.totalWrittenBytes = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new Disk instance using the specified properties.
         * @function create
         * @memberof sysinfo.Disk
         * @static
         * @param {sysinfo.IDisk=} [properties] Properties to set
         * @returns {sysinfo.Disk} Disk instance
         */
        Disk.create = function create(properties) {
            return new Disk(properties);
        };

        /**
         * Encodes the specified Disk message. Does not implicitly {@link sysinfo.Disk.verify|verify} messages.
         * @function encode
         * @memberof sysinfo.Disk
         * @static
         * @param {sysinfo.IDisk} message Disk message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Disk.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.kind != null && Object.hasOwnProperty.call(message, "kind"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.kind);
            if (message.fs != null && Object.hasOwnProperty.call(message, "fs"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.fs);
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.name);
            if (message.mountPoint != null && Object.hasOwnProperty.call(message, "mountPoint"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.mountPoint);
            if (message.removable != null && Object.hasOwnProperty.call(message, "removable"))
                writer.uint32(/* id 5, wireType 0 =*/40).bool(message.removable);
            if (message.readOnly != null && Object.hasOwnProperty.call(message, "readOnly"))
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.readOnly);
            if (message.totalSpaceBytes != null && Object.hasOwnProperty.call(message, "totalSpaceBytes"))
                writer.uint32(/* id 10, wireType 0 =*/80).uint64(message.totalSpaceBytes);
            if (message.availableSpaceBytes != null && Object.hasOwnProperty.call(message, "availableSpaceBytes"))
                writer.uint32(/* id 11, wireType 0 =*/88).uint64(message.availableSpaceBytes);
            if (message.totalReadBytes != null && Object.hasOwnProperty.call(message, "totalReadBytes"))
                writer.uint32(/* id 20, wireType 0 =*/160).uint64(message.totalReadBytes);
            if (message.totalWrittenBytes != null && Object.hasOwnProperty.call(message, "totalWrittenBytes"))
                writer.uint32(/* id 21, wireType 0 =*/168).uint64(message.totalWrittenBytes);
            return writer;
        };

        /**
         * Encodes the specified Disk message, length delimited. Does not implicitly {@link sysinfo.Disk.verify|verify} messages.
         * @function encodeDelimited
         * @memberof sysinfo.Disk
         * @static
         * @param {sysinfo.IDisk} message Disk message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Disk.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Disk message from the specified reader or buffer.
         * @function decode
         * @memberof sysinfo.Disk
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {sysinfo.Disk} Disk
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Disk.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.sysinfo.Disk();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.kind = reader.string();
                        break;
                    }
                case 2: {
                        message.fs = reader.string();
                        break;
                    }
                case 3: {
                        message.name = reader.string();
                        break;
                    }
                case 4: {
                        message.mountPoint = reader.string();
                        break;
                    }
                case 5: {
                        message.removable = reader.bool();
                        break;
                    }
                case 6: {
                        message.readOnly = reader.bool();
                        break;
                    }
                case 10: {
                        message.totalSpaceBytes = reader.uint64();
                        break;
                    }
                case 11: {
                        message.availableSpaceBytes = reader.uint64();
                        break;
                    }
                case 20: {
                        message.totalReadBytes = reader.uint64();
                        break;
                    }
                case 21: {
                        message.totalWrittenBytes = reader.uint64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Disk message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof sysinfo.Disk
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {sysinfo.Disk} Disk
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Disk.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Disk message.
         * @function verify
         * @memberof sysinfo.Disk
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Disk.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.kind != null && message.hasOwnProperty("kind"))
                if (!$util.isString(message.kind))
                    return "kind: string expected";
            if (message.fs != null && message.hasOwnProperty("fs"))
                if (!$util.isString(message.fs))
                    return "fs: string expected";
            if (message.name != null && message.hasOwnProperty("name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.mountPoint != null && message.hasOwnProperty("mountPoint"))
                if (!$util.isString(message.mountPoint))
                    return "mountPoint: string expected";
            if (message.removable != null && message.hasOwnProperty("removable"))
                if (typeof message.removable !== "boolean")
                    return "removable: boolean expected";
            if (message.readOnly != null && message.hasOwnProperty("readOnly"))
                if (typeof message.readOnly !== "boolean")
                    return "readOnly: boolean expected";
            if (message.totalSpaceBytes != null && message.hasOwnProperty("totalSpaceBytes"))
                if (!$util.isInteger(message.totalSpaceBytes) && !(message.totalSpaceBytes && $util.isInteger(message.totalSpaceBytes.low) && $util.isInteger(message.totalSpaceBytes.high)))
                    return "totalSpaceBytes: integer|Long expected";
            if (message.availableSpaceBytes != null && message.hasOwnProperty("availableSpaceBytes"))
                if (!$util.isInteger(message.availableSpaceBytes) && !(message.availableSpaceBytes && $util.isInteger(message.availableSpaceBytes.low) && $util.isInteger(message.availableSpaceBytes.high)))
                    return "availableSpaceBytes: integer|Long expected";
            if (message.totalReadBytes != null && message.hasOwnProperty("totalReadBytes"))
                if (!$util.isInteger(message.totalReadBytes) && !(message.totalReadBytes && $util.isInteger(message.totalReadBytes.low) && $util.isInteger(message.totalReadBytes.high)))
                    return "totalReadBytes: integer|Long expected";
            if (message.totalWrittenBytes != null && message.hasOwnProperty("totalWrittenBytes"))
                if (!$util.isInteger(message.totalWrittenBytes) && !(message.totalWrittenBytes && $util.isInteger(message.totalWrittenBytes.low) && $util.isInteger(message.totalWrittenBytes.high)))
                    return "totalWrittenBytes: integer|Long expected";
            return null;
        };

        /**
         * Creates a Disk message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof sysinfo.Disk
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {sysinfo.Disk} Disk
         */
        Disk.fromObject = function fromObject(object, long) {
            if (object instanceof $root.sysinfo.Disk)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.sysinfo.Disk();
            if (object.kind != null)
                message.kind = String(object.kind);
            if (object.fs != null)
                message.fs = String(object.fs);
            if (object.name != null)
                message.name = String(object.name);
            if (object.mountPoint != null)
                message.mountPoint = String(object.mountPoint);
            if (object.removable != null)
                message.removable = Boolean(object.removable);
            if (object.readOnly != null)
                message.readOnly = Boolean(object.readOnly);
            if (object.totalSpaceBytes != null)
                if ($util.Long)
                    (message.totalSpaceBytes = $util.Long.fromValue(object.totalSpaceBytes)).unsigned = true;
                else if (typeof object.totalSpaceBytes === "string")
                    message.totalSpaceBytes = parseInt(object.totalSpaceBytes, 10);
                else if (typeof object.totalSpaceBytes === "number")
                    message.totalSpaceBytes = object.totalSpaceBytes;
                else if (typeof object.totalSpaceBytes === "object")
                    message.totalSpaceBytes = new $util.LongBits(object.totalSpaceBytes.low >>> 0, object.totalSpaceBytes.high >>> 0).toNumber(true);
            if (object.availableSpaceBytes != null)
                if ($util.Long)
                    (message.availableSpaceBytes = $util.Long.fromValue(object.availableSpaceBytes)).unsigned = true;
                else if (typeof object.availableSpaceBytes === "string")
                    message.availableSpaceBytes = parseInt(object.availableSpaceBytes, 10);
                else if (typeof object.availableSpaceBytes === "number")
                    message.availableSpaceBytes = object.availableSpaceBytes;
                else if (typeof object.availableSpaceBytes === "object")
                    message.availableSpaceBytes = new $util.LongBits(object.availableSpaceBytes.low >>> 0, object.availableSpaceBytes.high >>> 0).toNumber(true);
            if (object.totalReadBytes != null)
                if ($util.Long)
                    (message.totalReadBytes = $util.Long.fromValue(object.totalReadBytes)).unsigned = true;
                else if (typeof object.totalReadBytes === "string")
                    message.totalReadBytes = parseInt(object.totalReadBytes, 10);
                else if (typeof object.totalReadBytes === "number")
                    message.totalReadBytes = object.totalReadBytes;
                else if (typeof object.totalReadBytes === "object")
                    message.totalReadBytes = new $util.LongBits(object.totalReadBytes.low >>> 0, object.totalReadBytes.high >>> 0).toNumber(true);
            if (object.totalWrittenBytes != null)
                if ($util.Long)
                    (message.totalWrittenBytes = $util.Long.fromValue(object.totalWrittenBytes)).unsigned = true;
                else if (typeof object.totalWrittenBytes === "string")
                    message.totalWrittenBytes = parseInt(object.totalWrittenBytes, 10);
                else if (typeof object.totalWrittenBytes === "number")
                    message.totalWrittenBytes = object.totalWrittenBytes;
                else if (typeof object.totalWrittenBytes === "object")
                    message.totalWrittenBytes = new $util.LongBits(object.totalWrittenBytes.low >>> 0, object.totalWrittenBytes.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a Disk message. Also converts values to other types if specified.
         * @function toObject
         * @memberof sysinfo.Disk
         * @static
         * @param {sysinfo.Disk} message Disk
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Disk.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.kind = "";
                object.fs = "";
                object.name = "";
                object.mountPoint = "";
                object.removable = false;
                object.readOnly = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.totalSpaceBytes = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.totalSpaceBytes = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.availableSpaceBytes = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.availableSpaceBytes = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.totalReadBytes = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.totalReadBytes = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.totalWrittenBytes = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.totalWrittenBytes = options.longs === String ? "0" : 0;
            }
            if (message.kind != null && message.hasOwnProperty("kind"))
                object.kind = message.kind;
            if (message.fs != null && message.hasOwnProperty("fs"))
                object.fs = message.fs;
            if (message.name != null && message.hasOwnProperty("name"))
                object.name = message.name;
            if (message.mountPoint != null && message.hasOwnProperty("mountPoint"))
                object.mountPoint = message.mountPoint;
            if (message.removable != null && message.hasOwnProperty("removable"))
                object.removable = message.removable;
            if (message.readOnly != null && message.hasOwnProperty("readOnly"))
                object.readOnly = message.readOnly;
            if (message.totalSpaceBytes != null && message.hasOwnProperty("totalSpaceBytes"))
                if (typeof message.totalSpaceBytes === "number")
                    object.totalSpaceBytes = options.longs === String ? String(message.totalSpaceBytes) : message.totalSpaceBytes;
                else
                    object.totalSpaceBytes = options.longs === String ? $util.Long.prototype.toString.call(message.totalSpaceBytes) : options.longs === Number ? new $util.LongBits(message.totalSpaceBytes.low >>> 0, message.totalSpaceBytes.high >>> 0).toNumber(true) : message.totalSpaceBytes;
            if (message.availableSpaceBytes != null && message.hasOwnProperty("availableSpaceBytes"))
                if (typeof message.availableSpaceBytes === "number")
                    object.availableSpaceBytes = options.longs === String ? String(message.availableSpaceBytes) : message.availableSpaceBytes;
                else
                    object.availableSpaceBytes = options.longs === String ? $util.Long.prototype.toString.call(message.availableSpaceBytes) : options.longs === Number ? new $util.LongBits(message.availableSpaceBytes.low >>> 0, message.availableSpaceBytes.high >>> 0).toNumber(true) : message.availableSpaceBytes;
            if (message.totalReadBytes != null && message.hasOwnProperty("totalReadBytes"))
                if (typeof message.totalReadBytes === "number")
                    object.totalReadBytes = options.longs === String ? String(message.totalReadBytes) : message.totalReadBytes;
                else
                    object.totalReadBytes = options.longs === String ? $util.Long.prototype.toString.call(message.totalReadBytes) : options.longs === Number ? new $util.LongBits(message.totalReadBytes.low >>> 0, message.totalReadBytes.high >>> 0).toNumber(true) : message.totalReadBytes;
            if (message.totalWrittenBytes != null && message.hasOwnProperty("totalWrittenBytes"))
                if (typeof message.totalWrittenBytes === "number")
                    object.totalWrittenBytes = options.longs === String ? String(message.totalWrittenBytes) : message.totalWrittenBytes;
                else
                    object.totalWrittenBytes = options.longs === String ? $util.Long.prototype.toString.call(message.totalWrittenBytes) : options.longs === Number ? new $util.LongBits(message.totalWrittenBytes.low >>> 0, message.totalWrittenBytes.high >>> 0).toNumber(true) : message.totalWrittenBytes;
            return object;
        };

        /**
         * Converts this Disk to JSON.
         * @function toJSON
         * @memberof sysinfo.Disk
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Disk.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Disk
         * @function getTypeUrl
         * @memberof sysinfo.Disk
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Disk.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/sysinfo.Disk";
        };

        return Disk;
    })();

    sysinfo.Motherboard = (function() {

        /**
         * Properties of a Motherboard.
         * @memberof sysinfo
         * @interface IMotherboard
         * @property {string|null} [name] Motherboard name
         * @property {string|null} [vendorName] Motherboard vendorName
         * @property {string|null} [version] Motherboard version
         * @property {string|null} [serialNumber] Motherboard serialNumber
         * @property {string|null} [assetTag] Motherboard assetTag
         */

        /**
         * Constructs a new Motherboard.
         * @memberof sysinfo
         * @classdesc Represents a Motherboard.
         * @implements IMotherboard
         * @constructor
         * @param {sysinfo.IMotherboard=} [properties] Properties to set
         */
        function Motherboard(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Motherboard name.
         * @member {string} name
         * @memberof sysinfo.Motherboard
         * @instance
         */
        Motherboard.prototype.name = "";

        /**
         * Motherboard vendorName.
         * @member {string} vendorName
         * @memberof sysinfo.Motherboard
         * @instance
         */
        Motherboard.prototype.vendorName = "";

        /**
         * Motherboard version.
         * @member {string} version
         * @memberof sysinfo.Motherboard
         * @instance
         */
        Motherboard.prototype.version = "";

        /**
         * Motherboard serialNumber.
         * @member {string} serialNumber
         * @memberof sysinfo.Motherboard
         * @instance
         */
        Motherboard.prototype.serialNumber = "";

        /**
         * Motherboard assetTag.
         * @member {string} assetTag
         * @memberof sysinfo.Motherboard
         * @instance
         */
        Motherboard.prototype.assetTag = "";

        /**
         * Creates a new Motherboard instance using the specified properties.
         * @function create
         * @memberof sysinfo.Motherboard
         * @static
         * @param {sysinfo.IMotherboard=} [properties] Properties to set
         * @returns {sysinfo.Motherboard} Motherboard instance
         */
        Motherboard.create = function create(properties) {
            return new Motherboard(properties);
        };

        /**
         * Encodes the specified Motherboard message. Does not implicitly {@link sysinfo.Motherboard.verify|verify} messages.
         * @function encode
         * @memberof sysinfo.Motherboard
         * @static
         * @param {sysinfo.IMotherboard} message Motherboard message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Motherboard.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
            if (message.vendorName != null && Object.hasOwnProperty.call(message, "vendorName"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.vendorName);
            if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.version);
            if (message.serialNumber != null && Object.hasOwnProperty.call(message, "serialNumber"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.serialNumber);
            if (message.assetTag != null && Object.hasOwnProperty.call(message, "assetTag"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.assetTag);
            return writer;
        };

        /**
         * Encodes the specified Motherboard message, length delimited. Does not implicitly {@link sysinfo.Motherboard.verify|verify} messages.
         * @function encodeDelimited
         * @memberof sysinfo.Motherboard
         * @static
         * @param {sysinfo.IMotherboard} message Motherboard message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Motherboard.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Motherboard message from the specified reader or buffer.
         * @function decode
         * @memberof sysinfo.Motherboard
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {sysinfo.Motherboard} Motherboard
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Motherboard.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.sysinfo.Motherboard();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.name = reader.string();
                        break;
                    }
                case 2: {
                        message.vendorName = reader.string();
                        break;
                    }
                case 3: {
                        message.version = reader.string();
                        break;
                    }
                case 4: {
                        message.serialNumber = reader.string();
                        break;
                    }
                case 5: {
                        message.assetTag = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Motherboard message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof sysinfo.Motherboard
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {sysinfo.Motherboard} Motherboard
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Motherboard.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Motherboard message.
         * @function verify
         * @memberof sysinfo.Motherboard
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Motherboard.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.name != null && message.hasOwnProperty("name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.vendorName != null && message.hasOwnProperty("vendorName"))
                if (!$util.isString(message.vendorName))
                    return "vendorName: string expected";
            if (message.version != null && message.hasOwnProperty("version"))
                if (!$util.isString(message.version))
                    return "version: string expected";
            if (message.serialNumber != null && message.hasOwnProperty("serialNumber"))
                if (!$util.isString(message.serialNumber))
                    return "serialNumber: string expected";
            if (message.assetTag != null && message.hasOwnProperty("assetTag"))
                if (!$util.isString(message.assetTag))
                    return "assetTag: string expected";
            return null;
        };

        /**
         * Creates a Motherboard message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof sysinfo.Motherboard
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {sysinfo.Motherboard} Motherboard
         */
        Motherboard.fromObject = function fromObject(object, long) {
            if (object instanceof $root.sysinfo.Motherboard)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.sysinfo.Motherboard();
            if (object.name != null)
                message.name = String(object.name);
            if (object.vendorName != null)
                message.vendorName = String(object.vendorName);
            if (object.version != null)
                message.version = String(object.version);
            if (object.serialNumber != null)
                message.serialNumber = String(object.serialNumber);
            if (object.assetTag != null)
                message.assetTag = String(object.assetTag);
            return message;
        };

        /**
         * Creates a plain object from a Motherboard message. Also converts values to other types if specified.
         * @function toObject
         * @memberof sysinfo.Motherboard
         * @static
         * @param {sysinfo.Motherboard} message Motherboard
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Motherboard.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.name = "";
                object.vendorName = "";
                object.version = "";
                object.serialNumber = "";
                object.assetTag = "";
            }
            if (message.name != null && message.hasOwnProperty("name"))
                object.name = message.name;
            if (message.vendorName != null && message.hasOwnProperty("vendorName"))
                object.vendorName = message.vendorName;
            if (message.version != null && message.hasOwnProperty("version"))
                object.version = message.version;
            if (message.serialNumber != null && message.hasOwnProperty("serialNumber"))
                object.serialNumber = message.serialNumber;
            if (message.assetTag != null && message.hasOwnProperty("assetTag"))
                object.assetTag = message.assetTag;
            return object;
        };

        /**
         * Converts this Motherboard to JSON.
         * @function toJSON
         * @memberof sysinfo.Motherboard
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Motherboard.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Motherboard
         * @function getTypeUrl
         * @memberof sysinfo.Motherboard
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Motherboard.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/sysinfo.Motherboard";
        };

        return Motherboard;
    })();

    sysinfo.User = (function() {

        /**
         * Properties of a User.
         * @memberof sysinfo
         * @interface IUser
         * @property {string|null} [name] User name
         * @property {Array.<string>|null} [groups] User groups
         */

        /**
         * Constructs a new User.
         * @memberof sysinfo
         * @classdesc Represents a User.
         * @implements IUser
         * @constructor
         * @param {sysinfo.IUser=} [properties] Properties to set
         */
        function User(properties) {
            this.groups = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * User name.
         * @member {string} name
         * @memberof sysinfo.User
         * @instance
         */
        User.prototype.name = "";

        /**
         * User groups.
         * @member {Array.<string>} groups
         * @memberof sysinfo.User
         * @instance
         */
        User.prototype.groups = $util.emptyArray;

        /**
         * Creates a new User instance using the specified properties.
         * @function create
         * @memberof sysinfo.User
         * @static
         * @param {sysinfo.IUser=} [properties] Properties to set
         * @returns {sysinfo.User} User instance
         */
        User.create = function create(properties) {
            return new User(properties);
        };

        /**
         * Encodes the specified User message. Does not implicitly {@link sysinfo.User.verify|verify} messages.
         * @function encode
         * @memberof sysinfo.User
         * @static
         * @param {sysinfo.IUser} message User message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        User.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
            if (message.groups != null && message.groups.length)
                for (let i = 0; i < message.groups.length; ++i)
                    writer.uint32(/* id 10, wireType 2 =*/82).string(message.groups[i]);
            return writer;
        };

        /**
         * Encodes the specified User message, length delimited. Does not implicitly {@link sysinfo.User.verify|verify} messages.
         * @function encodeDelimited
         * @memberof sysinfo.User
         * @static
         * @param {sysinfo.IUser} message User message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        User.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a User message from the specified reader or buffer.
         * @function decode
         * @memberof sysinfo.User
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {sysinfo.User} User
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        User.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.sysinfo.User();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.name = reader.string();
                        break;
                    }
                case 10: {
                        if (!(message.groups && message.groups.length))
                            message.groups = [];
                        message.groups.push(reader.string());
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a User message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof sysinfo.User
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {sysinfo.User} User
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        User.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a User message.
         * @function verify
         * @memberof sysinfo.User
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        User.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.name != null && message.hasOwnProperty("name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.groups != null && message.hasOwnProperty("groups")) {
                if (!Array.isArray(message.groups))
                    return "groups: array expected";
                for (let i = 0; i < message.groups.length; ++i)
                    if (!$util.isString(message.groups[i]))
                        return "groups: string[] expected";
            }
            return null;
        };

        /**
         * Creates a User message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof sysinfo.User
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {sysinfo.User} User
         */
        User.fromObject = function fromObject(object, long) {
            if (object instanceof $root.sysinfo.User)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.sysinfo.User();
            if (object.name != null)
                message.name = String(object.name);
            if (object.groups) {
                if (!Array.isArray(object.groups))
                    throw TypeError(".sysinfo.User.groups: array expected");
                message.groups = [];
                for (let i = 0; i < object.groups.length; ++i)
                    message.groups[i] = String(object.groups[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a User message. Also converts values to other types if specified.
         * @function toObject
         * @memberof sysinfo.User
         * @static
         * @param {sysinfo.User} message User
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        User.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.groups = [];
            if (options.defaults)
                object.name = "";
            if (message.name != null && message.hasOwnProperty("name"))
                object.name = message.name;
            if (message.groups && message.groups.length) {
                object.groups = [];
                for (let j = 0; j < message.groups.length; ++j)
                    object.groups[j] = message.groups[j];
            }
            return object;
        };

        /**
         * Converts this User to JSON.
         * @function toJSON
         * @memberof sysinfo.User
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        User.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for User
         * @function getTypeUrl
         * @memberof sysinfo.User
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        User.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/sysinfo.User";
        };

        return User;
    })();

    sysinfo.NetworkIp = (function() {

        /**
         * Properties of a NetworkIp.
         * @memberof sysinfo
         * @interface INetworkIp
         * @property {string|null} [addr] NetworkIp addr
         */

        /**
         * Constructs a new NetworkIp.
         * @memberof sysinfo
         * @classdesc Represents a NetworkIp.
         * @implements INetworkIp
         * @constructor
         * @param {sysinfo.INetworkIp=} [properties] Properties to set
         */
        function NetworkIp(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * NetworkIp addr.
         * @member {string} addr
         * @memberof sysinfo.NetworkIp
         * @instance
         */
        NetworkIp.prototype.addr = "";

        /**
         * Creates a new NetworkIp instance using the specified properties.
         * @function create
         * @memberof sysinfo.NetworkIp
         * @static
         * @param {sysinfo.INetworkIp=} [properties] Properties to set
         * @returns {sysinfo.NetworkIp} NetworkIp instance
         */
        NetworkIp.create = function create(properties) {
            return new NetworkIp(properties);
        };

        /**
         * Encodes the specified NetworkIp message. Does not implicitly {@link sysinfo.NetworkIp.verify|verify} messages.
         * @function encode
         * @memberof sysinfo.NetworkIp
         * @static
         * @param {sysinfo.INetworkIp} message NetworkIp message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NetworkIp.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.addr != null && Object.hasOwnProperty.call(message, "addr"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.addr);
            return writer;
        };

        /**
         * Encodes the specified NetworkIp message, length delimited. Does not implicitly {@link sysinfo.NetworkIp.verify|verify} messages.
         * @function encodeDelimited
         * @memberof sysinfo.NetworkIp
         * @static
         * @param {sysinfo.INetworkIp} message NetworkIp message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NetworkIp.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a NetworkIp message from the specified reader or buffer.
         * @function decode
         * @memberof sysinfo.NetworkIp
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {sysinfo.NetworkIp} NetworkIp
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NetworkIp.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.sysinfo.NetworkIp();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.addr = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a NetworkIp message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof sysinfo.NetworkIp
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {sysinfo.NetworkIp} NetworkIp
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NetworkIp.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a NetworkIp message.
         * @function verify
         * @memberof sysinfo.NetworkIp
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        NetworkIp.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.addr != null && message.hasOwnProperty("addr"))
                if (!$util.isString(message.addr))
                    return "addr: string expected";
            return null;
        };

        /**
         * Creates a NetworkIp message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof sysinfo.NetworkIp
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {sysinfo.NetworkIp} NetworkIp
         */
        NetworkIp.fromObject = function fromObject(object, long) {
            if (object instanceof $root.sysinfo.NetworkIp)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.sysinfo.NetworkIp();
            if (object.addr != null)
                message.addr = String(object.addr);
            return message;
        };

        /**
         * Creates a plain object from a NetworkIp message. Also converts values to other types if specified.
         * @function toObject
         * @memberof sysinfo.NetworkIp
         * @static
         * @param {sysinfo.NetworkIp} message NetworkIp
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NetworkIp.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.addr = "";
            if (message.addr != null && message.hasOwnProperty("addr"))
                object.addr = message.addr;
            return object;
        };

        /**
         * Converts this NetworkIp to JSON.
         * @function toJSON
         * @memberof sysinfo.NetworkIp
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NetworkIp.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for NetworkIp
         * @function getTypeUrl
         * @memberof sysinfo.NetworkIp
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        NetworkIp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/sysinfo.NetworkIp";
        };

        return NetworkIp;
    })();

    sysinfo.Network = (function() {

        /**
         * Properties of a Network.
         * @memberof sysinfo
         * @interface INetwork
         * @property {string|null} [iface] Network iface
         * @property {string|null} [macAddress] Network macAddress
         * @property {Array.<sysinfo.INetworkIp>|null} [ips] Network ips
         * @property {Long|null} [bytesReceived] Network bytesReceived
         * @property {Long|null} [bytesTransmitted] Network bytesTransmitted
         * @property {Long|null} [packetsReceived] Network packetsReceived
         * @property {Long|null} [packetsTransmitted] Network packetsTransmitted
         * @property {Long|null} [errorsReceived] Network errorsReceived
         * @property {Long|null} [errorsTransmitted] Network errorsTransmitted
         */

        /**
         * Constructs a new Network.
         * @memberof sysinfo
         * @classdesc Represents a Network.
         * @implements INetwork
         * @constructor
         * @param {sysinfo.INetwork=} [properties] Properties to set
         */
        function Network(properties) {
            this.ips = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Network iface.
         * @member {string} iface
         * @memberof sysinfo.Network
         * @instance
         */
        Network.prototype.iface = "";

        /**
         * Network macAddress.
         * @member {string} macAddress
         * @memberof sysinfo.Network
         * @instance
         */
        Network.prototype.macAddress = "";

        /**
         * Network ips.
         * @member {Array.<sysinfo.INetworkIp>} ips
         * @memberof sysinfo.Network
         * @instance
         */
        Network.prototype.ips = $util.emptyArray;

        /**
         * Network bytesReceived.
         * @member {Long} bytesReceived
         * @memberof sysinfo.Network
         * @instance
         */
        Network.prototype.bytesReceived = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Network bytesTransmitted.
         * @member {Long} bytesTransmitted
         * @memberof sysinfo.Network
         * @instance
         */
        Network.prototype.bytesTransmitted = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Network packetsReceived.
         * @member {Long} packetsReceived
         * @memberof sysinfo.Network
         * @instance
         */
        Network.prototype.packetsReceived = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Network packetsTransmitted.
         * @member {Long} packetsTransmitted
         * @memberof sysinfo.Network
         * @instance
         */
        Network.prototype.packetsTransmitted = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Network errorsReceived.
         * @member {Long} errorsReceived
         * @memberof sysinfo.Network
         * @instance
         */
        Network.prototype.errorsReceived = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Network errorsTransmitted.
         * @member {Long} errorsTransmitted
         * @memberof sysinfo.Network
         * @instance
         */
        Network.prototype.errorsTransmitted = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new Network instance using the specified properties.
         * @function create
         * @memberof sysinfo.Network
         * @static
         * @param {sysinfo.INetwork=} [properties] Properties to set
         * @returns {sysinfo.Network} Network instance
         */
        Network.create = function create(properties) {
            return new Network(properties);
        };

        /**
         * Encodes the specified Network message. Does not implicitly {@link sysinfo.Network.verify|verify} messages.
         * @function encode
         * @memberof sysinfo.Network
         * @static
         * @param {sysinfo.INetwork} message Network message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Network.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.iface != null && Object.hasOwnProperty.call(message, "iface"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.iface);
            if (message.macAddress != null && Object.hasOwnProperty.call(message, "macAddress"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.macAddress);
            if (message.ips != null && message.ips.length)
                for (let i = 0; i < message.ips.length; ++i)
                    $root.sysinfo.NetworkIp.encode(message.ips[i], writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            if (message.bytesReceived != null && Object.hasOwnProperty.call(message, "bytesReceived"))
                writer.uint32(/* id 20, wireType 0 =*/160).uint64(message.bytesReceived);
            if (message.bytesTransmitted != null && Object.hasOwnProperty.call(message, "bytesTransmitted"))
                writer.uint32(/* id 21, wireType 0 =*/168).uint64(message.bytesTransmitted);
            if (message.packetsReceived != null && Object.hasOwnProperty.call(message, "packetsReceived"))
                writer.uint32(/* id 22, wireType 0 =*/176).uint64(message.packetsReceived);
            if (message.packetsTransmitted != null && Object.hasOwnProperty.call(message, "packetsTransmitted"))
                writer.uint32(/* id 23, wireType 0 =*/184).uint64(message.packetsTransmitted);
            if (message.errorsReceived != null && Object.hasOwnProperty.call(message, "errorsReceived"))
                writer.uint32(/* id 24, wireType 0 =*/192).uint64(message.errorsReceived);
            if (message.errorsTransmitted != null && Object.hasOwnProperty.call(message, "errorsTransmitted"))
                writer.uint32(/* id 25, wireType 0 =*/200).uint64(message.errorsTransmitted);
            return writer;
        };

        /**
         * Encodes the specified Network message, length delimited. Does not implicitly {@link sysinfo.Network.verify|verify} messages.
         * @function encodeDelimited
         * @memberof sysinfo.Network
         * @static
         * @param {sysinfo.INetwork} message Network message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Network.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Network message from the specified reader or buffer.
         * @function decode
         * @memberof sysinfo.Network
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {sysinfo.Network} Network
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Network.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.sysinfo.Network();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.iface = reader.string();
                        break;
                    }
                case 2: {
                        message.macAddress = reader.string();
                        break;
                    }
                case 10: {
                        if (!(message.ips && message.ips.length))
                            message.ips = [];
                        message.ips.push($root.sysinfo.NetworkIp.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 20: {
                        message.bytesReceived = reader.uint64();
                        break;
                    }
                case 21: {
                        message.bytesTransmitted = reader.uint64();
                        break;
                    }
                case 22: {
                        message.packetsReceived = reader.uint64();
                        break;
                    }
                case 23: {
                        message.packetsTransmitted = reader.uint64();
                        break;
                    }
                case 24: {
                        message.errorsReceived = reader.uint64();
                        break;
                    }
                case 25: {
                        message.errorsTransmitted = reader.uint64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Network message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof sysinfo.Network
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {sysinfo.Network} Network
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Network.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Network message.
         * @function verify
         * @memberof sysinfo.Network
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Network.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.iface != null && message.hasOwnProperty("iface"))
                if (!$util.isString(message.iface))
                    return "iface: string expected";
            if (message.macAddress != null && message.hasOwnProperty("macAddress"))
                if (!$util.isString(message.macAddress))
                    return "macAddress: string expected";
            if (message.ips != null && message.hasOwnProperty("ips")) {
                if (!Array.isArray(message.ips))
                    return "ips: array expected";
                for (let i = 0; i < message.ips.length; ++i) {
                    let error = $root.sysinfo.NetworkIp.verify(message.ips[i], long + 1);
                    if (error)
                        return "ips." + error;
                }
            }
            if (message.bytesReceived != null && message.hasOwnProperty("bytesReceived"))
                if (!$util.isInteger(message.bytesReceived) && !(message.bytesReceived && $util.isInteger(message.bytesReceived.low) && $util.isInteger(message.bytesReceived.high)))
                    return "bytesReceived: integer|Long expected";
            if (message.bytesTransmitted != null && message.hasOwnProperty("bytesTransmitted"))
                if (!$util.isInteger(message.bytesTransmitted) && !(message.bytesTransmitted && $util.isInteger(message.bytesTransmitted.low) && $util.isInteger(message.bytesTransmitted.high)))
                    return "bytesTransmitted: integer|Long expected";
            if (message.packetsReceived != null && message.hasOwnProperty("packetsReceived"))
                if (!$util.isInteger(message.packetsReceived) && !(message.packetsReceived && $util.isInteger(message.packetsReceived.low) && $util.isInteger(message.packetsReceived.high)))
                    return "packetsReceived: integer|Long expected";
            if (message.packetsTransmitted != null && message.hasOwnProperty("packetsTransmitted"))
                if (!$util.isInteger(message.packetsTransmitted) && !(message.packetsTransmitted && $util.isInteger(message.packetsTransmitted.low) && $util.isInteger(message.packetsTransmitted.high)))
                    return "packetsTransmitted: integer|Long expected";
            if (message.errorsReceived != null && message.hasOwnProperty("errorsReceived"))
                if (!$util.isInteger(message.errorsReceived) && !(message.errorsReceived && $util.isInteger(message.errorsReceived.low) && $util.isInteger(message.errorsReceived.high)))
                    return "errorsReceived: integer|Long expected";
            if (message.errorsTransmitted != null && message.hasOwnProperty("errorsTransmitted"))
                if (!$util.isInteger(message.errorsTransmitted) && !(message.errorsTransmitted && $util.isInteger(message.errorsTransmitted.low) && $util.isInteger(message.errorsTransmitted.high)))
                    return "errorsTransmitted: integer|Long expected";
            return null;
        };

        /**
         * Creates a Network message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof sysinfo.Network
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {sysinfo.Network} Network
         */
        Network.fromObject = function fromObject(object, long) {
            if (object instanceof $root.sysinfo.Network)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.sysinfo.Network();
            if (object.iface != null)
                message.iface = String(object.iface);
            if (object.macAddress != null)
                message.macAddress = String(object.macAddress);
            if (object.ips) {
                if (!Array.isArray(object.ips))
                    throw TypeError(".sysinfo.Network.ips: array expected");
                message.ips = [];
                for (let i = 0; i < object.ips.length; ++i) {
                    if (typeof object.ips[i] !== "object")
                        throw TypeError(".sysinfo.Network.ips: object expected");
                    message.ips[i] = $root.sysinfo.NetworkIp.fromObject(object.ips[i], long + 1);
                }
            }
            if (object.bytesReceived != null)
                if ($util.Long)
                    (message.bytesReceived = $util.Long.fromValue(object.bytesReceived)).unsigned = true;
                else if (typeof object.bytesReceived === "string")
                    message.bytesReceived = parseInt(object.bytesReceived, 10);
                else if (typeof object.bytesReceived === "number")
                    message.bytesReceived = object.bytesReceived;
                else if (typeof object.bytesReceived === "object")
                    message.bytesReceived = new $util.LongBits(object.bytesReceived.low >>> 0, object.bytesReceived.high >>> 0).toNumber(true);
            if (object.bytesTransmitted != null)
                if ($util.Long)
                    (message.bytesTransmitted = $util.Long.fromValue(object.bytesTransmitted)).unsigned = true;
                else if (typeof object.bytesTransmitted === "string")
                    message.bytesTransmitted = parseInt(object.bytesTransmitted, 10);
                else if (typeof object.bytesTransmitted === "number")
                    message.bytesTransmitted = object.bytesTransmitted;
                else if (typeof object.bytesTransmitted === "object")
                    message.bytesTransmitted = new $util.LongBits(object.bytesTransmitted.low >>> 0, object.bytesTransmitted.high >>> 0).toNumber(true);
            if (object.packetsReceived != null)
                if ($util.Long)
                    (message.packetsReceived = $util.Long.fromValue(object.packetsReceived)).unsigned = true;
                else if (typeof object.packetsReceived === "string")
                    message.packetsReceived = parseInt(object.packetsReceived, 10);
                else if (typeof object.packetsReceived === "number")
                    message.packetsReceived = object.packetsReceived;
                else if (typeof object.packetsReceived === "object")
                    message.packetsReceived = new $util.LongBits(object.packetsReceived.low >>> 0, object.packetsReceived.high >>> 0).toNumber(true);
            if (object.packetsTransmitted != null)
                if ($util.Long)
                    (message.packetsTransmitted = $util.Long.fromValue(object.packetsTransmitted)).unsigned = true;
                else if (typeof object.packetsTransmitted === "string")
                    message.packetsTransmitted = parseInt(object.packetsTransmitted, 10);
                else if (typeof object.packetsTransmitted === "number")
                    message.packetsTransmitted = object.packetsTransmitted;
                else if (typeof object.packetsTransmitted === "object")
                    message.packetsTransmitted = new $util.LongBits(object.packetsTransmitted.low >>> 0, object.packetsTransmitted.high >>> 0).toNumber(true);
            if (object.errorsReceived != null)
                if ($util.Long)
                    (message.errorsReceived = $util.Long.fromValue(object.errorsReceived)).unsigned = true;
                else if (typeof object.errorsReceived === "string")
                    message.errorsReceived = parseInt(object.errorsReceived, 10);
                else if (typeof object.errorsReceived === "number")
                    message.errorsReceived = object.errorsReceived;
                else if (typeof object.errorsReceived === "object")
                    message.errorsReceived = new $util.LongBits(object.errorsReceived.low >>> 0, object.errorsReceived.high >>> 0).toNumber(true);
            if (object.errorsTransmitted != null)
                if ($util.Long)
                    (message.errorsTransmitted = $util.Long.fromValue(object.errorsTransmitted)).unsigned = true;
                else if (typeof object.errorsTransmitted === "string")
                    message.errorsTransmitted = parseInt(object.errorsTransmitted, 10);
                else if (typeof object.errorsTransmitted === "number")
                    message.errorsTransmitted = object.errorsTransmitted;
                else if (typeof object.errorsTransmitted === "object")
                    message.errorsTransmitted = new $util.LongBits(object.errorsTransmitted.low >>> 0, object.errorsTransmitted.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a Network message. Also converts values to other types if specified.
         * @function toObject
         * @memberof sysinfo.Network
         * @static
         * @param {sysinfo.Network} message Network
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Network.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.ips = [];
            if (options.defaults) {
                object.iface = "";
                object.macAddress = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.bytesReceived = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.bytesReceived = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.bytesTransmitted = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.bytesTransmitted = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.packetsReceived = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.packetsReceived = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.packetsTransmitted = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.packetsTransmitted = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.errorsReceived = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.errorsReceived = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.errorsTransmitted = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.errorsTransmitted = options.longs === String ? "0" : 0;
            }
            if (message.iface != null && message.hasOwnProperty("iface"))
                object.iface = message.iface;
            if (message.macAddress != null && message.hasOwnProperty("macAddress"))
                object.macAddress = message.macAddress;
            if (message.ips && message.ips.length) {
                object.ips = [];
                for (let j = 0; j < message.ips.length; ++j)
                    object.ips[j] = $root.sysinfo.NetworkIp.toObject(message.ips[j], options);
            }
            if (message.bytesReceived != null && message.hasOwnProperty("bytesReceived"))
                if (typeof message.bytesReceived === "number")
                    object.bytesReceived = options.longs === String ? String(message.bytesReceived) : message.bytesReceived;
                else
                    object.bytesReceived = options.longs === String ? $util.Long.prototype.toString.call(message.bytesReceived) : options.longs === Number ? new $util.LongBits(message.bytesReceived.low >>> 0, message.bytesReceived.high >>> 0).toNumber(true) : message.bytesReceived;
            if (message.bytesTransmitted != null && message.hasOwnProperty("bytesTransmitted"))
                if (typeof message.bytesTransmitted === "number")
                    object.bytesTransmitted = options.longs === String ? String(message.bytesTransmitted) : message.bytesTransmitted;
                else
                    object.bytesTransmitted = options.longs === String ? $util.Long.prototype.toString.call(message.bytesTransmitted) : options.longs === Number ? new $util.LongBits(message.bytesTransmitted.low >>> 0, message.bytesTransmitted.high >>> 0).toNumber(true) : message.bytesTransmitted;
            if (message.packetsReceived != null && message.hasOwnProperty("packetsReceived"))
                if (typeof message.packetsReceived === "number")
                    object.packetsReceived = options.longs === String ? String(message.packetsReceived) : message.packetsReceived;
                else
                    object.packetsReceived = options.longs === String ? $util.Long.prototype.toString.call(message.packetsReceived) : options.longs === Number ? new $util.LongBits(message.packetsReceived.low >>> 0, message.packetsReceived.high >>> 0).toNumber(true) : message.packetsReceived;
            if (message.packetsTransmitted != null && message.hasOwnProperty("packetsTransmitted"))
                if (typeof message.packetsTransmitted === "number")
                    object.packetsTransmitted = options.longs === String ? String(message.packetsTransmitted) : message.packetsTransmitted;
                else
                    object.packetsTransmitted = options.longs === String ? $util.Long.prototype.toString.call(message.packetsTransmitted) : options.longs === Number ? new $util.LongBits(message.packetsTransmitted.low >>> 0, message.packetsTransmitted.high >>> 0).toNumber(true) : message.packetsTransmitted;
            if (message.errorsReceived != null && message.hasOwnProperty("errorsReceived"))
                if (typeof message.errorsReceived === "number")
                    object.errorsReceived = options.longs === String ? String(message.errorsReceived) : message.errorsReceived;
                else
                    object.errorsReceived = options.longs === String ? $util.Long.prototype.toString.call(message.errorsReceived) : options.longs === Number ? new $util.LongBits(message.errorsReceived.low >>> 0, message.errorsReceived.high >>> 0).toNumber(true) : message.errorsReceived;
            if (message.errorsTransmitted != null && message.hasOwnProperty("errorsTransmitted"))
                if (typeof message.errorsTransmitted === "number")
                    object.errorsTransmitted = options.longs === String ? String(message.errorsTransmitted) : message.errorsTransmitted;
                else
                    object.errorsTransmitted = options.longs === String ? $util.Long.prototype.toString.call(message.errorsTransmitted) : options.longs === Number ? new $util.LongBits(message.errorsTransmitted.low >>> 0, message.errorsTransmitted.high >>> 0).toNumber(true) : message.errorsTransmitted;
            return object;
        };

        /**
         * Converts this Network to JSON.
         * @function toJSON
         * @memberof sysinfo.Network
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Network.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Network
         * @function getTypeUrl
         * @memberof sysinfo.Network
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Network.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/sysinfo.Network";
        };

        return Network;
    })();

    sysinfo.ProcessInfo = (function() {

        /**
         * Properties of a ProcessInfo.
         * @memberof sysinfo
         * @interface IProcessInfo
         * @property {number|null} [pid] ProcessInfo pid
         * @property {Long|null} [parentPid] ProcessInfo parentPid
         * @property {string|null} [name] ProcessInfo name
         * @property {string|null} [exe] ProcessInfo exe
         * @property {Array.<string>|null} [cmd] ProcessInfo cmd
         * @property {string|null} [cwd] ProcessInfo cwd
         * @property {string|null} [kind] ProcessInfo kind
         * @property {string|null} [status] ProcessInfo status
         * @property {string|null} [userName] ProcessInfo userName
         * @property {Long|null} [uid] ProcessInfo uid
         * @property {Long|null} [gid] ProcessInfo gid
         * @property {Long|null} [sessionId] ProcessInfo sessionId
         * @property {Long|null} [startTimeEpochSeconds] ProcessInfo startTimeEpochSeconds
         * @property {Long|null} [runTimeSeconds] ProcessInfo runTimeSeconds
         * @property {number|null} [threadCount] ProcessInfo threadCount
         * @property {number|null} [cpuUsage] ProcessInfo cpuUsage
         * @property {Long|null} [memoryBytes] ProcessInfo memoryBytes
         * @property {Long|null} [virtualMemoryBytes] ProcessInfo virtualMemoryBytes
         * @property {Long|null} [accumulatedCpuTimeMs] ProcessInfo accumulatedCpuTimeMs
         * @property {Long|null} [totalReadBytes] ProcessInfo totalReadBytes
         * @property {Long|null} [totalWrittenBytes] ProcessInfo totalWrittenBytes
         * @property {Long|null} [readBytes] ProcessInfo readBytes
         * @property {Long|null} [writtenBytes] ProcessInfo writtenBytes
         */

        /**
         * Constructs a new ProcessInfo.
         * @memberof sysinfo
         * @classdesc Represents a ProcessInfo.
         * @implements IProcessInfo
         * @constructor
         * @param {sysinfo.IProcessInfo=} [properties] Properties to set
         */
        function ProcessInfo(properties) {
            this.cmd = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ProcessInfo pid.
         * @member {number} pid
         * @memberof sysinfo.ProcessInfo
         * @instance
         */
        ProcessInfo.prototype.pid = 0;

        /**
         * ProcessInfo parentPid.
         * @member {Long} parentPid
         * @memberof sysinfo.ProcessInfo
         * @instance
         */
        ProcessInfo.prototype.parentPid = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * ProcessInfo name.
         * @member {string} name
         * @memberof sysinfo.ProcessInfo
         * @instance
         */
        ProcessInfo.prototype.name = "";

        /**
         * ProcessInfo exe.
         * @member {string} exe
         * @memberof sysinfo.ProcessInfo
         * @instance
         */
        ProcessInfo.prototype.exe = "";

        /**
         * ProcessInfo cmd.
         * @member {Array.<string>} cmd
         * @memberof sysinfo.ProcessInfo
         * @instance
         */
        ProcessInfo.prototype.cmd = $util.emptyArray;

        /**
         * ProcessInfo cwd.
         * @member {string} cwd
         * @memberof sysinfo.ProcessInfo
         * @instance
         */
        ProcessInfo.prototype.cwd = "";

        /**
         * ProcessInfo kind.
         * @member {string} kind
         * @memberof sysinfo.ProcessInfo
         * @instance
         */
        ProcessInfo.prototype.kind = "";

        /**
         * ProcessInfo status.
         * @member {string} status
         * @memberof sysinfo.ProcessInfo
         * @instance
         */
        ProcessInfo.prototype.status = "";

        /**
         * ProcessInfo userName.
         * @member {string} userName
         * @memberof sysinfo.ProcessInfo
         * @instance
         */
        ProcessInfo.prototype.userName = "";

        /**
         * ProcessInfo uid.
         * @member {Long} uid
         * @memberof sysinfo.ProcessInfo
         * @instance
         */
        ProcessInfo.prototype.uid = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * ProcessInfo gid.
         * @member {Long} gid
         * @memberof sysinfo.ProcessInfo
         * @instance
         */
        ProcessInfo.prototype.gid = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * ProcessInfo sessionId.
         * @member {Long} sessionId
         * @memberof sysinfo.ProcessInfo
         * @instance
         */
        ProcessInfo.prototype.sessionId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * ProcessInfo startTimeEpochSeconds.
         * @member {Long} startTimeEpochSeconds
         * @memberof sysinfo.ProcessInfo
         * @instance
         */
        ProcessInfo.prototype.startTimeEpochSeconds = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ProcessInfo runTimeSeconds.
         * @member {Long} runTimeSeconds
         * @memberof sysinfo.ProcessInfo
         * @instance
         */
        ProcessInfo.prototype.runTimeSeconds = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ProcessInfo threadCount.
         * @member {number} threadCount
         * @memberof sysinfo.ProcessInfo
         * @instance
         */
        ProcessInfo.prototype.threadCount = 0;

        /**
         * ProcessInfo cpuUsage.
         * @member {number} cpuUsage
         * @memberof sysinfo.ProcessInfo
         * @instance
         */
        ProcessInfo.prototype.cpuUsage = 0;

        /**
         * ProcessInfo memoryBytes.
         * @member {Long} memoryBytes
         * @memberof sysinfo.ProcessInfo
         * @instance
         */
        ProcessInfo.prototype.memoryBytes = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ProcessInfo virtualMemoryBytes.
         * @member {Long} virtualMemoryBytes
         * @memberof sysinfo.ProcessInfo
         * @instance
         */
        ProcessInfo.prototype.virtualMemoryBytes = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ProcessInfo accumulatedCpuTimeMs.
         * @member {Long} accumulatedCpuTimeMs
         * @memberof sysinfo.ProcessInfo
         * @instance
         */
        ProcessInfo.prototype.accumulatedCpuTimeMs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ProcessInfo totalReadBytes.
         * @member {Long} totalReadBytes
         * @memberof sysinfo.ProcessInfo
         * @instance
         */
        ProcessInfo.prototype.totalReadBytes = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ProcessInfo totalWrittenBytes.
         * @member {Long} totalWrittenBytes
         * @memberof sysinfo.ProcessInfo
         * @instance
         */
        ProcessInfo.prototype.totalWrittenBytes = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ProcessInfo readBytes.
         * @member {Long} readBytes
         * @memberof sysinfo.ProcessInfo
         * @instance
         */
        ProcessInfo.prototype.readBytes = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ProcessInfo writtenBytes.
         * @member {Long} writtenBytes
         * @memberof sysinfo.ProcessInfo
         * @instance
         */
        ProcessInfo.prototype.writtenBytes = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new ProcessInfo instance using the specified properties.
         * @function create
         * @memberof sysinfo.ProcessInfo
         * @static
         * @param {sysinfo.IProcessInfo=} [properties] Properties to set
         * @returns {sysinfo.ProcessInfo} ProcessInfo instance
         */
        ProcessInfo.create = function create(properties) {
            return new ProcessInfo(properties);
        };

        /**
         * Encodes the specified ProcessInfo message. Does not implicitly {@link sysinfo.ProcessInfo.verify|verify} messages.
         * @function encode
         * @memberof sysinfo.ProcessInfo
         * @static
         * @param {sysinfo.IProcessInfo} message ProcessInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ProcessInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.pid != null && Object.hasOwnProperty.call(message, "pid"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.pid);
            if (message.parentPid != null && Object.hasOwnProperty.call(message, "parentPid"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.parentPid);
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.name);
            if (message.exe != null && Object.hasOwnProperty.call(message, "exe"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.exe);
            if (message.cmd != null && message.cmd.length)
                for (let i = 0; i < message.cmd.length; ++i)
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.cmd[i]);
            if (message.cwd != null && Object.hasOwnProperty.call(message, "cwd"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.cwd);
            if (message.kind != null && Object.hasOwnProperty.call(message, "kind"))
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.kind);
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                writer.uint32(/* id 8, wireType 2 =*/66).string(message.status);
            if (message.userName != null && Object.hasOwnProperty.call(message, "userName"))
                writer.uint32(/* id 9, wireType 2 =*/74).string(message.userName);
            if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                writer.uint32(/* id 10, wireType 0 =*/80).int64(message.uid);
            if (message.gid != null && Object.hasOwnProperty.call(message, "gid"))
                writer.uint32(/* id 11, wireType 0 =*/88).int64(message.gid);
            if (message.sessionId != null && Object.hasOwnProperty.call(message, "sessionId"))
                writer.uint32(/* id 12, wireType 0 =*/96).int64(message.sessionId);
            if (message.startTimeEpochSeconds != null && Object.hasOwnProperty.call(message, "startTimeEpochSeconds"))
                writer.uint32(/* id 13, wireType 0 =*/104).uint64(message.startTimeEpochSeconds);
            if (message.runTimeSeconds != null && Object.hasOwnProperty.call(message, "runTimeSeconds"))
                writer.uint32(/* id 14, wireType 0 =*/112).uint64(message.runTimeSeconds);
            if (message.threadCount != null && Object.hasOwnProperty.call(message, "threadCount"))
                writer.uint32(/* id 15, wireType 0 =*/120).uint32(message.threadCount);
            if (message.cpuUsage != null && Object.hasOwnProperty.call(message, "cpuUsage"))
                writer.uint32(/* id 20, wireType 5 =*/165).float(message.cpuUsage);
            if (message.memoryBytes != null && Object.hasOwnProperty.call(message, "memoryBytes"))
                writer.uint32(/* id 21, wireType 0 =*/168).uint64(message.memoryBytes);
            if (message.virtualMemoryBytes != null && Object.hasOwnProperty.call(message, "virtualMemoryBytes"))
                writer.uint32(/* id 22, wireType 0 =*/176).uint64(message.virtualMemoryBytes);
            if (message.accumulatedCpuTimeMs != null && Object.hasOwnProperty.call(message, "accumulatedCpuTimeMs"))
                writer.uint32(/* id 23, wireType 0 =*/184).uint64(message.accumulatedCpuTimeMs);
            if (message.totalReadBytes != null && Object.hasOwnProperty.call(message, "totalReadBytes"))
                writer.uint32(/* id 30, wireType 0 =*/240).uint64(message.totalReadBytes);
            if (message.totalWrittenBytes != null && Object.hasOwnProperty.call(message, "totalWrittenBytes"))
                writer.uint32(/* id 31, wireType 0 =*/248).uint64(message.totalWrittenBytes);
            if (message.readBytes != null && Object.hasOwnProperty.call(message, "readBytes"))
                writer.uint32(/* id 32, wireType 0 =*/256).uint64(message.readBytes);
            if (message.writtenBytes != null && Object.hasOwnProperty.call(message, "writtenBytes"))
                writer.uint32(/* id 33, wireType 0 =*/264).uint64(message.writtenBytes);
            return writer;
        };

        /**
         * Encodes the specified ProcessInfo message, length delimited. Does not implicitly {@link sysinfo.ProcessInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof sysinfo.ProcessInfo
         * @static
         * @param {sysinfo.IProcessInfo} message ProcessInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ProcessInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ProcessInfo message from the specified reader or buffer.
         * @function decode
         * @memberof sysinfo.ProcessInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {sysinfo.ProcessInfo} ProcessInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ProcessInfo.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.sysinfo.ProcessInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.pid = reader.uint32();
                        break;
                    }
                case 2: {
                        message.parentPid = reader.int64();
                        break;
                    }
                case 3: {
                        message.name = reader.string();
                        break;
                    }
                case 4: {
                        message.exe = reader.string();
                        break;
                    }
                case 5: {
                        if (!(message.cmd && message.cmd.length))
                            message.cmd = [];
                        message.cmd.push(reader.string());
                        break;
                    }
                case 6: {
                        message.cwd = reader.string();
                        break;
                    }
                case 7: {
                        message.kind = reader.string();
                        break;
                    }
                case 8: {
                        message.status = reader.string();
                        break;
                    }
                case 9: {
                        message.userName = reader.string();
                        break;
                    }
                case 10: {
                        message.uid = reader.int64();
                        break;
                    }
                case 11: {
                        message.gid = reader.int64();
                        break;
                    }
                case 12: {
                        message.sessionId = reader.int64();
                        break;
                    }
                case 13: {
                        message.startTimeEpochSeconds = reader.uint64();
                        break;
                    }
                case 14: {
                        message.runTimeSeconds = reader.uint64();
                        break;
                    }
                case 15: {
                        message.threadCount = reader.uint32();
                        break;
                    }
                case 20: {
                        message.cpuUsage = reader.float();
                        break;
                    }
                case 21: {
                        message.memoryBytes = reader.uint64();
                        break;
                    }
                case 22: {
                        message.virtualMemoryBytes = reader.uint64();
                        break;
                    }
                case 23: {
                        message.accumulatedCpuTimeMs = reader.uint64();
                        break;
                    }
                case 30: {
                        message.totalReadBytes = reader.uint64();
                        break;
                    }
                case 31: {
                        message.totalWrittenBytes = reader.uint64();
                        break;
                    }
                case 32: {
                        message.readBytes = reader.uint64();
                        break;
                    }
                case 33: {
                        message.writtenBytes = reader.uint64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ProcessInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof sysinfo.ProcessInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {sysinfo.ProcessInfo} ProcessInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ProcessInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ProcessInfo message.
         * @function verify
         * @memberof sysinfo.ProcessInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ProcessInfo.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.pid != null && message.hasOwnProperty("pid"))
                if (!$util.isInteger(message.pid))
                    return "pid: integer expected";
            if (message.parentPid != null && message.hasOwnProperty("parentPid"))
                if (!$util.isInteger(message.parentPid) && !(message.parentPid && $util.isInteger(message.parentPid.low) && $util.isInteger(message.parentPid.high)))
                    return "parentPid: integer|Long expected";
            if (message.name != null && message.hasOwnProperty("name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.exe != null && message.hasOwnProperty("exe"))
                if (!$util.isString(message.exe))
                    return "exe: string expected";
            if (message.cmd != null && message.hasOwnProperty("cmd")) {
                if (!Array.isArray(message.cmd))
                    return "cmd: array expected";
                for (let i = 0; i < message.cmd.length; ++i)
                    if (!$util.isString(message.cmd[i]))
                        return "cmd: string[] expected";
            }
            if (message.cwd != null && message.hasOwnProperty("cwd"))
                if (!$util.isString(message.cwd))
                    return "cwd: string expected";
            if (message.kind != null && message.hasOwnProperty("kind"))
                if (!$util.isString(message.kind))
                    return "kind: string expected";
            if (message.status != null && message.hasOwnProperty("status"))
                if (!$util.isString(message.status))
                    return "status: string expected";
            if (message.userName != null && message.hasOwnProperty("userName"))
                if (!$util.isString(message.userName))
                    return "userName: string expected";
            if (message.uid != null && message.hasOwnProperty("uid"))
                if (!$util.isInteger(message.uid) && !(message.uid && $util.isInteger(message.uid.low) && $util.isInteger(message.uid.high)))
                    return "uid: integer|Long expected";
            if (message.gid != null && message.hasOwnProperty("gid"))
                if (!$util.isInteger(message.gid) && !(message.gid && $util.isInteger(message.gid.low) && $util.isInteger(message.gid.high)))
                    return "gid: integer|Long expected";
            if (message.sessionId != null && message.hasOwnProperty("sessionId"))
                if (!$util.isInteger(message.sessionId) && !(message.sessionId && $util.isInteger(message.sessionId.low) && $util.isInteger(message.sessionId.high)))
                    return "sessionId: integer|Long expected";
            if (message.startTimeEpochSeconds != null && message.hasOwnProperty("startTimeEpochSeconds"))
                if (!$util.isInteger(message.startTimeEpochSeconds) && !(message.startTimeEpochSeconds && $util.isInteger(message.startTimeEpochSeconds.low) && $util.isInteger(message.startTimeEpochSeconds.high)))
                    return "startTimeEpochSeconds: integer|Long expected";
            if (message.runTimeSeconds != null && message.hasOwnProperty("runTimeSeconds"))
                if (!$util.isInteger(message.runTimeSeconds) && !(message.runTimeSeconds && $util.isInteger(message.runTimeSeconds.low) && $util.isInteger(message.runTimeSeconds.high)))
                    return "runTimeSeconds: integer|Long expected";
            if (message.threadCount != null && message.hasOwnProperty("threadCount"))
                if (!$util.isInteger(message.threadCount))
                    return "threadCount: integer expected";
            if (message.cpuUsage != null && message.hasOwnProperty("cpuUsage"))
                if (typeof message.cpuUsage !== "number")
                    return "cpuUsage: number expected";
            if (message.memoryBytes != null && message.hasOwnProperty("memoryBytes"))
                if (!$util.isInteger(message.memoryBytes) && !(message.memoryBytes && $util.isInteger(message.memoryBytes.low) && $util.isInteger(message.memoryBytes.high)))
                    return "memoryBytes: integer|Long expected";
            if (message.virtualMemoryBytes != null && message.hasOwnProperty("virtualMemoryBytes"))
                if (!$util.isInteger(message.virtualMemoryBytes) && !(message.virtualMemoryBytes && $util.isInteger(message.virtualMemoryBytes.low) && $util.isInteger(message.virtualMemoryBytes.high)))
                    return "virtualMemoryBytes: integer|Long expected";
            if (message.accumulatedCpuTimeMs != null && message.hasOwnProperty("accumulatedCpuTimeMs"))
                if (!$util.isInteger(message.accumulatedCpuTimeMs) && !(message.accumulatedCpuTimeMs && $util.isInteger(message.accumulatedCpuTimeMs.low) && $util.isInteger(message.accumulatedCpuTimeMs.high)))
                    return "accumulatedCpuTimeMs: integer|Long expected";
            if (message.totalReadBytes != null && message.hasOwnProperty("totalReadBytes"))
                if (!$util.isInteger(message.totalReadBytes) && !(message.totalReadBytes && $util.isInteger(message.totalReadBytes.low) && $util.isInteger(message.totalReadBytes.high)))
                    return "totalReadBytes: integer|Long expected";
            if (message.totalWrittenBytes != null && message.hasOwnProperty("totalWrittenBytes"))
                if (!$util.isInteger(message.totalWrittenBytes) && !(message.totalWrittenBytes && $util.isInteger(message.totalWrittenBytes.low) && $util.isInteger(message.totalWrittenBytes.high)))
                    return "totalWrittenBytes: integer|Long expected";
            if (message.readBytes != null && message.hasOwnProperty("readBytes"))
                if (!$util.isInteger(message.readBytes) && !(message.readBytes && $util.isInteger(message.readBytes.low) && $util.isInteger(message.readBytes.high)))
                    return "readBytes: integer|Long expected";
            if (message.writtenBytes != null && message.hasOwnProperty("writtenBytes"))
                if (!$util.isInteger(message.writtenBytes) && !(message.writtenBytes && $util.isInteger(message.writtenBytes.low) && $util.isInteger(message.writtenBytes.high)))
                    return "writtenBytes: integer|Long expected";
            return null;
        };

        /**
         * Creates a ProcessInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof sysinfo.ProcessInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {sysinfo.ProcessInfo} ProcessInfo
         */
        ProcessInfo.fromObject = function fromObject(object, long) {
            if (object instanceof $root.sysinfo.ProcessInfo)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.sysinfo.ProcessInfo();
            if (object.pid != null)
                message.pid = object.pid >>> 0;
            if (object.parentPid != null)
                if ($util.Long)
                    (message.parentPid = $util.Long.fromValue(object.parentPid)).unsigned = false;
                else if (typeof object.parentPid === "string")
                    message.parentPid = parseInt(object.parentPid, 10);
                else if (typeof object.parentPid === "number")
                    message.parentPid = object.parentPid;
                else if (typeof object.parentPid === "object")
                    message.parentPid = new $util.LongBits(object.parentPid.low >>> 0, object.parentPid.high >>> 0).toNumber();
            if (object.name != null)
                message.name = String(object.name);
            if (object.exe != null)
                message.exe = String(object.exe);
            if (object.cmd) {
                if (!Array.isArray(object.cmd))
                    throw TypeError(".sysinfo.ProcessInfo.cmd: array expected");
                message.cmd = [];
                for (let i = 0; i < object.cmd.length; ++i)
                    message.cmd[i] = String(object.cmd[i]);
            }
            if (object.cwd != null)
                message.cwd = String(object.cwd);
            if (object.kind != null)
                message.kind = String(object.kind);
            if (object.status != null)
                message.status = String(object.status);
            if (object.userName != null)
                message.userName = String(object.userName);
            if (object.uid != null)
                if ($util.Long)
                    (message.uid = $util.Long.fromValue(object.uid)).unsigned = false;
                else if (typeof object.uid === "string")
                    message.uid = parseInt(object.uid, 10);
                else if (typeof object.uid === "number")
                    message.uid = object.uid;
                else if (typeof object.uid === "object")
                    message.uid = new $util.LongBits(object.uid.low >>> 0, object.uid.high >>> 0).toNumber();
            if (object.gid != null)
                if ($util.Long)
                    (message.gid = $util.Long.fromValue(object.gid)).unsigned = false;
                else if (typeof object.gid === "string")
                    message.gid = parseInt(object.gid, 10);
                else if (typeof object.gid === "number")
                    message.gid = object.gid;
                else if (typeof object.gid === "object")
                    message.gid = new $util.LongBits(object.gid.low >>> 0, object.gid.high >>> 0).toNumber();
            if (object.sessionId != null)
                if ($util.Long)
                    (message.sessionId = $util.Long.fromValue(object.sessionId)).unsigned = false;
                else if (typeof object.sessionId === "string")
                    message.sessionId = parseInt(object.sessionId, 10);
                else if (typeof object.sessionId === "number")
                    message.sessionId = object.sessionId;
                else if (typeof object.sessionId === "object")
                    message.sessionId = new $util.LongBits(object.sessionId.low >>> 0, object.sessionId.high >>> 0).toNumber();
            if (object.startTimeEpochSeconds != null)
                if ($util.Long)
                    (message.startTimeEpochSeconds = $util.Long.fromValue(object.startTimeEpochSeconds)).unsigned = true;
                else if (typeof object.startTimeEpochSeconds === "string")
                    message.startTimeEpochSeconds = parseInt(object.startTimeEpochSeconds, 10);
                else if (typeof object.startTimeEpochSeconds === "number")
                    message.startTimeEpochSeconds = object.startTimeEpochSeconds;
                else if (typeof object.startTimeEpochSeconds === "object")
                    message.startTimeEpochSeconds = new $util.LongBits(object.startTimeEpochSeconds.low >>> 0, object.startTimeEpochSeconds.high >>> 0).toNumber(true);
            if (object.runTimeSeconds != null)
                if ($util.Long)
                    (message.runTimeSeconds = $util.Long.fromValue(object.runTimeSeconds)).unsigned = true;
                else if (typeof object.runTimeSeconds === "string")
                    message.runTimeSeconds = parseInt(object.runTimeSeconds, 10);
                else if (typeof object.runTimeSeconds === "number")
                    message.runTimeSeconds = object.runTimeSeconds;
                else if (typeof object.runTimeSeconds === "object")
                    message.runTimeSeconds = new $util.LongBits(object.runTimeSeconds.low >>> 0, object.runTimeSeconds.high >>> 0).toNumber(true);
            if (object.threadCount != null)
                message.threadCount = object.threadCount >>> 0;
            if (object.cpuUsage != null)
                message.cpuUsage = Number(object.cpuUsage);
            if (object.memoryBytes != null)
                if ($util.Long)
                    (message.memoryBytes = $util.Long.fromValue(object.memoryBytes)).unsigned = true;
                else if (typeof object.memoryBytes === "string")
                    message.memoryBytes = parseInt(object.memoryBytes, 10);
                else if (typeof object.memoryBytes === "number")
                    message.memoryBytes = object.memoryBytes;
                else if (typeof object.memoryBytes === "object")
                    message.memoryBytes = new $util.LongBits(object.memoryBytes.low >>> 0, object.memoryBytes.high >>> 0).toNumber(true);
            if (object.virtualMemoryBytes != null)
                if ($util.Long)
                    (message.virtualMemoryBytes = $util.Long.fromValue(object.virtualMemoryBytes)).unsigned = true;
                else if (typeof object.virtualMemoryBytes === "string")
                    message.virtualMemoryBytes = parseInt(object.virtualMemoryBytes, 10);
                else if (typeof object.virtualMemoryBytes === "number")
                    message.virtualMemoryBytes = object.virtualMemoryBytes;
                else if (typeof object.virtualMemoryBytes === "object")
                    message.virtualMemoryBytes = new $util.LongBits(object.virtualMemoryBytes.low >>> 0, object.virtualMemoryBytes.high >>> 0).toNumber(true);
            if (object.accumulatedCpuTimeMs != null)
                if ($util.Long)
                    (message.accumulatedCpuTimeMs = $util.Long.fromValue(object.accumulatedCpuTimeMs)).unsigned = true;
                else if (typeof object.accumulatedCpuTimeMs === "string")
                    message.accumulatedCpuTimeMs = parseInt(object.accumulatedCpuTimeMs, 10);
                else if (typeof object.accumulatedCpuTimeMs === "number")
                    message.accumulatedCpuTimeMs = object.accumulatedCpuTimeMs;
                else if (typeof object.accumulatedCpuTimeMs === "object")
                    message.accumulatedCpuTimeMs = new $util.LongBits(object.accumulatedCpuTimeMs.low >>> 0, object.accumulatedCpuTimeMs.high >>> 0).toNumber(true);
            if (object.totalReadBytes != null)
                if ($util.Long)
                    (message.totalReadBytes = $util.Long.fromValue(object.totalReadBytes)).unsigned = true;
                else if (typeof object.totalReadBytes === "string")
                    message.totalReadBytes = parseInt(object.totalReadBytes, 10);
                else if (typeof object.totalReadBytes === "number")
                    message.totalReadBytes = object.totalReadBytes;
                else if (typeof object.totalReadBytes === "object")
                    message.totalReadBytes = new $util.LongBits(object.totalReadBytes.low >>> 0, object.totalReadBytes.high >>> 0).toNumber(true);
            if (object.totalWrittenBytes != null)
                if ($util.Long)
                    (message.totalWrittenBytes = $util.Long.fromValue(object.totalWrittenBytes)).unsigned = true;
                else if (typeof object.totalWrittenBytes === "string")
                    message.totalWrittenBytes = parseInt(object.totalWrittenBytes, 10);
                else if (typeof object.totalWrittenBytes === "number")
                    message.totalWrittenBytes = object.totalWrittenBytes;
                else if (typeof object.totalWrittenBytes === "object")
                    message.totalWrittenBytes = new $util.LongBits(object.totalWrittenBytes.low >>> 0, object.totalWrittenBytes.high >>> 0).toNumber(true);
            if (object.readBytes != null)
                if ($util.Long)
                    (message.readBytes = $util.Long.fromValue(object.readBytes)).unsigned = true;
                else if (typeof object.readBytes === "string")
                    message.readBytes = parseInt(object.readBytes, 10);
                else if (typeof object.readBytes === "number")
                    message.readBytes = object.readBytes;
                else if (typeof object.readBytes === "object")
                    message.readBytes = new $util.LongBits(object.readBytes.low >>> 0, object.readBytes.high >>> 0).toNumber(true);
            if (object.writtenBytes != null)
                if ($util.Long)
                    (message.writtenBytes = $util.Long.fromValue(object.writtenBytes)).unsigned = true;
                else if (typeof object.writtenBytes === "string")
                    message.writtenBytes = parseInt(object.writtenBytes, 10);
                else if (typeof object.writtenBytes === "number")
                    message.writtenBytes = object.writtenBytes;
                else if (typeof object.writtenBytes === "object")
                    message.writtenBytes = new $util.LongBits(object.writtenBytes.low >>> 0, object.writtenBytes.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a ProcessInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof sysinfo.ProcessInfo
         * @static
         * @param {sysinfo.ProcessInfo} message ProcessInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ProcessInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.cmd = [];
            if (options.defaults) {
                object.pid = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.parentPid = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.parentPid = options.longs === String ? "0" : 0;
                object.name = "";
                object.exe = "";
                object.cwd = "";
                object.kind = "";
                object.status = "";
                object.userName = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.uid = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.uid = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.gid = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.gid = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.sessionId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.sessionId = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.startTimeEpochSeconds = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.startTimeEpochSeconds = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.runTimeSeconds = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.runTimeSeconds = options.longs === String ? "0" : 0;
                object.threadCount = 0;
                object.cpuUsage = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.memoryBytes = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.memoryBytes = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.virtualMemoryBytes = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.virtualMemoryBytes = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.accumulatedCpuTimeMs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.accumulatedCpuTimeMs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.totalReadBytes = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.totalReadBytes = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.totalWrittenBytes = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.totalWrittenBytes = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.readBytes = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.readBytes = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.writtenBytes = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.writtenBytes = options.longs === String ? "0" : 0;
            }
            if (message.pid != null && message.hasOwnProperty("pid"))
                object.pid = message.pid;
            if (message.parentPid != null && message.hasOwnProperty("parentPid"))
                if (typeof message.parentPid === "number")
                    object.parentPid = options.longs === String ? String(message.parentPid) : message.parentPid;
                else
                    object.parentPid = options.longs === String ? $util.Long.prototype.toString.call(message.parentPid) : options.longs === Number ? new $util.LongBits(message.parentPid.low >>> 0, message.parentPid.high >>> 0).toNumber() : message.parentPid;
            if (message.name != null && message.hasOwnProperty("name"))
                object.name = message.name;
            if (message.exe != null && message.hasOwnProperty("exe"))
                object.exe = message.exe;
            if (message.cmd && message.cmd.length) {
                object.cmd = [];
                for (let j = 0; j < message.cmd.length; ++j)
                    object.cmd[j] = message.cmd[j];
            }
            if (message.cwd != null && message.hasOwnProperty("cwd"))
                object.cwd = message.cwd;
            if (message.kind != null && message.hasOwnProperty("kind"))
                object.kind = message.kind;
            if (message.status != null && message.hasOwnProperty("status"))
                object.status = message.status;
            if (message.userName != null && message.hasOwnProperty("userName"))
                object.userName = message.userName;
            if (message.uid != null && message.hasOwnProperty("uid"))
                if (typeof message.uid === "number")
                    object.uid = options.longs === String ? String(message.uid) : message.uid;
                else
                    object.uid = options.longs === String ? $util.Long.prototype.toString.call(message.uid) : options.longs === Number ? new $util.LongBits(message.uid.low >>> 0, message.uid.high >>> 0).toNumber() : message.uid;
            if (message.gid != null && message.hasOwnProperty("gid"))
                if (typeof message.gid === "number")
                    object.gid = options.longs === String ? String(message.gid) : message.gid;
                else
                    object.gid = options.longs === String ? $util.Long.prototype.toString.call(message.gid) : options.longs === Number ? new $util.LongBits(message.gid.low >>> 0, message.gid.high >>> 0).toNumber() : message.gid;
            if (message.sessionId != null && message.hasOwnProperty("sessionId"))
                if (typeof message.sessionId === "number")
                    object.sessionId = options.longs === String ? String(message.sessionId) : message.sessionId;
                else
                    object.sessionId = options.longs === String ? $util.Long.prototype.toString.call(message.sessionId) : options.longs === Number ? new $util.LongBits(message.sessionId.low >>> 0, message.sessionId.high >>> 0).toNumber() : message.sessionId;
            if (message.startTimeEpochSeconds != null && message.hasOwnProperty("startTimeEpochSeconds"))
                if (typeof message.startTimeEpochSeconds === "number")
                    object.startTimeEpochSeconds = options.longs === String ? String(message.startTimeEpochSeconds) : message.startTimeEpochSeconds;
                else
                    object.startTimeEpochSeconds = options.longs === String ? $util.Long.prototype.toString.call(message.startTimeEpochSeconds) : options.longs === Number ? new $util.LongBits(message.startTimeEpochSeconds.low >>> 0, message.startTimeEpochSeconds.high >>> 0).toNumber(true) : message.startTimeEpochSeconds;
            if (message.runTimeSeconds != null && message.hasOwnProperty("runTimeSeconds"))
                if (typeof message.runTimeSeconds === "number")
                    object.runTimeSeconds = options.longs === String ? String(message.runTimeSeconds) : message.runTimeSeconds;
                else
                    object.runTimeSeconds = options.longs === String ? $util.Long.prototype.toString.call(message.runTimeSeconds) : options.longs === Number ? new $util.LongBits(message.runTimeSeconds.low >>> 0, message.runTimeSeconds.high >>> 0).toNumber(true) : message.runTimeSeconds;
            if (message.threadCount != null && message.hasOwnProperty("threadCount"))
                object.threadCount = message.threadCount;
            if (message.cpuUsage != null && message.hasOwnProperty("cpuUsage"))
                object.cpuUsage = options.json && !isFinite(message.cpuUsage) ? String(message.cpuUsage) : message.cpuUsage;
            if (message.memoryBytes != null && message.hasOwnProperty("memoryBytes"))
                if (typeof message.memoryBytes === "number")
                    object.memoryBytes = options.longs === String ? String(message.memoryBytes) : message.memoryBytes;
                else
                    object.memoryBytes = options.longs === String ? $util.Long.prototype.toString.call(message.memoryBytes) : options.longs === Number ? new $util.LongBits(message.memoryBytes.low >>> 0, message.memoryBytes.high >>> 0).toNumber(true) : message.memoryBytes;
            if (message.virtualMemoryBytes != null && message.hasOwnProperty("virtualMemoryBytes"))
                if (typeof message.virtualMemoryBytes === "number")
                    object.virtualMemoryBytes = options.longs === String ? String(message.virtualMemoryBytes) : message.virtualMemoryBytes;
                else
                    object.virtualMemoryBytes = options.longs === String ? $util.Long.prototype.toString.call(message.virtualMemoryBytes) : options.longs === Number ? new $util.LongBits(message.virtualMemoryBytes.low >>> 0, message.virtualMemoryBytes.high >>> 0).toNumber(true) : message.virtualMemoryBytes;
            if (message.accumulatedCpuTimeMs != null && message.hasOwnProperty("accumulatedCpuTimeMs"))
                if (typeof message.accumulatedCpuTimeMs === "number")
                    object.accumulatedCpuTimeMs = options.longs === String ? String(message.accumulatedCpuTimeMs) : message.accumulatedCpuTimeMs;
                else
                    object.accumulatedCpuTimeMs = options.longs === String ? $util.Long.prototype.toString.call(message.accumulatedCpuTimeMs) : options.longs === Number ? new $util.LongBits(message.accumulatedCpuTimeMs.low >>> 0, message.accumulatedCpuTimeMs.high >>> 0).toNumber(true) : message.accumulatedCpuTimeMs;
            if (message.totalReadBytes != null && message.hasOwnProperty("totalReadBytes"))
                if (typeof message.totalReadBytes === "number")
                    object.totalReadBytes = options.longs === String ? String(message.totalReadBytes) : message.totalReadBytes;
                else
                    object.totalReadBytes = options.longs === String ? $util.Long.prototype.toString.call(message.totalReadBytes) : options.longs === Number ? new $util.LongBits(message.totalReadBytes.low >>> 0, message.totalReadBytes.high >>> 0).toNumber(true) : message.totalReadBytes;
            if (message.totalWrittenBytes != null && message.hasOwnProperty("totalWrittenBytes"))
                if (typeof message.totalWrittenBytes === "number")
                    object.totalWrittenBytes = options.longs === String ? String(message.totalWrittenBytes) : message.totalWrittenBytes;
                else
                    object.totalWrittenBytes = options.longs === String ? $util.Long.prototype.toString.call(message.totalWrittenBytes) : options.longs === Number ? new $util.LongBits(message.totalWrittenBytes.low >>> 0, message.totalWrittenBytes.high >>> 0).toNumber(true) : message.totalWrittenBytes;
            if (message.readBytes != null && message.hasOwnProperty("readBytes"))
                if (typeof message.readBytes === "number")
                    object.readBytes = options.longs === String ? String(message.readBytes) : message.readBytes;
                else
                    object.readBytes = options.longs === String ? $util.Long.prototype.toString.call(message.readBytes) : options.longs === Number ? new $util.LongBits(message.readBytes.low >>> 0, message.readBytes.high >>> 0).toNumber(true) : message.readBytes;
            if (message.writtenBytes != null && message.hasOwnProperty("writtenBytes"))
                if (typeof message.writtenBytes === "number")
                    object.writtenBytes = options.longs === String ? String(message.writtenBytes) : message.writtenBytes;
                else
                    object.writtenBytes = options.longs === String ? $util.Long.prototype.toString.call(message.writtenBytes) : options.longs === Number ? new $util.LongBits(message.writtenBytes.low >>> 0, message.writtenBytes.high >>> 0).toNumber(true) : message.writtenBytes;
            return object;
        };

        /**
         * Converts this ProcessInfo to JSON.
         * @function toJSON
         * @memberof sysinfo.ProcessInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ProcessInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ProcessInfo
         * @function getTypeUrl
         * @memberof sysinfo.ProcessInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ProcessInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/sysinfo.ProcessInfo";
        };

        return ProcessInfo;
    })();

    sysinfo.TemperatureSensor = (function() {

        /**
         * Properties of a TemperatureSensor.
         * @memberof sysinfo
         * @interface ITemperatureSensor
         * @property {string|null} [id] TemperatureSensor id
         * @property {string|null} [name] TemperatureSensor name
         * @property {number|null} [value] TemperatureSensor value
         * @property {number|null} [max] TemperatureSensor max
         * @property {number|null} [critical] TemperatureSensor critical
         */

        /**
         * Constructs a new TemperatureSensor.
         * @memberof sysinfo
         * @classdesc Represents a TemperatureSensor.
         * @implements ITemperatureSensor
         * @constructor
         * @param {sysinfo.ITemperatureSensor=} [properties] Properties to set
         */
        function TemperatureSensor(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * TemperatureSensor id.
         * @member {string} id
         * @memberof sysinfo.TemperatureSensor
         * @instance
         */
        TemperatureSensor.prototype.id = "";

        /**
         * TemperatureSensor name.
         * @member {string} name
         * @memberof sysinfo.TemperatureSensor
         * @instance
         */
        TemperatureSensor.prototype.name = "";

        /**
         * TemperatureSensor value.
         * @member {number} value
         * @memberof sysinfo.TemperatureSensor
         * @instance
         */
        TemperatureSensor.prototype.value = 0;

        /**
         * TemperatureSensor max.
         * @member {number} max
         * @memberof sysinfo.TemperatureSensor
         * @instance
         */
        TemperatureSensor.prototype.max = 0;

        /**
         * TemperatureSensor critical.
         * @member {number} critical
         * @memberof sysinfo.TemperatureSensor
         * @instance
         */
        TemperatureSensor.prototype.critical = 0;

        /**
         * Creates a new TemperatureSensor instance using the specified properties.
         * @function create
         * @memberof sysinfo.TemperatureSensor
         * @static
         * @param {sysinfo.ITemperatureSensor=} [properties] Properties to set
         * @returns {sysinfo.TemperatureSensor} TemperatureSensor instance
         */
        TemperatureSensor.create = function create(properties) {
            return new TemperatureSensor(properties);
        };

        /**
         * Encodes the specified TemperatureSensor message. Does not implicitly {@link sysinfo.TemperatureSensor.verify|verify} messages.
         * @function encode
         * @memberof sysinfo.TemperatureSensor
         * @static
         * @param {sysinfo.ITemperatureSensor} message TemperatureSensor message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TemperatureSensor.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.id);
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.name);
            if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                writer.uint32(/* id 3, wireType 5 =*/29).float(message.value);
            if (message.max != null && Object.hasOwnProperty.call(message, "max"))
                writer.uint32(/* id 10, wireType 5 =*/85).float(message.max);
            if (message.critical != null && Object.hasOwnProperty.call(message, "critical"))
                writer.uint32(/* id 11, wireType 5 =*/93).float(message.critical);
            return writer;
        };

        /**
         * Encodes the specified TemperatureSensor message, length delimited. Does not implicitly {@link sysinfo.TemperatureSensor.verify|verify} messages.
         * @function encodeDelimited
         * @memberof sysinfo.TemperatureSensor
         * @static
         * @param {sysinfo.ITemperatureSensor} message TemperatureSensor message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TemperatureSensor.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a TemperatureSensor message from the specified reader or buffer.
         * @function decode
         * @memberof sysinfo.TemperatureSensor
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {sysinfo.TemperatureSensor} TemperatureSensor
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TemperatureSensor.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.sysinfo.TemperatureSensor();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.id = reader.string();
                        break;
                    }
                case 2: {
                        message.name = reader.string();
                        break;
                    }
                case 3: {
                        message.value = reader.float();
                        break;
                    }
                case 10: {
                        message.max = reader.float();
                        break;
                    }
                case 11: {
                        message.critical = reader.float();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a TemperatureSensor message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof sysinfo.TemperatureSensor
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {sysinfo.TemperatureSensor} TemperatureSensor
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TemperatureSensor.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TemperatureSensor message.
         * @function verify
         * @memberof sysinfo.TemperatureSensor
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TemperatureSensor.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isString(message.id))
                    return "id: string expected";
            if (message.name != null && message.hasOwnProperty("name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.value != null && message.hasOwnProperty("value"))
                if (typeof message.value !== "number")
                    return "value: number expected";
            if (message.max != null && message.hasOwnProperty("max"))
                if (typeof message.max !== "number")
                    return "max: number expected";
            if (message.critical != null && message.hasOwnProperty("critical"))
                if (typeof message.critical !== "number")
                    return "critical: number expected";
            return null;
        };

        /**
         * Creates a TemperatureSensor message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof sysinfo.TemperatureSensor
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {sysinfo.TemperatureSensor} TemperatureSensor
         */
        TemperatureSensor.fromObject = function fromObject(object, long) {
            if (object instanceof $root.sysinfo.TemperatureSensor)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.sysinfo.TemperatureSensor();
            if (object.id != null)
                message.id = String(object.id);
            if (object.name != null)
                message.name = String(object.name);
            if (object.value != null)
                message.value = Number(object.value);
            if (object.max != null)
                message.max = Number(object.max);
            if (object.critical != null)
                message.critical = Number(object.critical);
            return message;
        };

        /**
         * Creates a plain object from a TemperatureSensor message. Also converts values to other types if specified.
         * @function toObject
         * @memberof sysinfo.TemperatureSensor
         * @static
         * @param {sysinfo.TemperatureSensor} message TemperatureSensor
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TemperatureSensor.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.id = "";
                object.name = "";
                object.value = 0;
                object.max = 0;
                object.critical = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.name != null && message.hasOwnProperty("name"))
                object.name = message.name;
            if (message.value != null && message.hasOwnProperty("value"))
                object.value = options.json && !isFinite(message.value) ? String(message.value) : message.value;
            if (message.max != null && message.hasOwnProperty("max"))
                object.max = options.json && !isFinite(message.max) ? String(message.max) : message.max;
            if (message.critical != null && message.hasOwnProperty("critical"))
                object.critical = options.json && !isFinite(message.critical) ? String(message.critical) : message.critical;
            return object;
        };

        /**
         * Converts this TemperatureSensor to JSON.
         * @function toJSON
         * @memberof sysinfo.TemperatureSensor
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TemperatureSensor.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for TemperatureSensor
         * @function getTypeUrl
         * @memberof sysinfo.TemperatureSensor
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        TemperatureSensor.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/sysinfo.TemperatureSensor";
        };

        return TemperatureSensor;
    })();

    sysinfo.PowerSourceAttribute = (function() {

        /**
         * Properties of a PowerSourceAttribute.
         * @memberof sysinfo
         * @interface IPowerSourceAttribute
         * @property {string|null} [key] PowerSourceAttribute key
         * @property {string|null} [value] PowerSourceAttribute value
         */

        /**
         * Constructs a new PowerSourceAttribute.
         * @memberof sysinfo
         * @classdesc Represents a PowerSourceAttribute.
         * @implements IPowerSourceAttribute
         * @constructor
         * @param {sysinfo.IPowerSourceAttribute=} [properties] Properties to set
         */
        function PowerSourceAttribute(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PowerSourceAttribute key.
         * @member {string} key
         * @memberof sysinfo.PowerSourceAttribute
         * @instance
         */
        PowerSourceAttribute.prototype.key = "";

        /**
         * PowerSourceAttribute value.
         * @member {string} value
         * @memberof sysinfo.PowerSourceAttribute
         * @instance
         */
        PowerSourceAttribute.prototype.value = "";

        /**
         * Creates a new PowerSourceAttribute instance using the specified properties.
         * @function create
         * @memberof sysinfo.PowerSourceAttribute
         * @static
         * @param {sysinfo.IPowerSourceAttribute=} [properties] Properties to set
         * @returns {sysinfo.PowerSourceAttribute} PowerSourceAttribute instance
         */
        PowerSourceAttribute.create = function create(properties) {
            return new PowerSourceAttribute(properties);
        };

        /**
         * Encodes the specified PowerSourceAttribute message. Does not implicitly {@link sysinfo.PowerSourceAttribute.verify|verify} messages.
         * @function encode
         * @memberof sysinfo.PowerSourceAttribute
         * @static
         * @param {sysinfo.IPowerSourceAttribute} message PowerSourceAttribute message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PowerSourceAttribute.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.key);
            if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.value);
            return writer;
        };

        /**
         * Encodes the specified PowerSourceAttribute message, length delimited. Does not implicitly {@link sysinfo.PowerSourceAttribute.verify|verify} messages.
         * @function encodeDelimited
         * @memberof sysinfo.PowerSourceAttribute
         * @static
         * @param {sysinfo.IPowerSourceAttribute} message PowerSourceAttribute message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PowerSourceAttribute.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a PowerSourceAttribute message from the specified reader or buffer.
         * @function decode
         * @memberof sysinfo.PowerSourceAttribute
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {sysinfo.PowerSourceAttribute} PowerSourceAttribute
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PowerSourceAttribute.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.sysinfo.PowerSourceAttribute();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.key = reader.string();
                        break;
                    }
                case 2: {
                        message.value = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a PowerSourceAttribute message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof sysinfo.PowerSourceAttribute
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {sysinfo.PowerSourceAttribute} PowerSourceAttribute
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PowerSourceAttribute.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PowerSourceAttribute message.
         * @function verify
         * @memberof sysinfo.PowerSourceAttribute
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PowerSourceAttribute.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.key != null && message.hasOwnProperty("key"))
                if (!$util.isString(message.key))
                    return "key: string expected";
            if (message.value != null && message.hasOwnProperty("value"))
                if (!$util.isString(message.value))
                    return "value: string expected";
            return null;
        };

        /**
         * Creates a PowerSourceAttribute message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof sysinfo.PowerSourceAttribute
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {sysinfo.PowerSourceAttribute} PowerSourceAttribute
         */
        PowerSourceAttribute.fromObject = function fromObject(object, long) {
            if (object instanceof $root.sysinfo.PowerSourceAttribute)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.sysinfo.PowerSourceAttribute();
            if (object.key != null)
                message.key = String(object.key);
            if (object.value != null)
                message.value = String(object.value);
            return message;
        };

        /**
         * Creates a plain object from a PowerSourceAttribute message. Also converts values to other types if specified.
         * @function toObject
         * @memberof sysinfo.PowerSourceAttribute
         * @static
         * @param {sysinfo.PowerSourceAttribute} message PowerSourceAttribute
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PowerSourceAttribute.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.key = "";
                object.value = "";
            }
            if (message.key != null && message.hasOwnProperty("key"))
                object.key = message.key;
            if (message.value != null && message.hasOwnProperty("value"))
                object.value = message.value;
            return object;
        };

        /**
         * Converts this PowerSourceAttribute to JSON.
         * @function toJSON
         * @memberof sysinfo.PowerSourceAttribute
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PowerSourceAttribute.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PowerSourceAttribute
         * @function getTypeUrl
         * @memberof sysinfo.PowerSourceAttribute
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PowerSourceAttribute.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/sysinfo.PowerSourceAttribute";
        };

        return PowerSourceAttribute;
    })();

    sysinfo.PowerSource = (function() {

        /**
         * Properties of a PowerSource.
         * @memberof sysinfo
         * @interface IPowerSource
         * @property {string|null} [name] PowerSource name
         * @property {Array.<sysinfo.IPowerSourceAttribute>|null} [attributes] PowerSource attributes
         */

        /**
         * Constructs a new PowerSource.
         * @memberof sysinfo
         * @classdesc Represents a PowerSource.
         * @implements IPowerSource
         * @constructor
         * @param {sysinfo.IPowerSource=} [properties] Properties to set
         */
        function PowerSource(properties) {
            this.attributes = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PowerSource name.
         * @member {string} name
         * @memberof sysinfo.PowerSource
         * @instance
         */
        PowerSource.prototype.name = "";

        /**
         * PowerSource attributes.
         * @member {Array.<sysinfo.IPowerSourceAttribute>} attributes
         * @memberof sysinfo.PowerSource
         * @instance
         */
        PowerSource.prototype.attributes = $util.emptyArray;

        /**
         * Creates a new PowerSource instance using the specified properties.
         * @function create
         * @memberof sysinfo.PowerSource
         * @static
         * @param {sysinfo.IPowerSource=} [properties] Properties to set
         * @returns {sysinfo.PowerSource} PowerSource instance
         */
        PowerSource.create = function create(properties) {
            return new PowerSource(properties);
        };

        /**
         * Encodes the specified PowerSource message. Does not implicitly {@link sysinfo.PowerSource.verify|verify} messages.
         * @function encode
         * @memberof sysinfo.PowerSource
         * @static
         * @param {sysinfo.IPowerSource} message PowerSource message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PowerSource.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
            if (message.attributes != null && message.attributes.length)
                for (let i = 0; i < message.attributes.length; ++i)
                    $root.sysinfo.PowerSourceAttribute.encode(message.attributes[i], writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified PowerSource message, length delimited. Does not implicitly {@link sysinfo.PowerSource.verify|verify} messages.
         * @function encodeDelimited
         * @memberof sysinfo.PowerSource
         * @static
         * @param {sysinfo.IPowerSource} message PowerSource message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PowerSource.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a PowerSource message from the specified reader or buffer.
         * @function decode
         * @memberof sysinfo.PowerSource
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {sysinfo.PowerSource} PowerSource
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PowerSource.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.sysinfo.PowerSource();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.name = reader.string();
                        break;
                    }
                case 10: {
                        if (!(message.attributes && message.attributes.length))
                            message.attributes = [];
                        message.attributes.push($root.sysinfo.PowerSourceAttribute.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a PowerSource message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof sysinfo.PowerSource
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {sysinfo.PowerSource} PowerSource
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PowerSource.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PowerSource message.
         * @function verify
         * @memberof sysinfo.PowerSource
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PowerSource.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.name != null && message.hasOwnProperty("name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.attributes != null && message.hasOwnProperty("attributes")) {
                if (!Array.isArray(message.attributes))
                    return "attributes: array expected";
                for (let i = 0; i < message.attributes.length; ++i) {
                    let error = $root.sysinfo.PowerSourceAttribute.verify(message.attributes[i], long + 1);
                    if (error)
                        return "attributes." + error;
                }
            }
            return null;
        };

        /**
         * Creates a PowerSource message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof sysinfo.PowerSource
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {sysinfo.PowerSource} PowerSource
         */
        PowerSource.fromObject = function fromObject(object, long) {
            if (object instanceof $root.sysinfo.PowerSource)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.sysinfo.PowerSource();
            if (object.name != null)
                message.name = String(object.name);
            if (object.attributes) {
                if (!Array.isArray(object.attributes))
                    throw TypeError(".sysinfo.PowerSource.attributes: array expected");
                message.attributes = [];
                for (let i = 0; i < object.attributes.length; ++i) {
                    if (typeof object.attributes[i] !== "object")
                        throw TypeError(".sysinfo.PowerSource.attributes: object expected");
                    message.attributes[i] = $root.sysinfo.PowerSourceAttribute.fromObject(object.attributes[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a PowerSource message. Also converts values to other types if specified.
         * @function toObject
         * @memberof sysinfo.PowerSource
         * @static
         * @param {sysinfo.PowerSource} message PowerSource
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PowerSource.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.attributes = [];
            if (options.defaults)
                object.name = "";
            if (message.name != null && message.hasOwnProperty("name"))
                object.name = message.name;
            if (message.attributes && message.attributes.length) {
                object.attributes = [];
                for (let j = 0; j < message.attributes.length; ++j)
                    object.attributes[j] = $root.sysinfo.PowerSourceAttribute.toObject(message.attributes[j], options);
            }
            return object;
        };

        /**
         * Converts this PowerSource to JSON.
         * @function toJSON
         * @memberof sysinfo.PowerSource
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PowerSource.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PowerSource
         * @function getTypeUrl
         * @memberof sysinfo.PowerSource
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PowerSource.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/sysinfo.PowerSource";
        };

        return PowerSource;
    })();

    sysinfo.RpiThrottling = (function() {

        /**
         * Properties of a RpiThrottling.
         * @memberof sysinfo
         * @interface IRpiThrottling
         * @property {number|null} [raw] RpiThrottling raw
         * @property {boolean|null} [underVoltage] RpiThrottling underVoltage
         * @property {boolean|null} [armFrequencyCapped] RpiThrottling armFrequencyCapped
         * @property {boolean|null} [throttled] RpiThrottling throttled
         * @property {boolean|null} [softTempLimit] RpiThrottling softTempLimit
         * @property {boolean|null} [underVoltageSinceBoot] RpiThrottling underVoltageSinceBoot
         * @property {boolean|null} [armFrequencyCappedSinceBoot] RpiThrottling armFrequencyCappedSinceBoot
         * @property {boolean|null} [throttledSinceBoot] RpiThrottling throttledSinceBoot
         * @property {boolean|null} [softTempLimitSinceBoot] RpiThrottling softTempLimitSinceBoot
         */

        /**
         * Constructs a new RpiThrottling.
         * @memberof sysinfo
         * @classdesc Represents a RpiThrottling.
         * @implements IRpiThrottling
         * @constructor
         * @param {sysinfo.IRpiThrottling=} [properties] Properties to set
         */
        function RpiThrottling(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RpiThrottling raw.
         * @member {number} raw
         * @memberof sysinfo.RpiThrottling
         * @instance
         */
        RpiThrottling.prototype.raw = 0;

        /**
         * RpiThrottling underVoltage.
         * @member {boolean} underVoltage
         * @memberof sysinfo.RpiThrottling
         * @instance
         */
        RpiThrottling.prototype.underVoltage = false;

        /**
         * RpiThrottling armFrequencyCapped.
         * @member {boolean} armFrequencyCapped
         * @memberof sysinfo.RpiThrottling
         * @instance
         */
        RpiThrottling.prototype.armFrequencyCapped = false;

        /**
         * RpiThrottling throttled.
         * @member {boolean} throttled
         * @memberof sysinfo.RpiThrottling
         * @instance
         */
        RpiThrottling.prototype.throttled = false;

        /**
         * RpiThrottling softTempLimit.
         * @member {boolean} softTempLimit
         * @memberof sysinfo.RpiThrottling
         * @instance
         */
        RpiThrottling.prototype.softTempLimit = false;

        /**
         * RpiThrottling underVoltageSinceBoot.
         * @member {boolean} underVoltageSinceBoot
         * @memberof sysinfo.RpiThrottling
         * @instance
         */
        RpiThrottling.prototype.underVoltageSinceBoot = false;

        /**
         * RpiThrottling armFrequencyCappedSinceBoot.
         * @member {boolean} armFrequencyCappedSinceBoot
         * @memberof sysinfo.RpiThrottling
         * @instance
         */
        RpiThrottling.prototype.armFrequencyCappedSinceBoot = false;

        /**
         * RpiThrottling throttledSinceBoot.
         * @member {boolean} throttledSinceBoot
         * @memberof sysinfo.RpiThrottling
         * @instance
         */
        RpiThrottling.prototype.throttledSinceBoot = false;

        /**
         * RpiThrottling softTempLimitSinceBoot.
         * @member {boolean} softTempLimitSinceBoot
         * @memberof sysinfo.RpiThrottling
         * @instance
         */
        RpiThrottling.prototype.softTempLimitSinceBoot = false;

        /**
         * Creates a new RpiThrottling instance using the specified properties.
         * @function create
         * @memberof sysinfo.RpiThrottling
         * @static
         * @param {sysinfo.IRpiThrottling=} [properties] Properties to set
         * @returns {sysinfo.RpiThrottling} RpiThrottling instance
         */
        RpiThrottling.create = function create(properties) {
            return new RpiThrottling(properties);
        };

        /**
         * Encodes the specified RpiThrottling message. Does not implicitly {@link sysinfo.RpiThrottling.verify|verify} messages.
         * @function encode
         * @memberof sysinfo.RpiThrottling
         * @static
         * @param {sysinfo.IRpiThrottling} message RpiThrottling message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RpiThrottling.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.raw != null && Object.hasOwnProperty.call(message, "raw"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.raw);
            if (message.underVoltage != null && Object.hasOwnProperty.call(message, "underVoltage"))
                writer.uint32(/* id 10, wireType 0 =*/80).bool(message.underVoltage);
            if (message.armFrequencyCapped != null && Object.hasOwnProperty.call(message, "armFrequencyCapped"))
                writer.uint32(/* id 11, wireType 0 =*/88).bool(message.armFrequencyCapped);
            if (message.throttled != null && Object.hasOwnProperty.call(message, "throttled"))
                writer.uint32(/* id 12, wireType 0 =*/96).bool(message.throttled);
            if (message.softTempLimit != null && Object.hasOwnProperty.call(message, "softTempLimit"))
                writer.uint32(/* id 13, wireType 0 =*/104).bool(message.softTempLimit);
            if (message.underVoltageSinceBoot != null && Object.hasOwnProperty.call(message, "underVoltageSinceBoot"))
                writer.uint32(/* id 20, wireType 0 =*/160).bool(message.underVoltageSinceBoot);
            if (message.armFrequencyCappedSinceBoot != null && Object.hasOwnProperty.call(message, "armFrequencyCappedSinceBoot"))
                writer.uint32(/* id 21, wireType 0 =*/168).bool(message.armFrequencyCappedSinceBoot);
            if (message.throttledSinceBoot != null && Object.hasOwnProperty.call(message, "throttledSinceBoot"))
                writer.uint32(/* id 22, wireType 0 =*/176).bool(message.throttledSinceBoot);
            if (message.softTempLimitSinceBoot != null && Object.hasOwnProperty.call(message, "softTempLimitSinceBoot"))
                writer.uint32(/* id 23, wireType 0 =*/184).bool(message.softTempLimitSinceBoot);
            return writer;
        };

        /**
         * Encodes the specified RpiThrottling message, length delimited. Does not implicitly {@link sysinfo.RpiThrottling.verify|verify} messages.
         * @function encodeDelimited
         * @memberof sysinfo.RpiThrottling
         * @static
         * @param {sysinfo.IRpiThrottling} message RpiThrottling message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RpiThrottling.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RpiThrottling message from the specified reader or buffer.
         * @function decode
         * @memberof sysinfo.RpiThrottling
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {sysinfo.RpiThrottling} RpiThrottling
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RpiThrottling.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.sysinfo.RpiThrottling();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.raw = reader.uint32();
                        break;
                    }
                case 10: {
                        message.underVoltage = reader.bool();
                        break;
                    }
                case 11: {
                        message.armFrequencyCapped = reader.bool();
                        break;
                    }
                case 12: {
                        message.throttled = reader.bool();
                        break;
                    }
                case 13: {
                        message.softTempLimit = reader.bool();
                        break;
                    }
                case 20: {
                        message.underVoltageSinceBoot = reader.bool();
                        break;
                    }
                case 21: {
                        message.armFrequencyCappedSinceBoot = reader.bool();
                        break;
                    }
                case 22: {
                        message.throttledSinceBoot = reader.bool();
                        break;
                    }
                case 23: {
                        message.softTempLimitSinceBoot = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RpiThrottling message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof sysinfo.RpiThrottling
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {sysinfo.RpiThrottling} RpiThrottling
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RpiThrottling.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RpiThrottling message.
         * @function verify
         * @memberof sysinfo.RpiThrottling
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RpiThrottling.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.raw != null && message.hasOwnProperty("raw"))
                if (!$util.isInteger(message.raw))
                    return "raw: integer expected";
            if (message.underVoltage != null && message.hasOwnProperty("underVoltage"))
                if (typeof message.underVoltage !== "boolean")
                    return "underVoltage: boolean expected";
            if (message.armFrequencyCapped != null && message.hasOwnProperty("armFrequencyCapped"))
                if (typeof message.armFrequencyCapped !== "boolean")
                    return "armFrequencyCapped: boolean expected";
            if (message.throttled != null && message.hasOwnProperty("throttled"))
                if (typeof message.throttled !== "boolean")
                    return "throttled: boolean expected";
            if (message.softTempLimit != null && message.hasOwnProperty("softTempLimit"))
                if (typeof message.softTempLimit !== "boolean")
                    return "softTempLimit: boolean expected";
            if (message.underVoltageSinceBoot != null && message.hasOwnProperty("underVoltageSinceBoot"))
                if (typeof message.underVoltageSinceBoot !== "boolean")
                    return "underVoltageSinceBoot: boolean expected";
            if (message.armFrequencyCappedSinceBoot != null && message.hasOwnProperty("armFrequencyCappedSinceBoot"))
                if (typeof message.armFrequencyCappedSinceBoot !== "boolean")
                    return "armFrequencyCappedSinceBoot: boolean expected";
            if (message.throttledSinceBoot != null && message.hasOwnProperty("throttledSinceBoot"))
                if (typeof message.throttledSinceBoot !== "boolean")
                    return "throttledSinceBoot: boolean expected";
            if (message.softTempLimitSinceBoot != null && message.hasOwnProperty("softTempLimitSinceBoot"))
                if (typeof message.softTempLimitSinceBoot !== "boolean")
                    return "softTempLimitSinceBoot: boolean expected";
            return null;
        };

        /**
         * Creates a RpiThrottling message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof sysinfo.RpiThrottling
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {sysinfo.RpiThrottling} RpiThrottling
         */
        RpiThrottling.fromObject = function fromObject(object, long) {
            if (object instanceof $root.sysinfo.RpiThrottling)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.sysinfo.RpiThrottling();
            if (object.raw != null)
                message.raw = object.raw >>> 0;
            if (object.underVoltage != null)
                message.underVoltage = Boolean(object.underVoltage);
            if (object.armFrequencyCapped != null)
                message.armFrequencyCapped = Boolean(object.armFrequencyCapped);
            if (object.throttled != null)
                message.throttled = Boolean(object.throttled);
            if (object.softTempLimit != null)
                message.softTempLimit = Boolean(object.softTempLimit);
            if (object.underVoltageSinceBoot != null)
                message.underVoltageSinceBoot = Boolean(object.underVoltageSinceBoot);
            if (object.armFrequencyCappedSinceBoot != null)
                message.armFrequencyCappedSinceBoot = Boolean(object.armFrequencyCappedSinceBoot);
            if (object.throttledSinceBoot != null)
                message.throttledSinceBoot = Boolean(object.throttledSinceBoot);
            if (object.softTempLimitSinceBoot != null)
                message.softTempLimitSinceBoot = Boolean(object.softTempLimitSinceBoot);
            return message;
        };

        /**
         * Creates a plain object from a RpiThrottling message. Also converts values to other types if specified.
         * @function toObject
         * @memberof sysinfo.RpiThrottling
         * @static
         * @param {sysinfo.RpiThrottling} message RpiThrottling
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RpiThrottling.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.raw = 0;
                object.underVoltage = false;
                object.armFrequencyCapped = false;
                object.throttled = false;
                object.softTempLimit = false;
                object.underVoltageSinceBoot = false;
                object.armFrequencyCappedSinceBoot = false;
                object.throttledSinceBoot = false;
                object.softTempLimitSinceBoot = false;
            }
            if (message.raw != null && message.hasOwnProperty("raw"))
                object.raw = message.raw;
            if (message.underVoltage != null && message.hasOwnProperty("underVoltage"))
                object.underVoltage = message.underVoltage;
            if (message.armFrequencyCapped != null && message.hasOwnProperty("armFrequencyCapped"))
                object.armFrequencyCapped = message.armFrequencyCapped;
            if (message.throttled != null && message.hasOwnProperty("throttled"))
                object.throttled = message.throttled;
            if (message.softTempLimit != null && message.hasOwnProperty("softTempLimit"))
                object.softTempLimit = message.softTempLimit;
            if (message.underVoltageSinceBoot != null && message.hasOwnProperty("underVoltageSinceBoot"))
                object.underVoltageSinceBoot = message.underVoltageSinceBoot;
            if (message.armFrequencyCappedSinceBoot != null && message.hasOwnProperty("armFrequencyCappedSinceBoot"))
                object.armFrequencyCappedSinceBoot = message.armFrequencyCappedSinceBoot;
            if (message.throttledSinceBoot != null && message.hasOwnProperty("throttledSinceBoot"))
                object.throttledSinceBoot = message.throttledSinceBoot;
            if (message.softTempLimitSinceBoot != null && message.hasOwnProperty("softTempLimitSinceBoot"))
                object.softTempLimitSinceBoot = message.softTempLimitSinceBoot;
            return object;
        };

        /**
         * Converts this RpiThrottling to JSON.
         * @function toJSON
         * @memberof sysinfo.RpiThrottling
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RpiThrottling.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RpiThrottling
         * @function getTypeUrl
         * @memberof sysinfo.RpiThrottling
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RpiThrottling.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/sysinfo.RpiThrottling";
        };

        return RpiThrottling;
    })();

    sysinfo.CoolingDevice = (function() {

        /**
         * Properties of a CoolingDevice.
         * @memberof sysinfo
         * @interface ICoolingDevice
         * @property {string|null} [name] CoolingDevice name
         * @property {string|null} [type] CoolingDevice type
         * @property {Long|null} [curState] CoolingDevice curState
         * @property {Long|null} [maxState] CoolingDevice maxState
         */

        /**
         * Constructs a new CoolingDevice.
         * @memberof sysinfo
         * @classdesc Represents a CoolingDevice.
         * @implements ICoolingDevice
         * @constructor
         * @param {sysinfo.ICoolingDevice=} [properties] Properties to set
         */
        function CoolingDevice(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CoolingDevice name.
         * @member {string} name
         * @memberof sysinfo.CoolingDevice
         * @instance
         */
        CoolingDevice.prototype.name = "";

        /**
         * CoolingDevice type.
         * @member {string} type
         * @memberof sysinfo.CoolingDevice
         * @instance
         */
        CoolingDevice.prototype.type = "";

        /**
         * CoolingDevice curState.
         * @member {Long} curState
         * @memberof sysinfo.CoolingDevice
         * @instance
         */
        CoolingDevice.prototype.curState = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * CoolingDevice maxState.
         * @member {Long} maxState
         * @memberof sysinfo.CoolingDevice
         * @instance
         */
        CoolingDevice.prototype.maxState = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new CoolingDevice instance using the specified properties.
         * @function create
         * @memberof sysinfo.CoolingDevice
         * @static
         * @param {sysinfo.ICoolingDevice=} [properties] Properties to set
         * @returns {sysinfo.CoolingDevice} CoolingDevice instance
         */
        CoolingDevice.create = function create(properties) {
            return new CoolingDevice(properties);
        };

        /**
         * Encodes the specified CoolingDevice message. Does not implicitly {@link sysinfo.CoolingDevice.verify|verify} messages.
         * @function encode
         * @memberof sysinfo.CoolingDevice
         * @static
         * @param {sysinfo.ICoolingDevice} message CoolingDevice message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CoolingDevice.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.type);
            if (message.curState != null && Object.hasOwnProperty.call(message, "curState"))
                writer.uint32(/* id 10, wireType 0 =*/80).uint64(message.curState);
            if (message.maxState != null && Object.hasOwnProperty.call(message, "maxState"))
                writer.uint32(/* id 11, wireType 0 =*/88).uint64(message.maxState);
            return writer;
        };

        /**
         * Encodes the specified CoolingDevice message, length delimited. Does not implicitly {@link sysinfo.CoolingDevice.verify|verify} messages.
         * @function encodeDelimited
         * @memberof sysinfo.CoolingDevice
         * @static
         * @param {sysinfo.ICoolingDevice} message CoolingDevice message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CoolingDevice.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CoolingDevice message from the specified reader or buffer.
         * @function decode
         * @memberof sysinfo.CoolingDevice
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {sysinfo.CoolingDevice} CoolingDevice
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CoolingDevice.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.sysinfo.CoolingDevice();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.name = reader.string();
                        break;
                    }
                case 2: {
                        message.type = reader.string();
                        break;
                    }
                case 10: {
                        message.curState = reader.uint64();
                        break;
                    }
                case 11: {
                        message.maxState = reader.uint64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CoolingDevice message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof sysinfo.CoolingDevice
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {sysinfo.CoolingDevice} CoolingDevice
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CoolingDevice.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CoolingDevice message.
         * @function verify
         * @memberof sysinfo.CoolingDevice
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CoolingDevice.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.name != null && message.hasOwnProperty("name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isString(message.type))
                    return "type: string expected";
            if (message.curState != null && message.hasOwnProperty("curState"))
                if (!$util.isInteger(message.curState) && !(message.curState && $util.isInteger(message.curState.low) && $util.isInteger(message.curState.high)))
                    return "curState: integer|Long expected";
            if (message.maxState != null && message.hasOwnProperty("maxState"))
                if (!$util.isInteger(message.maxState) && !(message.maxState && $util.isInteger(message.maxState.low) && $util.isInteger(message.maxState.high)))
                    return "maxState: integer|Long expected";
            return null;
        };

        /**
         * Creates a CoolingDevice message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof sysinfo.CoolingDevice
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {sysinfo.CoolingDevice} CoolingDevice
         */
        CoolingDevice.fromObject = function fromObject(object, long) {
            if (object instanceof $root.sysinfo.CoolingDevice)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.sysinfo.CoolingDevice();
            if (object.name != null)
                message.name = String(object.name);
            if (object.type != null)
                message.type = String(object.type);
            if (object.curState != null)
                if ($util.Long)
                    (message.curState = $util.Long.fromValue(object.curState)).unsigned = true;
                else if (typeof object.curState === "string")
                    message.curState = parseInt(object.curState, 10);
                else if (typeof object.curState === "number")
                    message.curState = object.curState;
                else if (typeof object.curState === "object")
                    message.curState = new $util.LongBits(object.curState.low >>> 0, object.curState.high >>> 0).toNumber(true);
            if (object.maxState != null)
                if ($util.Long)
                    (message.maxState = $util.Long.fromValue(object.maxState)).unsigned = true;
                else if (typeof object.maxState === "string")
                    message.maxState = parseInt(object.maxState, 10);
                else if (typeof object.maxState === "number")
                    message.maxState = object.maxState;
                else if (typeof object.maxState === "object")
                    message.maxState = new $util.LongBits(object.maxState.low >>> 0, object.maxState.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a CoolingDevice message. Also converts values to other types if specified.
         * @function toObject
         * @memberof sysinfo.CoolingDevice
         * @static
         * @param {sysinfo.CoolingDevice} message CoolingDevice
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CoolingDevice.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.name = "";
                object.type = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.curState = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.curState = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.maxState = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.maxState = options.longs === String ? "0" : 0;
            }
            if (message.name != null && message.hasOwnProperty("name"))
                object.name = message.name;
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.curState != null && message.hasOwnProperty("curState"))
                if (typeof message.curState === "number")
                    object.curState = options.longs === String ? String(message.curState) : message.curState;
                else
                    object.curState = options.longs === String ? $util.Long.prototype.toString.call(message.curState) : options.longs === Number ? new $util.LongBits(message.curState.low >>> 0, message.curState.high >>> 0).toNumber(true) : message.curState;
            if (message.maxState != null && message.hasOwnProperty("maxState"))
                if (typeof message.maxState === "number")
                    object.maxState = options.longs === String ? String(message.maxState) : message.maxState;
                else
                    object.maxState = options.longs === String ? $util.Long.prototype.toString.call(message.maxState) : options.longs === Number ? new $util.LongBits(message.maxState.low >>> 0, message.maxState.high >>> 0).toNumber(true) : message.maxState;
            return object;
        };

        /**
         * Converts this CoolingDevice to JSON.
         * @function toJSON
         * @memberof sysinfo.CoolingDevice
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CoolingDevice.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CoolingDevice
         * @function getTypeUrl
         * @memberof sysinfo.CoolingDevice
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CoolingDevice.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/sysinfo.CoolingDevice";
        };

        return CoolingDevice;
    })();

    sysinfo.CpuFreqPolicy = (function() {

        /**
         * Properties of a CpuFreqPolicy.
         * @memberof sysinfo
         * @interface ICpuFreqPolicy
         * @property {string|null} [name] CpuFreqPolicy name
         * @property {string|null} [scalingGovernor] CpuFreqPolicy scalingGovernor
         * @property {Long|null} [scalingCurFreqKhz] CpuFreqPolicy scalingCurFreqKhz
         * @property {Long|null} [scalingMinFreqKhz] CpuFreqPolicy scalingMinFreqKhz
         * @property {Long|null} [scalingMaxFreqKhz] CpuFreqPolicy scalingMaxFreqKhz
         * @property {Long|null} [cpuinfoMinFreqKhz] CpuFreqPolicy cpuinfoMinFreqKhz
         * @property {Long|null} [cpuinfoMaxFreqKhz] CpuFreqPolicy cpuinfoMaxFreqKhz
         */

        /**
         * Constructs a new CpuFreqPolicy.
         * @memberof sysinfo
         * @classdesc Represents a CpuFreqPolicy.
         * @implements ICpuFreqPolicy
         * @constructor
         * @param {sysinfo.ICpuFreqPolicy=} [properties] Properties to set
         */
        function CpuFreqPolicy(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CpuFreqPolicy name.
         * @member {string} name
         * @memberof sysinfo.CpuFreqPolicy
         * @instance
         */
        CpuFreqPolicy.prototype.name = "";

        /**
         * CpuFreqPolicy scalingGovernor.
         * @member {string} scalingGovernor
         * @memberof sysinfo.CpuFreqPolicy
         * @instance
         */
        CpuFreqPolicy.prototype.scalingGovernor = "";

        /**
         * CpuFreqPolicy scalingCurFreqKhz.
         * @member {Long} scalingCurFreqKhz
         * @memberof sysinfo.CpuFreqPolicy
         * @instance
         */
        CpuFreqPolicy.prototype.scalingCurFreqKhz = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * CpuFreqPolicy scalingMinFreqKhz.
         * @member {Long} scalingMinFreqKhz
         * @memberof sysinfo.CpuFreqPolicy
         * @instance
         */
        CpuFreqPolicy.prototype.scalingMinFreqKhz = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * CpuFreqPolicy scalingMaxFreqKhz.
         * @member {Long} scalingMaxFreqKhz
         * @memberof sysinfo.CpuFreqPolicy
         * @instance
         */
        CpuFreqPolicy.prototype.scalingMaxFreqKhz = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * CpuFreqPolicy cpuinfoMinFreqKhz.
         * @member {Long} cpuinfoMinFreqKhz
         * @memberof sysinfo.CpuFreqPolicy
         * @instance
         */
        CpuFreqPolicy.prototype.cpuinfoMinFreqKhz = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * CpuFreqPolicy cpuinfoMaxFreqKhz.
         * @member {Long} cpuinfoMaxFreqKhz
         * @memberof sysinfo.CpuFreqPolicy
         * @instance
         */
        CpuFreqPolicy.prototype.cpuinfoMaxFreqKhz = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new CpuFreqPolicy instance using the specified properties.
         * @function create
         * @memberof sysinfo.CpuFreqPolicy
         * @static
         * @param {sysinfo.ICpuFreqPolicy=} [properties] Properties to set
         * @returns {sysinfo.CpuFreqPolicy} CpuFreqPolicy instance
         */
        CpuFreqPolicy.create = function create(properties) {
            return new CpuFreqPolicy(properties);
        };

        /**
         * Encodes the specified CpuFreqPolicy message. Does not implicitly {@link sysinfo.CpuFreqPolicy.verify|verify} messages.
         * @function encode
         * @memberof sysinfo.CpuFreqPolicy
         * @static
         * @param {sysinfo.ICpuFreqPolicy} message CpuFreqPolicy message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CpuFreqPolicy.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
            if (message.scalingGovernor != null && Object.hasOwnProperty.call(message, "scalingGovernor"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.scalingGovernor);
            if (message.scalingCurFreqKhz != null && Object.hasOwnProperty.call(message, "scalingCurFreqKhz"))
                writer.uint32(/* id 10, wireType 0 =*/80).uint64(message.scalingCurFreqKhz);
            if (message.scalingMinFreqKhz != null && Object.hasOwnProperty.call(message, "scalingMinFreqKhz"))
                writer.uint32(/* id 11, wireType 0 =*/88).uint64(message.scalingMinFreqKhz);
            if (message.scalingMaxFreqKhz != null && Object.hasOwnProperty.call(message, "scalingMaxFreqKhz"))
                writer.uint32(/* id 12, wireType 0 =*/96).uint64(message.scalingMaxFreqKhz);
            if (message.cpuinfoMinFreqKhz != null && Object.hasOwnProperty.call(message, "cpuinfoMinFreqKhz"))
                writer.uint32(/* id 13, wireType 0 =*/104).uint64(message.cpuinfoMinFreqKhz);
            if (message.cpuinfoMaxFreqKhz != null && Object.hasOwnProperty.call(message, "cpuinfoMaxFreqKhz"))
                writer.uint32(/* id 14, wireType 0 =*/112).uint64(message.cpuinfoMaxFreqKhz);
            return writer;
        };

        /**
         * Encodes the specified CpuFreqPolicy message, length delimited. Does not implicitly {@link sysinfo.CpuFreqPolicy.verify|verify} messages.
         * @function encodeDelimited
         * @memberof sysinfo.CpuFreqPolicy
         * @static
         * @param {sysinfo.ICpuFreqPolicy} message CpuFreqPolicy message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CpuFreqPolicy.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CpuFreqPolicy message from the specified reader or buffer.
         * @function decode
         * @memberof sysinfo.CpuFreqPolicy
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {sysinfo.CpuFreqPolicy} CpuFreqPolicy
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CpuFreqPolicy.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.sysinfo.CpuFreqPolicy();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.name = reader.string();
                        break;
                    }
                case 2: {
                        message.scalingGovernor = reader.string();
                        break;
                    }
                case 10: {
                        message.scalingCurFreqKhz = reader.uint64();
                        break;
                    }
                case 11: {
                        message.scalingMinFreqKhz = reader.uint64();
                        break;
                    }
                case 12: {
                        message.scalingMaxFreqKhz = reader.uint64();
                        break;
                    }
                case 13: {
                        message.cpuinfoMinFreqKhz = reader.uint64();
                        break;
                    }
                case 14: {
                        message.cpuinfoMaxFreqKhz = reader.uint64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CpuFreqPolicy message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof sysinfo.CpuFreqPolicy
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {sysinfo.CpuFreqPolicy} CpuFreqPolicy
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CpuFreqPolicy.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CpuFreqPolicy message.
         * @function verify
         * @memberof sysinfo.CpuFreqPolicy
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CpuFreqPolicy.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.name != null && message.hasOwnProperty("name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.scalingGovernor != null && message.hasOwnProperty("scalingGovernor"))
                if (!$util.isString(message.scalingGovernor))
                    return "scalingGovernor: string expected";
            if (message.scalingCurFreqKhz != null && message.hasOwnProperty("scalingCurFreqKhz"))
                if (!$util.isInteger(message.scalingCurFreqKhz) && !(message.scalingCurFreqKhz && $util.isInteger(message.scalingCurFreqKhz.low) && $util.isInteger(message.scalingCurFreqKhz.high)))
                    return "scalingCurFreqKhz: integer|Long expected";
            if (message.scalingMinFreqKhz != null && message.hasOwnProperty("scalingMinFreqKhz"))
                if (!$util.isInteger(message.scalingMinFreqKhz) && !(message.scalingMinFreqKhz && $util.isInteger(message.scalingMinFreqKhz.low) && $util.isInteger(message.scalingMinFreqKhz.high)))
                    return "scalingMinFreqKhz: integer|Long expected";
            if (message.scalingMaxFreqKhz != null && message.hasOwnProperty("scalingMaxFreqKhz"))
                if (!$util.isInteger(message.scalingMaxFreqKhz) && !(message.scalingMaxFreqKhz && $util.isInteger(message.scalingMaxFreqKhz.low) && $util.isInteger(message.scalingMaxFreqKhz.high)))
                    return "scalingMaxFreqKhz: integer|Long expected";
            if (message.cpuinfoMinFreqKhz != null && message.hasOwnProperty("cpuinfoMinFreqKhz"))
                if (!$util.isInteger(message.cpuinfoMinFreqKhz) && !(message.cpuinfoMinFreqKhz && $util.isInteger(message.cpuinfoMinFreqKhz.low) && $util.isInteger(message.cpuinfoMinFreqKhz.high)))
                    return "cpuinfoMinFreqKhz: integer|Long expected";
            if (message.cpuinfoMaxFreqKhz != null && message.hasOwnProperty("cpuinfoMaxFreqKhz"))
                if (!$util.isInteger(message.cpuinfoMaxFreqKhz) && !(message.cpuinfoMaxFreqKhz && $util.isInteger(message.cpuinfoMaxFreqKhz.low) && $util.isInteger(message.cpuinfoMaxFreqKhz.high)))
                    return "cpuinfoMaxFreqKhz: integer|Long expected";
            return null;
        };

        /**
         * Creates a CpuFreqPolicy message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof sysinfo.CpuFreqPolicy
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {sysinfo.CpuFreqPolicy} CpuFreqPolicy
         */
        CpuFreqPolicy.fromObject = function fromObject(object, long) {
            if (object instanceof $root.sysinfo.CpuFreqPolicy)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.sysinfo.CpuFreqPolicy();
            if (object.name != null)
                message.name = String(object.name);
            if (object.scalingGovernor != null)
                message.scalingGovernor = String(object.scalingGovernor);
            if (object.scalingCurFreqKhz != null)
                if ($util.Long)
                    (message.scalingCurFreqKhz = $util.Long.fromValue(object.scalingCurFreqKhz)).unsigned = true;
                else if (typeof object.scalingCurFreqKhz === "string")
                    message.scalingCurFreqKhz = parseInt(object.scalingCurFreqKhz, 10);
                else if (typeof object.scalingCurFreqKhz === "number")
                    message.scalingCurFreqKhz = object.scalingCurFreqKhz;
                else if (typeof object.scalingCurFreqKhz === "object")
                    message.scalingCurFreqKhz = new $util.LongBits(object.scalingCurFreqKhz.low >>> 0, object.scalingCurFreqKhz.high >>> 0).toNumber(true);
            if (object.scalingMinFreqKhz != null)
                if ($util.Long)
                    (message.scalingMinFreqKhz = $util.Long.fromValue(object.scalingMinFreqKhz)).unsigned = true;
                else if (typeof object.scalingMinFreqKhz === "string")
                    message.scalingMinFreqKhz = parseInt(object.scalingMinFreqKhz, 10);
                else if (typeof object.scalingMinFreqKhz === "number")
                    message.scalingMinFreqKhz = object.scalingMinFreqKhz;
                else if (typeof object.scalingMinFreqKhz === "object")
                    message.scalingMinFreqKhz = new $util.LongBits(object.scalingMinFreqKhz.low >>> 0, object.scalingMinFreqKhz.high >>> 0).toNumber(true);
            if (object.scalingMaxFreqKhz != null)
                if ($util.Long)
                    (message.scalingMaxFreqKhz = $util.Long.fromValue(object.scalingMaxFreqKhz)).unsigned = true;
                else if (typeof object.scalingMaxFreqKhz === "string")
                    message.scalingMaxFreqKhz = parseInt(object.scalingMaxFreqKhz, 10);
                else if (typeof object.scalingMaxFreqKhz === "number")
                    message.scalingMaxFreqKhz = object.scalingMaxFreqKhz;
                else if (typeof object.scalingMaxFreqKhz === "object")
                    message.scalingMaxFreqKhz = new $util.LongBits(object.scalingMaxFreqKhz.low >>> 0, object.scalingMaxFreqKhz.high >>> 0).toNumber(true);
            if (object.cpuinfoMinFreqKhz != null)
                if ($util.Long)
                    (message.cpuinfoMinFreqKhz = $util.Long.fromValue(object.cpuinfoMinFreqKhz)).unsigned = true;
                else if (typeof object.cpuinfoMinFreqKhz === "string")
                    message.cpuinfoMinFreqKhz = parseInt(object.cpuinfoMinFreqKhz, 10);
                else if (typeof object.cpuinfoMinFreqKhz === "number")
                    message.cpuinfoMinFreqKhz = object.cpuinfoMinFreqKhz;
                else if (typeof object.cpuinfoMinFreqKhz === "object")
                    message.cpuinfoMinFreqKhz = new $util.LongBits(object.cpuinfoMinFreqKhz.low >>> 0, object.cpuinfoMinFreqKhz.high >>> 0).toNumber(true);
            if (object.cpuinfoMaxFreqKhz != null)
                if ($util.Long)
                    (message.cpuinfoMaxFreqKhz = $util.Long.fromValue(object.cpuinfoMaxFreqKhz)).unsigned = true;
                else if (typeof object.cpuinfoMaxFreqKhz === "string")
                    message.cpuinfoMaxFreqKhz = parseInt(object.cpuinfoMaxFreqKhz, 10);
                else if (typeof object.cpuinfoMaxFreqKhz === "number")
                    message.cpuinfoMaxFreqKhz = object.cpuinfoMaxFreqKhz;
                else if (typeof object.cpuinfoMaxFreqKhz === "object")
                    message.cpuinfoMaxFreqKhz = new $util.LongBits(object.cpuinfoMaxFreqKhz.low >>> 0, object.cpuinfoMaxFreqKhz.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a CpuFreqPolicy message. Also converts values to other types if specified.
         * @function toObject
         * @memberof sysinfo.CpuFreqPolicy
         * @static
         * @param {sysinfo.CpuFreqPolicy} message CpuFreqPolicy
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CpuFreqPolicy.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.name = "";
                object.scalingGovernor = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.scalingCurFreqKhz = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.scalingCurFreqKhz = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.scalingMinFreqKhz = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.scalingMinFreqKhz = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.scalingMaxFreqKhz = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.scalingMaxFreqKhz = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.cpuinfoMinFreqKhz = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.cpuinfoMinFreqKhz = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.cpuinfoMaxFreqKhz = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.cpuinfoMaxFreqKhz = options.longs === String ? "0" : 0;
            }
            if (message.name != null && message.hasOwnProperty("name"))
                object.name = message.name;
            if (message.scalingGovernor != null && message.hasOwnProperty("scalingGovernor"))
                object.scalingGovernor = message.scalingGovernor;
            if (message.scalingCurFreqKhz != null && message.hasOwnProperty("scalingCurFreqKhz"))
                if (typeof message.scalingCurFreqKhz === "number")
                    object.scalingCurFreqKhz = options.longs === String ? String(message.scalingCurFreqKhz) : message.scalingCurFreqKhz;
                else
                    object.scalingCurFreqKhz = options.longs === String ? $util.Long.prototype.toString.call(message.scalingCurFreqKhz) : options.longs === Number ? new $util.LongBits(message.scalingCurFreqKhz.low >>> 0, message.scalingCurFreqKhz.high >>> 0).toNumber(true) : message.scalingCurFreqKhz;
            if (message.scalingMinFreqKhz != null && message.hasOwnProperty("scalingMinFreqKhz"))
                if (typeof message.scalingMinFreqKhz === "number")
                    object.scalingMinFreqKhz = options.longs === String ? String(message.scalingMinFreqKhz) : message.scalingMinFreqKhz;
                else
                    object.scalingMinFreqKhz = options.longs === String ? $util.Long.prototype.toString.call(message.scalingMinFreqKhz) : options.longs === Number ? new $util.LongBits(message.scalingMinFreqKhz.low >>> 0, message.scalingMinFreqKhz.high >>> 0).toNumber(true) : message.scalingMinFreqKhz;
            if (message.scalingMaxFreqKhz != null && message.hasOwnProperty("scalingMaxFreqKhz"))
                if (typeof message.scalingMaxFreqKhz === "number")
                    object.scalingMaxFreqKhz = options.longs === String ? String(message.scalingMaxFreqKhz) : message.scalingMaxFreqKhz;
                else
                    object.scalingMaxFreqKhz = options.longs === String ? $util.Long.prototype.toString.call(message.scalingMaxFreqKhz) : options.longs === Number ? new $util.LongBits(message.scalingMaxFreqKhz.low >>> 0, message.scalingMaxFreqKhz.high >>> 0).toNumber(true) : message.scalingMaxFreqKhz;
            if (message.cpuinfoMinFreqKhz != null && message.hasOwnProperty("cpuinfoMinFreqKhz"))
                if (typeof message.cpuinfoMinFreqKhz === "number")
                    object.cpuinfoMinFreqKhz = options.longs === String ? String(message.cpuinfoMinFreqKhz) : message.cpuinfoMinFreqKhz;
                else
                    object.cpuinfoMinFreqKhz = options.longs === String ? $util.Long.prototype.toString.call(message.cpuinfoMinFreqKhz) : options.longs === Number ? new $util.LongBits(message.cpuinfoMinFreqKhz.low >>> 0, message.cpuinfoMinFreqKhz.high >>> 0).toNumber(true) : message.cpuinfoMinFreqKhz;
            if (message.cpuinfoMaxFreqKhz != null && message.hasOwnProperty("cpuinfoMaxFreqKhz"))
                if (typeof message.cpuinfoMaxFreqKhz === "number")
                    object.cpuinfoMaxFreqKhz = options.longs === String ? String(message.cpuinfoMaxFreqKhz) : message.cpuinfoMaxFreqKhz;
                else
                    object.cpuinfoMaxFreqKhz = options.longs === String ? $util.Long.prototype.toString.call(message.cpuinfoMaxFreqKhz) : options.longs === Number ? new $util.LongBits(message.cpuinfoMaxFreqKhz.low >>> 0, message.cpuinfoMaxFreqKhz.high >>> 0).toNumber(true) : message.cpuinfoMaxFreqKhz;
            return object;
        };

        /**
         * Converts this CpuFreqPolicy to JSON.
         * @function toJSON
         * @memberof sysinfo.CpuFreqPolicy
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CpuFreqPolicy.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CpuFreqPolicy
         * @function getTypeUrl
         * @memberof sysinfo.CpuFreqPolicy
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CpuFreqPolicy.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/sysinfo.CpuFreqPolicy";
        };

        return CpuFreqPolicy;
    })();

    sysinfo.ThrottlingState = (function() {

        /**
         * Properties of a ThrottlingState.
         * @memberof sysinfo
         * @interface IThrottlingState
         * @property {sysinfo.IRpiThrottling|null} [rpi] ThrottlingState rpi
         * @property {boolean|null} [thermallyThrottled] ThrottlingState thermallyThrottled
         * @property {Array.<sysinfo.ICoolingDevice>|null} [coolingDevices] ThrottlingState coolingDevices
         * @property {Array.<sysinfo.ICpuFreqPolicy>|null} [cpufreqPolicies] ThrottlingState cpufreqPolicies
         */

        /**
         * Constructs a new ThrottlingState.
         * @memberof sysinfo
         * @classdesc Represents a ThrottlingState.
         * @implements IThrottlingState
         * @constructor
         * @param {sysinfo.IThrottlingState=} [properties] Properties to set
         */
        function ThrottlingState(properties) {
            this.coolingDevices = [];
            this.cpufreqPolicies = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ThrottlingState rpi.
         * @member {sysinfo.IRpiThrottling|null|undefined} rpi
         * @memberof sysinfo.ThrottlingState
         * @instance
         */
        ThrottlingState.prototype.rpi = null;

        /**
         * ThrottlingState thermallyThrottled.
         * @member {boolean} thermallyThrottled
         * @memberof sysinfo.ThrottlingState
         * @instance
         */
        ThrottlingState.prototype.thermallyThrottled = false;

        /**
         * ThrottlingState coolingDevices.
         * @member {Array.<sysinfo.ICoolingDevice>} coolingDevices
         * @memberof sysinfo.ThrottlingState
         * @instance
         */
        ThrottlingState.prototype.coolingDevices = $util.emptyArray;

        /**
         * ThrottlingState cpufreqPolicies.
         * @member {Array.<sysinfo.ICpuFreqPolicy>} cpufreqPolicies
         * @memberof sysinfo.ThrottlingState
         * @instance
         */
        ThrottlingState.prototype.cpufreqPolicies = $util.emptyArray;

        /**
         * Creates a new ThrottlingState instance using the specified properties.
         * @function create
         * @memberof sysinfo.ThrottlingState
         * @static
         * @param {sysinfo.IThrottlingState=} [properties] Properties to set
         * @returns {sysinfo.ThrottlingState} ThrottlingState instance
         */
        ThrottlingState.create = function create(properties) {
            return new ThrottlingState(properties);
        };

        /**
         * Encodes the specified ThrottlingState message. Does not implicitly {@link sysinfo.ThrottlingState.verify|verify} messages.
         * @function encode
         * @memberof sysinfo.ThrottlingState
         * @static
         * @param {sysinfo.IThrottlingState} message ThrottlingState message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ThrottlingState.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.rpi != null && Object.hasOwnProperty.call(message, "rpi"))
                $root.sysinfo.RpiThrottling.encode(message.rpi, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.thermallyThrottled != null && Object.hasOwnProperty.call(message, "thermallyThrottled"))
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.thermallyThrottled);
            if (message.coolingDevices != null && message.coolingDevices.length)
                for (let i = 0; i < message.coolingDevices.length; ++i)
                    $root.sysinfo.CoolingDevice.encode(message.coolingDevices[i], writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            if (message.cpufreqPolicies != null && message.cpufreqPolicies.length)
                for (let i = 0; i < message.cpufreqPolicies.length; ++i)
                    $root.sysinfo.CpuFreqPolicy.encode(message.cpufreqPolicies[i], writer.uint32(/* id 11, wireType 2 =*/90).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified ThrottlingState message, length delimited. Does not implicitly {@link sysinfo.ThrottlingState.verify|verify} messages.
         * @function encodeDelimited
         * @memberof sysinfo.ThrottlingState
         * @static
         * @param {sysinfo.IThrottlingState} message ThrottlingState message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ThrottlingState.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ThrottlingState message from the specified reader or buffer.
         * @function decode
         * @memberof sysinfo.ThrottlingState
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {sysinfo.ThrottlingState} ThrottlingState
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ThrottlingState.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.sysinfo.ThrottlingState();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.rpi = $root.sysinfo.RpiThrottling.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 2: {
                        message.thermallyThrottled = reader.bool();
                        break;
                    }
                case 10: {
                        if (!(message.coolingDevices && message.coolingDevices.length))
                            message.coolingDevices = [];
                        message.coolingDevices.push($root.sysinfo.CoolingDevice.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 11: {
                        if (!(message.cpufreqPolicies && message.cpufreqPolicies.length))
                            message.cpufreqPolicies = [];
                        message.cpufreqPolicies.push($root.sysinfo.CpuFreqPolicy.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ThrottlingState message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof sysinfo.ThrottlingState
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {sysinfo.ThrottlingState} ThrottlingState
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ThrottlingState.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ThrottlingState message.
         * @function verify
         * @memberof sysinfo.ThrottlingState
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ThrottlingState.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.rpi != null && message.hasOwnProperty("rpi")) {
                let error = $root.sysinfo.RpiThrottling.verify(message.rpi, long + 1);
                if (error)
                    return "rpi." + error;
            }
            if (message.thermallyThrottled != null && message.hasOwnProperty("thermallyThrottled"))
                if (typeof message.thermallyThrottled !== "boolean")
                    return "thermallyThrottled: boolean expected";
            if (message.coolingDevices != null && message.hasOwnProperty("coolingDevices")) {
                if (!Array.isArray(message.coolingDevices))
                    return "coolingDevices: array expected";
                for (let i = 0; i < message.coolingDevices.length; ++i) {
                    let error = $root.sysinfo.CoolingDevice.verify(message.coolingDevices[i], long + 1);
                    if (error)
                        return "coolingDevices." + error;
                }
            }
            if (message.cpufreqPolicies != null && message.hasOwnProperty("cpufreqPolicies")) {
                if (!Array.isArray(message.cpufreqPolicies))
                    return "cpufreqPolicies: array expected";
                for (let i = 0; i < message.cpufreqPolicies.length; ++i) {
                    let error = $root.sysinfo.CpuFreqPolicy.verify(message.cpufreqPolicies[i], long + 1);
                    if (error)
                        return "cpufreqPolicies." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ThrottlingState message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof sysinfo.ThrottlingState
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {sysinfo.ThrottlingState} ThrottlingState
         */
        ThrottlingState.fromObject = function fromObject(object, long) {
            if (object instanceof $root.sysinfo.ThrottlingState)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.sysinfo.ThrottlingState();
            if (object.rpi != null) {
                if (typeof object.rpi !== "object")
                    throw TypeError(".sysinfo.ThrottlingState.rpi: object expected");
                message.rpi = $root.sysinfo.RpiThrottling.fromObject(object.rpi, long + 1);
            }
            if (object.thermallyThrottled != null)
                message.thermallyThrottled = Boolean(object.thermallyThrottled);
            if (object.coolingDevices) {
                if (!Array.isArray(object.coolingDevices))
                    throw TypeError(".sysinfo.ThrottlingState.coolingDevices: array expected");
                message.coolingDevices = [];
                for (let i = 0; i < object.coolingDevices.length; ++i) {
                    if (typeof object.coolingDevices[i] !== "object")
                        throw TypeError(".sysinfo.ThrottlingState.coolingDevices: object expected");
                    message.coolingDevices[i] = $root.sysinfo.CoolingDevice.fromObject(object.coolingDevices[i], long + 1);
                }
            }
            if (object.cpufreqPolicies) {
                if (!Array.isArray(object.cpufreqPolicies))
                    throw TypeError(".sysinfo.ThrottlingState.cpufreqPolicies: array expected");
                message.cpufreqPolicies = [];
                for (let i = 0; i < object.cpufreqPolicies.length; ++i) {
                    if (typeof object.cpufreqPolicies[i] !== "object")
                        throw TypeError(".sysinfo.ThrottlingState.cpufreqPolicies: object expected");
                    message.cpufreqPolicies[i] = $root.sysinfo.CpuFreqPolicy.fromObject(object.cpufreqPolicies[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ThrottlingState message. Also converts values to other types if specified.
         * @function toObject
         * @memberof sysinfo.ThrottlingState
         * @static
         * @param {sysinfo.ThrottlingState} message ThrottlingState
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ThrottlingState.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.coolingDevices = [];
                object.cpufreqPolicies = [];
            }
            if (options.defaults) {
                object.rpi = null;
                object.thermallyThrottled = false;
            }
            if (message.rpi != null && message.hasOwnProperty("rpi"))
                object.rpi = $root.sysinfo.RpiThrottling.toObject(message.rpi, options);
            if (message.thermallyThrottled != null && message.hasOwnProperty("thermallyThrottled"))
                object.thermallyThrottled = message.thermallyThrottled;
            if (message.coolingDevices && message.coolingDevices.length) {
                object.coolingDevices = [];
                for (let j = 0; j < message.coolingDevices.length; ++j)
                    object.coolingDevices[j] = $root.sysinfo.CoolingDevice.toObject(message.coolingDevices[j], options);
            }
            if (message.cpufreqPolicies && message.cpufreqPolicies.length) {
                object.cpufreqPolicies = [];
                for (let j = 0; j < message.cpufreqPolicies.length; ++j)
                    object.cpufreqPolicies[j] = $root.sysinfo.CpuFreqPolicy.toObject(message.cpufreqPolicies[j], options);
            }
            return object;
        };

        /**
         * Converts this ThrottlingState to JSON.
         * @function toJSON
         * @memberof sysinfo.ThrottlingState
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ThrottlingState.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ThrottlingState
         * @function getTypeUrl
         * @memberof sysinfo.ThrottlingState
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ThrottlingState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/sysinfo.ThrottlingState";
        };

        return ThrottlingState;
    })();

    sysinfo.CellularAttribute = (function() {

        /**
         * Properties of a CellularAttribute.
         * @memberof sysinfo
         * @interface ICellularAttribute
         * @property {string|null} [key] CellularAttribute key
         * @property {string|null} [value] CellularAttribute value
         */

        /**
         * Constructs a new CellularAttribute.
         * @memberof sysinfo
         * @classdesc Represents a CellularAttribute.
         * @implements ICellularAttribute
         * @constructor
         * @param {sysinfo.ICellularAttribute=} [properties] Properties to set
         */
        function CellularAttribute(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CellularAttribute key.
         * @member {string} key
         * @memberof sysinfo.CellularAttribute
         * @instance
         */
        CellularAttribute.prototype.key = "";

        /**
         * CellularAttribute value.
         * @member {string} value
         * @memberof sysinfo.CellularAttribute
         * @instance
         */
        CellularAttribute.prototype.value = "";

        /**
         * Creates a new CellularAttribute instance using the specified properties.
         * @function create
         * @memberof sysinfo.CellularAttribute
         * @static
         * @param {sysinfo.ICellularAttribute=} [properties] Properties to set
         * @returns {sysinfo.CellularAttribute} CellularAttribute instance
         */
        CellularAttribute.create = function create(properties) {
            return new CellularAttribute(properties);
        };

        /**
         * Encodes the specified CellularAttribute message. Does not implicitly {@link sysinfo.CellularAttribute.verify|verify} messages.
         * @function encode
         * @memberof sysinfo.CellularAttribute
         * @static
         * @param {sysinfo.ICellularAttribute} message CellularAttribute message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CellularAttribute.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.key);
            if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.value);
            return writer;
        };

        /**
         * Encodes the specified CellularAttribute message, length delimited. Does not implicitly {@link sysinfo.CellularAttribute.verify|verify} messages.
         * @function encodeDelimited
         * @memberof sysinfo.CellularAttribute
         * @static
         * @param {sysinfo.ICellularAttribute} message CellularAttribute message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CellularAttribute.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CellularAttribute message from the specified reader or buffer.
         * @function decode
         * @memberof sysinfo.CellularAttribute
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {sysinfo.CellularAttribute} CellularAttribute
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CellularAttribute.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.sysinfo.CellularAttribute();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.key = reader.string();
                        break;
                    }
                case 2: {
                        message.value = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CellularAttribute message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof sysinfo.CellularAttribute
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {sysinfo.CellularAttribute} CellularAttribute
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CellularAttribute.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CellularAttribute message.
         * @function verify
         * @memberof sysinfo.CellularAttribute
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CellularAttribute.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.key != null && message.hasOwnProperty("key"))
                if (!$util.isString(message.key))
                    return "key: string expected";
            if (message.value != null && message.hasOwnProperty("value"))
                if (!$util.isString(message.value))
                    return "value: string expected";
            return null;
        };

        /**
         * Creates a CellularAttribute message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof sysinfo.CellularAttribute
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {sysinfo.CellularAttribute} CellularAttribute
         */
        CellularAttribute.fromObject = function fromObject(object, long) {
            if (object instanceof $root.sysinfo.CellularAttribute)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.sysinfo.CellularAttribute();
            if (object.key != null)
                message.key = String(object.key);
            if (object.value != null)
                message.value = String(object.value);
            return message;
        };

        /**
         * Creates a plain object from a CellularAttribute message. Also converts values to other types if specified.
         * @function toObject
         * @memberof sysinfo.CellularAttribute
         * @static
         * @param {sysinfo.CellularAttribute} message CellularAttribute
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CellularAttribute.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.key = "";
                object.value = "";
            }
            if (message.key != null && message.hasOwnProperty("key"))
                object.key = message.key;
            if (message.value != null && message.hasOwnProperty("value"))
                object.value = message.value;
            return object;
        };

        /**
         * Converts this CellularAttribute to JSON.
         * @function toJSON
         * @memberof sysinfo.CellularAttribute
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CellularAttribute.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CellularAttribute
         * @function getTypeUrl
         * @memberof sysinfo.CellularAttribute
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CellularAttribute.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/sysinfo.CellularAttribute";
        };

        return CellularAttribute;
    })();

    sysinfo.CellularError = (function() {

        /**
         * Properties of a CellularError.
         * @memberof sysinfo
         * @interface ICellularError
         * @property {Long|null} [monotonicStampNs] CellularError monotonicStampNs
         * @property {Long|null} [localStampNs] CellularError localStampNs
         * @property {Long|null} [appStartId] CellularError appStartId
         * @property {string|null} [scope] CellularError scope
         * @property {string|null} [path] CellularError path
         * @property {string|null} [message] CellularError message
         * @property {number|null} [exitCode] CellularError exitCode
         * @property {boolean|null} [timedOut] CellularError timedOut
         */

        /**
         * Constructs a new CellularError.
         * @memberof sysinfo
         * @classdesc Represents a CellularError.
         * @implements ICellularError
         * @constructor
         * @param {sysinfo.ICellularError=} [properties] Properties to set
         */
        function CellularError(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CellularError monotonicStampNs.
         * @member {Long} monotonicStampNs
         * @memberof sysinfo.CellularError
         * @instance
         */
        CellularError.prototype.monotonicStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * CellularError localStampNs.
         * @member {Long} localStampNs
         * @memberof sysinfo.CellularError
         * @instance
         */
        CellularError.prototype.localStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * CellularError appStartId.
         * @member {Long} appStartId
         * @memberof sysinfo.CellularError
         * @instance
         */
        CellularError.prototype.appStartId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * CellularError scope.
         * @member {string} scope
         * @memberof sysinfo.CellularError
         * @instance
         */
        CellularError.prototype.scope = "";

        /**
         * CellularError path.
         * @member {string} path
         * @memberof sysinfo.CellularError
         * @instance
         */
        CellularError.prototype.path = "";

        /**
         * CellularError message.
         * @member {string} message
         * @memberof sysinfo.CellularError
         * @instance
         */
        CellularError.prototype.message = "";

        /**
         * CellularError exitCode.
         * @member {number} exitCode
         * @memberof sysinfo.CellularError
         * @instance
         */
        CellularError.prototype.exitCode = 0;

        /**
         * CellularError timedOut.
         * @member {boolean} timedOut
         * @memberof sysinfo.CellularError
         * @instance
         */
        CellularError.prototype.timedOut = false;

        /**
         * Creates a new CellularError instance using the specified properties.
         * @function create
         * @memberof sysinfo.CellularError
         * @static
         * @param {sysinfo.ICellularError=} [properties] Properties to set
         * @returns {sysinfo.CellularError} CellularError instance
         */
        CellularError.create = function create(properties) {
            return new CellularError(properties);
        };

        /**
         * Encodes the specified CellularError message. Does not implicitly {@link sysinfo.CellularError.verify|verify} messages.
         * @function encode
         * @memberof sysinfo.CellularError
         * @static
         * @param {sysinfo.ICellularError} message CellularError message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CellularError.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.monotonicStampNs != null && Object.hasOwnProperty.call(message, "monotonicStampNs"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.monotonicStampNs);
            if (message.localStampNs != null && Object.hasOwnProperty.call(message, "localStampNs"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.localStampNs);
            if (message.appStartId != null && Object.hasOwnProperty.call(message, "appStartId"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.appStartId);
            if (message.scope != null && Object.hasOwnProperty.call(message, "scope"))
                writer.uint32(/* id 10, wireType 2 =*/82).string(message.scope);
            if (message.path != null && Object.hasOwnProperty.call(message, "path"))
                writer.uint32(/* id 11, wireType 2 =*/90).string(message.path);
            if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                writer.uint32(/* id 12, wireType 2 =*/98).string(message.message);
            if (message.exitCode != null && Object.hasOwnProperty.call(message, "exitCode"))
                writer.uint32(/* id 13, wireType 0 =*/104).int32(message.exitCode);
            if (message.timedOut != null && Object.hasOwnProperty.call(message, "timedOut"))
                writer.uint32(/* id 14, wireType 0 =*/112).bool(message.timedOut);
            return writer;
        };

        /**
         * Encodes the specified CellularError message, length delimited. Does not implicitly {@link sysinfo.CellularError.verify|verify} messages.
         * @function encodeDelimited
         * @memberof sysinfo.CellularError
         * @static
         * @param {sysinfo.ICellularError} message CellularError message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CellularError.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CellularError message from the specified reader or buffer.
         * @function decode
         * @memberof sysinfo.CellularError
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {sysinfo.CellularError} CellularError
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CellularError.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.sysinfo.CellularError();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.monotonicStampNs = reader.uint64();
                        break;
                    }
                case 2: {
                        message.localStampNs = reader.uint64();
                        break;
                    }
                case 3: {
                        message.appStartId = reader.uint64();
                        break;
                    }
                case 10: {
                        message.scope = reader.string();
                        break;
                    }
                case 11: {
                        message.path = reader.string();
                        break;
                    }
                case 12: {
                        message.message = reader.string();
                        break;
                    }
                case 13: {
                        message.exitCode = reader.int32();
                        break;
                    }
                case 14: {
                        message.timedOut = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CellularError message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof sysinfo.CellularError
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {sysinfo.CellularError} CellularError
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CellularError.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CellularError message.
         * @function verify
         * @memberof sysinfo.CellularError
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CellularError.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (!$util.isInteger(message.monotonicStampNs) && !(message.monotonicStampNs && $util.isInteger(message.monotonicStampNs.low) && $util.isInteger(message.monotonicStampNs.high)))
                    return "monotonicStampNs: integer|Long expected";
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (!$util.isInteger(message.localStampNs) && !(message.localStampNs && $util.isInteger(message.localStampNs.low) && $util.isInteger(message.localStampNs.high)))
                    return "localStampNs: integer|Long expected";
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (!$util.isInteger(message.appStartId) && !(message.appStartId && $util.isInteger(message.appStartId.low) && $util.isInteger(message.appStartId.high)))
                    return "appStartId: integer|Long expected";
            if (message.scope != null && message.hasOwnProperty("scope"))
                if (!$util.isString(message.scope))
                    return "scope: string expected";
            if (message.path != null && message.hasOwnProperty("path"))
                if (!$util.isString(message.path))
                    return "path: string expected";
            if (message.message != null && message.hasOwnProperty("message"))
                if (!$util.isString(message.message))
                    return "message: string expected";
            if (message.exitCode != null && message.hasOwnProperty("exitCode"))
                if (!$util.isInteger(message.exitCode))
                    return "exitCode: integer expected";
            if (message.timedOut != null && message.hasOwnProperty("timedOut"))
                if (typeof message.timedOut !== "boolean")
                    return "timedOut: boolean expected";
            return null;
        };

        /**
         * Creates a CellularError message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof sysinfo.CellularError
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {sysinfo.CellularError} CellularError
         */
        CellularError.fromObject = function fromObject(object, long) {
            if (object instanceof $root.sysinfo.CellularError)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.sysinfo.CellularError();
            if (object.monotonicStampNs != null)
                if ($util.Long)
                    (message.monotonicStampNs = $util.Long.fromValue(object.monotonicStampNs)).unsigned = true;
                else if (typeof object.monotonicStampNs === "string")
                    message.monotonicStampNs = parseInt(object.monotonicStampNs, 10);
                else if (typeof object.monotonicStampNs === "number")
                    message.monotonicStampNs = object.monotonicStampNs;
                else if (typeof object.monotonicStampNs === "object")
                    message.monotonicStampNs = new $util.LongBits(object.monotonicStampNs.low >>> 0, object.monotonicStampNs.high >>> 0).toNumber(true);
            if (object.localStampNs != null)
                if ($util.Long)
                    (message.localStampNs = $util.Long.fromValue(object.localStampNs)).unsigned = true;
                else if (typeof object.localStampNs === "string")
                    message.localStampNs = parseInt(object.localStampNs, 10);
                else if (typeof object.localStampNs === "number")
                    message.localStampNs = object.localStampNs;
                else if (typeof object.localStampNs === "object")
                    message.localStampNs = new $util.LongBits(object.localStampNs.low >>> 0, object.localStampNs.high >>> 0).toNumber(true);
            if (object.appStartId != null)
                if ($util.Long)
                    (message.appStartId = $util.Long.fromValue(object.appStartId)).unsigned = true;
                else if (typeof object.appStartId === "string")
                    message.appStartId = parseInt(object.appStartId, 10);
                else if (typeof object.appStartId === "number")
                    message.appStartId = object.appStartId;
                else if (typeof object.appStartId === "object")
                    message.appStartId = new $util.LongBits(object.appStartId.low >>> 0, object.appStartId.high >>> 0).toNumber(true);
            if (object.scope != null)
                message.scope = String(object.scope);
            if (object.path != null)
                message.path = String(object.path);
            if (object.message != null)
                message.message = String(object.message);
            if (object.exitCode != null)
                message.exitCode = object.exitCode | 0;
            if (object.timedOut != null)
                message.timedOut = Boolean(object.timedOut);
            return message;
        };

        /**
         * Creates a plain object from a CellularError message. Also converts values to other types if specified.
         * @function toObject
         * @memberof sysinfo.CellularError
         * @static
         * @param {sysinfo.CellularError} message CellularError
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CellularError.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.monotonicStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.monotonicStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.localStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.localStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.appStartId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.appStartId = options.longs === String ? "0" : 0;
                object.scope = "";
                object.path = "";
                object.message = "";
                object.exitCode = 0;
                object.timedOut = false;
            }
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (typeof message.monotonicStampNs === "number")
                    object.monotonicStampNs = options.longs === String ? String(message.monotonicStampNs) : message.monotonicStampNs;
                else
                    object.monotonicStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.monotonicStampNs) : options.longs === Number ? new $util.LongBits(message.monotonicStampNs.low >>> 0, message.monotonicStampNs.high >>> 0).toNumber(true) : message.monotonicStampNs;
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (typeof message.localStampNs === "number")
                    object.localStampNs = options.longs === String ? String(message.localStampNs) : message.localStampNs;
                else
                    object.localStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.localStampNs) : options.longs === Number ? new $util.LongBits(message.localStampNs.low >>> 0, message.localStampNs.high >>> 0).toNumber(true) : message.localStampNs;
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (typeof message.appStartId === "number")
                    object.appStartId = options.longs === String ? String(message.appStartId) : message.appStartId;
                else
                    object.appStartId = options.longs === String ? $util.Long.prototype.toString.call(message.appStartId) : options.longs === Number ? new $util.LongBits(message.appStartId.low >>> 0, message.appStartId.high >>> 0).toNumber(true) : message.appStartId;
            if (message.scope != null && message.hasOwnProperty("scope"))
                object.scope = message.scope;
            if (message.path != null && message.hasOwnProperty("path"))
                object.path = message.path;
            if (message.message != null && message.hasOwnProperty("message"))
                object.message = message.message;
            if (message.exitCode != null && message.hasOwnProperty("exitCode"))
                object.exitCode = message.exitCode;
            if (message.timedOut != null && message.hasOwnProperty("timedOut"))
                object.timedOut = message.timedOut;
            return object;
        };

        /**
         * Converts this CellularError to JSON.
         * @function toJSON
         * @memberof sysinfo.CellularError
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CellularError.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CellularError
         * @function getTypeUrl
         * @memberof sysinfo.CellularError
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CellularError.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/sysinfo.CellularError";
        };

        return CellularError;
    })();

    sysinfo.CellularIpConfig = (function() {

        /**
         * Properties of a CellularIpConfig.
         * @memberof sysinfo
         * @interface ICellularIpConfig
         * @property {string|null} [family] CellularIpConfig family
         * @property {string|null} [method] CellularIpConfig method
         * @property {string|null} [address] CellularIpConfig address
         * @property {number|null} [prefix] CellularIpConfig prefix
         * @property {string|null} [gateway] CellularIpConfig gateway
         * @property {Array.<string>|null} [dns] CellularIpConfig dns
         * @property {Array.<sysinfo.ICellularAttribute>|null} [attributes] CellularIpConfig attributes
         */

        /**
         * Constructs a new CellularIpConfig.
         * @memberof sysinfo
         * @classdesc Represents a CellularIpConfig.
         * @implements ICellularIpConfig
         * @constructor
         * @param {sysinfo.ICellularIpConfig=} [properties] Properties to set
         */
        function CellularIpConfig(properties) {
            this.dns = [];
            this.attributes = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CellularIpConfig family.
         * @member {string} family
         * @memberof sysinfo.CellularIpConfig
         * @instance
         */
        CellularIpConfig.prototype.family = "";

        /**
         * CellularIpConfig method.
         * @member {string} method
         * @memberof sysinfo.CellularIpConfig
         * @instance
         */
        CellularIpConfig.prototype.method = "";

        /**
         * CellularIpConfig address.
         * @member {string} address
         * @memberof sysinfo.CellularIpConfig
         * @instance
         */
        CellularIpConfig.prototype.address = "";

        /**
         * CellularIpConfig prefix.
         * @member {number} prefix
         * @memberof sysinfo.CellularIpConfig
         * @instance
         */
        CellularIpConfig.prototype.prefix = 0;

        /**
         * CellularIpConfig gateway.
         * @member {string} gateway
         * @memberof sysinfo.CellularIpConfig
         * @instance
         */
        CellularIpConfig.prototype.gateway = "";

        /**
         * CellularIpConfig dns.
         * @member {Array.<string>} dns
         * @memberof sysinfo.CellularIpConfig
         * @instance
         */
        CellularIpConfig.prototype.dns = $util.emptyArray;

        /**
         * CellularIpConfig attributes.
         * @member {Array.<sysinfo.ICellularAttribute>} attributes
         * @memberof sysinfo.CellularIpConfig
         * @instance
         */
        CellularIpConfig.prototype.attributes = $util.emptyArray;

        /**
         * Creates a new CellularIpConfig instance using the specified properties.
         * @function create
         * @memberof sysinfo.CellularIpConfig
         * @static
         * @param {sysinfo.ICellularIpConfig=} [properties] Properties to set
         * @returns {sysinfo.CellularIpConfig} CellularIpConfig instance
         */
        CellularIpConfig.create = function create(properties) {
            return new CellularIpConfig(properties);
        };

        /**
         * Encodes the specified CellularIpConfig message. Does not implicitly {@link sysinfo.CellularIpConfig.verify|verify} messages.
         * @function encode
         * @memberof sysinfo.CellularIpConfig
         * @static
         * @param {sysinfo.ICellularIpConfig} message CellularIpConfig message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CellularIpConfig.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.family != null && Object.hasOwnProperty.call(message, "family"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.family);
            if (message.method != null && Object.hasOwnProperty.call(message, "method"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.method);
            if (message.address != null && Object.hasOwnProperty.call(message, "address"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.address);
            if (message.prefix != null && Object.hasOwnProperty.call(message, "prefix"))
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.prefix);
            if (message.gateway != null && Object.hasOwnProperty.call(message, "gateway"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.gateway);
            if (message.dns != null && message.dns.length)
                for (let i = 0; i < message.dns.length; ++i)
                    writer.uint32(/* id 6, wireType 2 =*/50).string(message.dns[i]);
            if (message.attributes != null && message.attributes.length)
                for (let i = 0; i < message.attributes.length; ++i)
                    $root.sysinfo.CellularAttribute.encode(message.attributes[i], writer.uint32(/* id 100, wireType 2 =*/802).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified CellularIpConfig message, length delimited. Does not implicitly {@link sysinfo.CellularIpConfig.verify|verify} messages.
         * @function encodeDelimited
         * @memberof sysinfo.CellularIpConfig
         * @static
         * @param {sysinfo.ICellularIpConfig} message CellularIpConfig message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CellularIpConfig.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CellularIpConfig message from the specified reader or buffer.
         * @function decode
         * @memberof sysinfo.CellularIpConfig
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {sysinfo.CellularIpConfig} CellularIpConfig
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CellularIpConfig.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.sysinfo.CellularIpConfig();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.family = reader.string();
                        break;
                    }
                case 2: {
                        message.method = reader.string();
                        break;
                    }
                case 3: {
                        message.address = reader.string();
                        break;
                    }
                case 4: {
                        message.prefix = reader.uint32();
                        break;
                    }
                case 5: {
                        message.gateway = reader.string();
                        break;
                    }
                case 6: {
                        if (!(message.dns && message.dns.length))
                            message.dns = [];
                        message.dns.push(reader.string());
                        break;
                    }
                case 100: {
                        if (!(message.attributes && message.attributes.length))
                            message.attributes = [];
                        message.attributes.push($root.sysinfo.CellularAttribute.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CellularIpConfig message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof sysinfo.CellularIpConfig
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {sysinfo.CellularIpConfig} CellularIpConfig
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CellularIpConfig.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CellularIpConfig message.
         * @function verify
         * @memberof sysinfo.CellularIpConfig
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CellularIpConfig.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.family != null && message.hasOwnProperty("family"))
                if (!$util.isString(message.family))
                    return "family: string expected";
            if (message.method != null && message.hasOwnProperty("method"))
                if (!$util.isString(message.method))
                    return "method: string expected";
            if (message.address != null && message.hasOwnProperty("address"))
                if (!$util.isString(message.address))
                    return "address: string expected";
            if (message.prefix != null && message.hasOwnProperty("prefix"))
                if (!$util.isInteger(message.prefix))
                    return "prefix: integer expected";
            if (message.gateway != null && message.hasOwnProperty("gateway"))
                if (!$util.isString(message.gateway))
                    return "gateway: string expected";
            if (message.dns != null && message.hasOwnProperty("dns")) {
                if (!Array.isArray(message.dns))
                    return "dns: array expected";
                for (let i = 0; i < message.dns.length; ++i)
                    if (!$util.isString(message.dns[i]))
                        return "dns: string[] expected";
            }
            if (message.attributes != null && message.hasOwnProperty("attributes")) {
                if (!Array.isArray(message.attributes))
                    return "attributes: array expected";
                for (let i = 0; i < message.attributes.length; ++i) {
                    let error = $root.sysinfo.CellularAttribute.verify(message.attributes[i], long + 1);
                    if (error)
                        return "attributes." + error;
                }
            }
            return null;
        };

        /**
         * Creates a CellularIpConfig message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof sysinfo.CellularIpConfig
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {sysinfo.CellularIpConfig} CellularIpConfig
         */
        CellularIpConfig.fromObject = function fromObject(object, long) {
            if (object instanceof $root.sysinfo.CellularIpConfig)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.sysinfo.CellularIpConfig();
            if (object.family != null)
                message.family = String(object.family);
            if (object.method != null)
                message.method = String(object.method);
            if (object.address != null)
                message.address = String(object.address);
            if (object.prefix != null)
                message.prefix = object.prefix >>> 0;
            if (object.gateway != null)
                message.gateway = String(object.gateway);
            if (object.dns) {
                if (!Array.isArray(object.dns))
                    throw TypeError(".sysinfo.CellularIpConfig.dns: array expected");
                message.dns = [];
                for (let i = 0; i < object.dns.length; ++i)
                    message.dns[i] = String(object.dns[i]);
            }
            if (object.attributes) {
                if (!Array.isArray(object.attributes))
                    throw TypeError(".sysinfo.CellularIpConfig.attributes: array expected");
                message.attributes = [];
                for (let i = 0; i < object.attributes.length; ++i) {
                    if (typeof object.attributes[i] !== "object")
                        throw TypeError(".sysinfo.CellularIpConfig.attributes: object expected");
                    message.attributes[i] = $root.sysinfo.CellularAttribute.fromObject(object.attributes[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a CellularIpConfig message. Also converts values to other types if specified.
         * @function toObject
         * @memberof sysinfo.CellularIpConfig
         * @static
         * @param {sysinfo.CellularIpConfig} message CellularIpConfig
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CellularIpConfig.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.dns = [];
                object.attributes = [];
            }
            if (options.defaults) {
                object.family = "";
                object.method = "";
                object.address = "";
                object.prefix = 0;
                object.gateway = "";
            }
            if (message.family != null && message.hasOwnProperty("family"))
                object.family = message.family;
            if (message.method != null && message.hasOwnProperty("method"))
                object.method = message.method;
            if (message.address != null && message.hasOwnProperty("address"))
                object.address = message.address;
            if (message.prefix != null && message.hasOwnProperty("prefix"))
                object.prefix = message.prefix;
            if (message.gateway != null && message.hasOwnProperty("gateway"))
                object.gateway = message.gateway;
            if (message.dns && message.dns.length) {
                object.dns = [];
                for (let j = 0; j < message.dns.length; ++j)
                    object.dns[j] = message.dns[j];
            }
            if (message.attributes && message.attributes.length) {
                object.attributes = [];
                for (let j = 0; j < message.attributes.length; ++j)
                    object.attributes[j] = $root.sysinfo.CellularAttribute.toObject(message.attributes[j], options);
            }
            return object;
        };

        /**
         * Converts this CellularIpConfig to JSON.
         * @function toJSON
         * @memberof sysinfo.CellularIpConfig
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CellularIpConfig.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CellularIpConfig
         * @function getTypeUrl
         * @memberof sysinfo.CellularIpConfig
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CellularIpConfig.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/sysinfo.CellularIpConfig";
        };

        return CellularIpConfig;
    })();

    sysinfo.CellularBearer = (function() {

        /**
         * Properties of a CellularBearer.
         * @memberof sysinfo
         * @interface ICellularBearer
         * @property {Long|null} [monotonicStampNs] CellularBearer monotonicStampNs
         * @property {Long|null} [localStampNs] CellularBearer localStampNs
         * @property {Long|null} [appStartId] CellularBearer appStartId
         * @property {string|null} [path] CellularBearer path
         * @property {string|null} [bearerId] CellularBearer bearerId
         * @property {string|null} [type] CellularBearer type
         * @property {boolean|null} [connected] CellularBearer connected
         * @property {boolean|null} [suspended] CellularBearer suspended
         * @property {boolean|null} [multiplexed] CellularBearer multiplexed
         * @property {string|null} ["interface"] CellularBearer interface
         * @property {number|null} [ipTimeoutSeconds] CellularBearer ipTimeoutSeconds
         * @property {number|null} [profileId] CellularBearer profileId
         * @property {string|null} [apn] CellularBearer apn
         * @property {string|null} [apnType] CellularBearer apnType
         * @property {string|null} [roaming] CellularBearer roaming
         * @property {Array.<sysinfo.ICellularIpConfig>|null} [ip] CellularBearer ip
         * @property {Array.<sysinfo.ICellularAttribute>|null} [attributes] CellularBearer attributes
         */

        /**
         * Constructs a new CellularBearer.
         * @memberof sysinfo
         * @classdesc Represents a CellularBearer.
         * @implements ICellularBearer
         * @constructor
         * @param {sysinfo.ICellularBearer=} [properties] Properties to set
         */
        function CellularBearer(properties) {
            this.ip = [];
            this.attributes = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CellularBearer monotonicStampNs.
         * @member {Long} monotonicStampNs
         * @memberof sysinfo.CellularBearer
         * @instance
         */
        CellularBearer.prototype.monotonicStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * CellularBearer localStampNs.
         * @member {Long} localStampNs
         * @memberof sysinfo.CellularBearer
         * @instance
         */
        CellularBearer.prototype.localStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * CellularBearer appStartId.
         * @member {Long} appStartId
         * @memberof sysinfo.CellularBearer
         * @instance
         */
        CellularBearer.prototype.appStartId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * CellularBearer path.
         * @member {string} path
         * @memberof sysinfo.CellularBearer
         * @instance
         */
        CellularBearer.prototype.path = "";

        /**
         * CellularBearer bearerId.
         * @member {string} bearerId
         * @memberof sysinfo.CellularBearer
         * @instance
         */
        CellularBearer.prototype.bearerId = "";

        /**
         * CellularBearer type.
         * @member {string} type
         * @memberof sysinfo.CellularBearer
         * @instance
         */
        CellularBearer.prototype.type = "";

        /**
         * CellularBearer connected.
         * @member {boolean} connected
         * @memberof sysinfo.CellularBearer
         * @instance
         */
        CellularBearer.prototype.connected = false;

        /**
         * CellularBearer suspended.
         * @member {boolean} suspended
         * @memberof sysinfo.CellularBearer
         * @instance
         */
        CellularBearer.prototype.suspended = false;

        /**
         * CellularBearer multiplexed.
         * @member {boolean} multiplexed
         * @memberof sysinfo.CellularBearer
         * @instance
         */
        CellularBearer.prototype.multiplexed = false;

        /**
         * CellularBearer interface.
         * @member {string} interface
         * @memberof sysinfo.CellularBearer
         * @instance
         */
        CellularBearer.prototype["interface"] = "";

        /**
         * CellularBearer ipTimeoutSeconds.
         * @member {number} ipTimeoutSeconds
         * @memberof sysinfo.CellularBearer
         * @instance
         */
        CellularBearer.prototype.ipTimeoutSeconds = 0;

        /**
         * CellularBearer profileId.
         * @member {number} profileId
         * @memberof sysinfo.CellularBearer
         * @instance
         */
        CellularBearer.prototype.profileId = 0;

        /**
         * CellularBearer apn.
         * @member {string} apn
         * @memberof sysinfo.CellularBearer
         * @instance
         */
        CellularBearer.prototype.apn = "";

        /**
         * CellularBearer apnType.
         * @member {string} apnType
         * @memberof sysinfo.CellularBearer
         * @instance
         */
        CellularBearer.prototype.apnType = "";

        /**
         * CellularBearer roaming.
         * @member {string} roaming
         * @memberof sysinfo.CellularBearer
         * @instance
         */
        CellularBearer.prototype.roaming = "";

        /**
         * CellularBearer ip.
         * @member {Array.<sysinfo.ICellularIpConfig>} ip
         * @memberof sysinfo.CellularBearer
         * @instance
         */
        CellularBearer.prototype.ip = $util.emptyArray;

        /**
         * CellularBearer attributes.
         * @member {Array.<sysinfo.ICellularAttribute>} attributes
         * @memberof sysinfo.CellularBearer
         * @instance
         */
        CellularBearer.prototype.attributes = $util.emptyArray;

        /**
         * Creates a new CellularBearer instance using the specified properties.
         * @function create
         * @memberof sysinfo.CellularBearer
         * @static
         * @param {sysinfo.ICellularBearer=} [properties] Properties to set
         * @returns {sysinfo.CellularBearer} CellularBearer instance
         */
        CellularBearer.create = function create(properties) {
            return new CellularBearer(properties);
        };

        /**
         * Encodes the specified CellularBearer message. Does not implicitly {@link sysinfo.CellularBearer.verify|verify} messages.
         * @function encode
         * @memberof sysinfo.CellularBearer
         * @static
         * @param {sysinfo.ICellularBearer} message CellularBearer message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CellularBearer.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.monotonicStampNs != null && Object.hasOwnProperty.call(message, "monotonicStampNs"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.monotonicStampNs);
            if (message.localStampNs != null && Object.hasOwnProperty.call(message, "localStampNs"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.localStampNs);
            if (message.appStartId != null && Object.hasOwnProperty.call(message, "appStartId"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.appStartId);
            if (message.path != null && Object.hasOwnProperty.call(message, "path"))
                writer.uint32(/* id 10, wireType 2 =*/82).string(message.path);
            if (message.bearerId != null && Object.hasOwnProperty.call(message, "bearerId"))
                writer.uint32(/* id 11, wireType 2 =*/90).string(message.bearerId);
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(/* id 12, wireType 2 =*/98).string(message.type);
            if (message.connected != null && Object.hasOwnProperty.call(message, "connected"))
                writer.uint32(/* id 13, wireType 0 =*/104).bool(message.connected);
            if (message.suspended != null && Object.hasOwnProperty.call(message, "suspended"))
                writer.uint32(/* id 14, wireType 0 =*/112).bool(message.suspended);
            if (message.multiplexed != null && Object.hasOwnProperty.call(message, "multiplexed"))
                writer.uint32(/* id 15, wireType 0 =*/120).bool(message.multiplexed);
            if (message["interface"] != null && Object.hasOwnProperty.call(message, "interface"))
                writer.uint32(/* id 16, wireType 2 =*/130).string(message["interface"]);
            if (message.ipTimeoutSeconds != null && Object.hasOwnProperty.call(message, "ipTimeoutSeconds"))
                writer.uint32(/* id 17, wireType 0 =*/136).uint32(message.ipTimeoutSeconds);
            if (message.profileId != null && Object.hasOwnProperty.call(message, "profileId"))
                writer.uint32(/* id 18, wireType 0 =*/144).int32(message.profileId);
            if (message.apn != null && Object.hasOwnProperty.call(message, "apn"))
                writer.uint32(/* id 20, wireType 2 =*/162).string(message.apn);
            if (message.apnType != null && Object.hasOwnProperty.call(message, "apnType"))
                writer.uint32(/* id 21, wireType 2 =*/170).string(message.apnType);
            if (message.roaming != null && Object.hasOwnProperty.call(message, "roaming"))
                writer.uint32(/* id 22, wireType 2 =*/178).string(message.roaming);
            if (message.ip != null && message.ip.length)
                for (let i = 0; i < message.ip.length; ++i)
                    $root.sysinfo.CellularIpConfig.encode(message.ip[i], writer.uint32(/* id 30, wireType 2 =*/242).fork()).ldelim();
            if (message.attributes != null && message.attributes.length)
                for (let i = 0; i < message.attributes.length; ++i)
                    $root.sysinfo.CellularAttribute.encode(message.attributes[i], writer.uint32(/* id 100, wireType 2 =*/802).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified CellularBearer message, length delimited. Does not implicitly {@link sysinfo.CellularBearer.verify|verify} messages.
         * @function encodeDelimited
         * @memberof sysinfo.CellularBearer
         * @static
         * @param {sysinfo.ICellularBearer} message CellularBearer message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CellularBearer.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CellularBearer message from the specified reader or buffer.
         * @function decode
         * @memberof sysinfo.CellularBearer
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {sysinfo.CellularBearer} CellularBearer
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CellularBearer.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.sysinfo.CellularBearer();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.monotonicStampNs = reader.uint64();
                        break;
                    }
                case 2: {
                        message.localStampNs = reader.uint64();
                        break;
                    }
                case 3: {
                        message.appStartId = reader.uint64();
                        break;
                    }
                case 10: {
                        message.path = reader.string();
                        break;
                    }
                case 11: {
                        message.bearerId = reader.string();
                        break;
                    }
                case 12: {
                        message.type = reader.string();
                        break;
                    }
                case 13: {
                        message.connected = reader.bool();
                        break;
                    }
                case 14: {
                        message.suspended = reader.bool();
                        break;
                    }
                case 15: {
                        message.multiplexed = reader.bool();
                        break;
                    }
                case 16: {
                        message["interface"] = reader.string();
                        break;
                    }
                case 17: {
                        message.ipTimeoutSeconds = reader.uint32();
                        break;
                    }
                case 18: {
                        message.profileId = reader.int32();
                        break;
                    }
                case 20: {
                        message.apn = reader.string();
                        break;
                    }
                case 21: {
                        message.apnType = reader.string();
                        break;
                    }
                case 22: {
                        message.roaming = reader.string();
                        break;
                    }
                case 30: {
                        if (!(message.ip && message.ip.length))
                            message.ip = [];
                        message.ip.push($root.sysinfo.CellularIpConfig.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 100: {
                        if (!(message.attributes && message.attributes.length))
                            message.attributes = [];
                        message.attributes.push($root.sysinfo.CellularAttribute.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CellularBearer message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof sysinfo.CellularBearer
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {sysinfo.CellularBearer} CellularBearer
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CellularBearer.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CellularBearer message.
         * @function verify
         * @memberof sysinfo.CellularBearer
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CellularBearer.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (!$util.isInteger(message.monotonicStampNs) && !(message.monotonicStampNs && $util.isInteger(message.monotonicStampNs.low) && $util.isInteger(message.monotonicStampNs.high)))
                    return "monotonicStampNs: integer|Long expected";
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (!$util.isInteger(message.localStampNs) && !(message.localStampNs && $util.isInteger(message.localStampNs.low) && $util.isInteger(message.localStampNs.high)))
                    return "localStampNs: integer|Long expected";
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (!$util.isInteger(message.appStartId) && !(message.appStartId && $util.isInteger(message.appStartId.low) && $util.isInteger(message.appStartId.high)))
                    return "appStartId: integer|Long expected";
            if (message.path != null && message.hasOwnProperty("path"))
                if (!$util.isString(message.path))
                    return "path: string expected";
            if (message.bearerId != null && message.hasOwnProperty("bearerId"))
                if (!$util.isString(message.bearerId))
                    return "bearerId: string expected";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isString(message.type))
                    return "type: string expected";
            if (message.connected != null && message.hasOwnProperty("connected"))
                if (typeof message.connected !== "boolean")
                    return "connected: boolean expected";
            if (message.suspended != null && message.hasOwnProperty("suspended"))
                if (typeof message.suspended !== "boolean")
                    return "suspended: boolean expected";
            if (message.multiplexed != null && message.hasOwnProperty("multiplexed"))
                if (typeof message.multiplexed !== "boolean")
                    return "multiplexed: boolean expected";
            if (message["interface"] != null && message.hasOwnProperty("interface"))
                if (!$util.isString(message["interface"]))
                    return "interface: string expected";
            if (message.ipTimeoutSeconds != null && message.hasOwnProperty("ipTimeoutSeconds"))
                if (!$util.isInteger(message.ipTimeoutSeconds))
                    return "ipTimeoutSeconds: integer expected";
            if (message.profileId != null && message.hasOwnProperty("profileId"))
                if (!$util.isInteger(message.profileId))
                    return "profileId: integer expected";
            if (message.apn != null && message.hasOwnProperty("apn"))
                if (!$util.isString(message.apn))
                    return "apn: string expected";
            if (message.apnType != null && message.hasOwnProperty("apnType"))
                if (!$util.isString(message.apnType))
                    return "apnType: string expected";
            if (message.roaming != null && message.hasOwnProperty("roaming"))
                if (!$util.isString(message.roaming))
                    return "roaming: string expected";
            if (message.ip != null && message.hasOwnProperty("ip")) {
                if (!Array.isArray(message.ip))
                    return "ip: array expected";
                for (let i = 0; i < message.ip.length; ++i) {
                    let error = $root.sysinfo.CellularIpConfig.verify(message.ip[i], long + 1);
                    if (error)
                        return "ip." + error;
                }
            }
            if (message.attributes != null && message.hasOwnProperty("attributes")) {
                if (!Array.isArray(message.attributes))
                    return "attributes: array expected";
                for (let i = 0; i < message.attributes.length; ++i) {
                    let error = $root.sysinfo.CellularAttribute.verify(message.attributes[i], long + 1);
                    if (error)
                        return "attributes." + error;
                }
            }
            return null;
        };

        /**
         * Creates a CellularBearer message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof sysinfo.CellularBearer
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {sysinfo.CellularBearer} CellularBearer
         */
        CellularBearer.fromObject = function fromObject(object, long) {
            if (object instanceof $root.sysinfo.CellularBearer)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.sysinfo.CellularBearer();
            if (object.monotonicStampNs != null)
                if ($util.Long)
                    (message.monotonicStampNs = $util.Long.fromValue(object.monotonicStampNs)).unsigned = true;
                else if (typeof object.monotonicStampNs === "string")
                    message.monotonicStampNs = parseInt(object.monotonicStampNs, 10);
                else if (typeof object.monotonicStampNs === "number")
                    message.monotonicStampNs = object.monotonicStampNs;
                else if (typeof object.monotonicStampNs === "object")
                    message.monotonicStampNs = new $util.LongBits(object.monotonicStampNs.low >>> 0, object.monotonicStampNs.high >>> 0).toNumber(true);
            if (object.localStampNs != null)
                if ($util.Long)
                    (message.localStampNs = $util.Long.fromValue(object.localStampNs)).unsigned = true;
                else if (typeof object.localStampNs === "string")
                    message.localStampNs = parseInt(object.localStampNs, 10);
                else if (typeof object.localStampNs === "number")
                    message.localStampNs = object.localStampNs;
                else if (typeof object.localStampNs === "object")
                    message.localStampNs = new $util.LongBits(object.localStampNs.low >>> 0, object.localStampNs.high >>> 0).toNumber(true);
            if (object.appStartId != null)
                if ($util.Long)
                    (message.appStartId = $util.Long.fromValue(object.appStartId)).unsigned = true;
                else if (typeof object.appStartId === "string")
                    message.appStartId = parseInt(object.appStartId, 10);
                else if (typeof object.appStartId === "number")
                    message.appStartId = object.appStartId;
                else if (typeof object.appStartId === "object")
                    message.appStartId = new $util.LongBits(object.appStartId.low >>> 0, object.appStartId.high >>> 0).toNumber(true);
            if (object.path != null)
                message.path = String(object.path);
            if (object.bearerId != null)
                message.bearerId = String(object.bearerId);
            if (object.type != null)
                message.type = String(object.type);
            if (object.connected != null)
                message.connected = Boolean(object.connected);
            if (object.suspended != null)
                message.suspended = Boolean(object.suspended);
            if (object.multiplexed != null)
                message.multiplexed = Boolean(object.multiplexed);
            if (object["interface"] != null)
                message["interface"] = String(object["interface"]);
            if (object.ipTimeoutSeconds != null)
                message.ipTimeoutSeconds = object.ipTimeoutSeconds >>> 0;
            if (object.profileId != null)
                message.profileId = object.profileId | 0;
            if (object.apn != null)
                message.apn = String(object.apn);
            if (object.apnType != null)
                message.apnType = String(object.apnType);
            if (object.roaming != null)
                message.roaming = String(object.roaming);
            if (object.ip) {
                if (!Array.isArray(object.ip))
                    throw TypeError(".sysinfo.CellularBearer.ip: array expected");
                message.ip = [];
                for (let i = 0; i < object.ip.length; ++i) {
                    if (typeof object.ip[i] !== "object")
                        throw TypeError(".sysinfo.CellularBearer.ip: object expected");
                    message.ip[i] = $root.sysinfo.CellularIpConfig.fromObject(object.ip[i], long + 1);
                }
            }
            if (object.attributes) {
                if (!Array.isArray(object.attributes))
                    throw TypeError(".sysinfo.CellularBearer.attributes: array expected");
                message.attributes = [];
                for (let i = 0; i < object.attributes.length; ++i) {
                    if (typeof object.attributes[i] !== "object")
                        throw TypeError(".sysinfo.CellularBearer.attributes: object expected");
                    message.attributes[i] = $root.sysinfo.CellularAttribute.fromObject(object.attributes[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a CellularBearer message. Also converts values to other types if specified.
         * @function toObject
         * @memberof sysinfo.CellularBearer
         * @static
         * @param {sysinfo.CellularBearer} message CellularBearer
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CellularBearer.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.ip = [];
                object.attributes = [];
            }
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.monotonicStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.monotonicStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.localStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.localStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.appStartId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.appStartId = options.longs === String ? "0" : 0;
                object.path = "";
                object.bearerId = "";
                object.type = "";
                object.connected = false;
                object.suspended = false;
                object.multiplexed = false;
                object["interface"] = "";
                object.ipTimeoutSeconds = 0;
                object.profileId = 0;
                object.apn = "";
                object.apnType = "";
                object.roaming = "";
            }
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (typeof message.monotonicStampNs === "number")
                    object.monotonicStampNs = options.longs === String ? String(message.monotonicStampNs) : message.monotonicStampNs;
                else
                    object.monotonicStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.monotonicStampNs) : options.longs === Number ? new $util.LongBits(message.monotonicStampNs.low >>> 0, message.monotonicStampNs.high >>> 0).toNumber(true) : message.monotonicStampNs;
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (typeof message.localStampNs === "number")
                    object.localStampNs = options.longs === String ? String(message.localStampNs) : message.localStampNs;
                else
                    object.localStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.localStampNs) : options.longs === Number ? new $util.LongBits(message.localStampNs.low >>> 0, message.localStampNs.high >>> 0).toNumber(true) : message.localStampNs;
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (typeof message.appStartId === "number")
                    object.appStartId = options.longs === String ? String(message.appStartId) : message.appStartId;
                else
                    object.appStartId = options.longs === String ? $util.Long.prototype.toString.call(message.appStartId) : options.longs === Number ? new $util.LongBits(message.appStartId.low >>> 0, message.appStartId.high >>> 0).toNumber(true) : message.appStartId;
            if (message.path != null && message.hasOwnProperty("path"))
                object.path = message.path;
            if (message.bearerId != null && message.hasOwnProperty("bearerId"))
                object.bearerId = message.bearerId;
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.connected != null && message.hasOwnProperty("connected"))
                object.connected = message.connected;
            if (message.suspended != null && message.hasOwnProperty("suspended"))
                object.suspended = message.suspended;
            if (message.multiplexed != null && message.hasOwnProperty("multiplexed"))
                object.multiplexed = message.multiplexed;
            if (message["interface"] != null && message.hasOwnProperty("interface"))
                object["interface"] = message["interface"];
            if (message.ipTimeoutSeconds != null && message.hasOwnProperty("ipTimeoutSeconds"))
                object.ipTimeoutSeconds = message.ipTimeoutSeconds;
            if (message.profileId != null && message.hasOwnProperty("profileId"))
                object.profileId = message.profileId;
            if (message.apn != null && message.hasOwnProperty("apn"))
                object.apn = message.apn;
            if (message.apnType != null && message.hasOwnProperty("apnType"))
                object.apnType = message.apnType;
            if (message.roaming != null && message.hasOwnProperty("roaming"))
                object.roaming = message.roaming;
            if (message.ip && message.ip.length) {
                object.ip = [];
                for (let j = 0; j < message.ip.length; ++j)
                    object.ip[j] = $root.sysinfo.CellularIpConfig.toObject(message.ip[j], options);
            }
            if (message.attributes && message.attributes.length) {
                object.attributes = [];
                for (let j = 0; j < message.attributes.length; ++j)
                    object.attributes[j] = $root.sysinfo.CellularAttribute.toObject(message.attributes[j], options);
            }
            return object;
        };

        /**
         * Converts this CellularBearer to JSON.
         * @function toJSON
         * @memberof sysinfo.CellularBearer
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CellularBearer.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CellularBearer
         * @function getTypeUrl
         * @memberof sysinfo.CellularBearer
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CellularBearer.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/sysinfo.CellularBearer";
        };

        return CellularBearer;
    })();

    sysinfo.CellularSignal = (function() {

        /**
         * Properties of a CellularSignal.
         * @memberof sysinfo
         * @interface ICellularSignal
         * @property {Long|null} [monotonicStampNs] CellularSignal monotonicStampNs
         * @property {Long|null} [localStampNs] CellularSignal localStampNs
         * @property {Long|null} [appStartId] CellularSignal appStartId
         * @property {string|null} [accessTech] CellularSignal accessTech
         * @property {Array.<sysinfo.ICellularAttribute>|null} [metrics] CellularSignal metrics
         */

        /**
         * Constructs a new CellularSignal.
         * @memberof sysinfo
         * @classdesc Represents a CellularSignal.
         * @implements ICellularSignal
         * @constructor
         * @param {sysinfo.ICellularSignal=} [properties] Properties to set
         */
        function CellularSignal(properties) {
            this.metrics = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CellularSignal monotonicStampNs.
         * @member {Long} monotonicStampNs
         * @memberof sysinfo.CellularSignal
         * @instance
         */
        CellularSignal.prototype.monotonicStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * CellularSignal localStampNs.
         * @member {Long} localStampNs
         * @memberof sysinfo.CellularSignal
         * @instance
         */
        CellularSignal.prototype.localStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * CellularSignal appStartId.
         * @member {Long} appStartId
         * @memberof sysinfo.CellularSignal
         * @instance
         */
        CellularSignal.prototype.appStartId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * CellularSignal accessTech.
         * @member {string} accessTech
         * @memberof sysinfo.CellularSignal
         * @instance
         */
        CellularSignal.prototype.accessTech = "";

        /**
         * CellularSignal metrics.
         * @member {Array.<sysinfo.ICellularAttribute>} metrics
         * @memberof sysinfo.CellularSignal
         * @instance
         */
        CellularSignal.prototype.metrics = $util.emptyArray;

        /**
         * Creates a new CellularSignal instance using the specified properties.
         * @function create
         * @memberof sysinfo.CellularSignal
         * @static
         * @param {sysinfo.ICellularSignal=} [properties] Properties to set
         * @returns {sysinfo.CellularSignal} CellularSignal instance
         */
        CellularSignal.create = function create(properties) {
            return new CellularSignal(properties);
        };

        /**
         * Encodes the specified CellularSignal message. Does not implicitly {@link sysinfo.CellularSignal.verify|verify} messages.
         * @function encode
         * @memberof sysinfo.CellularSignal
         * @static
         * @param {sysinfo.ICellularSignal} message CellularSignal message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CellularSignal.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.monotonicStampNs != null && Object.hasOwnProperty.call(message, "monotonicStampNs"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.monotonicStampNs);
            if (message.localStampNs != null && Object.hasOwnProperty.call(message, "localStampNs"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.localStampNs);
            if (message.appStartId != null && Object.hasOwnProperty.call(message, "appStartId"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.appStartId);
            if (message.accessTech != null && Object.hasOwnProperty.call(message, "accessTech"))
                writer.uint32(/* id 10, wireType 2 =*/82).string(message.accessTech);
            if (message.metrics != null && message.metrics.length)
                for (let i = 0; i < message.metrics.length; ++i)
                    $root.sysinfo.CellularAttribute.encode(message.metrics[i], writer.uint32(/* id 20, wireType 2 =*/162).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified CellularSignal message, length delimited. Does not implicitly {@link sysinfo.CellularSignal.verify|verify} messages.
         * @function encodeDelimited
         * @memberof sysinfo.CellularSignal
         * @static
         * @param {sysinfo.ICellularSignal} message CellularSignal message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CellularSignal.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CellularSignal message from the specified reader or buffer.
         * @function decode
         * @memberof sysinfo.CellularSignal
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {sysinfo.CellularSignal} CellularSignal
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CellularSignal.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.sysinfo.CellularSignal();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.monotonicStampNs = reader.uint64();
                        break;
                    }
                case 2: {
                        message.localStampNs = reader.uint64();
                        break;
                    }
                case 3: {
                        message.appStartId = reader.uint64();
                        break;
                    }
                case 10: {
                        message.accessTech = reader.string();
                        break;
                    }
                case 20: {
                        if (!(message.metrics && message.metrics.length))
                            message.metrics = [];
                        message.metrics.push($root.sysinfo.CellularAttribute.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CellularSignal message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof sysinfo.CellularSignal
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {sysinfo.CellularSignal} CellularSignal
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CellularSignal.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CellularSignal message.
         * @function verify
         * @memberof sysinfo.CellularSignal
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CellularSignal.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (!$util.isInteger(message.monotonicStampNs) && !(message.monotonicStampNs && $util.isInteger(message.monotonicStampNs.low) && $util.isInteger(message.monotonicStampNs.high)))
                    return "monotonicStampNs: integer|Long expected";
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (!$util.isInteger(message.localStampNs) && !(message.localStampNs && $util.isInteger(message.localStampNs.low) && $util.isInteger(message.localStampNs.high)))
                    return "localStampNs: integer|Long expected";
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (!$util.isInteger(message.appStartId) && !(message.appStartId && $util.isInteger(message.appStartId.low) && $util.isInteger(message.appStartId.high)))
                    return "appStartId: integer|Long expected";
            if (message.accessTech != null && message.hasOwnProperty("accessTech"))
                if (!$util.isString(message.accessTech))
                    return "accessTech: string expected";
            if (message.metrics != null && message.hasOwnProperty("metrics")) {
                if (!Array.isArray(message.metrics))
                    return "metrics: array expected";
                for (let i = 0; i < message.metrics.length; ++i) {
                    let error = $root.sysinfo.CellularAttribute.verify(message.metrics[i], long + 1);
                    if (error)
                        return "metrics." + error;
                }
            }
            return null;
        };

        /**
         * Creates a CellularSignal message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof sysinfo.CellularSignal
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {sysinfo.CellularSignal} CellularSignal
         */
        CellularSignal.fromObject = function fromObject(object, long) {
            if (object instanceof $root.sysinfo.CellularSignal)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.sysinfo.CellularSignal();
            if (object.monotonicStampNs != null)
                if ($util.Long)
                    (message.monotonicStampNs = $util.Long.fromValue(object.monotonicStampNs)).unsigned = true;
                else if (typeof object.monotonicStampNs === "string")
                    message.monotonicStampNs = parseInt(object.monotonicStampNs, 10);
                else if (typeof object.monotonicStampNs === "number")
                    message.monotonicStampNs = object.monotonicStampNs;
                else if (typeof object.monotonicStampNs === "object")
                    message.monotonicStampNs = new $util.LongBits(object.monotonicStampNs.low >>> 0, object.monotonicStampNs.high >>> 0).toNumber(true);
            if (object.localStampNs != null)
                if ($util.Long)
                    (message.localStampNs = $util.Long.fromValue(object.localStampNs)).unsigned = true;
                else if (typeof object.localStampNs === "string")
                    message.localStampNs = parseInt(object.localStampNs, 10);
                else if (typeof object.localStampNs === "number")
                    message.localStampNs = object.localStampNs;
                else if (typeof object.localStampNs === "object")
                    message.localStampNs = new $util.LongBits(object.localStampNs.low >>> 0, object.localStampNs.high >>> 0).toNumber(true);
            if (object.appStartId != null)
                if ($util.Long)
                    (message.appStartId = $util.Long.fromValue(object.appStartId)).unsigned = true;
                else if (typeof object.appStartId === "string")
                    message.appStartId = parseInt(object.appStartId, 10);
                else if (typeof object.appStartId === "number")
                    message.appStartId = object.appStartId;
                else if (typeof object.appStartId === "object")
                    message.appStartId = new $util.LongBits(object.appStartId.low >>> 0, object.appStartId.high >>> 0).toNumber(true);
            if (object.accessTech != null)
                message.accessTech = String(object.accessTech);
            if (object.metrics) {
                if (!Array.isArray(object.metrics))
                    throw TypeError(".sysinfo.CellularSignal.metrics: array expected");
                message.metrics = [];
                for (let i = 0; i < object.metrics.length; ++i) {
                    if (typeof object.metrics[i] !== "object")
                        throw TypeError(".sysinfo.CellularSignal.metrics: object expected");
                    message.metrics[i] = $root.sysinfo.CellularAttribute.fromObject(object.metrics[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a CellularSignal message. Also converts values to other types if specified.
         * @function toObject
         * @memberof sysinfo.CellularSignal
         * @static
         * @param {sysinfo.CellularSignal} message CellularSignal
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CellularSignal.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.metrics = [];
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.monotonicStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.monotonicStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.localStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.localStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.appStartId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.appStartId = options.longs === String ? "0" : 0;
                object.accessTech = "";
            }
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (typeof message.monotonicStampNs === "number")
                    object.monotonicStampNs = options.longs === String ? String(message.monotonicStampNs) : message.monotonicStampNs;
                else
                    object.monotonicStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.monotonicStampNs) : options.longs === Number ? new $util.LongBits(message.monotonicStampNs.low >>> 0, message.monotonicStampNs.high >>> 0).toNumber(true) : message.monotonicStampNs;
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (typeof message.localStampNs === "number")
                    object.localStampNs = options.longs === String ? String(message.localStampNs) : message.localStampNs;
                else
                    object.localStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.localStampNs) : options.longs === Number ? new $util.LongBits(message.localStampNs.low >>> 0, message.localStampNs.high >>> 0).toNumber(true) : message.localStampNs;
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (typeof message.appStartId === "number")
                    object.appStartId = options.longs === String ? String(message.appStartId) : message.appStartId;
                else
                    object.appStartId = options.longs === String ? $util.Long.prototype.toString.call(message.appStartId) : options.longs === Number ? new $util.LongBits(message.appStartId.low >>> 0, message.appStartId.high >>> 0).toNumber(true) : message.appStartId;
            if (message.accessTech != null && message.hasOwnProperty("accessTech"))
                object.accessTech = message.accessTech;
            if (message.metrics && message.metrics.length) {
                object.metrics = [];
                for (let j = 0; j < message.metrics.length; ++j)
                    object.metrics[j] = $root.sysinfo.CellularAttribute.toObject(message.metrics[j], options);
            }
            return object;
        };

        /**
         * Converts this CellularSignal to JSON.
         * @function toJSON
         * @memberof sysinfo.CellularSignal
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CellularSignal.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CellularSignal
         * @function getTypeUrl
         * @memberof sysinfo.CellularSignal
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CellularSignal.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/sysinfo.CellularSignal";
        };

        return CellularSignal;
    })();

    sysinfo.CellularModem = (function() {

        /**
         * Properties of a CellularModem.
         * @memberof sysinfo
         * @interface ICellularModem
         * @property {Long|null} [monotonicStampNs] CellularModem monotonicStampNs
         * @property {Long|null} [localStampNs] CellularModem localStampNs
         * @property {Long|null} [appStartId] CellularModem appStartId
         * @property {string|null} [path] CellularModem path
         * @property {string|null} [modemId] CellularModem modemId
         * @property {string|null} [manufacturer] CellularModem manufacturer
         * @property {string|null} [model] CellularModem model
         * @property {string|null} [firmwareRevision] CellularModem firmwareRevision
         * @property {string|null} [hardwareRevision] CellularModem hardwareRevision
         * @property {string|null} [carrierConfig] CellularModem carrierConfig
         * @property {string|null} [equipmentId] CellularModem equipmentId
         * @property {string|null} [deviceId] CellularModem deviceId
         * @property {string|null} [device] CellularModem device
         * @property {string|null} [physdev] CellularModem physdev
         * @property {string|null} [drivers] CellularModem drivers
         * @property {string|null} [plugin] CellularModem plugin
         * @property {string|null} [primaryPort] CellularModem primaryPort
         * @property {string|null} [ports] CellularModem ports
         * @property {string|null} [state] CellularModem state
         * @property {string|null} [failedReason] CellularModem failedReason
         * @property {string|null} [powerState] CellularModem powerState
         * @property {string|null} [accessTech] CellularModem accessTech
         * @property {number|null} [signalQualityPercent] CellularModem signalQualityPercent
         * @property {boolean|null} [signalQualityRecent] CellularModem signalQualityRecent
         * @property {string|null} [imei] CellularModem imei
         * @property {string|null} [operatorId] CellularModem operatorId
         * @property {string|null} [operatorName] CellularModem operatorName
         * @property {string|null} [registration] CellularModem registration
         * @property {string|null} [packetServiceState] CellularModem packetServiceState
         * @property {string|null} [primarySimPath] CellularModem primarySimPath
         * @property {string|null} [ownNumbers] CellularModem ownNumbers
         * @property {Array.<sysinfo.ICellularBearer>|null} [bearers] CellularModem bearers
         * @property {Array.<sysinfo.ICellularSignal>|null} [signals] CellularModem signals
         * @property {Array.<sysinfo.ICellularAttribute>|null} [attributes] CellularModem attributes
         * @property {Array.<sysinfo.ICellularError>|null} [errors] CellularModem errors
         */

        /**
         * Constructs a new CellularModem.
         * @memberof sysinfo
         * @classdesc Represents a CellularModem.
         * @implements ICellularModem
         * @constructor
         * @param {sysinfo.ICellularModem=} [properties] Properties to set
         */
        function CellularModem(properties) {
            this.bearers = [];
            this.signals = [];
            this.attributes = [];
            this.errors = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CellularModem monotonicStampNs.
         * @member {Long} monotonicStampNs
         * @memberof sysinfo.CellularModem
         * @instance
         */
        CellularModem.prototype.monotonicStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * CellularModem localStampNs.
         * @member {Long} localStampNs
         * @memberof sysinfo.CellularModem
         * @instance
         */
        CellularModem.prototype.localStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * CellularModem appStartId.
         * @member {Long} appStartId
         * @memberof sysinfo.CellularModem
         * @instance
         */
        CellularModem.prototype.appStartId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * CellularModem path.
         * @member {string} path
         * @memberof sysinfo.CellularModem
         * @instance
         */
        CellularModem.prototype.path = "";

        /**
         * CellularModem modemId.
         * @member {string} modemId
         * @memberof sysinfo.CellularModem
         * @instance
         */
        CellularModem.prototype.modemId = "";

        /**
         * CellularModem manufacturer.
         * @member {string} manufacturer
         * @memberof sysinfo.CellularModem
         * @instance
         */
        CellularModem.prototype.manufacturer = "";

        /**
         * CellularModem model.
         * @member {string} model
         * @memberof sysinfo.CellularModem
         * @instance
         */
        CellularModem.prototype.model = "";

        /**
         * CellularModem firmwareRevision.
         * @member {string} firmwareRevision
         * @memberof sysinfo.CellularModem
         * @instance
         */
        CellularModem.prototype.firmwareRevision = "";

        /**
         * CellularModem hardwareRevision.
         * @member {string} hardwareRevision
         * @memberof sysinfo.CellularModem
         * @instance
         */
        CellularModem.prototype.hardwareRevision = "";

        /**
         * CellularModem carrierConfig.
         * @member {string} carrierConfig
         * @memberof sysinfo.CellularModem
         * @instance
         */
        CellularModem.prototype.carrierConfig = "";

        /**
         * CellularModem equipmentId.
         * @member {string} equipmentId
         * @memberof sysinfo.CellularModem
         * @instance
         */
        CellularModem.prototype.equipmentId = "";

        /**
         * CellularModem deviceId.
         * @member {string} deviceId
         * @memberof sysinfo.CellularModem
         * @instance
         */
        CellularModem.prototype.deviceId = "";

        /**
         * CellularModem device.
         * @member {string} device
         * @memberof sysinfo.CellularModem
         * @instance
         */
        CellularModem.prototype.device = "";

        /**
         * CellularModem physdev.
         * @member {string} physdev
         * @memberof sysinfo.CellularModem
         * @instance
         */
        CellularModem.prototype.physdev = "";

        /**
         * CellularModem drivers.
         * @member {string} drivers
         * @memberof sysinfo.CellularModem
         * @instance
         */
        CellularModem.prototype.drivers = "";

        /**
         * CellularModem plugin.
         * @member {string} plugin
         * @memberof sysinfo.CellularModem
         * @instance
         */
        CellularModem.prototype.plugin = "";

        /**
         * CellularModem primaryPort.
         * @member {string} primaryPort
         * @memberof sysinfo.CellularModem
         * @instance
         */
        CellularModem.prototype.primaryPort = "";

        /**
         * CellularModem ports.
         * @member {string} ports
         * @memberof sysinfo.CellularModem
         * @instance
         */
        CellularModem.prototype.ports = "";

        /**
         * CellularModem state.
         * @member {string} state
         * @memberof sysinfo.CellularModem
         * @instance
         */
        CellularModem.prototype.state = "";

        /**
         * CellularModem failedReason.
         * @member {string} failedReason
         * @memberof sysinfo.CellularModem
         * @instance
         */
        CellularModem.prototype.failedReason = "";

        /**
         * CellularModem powerState.
         * @member {string} powerState
         * @memberof sysinfo.CellularModem
         * @instance
         */
        CellularModem.prototype.powerState = "";

        /**
         * CellularModem accessTech.
         * @member {string} accessTech
         * @memberof sysinfo.CellularModem
         * @instance
         */
        CellularModem.prototype.accessTech = "";

        /**
         * CellularModem signalQualityPercent.
         * @member {number} signalQualityPercent
         * @memberof sysinfo.CellularModem
         * @instance
         */
        CellularModem.prototype.signalQualityPercent = 0;

        /**
         * CellularModem signalQualityRecent.
         * @member {boolean} signalQualityRecent
         * @memberof sysinfo.CellularModem
         * @instance
         */
        CellularModem.prototype.signalQualityRecent = false;

        /**
         * CellularModem imei.
         * @member {string} imei
         * @memberof sysinfo.CellularModem
         * @instance
         */
        CellularModem.prototype.imei = "";

        /**
         * CellularModem operatorId.
         * @member {string} operatorId
         * @memberof sysinfo.CellularModem
         * @instance
         */
        CellularModem.prototype.operatorId = "";

        /**
         * CellularModem operatorName.
         * @member {string} operatorName
         * @memberof sysinfo.CellularModem
         * @instance
         */
        CellularModem.prototype.operatorName = "";

        /**
         * CellularModem registration.
         * @member {string} registration
         * @memberof sysinfo.CellularModem
         * @instance
         */
        CellularModem.prototype.registration = "";

        /**
         * CellularModem packetServiceState.
         * @member {string} packetServiceState
         * @memberof sysinfo.CellularModem
         * @instance
         */
        CellularModem.prototype.packetServiceState = "";

        /**
         * CellularModem primarySimPath.
         * @member {string} primarySimPath
         * @memberof sysinfo.CellularModem
         * @instance
         */
        CellularModem.prototype.primarySimPath = "";

        /**
         * CellularModem ownNumbers.
         * @member {string} ownNumbers
         * @memberof sysinfo.CellularModem
         * @instance
         */
        CellularModem.prototype.ownNumbers = "";

        /**
         * CellularModem bearers.
         * @member {Array.<sysinfo.ICellularBearer>} bearers
         * @memberof sysinfo.CellularModem
         * @instance
         */
        CellularModem.prototype.bearers = $util.emptyArray;

        /**
         * CellularModem signals.
         * @member {Array.<sysinfo.ICellularSignal>} signals
         * @memberof sysinfo.CellularModem
         * @instance
         */
        CellularModem.prototype.signals = $util.emptyArray;

        /**
         * CellularModem attributes.
         * @member {Array.<sysinfo.ICellularAttribute>} attributes
         * @memberof sysinfo.CellularModem
         * @instance
         */
        CellularModem.prototype.attributes = $util.emptyArray;

        /**
         * CellularModem errors.
         * @member {Array.<sysinfo.ICellularError>} errors
         * @memberof sysinfo.CellularModem
         * @instance
         */
        CellularModem.prototype.errors = $util.emptyArray;

        /**
         * Creates a new CellularModem instance using the specified properties.
         * @function create
         * @memberof sysinfo.CellularModem
         * @static
         * @param {sysinfo.ICellularModem=} [properties] Properties to set
         * @returns {sysinfo.CellularModem} CellularModem instance
         */
        CellularModem.create = function create(properties) {
            return new CellularModem(properties);
        };

        /**
         * Encodes the specified CellularModem message. Does not implicitly {@link sysinfo.CellularModem.verify|verify} messages.
         * @function encode
         * @memberof sysinfo.CellularModem
         * @static
         * @param {sysinfo.ICellularModem} message CellularModem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CellularModem.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.monotonicStampNs != null && Object.hasOwnProperty.call(message, "monotonicStampNs"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.monotonicStampNs);
            if (message.localStampNs != null && Object.hasOwnProperty.call(message, "localStampNs"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.localStampNs);
            if (message.appStartId != null && Object.hasOwnProperty.call(message, "appStartId"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.appStartId);
            if (message.path != null && Object.hasOwnProperty.call(message, "path"))
                writer.uint32(/* id 10, wireType 2 =*/82).string(message.path);
            if (message.modemId != null && Object.hasOwnProperty.call(message, "modemId"))
                writer.uint32(/* id 11, wireType 2 =*/90).string(message.modemId);
            if (message.manufacturer != null && Object.hasOwnProperty.call(message, "manufacturer"))
                writer.uint32(/* id 12, wireType 2 =*/98).string(message.manufacturer);
            if (message.model != null && Object.hasOwnProperty.call(message, "model"))
                writer.uint32(/* id 13, wireType 2 =*/106).string(message.model);
            if (message.firmwareRevision != null && Object.hasOwnProperty.call(message, "firmwareRevision"))
                writer.uint32(/* id 14, wireType 2 =*/114).string(message.firmwareRevision);
            if (message.hardwareRevision != null && Object.hasOwnProperty.call(message, "hardwareRevision"))
                writer.uint32(/* id 15, wireType 2 =*/122).string(message.hardwareRevision);
            if (message.carrierConfig != null && Object.hasOwnProperty.call(message, "carrierConfig"))
                writer.uint32(/* id 16, wireType 2 =*/130).string(message.carrierConfig);
            if (message.equipmentId != null && Object.hasOwnProperty.call(message, "equipmentId"))
                writer.uint32(/* id 17, wireType 2 =*/138).string(message.equipmentId);
            if (message.deviceId != null && Object.hasOwnProperty.call(message, "deviceId"))
                writer.uint32(/* id 18, wireType 2 =*/146).string(message.deviceId);
            if (message.device != null && Object.hasOwnProperty.call(message, "device"))
                writer.uint32(/* id 20, wireType 2 =*/162).string(message.device);
            if (message.physdev != null && Object.hasOwnProperty.call(message, "physdev"))
                writer.uint32(/* id 21, wireType 2 =*/170).string(message.physdev);
            if (message.drivers != null && Object.hasOwnProperty.call(message, "drivers"))
                writer.uint32(/* id 22, wireType 2 =*/178).string(message.drivers);
            if (message.plugin != null && Object.hasOwnProperty.call(message, "plugin"))
                writer.uint32(/* id 23, wireType 2 =*/186).string(message.plugin);
            if (message.primaryPort != null && Object.hasOwnProperty.call(message, "primaryPort"))
                writer.uint32(/* id 24, wireType 2 =*/194).string(message.primaryPort);
            if (message.ports != null && Object.hasOwnProperty.call(message, "ports"))
                writer.uint32(/* id 25, wireType 2 =*/202).string(message.ports);
            if (message.state != null && Object.hasOwnProperty.call(message, "state"))
                writer.uint32(/* id 30, wireType 2 =*/242).string(message.state);
            if (message.failedReason != null && Object.hasOwnProperty.call(message, "failedReason"))
                writer.uint32(/* id 31, wireType 2 =*/250).string(message.failedReason);
            if (message.powerState != null && Object.hasOwnProperty.call(message, "powerState"))
                writer.uint32(/* id 32, wireType 2 =*/258).string(message.powerState);
            if (message.accessTech != null && Object.hasOwnProperty.call(message, "accessTech"))
                writer.uint32(/* id 33, wireType 2 =*/266).string(message.accessTech);
            if (message.signalQualityPercent != null && Object.hasOwnProperty.call(message, "signalQualityPercent"))
                writer.uint32(/* id 34, wireType 0 =*/272).uint32(message.signalQualityPercent);
            if (message.signalQualityRecent != null && Object.hasOwnProperty.call(message, "signalQualityRecent"))
                writer.uint32(/* id 35, wireType 0 =*/280).bool(message.signalQualityRecent);
            if (message.imei != null && Object.hasOwnProperty.call(message, "imei"))
                writer.uint32(/* id 40, wireType 2 =*/322).string(message.imei);
            if (message.operatorId != null && Object.hasOwnProperty.call(message, "operatorId"))
                writer.uint32(/* id 41, wireType 2 =*/330).string(message.operatorId);
            if (message.operatorName != null && Object.hasOwnProperty.call(message, "operatorName"))
                writer.uint32(/* id 42, wireType 2 =*/338).string(message.operatorName);
            if (message.registration != null && Object.hasOwnProperty.call(message, "registration"))
                writer.uint32(/* id 43, wireType 2 =*/346).string(message.registration);
            if (message.packetServiceState != null && Object.hasOwnProperty.call(message, "packetServiceState"))
                writer.uint32(/* id 44, wireType 2 =*/354).string(message.packetServiceState);
            if (message.primarySimPath != null && Object.hasOwnProperty.call(message, "primarySimPath"))
                writer.uint32(/* id 45, wireType 2 =*/362).string(message.primarySimPath);
            if (message.ownNumbers != null && Object.hasOwnProperty.call(message, "ownNumbers"))
                writer.uint32(/* id 46, wireType 2 =*/370).string(message.ownNumbers);
            if (message.bearers != null && message.bearers.length)
                for (let i = 0; i < message.bearers.length; ++i)
                    $root.sysinfo.CellularBearer.encode(message.bearers[i], writer.uint32(/* id 60, wireType 2 =*/482).fork()).ldelim();
            if (message.signals != null && message.signals.length)
                for (let i = 0; i < message.signals.length; ++i)
                    $root.sysinfo.CellularSignal.encode(message.signals[i], writer.uint32(/* id 61, wireType 2 =*/490).fork()).ldelim();
            if (message.attributes != null && message.attributes.length)
                for (let i = 0; i < message.attributes.length; ++i)
                    $root.sysinfo.CellularAttribute.encode(message.attributes[i], writer.uint32(/* id 100, wireType 2 =*/802).fork()).ldelim();
            if (message.errors != null && message.errors.length)
                for (let i = 0; i < message.errors.length; ++i)
                    $root.sysinfo.CellularError.encode(message.errors[i], writer.uint32(/* id 101, wireType 2 =*/810).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified CellularModem message, length delimited. Does not implicitly {@link sysinfo.CellularModem.verify|verify} messages.
         * @function encodeDelimited
         * @memberof sysinfo.CellularModem
         * @static
         * @param {sysinfo.ICellularModem} message CellularModem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CellularModem.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CellularModem message from the specified reader or buffer.
         * @function decode
         * @memberof sysinfo.CellularModem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {sysinfo.CellularModem} CellularModem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CellularModem.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.sysinfo.CellularModem();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.monotonicStampNs = reader.uint64();
                        break;
                    }
                case 2: {
                        message.localStampNs = reader.uint64();
                        break;
                    }
                case 3: {
                        message.appStartId = reader.uint64();
                        break;
                    }
                case 10: {
                        message.path = reader.string();
                        break;
                    }
                case 11: {
                        message.modemId = reader.string();
                        break;
                    }
                case 12: {
                        message.manufacturer = reader.string();
                        break;
                    }
                case 13: {
                        message.model = reader.string();
                        break;
                    }
                case 14: {
                        message.firmwareRevision = reader.string();
                        break;
                    }
                case 15: {
                        message.hardwareRevision = reader.string();
                        break;
                    }
                case 16: {
                        message.carrierConfig = reader.string();
                        break;
                    }
                case 17: {
                        message.equipmentId = reader.string();
                        break;
                    }
                case 18: {
                        message.deviceId = reader.string();
                        break;
                    }
                case 20: {
                        message.device = reader.string();
                        break;
                    }
                case 21: {
                        message.physdev = reader.string();
                        break;
                    }
                case 22: {
                        message.drivers = reader.string();
                        break;
                    }
                case 23: {
                        message.plugin = reader.string();
                        break;
                    }
                case 24: {
                        message.primaryPort = reader.string();
                        break;
                    }
                case 25: {
                        message.ports = reader.string();
                        break;
                    }
                case 30: {
                        message.state = reader.string();
                        break;
                    }
                case 31: {
                        message.failedReason = reader.string();
                        break;
                    }
                case 32: {
                        message.powerState = reader.string();
                        break;
                    }
                case 33: {
                        message.accessTech = reader.string();
                        break;
                    }
                case 34: {
                        message.signalQualityPercent = reader.uint32();
                        break;
                    }
                case 35: {
                        message.signalQualityRecent = reader.bool();
                        break;
                    }
                case 40: {
                        message.imei = reader.string();
                        break;
                    }
                case 41: {
                        message.operatorId = reader.string();
                        break;
                    }
                case 42: {
                        message.operatorName = reader.string();
                        break;
                    }
                case 43: {
                        message.registration = reader.string();
                        break;
                    }
                case 44: {
                        message.packetServiceState = reader.string();
                        break;
                    }
                case 45: {
                        message.primarySimPath = reader.string();
                        break;
                    }
                case 46: {
                        message.ownNumbers = reader.string();
                        break;
                    }
                case 60: {
                        if (!(message.bearers && message.bearers.length))
                            message.bearers = [];
                        message.bearers.push($root.sysinfo.CellularBearer.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 61: {
                        if (!(message.signals && message.signals.length))
                            message.signals = [];
                        message.signals.push($root.sysinfo.CellularSignal.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 100: {
                        if (!(message.attributes && message.attributes.length))
                            message.attributes = [];
                        message.attributes.push($root.sysinfo.CellularAttribute.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 101: {
                        if (!(message.errors && message.errors.length))
                            message.errors = [];
                        message.errors.push($root.sysinfo.CellularError.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CellularModem message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof sysinfo.CellularModem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {sysinfo.CellularModem} CellularModem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CellularModem.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CellularModem message.
         * @function verify
         * @memberof sysinfo.CellularModem
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CellularModem.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (!$util.isInteger(message.monotonicStampNs) && !(message.monotonicStampNs && $util.isInteger(message.monotonicStampNs.low) && $util.isInteger(message.monotonicStampNs.high)))
                    return "monotonicStampNs: integer|Long expected";
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (!$util.isInteger(message.localStampNs) && !(message.localStampNs && $util.isInteger(message.localStampNs.low) && $util.isInteger(message.localStampNs.high)))
                    return "localStampNs: integer|Long expected";
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (!$util.isInteger(message.appStartId) && !(message.appStartId && $util.isInteger(message.appStartId.low) && $util.isInteger(message.appStartId.high)))
                    return "appStartId: integer|Long expected";
            if (message.path != null && message.hasOwnProperty("path"))
                if (!$util.isString(message.path))
                    return "path: string expected";
            if (message.modemId != null && message.hasOwnProperty("modemId"))
                if (!$util.isString(message.modemId))
                    return "modemId: string expected";
            if (message.manufacturer != null && message.hasOwnProperty("manufacturer"))
                if (!$util.isString(message.manufacturer))
                    return "manufacturer: string expected";
            if (message.model != null && message.hasOwnProperty("model"))
                if (!$util.isString(message.model))
                    return "model: string expected";
            if (message.firmwareRevision != null && message.hasOwnProperty("firmwareRevision"))
                if (!$util.isString(message.firmwareRevision))
                    return "firmwareRevision: string expected";
            if (message.hardwareRevision != null && message.hasOwnProperty("hardwareRevision"))
                if (!$util.isString(message.hardwareRevision))
                    return "hardwareRevision: string expected";
            if (message.carrierConfig != null && message.hasOwnProperty("carrierConfig"))
                if (!$util.isString(message.carrierConfig))
                    return "carrierConfig: string expected";
            if (message.equipmentId != null && message.hasOwnProperty("equipmentId"))
                if (!$util.isString(message.equipmentId))
                    return "equipmentId: string expected";
            if (message.deviceId != null && message.hasOwnProperty("deviceId"))
                if (!$util.isString(message.deviceId))
                    return "deviceId: string expected";
            if (message.device != null && message.hasOwnProperty("device"))
                if (!$util.isString(message.device))
                    return "device: string expected";
            if (message.physdev != null && message.hasOwnProperty("physdev"))
                if (!$util.isString(message.physdev))
                    return "physdev: string expected";
            if (message.drivers != null && message.hasOwnProperty("drivers"))
                if (!$util.isString(message.drivers))
                    return "drivers: string expected";
            if (message.plugin != null && message.hasOwnProperty("plugin"))
                if (!$util.isString(message.plugin))
                    return "plugin: string expected";
            if (message.primaryPort != null && message.hasOwnProperty("primaryPort"))
                if (!$util.isString(message.primaryPort))
                    return "primaryPort: string expected";
            if (message.ports != null && message.hasOwnProperty("ports"))
                if (!$util.isString(message.ports))
                    return "ports: string expected";
            if (message.state != null && message.hasOwnProperty("state"))
                if (!$util.isString(message.state))
                    return "state: string expected";
            if (message.failedReason != null && message.hasOwnProperty("failedReason"))
                if (!$util.isString(message.failedReason))
                    return "failedReason: string expected";
            if (message.powerState != null && message.hasOwnProperty("powerState"))
                if (!$util.isString(message.powerState))
                    return "powerState: string expected";
            if (message.accessTech != null && message.hasOwnProperty("accessTech"))
                if (!$util.isString(message.accessTech))
                    return "accessTech: string expected";
            if (message.signalQualityPercent != null && message.hasOwnProperty("signalQualityPercent"))
                if (!$util.isInteger(message.signalQualityPercent))
                    return "signalQualityPercent: integer expected";
            if (message.signalQualityRecent != null && message.hasOwnProperty("signalQualityRecent"))
                if (typeof message.signalQualityRecent !== "boolean")
                    return "signalQualityRecent: boolean expected";
            if (message.imei != null && message.hasOwnProperty("imei"))
                if (!$util.isString(message.imei))
                    return "imei: string expected";
            if (message.operatorId != null && message.hasOwnProperty("operatorId"))
                if (!$util.isString(message.operatorId))
                    return "operatorId: string expected";
            if (message.operatorName != null && message.hasOwnProperty("operatorName"))
                if (!$util.isString(message.operatorName))
                    return "operatorName: string expected";
            if (message.registration != null && message.hasOwnProperty("registration"))
                if (!$util.isString(message.registration))
                    return "registration: string expected";
            if (message.packetServiceState != null && message.hasOwnProperty("packetServiceState"))
                if (!$util.isString(message.packetServiceState))
                    return "packetServiceState: string expected";
            if (message.primarySimPath != null && message.hasOwnProperty("primarySimPath"))
                if (!$util.isString(message.primarySimPath))
                    return "primarySimPath: string expected";
            if (message.ownNumbers != null && message.hasOwnProperty("ownNumbers"))
                if (!$util.isString(message.ownNumbers))
                    return "ownNumbers: string expected";
            if (message.bearers != null && message.hasOwnProperty("bearers")) {
                if (!Array.isArray(message.bearers))
                    return "bearers: array expected";
                for (let i = 0; i < message.bearers.length; ++i) {
                    let error = $root.sysinfo.CellularBearer.verify(message.bearers[i], long + 1);
                    if (error)
                        return "bearers." + error;
                }
            }
            if (message.signals != null && message.hasOwnProperty("signals")) {
                if (!Array.isArray(message.signals))
                    return "signals: array expected";
                for (let i = 0; i < message.signals.length; ++i) {
                    let error = $root.sysinfo.CellularSignal.verify(message.signals[i], long + 1);
                    if (error)
                        return "signals." + error;
                }
            }
            if (message.attributes != null && message.hasOwnProperty("attributes")) {
                if (!Array.isArray(message.attributes))
                    return "attributes: array expected";
                for (let i = 0; i < message.attributes.length; ++i) {
                    let error = $root.sysinfo.CellularAttribute.verify(message.attributes[i], long + 1);
                    if (error)
                        return "attributes." + error;
                }
            }
            if (message.errors != null && message.hasOwnProperty("errors")) {
                if (!Array.isArray(message.errors))
                    return "errors: array expected";
                for (let i = 0; i < message.errors.length; ++i) {
                    let error = $root.sysinfo.CellularError.verify(message.errors[i], long + 1);
                    if (error)
                        return "errors." + error;
                }
            }
            return null;
        };

        /**
         * Creates a CellularModem message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof sysinfo.CellularModem
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {sysinfo.CellularModem} CellularModem
         */
        CellularModem.fromObject = function fromObject(object, long) {
            if (object instanceof $root.sysinfo.CellularModem)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.sysinfo.CellularModem();
            if (object.monotonicStampNs != null)
                if ($util.Long)
                    (message.monotonicStampNs = $util.Long.fromValue(object.monotonicStampNs)).unsigned = true;
                else if (typeof object.monotonicStampNs === "string")
                    message.monotonicStampNs = parseInt(object.monotonicStampNs, 10);
                else if (typeof object.monotonicStampNs === "number")
                    message.monotonicStampNs = object.monotonicStampNs;
                else if (typeof object.monotonicStampNs === "object")
                    message.monotonicStampNs = new $util.LongBits(object.monotonicStampNs.low >>> 0, object.monotonicStampNs.high >>> 0).toNumber(true);
            if (object.localStampNs != null)
                if ($util.Long)
                    (message.localStampNs = $util.Long.fromValue(object.localStampNs)).unsigned = true;
                else if (typeof object.localStampNs === "string")
                    message.localStampNs = parseInt(object.localStampNs, 10);
                else if (typeof object.localStampNs === "number")
                    message.localStampNs = object.localStampNs;
                else if (typeof object.localStampNs === "object")
                    message.localStampNs = new $util.LongBits(object.localStampNs.low >>> 0, object.localStampNs.high >>> 0).toNumber(true);
            if (object.appStartId != null)
                if ($util.Long)
                    (message.appStartId = $util.Long.fromValue(object.appStartId)).unsigned = true;
                else if (typeof object.appStartId === "string")
                    message.appStartId = parseInt(object.appStartId, 10);
                else if (typeof object.appStartId === "number")
                    message.appStartId = object.appStartId;
                else if (typeof object.appStartId === "object")
                    message.appStartId = new $util.LongBits(object.appStartId.low >>> 0, object.appStartId.high >>> 0).toNumber(true);
            if (object.path != null)
                message.path = String(object.path);
            if (object.modemId != null)
                message.modemId = String(object.modemId);
            if (object.manufacturer != null)
                message.manufacturer = String(object.manufacturer);
            if (object.model != null)
                message.model = String(object.model);
            if (object.firmwareRevision != null)
                message.firmwareRevision = String(object.firmwareRevision);
            if (object.hardwareRevision != null)
                message.hardwareRevision = String(object.hardwareRevision);
            if (object.carrierConfig != null)
                message.carrierConfig = String(object.carrierConfig);
            if (object.equipmentId != null)
                message.equipmentId = String(object.equipmentId);
            if (object.deviceId != null)
                message.deviceId = String(object.deviceId);
            if (object.device != null)
                message.device = String(object.device);
            if (object.physdev != null)
                message.physdev = String(object.physdev);
            if (object.drivers != null)
                message.drivers = String(object.drivers);
            if (object.plugin != null)
                message.plugin = String(object.plugin);
            if (object.primaryPort != null)
                message.primaryPort = String(object.primaryPort);
            if (object.ports != null)
                message.ports = String(object.ports);
            if (object.state != null)
                message.state = String(object.state);
            if (object.failedReason != null)
                message.failedReason = String(object.failedReason);
            if (object.powerState != null)
                message.powerState = String(object.powerState);
            if (object.accessTech != null)
                message.accessTech = String(object.accessTech);
            if (object.signalQualityPercent != null)
                message.signalQualityPercent = object.signalQualityPercent >>> 0;
            if (object.signalQualityRecent != null)
                message.signalQualityRecent = Boolean(object.signalQualityRecent);
            if (object.imei != null)
                message.imei = String(object.imei);
            if (object.operatorId != null)
                message.operatorId = String(object.operatorId);
            if (object.operatorName != null)
                message.operatorName = String(object.operatorName);
            if (object.registration != null)
                message.registration = String(object.registration);
            if (object.packetServiceState != null)
                message.packetServiceState = String(object.packetServiceState);
            if (object.primarySimPath != null)
                message.primarySimPath = String(object.primarySimPath);
            if (object.ownNumbers != null)
                message.ownNumbers = String(object.ownNumbers);
            if (object.bearers) {
                if (!Array.isArray(object.bearers))
                    throw TypeError(".sysinfo.CellularModem.bearers: array expected");
                message.bearers = [];
                for (let i = 0; i < object.bearers.length; ++i) {
                    if (typeof object.bearers[i] !== "object")
                        throw TypeError(".sysinfo.CellularModem.bearers: object expected");
                    message.bearers[i] = $root.sysinfo.CellularBearer.fromObject(object.bearers[i], long + 1);
                }
            }
            if (object.signals) {
                if (!Array.isArray(object.signals))
                    throw TypeError(".sysinfo.CellularModem.signals: array expected");
                message.signals = [];
                for (let i = 0; i < object.signals.length; ++i) {
                    if (typeof object.signals[i] !== "object")
                        throw TypeError(".sysinfo.CellularModem.signals: object expected");
                    message.signals[i] = $root.sysinfo.CellularSignal.fromObject(object.signals[i], long + 1);
                }
            }
            if (object.attributes) {
                if (!Array.isArray(object.attributes))
                    throw TypeError(".sysinfo.CellularModem.attributes: array expected");
                message.attributes = [];
                for (let i = 0; i < object.attributes.length; ++i) {
                    if (typeof object.attributes[i] !== "object")
                        throw TypeError(".sysinfo.CellularModem.attributes: object expected");
                    message.attributes[i] = $root.sysinfo.CellularAttribute.fromObject(object.attributes[i], long + 1);
                }
            }
            if (object.errors) {
                if (!Array.isArray(object.errors))
                    throw TypeError(".sysinfo.CellularModem.errors: array expected");
                message.errors = [];
                for (let i = 0; i < object.errors.length; ++i) {
                    if (typeof object.errors[i] !== "object")
                        throw TypeError(".sysinfo.CellularModem.errors: object expected");
                    message.errors[i] = $root.sysinfo.CellularError.fromObject(object.errors[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a CellularModem message. Also converts values to other types if specified.
         * @function toObject
         * @memberof sysinfo.CellularModem
         * @static
         * @param {sysinfo.CellularModem} message CellularModem
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CellularModem.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.bearers = [];
                object.signals = [];
                object.attributes = [];
                object.errors = [];
            }
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.monotonicStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.monotonicStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.localStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.localStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.appStartId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.appStartId = options.longs === String ? "0" : 0;
                object.path = "";
                object.modemId = "";
                object.manufacturer = "";
                object.model = "";
                object.firmwareRevision = "";
                object.hardwareRevision = "";
                object.carrierConfig = "";
                object.equipmentId = "";
                object.deviceId = "";
                object.device = "";
                object.physdev = "";
                object.drivers = "";
                object.plugin = "";
                object.primaryPort = "";
                object.ports = "";
                object.state = "";
                object.failedReason = "";
                object.powerState = "";
                object.accessTech = "";
                object.signalQualityPercent = 0;
                object.signalQualityRecent = false;
                object.imei = "";
                object.operatorId = "";
                object.operatorName = "";
                object.registration = "";
                object.packetServiceState = "";
                object.primarySimPath = "";
                object.ownNumbers = "";
            }
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (typeof message.monotonicStampNs === "number")
                    object.monotonicStampNs = options.longs === String ? String(message.monotonicStampNs) : message.monotonicStampNs;
                else
                    object.monotonicStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.monotonicStampNs) : options.longs === Number ? new $util.LongBits(message.monotonicStampNs.low >>> 0, message.monotonicStampNs.high >>> 0).toNumber(true) : message.monotonicStampNs;
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (typeof message.localStampNs === "number")
                    object.localStampNs = options.longs === String ? String(message.localStampNs) : message.localStampNs;
                else
                    object.localStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.localStampNs) : options.longs === Number ? new $util.LongBits(message.localStampNs.low >>> 0, message.localStampNs.high >>> 0).toNumber(true) : message.localStampNs;
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (typeof message.appStartId === "number")
                    object.appStartId = options.longs === String ? String(message.appStartId) : message.appStartId;
                else
                    object.appStartId = options.longs === String ? $util.Long.prototype.toString.call(message.appStartId) : options.longs === Number ? new $util.LongBits(message.appStartId.low >>> 0, message.appStartId.high >>> 0).toNumber(true) : message.appStartId;
            if (message.path != null && message.hasOwnProperty("path"))
                object.path = message.path;
            if (message.modemId != null && message.hasOwnProperty("modemId"))
                object.modemId = message.modemId;
            if (message.manufacturer != null && message.hasOwnProperty("manufacturer"))
                object.manufacturer = message.manufacturer;
            if (message.model != null && message.hasOwnProperty("model"))
                object.model = message.model;
            if (message.firmwareRevision != null && message.hasOwnProperty("firmwareRevision"))
                object.firmwareRevision = message.firmwareRevision;
            if (message.hardwareRevision != null && message.hasOwnProperty("hardwareRevision"))
                object.hardwareRevision = message.hardwareRevision;
            if (message.carrierConfig != null && message.hasOwnProperty("carrierConfig"))
                object.carrierConfig = message.carrierConfig;
            if (message.equipmentId != null && message.hasOwnProperty("equipmentId"))
                object.equipmentId = message.equipmentId;
            if (message.deviceId != null && message.hasOwnProperty("deviceId"))
                object.deviceId = message.deviceId;
            if (message.device != null && message.hasOwnProperty("device"))
                object.device = message.device;
            if (message.physdev != null && message.hasOwnProperty("physdev"))
                object.physdev = message.physdev;
            if (message.drivers != null && message.hasOwnProperty("drivers"))
                object.drivers = message.drivers;
            if (message.plugin != null && message.hasOwnProperty("plugin"))
                object.plugin = message.plugin;
            if (message.primaryPort != null && message.hasOwnProperty("primaryPort"))
                object.primaryPort = message.primaryPort;
            if (message.ports != null && message.hasOwnProperty("ports"))
                object.ports = message.ports;
            if (message.state != null && message.hasOwnProperty("state"))
                object.state = message.state;
            if (message.failedReason != null && message.hasOwnProperty("failedReason"))
                object.failedReason = message.failedReason;
            if (message.powerState != null && message.hasOwnProperty("powerState"))
                object.powerState = message.powerState;
            if (message.accessTech != null && message.hasOwnProperty("accessTech"))
                object.accessTech = message.accessTech;
            if (message.signalQualityPercent != null && message.hasOwnProperty("signalQualityPercent"))
                object.signalQualityPercent = message.signalQualityPercent;
            if (message.signalQualityRecent != null && message.hasOwnProperty("signalQualityRecent"))
                object.signalQualityRecent = message.signalQualityRecent;
            if (message.imei != null && message.hasOwnProperty("imei"))
                object.imei = message.imei;
            if (message.operatorId != null && message.hasOwnProperty("operatorId"))
                object.operatorId = message.operatorId;
            if (message.operatorName != null && message.hasOwnProperty("operatorName"))
                object.operatorName = message.operatorName;
            if (message.registration != null && message.hasOwnProperty("registration"))
                object.registration = message.registration;
            if (message.packetServiceState != null && message.hasOwnProperty("packetServiceState"))
                object.packetServiceState = message.packetServiceState;
            if (message.primarySimPath != null && message.hasOwnProperty("primarySimPath"))
                object.primarySimPath = message.primarySimPath;
            if (message.ownNumbers != null && message.hasOwnProperty("ownNumbers"))
                object.ownNumbers = message.ownNumbers;
            if (message.bearers && message.bearers.length) {
                object.bearers = [];
                for (let j = 0; j < message.bearers.length; ++j)
                    object.bearers[j] = $root.sysinfo.CellularBearer.toObject(message.bearers[j], options);
            }
            if (message.signals && message.signals.length) {
                object.signals = [];
                for (let j = 0; j < message.signals.length; ++j)
                    object.signals[j] = $root.sysinfo.CellularSignal.toObject(message.signals[j], options);
            }
            if (message.attributes && message.attributes.length) {
                object.attributes = [];
                for (let j = 0; j < message.attributes.length; ++j)
                    object.attributes[j] = $root.sysinfo.CellularAttribute.toObject(message.attributes[j], options);
            }
            if (message.errors && message.errors.length) {
                object.errors = [];
                for (let j = 0; j < message.errors.length; ++j)
                    object.errors[j] = $root.sysinfo.CellularError.toObject(message.errors[j], options);
            }
            return object;
        };

        /**
         * Converts this CellularModem to JSON.
         * @function toJSON
         * @memberof sysinfo.CellularModem
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CellularModem.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CellularModem
         * @function getTypeUrl
         * @memberof sysinfo.CellularModem
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CellularModem.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/sysinfo.CellularModem";
        };

        return CellularModem;
    })();

    return sysinfo;
})();

export const arduino_nicla_sense_env = $root.arduino_nicla_sense_env = (() => {

    /**
     * Namespace arduino_nicla_sense_env.
     * @exports arduino_nicla_sense_env
     * @namespace
     */
    const arduino_nicla_sense_env = {};

    /**
     * ArduinoNiclaSenseEnvSignalType enum.
     * @name arduino_nicla_sense_env.ArduinoNiclaSenseEnvSignalType
     * @enum {number}
     * @property {number} ARDUINO_NICLA_SENSE_ENV_SIGNAL_TYPE_UNSPECIFIED=0 ARDUINO_NICLA_SENSE_ENV_SIGNAL_TYPE_UNSPECIFIED value
     * @property {number} ARDUINO_NICLA_SENSE_ENV_CONNECTED=1 ARDUINO_NICLA_SENSE_ENV_CONNECTED value
     * @property {number} ARDUINO_NICLA_SENSE_ENV_DISCONNECTED=2 ARDUINO_NICLA_SENSE_ENV_DISCONNECTED value
     * @property {number} ARDUINO_NICLA_SENSE_ENV_REGISTERS_SNAPSHOT=3 ARDUINO_NICLA_SENSE_ENV_REGISTERS_SNAPSHOT value
     * @property {number} ARDUINO_NICLA_SENSE_ENV_COMMAND=4 ARDUINO_NICLA_SENSE_ENV_COMMAND value
     * @property {number} ARDUINO_NICLA_SENSE_ENV_COMMAND_SUCCESS=5 ARDUINO_NICLA_SENSE_ENV_COMMAND_SUCCESS value
     * @property {number} ARDUINO_NICLA_SENSE_ENV_COMMAND_REJECTED=6 ARDUINO_NICLA_SENSE_ENV_COMMAND_REJECTED value
     * @property {number} ARDUINO_NICLA_SENSE_ENV_COMMAND_FAILED=7 ARDUINO_NICLA_SENSE_ENV_COMMAND_FAILED value
     * @property {number} ARDUINO_NICLA_SENSE_ENV_ERROR=8 ARDUINO_NICLA_SENSE_ENV_ERROR value
     */
    arduino_nicla_sense_env.ArduinoNiclaSenseEnvSignalType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "ARDUINO_NICLA_SENSE_ENV_SIGNAL_TYPE_UNSPECIFIED"] = 0;
        values[valuesById[1] = "ARDUINO_NICLA_SENSE_ENV_CONNECTED"] = 1;
        values[valuesById[2] = "ARDUINO_NICLA_SENSE_ENV_DISCONNECTED"] = 2;
        values[valuesById[3] = "ARDUINO_NICLA_SENSE_ENV_REGISTERS_SNAPSHOT"] = 3;
        values[valuesById[4] = "ARDUINO_NICLA_SENSE_ENV_COMMAND"] = 4;
        values[valuesById[5] = "ARDUINO_NICLA_SENSE_ENV_COMMAND_SUCCESS"] = 5;
        values[valuesById[6] = "ARDUINO_NICLA_SENSE_ENV_COMMAND_REJECTED"] = 6;
        values[valuesById[7] = "ARDUINO_NICLA_SENSE_ENV_COMMAND_FAILED"] = 7;
        values[valuesById[8] = "ARDUINO_NICLA_SENSE_ENV_ERROR"] = 8;
        return values;
    })();

    arduino_nicla_sense_env.ArduinoNiclaSenseEnvDeviceInfo = (function() {

        /**
         * Properties of an ArduinoNiclaSenseEnvDeviceInfo.
         * @memberof arduino_nicla_sense_env
         * @interface IArduinoNiclaSenseEnvDeviceInfo
         * @property {number|null} [softwareRevision] ArduinoNiclaSenseEnvDeviceInfo softwareRevision
         * @property {number|null} [productId] ArduinoNiclaSenseEnvDeviceInfo productId
         * @property {Uint8Array|null} [serialNumber] ArduinoNiclaSenseEnvDeviceInfo serialNumber
         */

        /**
         * Constructs a new ArduinoNiclaSenseEnvDeviceInfo.
         * @memberof arduino_nicla_sense_env
         * @classdesc Represents an ArduinoNiclaSenseEnvDeviceInfo.
         * @implements IArduinoNiclaSenseEnvDeviceInfo
         * @constructor
         * @param {arduino_nicla_sense_env.IArduinoNiclaSenseEnvDeviceInfo=} [properties] Properties to set
         */
        function ArduinoNiclaSenseEnvDeviceInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ArduinoNiclaSenseEnvDeviceInfo softwareRevision.
         * @member {number} softwareRevision
         * @memberof arduino_nicla_sense_env.ArduinoNiclaSenseEnvDeviceInfo
         * @instance
         */
        ArduinoNiclaSenseEnvDeviceInfo.prototype.softwareRevision = 0;

        /**
         * ArduinoNiclaSenseEnvDeviceInfo productId.
         * @member {number} productId
         * @memberof arduino_nicla_sense_env.ArduinoNiclaSenseEnvDeviceInfo
         * @instance
         */
        ArduinoNiclaSenseEnvDeviceInfo.prototype.productId = 0;

        /**
         * ArduinoNiclaSenseEnvDeviceInfo serialNumber.
         * @member {Uint8Array} serialNumber
         * @memberof arduino_nicla_sense_env.ArduinoNiclaSenseEnvDeviceInfo
         * @instance
         */
        ArduinoNiclaSenseEnvDeviceInfo.prototype.serialNumber = $util.newBuffer([]);

        /**
         * Creates a new ArduinoNiclaSenseEnvDeviceInfo instance using the specified properties.
         * @function create
         * @memberof arduino_nicla_sense_env.ArduinoNiclaSenseEnvDeviceInfo
         * @static
         * @param {arduino_nicla_sense_env.IArduinoNiclaSenseEnvDeviceInfo=} [properties] Properties to set
         * @returns {arduino_nicla_sense_env.ArduinoNiclaSenseEnvDeviceInfo} ArduinoNiclaSenseEnvDeviceInfo instance
         */
        ArduinoNiclaSenseEnvDeviceInfo.create = function create(properties) {
            return new ArduinoNiclaSenseEnvDeviceInfo(properties);
        };

        /**
         * Encodes the specified ArduinoNiclaSenseEnvDeviceInfo message. Does not implicitly {@link arduino_nicla_sense_env.ArduinoNiclaSenseEnvDeviceInfo.verify|verify} messages.
         * @function encode
         * @memberof arduino_nicla_sense_env.ArduinoNiclaSenseEnvDeviceInfo
         * @static
         * @param {arduino_nicla_sense_env.IArduinoNiclaSenseEnvDeviceInfo} message ArduinoNiclaSenseEnvDeviceInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ArduinoNiclaSenseEnvDeviceInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.softwareRevision != null && Object.hasOwnProperty.call(message, "softwareRevision"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.softwareRevision);
            if (message.productId != null && Object.hasOwnProperty.call(message, "productId"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.productId);
            if (message.serialNumber != null && Object.hasOwnProperty.call(message, "serialNumber"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.serialNumber);
            return writer;
        };

        /**
         * Encodes the specified ArduinoNiclaSenseEnvDeviceInfo message, length delimited. Does not implicitly {@link arduino_nicla_sense_env.ArduinoNiclaSenseEnvDeviceInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof arduino_nicla_sense_env.ArduinoNiclaSenseEnvDeviceInfo
         * @static
         * @param {arduino_nicla_sense_env.IArduinoNiclaSenseEnvDeviceInfo} message ArduinoNiclaSenseEnvDeviceInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ArduinoNiclaSenseEnvDeviceInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an ArduinoNiclaSenseEnvDeviceInfo message from the specified reader or buffer.
         * @function decode
         * @memberof arduino_nicla_sense_env.ArduinoNiclaSenseEnvDeviceInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {arduino_nicla_sense_env.ArduinoNiclaSenseEnvDeviceInfo} ArduinoNiclaSenseEnvDeviceInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ArduinoNiclaSenseEnvDeviceInfo.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.arduino_nicla_sense_env.ArduinoNiclaSenseEnvDeviceInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.softwareRevision = reader.uint32();
                        break;
                    }
                case 2: {
                        message.productId = reader.uint32();
                        break;
                    }
                case 3: {
                        message.serialNumber = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an ArduinoNiclaSenseEnvDeviceInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof arduino_nicla_sense_env.ArduinoNiclaSenseEnvDeviceInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {arduino_nicla_sense_env.ArduinoNiclaSenseEnvDeviceInfo} ArduinoNiclaSenseEnvDeviceInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ArduinoNiclaSenseEnvDeviceInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an ArduinoNiclaSenseEnvDeviceInfo message.
         * @function verify
         * @memberof arduino_nicla_sense_env.ArduinoNiclaSenseEnvDeviceInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ArduinoNiclaSenseEnvDeviceInfo.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.softwareRevision != null && message.hasOwnProperty("softwareRevision"))
                if (!$util.isInteger(message.softwareRevision))
                    return "softwareRevision: integer expected";
            if (message.productId != null && message.hasOwnProperty("productId"))
                if (!$util.isInteger(message.productId))
                    return "productId: integer expected";
            if (message.serialNumber != null && message.hasOwnProperty("serialNumber"))
                if (!(message.serialNumber && typeof message.serialNumber.length === "number" || $util.isString(message.serialNumber)))
                    return "serialNumber: buffer expected";
            return null;
        };

        /**
         * Creates an ArduinoNiclaSenseEnvDeviceInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof arduino_nicla_sense_env.ArduinoNiclaSenseEnvDeviceInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {arduino_nicla_sense_env.ArduinoNiclaSenseEnvDeviceInfo} ArduinoNiclaSenseEnvDeviceInfo
         */
        ArduinoNiclaSenseEnvDeviceInfo.fromObject = function fromObject(object, long) {
            if (object instanceof $root.arduino_nicla_sense_env.ArduinoNiclaSenseEnvDeviceInfo)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.arduino_nicla_sense_env.ArduinoNiclaSenseEnvDeviceInfo();
            if (object.softwareRevision != null)
                message.softwareRevision = object.softwareRevision >>> 0;
            if (object.productId != null)
                message.productId = object.productId >>> 0;
            if (object.serialNumber != null)
                if (typeof object.serialNumber === "string")
                    $util.base64.decode(object.serialNumber, message.serialNumber = $util.newBuffer($util.base64.length(object.serialNumber)), 0);
                else if (object.serialNumber.length >= 0)
                    message.serialNumber = object.serialNumber;
            return message;
        };

        /**
         * Creates a plain object from an ArduinoNiclaSenseEnvDeviceInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof arduino_nicla_sense_env.ArduinoNiclaSenseEnvDeviceInfo
         * @static
         * @param {arduino_nicla_sense_env.ArduinoNiclaSenseEnvDeviceInfo} message ArduinoNiclaSenseEnvDeviceInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ArduinoNiclaSenseEnvDeviceInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.softwareRevision = 0;
                object.productId = 0;
                if (options.bytes === String)
                    object.serialNumber = "";
                else {
                    object.serialNumber = [];
                    if (options.bytes !== Array)
                        object.serialNumber = $util.newBuffer(object.serialNumber);
                }
            }
            if (message.softwareRevision != null && message.hasOwnProperty("softwareRevision"))
                object.softwareRevision = message.softwareRevision;
            if (message.productId != null && message.hasOwnProperty("productId"))
                object.productId = message.productId;
            if (message.serialNumber != null && message.hasOwnProperty("serialNumber"))
                object.serialNumber = options.bytes === String ? $util.base64.encode(message.serialNumber, 0, message.serialNumber.length) : options.bytes === Array ? Array.prototype.slice.call(message.serialNumber) : message.serialNumber;
            return object;
        };

        /**
         * Converts this ArduinoNiclaSenseEnvDeviceInfo to JSON.
         * @function toJSON
         * @memberof arduino_nicla_sense_env.ArduinoNiclaSenseEnvDeviceInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ArduinoNiclaSenseEnvDeviceInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ArduinoNiclaSenseEnvDeviceInfo
         * @function getTypeUrl
         * @memberof arduino_nicla_sense_env.ArduinoNiclaSenseEnvDeviceInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ArduinoNiclaSenseEnvDeviceInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/arduino_nicla_sense_env.ArduinoNiclaSenseEnvDeviceInfo";
        };

        return ArduinoNiclaSenseEnvDeviceInfo;
    })();

    arduino_nicla_sense_env.ArduinoNiclaSenseEnvDevice = (function() {

        /**
         * Properties of an ArduinoNiclaSenseEnvDevice.
         * @memberof arduino_nicla_sense_env
         * @interface IArduinoNiclaSenseEnvDevice
         * @property {string|null} [id] ArduinoNiclaSenseEnvDevice id
         * @property {number|null} [i2cBus] ArduinoNiclaSenseEnvDevice i2cBus
         * @property {number|null} [i2cAddress] ArduinoNiclaSenseEnvDevice i2cAddress
         * @property {arduino_nicla_sense_env.IArduinoNiclaSenseEnvDeviceInfo|null} [info] ArduinoNiclaSenseEnvDevice info
         */

        /**
         * Constructs a new ArduinoNiclaSenseEnvDevice.
         * @memberof arduino_nicla_sense_env
         * @classdesc Represents an ArduinoNiclaSenseEnvDevice.
         * @implements IArduinoNiclaSenseEnvDevice
         * @constructor
         * @param {arduino_nicla_sense_env.IArduinoNiclaSenseEnvDevice=} [properties] Properties to set
         */
        function ArduinoNiclaSenseEnvDevice(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ArduinoNiclaSenseEnvDevice id.
         * @member {string} id
         * @memberof arduino_nicla_sense_env.ArduinoNiclaSenseEnvDevice
         * @instance
         */
        ArduinoNiclaSenseEnvDevice.prototype.id = "";

        /**
         * ArduinoNiclaSenseEnvDevice i2cBus.
         * @member {number} i2cBus
         * @memberof arduino_nicla_sense_env.ArduinoNiclaSenseEnvDevice
         * @instance
         */
        ArduinoNiclaSenseEnvDevice.prototype.i2cBus = 0;

        /**
         * ArduinoNiclaSenseEnvDevice i2cAddress.
         * @member {number} i2cAddress
         * @memberof arduino_nicla_sense_env.ArduinoNiclaSenseEnvDevice
         * @instance
         */
        ArduinoNiclaSenseEnvDevice.prototype.i2cAddress = 0;

        /**
         * ArduinoNiclaSenseEnvDevice info.
         * @member {arduino_nicla_sense_env.IArduinoNiclaSenseEnvDeviceInfo|null|undefined} info
         * @memberof arduino_nicla_sense_env.ArduinoNiclaSenseEnvDevice
         * @instance
         */
        ArduinoNiclaSenseEnvDevice.prototype.info = null;

        /**
         * Creates a new ArduinoNiclaSenseEnvDevice instance using the specified properties.
         * @function create
         * @memberof arduino_nicla_sense_env.ArduinoNiclaSenseEnvDevice
         * @static
         * @param {arduino_nicla_sense_env.IArduinoNiclaSenseEnvDevice=} [properties] Properties to set
         * @returns {arduino_nicla_sense_env.ArduinoNiclaSenseEnvDevice} ArduinoNiclaSenseEnvDevice instance
         */
        ArduinoNiclaSenseEnvDevice.create = function create(properties) {
            return new ArduinoNiclaSenseEnvDevice(properties);
        };

        /**
         * Encodes the specified ArduinoNiclaSenseEnvDevice message. Does not implicitly {@link arduino_nicla_sense_env.ArduinoNiclaSenseEnvDevice.verify|verify} messages.
         * @function encode
         * @memberof arduino_nicla_sense_env.ArduinoNiclaSenseEnvDevice
         * @static
         * @param {arduino_nicla_sense_env.IArduinoNiclaSenseEnvDevice} message ArduinoNiclaSenseEnvDevice message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ArduinoNiclaSenseEnvDevice.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.id);
            if (message.i2cBus != null && Object.hasOwnProperty.call(message, "i2cBus"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.i2cBus);
            if (message.i2cAddress != null && Object.hasOwnProperty.call(message, "i2cAddress"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.i2cAddress);
            if (message.info != null && Object.hasOwnProperty.call(message, "info"))
                $root.arduino_nicla_sense_env.ArduinoNiclaSenseEnvDeviceInfo.encode(message.info, writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified ArduinoNiclaSenseEnvDevice message, length delimited. Does not implicitly {@link arduino_nicla_sense_env.ArduinoNiclaSenseEnvDevice.verify|verify} messages.
         * @function encodeDelimited
         * @memberof arduino_nicla_sense_env.ArduinoNiclaSenseEnvDevice
         * @static
         * @param {arduino_nicla_sense_env.IArduinoNiclaSenseEnvDevice} message ArduinoNiclaSenseEnvDevice message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ArduinoNiclaSenseEnvDevice.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an ArduinoNiclaSenseEnvDevice message from the specified reader or buffer.
         * @function decode
         * @memberof arduino_nicla_sense_env.ArduinoNiclaSenseEnvDevice
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {arduino_nicla_sense_env.ArduinoNiclaSenseEnvDevice} ArduinoNiclaSenseEnvDevice
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ArduinoNiclaSenseEnvDevice.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.arduino_nicla_sense_env.ArduinoNiclaSenseEnvDevice();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.id = reader.string();
                        break;
                    }
                case 2: {
                        message.i2cBus = reader.uint32();
                        break;
                    }
                case 3: {
                        message.i2cAddress = reader.uint32();
                        break;
                    }
                case 10: {
                        message.info = $root.arduino_nicla_sense_env.ArduinoNiclaSenseEnvDeviceInfo.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an ArduinoNiclaSenseEnvDevice message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof arduino_nicla_sense_env.ArduinoNiclaSenseEnvDevice
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {arduino_nicla_sense_env.ArduinoNiclaSenseEnvDevice} ArduinoNiclaSenseEnvDevice
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ArduinoNiclaSenseEnvDevice.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an ArduinoNiclaSenseEnvDevice message.
         * @function verify
         * @memberof arduino_nicla_sense_env.ArduinoNiclaSenseEnvDevice
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ArduinoNiclaSenseEnvDevice.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isString(message.id))
                    return "id: string expected";
            if (message.i2cBus != null && message.hasOwnProperty("i2cBus"))
                if (!$util.isInteger(message.i2cBus))
                    return "i2cBus: integer expected";
            if (message.i2cAddress != null && message.hasOwnProperty("i2cAddress"))
                if (!$util.isInteger(message.i2cAddress))
                    return "i2cAddress: integer expected";
            if (message.info != null && message.hasOwnProperty("info")) {
                let error = $root.arduino_nicla_sense_env.ArduinoNiclaSenseEnvDeviceInfo.verify(message.info, long + 1);
                if (error)
                    return "info." + error;
            }
            return null;
        };

        /**
         * Creates an ArduinoNiclaSenseEnvDevice message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof arduino_nicla_sense_env.ArduinoNiclaSenseEnvDevice
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {arduino_nicla_sense_env.ArduinoNiclaSenseEnvDevice} ArduinoNiclaSenseEnvDevice
         */
        ArduinoNiclaSenseEnvDevice.fromObject = function fromObject(object, long) {
            if (object instanceof $root.arduino_nicla_sense_env.ArduinoNiclaSenseEnvDevice)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.arduino_nicla_sense_env.ArduinoNiclaSenseEnvDevice();
            if (object.id != null)
                message.id = String(object.id);
            if (object.i2cBus != null)
                message.i2cBus = object.i2cBus >>> 0;
            if (object.i2cAddress != null)
                message.i2cAddress = object.i2cAddress >>> 0;
            if (object.info != null) {
                if (typeof object.info !== "object")
                    throw TypeError(".arduino_nicla_sense_env.ArduinoNiclaSenseEnvDevice.info: object expected");
                message.info = $root.arduino_nicla_sense_env.ArduinoNiclaSenseEnvDeviceInfo.fromObject(object.info, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from an ArduinoNiclaSenseEnvDevice message. Also converts values to other types if specified.
         * @function toObject
         * @memberof arduino_nicla_sense_env.ArduinoNiclaSenseEnvDevice
         * @static
         * @param {arduino_nicla_sense_env.ArduinoNiclaSenseEnvDevice} message ArduinoNiclaSenseEnvDevice
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ArduinoNiclaSenseEnvDevice.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.id = "";
                object.i2cBus = 0;
                object.i2cAddress = 0;
                object.info = null;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.i2cBus != null && message.hasOwnProperty("i2cBus"))
                object.i2cBus = message.i2cBus;
            if (message.i2cAddress != null && message.hasOwnProperty("i2cAddress"))
                object.i2cAddress = message.i2cAddress;
            if (message.info != null && message.hasOwnProperty("info"))
                object.info = $root.arduino_nicla_sense_env.ArduinoNiclaSenseEnvDeviceInfo.toObject(message.info, options);
            return object;
        };

        /**
         * Converts this ArduinoNiclaSenseEnvDevice to JSON.
         * @function toJSON
         * @memberof arduino_nicla_sense_env.ArduinoNiclaSenseEnvDevice
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ArduinoNiclaSenseEnvDevice.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ArduinoNiclaSenseEnvDevice
         * @function getTypeUrl
         * @memberof arduino_nicla_sense_env.ArduinoNiclaSenseEnvDevice
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ArduinoNiclaSenseEnvDevice.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/arduino_nicla_sense_env.ArduinoNiclaSenseEnvDevice";
        };

        return ArduinoNiclaSenseEnvDevice;
    })();

    arduino_nicla_sense_env.WriteRegistersCommand = (function() {

        /**
         * Properties of a WriteRegistersCommand.
         * @memberof arduino_nicla_sense_env
         * @interface IWriteRegistersCommand
         * @property {number|null} [startRegister] WriteRegistersCommand startRegister
         * @property {Uint8Array|null} [data] WriteRegistersCommand data
         */

        /**
         * Constructs a new WriteRegistersCommand.
         * @memberof arduino_nicla_sense_env
         * @classdesc Represents a WriteRegistersCommand.
         * @implements IWriteRegistersCommand
         * @constructor
         * @param {arduino_nicla_sense_env.IWriteRegistersCommand=} [properties] Properties to set
         */
        function WriteRegistersCommand(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * WriteRegistersCommand startRegister.
         * @member {number} startRegister
         * @memberof arduino_nicla_sense_env.WriteRegistersCommand
         * @instance
         */
        WriteRegistersCommand.prototype.startRegister = 0;

        /**
         * WriteRegistersCommand data.
         * @member {Uint8Array} data
         * @memberof arduino_nicla_sense_env.WriteRegistersCommand
         * @instance
         */
        WriteRegistersCommand.prototype.data = $util.newBuffer([]);

        /**
         * Creates a new WriteRegistersCommand instance using the specified properties.
         * @function create
         * @memberof arduino_nicla_sense_env.WriteRegistersCommand
         * @static
         * @param {arduino_nicla_sense_env.IWriteRegistersCommand=} [properties] Properties to set
         * @returns {arduino_nicla_sense_env.WriteRegistersCommand} WriteRegistersCommand instance
         */
        WriteRegistersCommand.create = function create(properties) {
            return new WriteRegistersCommand(properties);
        };

        /**
         * Encodes the specified WriteRegistersCommand message. Does not implicitly {@link arduino_nicla_sense_env.WriteRegistersCommand.verify|verify} messages.
         * @function encode
         * @memberof arduino_nicla_sense_env.WriteRegistersCommand
         * @static
         * @param {arduino_nicla_sense_env.IWriteRegistersCommand} message WriteRegistersCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WriteRegistersCommand.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.startRegister != null && Object.hasOwnProperty.call(message, "startRegister"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.startRegister);
            if (message.data != null && Object.hasOwnProperty.call(message, "data"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.data);
            return writer;
        };

        /**
         * Encodes the specified WriteRegistersCommand message, length delimited. Does not implicitly {@link arduino_nicla_sense_env.WriteRegistersCommand.verify|verify} messages.
         * @function encodeDelimited
         * @memberof arduino_nicla_sense_env.WriteRegistersCommand
         * @static
         * @param {arduino_nicla_sense_env.IWriteRegistersCommand} message WriteRegistersCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WriteRegistersCommand.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a WriteRegistersCommand message from the specified reader or buffer.
         * @function decode
         * @memberof arduino_nicla_sense_env.WriteRegistersCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {arduino_nicla_sense_env.WriteRegistersCommand} WriteRegistersCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WriteRegistersCommand.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.arduino_nicla_sense_env.WriteRegistersCommand();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.startRegister = reader.uint32();
                        break;
                    }
                case 2: {
                        message.data = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a WriteRegistersCommand message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof arduino_nicla_sense_env.WriteRegistersCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {arduino_nicla_sense_env.WriteRegistersCommand} WriteRegistersCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WriteRegistersCommand.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a WriteRegistersCommand message.
         * @function verify
         * @memberof arduino_nicla_sense_env.WriteRegistersCommand
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        WriteRegistersCommand.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.startRegister != null && message.hasOwnProperty("startRegister"))
                if (!$util.isInteger(message.startRegister))
                    return "startRegister: integer expected";
            if (message.data != null && message.hasOwnProperty("data"))
                if (!(message.data && typeof message.data.length === "number" || $util.isString(message.data)))
                    return "data: buffer expected";
            return null;
        };

        /**
         * Creates a WriteRegistersCommand message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof arduino_nicla_sense_env.WriteRegistersCommand
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {arduino_nicla_sense_env.WriteRegistersCommand} WriteRegistersCommand
         */
        WriteRegistersCommand.fromObject = function fromObject(object, long) {
            if (object instanceof $root.arduino_nicla_sense_env.WriteRegistersCommand)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.arduino_nicla_sense_env.WriteRegistersCommand();
            if (object.startRegister != null)
                message.startRegister = object.startRegister >>> 0;
            if (object.data != null)
                if (typeof object.data === "string")
                    $util.base64.decode(object.data, message.data = $util.newBuffer($util.base64.length(object.data)), 0);
                else if (object.data.length >= 0)
                    message.data = object.data;
            return message;
        };

        /**
         * Creates a plain object from a WriteRegistersCommand message. Also converts values to other types if specified.
         * @function toObject
         * @memberof arduino_nicla_sense_env.WriteRegistersCommand
         * @static
         * @param {arduino_nicla_sense_env.WriteRegistersCommand} message WriteRegistersCommand
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        WriteRegistersCommand.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.startRegister = 0;
                if (options.bytes === String)
                    object.data = "";
                else {
                    object.data = [];
                    if (options.bytes !== Array)
                        object.data = $util.newBuffer(object.data);
                }
            }
            if (message.startRegister != null && message.hasOwnProperty("startRegister"))
                object.startRegister = message.startRegister;
            if (message.data != null && message.hasOwnProperty("data"))
                object.data = options.bytes === String ? $util.base64.encode(message.data, 0, message.data.length) : options.bytes === Array ? Array.prototype.slice.call(message.data) : message.data;
            return object;
        };

        /**
         * Converts this WriteRegistersCommand to JSON.
         * @function toJSON
         * @memberof arduino_nicla_sense_env.WriteRegistersCommand
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        WriteRegistersCommand.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for WriteRegistersCommand
         * @function getTypeUrl
         * @memberof arduino_nicla_sense_env.WriteRegistersCommand
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        WriteRegistersCommand.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/arduino_nicla_sense_env.WriteRegistersCommand";
        };

        return WriteRegistersCommand;
    })();

    arduino_nicla_sense_env.Command = (function() {

        /**
         * Properties of a Command.
         * @memberof arduino_nicla_sense_env
         * @interface ICommand
         * @property {string|null} [targetDeviceId] Command targetDeviceId
         * @property {arduino_nicla_sense_env.IWriteRegistersCommand|null} [writeRegisters] Command writeRegisters
         */

        /**
         * Constructs a new Command.
         * @memberof arduino_nicla_sense_env
         * @classdesc Represents a Command.
         * @implements ICommand
         * @constructor
         * @param {arduino_nicla_sense_env.ICommand=} [properties] Properties to set
         */
        function Command(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Command targetDeviceId.
         * @member {string} targetDeviceId
         * @memberof arduino_nicla_sense_env.Command
         * @instance
         */
        Command.prototype.targetDeviceId = "";

        /**
         * Command writeRegisters.
         * @member {arduino_nicla_sense_env.IWriteRegistersCommand|null|undefined} writeRegisters
         * @memberof arduino_nicla_sense_env.Command
         * @instance
         */
        Command.prototype.writeRegisters = null;

        /**
         * Creates a new Command instance using the specified properties.
         * @function create
         * @memberof arduino_nicla_sense_env.Command
         * @static
         * @param {arduino_nicla_sense_env.ICommand=} [properties] Properties to set
         * @returns {arduino_nicla_sense_env.Command} Command instance
         */
        Command.create = function create(properties) {
            return new Command(properties);
        };

        /**
         * Encodes the specified Command message. Does not implicitly {@link arduino_nicla_sense_env.Command.verify|verify} messages.
         * @function encode
         * @memberof arduino_nicla_sense_env.Command
         * @static
         * @param {arduino_nicla_sense_env.ICommand} message Command message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Command.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.targetDeviceId != null && Object.hasOwnProperty.call(message, "targetDeviceId"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.targetDeviceId);
            if (message.writeRegisters != null && Object.hasOwnProperty.call(message, "writeRegisters"))
                $root.arduino_nicla_sense_env.WriteRegistersCommand.encode(message.writeRegisters, writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified Command message, length delimited. Does not implicitly {@link arduino_nicla_sense_env.Command.verify|verify} messages.
         * @function encodeDelimited
         * @memberof arduino_nicla_sense_env.Command
         * @static
         * @param {arduino_nicla_sense_env.ICommand} message Command message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Command.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Command message from the specified reader or buffer.
         * @function decode
         * @memberof arduino_nicla_sense_env.Command
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {arduino_nicla_sense_env.Command} Command
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Command.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.arduino_nicla_sense_env.Command();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.targetDeviceId = reader.string();
                        break;
                    }
                case 10: {
                        message.writeRegisters = $root.arduino_nicla_sense_env.WriteRegistersCommand.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Command message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof arduino_nicla_sense_env.Command
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {arduino_nicla_sense_env.Command} Command
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Command.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Command message.
         * @function verify
         * @memberof arduino_nicla_sense_env.Command
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Command.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.targetDeviceId != null && message.hasOwnProperty("targetDeviceId"))
                if (!$util.isString(message.targetDeviceId))
                    return "targetDeviceId: string expected";
            if (message.writeRegisters != null && message.hasOwnProperty("writeRegisters")) {
                let error = $root.arduino_nicla_sense_env.WriteRegistersCommand.verify(message.writeRegisters, long + 1);
                if (error)
                    return "writeRegisters." + error;
            }
            return null;
        };

        /**
         * Creates a Command message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof arduino_nicla_sense_env.Command
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {arduino_nicla_sense_env.Command} Command
         */
        Command.fromObject = function fromObject(object, long) {
            if (object instanceof $root.arduino_nicla_sense_env.Command)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.arduino_nicla_sense_env.Command();
            if (object.targetDeviceId != null)
                message.targetDeviceId = String(object.targetDeviceId);
            if (object.writeRegisters != null) {
                if (typeof object.writeRegisters !== "object")
                    throw TypeError(".arduino_nicla_sense_env.Command.writeRegisters: object expected");
                message.writeRegisters = $root.arduino_nicla_sense_env.WriteRegistersCommand.fromObject(object.writeRegisters, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a Command message. Also converts values to other types if specified.
         * @function toObject
         * @memberof arduino_nicla_sense_env.Command
         * @static
         * @param {arduino_nicla_sense_env.Command} message Command
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Command.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.targetDeviceId = "";
                object.writeRegisters = null;
            }
            if (message.targetDeviceId != null && message.hasOwnProperty("targetDeviceId"))
                object.targetDeviceId = message.targetDeviceId;
            if (message.writeRegisters != null && message.hasOwnProperty("writeRegisters"))
                object.writeRegisters = $root.arduino_nicla_sense_env.WriteRegistersCommand.toObject(message.writeRegisters, options);
            return object;
        };

        /**
         * Converts this Command to JSON.
         * @function toJSON
         * @memberof arduino_nicla_sense_env.Command
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Command.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Command
         * @function getTypeUrl
         * @memberof arduino_nicla_sense_env.Command
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Command.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/arduino_nicla_sense_env.Command";
        };

        return Command;
    })();

    arduino_nicla_sense_env.TxEnvelope = (function() {

        /**
         * Properties of a TxEnvelope.
         * @memberof arduino_nicla_sense_env
         * @interface ITxEnvelope
         * @property {Long|null} [monotonicStampNs] TxEnvelope monotonicStampNs
         * @property {Long|null} [localStampNs] TxEnvelope localStampNs
         * @property {Long|null} [appStartId] TxEnvelope appStartId
         * @property {Uint8Array|null} [commandId] TxEnvelope commandId
         * @property {string|null} [targetDeviceId] TxEnvelope targetDeviceId
         * @property {arduino_nicla_sense_env.ICommand|null} [command] TxEnvelope command
         */

        /**
         * Constructs a new TxEnvelope.
         * @memberof arduino_nicla_sense_env
         * @classdesc Represents a TxEnvelope.
         * @implements ITxEnvelope
         * @constructor
         * @param {arduino_nicla_sense_env.ITxEnvelope=} [properties] Properties to set
         */
        function TxEnvelope(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * TxEnvelope monotonicStampNs.
         * @member {Long} monotonicStampNs
         * @memberof arduino_nicla_sense_env.TxEnvelope
         * @instance
         */
        TxEnvelope.prototype.monotonicStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * TxEnvelope localStampNs.
         * @member {Long} localStampNs
         * @memberof arduino_nicla_sense_env.TxEnvelope
         * @instance
         */
        TxEnvelope.prototype.localStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * TxEnvelope appStartId.
         * @member {Long} appStartId
         * @memberof arduino_nicla_sense_env.TxEnvelope
         * @instance
         */
        TxEnvelope.prototype.appStartId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * TxEnvelope commandId.
         * @member {Uint8Array} commandId
         * @memberof arduino_nicla_sense_env.TxEnvelope
         * @instance
         */
        TxEnvelope.prototype.commandId = $util.newBuffer([]);

        /**
         * TxEnvelope targetDeviceId.
         * @member {string} targetDeviceId
         * @memberof arduino_nicla_sense_env.TxEnvelope
         * @instance
         */
        TxEnvelope.prototype.targetDeviceId = "";

        /**
         * TxEnvelope command.
         * @member {arduino_nicla_sense_env.ICommand|null|undefined} command
         * @memberof arduino_nicla_sense_env.TxEnvelope
         * @instance
         */
        TxEnvelope.prototype.command = null;

        /**
         * Creates a new TxEnvelope instance using the specified properties.
         * @function create
         * @memberof arduino_nicla_sense_env.TxEnvelope
         * @static
         * @param {arduino_nicla_sense_env.ITxEnvelope=} [properties] Properties to set
         * @returns {arduino_nicla_sense_env.TxEnvelope} TxEnvelope instance
         */
        TxEnvelope.create = function create(properties) {
            return new TxEnvelope(properties);
        };

        /**
         * Encodes the specified TxEnvelope message. Does not implicitly {@link arduino_nicla_sense_env.TxEnvelope.verify|verify} messages.
         * @function encode
         * @memberof arduino_nicla_sense_env.TxEnvelope
         * @static
         * @param {arduino_nicla_sense_env.ITxEnvelope} message TxEnvelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TxEnvelope.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.monotonicStampNs != null && Object.hasOwnProperty.call(message, "monotonicStampNs"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.monotonicStampNs);
            if (message.localStampNs != null && Object.hasOwnProperty.call(message, "localStampNs"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.localStampNs);
            if (message.appStartId != null && Object.hasOwnProperty.call(message, "appStartId"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.appStartId);
            if (message.commandId != null && Object.hasOwnProperty.call(message, "commandId"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.commandId);
            if (message.targetDeviceId != null && Object.hasOwnProperty.call(message, "targetDeviceId"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.targetDeviceId);
            if (message.command != null && Object.hasOwnProperty.call(message, "command"))
                $root.arduino_nicla_sense_env.Command.encode(message.command, writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified TxEnvelope message, length delimited. Does not implicitly {@link arduino_nicla_sense_env.TxEnvelope.verify|verify} messages.
         * @function encodeDelimited
         * @memberof arduino_nicla_sense_env.TxEnvelope
         * @static
         * @param {arduino_nicla_sense_env.ITxEnvelope} message TxEnvelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TxEnvelope.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a TxEnvelope message from the specified reader or buffer.
         * @function decode
         * @memberof arduino_nicla_sense_env.TxEnvelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {arduino_nicla_sense_env.TxEnvelope} TxEnvelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TxEnvelope.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.arduino_nicla_sense_env.TxEnvelope();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.monotonicStampNs = reader.uint64();
                        break;
                    }
                case 2: {
                        message.localStampNs = reader.uint64();
                        break;
                    }
                case 3: {
                        message.appStartId = reader.uint64();
                        break;
                    }
                case 4: {
                        message.commandId = reader.bytes();
                        break;
                    }
                case 5: {
                        message.targetDeviceId = reader.string();
                        break;
                    }
                case 10: {
                        message.command = $root.arduino_nicla_sense_env.Command.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a TxEnvelope message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof arduino_nicla_sense_env.TxEnvelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {arduino_nicla_sense_env.TxEnvelope} TxEnvelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TxEnvelope.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TxEnvelope message.
         * @function verify
         * @memberof arduino_nicla_sense_env.TxEnvelope
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TxEnvelope.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (!$util.isInteger(message.monotonicStampNs) && !(message.monotonicStampNs && $util.isInteger(message.monotonicStampNs.low) && $util.isInteger(message.monotonicStampNs.high)))
                    return "monotonicStampNs: integer|Long expected";
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (!$util.isInteger(message.localStampNs) && !(message.localStampNs && $util.isInteger(message.localStampNs.low) && $util.isInteger(message.localStampNs.high)))
                    return "localStampNs: integer|Long expected";
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (!$util.isInteger(message.appStartId) && !(message.appStartId && $util.isInteger(message.appStartId.low) && $util.isInteger(message.appStartId.high)))
                    return "appStartId: integer|Long expected";
            if (message.commandId != null && message.hasOwnProperty("commandId"))
                if (!(message.commandId && typeof message.commandId.length === "number" || $util.isString(message.commandId)))
                    return "commandId: buffer expected";
            if (message.targetDeviceId != null && message.hasOwnProperty("targetDeviceId"))
                if (!$util.isString(message.targetDeviceId))
                    return "targetDeviceId: string expected";
            if (message.command != null && message.hasOwnProperty("command")) {
                let error = $root.arduino_nicla_sense_env.Command.verify(message.command, long + 1);
                if (error)
                    return "command." + error;
            }
            return null;
        };

        /**
         * Creates a TxEnvelope message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof arduino_nicla_sense_env.TxEnvelope
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {arduino_nicla_sense_env.TxEnvelope} TxEnvelope
         */
        TxEnvelope.fromObject = function fromObject(object, long) {
            if (object instanceof $root.arduino_nicla_sense_env.TxEnvelope)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.arduino_nicla_sense_env.TxEnvelope();
            if (object.monotonicStampNs != null)
                if ($util.Long)
                    (message.monotonicStampNs = $util.Long.fromValue(object.monotonicStampNs)).unsigned = true;
                else if (typeof object.monotonicStampNs === "string")
                    message.monotonicStampNs = parseInt(object.monotonicStampNs, 10);
                else if (typeof object.monotonicStampNs === "number")
                    message.monotonicStampNs = object.monotonicStampNs;
                else if (typeof object.monotonicStampNs === "object")
                    message.monotonicStampNs = new $util.LongBits(object.monotonicStampNs.low >>> 0, object.monotonicStampNs.high >>> 0).toNumber(true);
            if (object.localStampNs != null)
                if ($util.Long)
                    (message.localStampNs = $util.Long.fromValue(object.localStampNs)).unsigned = true;
                else if (typeof object.localStampNs === "string")
                    message.localStampNs = parseInt(object.localStampNs, 10);
                else if (typeof object.localStampNs === "number")
                    message.localStampNs = object.localStampNs;
                else if (typeof object.localStampNs === "object")
                    message.localStampNs = new $util.LongBits(object.localStampNs.low >>> 0, object.localStampNs.high >>> 0).toNumber(true);
            if (object.appStartId != null)
                if ($util.Long)
                    (message.appStartId = $util.Long.fromValue(object.appStartId)).unsigned = true;
                else if (typeof object.appStartId === "string")
                    message.appStartId = parseInt(object.appStartId, 10);
                else if (typeof object.appStartId === "number")
                    message.appStartId = object.appStartId;
                else if (typeof object.appStartId === "object")
                    message.appStartId = new $util.LongBits(object.appStartId.low >>> 0, object.appStartId.high >>> 0).toNumber(true);
            if (object.commandId != null)
                if (typeof object.commandId === "string")
                    $util.base64.decode(object.commandId, message.commandId = $util.newBuffer($util.base64.length(object.commandId)), 0);
                else if (object.commandId.length >= 0)
                    message.commandId = object.commandId;
            if (object.targetDeviceId != null)
                message.targetDeviceId = String(object.targetDeviceId);
            if (object.command != null) {
                if (typeof object.command !== "object")
                    throw TypeError(".arduino_nicla_sense_env.TxEnvelope.command: object expected");
                message.command = $root.arduino_nicla_sense_env.Command.fromObject(object.command, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a TxEnvelope message. Also converts values to other types if specified.
         * @function toObject
         * @memberof arduino_nicla_sense_env.TxEnvelope
         * @static
         * @param {arduino_nicla_sense_env.TxEnvelope} message TxEnvelope
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TxEnvelope.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.monotonicStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.monotonicStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.localStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.localStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.appStartId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.appStartId = options.longs === String ? "0" : 0;
                if (options.bytes === String)
                    object.commandId = "";
                else {
                    object.commandId = [];
                    if (options.bytes !== Array)
                        object.commandId = $util.newBuffer(object.commandId);
                }
                object.targetDeviceId = "";
                object.command = null;
            }
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (typeof message.monotonicStampNs === "number")
                    object.monotonicStampNs = options.longs === String ? String(message.monotonicStampNs) : message.monotonicStampNs;
                else
                    object.monotonicStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.monotonicStampNs) : options.longs === Number ? new $util.LongBits(message.monotonicStampNs.low >>> 0, message.monotonicStampNs.high >>> 0).toNumber(true) : message.monotonicStampNs;
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (typeof message.localStampNs === "number")
                    object.localStampNs = options.longs === String ? String(message.localStampNs) : message.localStampNs;
                else
                    object.localStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.localStampNs) : options.longs === Number ? new $util.LongBits(message.localStampNs.low >>> 0, message.localStampNs.high >>> 0).toNumber(true) : message.localStampNs;
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (typeof message.appStartId === "number")
                    object.appStartId = options.longs === String ? String(message.appStartId) : message.appStartId;
                else
                    object.appStartId = options.longs === String ? $util.Long.prototype.toString.call(message.appStartId) : options.longs === Number ? new $util.LongBits(message.appStartId.low >>> 0, message.appStartId.high >>> 0).toNumber(true) : message.appStartId;
            if (message.commandId != null && message.hasOwnProperty("commandId"))
                object.commandId = options.bytes === String ? $util.base64.encode(message.commandId, 0, message.commandId.length) : options.bytes === Array ? Array.prototype.slice.call(message.commandId) : message.commandId;
            if (message.targetDeviceId != null && message.hasOwnProperty("targetDeviceId"))
                object.targetDeviceId = message.targetDeviceId;
            if (message.command != null && message.hasOwnProperty("command"))
                object.command = $root.arduino_nicla_sense_env.Command.toObject(message.command, options);
            return object;
        };

        /**
         * Converts this TxEnvelope to JSON.
         * @function toJSON
         * @memberof arduino_nicla_sense_env.TxEnvelope
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TxEnvelope.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for TxEnvelope
         * @function getTypeUrl
         * @memberof arduino_nicla_sense_env.TxEnvelope
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        TxEnvelope.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/arduino_nicla_sense_env.TxEnvelope";
        };

        return TxEnvelope;
    })();

    arduino_nicla_sense_env.RxEnvelope = (function() {

        /**
         * Properties of a RxEnvelope.
         * @memberof arduino_nicla_sense_env
         * @interface IRxEnvelope
         * @property {Long|null} [monotonicStampNs] RxEnvelope monotonicStampNs
         * @property {Long|null} [localStampNs] RxEnvelope localStampNs
         * @property {Long|null} [appStartId] RxEnvelope appStartId
         * @property {arduino_nicla_sense_env.ArduinoNiclaSenseEnvSignalType|null} [signalType] RxEnvelope signalType
         * @property {arduino_nicla_sense_env.IArduinoNiclaSenseEnvDevice|null} [device] RxEnvelope device
         * @property {Uint8Array|null} [data] RxEnvelope data
         * @property {arduino_nicla_sense_env.ITxEnvelope|null} [command] RxEnvelope command
         * @property {string|null} [error] RxEnvelope error
         */

        /**
         * Constructs a new RxEnvelope.
         * @memberof arduino_nicla_sense_env
         * @classdesc Represents a RxEnvelope.
         * @implements IRxEnvelope
         * @constructor
         * @param {arduino_nicla_sense_env.IRxEnvelope=} [properties] Properties to set
         */
        function RxEnvelope(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RxEnvelope monotonicStampNs.
         * @member {Long} monotonicStampNs
         * @memberof arduino_nicla_sense_env.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.monotonicStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * RxEnvelope localStampNs.
         * @member {Long} localStampNs
         * @memberof arduino_nicla_sense_env.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.localStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * RxEnvelope appStartId.
         * @member {Long} appStartId
         * @memberof arduino_nicla_sense_env.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.appStartId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * RxEnvelope signalType.
         * @member {arduino_nicla_sense_env.ArduinoNiclaSenseEnvSignalType} signalType
         * @memberof arduino_nicla_sense_env.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.signalType = 0;

        /**
         * RxEnvelope device.
         * @member {arduino_nicla_sense_env.IArduinoNiclaSenseEnvDevice|null|undefined} device
         * @memberof arduino_nicla_sense_env.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.device = null;

        /**
         * RxEnvelope data.
         * @member {Uint8Array} data
         * @memberof arduino_nicla_sense_env.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.data = $util.newBuffer([]);

        /**
         * RxEnvelope command.
         * @member {arduino_nicla_sense_env.ITxEnvelope|null|undefined} command
         * @memberof arduino_nicla_sense_env.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.command = null;

        /**
         * RxEnvelope error.
         * @member {string} error
         * @memberof arduino_nicla_sense_env.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.error = "";

        /**
         * Creates a new RxEnvelope instance using the specified properties.
         * @function create
         * @memberof arduino_nicla_sense_env.RxEnvelope
         * @static
         * @param {arduino_nicla_sense_env.IRxEnvelope=} [properties] Properties to set
         * @returns {arduino_nicla_sense_env.RxEnvelope} RxEnvelope instance
         */
        RxEnvelope.create = function create(properties) {
            return new RxEnvelope(properties);
        };

        /**
         * Encodes the specified RxEnvelope message. Does not implicitly {@link arduino_nicla_sense_env.RxEnvelope.verify|verify} messages.
         * @function encode
         * @memberof arduino_nicla_sense_env.RxEnvelope
         * @static
         * @param {arduino_nicla_sense_env.IRxEnvelope} message RxEnvelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RxEnvelope.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.monotonicStampNs != null && Object.hasOwnProperty.call(message, "monotonicStampNs"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.monotonicStampNs);
            if (message.localStampNs != null && Object.hasOwnProperty.call(message, "localStampNs"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.localStampNs);
            if (message.appStartId != null && Object.hasOwnProperty.call(message, "appStartId"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.appStartId);
            if (message.signalType != null && Object.hasOwnProperty.call(message, "signalType"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.signalType);
            if (message.device != null && Object.hasOwnProperty.call(message, "device"))
                $root.arduino_nicla_sense_env.ArduinoNiclaSenseEnvDevice.encode(message.device, writer.uint32(/* id 11, wireType 2 =*/90).fork()).ldelim();
            if (message.data != null && Object.hasOwnProperty.call(message, "data"))
                writer.uint32(/* id 20, wireType 2 =*/162).bytes(message.data);
            if (message.command != null && Object.hasOwnProperty.call(message, "command"))
                $root.arduino_nicla_sense_env.TxEnvelope.encode(message.command, writer.uint32(/* id 30, wireType 2 =*/242).fork()).ldelim();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                writer.uint32(/* id 50, wireType 2 =*/402).string(message.error);
            return writer;
        };

        /**
         * Encodes the specified RxEnvelope message, length delimited. Does not implicitly {@link arduino_nicla_sense_env.RxEnvelope.verify|verify} messages.
         * @function encodeDelimited
         * @memberof arduino_nicla_sense_env.RxEnvelope
         * @static
         * @param {arduino_nicla_sense_env.IRxEnvelope} message RxEnvelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RxEnvelope.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RxEnvelope message from the specified reader or buffer.
         * @function decode
         * @memberof arduino_nicla_sense_env.RxEnvelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {arduino_nicla_sense_env.RxEnvelope} RxEnvelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RxEnvelope.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.arduino_nicla_sense_env.RxEnvelope();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.monotonicStampNs = reader.uint64();
                        break;
                    }
                case 2: {
                        message.localStampNs = reader.uint64();
                        break;
                    }
                case 3: {
                        message.appStartId = reader.uint64();
                        break;
                    }
                case 10: {
                        message.signalType = reader.int32();
                        break;
                    }
                case 11: {
                        message.device = $root.arduino_nicla_sense_env.ArduinoNiclaSenseEnvDevice.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 20: {
                        message.data = reader.bytes();
                        break;
                    }
                case 30: {
                        message.command = $root.arduino_nicla_sense_env.TxEnvelope.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 50: {
                        message.error = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RxEnvelope message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof arduino_nicla_sense_env.RxEnvelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {arduino_nicla_sense_env.RxEnvelope} RxEnvelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RxEnvelope.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RxEnvelope message.
         * @function verify
         * @memberof arduino_nicla_sense_env.RxEnvelope
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RxEnvelope.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (!$util.isInteger(message.monotonicStampNs) && !(message.monotonicStampNs && $util.isInteger(message.monotonicStampNs.low) && $util.isInteger(message.monotonicStampNs.high)))
                    return "monotonicStampNs: integer|Long expected";
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (!$util.isInteger(message.localStampNs) && !(message.localStampNs && $util.isInteger(message.localStampNs.low) && $util.isInteger(message.localStampNs.high)))
                    return "localStampNs: integer|Long expected";
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (!$util.isInteger(message.appStartId) && !(message.appStartId && $util.isInteger(message.appStartId.low) && $util.isInteger(message.appStartId.high)))
                    return "appStartId: integer|Long expected";
            if (message.signalType != null && message.hasOwnProperty("signalType"))
                switch (message.signalType) {
                default:
                    return "signalType: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                case 5:
                case 6:
                case 7:
                case 8:
                    break;
                }
            if (message.device != null && message.hasOwnProperty("device")) {
                let error = $root.arduino_nicla_sense_env.ArduinoNiclaSenseEnvDevice.verify(message.device, long + 1);
                if (error)
                    return "device." + error;
            }
            if (message.data != null && message.hasOwnProperty("data"))
                if (!(message.data && typeof message.data.length === "number" || $util.isString(message.data)))
                    return "data: buffer expected";
            if (message.command != null && message.hasOwnProperty("command")) {
                let error = $root.arduino_nicla_sense_env.TxEnvelope.verify(message.command, long + 1);
                if (error)
                    return "command." + error;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                if (!$util.isString(message.error))
                    return "error: string expected";
            return null;
        };

        /**
         * Creates a RxEnvelope message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof arduino_nicla_sense_env.RxEnvelope
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {arduino_nicla_sense_env.RxEnvelope} RxEnvelope
         */
        RxEnvelope.fromObject = function fromObject(object, long) {
            if (object instanceof $root.arduino_nicla_sense_env.RxEnvelope)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.arduino_nicla_sense_env.RxEnvelope();
            if (object.monotonicStampNs != null)
                if ($util.Long)
                    (message.monotonicStampNs = $util.Long.fromValue(object.monotonicStampNs)).unsigned = true;
                else if (typeof object.monotonicStampNs === "string")
                    message.monotonicStampNs = parseInt(object.monotonicStampNs, 10);
                else if (typeof object.monotonicStampNs === "number")
                    message.monotonicStampNs = object.monotonicStampNs;
                else if (typeof object.monotonicStampNs === "object")
                    message.monotonicStampNs = new $util.LongBits(object.monotonicStampNs.low >>> 0, object.monotonicStampNs.high >>> 0).toNumber(true);
            if (object.localStampNs != null)
                if ($util.Long)
                    (message.localStampNs = $util.Long.fromValue(object.localStampNs)).unsigned = true;
                else if (typeof object.localStampNs === "string")
                    message.localStampNs = parseInt(object.localStampNs, 10);
                else if (typeof object.localStampNs === "number")
                    message.localStampNs = object.localStampNs;
                else if (typeof object.localStampNs === "object")
                    message.localStampNs = new $util.LongBits(object.localStampNs.low >>> 0, object.localStampNs.high >>> 0).toNumber(true);
            if (object.appStartId != null)
                if ($util.Long)
                    (message.appStartId = $util.Long.fromValue(object.appStartId)).unsigned = true;
                else if (typeof object.appStartId === "string")
                    message.appStartId = parseInt(object.appStartId, 10);
                else if (typeof object.appStartId === "number")
                    message.appStartId = object.appStartId;
                else if (typeof object.appStartId === "object")
                    message.appStartId = new $util.LongBits(object.appStartId.low >>> 0, object.appStartId.high >>> 0).toNumber(true);
            switch (object.signalType) {
            default:
                if (typeof object.signalType === "number") {
                    message.signalType = object.signalType;
                    break;
                }
                break;
            case "ARDUINO_NICLA_SENSE_ENV_SIGNAL_TYPE_UNSPECIFIED":
            case 0:
                message.signalType = 0;
                break;
            case "ARDUINO_NICLA_SENSE_ENV_CONNECTED":
            case 1:
                message.signalType = 1;
                break;
            case "ARDUINO_NICLA_SENSE_ENV_DISCONNECTED":
            case 2:
                message.signalType = 2;
                break;
            case "ARDUINO_NICLA_SENSE_ENV_REGISTERS_SNAPSHOT":
            case 3:
                message.signalType = 3;
                break;
            case "ARDUINO_NICLA_SENSE_ENV_COMMAND":
            case 4:
                message.signalType = 4;
                break;
            case "ARDUINO_NICLA_SENSE_ENV_COMMAND_SUCCESS":
            case 5:
                message.signalType = 5;
                break;
            case "ARDUINO_NICLA_SENSE_ENV_COMMAND_REJECTED":
            case 6:
                message.signalType = 6;
                break;
            case "ARDUINO_NICLA_SENSE_ENV_COMMAND_FAILED":
            case 7:
                message.signalType = 7;
                break;
            case "ARDUINO_NICLA_SENSE_ENV_ERROR":
            case 8:
                message.signalType = 8;
                break;
            }
            if (object.device != null) {
                if (typeof object.device !== "object")
                    throw TypeError(".arduino_nicla_sense_env.RxEnvelope.device: object expected");
                message.device = $root.arduino_nicla_sense_env.ArduinoNiclaSenseEnvDevice.fromObject(object.device, long + 1);
            }
            if (object.data != null)
                if (typeof object.data === "string")
                    $util.base64.decode(object.data, message.data = $util.newBuffer($util.base64.length(object.data)), 0);
                else if (object.data.length >= 0)
                    message.data = object.data;
            if (object.command != null) {
                if (typeof object.command !== "object")
                    throw TypeError(".arduino_nicla_sense_env.RxEnvelope.command: object expected");
                message.command = $root.arduino_nicla_sense_env.TxEnvelope.fromObject(object.command, long + 1);
            }
            if (object.error != null)
                message.error = String(object.error);
            return message;
        };

        /**
         * Creates a plain object from a RxEnvelope message. Also converts values to other types if specified.
         * @function toObject
         * @memberof arduino_nicla_sense_env.RxEnvelope
         * @static
         * @param {arduino_nicla_sense_env.RxEnvelope} message RxEnvelope
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RxEnvelope.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.monotonicStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.monotonicStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.localStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.localStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.appStartId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.appStartId = options.longs === String ? "0" : 0;
                object.signalType = options.enums === String ? "ARDUINO_NICLA_SENSE_ENV_SIGNAL_TYPE_UNSPECIFIED" : 0;
                object.device = null;
                if (options.bytes === String)
                    object.data = "";
                else {
                    object.data = [];
                    if (options.bytes !== Array)
                        object.data = $util.newBuffer(object.data);
                }
                object.command = null;
                object.error = "";
            }
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (typeof message.monotonicStampNs === "number")
                    object.monotonicStampNs = options.longs === String ? String(message.monotonicStampNs) : message.monotonicStampNs;
                else
                    object.monotonicStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.monotonicStampNs) : options.longs === Number ? new $util.LongBits(message.monotonicStampNs.low >>> 0, message.monotonicStampNs.high >>> 0).toNumber(true) : message.monotonicStampNs;
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (typeof message.localStampNs === "number")
                    object.localStampNs = options.longs === String ? String(message.localStampNs) : message.localStampNs;
                else
                    object.localStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.localStampNs) : options.longs === Number ? new $util.LongBits(message.localStampNs.low >>> 0, message.localStampNs.high >>> 0).toNumber(true) : message.localStampNs;
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (typeof message.appStartId === "number")
                    object.appStartId = options.longs === String ? String(message.appStartId) : message.appStartId;
                else
                    object.appStartId = options.longs === String ? $util.Long.prototype.toString.call(message.appStartId) : options.longs === Number ? new $util.LongBits(message.appStartId.low >>> 0, message.appStartId.high >>> 0).toNumber(true) : message.appStartId;
            if (message.signalType != null && message.hasOwnProperty("signalType"))
                object.signalType = options.enums === String ? $root.arduino_nicla_sense_env.ArduinoNiclaSenseEnvSignalType[message.signalType] === undefined ? message.signalType : $root.arduino_nicla_sense_env.ArduinoNiclaSenseEnvSignalType[message.signalType] : message.signalType;
            if (message.device != null && message.hasOwnProperty("device"))
                object.device = $root.arduino_nicla_sense_env.ArduinoNiclaSenseEnvDevice.toObject(message.device, options);
            if (message.data != null && message.hasOwnProperty("data"))
                object.data = options.bytes === String ? $util.base64.encode(message.data, 0, message.data.length) : options.bytes === Array ? Array.prototype.slice.call(message.data) : message.data;
            if (message.command != null && message.hasOwnProperty("command"))
                object.command = $root.arduino_nicla_sense_env.TxEnvelope.toObject(message.command, options);
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = message.error;
            return object;
        };

        /**
         * Converts this RxEnvelope to JSON.
         * @function toJSON
         * @memberof arduino_nicla_sense_env.RxEnvelope
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RxEnvelope.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RxEnvelope
         * @function getTypeUrl
         * @memberof arduino_nicla_sense_env.RxEnvelope
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RxEnvelope.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/arduino_nicla_sense_env.RxEnvelope";
        };

        return RxEnvelope;
    })();

    return arduino_nicla_sense_env;
})();

export const ina226 = $root.ina226 = (() => {

    /**
     * Namespace ina226.
     * @exports ina226
     * @namespace
     */
    const ina226 = {};

    /**
     * Ina226SignalType enum.
     * @name ina226.Ina226SignalType
     * @enum {number}
     * @property {number} INA226_SIGNAL_TYPE_UNSPECIFIED=0 INA226_SIGNAL_TYPE_UNSPECIFIED value
     * @property {number} INA226_CONNECTED=1 INA226_CONNECTED value
     * @property {number} INA226_DISCONNECTED=2 INA226_DISCONNECTED value
     * @property {number} INA226_REGISTERS_SNAPSHOT=3 INA226_REGISTERS_SNAPSHOT value
     * @property {number} INA226_ERROR=4 INA226_ERROR value
     */
    ina226.Ina226SignalType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "INA226_SIGNAL_TYPE_UNSPECIFIED"] = 0;
        values[valuesById[1] = "INA226_CONNECTED"] = 1;
        values[valuesById[2] = "INA226_DISCONNECTED"] = 2;
        values[valuesById[3] = "INA226_REGISTERS_SNAPSHOT"] = 3;
        values[valuesById[4] = "INA226_ERROR"] = 4;
        return values;
    })();

    ina226.Ina226DeviceInfo = (function() {

        /**
         * Properties of an Ina226DeviceInfo.
         * @memberof ina226
         * @interface IIna226DeviceInfo
         * @property {number|null} [manufacturerId] Ina226DeviceInfo manufacturerId
         * @property {number|null} [dieId] Ina226DeviceInfo dieId
         * @property {number|null} [revisionId] Ina226DeviceInfo revisionId
         * @property {number|null} [shuntResistanceOhms] Ina226DeviceInfo shuntResistanceOhms
         */

        /**
         * Constructs a new Ina226DeviceInfo.
         * @memberof ina226
         * @classdesc Represents an Ina226DeviceInfo.
         * @implements IIna226DeviceInfo
         * @constructor
         * @param {ina226.IIna226DeviceInfo=} [properties] Properties to set
         */
        function Ina226DeviceInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Ina226DeviceInfo manufacturerId.
         * @member {number} manufacturerId
         * @memberof ina226.Ina226DeviceInfo
         * @instance
         */
        Ina226DeviceInfo.prototype.manufacturerId = 0;

        /**
         * Ina226DeviceInfo dieId.
         * @member {number} dieId
         * @memberof ina226.Ina226DeviceInfo
         * @instance
         */
        Ina226DeviceInfo.prototype.dieId = 0;

        /**
         * Ina226DeviceInfo revisionId.
         * @member {number} revisionId
         * @memberof ina226.Ina226DeviceInfo
         * @instance
         */
        Ina226DeviceInfo.prototype.revisionId = 0;

        /**
         * Ina226DeviceInfo shuntResistanceOhms.
         * @member {number} shuntResistanceOhms
         * @memberof ina226.Ina226DeviceInfo
         * @instance
         */
        Ina226DeviceInfo.prototype.shuntResistanceOhms = 0;

        /**
         * Creates a new Ina226DeviceInfo instance using the specified properties.
         * @function create
         * @memberof ina226.Ina226DeviceInfo
         * @static
         * @param {ina226.IIna226DeviceInfo=} [properties] Properties to set
         * @returns {ina226.Ina226DeviceInfo} Ina226DeviceInfo instance
         */
        Ina226DeviceInfo.create = function create(properties) {
            return new Ina226DeviceInfo(properties);
        };

        /**
         * Encodes the specified Ina226DeviceInfo message. Does not implicitly {@link ina226.Ina226DeviceInfo.verify|verify} messages.
         * @function encode
         * @memberof ina226.Ina226DeviceInfo
         * @static
         * @param {ina226.IIna226DeviceInfo} message Ina226DeviceInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Ina226DeviceInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.manufacturerId != null && Object.hasOwnProperty.call(message, "manufacturerId"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.manufacturerId);
            if (message.dieId != null && Object.hasOwnProperty.call(message, "dieId"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.dieId);
            if (message.revisionId != null && Object.hasOwnProperty.call(message, "revisionId"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.revisionId);
            if (message.shuntResistanceOhms != null && Object.hasOwnProperty.call(message, "shuntResistanceOhms"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.shuntResistanceOhms);
            return writer;
        };

        /**
         * Encodes the specified Ina226DeviceInfo message, length delimited. Does not implicitly {@link ina226.Ina226DeviceInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof ina226.Ina226DeviceInfo
         * @static
         * @param {ina226.IIna226DeviceInfo} message Ina226DeviceInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Ina226DeviceInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an Ina226DeviceInfo message from the specified reader or buffer.
         * @function decode
         * @memberof ina226.Ina226DeviceInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {ina226.Ina226DeviceInfo} Ina226DeviceInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Ina226DeviceInfo.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.ina226.Ina226DeviceInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.manufacturerId = reader.uint32();
                        break;
                    }
                case 2: {
                        message.dieId = reader.uint32();
                        break;
                    }
                case 3: {
                        message.revisionId = reader.uint32();
                        break;
                    }
                case 4: {
                        message.shuntResistanceOhms = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an Ina226DeviceInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof ina226.Ina226DeviceInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {ina226.Ina226DeviceInfo} Ina226DeviceInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Ina226DeviceInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an Ina226DeviceInfo message.
         * @function verify
         * @memberof ina226.Ina226DeviceInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Ina226DeviceInfo.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.manufacturerId != null && message.hasOwnProperty("manufacturerId"))
                if (!$util.isInteger(message.manufacturerId))
                    return "manufacturerId: integer expected";
            if (message.dieId != null && message.hasOwnProperty("dieId"))
                if (!$util.isInteger(message.dieId))
                    return "dieId: integer expected";
            if (message.revisionId != null && message.hasOwnProperty("revisionId"))
                if (!$util.isInteger(message.revisionId))
                    return "revisionId: integer expected";
            if (message.shuntResistanceOhms != null && message.hasOwnProperty("shuntResistanceOhms"))
                if (typeof message.shuntResistanceOhms !== "number")
                    return "shuntResistanceOhms: number expected";
            return null;
        };

        /**
         * Creates an Ina226DeviceInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof ina226.Ina226DeviceInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {ina226.Ina226DeviceInfo} Ina226DeviceInfo
         */
        Ina226DeviceInfo.fromObject = function fromObject(object, long) {
            if (object instanceof $root.ina226.Ina226DeviceInfo)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.ina226.Ina226DeviceInfo();
            if (object.manufacturerId != null)
                message.manufacturerId = object.manufacturerId >>> 0;
            if (object.dieId != null)
                message.dieId = object.dieId >>> 0;
            if (object.revisionId != null)
                message.revisionId = object.revisionId >>> 0;
            if (object.shuntResistanceOhms != null)
                message.shuntResistanceOhms = Number(object.shuntResistanceOhms);
            return message;
        };

        /**
         * Creates a plain object from an Ina226DeviceInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof ina226.Ina226DeviceInfo
         * @static
         * @param {ina226.Ina226DeviceInfo} message Ina226DeviceInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Ina226DeviceInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.manufacturerId = 0;
                object.dieId = 0;
                object.revisionId = 0;
                object.shuntResistanceOhms = 0;
            }
            if (message.manufacturerId != null && message.hasOwnProperty("manufacturerId"))
                object.manufacturerId = message.manufacturerId;
            if (message.dieId != null && message.hasOwnProperty("dieId"))
                object.dieId = message.dieId;
            if (message.revisionId != null && message.hasOwnProperty("revisionId"))
                object.revisionId = message.revisionId;
            if (message.shuntResistanceOhms != null && message.hasOwnProperty("shuntResistanceOhms"))
                object.shuntResistanceOhms = options.json && !isFinite(message.shuntResistanceOhms) ? String(message.shuntResistanceOhms) : message.shuntResistanceOhms;
            return object;
        };

        /**
         * Converts this Ina226DeviceInfo to JSON.
         * @function toJSON
         * @memberof ina226.Ina226DeviceInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Ina226DeviceInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Ina226DeviceInfo
         * @function getTypeUrl
         * @memberof ina226.Ina226DeviceInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Ina226DeviceInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/ina226.Ina226DeviceInfo";
        };

        return Ina226DeviceInfo;
    })();

    ina226.Ina226Device = (function() {

        /**
         * Properties of an Ina226Device.
         * @memberof ina226
         * @interface IIna226Device
         * @property {string|null} [id] Ina226Device id
         * @property {number|null} [i2cBus] Ina226Device i2cBus
         * @property {number|null} [i2cAddress] Ina226Device i2cAddress
         * @property {ina226.IIna226DeviceInfo|null} [info] Ina226Device info
         */

        /**
         * Constructs a new Ina226Device.
         * @memberof ina226
         * @classdesc Represents an Ina226Device.
         * @implements IIna226Device
         * @constructor
         * @param {ina226.IIna226Device=} [properties] Properties to set
         */
        function Ina226Device(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Ina226Device id.
         * @member {string} id
         * @memberof ina226.Ina226Device
         * @instance
         */
        Ina226Device.prototype.id = "";

        /**
         * Ina226Device i2cBus.
         * @member {number} i2cBus
         * @memberof ina226.Ina226Device
         * @instance
         */
        Ina226Device.prototype.i2cBus = 0;

        /**
         * Ina226Device i2cAddress.
         * @member {number} i2cAddress
         * @memberof ina226.Ina226Device
         * @instance
         */
        Ina226Device.prototype.i2cAddress = 0;

        /**
         * Ina226Device info.
         * @member {ina226.IIna226DeviceInfo|null|undefined} info
         * @memberof ina226.Ina226Device
         * @instance
         */
        Ina226Device.prototype.info = null;

        /**
         * Creates a new Ina226Device instance using the specified properties.
         * @function create
         * @memberof ina226.Ina226Device
         * @static
         * @param {ina226.IIna226Device=} [properties] Properties to set
         * @returns {ina226.Ina226Device} Ina226Device instance
         */
        Ina226Device.create = function create(properties) {
            return new Ina226Device(properties);
        };

        /**
         * Encodes the specified Ina226Device message. Does not implicitly {@link ina226.Ina226Device.verify|verify} messages.
         * @function encode
         * @memberof ina226.Ina226Device
         * @static
         * @param {ina226.IIna226Device} message Ina226Device message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Ina226Device.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.id);
            if (message.i2cBus != null && Object.hasOwnProperty.call(message, "i2cBus"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.i2cBus);
            if (message.i2cAddress != null && Object.hasOwnProperty.call(message, "i2cAddress"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.i2cAddress);
            if (message.info != null && Object.hasOwnProperty.call(message, "info"))
                $root.ina226.Ina226DeviceInfo.encode(message.info, writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified Ina226Device message, length delimited. Does not implicitly {@link ina226.Ina226Device.verify|verify} messages.
         * @function encodeDelimited
         * @memberof ina226.Ina226Device
         * @static
         * @param {ina226.IIna226Device} message Ina226Device message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Ina226Device.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an Ina226Device message from the specified reader or buffer.
         * @function decode
         * @memberof ina226.Ina226Device
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {ina226.Ina226Device} Ina226Device
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Ina226Device.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.ina226.Ina226Device();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.id = reader.string();
                        break;
                    }
                case 2: {
                        message.i2cBus = reader.uint32();
                        break;
                    }
                case 3: {
                        message.i2cAddress = reader.uint32();
                        break;
                    }
                case 10: {
                        message.info = $root.ina226.Ina226DeviceInfo.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an Ina226Device message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof ina226.Ina226Device
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {ina226.Ina226Device} Ina226Device
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Ina226Device.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an Ina226Device message.
         * @function verify
         * @memberof ina226.Ina226Device
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Ina226Device.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isString(message.id))
                    return "id: string expected";
            if (message.i2cBus != null && message.hasOwnProperty("i2cBus"))
                if (!$util.isInteger(message.i2cBus))
                    return "i2cBus: integer expected";
            if (message.i2cAddress != null && message.hasOwnProperty("i2cAddress"))
                if (!$util.isInteger(message.i2cAddress))
                    return "i2cAddress: integer expected";
            if (message.info != null && message.hasOwnProperty("info")) {
                let error = $root.ina226.Ina226DeviceInfo.verify(message.info, long + 1);
                if (error)
                    return "info." + error;
            }
            return null;
        };

        /**
         * Creates an Ina226Device message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof ina226.Ina226Device
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {ina226.Ina226Device} Ina226Device
         */
        Ina226Device.fromObject = function fromObject(object, long) {
            if (object instanceof $root.ina226.Ina226Device)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.ina226.Ina226Device();
            if (object.id != null)
                message.id = String(object.id);
            if (object.i2cBus != null)
                message.i2cBus = object.i2cBus >>> 0;
            if (object.i2cAddress != null)
                message.i2cAddress = object.i2cAddress >>> 0;
            if (object.info != null) {
                if (typeof object.info !== "object")
                    throw TypeError(".ina226.Ina226Device.info: object expected");
                message.info = $root.ina226.Ina226DeviceInfo.fromObject(object.info, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from an Ina226Device message. Also converts values to other types if specified.
         * @function toObject
         * @memberof ina226.Ina226Device
         * @static
         * @param {ina226.Ina226Device} message Ina226Device
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Ina226Device.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.id = "";
                object.i2cBus = 0;
                object.i2cAddress = 0;
                object.info = null;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.i2cBus != null && message.hasOwnProperty("i2cBus"))
                object.i2cBus = message.i2cBus;
            if (message.i2cAddress != null && message.hasOwnProperty("i2cAddress"))
                object.i2cAddress = message.i2cAddress;
            if (message.info != null && message.hasOwnProperty("info"))
                object.info = $root.ina226.Ina226DeviceInfo.toObject(message.info, options);
            return object;
        };

        /**
         * Converts this Ina226Device to JSON.
         * @function toJSON
         * @memberof ina226.Ina226Device
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Ina226Device.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Ina226Device
         * @function getTypeUrl
         * @memberof ina226.Ina226Device
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Ina226Device.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/ina226.Ina226Device";
        };

        return Ina226Device;
    })();

    ina226.RxEnvelope = (function() {

        /**
         * Properties of a RxEnvelope.
         * @memberof ina226
         * @interface IRxEnvelope
         * @property {Long|null} [monotonicStampNs] RxEnvelope monotonicStampNs
         * @property {Long|null} [localStampNs] RxEnvelope localStampNs
         * @property {Long|null} [appStartId] RxEnvelope appStartId
         * @property {ina226.Ina226SignalType|null} [signalType] RxEnvelope signalType
         * @property {ina226.IIna226Device|null} [device] RxEnvelope device
         * @property {Uint8Array|null} [data] RxEnvelope data
         * @property {string|null} [error] RxEnvelope error
         */

        /**
         * Constructs a new RxEnvelope.
         * @memberof ina226
         * @classdesc Represents a RxEnvelope.
         * @implements IRxEnvelope
         * @constructor
         * @param {ina226.IRxEnvelope=} [properties] Properties to set
         */
        function RxEnvelope(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RxEnvelope monotonicStampNs.
         * @member {Long} monotonicStampNs
         * @memberof ina226.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.monotonicStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * RxEnvelope localStampNs.
         * @member {Long} localStampNs
         * @memberof ina226.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.localStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * RxEnvelope appStartId.
         * @member {Long} appStartId
         * @memberof ina226.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.appStartId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * RxEnvelope signalType.
         * @member {ina226.Ina226SignalType} signalType
         * @memberof ina226.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.signalType = 0;

        /**
         * RxEnvelope device.
         * @member {ina226.IIna226Device|null|undefined} device
         * @memberof ina226.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.device = null;

        /**
         * RxEnvelope data.
         * @member {Uint8Array} data
         * @memberof ina226.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.data = $util.newBuffer([]);

        /**
         * RxEnvelope error.
         * @member {string} error
         * @memberof ina226.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.error = "";

        /**
         * Creates a new RxEnvelope instance using the specified properties.
         * @function create
         * @memberof ina226.RxEnvelope
         * @static
         * @param {ina226.IRxEnvelope=} [properties] Properties to set
         * @returns {ina226.RxEnvelope} RxEnvelope instance
         */
        RxEnvelope.create = function create(properties) {
            return new RxEnvelope(properties);
        };

        /**
         * Encodes the specified RxEnvelope message. Does not implicitly {@link ina226.RxEnvelope.verify|verify} messages.
         * @function encode
         * @memberof ina226.RxEnvelope
         * @static
         * @param {ina226.IRxEnvelope} message RxEnvelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RxEnvelope.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.monotonicStampNs != null && Object.hasOwnProperty.call(message, "monotonicStampNs"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.monotonicStampNs);
            if (message.localStampNs != null && Object.hasOwnProperty.call(message, "localStampNs"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.localStampNs);
            if (message.appStartId != null && Object.hasOwnProperty.call(message, "appStartId"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.appStartId);
            if (message.signalType != null && Object.hasOwnProperty.call(message, "signalType"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.signalType);
            if (message.device != null && Object.hasOwnProperty.call(message, "device"))
                $root.ina226.Ina226Device.encode(message.device, writer.uint32(/* id 11, wireType 2 =*/90).fork()).ldelim();
            if (message.data != null && Object.hasOwnProperty.call(message, "data"))
                writer.uint32(/* id 20, wireType 2 =*/162).bytes(message.data);
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                writer.uint32(/* id 50, wireType 2 =*/402).string(message.error);
            return writer;
        };

        /**
         * Encodes the specified RxEnvelope message, length delimited. Does not implicitly {@link ina226.RxEnvelope.verify|verify} messages.
         * @function encodeDelimited
         * @memberof ina226.RxEnvelope
         * @static
         * @param {ina226.IRxEnvelope} message RxEnvelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RxEnvelope.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RxEnvelope message from the specified reader or buffer.
         * @function decode
         * @memberof ina226.RxEnvelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {ina226.RxEnvelope} RxEnvelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RxEnvelope.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.ina226.RxEnvelope();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.monotonicStampNs = reader.uint64();
                        break;
                    }
                case 2: {
                        message.localStampNs = reader.uint64();
                        break;
                    }
                case 3: {
                        message.appStartId = reader.uint64();
                        break;
                    }
                case 10: {
                        message.signalType = reader.int32();
                        break;
                    }
                case 11: {
                        message.device = $root.ina226.Ina226Device.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 20: {
                        message.data = reader.bytes();
                        break;
                    }
                case 50: {
                        message.error = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RxEnvelope message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof ina226.RxEnvelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {ina226.RxEnvelope} RxEnvelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RxEnvelope.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RxEnvelope message.
         * @function verify
         * @memberof ina226.RxEnvelope
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RxEnvelope.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (!$util.isInteger(message.monotonicStampNs) && !(message.monotonicStampNs && $util.isInteger(message.monotonicStampNs.low) && $util.isInteger(message.monotonicStampNs.high)))
                    return "monotonicStampNs: integer|Long expected";
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (!$util.isInteger(message.localStampNs) && !(message.localStampNs && $util.isInteger(message.localStampNs.low) && $util.isInteger(message.localStampNs.high)))
                    return "localStampNs: integer|Long expected";
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (!$util.isInteger(message.appStartId) && !(message.appStartId && $util.isInteger(message.appStartId.low) && $util.isInteger(message.appStartId.high)))
                    return "appStartId: integer|Long expected";
            if (message.signalType != null && message.hasOwnProperty("signalType"))
                switch (message.signalType) {
                default:
                    return "signalType: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                    break;
                }
            if (message.device != null && message.hasOwnProperty("device")) {
                let error = $root.ina226.Ina226Device.verify(message.device, long + 1);
                if (error)
                    return "device." + error;
            }
            if (message.data != null && message.hasOwnProperty("data"))
                if (!(message.data && typeof message.data.length === "number" || $util.isString(message.data)))
                    return "data: buffer expected";
            if (message.error != null && message.hasOwnProperty("error"))
                if (!$util.isString(message.error))
                    return "error: string expected";
            return null;
        };

        /**
         * Creates a RxEnvelope message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof ina226.RxEnvelope
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {ina226.RxEnvelope} RxEnvelope
         */
        RxEnvelope.fromObject = function fromObject(object, long) {
            if (object instanceof $root.ina226.RxEnvelope)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.ina226.RxEnvelope();
            if (object.monotonicStampNs != null)
                if ($util.Long)
                    (message.monotonicStampNs = $util.Long.fromValue(object.monotonicStampNs)).unsigned = true;
                else if (typeof object.monotonicStampNs === "string")
                    message.monotonicStampNs = parseInt(object.monotonicStampNs, 10);
                else if (typeof object.monotonicStampNs === "number")
                    message.monotonicStampNs = object.monotonicStampNs;
                else if (typeof object.monotonicStampNs === "object")
                    message.monotonicStampNs = new $util.LongBits(object.monotonicStampNs.low >>> 0, object.monotonicStampNs.high >>> 0).toNumber(true);
            if (object.localStampNs != null)
                if ($util.Long)
                    (message.localStampNs = $util.Long.fromValue(object.localStampNs)).unsigned = true;
                else if (typeof object.localStampNs === "string")
                    message.localStampNs = parseInt(object.localStampNs, 10);
                else if (typeof object.localStampNs === "number")
                    message.localStampNs = object.localStampNs;
                else if (typeof object.localStampNs === "object")
                    message.localStampNs = new $util.LongBits(object.localStampNs.low >>> 0, object.localStampNs.high >>> 0).toNumber(true);
            if (object.appStartId != null)
                if ($util.Long)
                    (message.appStartId = $util.Long.fromValue(object.appStartId)).unsigned = true;
                else if (typeof object.appStartId === "string")
                    message.appStartId = parseInt(object.appStartId, 10);
                else if (typeof object.appStartId === "number")
                    message.appStartId = object.appStartId;
                else if (typeof object.appStartId === "object")
                    message.appStartId = new $util.LongBits(object.appStartId.low >>> 0, object.appStartId.high >>> 0).toNumber(true);
            switch (object.signalType) {
            default:
                if (typeof object.signalType === "number") {
                    message.signalType = object.signalType;
                    break;
                }
                break;
            case "INA226_SIGNAL_TYPE_UNSPECIFIED":
            case 0:
                message.signalType = 0;
                break;
            case "INA226_CONNECTED":
            case 1:
                message.signalType = 1;
                break;
            case "INA226_DISCONNECTED":
            case 2:
                message.signalType = 2;
                break;
            case "INA226_REGISTERS_SNAPSHOT":
            case 3:
                message.signalType = 3;
                break;
            case "INA226_ERROR":
            case 4:
                message.signalType = 4;
                break;
            }
            if (object.device != null) {
                if (typeof object.device !== "object")
                    throw TypeError(".ina226.RxEnvelope.device: object expected");
                message.device = $root.ina226.Ina226Device.fromObject(object.device, long + 1);
            }
            if (object.data != null)
                if (typeof object.data === "string")
                    $util.base64.decode(object.data, message.data = $util.newBuffer($util.base64.length(object.data)), 0);
                else if (object.data.length >= 0)
                    message.data = object.data;
            if (object.error != null)
                message.error = String(object.error);
            return message;
        };

        /**
         * Creates a plain object from a RxEnvelope message. Also converts values to other types if specified.
         * @function toObject
         * @memberof ina226.RxEnvelope
         * @static
         * @param {ina226.RxEnvelope} message RxEnvelope
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RxEnvelope.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.monotonicStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.monotonicStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.localStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.localStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.appStartId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.appStartId = options.longs === String ? "0" : 0;
                object.signalType = options.enums === String ? "INA226_SIGNAL_TYPE_UNSPECIFIED" : 0;
                object.device = null;
                if (options.bytes === String)
                    object.data = "";
                else {
                    object.data = [];
                    if (options.bytes !== Array)
                        object.data = $util.newBuffer(object.data);
                }
                object.error = "";
            }
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (typeof message.monotonicStampNs === "number")
                    object.monotonicStampNs = options.longs === String ? String(message.monotonicStampNs) : message.monotonicStampNs;
                else
                    object.monotonicStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.monotonicStampNs) : options.longs === Number ? new $util.LongBits(message.monotonicStampNs.low >>> 0, message.monotonicStampNs.high >>> 0).toNumber(true) : message.monotonicStampNs;
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (typeof message.localStampNs === "number")
                    object.localStampNs = options.longs === String ? String(message.localStampNs) : message.localStampNs;
                else
                    object.localStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.localStampNs) : options.longs === Number ? new $util.LongBits(message.localStampNs.low >>> 0, message.localStampNs.high >>> 0).toNumber(true) : message.localStampNs;
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (typeof message.appStartId === "number")
                    object.appStartId = options.longs === String ? String(message.appStartId) : message.appStartId;
                else
                    object.appStartId = options.longs === String ? $util.Long.prototype.toString.call(message.appStartId) : options.longs === Number ? new $util.LongBits(message.appStartId.low >>> 0, message.appStartId.high >>> 0).toNumber(true) : message.appStartId;
            if (message.signalType != null && message.hasOwnProperty("signalType"))
                object.signalType = options.enums === String ? $root.ina226.Ina226SignalType[message.signalType] === undefined ? message.signalType : $root.ina226.Ina226SignalType[message.signalType] : message.signalType;
            if (message.device != null && message.hasOwnProperty("device"))
                object.device = $root.ina226.Ina226Device.toObject(message.device, options);
            if (message.data != null && message.hasOwnProperty("data"))
                object.data = options.bytes === String ? $util.base64.encode(message.data, 0, message.data.length) : options.bytes === Array ? Array.prototype.slice.call(message.data) : message.data;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = message.error;
            return object;
        };

        /**
         * Converts this RxEnvelope to JSON.
         * @function toJSON
         * @memberof ina226.RxEnvelope
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RxEnvelope.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RxEnvelope
         * @function getTypeUrl
         * @memberof ina226.RxEnvelope
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RxEnvelope.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/ina226.RxEnvelope";
        };

        return RxEnvelope;
    })();

    return ina226;
})();

export const airgradient_open_air_o_1pst = $root.airgradient_open_air_o_1pst = (() => {

    /**
     * Namespace airgradient_open_air_o_1pst.
     * @exports airgradient_open_air_o_1pst
     * @namespace
     */
    const airgradient_open_air_o_1pst = {};

    /**
     * AirGradientSignalType enum.
     * @name airgradient_open_air_o_1pst.AirGradientSignalType
     * @enum {number}
     * @property {number} AIRGRADIENT_SIGNAL_TYPE_UNSPECIFIED=0 AIRGRADIENT_SIGNAL_TYPE_UNSPECIFIED value
     * @property {number} AIRGRADIENT_CONNECTED=1 AIRGRADIENT_CONNECTED value
     * @property {number} AIRGRADIENT_DISCONNECTED=2 AIRGRADIENT_DISCONNECTED value
     * @property {number} AIRGRADIENT_MEASUREMENT=3 AIRGRADIENT_MEASUREMENT value
     * @property {number} AIRGRADIENT_ERROR=4 AIRGRADIENT_ERROR value
     */
    airgradient_open_air_o_1pst.AirGradientSignalType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "AIRGRADIENT_SIGNAL_TYPE_UNSPECIFIED"] = 0;
        values[valuesById[1] = "AIRGRADIENT_CONNECTED"] = 1;
        values[valuesById[2] = "AIRGRADIENT_DISCONNECTED"] = 2;
        values[valuesById[3] = "AIRGRADIENT_MEASUREMENT"] = 3;
        values[valuesById[4] = "AIRGRADIENT_ERROR"] = 4;
        return values;
    })();

    airgradient_open_air_o_1pst.AirGradientDevice = (function() {

        /**
         * Properties of an AirGradientDevice.
         * @memberof airgradient_open_air_o_1pst
         * @interface IAirGradientDevice
         * @property {string|null} [portName] AirGradientDevice portName
         * @property {number|null} [vid] AirGradientDevice vid
         * @property {number|null} [pid] AirGradientDevice pid
         * @property {string|null} [serialNumber] AirGradientDevice serialNumber
         * @property {string|null} [manufacturer] AirGradientDevice manufacturer
         * @property {string|null} [product] AirGradientDevice product
         * @property {number|null} [portBaudRate] AirGradientDevice portBaudRate
         * @property {string|null} [deviceId] AirGradientDevice deviceId
         */

        /**
         * Constructs a new AirGradientDevice.
         * @memberof airgradient_open_air_o_1pst
         * @classdesc Represents an AirGradientDevice.
         * @implements IAirGradientDevice
         * @constructor
         * @param {airgradient_open_air_o_1pst.IAirGradientDevice=} [properties] Properties to set
         */
        function AirGradientDevice(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AirGradientDevice portName.
         * @member {string} portName
         * @memberof airgradient_open_air_o_1pst.AirGradientDevice
         * @instance
         */
        AirGradientDevice.prototype.portName = "";

        /**
         * AirGradientDevice vid.
         * @member {number} vid
         * @memberof airgradient_open_air_o_1pst.AirGradientDevice
         * @instance
         */
        AirGradientDevice.prototype.vid = 0;

        /**
         * AirGradientDevice pid.
         * @member {number} pid
         * @memberof airgradient_open_air_o_1pst.AirGradientDevice
         * @instance
         */
        AirGradientDevice.prototype.pid = 0;

        /**
         * AirGradientDevice serialNumber.
         * @member {string} serialNumber
         * @memberof airgradient_open_air_o_1pst.AirGradientDevice
         * @instance
         */
        AirGradientDevice.prototype.serialNumber = "";

        /**
         * AirGradientDevice manufacturer.
         * @member {string} manufacturer
         * @memberof airgradient_open_air_o_1pst.AirGradientDevice
         * @instance
         */
        AirGradientDevice.prototype.manufacturer = "";

        /**
         * AirGradientDevice product.
         * @member {string} product
         * @memberof airgradient_open_air_o_1pst.AirGradientDevice
         * @instance
         */
        AirGradientDevice.prototype.product = "";

        /**
         * AirGradientDevice portBaudRate.
         * @member {number} portBaudRate
         * @memberof airgradient_open_air_o_1pst.AirGradientDevice
         * @instance
         */
        AirGradientDevice.prototype.portBaudRate = 0;

        /**
         * AirGradientDevice deviceId.
         * @member {string} deviceId
         * @memberof airgradient_open_air_o_1pst.AirGradientDevice
         * @instance
         */
        AirGradientDevice.prototype.deviceId = "";

        /**
         * Creates a new AirGradientDevice instance using the specified properties.
         * @function create
         * @memberof airgradient_open_air_o_1pst.AirGradientDevice
         * @static
         * @param {airgradient_open_air_o_1pst.IAirGradientDevice=} [properties] Properties to set
         * @returns {airgradient_open_air_o_1pst.AirGradientDevice} AirGradientDevice instance
         */
        AirGradientDevice.create = function create(properties) {
            return new AirGradientDevice(properties);
        };

        /**
         * Encodes the specified AirGradientDevice message. Does not implicitly {@link airgradient_open_air_o_1pst.AirGradientDevice.verify|verify} messages.
         * @function encode
         * @memberof airgradient_open_air_o_1pst.AirGradientDevice
         * @static
         * @param {airgradient_open_air_o_1pst.IAirGradientDevice} message AirGradientDevice message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AirGradientDevice.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.portName != null && Object.hasOwnProperty.call(message, "portName"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.portName);
            if (message.vid != null && Object.hasOwnProperty.call(message, "vid"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.vid);
            if (message.pid != null && Object.hasOwnProperty.call(message, "pid"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.pid);
            if (message.serialNumber != null && Object.hasOwnProperty.call(message, "serialNumber"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.serialNumber);
            if (message.manufacturer != null && Object.hasOwnProperty.call(message, "manufacturer"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.manufacturer);
            if (message.product != null && Object.hasOwnProperty.call(message, "product"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.product);
            if (message.portBaudRate != null && Object.hasOwnProperty.call(message, "portBaudRate"))
                writer.uint32(/* id 7, wireType 0 =*/56).uint32(message.portBaudRate);
            if (message.deviceId != null && Object.hasOwnProperty.call(message, "deviceId"))
                writer.uint32(/* id 8, wireType 2 =*/66).string(message.deviceId);
            return writer;
        };

        /**
         * Encodes the specified AirGradientDevice message, length delimited. Does not implicitly {@link airgradient_open_air_o_1pst.AirGradientDevice.verify|verify} messages.
         * @function encodeDelimited
         * @memberof airgradient_open_air_o_1pst.AirGradientDevice
         * @static
         * @param {airgradient_open_air_o_1pst.IAirGradientDevice} message AirGradientDevice message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AirGradientDevice.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AirGradientDevice message from the specified reader or buffer.
         * @function decode
         * @memberof airgradient_open_air_o_1pst.AirGradientDevice
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {airgradient_open_air_o_1pst.AirGradientDevice} AirGradientDevice
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AirGradientDevice.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.airgradient_open_air_o_1pst.AirGradientDevice();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.portName = reader.string();
                        break;
                    }
                case 2: {
                        message.vid = reader.uint32();
                        break;
                    }
                case 3: {
                        message.pid = reader.uint32();
                        break;
                    }
                case 4: {
                        message.serialNumber = reader.string();
                        break;
                    }
                case 5: {
                        message.manufacturer = reader.string();
                        break;
                    }
                case 6: {
                        message.product = reader.string();
                        break;
                    }
                case 7: {
                        message.portBaudRate = reader.uint32();
                        break;
                    }
                case 8: {
                        message.deviceId = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AirGradientDevice message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof airgradient_open_air_o_1pst.AirGradientDevice
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {airgradient_open_air_o_1pst.AirGradientDevice} AirGradientDevice
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AirGradientDevice.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AirGradientDevice message.
         * @function verify
         * @memberof airgradient_open_air_o_1pst.AirGradientDevice
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AirGradientDevice.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.portName != null && message.hasOwnProperty("portName"))
                if (!$util.isString(message.portName))
                    return "portName: string expected";
            if (message.vid != null && message.hasOwnProperty("vid"))
                if (!$util.isInteger(message.vid))
                    return "vid: integer expected";
            if (message.pid != null && message.hasOwnProperty("pid"))
                if (!$util.isInteger(message.pid))
                    return "pid: integer expected";
            if (message.serialNumber != null && message.hasOwnProperty("serialNumber"))
                if (!$util.isString(message.serialNumber))
                    return "serialNumber: string expected";
            if (message.manufacturer != null && message.hasOwnProperty("manufacturer"))
                if (!$util.isString(message.manufacturer))
                    return "manufacturer: string expected";
            if (message.product != null && message.hasOwnProperty("product"))
                if (!$util.isString(message.product))
                    return "product: string expected";
            if (message.portBaudRate != null && message.hasOwnProperty("portBaudRate"))
                if (!$util.isInteger(message.portBaudRate))
                    return "portBaudRate: integer expected";
            if (message.deviceId != null && message.hasOwnProperty("deviceId"))
                if (!$util.isString(message.deviceId))
                    return "deviceId: string expected";
            return null;
        };

        /**
         * Creates an AirGradientDevice message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof airgradient_open_air_o_1pst.AirGradientDevice
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {airgradient_open_air_o_1pst.AirGradientDevice} AirGradientDevice
         */
        AirGradientDevice.fromObject = function fromObject(object, long) {
            if (object instanceof $root.airgradient_open_air_o_1pst.AirGradientDevice)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.airgradient_open_air_o_1pst.AirGradientDevice();
            if (object.portName != null)
                message.portName = String(object.portName);
            if (object.vid != null)
                message.vid = object.vid >>> 0;
            if (object.pid != null)
                message.pid = object.pid >>> 0;
            if (object.serialNumber != null)
                message.serialNumber = String(object.serialNumber);
            if (object.manufacturer != null)
                message.manufacturer = String(object.manufacturer);
            if (object.product != null)
                message.product = String(object.product);
            if (object.portBaudRate != null)
                message.portBaudRate = object.portBaudRate >>> 0;
            if (object.deviceId != null)
                message.deviceId = String(object.deviceId);
            return message;
        };

        /**
         * Creates a plain object from an AirGradientDevice message. Also converts values to other types if specified.
         * @function toObject
         * @memberof airgradient_open_air_o_1pst.AirGradientDevice
         * @static
         * @param {airgradient_open_air_o_1pst.AirGradientDevice} message AirGradientDevice
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AirGradientDevice.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.portName = "";
                object.vid = 0;
                object.pid = 0;
                object.serialNumber = "";
                object.manufacturer = "";
                object.product = "";
                object.portBaudRate = 0;
                object.deviceId = "";
            }
            if (message.portName != null && message.hasOwnProperty("portName"))
                object.portName = message.portName;
            if (message.vid != null && message.hasOwnProperty("vid"))
                object.vid = message.vid;
            if (message.pid != null && message.hasOwnProperty("pid"))
                object.pid = message.pid;
            if (message.serialNumber != null && message.hasOwnProperty("serialNumber"))
                object.serialNumber = message.serialNumber;
            if (message.manufacturer != null && message.hasOwnProperty("manufacturer"))
                object.manufacturer = message.manufacturer;
            if (message.product != null && message.hasOwnProperty("product"))
                object.product = message.product;
            if (message.portBaudRate != null && message.hasOwnProperty("portBaudRate"))
                object.portBaudRate = message.portBaudRate;
            if (message.deviceId != null && message.hasOwnProperty("deviceId"))
                object.deviceId = message.deviceId;
            return object;
        };

        /**
         * Converts this AirGradientDevice to JSON.
         * @function toJSON
         * @memberof airgradient_open_air_o_1pst.AirGradientDevice
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AirGradientDevice.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AirGradientDevice
         * @function getTypeUrl
         * @memberof airgradient_open_air_o_1pst.AirGradientDevice
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AirGradientDevice.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/airgradient_open_air_o_1pst.AirGradientDevice";
        };

        return AirGradientDevice;
    })();

    airgradient_open_air_o_1pst.RxEnvelope = (function() {

        /**
         * Properties of a RxEnvelope.
         * @memberof airgradient_open_air_o_1pst
         * @interface IRxEnvelope
         * @property {Long|null} [monotonicStampNs] RxEnvelope monotonicStampNs
         * @property {Long|null} [localStampNs] RxEnvelope localStampNs
         * @property {Long|null} [appStartId] RxEnvelope appStartId
         * @property {airgradient_open_air_o_1pst.AirGradientSignalType|null} [signalType] RxEnvelope signalType
         * @property {airgradient_open_air_o_1pst.IAirGradientDevice|null} [device] RxEnvelope device
         * @property {Uint8Array|null} [data] RxEnvelope data
         * @property {string|null} [error] RxEnvelope error
         */

        /**
         * Constructs a new RxEnvelope.
         * @memberof airgradient_open_air_o_1pst
         * @classdesc Represents a RxEnvelope.
         * @implements IRxEnvelope
         * @constructor
         * @param {airgradient_open_air_o_1pst.IRxEnvelope=} [properties] Properties to set
         */
        function RxEnvelope(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RxEnvelope monotonicStampNs.
         * @member {Long} monotonicStampNs
         * @memberof airgradient_open_air_o_1pst.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.monotonicStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * RxEnvelope localStampNs.
         * @member {Long} localStampNs
         * @memberof airgradient_open_air_o_1pst.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.localStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * RxEnvelope appStartId.
         * @member {Long} appStartId
         * @memberof airgradient_open_air_o_1pst.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.appStartId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * RxEnvelope signalType.
         * @member {airgradient_open_air_o_1pst.AirGradientSignalType} signalType
         * @memberof airgradient_open_air_o_1pst.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.signalType = 0;

        /**
         * RxEnvelope device.
         * @member {airgradient_open_air_o_1pst.IAirGradientDevice|null|undefined} device
         * @memberof airgradient_open_air_o_1pst.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.device = null;

        /**
         * RxEnvelope data.
         * @member {Uint8Array} data
         * @memberof airgradient_open_air_o_1pst.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.data = $util.newBuffer([]);

        /**
         * RxEnvelope error.
         * @member {string} error
         * @memberof airgradient_open_air_o_1pst.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.error = "";

        /**
         * Creates a new RxEnvelope instance using the specified properties.
         * @function create
         * @memberof airgradient_open_air_o_1pst.RxEnvelope
         * @static
         * @param {airgradient_open_air_o_1pst.IRxEnvelope=} [properties] Properties to set
         * @returns {airgradient_open_air_o_1pst.RxEnvelope} RxEnvelope instance
         */
        RxEnvelope.create = function create(properties) {
            return new RxEnvelope(properties);
        };

        /**
         * Encodes the specified RxEnvelope message. Does not implicitly {@link airgradient_open_air_o_1pst.RxEnvelope.verify|verify} messages.
         * @function encode
         * @memberof airgradient_open_air_o_1pst.RxEnvelope
         * @static
         * @param {airgradient_open_air_o_1pst.IRxEnvelope} message RxEnvelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RxEnvelope.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.monotonicStampNs != null && Object.hasOwnProperty.call(message, "monotonicStampNs"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.monotonicStampNs);
            if (message.localStampNs != null && Object.hasOwnProperty.call(message, "localStampNs"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.localStampNs);
            if (message.appStartId != null && Object.hasOwnProperty.call(message, "appStartId"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.appStartId);
            if (message.signalType != null && Object.hasOwnProperty.call(message, "signalType"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.signalType);
            if (message.device != null && Object.hasOwnProperty.call(message, "device"))
                $root.airgradient_open_air_o_1pst.AirGradientDevice.encode(message.device, writer.uint32(/* id 11, wireType 2 =*/90).fork()).ldelim();
            if (message.data != null && Object.hasOwnProperty.call(message, "data"))
                writer.uint32(/* id 20, wireType 2 =*/162).bytes(message.data);
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                writer.uint32(/* id 50, wireType 2 =*/402).string(message.error);
            return writer;
        };

        /**
         * Encodes the specified RxEnvelope message, length delimited. Does not implicitly {@link airgradient_open_air_o_1pst.RxEnvelope.verify|verify} messages.
         * @function encodeDelimited
         * @memberof airgradient_open_air_o_1pst.RxEnvelope
         * @static
         * @param {airgradient_open_air_o_1pst.IRxEnvelope} message RxEnvelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RxEnvelope.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RxEnvelope message from the specified reader or buffer.
         * @function decode
         * @memberof airgradient_open_air_o_1pst.RxEnvelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {airgradient_open_air_o_1pst.RxEnvelope} RxEnvelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RxEnvelope.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.airgradient_open_air_o_1pst.RxEnvelope();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.monotonicStampNs = reader.uint64();
                        break;
                    }
                case 2: {
                        message.localStampNs = reader.uint64();
                        break;
                    }
                case 3: {
                        message.appStartId = reader.uint64();
                        break;
                    }
                case 10: {
                        message.signalType = reader.int32();
                        break;
                    }
                case 11: {
                        message.device = $root.airgradient_open_air_o_1pst.AirGradientDevice.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 20: {
                        message.data = reader.bytes();
                        break;
                    }
                case 50: {
                        message.error = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RxEnvelope message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof airgradient_open_air_o_1pst.RxEnvelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {airgradient_open_air_o_1pst.RxEnvelope} RxEnvelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RxEnvelope.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RxEnvelope message.
         * @function verify
         * @memberof airgradient_open_air_o_1pst.RxEnvelope
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RxEnvelope.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (!$util.isInteger(message.monotonicStampNs) && !(message.monotonicStampNs && $util.isInteger(message.monotonicStampNs.low) && $util.isInteger(message.monotonicStampNs.high)))
                    return "monotonicStampNs: integer|Long expected";
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (!$util.isInteger(message.localStampNs) && !(message.localStampNs && $util.isInteger(message.localStampNs.low) && $util.isInteger(message.localStampNs.high)))
                    return "localStampNs: integer|Long expected";
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (!$util.isInteger(message.appStartId) && !(message.appStartId && $util.isInteger(message.appStartId.low) && $util.isInteger(message.appStartId.high)))
                    return "appStartId: integer|Long expected";
            if (message.signalType != null && message.hasOwnProperty("signalType"))
                switch (message.signalType) {
                default:
                    return "signalType: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                    break;
                }
            if (message.device != null && message.hasOwnProperty("device")) {
                let error = $root.airgradient_open_air_o_1pst.AirGradientDevice.verify(message.device, long + 1);
                if (error)
                    return "device." + error;
            }
            if (message.data != null && message.hasOwnProperty("data"))
                if (!(message.data && typeof message.data.length === "number" || $util.isString(message.data)))
                    return "data: buffer expected";
            if (message.error != null && message.hasOwnProperty("error"))
                if (!$util.isString(message.error))
                    return "error: string expected";
            return null;
        };

        /**
         * Creates a RxEnvelope message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof airgradient_open_air_o_1pst.RxEnvelope
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {airgradient_open_air_o_1pst.RxEnvelope} RxEnvelope
         */
        RxEnvelope.fromObject = function fromObject(object, long) {
            if (object instanceof $root.airgradient_open_air_o_1pst.RxEnvelope)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.airgradient_open_air_o_1pst.RxEnvelope();
            if (object.monotonicStampNs != null)
                if ($util.Long)
                    (message.monotonicStampNs = $util.Long.fromValue(object.monotonicStampNs)).unsigned = true;
                else if (typeof object.monotonicStampNs === "string")
                    message.monotonicStampNs = parseInt(object.monotonicStampNs, 10);
                else if (typeof object.monotonicStampNs === "number")
                    message.monotonicStampNs = object.monotonicStampNs;
                else if (typeof object.monotonicStampNs === "object")
                    message.monotonicStampNs = new $util.LongBits(object.monotonicStampNs.low >>> 0, object.monotonicStampNs.high >>> 0).toNumber(true);
            if (object.localStampNs != null)
                if ($util.Long)
                    (message.localStampNs = $util.Long.fromValue(object.localStampNs)).unsigned = true;
                else if (typeof object.localStampNs === "string")
                    message.localStampNs = parseInt(object.localStampNs, 10);
                else if (typeof object.localStampNs === "number")
                    message.localStampNs = object.localStampNs;
                else if (typeof object.localStampNs === "object")
                    message.localStampNs = new $util.LongBits(object.localStampNs.low >>> 0, object.localStampNs.high >>> 0).toNumber(true);
            if (object.appStartId != null)
                if ($util.Long)
                    (message.appStartId = $util.Long.fromValue(object.appStartId)).unsigned = true;
                else if (typeof object.appStartId === "string")
                    message.appStartId = parseInt(object.appStartId, 10);
                else if (typeof object.appStartId === "number")
                    message.appStartId = object.appStartId;
                else if (typeof object.appStartId === "object")
                    message.appStartId = new $util.LongBits(object.appStartId.low >>> 0, object.appStartId.high >>> 0).toNumber(true);
            switch (object.signalType) {
            default:
                if (typeof object.signalType === "number") {
                    message.signalType = object.signalType;
                    break;
                }
                break;
            case "AIRGRADIENT_SIGNAL_TYPE_UNSPECIFIED":
            case 0:
                message.signalType = 0;
                break;
            case "AIRGRADIENT_CONNECTED":
            case 1:
                message.signalType = 1;
                break;
            case "AIRGRADIENT_DISCONNECTED":
            case 2:
                message.signalType = 2;
                break;
            case "AIRGRADIENT_MEASUREMENT":
            case 3:
                message.signalType = 3;
                break;
            case "AIRGRADIENT_ERROR":
            case 4:
                message.signalType = 4;
                break;
            }
            if (object.device != null) {
                if (typeof object.device !== "object")
                    throw TypeError(".airgradient_open_air_o_1pst.RxEnvelope.device: object expected");
                message.device = $root.airgradient_open_air_o_1pst.AirGradientDevice.fromObject(object.device, long + 1);
            }
            if (object.data != null)
                if (typeof object.data === "string")
                    $util.base64.decode(object.data, message.data = $util.newBuffer($util.base64.length(object.data)), 0);
                else if (object.data.length >= 0)
                    message.data = object.data;
            if (object.error != null)
                message.error = String(object.error);
            return message;
        };

        /**
         * Creates a plain object from a RxEnvelope message. Also converts values to other types if specified.
         * @function toObject
         * @memberof airgradient_open_air_o_1pst.RxEnvelope
         * @static
         * @param {airgradient_open_air_o_1pst.RxEnvelope} message RxEnvelope
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RxEnvelope.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.monotonicStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.monotonicStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.localStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.localStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.appStartId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.appStartId = options.longs === String ? "0" : 0;
                object.signalType = options.enums === String ? "AIRGRADIENT_SIGNAL_TYPE_UNSPECIFIED" : 0;
                object.device = null;
                if (options.bytes === String)
                    object.data = "";
                else {
                    object.data = [];
                    if (options.bytes !== Array)
                        object.data = $util.newBuffer(object.data);
                }
                object.error = "";
            }
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (typeof message.monotonicStampNs === "number")
                    object.monotonicStampNs = options.longs === String ? String(message.monotonicStampNs) : message.monotonicStampNs;
                else
                    object.monotonicStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.monotonicStampNs) : options.longs === Number ? new $util.LongBits(message.monotonicStampNs.low >>> 0, message.monotonicStampNs.high >>> 0).toNumber(true) : message.monotonicStampNs;
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (typeof message.localStampNs === "number")
                    object.localStampNs = options.longs === String ? String(message.localStampNs) : message.localStampNs;
                else
                    object.localStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.localStampNs) : options.longs === Number ? new $util.LongBits(message.localStampNs.low >>> 0, message.localStampNs.high >>> 0).toNumber(true) : message.localStampNs;
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (typeof message.appStartId === "number")
                    object.appStartId = options.longs === String ? String(message.appStartId) : message.appStartId;
                else
                    object.appStartId = options.longs === String ? $util.Long.prototype.toString.call(message.appStartId) : options.longs === Number ? new $util.LongBits(message.appStartId.low >>> 0, message.appStartId.high >>> 0).toNumber(true) : message.appStartId;
            if (message.signalType != null && message.hasOwnProperty("signalType"))
                object.signalType = options.enums === String ? $root.airgradient_open_air_o_1pst.AirGradientSignalType[message.signalType] === undefined ? message.signalType : $root.airgradient_open_air_o_1pst.AirGradientSignalType[message.signalType] : message.signalType;
            if (message.device != null && message.hasOwnProperty("device"))
                object.device = $root.airgradient_open_air_o_1pst.AirGradientDevice.toObject(message.device, options);
            if (message.data != null && message.hasOwnProperty("data"))
                object.data = options.bytes === String ? $util.base64.encode(message.data, 0, message.data.length) : options.bytes === Array ? Array.prototype.slice.call(message.data) : message.data;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = message.error;
            return object;
        };

        /**
         * Converts this RxEnvelope to JSON.
         * @function toJSON
         * @memberof airgradient_open_air_o_1pst.RxEnvelope
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RxEnvelope.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RxEnvelope
         * @function getTypeUrl
         * @memberof airgradient_open_air_o_1pst.RxEnvelope
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RxEnvelope.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/airgradient_open_air_o_1pst.RxEnvelope";
        };

        return RxEnvelope;
    })();

    return airgradient_open_air_o_1pst;
})();

export const victron_smartsolar_mppt = $root.victron_smartsolar_mppt = (() => {

    /**
     * Namespace victron_smartsolar_mppt.
     * @exports victron_smartsolar_mppt
     * @namespace
     */
    const victron_smartsolar_mppt = {};

    /**
     * VictronSignalType enum.
     * @name victron_smartsolar_mppt.VictronSignalType
     * @enum {number}
     * @property {number} VICTRON_SIGNAL_TYPE_UNSPECIFIED=0 VICTRON_SIGNAL_TYPE_UNSPECIFIED value
     * @property {number} VICTRON_CONNECTED=1 VICTRON_CONNECTED value
     * @property {number} VICTRON_DISCONNECTED=2 VICTRON_DISCONNECTED value
     * @property {number} VICTRON_TEXT_BLOCK=3 VICTRON_TEXT_BLOCK value
     * @property {number} VICTRON_HEX_FRAME=4 VICTRON_HEX_FRAME value
     * @property {number} VICTRON_ERROR=5 VICTRON_ERROR value
     */
    victron_smartsolar_mppt.VictronSignalType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "VICTRON_SIGNAL_TYPE_UNSPECIFIED"] = 0;
        values[valuesById[1] = "VICTRON_CONNECTED"] = 1;
        values[valuesById[2] = "VICTRON_DISCONNECTED"] = 2;
        values[valuesById[3] = "VICTRON_TEXT_BLOCK"] = 3;
        values[valuesById[4] = "VICTRON_HEX_FRAME"] = 4;
        values[valuesById[5] = "VICTRON_ERROR"] = 5;
        return values;
    })();

    victron_smartsolar_mppt.VictronDevice = (function() {

        /**
         * Properties of a VictronDevice.
         * @memberof victron_smartsolar_mppt
         * @interface IVictronDevice
         * @property {string|null} [portName] VictronDevice portName
         * @property {number|null} [vid] VictronDevice vid
         * @property {number|null} [pid] VictronDevice pid
         * @property {string|null} [serialNumber] VictronDevice serialNumber
         * @property {string|null} [manufacturer] VictronDevice manufacturer
         * @property {string|null} [product] VictronDevice product
         * @property {number|null} [portBaudRate] VictronDevice portBaudRate
         * @property {number|null} [productId] VictronDevice productId
         * @property {string|null} [modelName] VictronDevice modelName
         * @property {string|null} [firmwareVersion] VictronDevice firmwareVersion
         * @property {string|null} [deviceSerial] VictronDevice deviceSerial
         */

        /**
         * Constructs a new VictronDevice.
         * @memberof victron_smartsolar_mppt
         * @classdesc Represents a VictronDevice.
         * @implements IVictronDevice
         * @constructor
         * @param {victron_smartsolar_mppt.IVictronDevice=} [properties] Properties to set
         */
        function VictronDevice(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * VictronDevice portName.
         * @member {string} portName
         * @memberof victron_smartsolar_mppt.VictronDevice
         * @instance
         */
        VictronDevice.prototype.portName = "";

        /**
         * VictronDevice vid.
         * @member {number} vid
         * @memberof victron_smartsolar_mppt.VictronDevice
         * @instance
         */
        VictronDevice.prototype.vid = 0;

        /**
         * VictronDevice pid.
         * @member {number} pid
         * @memberof victron_smartsolar_mppt.VictronDevice
         * @instance
         */
        VictronDevice.prototype.pid = 0;

        /**
         * VictronDevice serialNumber.
         * @member {string} serialNumber
         * @memberof victron_smartsolar_mppt.VictronDevice
         * @instance
         */
        VictronDevice.prototype.serialNumber = "";

        /**
         * VictronDevice manufacturer.
         * @member {string} manufacturer
         * @memberof victron_smartsolar_mppt.VictronDevice
         * @instance
         */
        VictronDevice.prototype.manufacturer = "";

        /**
         * VictronDevice product.
         * @member {string} product
         * @memberof victron_smartsolar_mppt.VictronDevice
         * @instance
         */
        VictronDevice.prototype.product = "";

        /**
         * VictronDevice portBaudRate.
         * @member {number} portBaudRate
         * @memberof victron_smartsolar_mppt.VictronDevice
         * @instance
         */
        VictronDevice.prototype.portBaudRate = 0;

        /**
         * VictronDevice productId.
         * @member {number} productId
         * @memberof victron_smartsolar_mppt.VictronDevice
         * @instance
         */
        VictronDevice.prototype.productId = 0;

        /**
         * VictronDevice modelName.
         * @member {string} modelName
         * @memberof victron_smartsolar_mppt.VictronDevice
         * @instance
         */
        VictronDevice.prototype.modelName = "";

        /**
         * VictronDevice firmwareVersion.
         * @member {string} firmwareVersion
         * @memberof victron_smartsolar_mppt.VictronDevice
         * @instance
         */
        VictronDevice.prototype.firmwareVersion = "";

        /**
         * VictronDevice deviceSerial.
         * @member {string} deviceSerial
         * @memberof victron_smartsolar_mppt.VictronDevice
         * @instance
         */
        VictronDevice.prototype.deviceSerial = "";

        /**
         * Creates a new VictronDevice instance using the specified properties.
         * @function create
         * @memberof victron_smartsolar_mppt.VictronDevice
         * @static
         * @param {victron_smartsolar_mppt.IVictronDevice=} [properties] Properties to set
         * @returns {victron_smartsolar_mppt.VictronDevice} VictronDevice instance
         */
        VictronDevice.create = function create(properties) {
            return new VictronDevice(properties);
        };

        /**
         * Encodes the specified VictronDevice message. Does not implicitly {@link victron_smartsolar_mppt.VictronDevice.verify|verify} messages.
         * @function encode
         * @memberof victron_smartsolar_mppt.VictronDevice
         * @static
         * @param {victron_smartsolar_mppt.IVictronDevice} message VictronDevice message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        VictronDevice.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.portName != null && Object.hasOwnProperty.call(message, "portName"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.portName);
            if (message.vid != null && Object.hasOwnProperty.call(message, "vid"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.vid);
            if (message.pid != null && Object.hasOwnProperty.call(message, "pid"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.pid);
            if (message.serialNumber != null && Object.hasOwnProperty.call(message, "serialNumber"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.serialNumber);
            if (message.manufacturer != null && Object.hasOwnProperty.call(message, "manufacturer"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.manufacturer);
            if (message.product != null && Object.hasOwnProperty.call(message, "product"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.product);
            if (message.portBaudRate != null && Object.hasOwnProperty.call(message, "portBaudRate"))
                writer.uint32(/* id 7, wireType 0 =*/56).uint32(message.portBaudRate);
            if (message.productId != null && Object.hasOwnProperty.call(message, "productId"))
                writer.uint32(/* id 8, wireType 0 =*/64).uint32(message.productId);
            if (message.modelName != null && Object.hasOwnProperty.call(message, "modelName"))
                writer.uint32(/* id 9, wireType 2 =*/74).string(message.modelName);
            if (message.firmwareVersion != null && Object.hasOwnProperty.call(message, "firmwareVersion"))
                writer.uint32(/* id 10, wireType 2 =*/82).string(message.firmwareVersion);
            if (message.deviceSerial != null && Object.hasOwnProperty.call(message, "deviceSerial"))
                writer.uint32(/* id 11, wireType 2 =*/90).string(message.deviceSerial);
            return writer;
        };

        /**
         * Encodes the specified VictronDevice message, length delimited. Does not implicitly {@link victron_smartsolar_mppt.VictronDevice.verify|verify} messages.
         * @function encodeDelimited
         * @memberof victron_smartsolar_mppt.VictronDevice
         * @static
         * @param {victron_smartsolar_mppt.IVictronDevice} message VictronDevice message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        VictronDevice.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a VictronDevice message from the specified reader or buffer.
         * @function decode
         * @memberof victron_smartsolar_mppt.VictronDevice
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {victron_smartsolar_mppt.VictronDevice} VictronDevice
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        VictronDevice.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.victron_smartsolar_mppt.VictronDevice();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.portName = reader.string();
                        break;
                    }
                case 2: {
                        message.vid = reader.uint32();
                        break;
                    }
                case 3: {
                        message.pid = reader.uint32();
                        break;
                    }
                case 4: {
                        message.serialNumber = reader.string();
                        break;
                    }
                case 5: {
                        message.manufacturer = reader.string();
                        break;
                    }
                case 6: {
                        message.product = reader.string();
                        break;
                    }
                case 7: {
                        message.portBaudRate = reader.uint32();
                        break;
                    }
                case 8: {
                        message.productId = reader.uint32();
                        break;
                    }
                case 9: {
                        message.modelName = reader.string();
                        break;
                    }
                case 10: {
                        message.firmwareVersion = reader.string();
                        break;
                    }
                case 11: {
                        message.deviceSerial = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a VictronDevice message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof victron_smartsolar_mppt.VictronDevice
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {victron_smartsolar_mppt.VictronDevice} VictronDevice
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        VictronDevice.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a VictronDevice message.
         * @function verify
         * @memberof victron_smartsolar_mppt.VictronDevice
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        VictronDevice.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.portName != null && message.hasOwnProperty("portName"))
                if (!$util.isString(message.portName))
                    return "portName: string expected";
            if (message.vid != null && message.hasOwnProperty("vid"))
                if (!$util.isInteger(message.vid))
                    return "vid: integer expected";
            if (message.pid != null && message.hasOwnProperty("pid"))
                if (!$util.isInteger(message.pid))
                    return "pid: integer expected";
            if (message.serialNumber != null && message.hasOwnProperty("serialNumber"))
                if (!$util.isString(message.serialNumber))
                    return "serialNumber: string expected";
            if (message.manufacturer != null && message.hasOwnProperty("manufacturer"))
                if (!$util.isString(message.manufacturer))
                    return "manufacturer: string expected";
            if (message.product != null && message.hasOwnProperty("product"))
                if (!$util.isString(message.product))
                    return "product: string expected";
            if (message.portBaudRate != null && message.hasOwnProperty("portBaudRate"))
                if (!$util.isInteger(message.portBaudRate))
                    return "portBaudRate: integer expected";
            if (message.productId != null && message.hasOwnProperty("productId"))
                if (!$util.isInteger(message.productId))
                    return "productId: integer expected";
            if (message.modelName != null && message.hasOwnProperty("modelName"))
                if (!$util.isString(message.modelName))
                    return "modelName: string expected";
            if (message.firmwareVersion != null && message.hasOwnProperty("firmwareVersion"))
                if (!$util.isString(message.firmwareVersion))
                    return "firmwareVersion: string expected";
            if (message.deviceSerial != null && message.hasOwnProperty("deviceSerial"))
                if (!$util.isString(message.deviceSerial))
                    return "deviceSerial: string expected";
            return null;
        };

        /**
         * Creates a VictronDevice message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof victron_smartsolar_mppt.VictronDevice
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {victron_smartsolar_mppt.VictronDevice} VictronDevice
         */
        VictronDevice.fromObject = function fromObject(object, long) {
            if (object instanceof $root.victron_smartsolar_mppt.VictronDevice)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.victron_smartsolar_mppt.VictronDevice();
            if (object.portName != null)
                message.portName = String(object.portName);
            if (object.vid != null)
                message.vid = object.vid >>> 0;
            if (object.pid != null)
                message.pid = object.pid >>> 0;
            if (object.serialNumber != null)
                message.serialNumber = String(object.serialNumber);
            if (object.manufacturer != null)
                message.manufacturer = String(object.manufacturer);
            if (object.product != null)
                message.product = String(object.product);
            if (object.portBaudRate != null)
                message.portBaudRate = object.portBaudRate >>> 0;
            if (object.productId != null)
                message.productId = object.productId >>> 0;
            if (object.modelName != null)
                message.modelName = String(object.modelName);
            if (object.firmwareVersion != null)
                message.firmwareVersion = String(object.firmwareVersion);
            if (object.deviceSerial != null)
                message.deviceSerial = String(object.deviceSerial);
            return message;
        };

        /**
         * Creates a plain object from a VictronDevice message. Also converts values to other types if specified.
         * @function toObject
         * @memberof victron_smartsolar_mppt.VictronDevice
         * @static
         * @param {victron_smartsolar_mppt.VictronDevice} message VictronDevice
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        VictronDevice.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.portName = "";
                object.vid = 0;
                object.pid = 0;
                object.serialNumber = "";
                object.manufacturer = "";
                object.product = "";
                object.portBaudRate = 0;
                object.productId = 0;
                object.modelName = "";
                object.firmwareVersion = "";
                object.deviceSerial = "";
            }
            if (message.portName != null && message.hasOwnProperty("portName"))
                object.portName = message.portName;
            if (message.vid != null && message.hasOwnProperty("vid"))
                object.vid = message.vid;
            if (message.pid != null && message.hasOwnProperty("pid"))
                object.pid = message.pid;
            if (message.serialNumber != null && message.hasOwnProperty("serialNumber"))
                object.serialNumber = message.serialNumber;
            if (message.manufacturer != null && message.hasOwnProperty("manufacturer"))
                object.manufacturer = message.manufacturer;
            if (message.product != null && message.hasOwnProperty("product"))
                object.product = message.product;
            if (message.portBaudRate != null && message.hasOwnProperty("portBaudRate"))
                object.portBaudRate = message.portBaudRate;
            if (message.productId != null && message.hasOwnProperty("productId"))
                object.productId = message.productId;
            if (message.modelName != null && message.hasOwnProperty("modelName"))
                object.modelName = message.modelName;
            if (message.firmwareVersion != null && message.hasOwnProperty("firmwareVersion"))
                object.firmwareVersion = message.firmwareVersion;
            if (message.deviceSerial != null && message.hasOwnProperty("deviceSerial"))
                object.deviceSerial = message.deviceSerial;
            return object;
        };

        /**
         * Converts this VictronDevice to JSON.
         * @function toJSON
         * @memberof victron_smartsolar_mppt.VictronDevice
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        VictronDevice.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for VictronDevice
         * @function getTypeUrl
         * @memberof victron_smartsolar_mppt.VictronDevice
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        VictronDevice.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/victron_smartsolar_mppt.VictronDevice";
        };

        return VictronDevice;
    })();

    victron_smartsolar_mppt.RxEnvelope = (function() {

        /**
         * Properties of a RxEnvelope.
         * @memberof victron_smartsolar_mppt
         * @interface IRxEnvelope
         * @property {Long|null} [monotonicStampNs] RxEnvelope monotonicStampNs
         * @property {Long|null} [localStampNs] RxEnvelope localStampNs
         * @property {Long|null} [appStartId] RxEnvelope appStartId
         * @property {victron_smartsolar_mppt.VictronSignalType|null} [signalType] RxEnvelope signalType
         * @property {victron_smartsolar_mppt.IVictronDevice|null} [device] RxEnvelope device
         * @property {Uint8Array|null} [data] RxEnvelope data
         * @property {Uint8Array|null} [hexFrame] RxEnvelope hexFrame
         * @property {Array.<Uint8Array>|null} [hexFrames] RxEnvelope hexFrames
         * @property {string|null} [error] RxEnvelope error
         */

        /**
         * Constructs a new RxEnvelope.
         * @memberof victron_smartsolar_mppt
         * @classdesc Represents a RxEnvelope.
         * @implements IRxEnvelope
         * @constructor
         * @param {victron_smartsolar_mppt.IRxEnvelope=} [properties] Properties to set
         */
        function RxEnvelope(properties) {
            this.hexFrames = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RxEnvelope monotonicStampNs.
         * @member {Long} monotonicStampNs
         * @memberof victron_smartsolar_mppt.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.monotonicStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * RxEnvelope localStampNs.
         * @member {Long} localStampNs
         * @memberof victron_smartsolar_mppt.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.localStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * RxEnvelope appStartId.
         * @member {Long} appStartId
         * @memberof victron_smartsolar_mppt.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.appStartId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * RxEnvelope signalType.
         * @member {victron_smartsolar_mppt.VictronSignalType} signalType
         * @memberof victron_smartsolar_mppt.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.signalType = 0;

        /**
         * RxEnvelope device.
         * @member {victron_smartsolar_mppt.IVictronDevice|null|undefined} device
         * @memberof victron_smartsolar_mppt.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.device = null;

        /**
         * RxEnvelope data.
         * @member {Uint8Array} data
         * @memberof victron_smartsolar_mppt.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.data = $util.newBuffer([]);

        /**
         * RxEnvelope hexFrame.
         * @member {Uint8Array} hexFrame
         * @memberof victron_smartsolar_mppt.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.hexFrame = $util.newBuffer([]);

        /**
         * RxEnvelope hexFrames.
         * @member {Array.<Uint8Array>} hexFrames
         * @memberof victron_smartsolar_mppt.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.hexFrames = $util.emptyArray;

        /**
         * RxEnvelope error.
         * @member {string} error
         * @memberof victron_smartsolar_mppt.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.error = "";

        /**
         * Creates a new RxEnvelope instance using the specified properties.
         * @function create
         * @memberof victron_smartsolar_mppt.RxEnvelope
         * @static
         * @param {victron_smartsolar_mppt.IRxEnvelope=} [properties] Properties to set
         * @returns {victron_smartsolar_mppt.RxEnvelope} RxEnvelope instance
         */
        RxEnvelope.create = function create(properties) {
            return new RxEnvelope(properties);
        };

        /**
         * Encodes the specified RxEnvelope message. Does not implicitly {@link victron_smartsolar_mppt.RxEnvelope.verify|verify} messages.
         * @function encode
         * @memberof victron_smartsolar_mppt.RxEnvelope
         * @static
         * @param {victron_smartsolar_mppt.IRxEnvelope} message RxEnvelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RxEnvelope.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.monotonicStampNs != null && Object.hasOwnProperty.call(message, "monotonicStampNs"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.monotonicStampNs);
            if (message.localStampNs != null && Object.hasOwnProperty.call(message, "localStampNs"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.localStampNs);
            if (message.appStartId != null && Object.hasOwnProperty.call(message, "appStartId"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.appStartId);
            if (message.signalType != null && Object.hasOwnProperty.call(message, "signalType"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.signalType);
            if (message.device != null && Object.hasOwnProperty.call(message, "device"))
                $root.victron_smartsolar_mppt.VictronDevice.encode(message.device, writer.uint32(/* id 11, wireType 2 =*/90).fork()).ldelim();
            if (message.data != null && Object.hasOwnProperty.call(message, "data"))
                writer.uint32(/* id 20, wireType 2 =*/162).bytes(message.data);
            if (message.hexFrame != null && Object.hasOwnProperty.call(message, "hexFrame"))
                writer.uint32(/* id 21, wireType 2 =*/170).bytes(message.hexFrame);
            if (message.hexFrames != null && message.hexFrames.length)
                for (let i = 0; i < message.hexFrames.length; ++i)
                    writer.uint32(/* id 22, wireType 2 =*/178).bytes(message.hexFrames[i]);
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                writer.uint32(/* id 50, wireType 2 =*/402).string(message.error);
            return writer;
        };

        /**
         * Encodes the specified RxEnvelope message, length delimited. Does not implicitly {@link victron_smartsolar_mppt.RxEnvelope.verify|verify} messages.
         * @function encodeDelimited
         * @memberof victron_smartsolar_mppt.RxEnvelope
         * @static
         * @param {victron_smartsolar_mppt.IRxEnvelope} message RxEnvelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RxEnvelope.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RxEnvelope message from the specified reader or buffer.
         * @function decode
         * @memberof victron_smartsolar_mppt.RxEnvelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {victron_smartsolar_mppt.RxEnvelope} RxEnvelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RxEnvelope.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.victron_smartsolar_mppt.RxEnvelope();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.monotonicStampNs = reader.uint64();
                        break;
                    }
                case 2: {
                        message.localStampNs = reader.uint64();
                        break;
                    }
                case 3: {
                        message.appStartId = reader.uint64();
                        break;
                    }
                case 10: {
                        message.signalType = reader.int32();
                        break;
                    }
                case 11: {
                        message.device = $root.victron_smartsolar_mppt.VictronDevice.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 20: {
                        message.data = reader.bytes();
                        break;
                    }
                case 21: {
                        message.hexFrame = reader.bytes();
                        break;
                    }
                case 22: {
                        if (!(message.hexFrames && message.hexFrames.length))
                            message.hexFrames = [];
                        message.hexFrames.push(reader.bytes());
                        break;
                    }
                case 50: {
                        message.error = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RxEnvelope message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof victron_smartsolar_mppt.RxEnvelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {victron_smartsolar_mppt.RxEnvelope} RxEnvelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RxEnvelope.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RxEnvelope message.
         * @function verify
         * @memberof victron_smartsolar_mppt.RxEnvelope
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RxEnvelope.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (!$util.isInteger(message.monotonicStampNs) && !(message.monotonicStampNs && $util.isInteger(message.monotonicStampNs.low) && $util.isInteger(message.monotonicStampNs.high)))
                    return "monotonicStampNs: integer|Long expected";
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (!$util.isInteger(message.localStampNs) && !(message.localStampNs && $util.isInteger(message.localStampNs.low) && $util.isInteger(message.localStampNs.high)))
                    return "localStampNs: integer|Long expected";
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (!$util.isInteger(message.appStartId) && !(message.appStartId && $util.isInteger(message.appStartId.low) && $util.isInteger(message.appStartId.high)))
                    return "appStartId: integer|Long expected";
            if (message.signalType != null && message.hasOwnProperty("signalType"))
                switch (message.signalType) {
                default:
                    return "signalType: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                case 5:
                    break;
                }
            if (message.device != null && message.hasOwnProperty("device")) {
                let error = $root.victron_smartsolar_mppt.VictronDevice.verify(message.device, long + 1);
                if (error)
                    return "device." + error;
            }
            if (message.data != null && message.hasOwnProperty("data"))
                if (!(message.data && typeof message.data.length === "number" || $util.isString(message.data)))
                    return "data: buffer expected";
            if (message.hexFrame != null && message.hasOwnProperty("hexFrame"))
                if (!(message.hexFrame && typeof message.hexFrame.length === "number" || $util.isString(message.hexFrame)))
                    return "hexFrame: buffer expected";
            if (message.hexFrames != null && message.hasOwnProperty("hexFrames")) {
                if (!Array.isArray(message.hexFrames))
                    return "hexFrames: array expected";
                for (let i = 0; i < message.hexFrames.length; ++i)
                    if (!(message.hexFrames[i] && typeof message.hexFrames[i].length === "number" || $util.isString(message.hexFrames[i])))
                        return "hexFrames: buffer[] expected";
            }
            if (message.error != null && message.hasOwnProperty("error"))
                if (!$util.isString(message.error))
                    return "error: string expected";
            return null;
        };

        /**
         * Creates a RxEnvelope message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof victron_smartsolar_mppt.RxEnvelope
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {victron_smartsolar_mppt.RxEnvelope} RxEnvelope
         */
        RxEnvelope.fromObject = function fromObject(object, long) {
            if (object instanceof $root.victron_smartsolar_mppt.RxEnvelope)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.victron_smartsolar_mppt.RxEnvelope();
            if (object.monotonicStampNs != null)
                if ($util.Long)
                    (message.monotonicStampNs = $util.Long.fromValue(object.monotonicStampNs)).unsigned = true;
                else if (typeof object.monotonicStampNs === "string")
                    message.monotonicStampNs = parseInt(object.monotonicStampNs, 10);
                else if (typeof object.monotonicStampNs === "number")
                    message.monotonicStampNs = object.monotonicStampNs;
                else if (typeof object.monotonicStampNs === "object")
                    message.monotonicStampNs = new $util.LongBits(object.monotonicStampNs.low >>> 0, object.monotonicStampNs.high >>> 0).toNumber(true);
            if (object.localStampNs != null)
                if ($util.Long)
                    (message.localStampNs = $util.Long.fromValue(object.localStampNs)).unsigned = true;
                else if (typeof object.localStampNs === "string")
                    message.localStampNs = parseInt(object.localStampNs, 10);
                else if (typeof object.localStampNs === "number")
                    message.localStampNs = object.localStampNs;
                else if (typeof object.localStampNs === "object")
                    message.localStampNs = new $util.LongBits(object.localStampNs.low >>> 0, object.localStampNs.high >>> 0).toNumber(true);
            if (object.appStartId != null)
                if ($util.Long)
                    (message.appStartId = $util.Long.fromValue(object.appStartId)).unsigned = true;
                else if (typeof object.appStartId === "string")
                    message.appStartId = parseInt(object.appStartId, 10);
                else if (typeof object.appStartId === "number")
                    message.appStartId = object.appStartId;
                else if (typeof object.appStartId === "object")
                    message.appStartId = new $util.LongBits(object.appStartId.low >>> 0, object.appStartId.high >>> 0).toNumber(true);
            switch (object.signalType) {
            default:
                if (typeof object.signalType === "number") {
                    message.signalType = object.signalType;
                    break;
                }
                break;
            case "VICTRON_SIGNAL_TYPE_UNSPECIFIED":
            case 0:
                message.signalType = 0;
                break;
            case "VICTRON_CONNECTED":
            case 1:
                message.signalType = 1;
                break;
            case "VICTRON_DISCONNECTED":
            case 2:
                message.signalType = 2;
                break;
            case "VICTRON_TEXT_BLOCK":
            case 3:
                message.signalType = 3;
                break;
            case "VICTRON_HEX_FRAME":
            case 4:
                message.signalType = 4;
                break;
            case "VICTRON_ERROR":
            case 5:
                message.signalType = 5;
                break;
            }
            if (object.device != null) {
                if (typeof object.device !== "object")
                    throw TypeError(".victron_smartsolar_mppt.RxEnvelope.device: object expected");
                message.device = $root.victron_smartsolar_mppt.VictronDevice.fromObject(object.device, long + 1);
            }
            if (object.data != null)
                if (typeof object.data === "string")
                    $util.base64.decode(object.data, message.data = $util.newBuffer($util.base64.length(object.data)), 0);
                else if (object.data.length >= 0)
                    message.data = object.data;
            if (object.hexFrame != null)
                if (typeof object.hexFrame === "string")
                    $util.base64.decode(object.hexFrame, message.hexFrame = $util.newBuffer($util.base64.length(object.hexFrame)), 0);
                else if (object.hexFrame.length >= 0)
                    message.hexFrame = object.hexFrame;
            if (object.hexFrames) {
                if (!Array.isArray(object.hexFrames))
                    throw TypeError(".victron_smartsolar_mppt.RxEnvelope.hexFrames: array expected");
                message.hexFrames = [];
                for (let i = 0; i < object.hexFrames.length; ++i)
                    if (typeof object.hexFrames[i] === "string")
                        $util.base64.decode(object.hexFrames[i], message.hexFrames[i] = $util.newBuffer($util.base64.length(object.hexFrames[i])), 0);
                    else if (object.hexFrames[i].length >= 0)
                        message.hexFrames[i] = object.hexFrames[i];
            }
            if (object.error != null)
                message.error = String(object.error);
            return message;
        };

        /**
         * Creates a plain object from a RxEnvelope message. Also converts values to other types if specified.
         * @function toObject
         * @memberof victron_smartsolar_mppt.RxEnvelope
         * @static
         * @param {victron_smartsolar_mppt.RxEnvelope} message RxEnvelope
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RxEnvelope.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.hexFrames = [];
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.monotonicStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.monotonicStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.localStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.localStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.appStartId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.appStartId = options.longs === String ? "0" : 0;
                object.signalType = options.enums === String ? "VICTRON_SIGNAL_TYPE_UNSPECIFIED" : 0;
                object.device = null;
                if (options.bytes === String)
                    object.data = "";
                else {
                    object.data = [];
                    if (options.bytes !== Array)
                        object.data = $util.newBuffer(object.data);
                }
                if (options.bytes === String)
                    object.hexFrame = "";
                else {
                    object.hexFrame = [];
                    if (options.bytes !== Array)
                        object.hexFrame = $util.newBuffer(object.hexFrame);
                }
                object.error = "";
            }
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (typeof message.monotonicStampNs === "number")
                    object.monotonicStampNs = options.longs === String ? String(message.monotonicStampNs) : message.monotonicStampNs;
                else
                    object.monotonicStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.monotonicStampNs) : options.longs === Number ? new $util.LongBits(message.monotonicStampNs.low >>> 0, message.monotonicStampNs.high >>> 0).toNumber(true) : message.monotonicStampNs;
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (typeof message.localStampNs === "number")
                    object.localStampNs = options.longs === String ? String(message.localStampNs) : message.localStampNs;
                else
                    object.localStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.localStampNs) : options.longs === Number ? new $util.LongBits(message.localStampNs.low >>> 0, message.localStampNs.high >>> 0).toNumber(true) : message.localStampNs;
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (typeof message.appStartId === "number")
                    object.appStartId = options.longs === String ? String(message.appStartId) : message.appStartId;
                else
                    object.appStartId = options.longs === String ? $util.Long.prototype.toString.call(message.appStartId) : options.longs === Number ? new $util.LongBits(message.appStartId.low >>> 0, message.appStartId.high >>> 0).toNumber(true) : message.appStartId;
            if (message.signalType != null && message.hasOwnProperty("signalType"))
                object.signalType = options.enums === String ? $root.victron_smartsolar_mppt.VictronSignalType[message.signalType] === undefined ? message.signalType : $root.victron_smartsolar_mppt.VictronSignalType[message.signalType] : message.signalType;
            if (message.device != null && message.hasOwnProperty("device"))
                object.device = $root.victron_smartsolar_mppt.VictronDevice.toObject(message.device, options);
            if (message.data != null && message.hasOwnProperty("data"))
                object.data = options.bytes === String ? $util.base64.encode(message.data, 0, message.data.length) : options.bytes === Array ? Array.prototype.slice.call(message.data) : message.data;
            if (message.hexFrame != null && message.hasOwnProperty("hexFrame"))
                object.hexFrame = options.bytes === String ? $util.base64.encode(message.hexFrame, 0, message.hexFrame.length) : options.bytes === Array ? Array.prototype.slice.call(message.hexFrame) : message.hexFrame;
            if (message.hexFrames && message.hexFrames.length) {
                object.hexFrames = [];
                for (let j = 0; j < message.hexFrames.length; ++j)
                    object.hexFrames[j] = options.bytes === String ? $util.base64.encode(message.hexFrames[j], 0, message.hexFrames[j].length) : options.bytes === Array ? Array.prototype.slice.call(message.hexFrames[j]) : message.hexFrames[j];
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = message.error;
            return object;
        };

        /**
         * Converts this RxEnvelope to JSON.
         * @function toJSON
         * @memberof victron_smartsolar_mppt.RxEnvelope
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RxEnvelope.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RxEnvelope
         * @function getTypeUrl
         * @memberof victron_smartsolar_mppt.RxEnvelope
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RxEnvelope.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/victron_smartsolar_mppt.RxEnvelope";
        };

        return RxEnvelope;
    })();

    return victron_smartsolar_mppt;
})();

export const pwm_output = $root.pwm_output = (() => {

    /**
     * Namespace pwm_output.
     * @exports pwm_output
     * @namespace
     */
    const pwm_output = {};

    /**
     * PwmOutputSignalType enum.
     * @name pwm_output.PwmOutputSignalType
     * @enum {number}
     * @property {number} PWM_OUTPUT_SIGNAL_TYPE_UNSPECIFIED=0 PWM_OUTPUT_SIGNAL_TYPE_UNSPECIFIED value
     * @property {number} PWM_OUTPUT_CONFIGURED=1 PWM_OUTPUT_CONFIGURED value
     * @property {number} PWM_OUTPUT_COMMAND=2 PWM_OUTPUT_COMMAND value
     * @property {number} PWM_OUTPUT_COMMAND_SUCCESS=3 PWM_OUTPUT_COMMAND_SUCCESS value
     * @property {number} PWM_OUTPUT_COMMAND_REJECTED=4 PWM_OUTPUT_COMMAND_REJECTED value
     * @property {number} PWM_OUTPUT_COMMAND_FAILED=5 PWM_OUTPUT_COMMAND_FAILED value
     * @property {number} PWM_OUTPUT_ERROR=6 PWM_OUTPUT_ERROR value
     */
    pwm_output.PwmOutputSignalType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "PWM_OUTPUT_SIGNAL_TYPE_UNSPECIFIED"] = 0;
        values[valuesById[1] = "PWM_OUTPUT_CONFIGURED"] = 1;
        values[valuesById[2] = "PWM_OUTPUT_COMMAND"] = 2;
        values[valuesById[3] = "PWM_OUTPUT_COMMAND_SUCCESS"] = 3;
        values[valuesById[4] = "PWM_OUTPUT_COMMAND_REJECTED"] = 4;
        values[valuesById[5] = "PWM_OUTPUT_COMMAND_FAILED"] = 5;
        values[valuesById[6] = "PWM_OUTPUT_ERROR"] = 6;
        return values;
    })();

    pwm_output.PwmOutputDevice = (function() {

        /**
         * Properties of a PwmOutputDevice.
         * @memberof pwm_output
         * @interface IPwmOutputDevice
         * @property {string|null} [id] PwmOutputDevice id
         */

        /**
         * Constructs a new PwmOutputDevice.
         * @memberof pwm_output
         * @classdesc Represents a PwmOutputDevice.
         * @implements IPwmOutputDevice
         * @constructor
         * @param {pwm_output.IPwmOutputDevice=} [properties] Properties to set
         */
        function PwmOutputDevice(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PwmOutputDevice id.
         * @member {string} id
         * @memberof pwm_output.PwmOutputDevice
         * @instance
         */
        PwmOutputDevice.prototype.id = "";

        /**
         * Creates a new PwmOutputDevice instance using the specified properties.
         * @function create
         * @memberof pwm_output.PwmOutputDevice
         * @static
         * @param {pwm_output.IPwmOutputDevice=} [properties] Properties to set
         * @returns {pwm_output.PwmOutputDevice} PwmOutputDevice instance
         */
        PwmOutputDevice.create = function create(properties) {
            return new PwmOutputDevice(properties);
        };

        /**
         * Encodes the specified PwmOutputDevice message. Does not implicitly {@link pwm_output.PwmOutputDevice.verify|verify} messages.
         * @function encode
         * @memberof pwm_output.PwmOutputDevice
         * @static
         * @param {pwm_output.IPwmOutputDevice} message PwmOutputDevice message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PwmOutputDevice.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.id);
            return writer;
        };

        /**
         * Encodes the specified PwmOutputDevice message, length delimited. Does not implicitly {@link pwm_output.PwmOutputDevice.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pwm_output.PwmOutputDevice
         * @static
         * @param {pwm_output.IPwmOutputDevice} message PwmOutputDevice message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PwmOutputDevice.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a PwmOutputDevice message from the specified reader or buffer.
         * @function decode
         * @memberof pwm_output.PwmOutputDevice
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pwm_output.PwmOutputDevice} PwmOutputDevice
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PwmOutputDevice.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pwm_output.PwmOutputDevice();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.id = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a PwmOutputDevice message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pwm_output.PwmOutputDevice
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pwm_output.PwmOutputDevice} PwmOutputDevice
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PwmOutputDevice.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PwmOutputDevice message.
         * @function verify
         * @memberof pwm_output.PwmOutputDevice
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PwmOutputDevice.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isString(message.id))
                    return "id: string expected";
            return null;
        };

        /**
         * Creates a PwmOutputDevice message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pwm_output.PwmOutputDevice
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pwm_output.PwmOutputDevice} PwmOutputDevice
         */
        PwmOutputDevice.fromObject = function fromObject(object, long) {
            if (object instanceof $root.pwm_output.PwmOutputDevice)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.pwm_output.PwmOutputDevice();
            if (object.id != null)
                message.id = String(object.id);
            return message;
        };

        /**
         * Creates a plain object from a PwmOutputDevice message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pwm_output.PwmOutputDevice
         * @static
         * @param {pwm_output.PwmOutputDevice} message PwmOutputDevice
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PwmOutputDevice.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.id = "";
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            return object;
        };

        /**
         * Converts this PwmOutputDevice to JSON.
         * @function toJSON
         * @memberof pwm_output.PwmOutputDevice
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PwmOutputDevice.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PwmOutputDevice
         * @function getTypeUrl
         * @memberof pwm_output.PwmOutputDevice
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PwmOutputDevice.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pwm_output.PwmOutputDevice";
        };

        return PwmOutputDevice;
    })();

    pwm_output.OutputState = (function() {

        /**
         * Properties of an OutputState.
         * @memberof pwm_output
         * @interface IOutputState
         * @property {string|null} [id] OutputState id
         * @property {boolean|null} [enabled] OutputState enabled
         * @property {pwm_output.IWaveCommand|null} [wave] OutputState wave
         */

        /**
         * Constructs a new OutputState.
         * @memberof pwm_output
         * @classdesc Represents an OutputState.
         * @implements IOutputState
         * @constructor
         * @param {pwm_output.IOutputState=} [properties] Properties to set
         */
        function OutputState(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * OutputState id.
         * @member {string} id
         * @memberof pwm_output.OutputState
         * @instance
         */
        OutputState.prototype.id = "";

        /**
         * OutputState enabled.
         * @member {boolean} enabled
         * @memberof pwm_output.OutputState
         * @instance
         */
        OutputState.prototype.enabled = false;

        /**
         * OutputState wave.
         * @member {pwm_output.IWaveCommand|null|undefined} wave
         * @memberof pwm_output.OutputState
         * @instance
         */
        OutputState.prototype.wave = null;

        /**
         * Creates a new OutputState instance using the specified properties.
         * @function create
         * @memberof pwm_output.OutputState
         * @static
         * @param {pwm_output.IOutputState=} [properties] Properties to set
         * @returns {pwm_output.OutputState} OutputState instance
         */
        OutputState.create = function create(properties) {
            return new OutputState(properties);
        };

        /**
         * Encodes the specified OutputState message. Does not implicitly {@link pwm_output.OutputState.verify|verify} messages.
         * @function encode
         * @memberof pwm_output.OutputState
         * @static
         * @param {pwm_output.IOutputState} message OutputState message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        OutputState.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.id);
            if (message.enabled != null && Object.hasOwnProperty.call(message, "enabled"))
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.enabled);
            if (message.wave != null && Object.hasOwnProperty.call(message, "wave"))
                $root.pwm_output.WaveCommand.encode(message.wave, writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified OutputState message, length delimited. Does not implicitly {@link pwm_output.OutputState.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pwm_output.OutputState
         * @static
         * @param {pwm_output.IOutputState} message OutputState message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        OutputState.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an OutputState message from the specified reader or buffer.
         * @function decode
         * @memberof pwm_output.OutputState
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pwm_output.OutputState} OutputState
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        OutputState.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pwm_output.OutputState();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.id = reader.string();
                        break;
                    }
                case 2: {
                        message.enabled = reader.bool();
                        break;
                    }
                case 10: {
                        message.wave = $root.pwm_output.WaveCommand.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an OutputState message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pwm_output.OutputState
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pwm_output.OutputState} OutputState
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        OutputState.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an OutputState message.
         * @function verify
         * @memberof pwm_output.OutputState
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        OutputState.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isString(message.id))
                    return "id: string expected";
            if (message.enabled != null && message.hasOwnProperty("enabled"))
                if (typeof message.enabled !== "boolean")
                    return "enabled: boolean expected";
            if (message.wave != null && message.hasOwnProperty("wave")) {
                let error = $root.pwm_output.WaveCommand.verify(message.wave, long + 1);
                if (error)
                    return "wave." + error;
            }
            return null;
        };

        /**
         * Creates an OutputState message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pwm_output.OutputState
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pwm_output.OutputState} OutputState
         */
        OutputState.fromObject = function fromObject(object, long) {
            if (object instanceof $root.pwm_output.OutputState)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.pwm_output.OutputState();
            if (object.id != null)
                message.id = String(object.id);
            if (object.enabled != null)
                message.enabled = Boolean(object.enabled);
            if (object.wave != null) {
                if (typeof object.wave !== "object")
                    throw TypeError(".pwm_output.OutputState.wave: object expected");
                message.wave = $root.pwm_output.WaveCommand.fromObject(object.wave, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from an OutputState message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pwm_output.OutputState
         * @static
         * @param {pwm_output.OutputState} message OutputState
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        OutputState.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.id = "";
                object.enabled = false;
                object.wave = null;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.enabled != null && message.hasOwnProperty("enabled"))
                object.enabled = message.enabled;
            if (message.wave != null && message.hasOwnProperty("wave"))
                object.wave = $root.pwm_output.WaveCommand.toObject(message.wave, options);
            return object;
        };

        /**
         * Converts this OutputState to JSON.
         * @function toJSON
         * @memberof pwm_output.OutputState
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        OutputState.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for OutputState
         * @function getTypeUrl
         * @memberof pwm_output.OutputState
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        OutputState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pwm_output.OutputState";
        };

        return OutputState;
    })();

    /**
     * WaveLevel enum.
     * @name pwm_output.WaveLevel
     * @enum {number}
     * @property {number} WAVE_LEVEL_UNSPECIFIED=0 WAVE_LEVEL_UNSPECIFIED value
     * @property {number} WAVE_LEVEL_LOW=1 WAVE_LEVEL_LOW value
     * @property {number} WAVE_LEVEL_HIGH=2 WAVE_LEVEL_HIGH value
     */
    pwm_output.WaveLevel = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "WAVE_LEVEL_UNSPECIFIED"] = 0;
        values[valuesById[1] = "WAVE_LEVEL_LOW"] = 1;
        values[valuesById[2] = "WAVE_LEVEL_HIGH"] = 2;
        return values;
    })();

    pwm_output.WaveSegment = (function() {

        /**
         * Properties of a WaveSegment.
         * @memberof pwm_output
         * @interface IWaveSegment
         * @property {pwm_output.WaveLevel|null} [level] WaveSegment level
         * @property {number|null} [durationUs] WaveSegment durationUs
         */

        /**
         * Constructs a new WaveSegment.
         * @memberof pwm_output
         * @classdesc Represents a WaveSegment.
         * @implements IWaveSegment
         * @constructor
         * @param {pwm_output.IWaveSegment=} [properties] Properties to set
         */
        function WaveSegment(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * WaveSegment level.
         * @member {pwm_output.WaveLevel} level
         * @memberof pwm_output.WaveSegment
         * @instance
         */
        WaveSegment.prototype.level = 0;

        /**
         * WaveSegment durationUs.
         * @member {number} durationUs
         * @memberof pwm_output.WaveSegment
         * @instance
         */
        WaveSegment.prototype.durationUs = 0;

        /**
         * Creates a new WaveSegment instance using the specified properties.
         * @function create
         * @memberof pwm_output.WaveSegment
         * @static
         * @param {pwm_output.IWaveSegment=} [properties] Properties to set
         * @returns {pwm_output.WaveSegment} WaveSegment instance
         */
        WaveSegment.create = function create(properties) {
            return new WaveSegment(properties);
        };

        /**
         * Encodes the specified WaveSegment message. Does not implicitly {@link pwm_output.WaveSegment.verify|verify} messages.
         * @function encode
         * @memberof pwm_output.WaveSegment
         * @static
         * @param {pwm_output.IWaveSegment} message WaveSegment message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WaveSegment.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.level);
            if (message.durationUs != null && Object.hasOwnProperty.call(message, "durationUs"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.durationUs);
            return writer;
        };

        /**
         * Encodes the specified WaveSegment message, length delimited. Does not implicitly {@link pwm_output.WaveSegment.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pwm_output.WaveSegment
         * @static
         * @param {pwm_output.IWaveSegment} message WaveSegment message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WaveSegment.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a WaveSegment message from the specified reader or buffer.
         * @function decode
         * @memberof pwm_output.WaveSegment
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pwm_output.WaveSegment} WaveSegment
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WaveSegment.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pwm_output.WaveSegment();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.level = reader.int32();
                        break;
                    }
                case 2: {
                        message.durationUs = reader.uint32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a WaveSegment message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pwm_output.WaveSegment
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pwm_output.WaveSegment} WaveSegment
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WaveSegment.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a WaveSegment message.
         * @function verify
         * @memberof pwm_output.WaveSegment
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        WaveSegment.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.level != null && message.hasOwnProperty("level"))
                switch (message.level) {
                default:
                    return "level: enum value expected";
                case 0:
                case 1:
                case 2:
                    break;
                }
            if (message.durationUs != null && message.hasOwnProperty("durationUs"))
                if (!$util.isInteger(message.durationUs))
                    return "durationUs: integer expected";
            return null;
        };

        /**
         * Creates a WaveSegment message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pwm_output.WaveSegment
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pwm_output.WaveSegment} WaveSegment
         */
        WaveSegment.fromObject = function fromObject(object, long) {
            if (object instanceof $root.pwm_output.WaveSegment)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.pwm_output.WaveSegment();
            switch (object.level) {
            default:
                if (typeof object.level === "number") {
                    message.level = object.level;
                    break;
                }
                break;
            case "WAVE_LEVEL_UNSPECIFIED":
            case 0:
                message.level = 0;
                break;
            case "WAVE_LEVEL_LOW":
            case 1:
                message.level = 1;
                break;
            case "WAVE_LEVEL_HIGH":
            case 2:
                message.level = 2;
                break;
            }
            if (object.durationUs != null)
                message.durationUs = object.durationUs >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a WaveSegment message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pwm_output.WaveSegment
         * @static
         * @param {pwm_output.WaveSegment} message WaveSegment
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        WaveSegment.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.level = options.enums === String ? "WAVE_LEVEL_UNSPECIFIED" : 0;
                object.durationUs = 0;
            }
            if (message.level != null && message.hasOwnProperty("level"))
                object.level = options.enums === String ? $root.pwm_output.WaveLevel[message.level] === undefined ? message.level : $root.pwm_output.WaveLevel[message.level] : message.level;
            if (message.durationUs != null && message.hasOwnProperty("durationUs"))
                object.durationUs = message.durationUs;
            return object;
        };

        /**
         * Converts this WaveSegment to JSON.
         * @function toJSON
         * @memberof pwm_output.WaveSegment
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        WaveSegment.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for WaveSegment
         * @function getTypeUrl
         * @memberof pwm_output.WaveSegment
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        WaveSegment.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pwm_output.WaveSegment";
        };

        return WaveSegment;
    })();

    pwm_output.WaveCommand = (function() {

        /**
         * Properties of a WaveCommand.
         * @memberof pwm_output
         * @interface IWaveCommand
         * @property {number|null} [channel] WaveCommand channel
         * @property {Array.<pwm_output.IWaveSegment>|null} [segments] WaveCommand segments
         * @property {number|null} [repeat] WaveCommand repeat
         */

        /**
         * Constructs a new WaveCommand.
         * @memberof pwm_output
         * @classdesc Represents a WaveCommand.
         * @implements IWaveCommand
         * @constructor
         * @param {pwm_output.IWaveCommand=} [properties] Properties to set
         */
        function WaveCommand(properties) {
            this.segments = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * WaveCommand channel.
         * @member {number} channel
         * @memberof pwm_output.WaveCommand
         * @instance
         */
        WaveCommand.prototype.channel = 0;

        /**
         * WaveCommand segments.
         * @member {Array.<pwm_output.IWaveSegment>} segments
         * @memberof pwm_output.WaveCommand
         * @instance
         */
        WaveCommand.prototype.segments = $util.emptyArray;

        /**
         * WaveCommand repeat.
         * @member {number} repeat
         * @memberof pwm_output.WaveCommand
         * @instance
         */
        WaveCommand.prototype.repeat = 0;

        /**
         * Creates a new WaveCommand instance using the specified properties.
         * @function create
         * @memberof pwm_output.WaveCommand
         * @static
         * @param {pwm_output.IWaveCommand=} [properties] Properties to set
         * @returns {pwm_output.WaveCommand} WaveCommand instance
         */
        WaveCommand.create = function create(properties) {
            return new WaveCommand(properties);
        };

        /**
         * Encodes the specified WaveCommand message. Does not implicitly {@link pwm_output.WaveCommand.verify|verify} messages.
         * @function encode
         * @memberof pwm_output.WaveCommand
         * @static
         * @param {pwm_output.IWaveCommand} message WaveCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WaveCommand.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.channel != null && Object.hasOwnProperty.call(message, "channel"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.channel);
            if (message.segments != null && message.segments.length)
                for (let i = 0; i < message.segments.length; ++i)
                    $root.pwm_output.WaveSegment.encode(message.segments[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.repeat != null && Object.hasOwnProperty.call(message, "repeat"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.repeat);
            return writer;
        };

        /**
         * Encodes the specified WaveCommand message, length delimited. Does not implicitly {@link pwm_output.WaveCommand.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pwm_output.WaveCommand
         * @static
         * @param {pwm_output.IWaveCommand} message WaveCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WaveCommand.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a WaveCommand message from the specified reader or buffer.
         * @function decode
         * @memberof pwm_output.WaveCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pwm_output.WaveCommand} WaveCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WaveCommand.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pwm_output.WaveCommand();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.channel = reader.uint32();
                        break;
                    }
                case 2: {
                        if (!(message.segments && message.segments.length))
                            message.segments = [];
                        message.segments.push($root.pwm_output.WaveSegment.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 3: {
                        message.repeat = reader.uint32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a WaveCommand message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pwm_output.WaveCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pwm_output.WaveCommand} WaveCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WaveCommand.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a WaveCommand message.
         * @function verify
         * @memberof pwm_output.WaveCommand
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        WaveCommand.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.channel != null && message.hasOwnProperty("channel"))
                if (!$util.isInteger(message.channel))
                    return "channel: integer expected";
            if (message.segments != null && message.hasOwnProperty("segments")) {
                if (!Array.isArray(message.segments))
                    return "segments: array expected";
                for (let i = 0; i < message.segments.length; ++i) {
                    let error = $root.pwm_output.WaveSegment.verify(message.segments[i], long + 1);
                    if (error)
                        return "segments." + error;
                }
            }
            if (message.repeat != null && message.hasOwnProperty("repeat"))
                if (!$util.isInteger(message.repeat))
                    return "repeat: integer expected";
            return null;
        };

        /**
         * Creates a WaveCommand message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pwm_output.WaveCommand
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pwm_output.WaveCommand} WaveCommand
         */
        WaveCommand.fromObject = function fromObject(object, long) {
            if (object instanceof $root.pwm_output.WaveCommand)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.pwm_output.WaveCommand();
            if (object.channel != null)
                message.channel = object.channel >>> 0;
            if (object.segments) {
                if (!Array.isArray(object.segments))
                    throw TypeError(".pwm_output.WaveCommand.segments: array expected");
                message.segments = [];
                for (let i = 0; i < object.segments.length; ++i) {
                    if (typeof object.segments[i] !== "object")
                        throw TypeError(".pwm_output.WaveCommand.segments: object expected");
                    message.segments[i] = $root.pwm_output.WaveSegment.fromObject(object.segments[i], long + 1);
                }
            }
            if (object.repeat != null)
                message.repeat = object.repeat >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a WaveCommand message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pwm_output.WaveCommand
         * @static
         * @param {pwm_output.WaveCommand} message WaveCommand
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        WaveCommand.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.segments = [];
            if (options.defaults) {
                object.channel = 0;
                object.repeat = 0;
            }
            if (message.channel != null && message.hasOwnProperty("channel"))
                object.channel = message.channel;
            if (message.segments && message.segments.length) {
                object.segments = [];
                for (let j = 0; j < message.segments.length; ++j)
                    object.segments[j] = $root.pwm_output.WaveSegment.toObject(message.segments[j], options);
            }
            if (message.repeat != null && message.hasOwnProperty("repeat"))
                object.repeat = message.repeat;
            return object;
        };

        /**
         * Converts this WaveCommand to JSON.
         * @function toJSON
         * @memberof pwm_output.WaveCommand
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        WaveCommand.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for WaveCommand
         * @function getTypeUrl
         * @memberof pwm_output.WaveCommand
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        WaveCommand.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pwm_output.WaveCommand";
        };

        return WaveCommand;
    })();

    pwm_output.DisableCommand = (function() {

        /**
         * Properties of a DisableCommand.
         * @memberof pwm_output
         * @interface IDisableCommand
         * @property {number|null} [channel] DisableCommand channel
         */

        /**
         * Constructs a new DisableCommand.
         * @memberof pwm_output
         * @classdesc Represents a DisableCommand.
         * @implements IDisableCommand
         * @constructor
         * @param {pwm_output.IDisableCommand=} [properties] Properties to set
         */
        function DisableCommand(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * DisableCommand channel.
         * @member {number} channel
         * @memberof pwm_output.DisableCommand
         * @instance
         */
        DisableCommand.prototype.channel = 0;

        /**
         * Creates a new DisableCommand instance using the specified properties.
         * @function create
         * @memberof pwm_output.DisableCommand
         * @static
         * @param {pwm_output.IDisableCommand=} [properties] Properties to set
         * @returns {pwm_output.DisableCommand} DisableCommand instance
         */
        DisableCommand.create = function create(properties) {
            return new DisableCommand(properties);
        };

        /**
         * Encodes the specified DisableCommand message. Does not implicitly {@link pwm_output.DisableCommand.verify|verify} messages.
         * @function encode
         * @memberof pwm_output.DisableCommand
         * @static
         * @param {pwm_output.IDisableCommand} message DisableCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DisableCommand.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.channel != null && Object.hasOwnProperty.call(message, "channel"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.channel);
            return writer;
        };

        /**
         * Encodes the specified DisableCommand message, length delimited. Does not implicitly {@link pwm_output.DisableCommand.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pwm_output.DisableCommand
         * @static
         * @param {pwm_output.IDisableCommand} message DisableCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DisableCommand.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a DisableCommand message from the specified reader or buffer.
         * @function decode
         * @memberof pwm_output.DisableCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pwm_output.DisableCommand} DisableCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DisableCommand.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pwm_output.DisableCommand();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.channel = reader.uint32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a DisableCommand message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pwm_output.DisableCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pwm_output.DisableCommand} DisableCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DisableCommand.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a DisableCommand message.
         * @function verify
         * @memberof pwm_output.DisableCommand
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        DisableCommand.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.channel != null && message.hasOwnProperty("channel"))
                if (!$util.isInteger(message.channel))
                    return "channel: integer expected";
            return null;
        };

        /**
         * Creates a DisableCommand message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pwm_output.DisableCommand
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pwm_output.DisableCommand} DisableCommand
         */
        DisableCommand.fromObject = function fromObject(object, long) {
            if (object instanceof $root.pwm_output.DisableCommand)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.pwm_output.DisableCommand();
            if (object.channel != null)
                message.channel = object.channel >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a DisableCommand message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pwm_output.DisableCommand
         * @static
         * @param {pwm_output.DisableCommand} message DisableCommand
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DisableCommand.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.channel = 0;
            if (message.channel != null && message.hasOwnProperty("channel"))
                object.channel = message.channel;
            return object;
        };

        /**
         * Converts this DisableCommand to JSON.
         * @function toJSON
         * @memberof pwm_output.DisableCommand
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DisableCommand.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for DisableCommand
         * @function getTypeUrl
         * @memberof pwm_output.DisableCommand
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        DisableCommand.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pwm_output.DisableCommand";
        };

        return DisableCommand;
    })();

    pwm_output.Command = (function() {

        /**
         * Properties of a Command.
         * @memberof pwm_output
         * @interface ICommand
         * @property {string|null} [targetOutputId] Command targetOutputId
         * @property {pwm_output.IWaveCommand|null} [wave] Command wave
         * @property {pwm_output.IDisableCommand|null} [disable] Command disable
         */

        /**
         * Constructs a new Command.
         * @memberof pwm_output
         * @classdesc Represents a Command.
         * @implements ICommand
         * @constructor
         * @param {pwm_output.ICommand=} [properties] Properties to set
         */
        function Command(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Command targetOutputId.
         * @member {string} targetOutputId
         * @memberof pwm_output.Command
         * @instance
         */
        Command.prototype.targetOutputId = "";

        /**
         * Command wave.
         * @member {pwm_output.IWaveCommand|null|undefined} wave
         * @memberof pwm_output.Command
         * @instance
         */
        Command.prototype.wave = null;

        /**
         * Command disable.
         * @member {pwm_output.IDisableCommand|null|undefined} disable
         * @memberof pwm_output.Command
         * @instance
         */
        Command.prototype.disable = null;

        /**
         * Creates a new Command instance using the specified properties.
         * @function create
         * @memberof pwm_output.Command
         * @static
         * @param {pwm_output.ICommand=} [properties] Properties to set
         * @returns {pwm_output.Command} Command instance
         */
        Command.create = function create(properties) {
            return new Command(properties);
        };

        /**
         * Encodes the specified Command message. Does not implicitly {@link pwm_output.Command.verify|verify} messages.
         * @function encode
         * @memberof pwm_output.Command
         * @static
         * @param {pwm_output.ICommand} message Command message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Command.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.targetOutputId != null && Object.hasOwnProperty.call(message, "targetOutputId"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.targetOutputId);
            if (message.wave != null && Object.hasOwnProperty.call(message, "wave"))
                $root.pwm_output.WaveCommand.encode(message.wave, writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            if (message.disable != null && Object.hasOwnProperty.call(message, "disable"))
                $root.pwm_output.DisableCommand.encode(message.disable, writer.uint32(/* id 11, wireType 2 =*/90).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified Command message, length delimited. Does not implicitly {@link pwm_output.Command.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pwm_output.Command
         * @static
         * @param {pwm_output.ICommand} message Command message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Command.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Command message from the specified reader or buffer.
         * @function decode
         * @memberof pwm_output.Command
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pwm_output.Command} Command
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Command.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pwm_output.Command();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.targetOutputId = reader.string();
                        break;
                    }
                case 10: {
                        message.wave = $root.pwm_output.WaveCommand.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 11: {
                        message.disable = $root.pwm_output.DisableCommand.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Command message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pwm_output.Command
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pwm_output.Command} Command
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Command.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Command message.
         * @function verify
         * @memberof pwm_output.Command
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Command.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.targetOutputId != null && message.hasOwnProperty("targetOutputId"))
                if (!$util.isString(message.targetOutputId))
                    return "targetOutputId: string expected";
            if (message.wave != null && message.hasOwnProperty("wave")) {
                let error = $root.pwm_output.WaveCommand.verify(message.wave, long + 1);
                if (error)
                    return "wave." + error;
            }
            if (message.disable != null && message.hasOwnProperty("disable")) {
                let error = $root.pwm_output.DisableCommand.verify(message.disable, long + 1);
                if (error)
                    return "disable." + error;
            }
            return null;
        };

        /**
         * Creates a Command message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pwm_output.Command
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pwm_output.Command} Command
         */
        Command.fromObject = function fromObject(object, long) {
            if (object instanceof $root.pwm_output.Command)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.pwm_output.Command();
            if (object.targetOutputId != null)
                message.targetOutputId = String(object.targetOutputId);
            if (object.wave != null) {
                if (typeof object.wave !== "object")
                    throw TypeError(".pwm_output.Command.wave: object expected");
                message.wave = $root.pwm_output.WaveCommand.fromObject(object.wave, long + 1);
            }
            if (object.disable != null) {
                if (typeof object.disable !== "object")
                    throw TypeError(".pwm_output.Command.disable: object expected");
                message.disable = $root.pwm_output.DisableCommand.fromObject(object.disable, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a Command message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pwm_output.Command
         * @static
         * @param {pwm_output.Command} message Command
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Command.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.targetOutputId = "";
                object.wave = null;
                object.disable = null;
            }
            if (message.targetOutputId != null && message.hasOwnProperty("targetOutputId"))
                object.targetOutputId = message.targetOutputId;
            if (message.wave != null && message.hasOwnProperty("wave"))
                object.wave = $root.pwm_output.WaveCommand.toObject(message.wave, options);
            if (message.disable != null && message.hasOwnProperty("disable"))
                object.disable = $root.pwm_output.DisableCommand.toObject(message.disable, options);
            return object;
        };

        /**
         * Converts this Command to JSON.
         * @function toJSON
         * @memberof pwm_output.Command
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Command.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Command
         * @function getTypeUrl
         * @memberof pwm_output.Command
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Command.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pwm_output.Command";
        };

        return Command;
    })();

    pwm_output.TxEnvelope = (function() {

        /**
         * Properties of a TxEnvelope.
         * @memberof pwm_output
         * @interface ITxEnvelope
         * @property {Long|null} [monotonicStampNs] TxEnvelope monotonicStampNs
         * @property {Long|null} [localStampNs] TxEnvelope localStampNs
         * @property {Long|null} [appStartId] TxEnvelope appStartId
         * @property {Uint8Array|null} [commandId] TxEnvelope commandId
         * @property {string|null} [targetOutputId] TxEnvelope targetOutputId
         * @property {pwm_output.ICommand|null} [command] TxEnvelope command
         */

        /**
         * Constructs a new TxEnvelope.
         * @memberof pwm_output
         * @classdesc Represents a TxEnvelope.
         * @implements ITxEnvelope
         * @constructor
         * @param {pwm_output.ITxEnvelope=} [properties] Properties to set
         */
        function TxEnvelope(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * TxEnvelope monotonicStampNs.
         * @member {Long} monotonicStampNs
         * @memberof pwm_output.TxEnvelope
         * @instance
         */
        TxEnvelope.prototype.monotonicStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * TxEnvelope localStampNs.
         * @member {Long} localStampNs
         * @memberof pwm_output.TxEnvelope
         * @instance
         */
        TxEnvelope.prototype.localStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * TxEnvelope appStartId.
         * @member {Long} appStartId
         * @memberof pwm_output.TxEnvelope
         * @instance
         */
        TxEnvelope.prototype.appStartId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * TxEnvelope commandId.
         * @member {Uint8Array} commandId
         * @memberof pwm_output.TxEnvelope
         * @instance
         */
        TxEnvelope.prototype.commandId = $util.newBuffer([]);

        /**
         * TxEnvelope targetOutputId.
         * @member {string} targetOutputId
         * @memberof pwm_output.TxEnvelope
         * @instance
         */
        TxEnvelope.prototype.targetOutputId = "";

        /**
         * TxEnvelope command.
         * @member {pwm_output.ICommand|null|undefined} command
         * @memberof pwm_output.TxEnvelope
         * @instance
         */
        TxEnvelope.prototype.command = null;

        /**
         * Creates a new TxEnvelope instance using the specified properties.
         * @function create
         * @memberof pwm_output.TxEnvelope
         * @static
         * @param {pwm_output.ITxEnvelope=} [properties] Properties to set
         * @returns {pwm_output.TxEnvelope} TxEnvelope instance
         */
        TxEnvelope.create = function create(properties) {
            return new TxEnvelope(properties);
        };

        /**
         * Encodes the specified TxEnvelope message. Does not implicitly {@link pwm_output.TxEnvelope.verify|verify} messages.
         * @function encode
         * @memberof pwm_output.TxEnvelope
         * @static
         * @param {pwm_output.ITxEnvelope} message TxEnvelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TxEnvelope.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.monotonicStampNs != null && Object.hasOwnProperty.call(message, "monotonicStampNs"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.monotonicStampNs);
            if (message.localStampNs != null && Object.hasOwnProperty.call(message, "localStampNs"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.localStampNs);
            if (message.appStartId != null && Object.hasOwnProperty.call(message, "appStartId"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.appStartId);
            if (message.commandId != null && Object.hasOwnProperty.call(message, "commandId"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.commandId);
            if (message.targetOutputId != null && Object.hasOwnProperty.call(message, "targetOutputId"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.targetOutputId);
            if (message.command != null && Object.hasOwnProperty.call(message, "command"))
                $root.pwm_output.Command.encode(message.command, writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified TxEnvelope message, length delimited. Does not implicitly {@link pwm_output.TxEnvelope.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pwm_output.TxEnvelope
         * @static
         * @param {pwm_output.ITxEnvelope} message TxEnvelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TxEnvelope.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a TxEnvelope message from the specified reader or buffer.
         * @function decode
         * @memberof pwm_output.TxEnvelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pwm_output.TxEnvelope} TxEnvelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TxEnvelope.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pwm_output.TxEnvelope();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.monotonicStampNs = reader.uint64();
                        break;
                    }
                case 2: {
                        message.localStampNs = reader.uint64();
                        break;
                    }
                case 3: {
                        message.appStartId = reader.uint64();
                        break;
                    }
                case 4: {
                        message.commandId = reader.bytes();
                        break;
                    }
                case 5: {
                        message.targetOutputId = reader.string();
                        break;
                    }
                case 10: {
                        message.command = $root.pwm_output.Command.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a TxEnvelope message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pwm_output.TxEnvelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pwm_output.TxEnvelope} TxEnvelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TxEnvelope.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TxEnvelope message.
         * @function verify
         * @memberof pwm_output.TxEnvelope
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TxEnvelope.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (!$util.isInteger(message.monotonicStampNs) && !(message.monotonicStampNs && $util.isInteger(message.monotonicStampNs.low) && $util.isInteger(message.monotonicStampNs.high)))
                    return "monotonicStampNs: integer|Long expected";
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (!$util.isInteger(message.localStampNs) && !(message.localStampNs && $util.isInteger(message.localStampNs.low) && $util.isInteger(message.localStampNs.high)))
                    return "localStampNs: integer|Long expected";
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (!$util.isInteger(message.appStartId) && !(message.appStartId && $util.isInteger(message.appStartId.low) && $util.isInteger(message.appStartId.high)))
                    return "appStartId: integer|Long expected";
            if (message.commandId != null && message.hasOwnProperty("commandId"))
                if (!(message.commandId && typeof message.commandId.length === "number" || $util.isString(message.commandId)))
                    return "commandId: buffer expected";
            if (message.targetOutputId != null && message.hasOwnProperty("targetOutputId"))
                if (!$util.isString(message.targetOutputId))
                    return "targetOutputId: string expected";
            if (message.command != null && message.hasOwnProperty("command")) {
                let error = $root.pwm_output.Command.verify(message.command, long + 1);
                if (error)
                    return "command." + error;
            }
            return null;
        };

        /**
         * Creates a TxEnvelope message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pwm_output.TxEnvelope
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pwm_output.TxEnvelope} TxEnvelope
         */
        TxEnvelope.fromObject = function fromObject(object, long) {
            if (object instanceof $root.pwm_output.TxEnvelope)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.pwm_output.TxEnvelope();
            if (object.monotonicStampNs != null)
                if ($util.Long)
                    (message.monotonicStampNs = $util.Long.fromValue(object.monotonicStampNs)).unsigned = true;
                else if (typeof object.monotonicStampNs === "string")
                    message.monotonicStampNs = parseInt(object.monotonicStampNs, 10);
                else if (typeof object.monotonicStampNs === "number")
                    message.monotonicStampNs = object.monotonicStampNs;
                else if (typeof object.monotonicStampNs === "object")
                    message.monotonicStampNs = new $util.LongBits(object.monotonicStampNs.low >>> 0, object.monotonicStampNs.high >>> 0).toNumber(true);
            if (object.localStampNs != null)
                if ($util.Long)
                    (message.localStampNs = $util.Long.fromValue(object.localStampNs)).unsigned = true;
                else if (typeof object.localStampNs === "string")
                    message.localStampNs = parseInt(object.localStampNs, 10);
                else if (typeof object.localStampNs === "number")
                    message.localStampNs = object.localStampNs;
                else if (typeof object.localStampNs === "object")
                    message.localStampNs = new $util.LongBits(object.localStampNs.low >>> 0, object.localStampNs.high >>> 0).toNumber(true);
            if (object.appStartId != null)
                if ($util.Long)
                    (message.appStartId = $util.Long.fromValue(object.appStartId)).unsigned = true;
                else if (typeof object.appStartId === "string")
                    message.appStartId = parseInt(object.appStartId, 10);
                else if (typeof object.appStartId === "number")
                    message.appStartId = object.appStartId;
                else if (typeof object.appStartId === "object")
                    message.appStartId = new $util.LongBits(object.appStartId.low >>> 0, object.appStartId.high >>> 0).toNumber(true);
            if (object.commandId != null)
                if (typeof object.commandId === "string")
                    $util.base64.decode(object.commandId, message.commandId = $util.newBuffer($util.base64.length(object.commandId)), 0);
                else if (object.commandId.length >= 0)
                    message.commandId = object.commandId;
            if (object.targetOutputId != null)
                message.targetOutputId = String(object.targetOutputId);
            if (object.command != null) {
                if (typeof object.command !== "object")
                    throw TypeError(".pwm_output.TxEnvelope.command: object expected");
                message.command = $root.pwm_output.Command.fromObject(object.command, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a TxEnvelope message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pwm_output.TxEnvelope
         * @static
         * @param {pwm_output.TxEnvelope} message TxEnvelope
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TxEnvelope.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.monotonicStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.monotonicStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.localStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.localStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.appStartId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.appStartId = options.longs === String ? "0" : 0;
                if (options.bytes === String)
                    object.commandId = "";
                else {
                    object.commandId = [];
                    if (options.bytes !== Array)
                        object.commandId = $util.newBuffer(object.commandId);
                }
                object.targetOutputId = "";
                object.command = null;
            }
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (typeof message.monotonicStampNs === "number")
                    object.monotonicStampNs = options.longs === String ? String(message.monotonicStampNs) : message.monotonicStampNs;
                else
                    object.monotonicStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.monotonicStampNs) : options.longs === Number ? new $util.LongBits(message.monotonicStampNs.low >>> 0, message.monotonicStampNs.high >>> 0).toNumber(true) : message.monotonicStampNs;
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (typeof message.localStampNs === "number")
                    object.localStampNs = options.longs === String ? String(message.localStampNs) : message.localStampNs;
                else
                    object.localStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.localStampNs) : options.longs === Number ? new $util.LongBits(message.localStampNs.low >>> 0, message.localStampNs.high >>> 0).toNumber(true) : message.localStampNs;
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (typeof message.appStartId === "number")
                    object.appStartId = options.longs === String ? String(message.appStartId) : message.appStartId;
                else
                    object.appStartId = options.longs === String ? $util.Long.prototype.toString.call(message.appStartId) : options.longs === Number ? new $util.LongBits(message.appStartId.low >>> 0, message.appStartId.high >>> 0).toNumber(true) : message.appStartId;
            if (message.commandId != null && message.hasOwnProperty("commandId"))
                object.commandId = options.bytes === String ? $util.base64.encode(message.commandId, 0, message.commandId.length) : options.bytes === Array ? Array.prototype.slice.call(message.commandId) : message.commandId;
            if (message.targetOutputId != null && message.hasOwnProperty("targetOutputId"))
                object.targetOutputId = message.targetOutputId;
            if (message.command != null && message.hasOwnProperty("command"))
                object.command = $root.pwm_output.Command.toObject(message.command, options);
            return object;
        };

        /**
         * Converts this TxEnvelope to JSON.
         * @function toJSON
         * @memberof pwm_output.TxEnvelope
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TxEnvelope.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for TxEnvelope
         * @function getTypeUrl
         * @memberof pwm_output.TxEnvelope
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        TxEnvelope.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pwm_output.TxEnvelope";
        };

        return TxEnvelope;
    })();

    pwm_output.RxEnvelope = (function() {

        /**
         * Properties of a RxEnvelope.
         * @memberof pwm_output
         * @interface IRxEnvelope
         * @property {Long|null} [monotonicStampNs] RxEnvelope monotonicStampNs
         * @property {Long|null} [localStampNs] RxEnvelope localStampNs
         * @property {Long|null} [appStartId] RxEnvelope appStartId
         * @property {pwm_output.PwmOutputSignalType|null} [signalType] RxEnvelope signalType
         * @property {pwm_output.IPwmOutputDevice|null} [device] RxEnvelope device
         * @property {pwm_output.IOutputState|null} [state] RxEnvelope state
         * @property {pwm_output.ITxEnvelope|null} [command] RxEnvelope command
         * @property {string|null} [error] RxEnvelope error
         */

        /**
         * Constructs a new RxEnvelope.
         * @memberof pwm_output
         * @classdesc Represents a RxEnvelope.
         * @implements IRxEnvelope
         * @constructor
         * @param {pwm_output.IRxEnvelope=} [properties] Properties to set
         */
        function RxEnvelope(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RxEnvelope monotonicStampNs.
         * @member {Long} monotonicStampNs
         * @memberof pwm_output.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.monotonicStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * RxEnvelope localStampNs.
         * @member {Long} localStampNs
         * @memberof pwm_output.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.localStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * RxEnvelope appStartId.
         * @member {Long} appStartId
         * @memberof pwm_output.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.appStartId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * RxEnvelope signalType.
         * @member {pwm_output.PwmOutputSignalType} signalType
         * @memberof pwm_output.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.signalType = 0;

        /**
         * RxEnvelope device.
         * @member {pwm_output.IPwmOutputDevice|null|undefined} device
         * @memberof pwm_output.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.device = null;

        /**
         * RxEnvelope state.
         * @member {pwm_output.IOutputState|null|undefined} state
         * @memberof pwm_output.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.state = null;

        /**
         * RxEnvelope command.
         * @member {pwm_output.ITxEnvelope|null|undefined} command
         * @memberof pwm_output.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.command = null;

        /**
         * RxEnvelope error.
         * @member {string} error
         * @memberof pwm_output.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.error = "";

        /**
         * Creates a new RxEnvelope instance using the specified properties.
         * @function create
         * @memberof pwm_output.RxEnvelope
         * @static
         * @param {pwm_output.IRxEnvelope=} [properties] Properties to set
         * @returns {pwm_output.RxEnvelope} RxEnvelope instance
         */
        RxEnvelope.create = function create(properties) {
            return new RxEnvelope(properties);
        };

        /**
         * Encodes the specified RxEnvelope message. Does not implicitly {@link pwm_output.RxEnvelope.verify|verify} messages.
         * @function encode
         * @memberof pwm_output.RxEnvelope
         * @static
         * @param {pwm_output.IRxEnvelope} message RxEnvelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RxEnvelope.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.monotonicStampNs != null && Object.hasOwnProperty.call(message, "monotonicStampNs"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.monotonicStampNs);
            if (message.localStampNs != null && Object.hasOwnProperty.call(message, "localStampNs"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.localStampNs);
            if (message.appStartId != null && Object.hasOwnProperty.call(message, "appStartId"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.appStartId);
            if (message.signalType != null && Object.hasOwnProperty.call(message, "signalType"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.signalType);
            if (message.device != null && Object.hasOwnProperty.call(message, "device"))
                $root.pwm_output.PwmOutputDevice.encode(message.device, writer.uint32(/* id 11, wireType 2 =*/90).fork()).ldelim();
            if (message.state != null && Object.hasOwnProperty.call(message, "state"))
                $root.pwm_output.OutputState.encode(message.state, writer.uint32(/* id 12, wireType 2 =*/98).fork()).ldelim();
            if (message.command != null && Object.hasOwnProperty.call(message, "command"))
                $root.pwm_output.TxEnvelope.encode(message.command, writer.uint32(/* id 20, wireType 2 =*/162).fork()).ldelim();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                writer.uint32(/* id 50, wireType 2 =*/402).string(message.error);
            return writer;
        };

        /**
         * Encodes the specified RxEnvelope message, length delimited. Does not implicitly {@link pwm_output.RxEnvelope.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pwm_output.RxEnvelope
         * @static
         * @param {pwm_output.IRxEnvelope} message RxEnvelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RxEnvelope.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RxEnvelope message from the specified reader or buffer.
         * @function decode
         * @memberof pwm_output.RxEnvelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pwm_output.RxEnvelope} RxEnvelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RxEnvelope.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pwm_output.RxEnvelope();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.monotonicStampNs = reader.uint64();
                        break;
                    }
                case 2: {
                        message.localStampNs = reader.uint64();
                        break;
                    }
                case 3: {
                        message.appStartId = reader.uint64();
                        break;
                    }
                case 10: {
                        message.signalType = reader.int32();
                        break;
                    }
                case 11: {
                        message.device = $root.pwm_output.PwmOutputDevice.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 12: {
                        message.state = $root.pwm_output.OutputState.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 20: {
                        message.command = $root.pwm_output.TxEnvelope.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 50: {
                        message.error = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RxEnvelope message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pwm_output.RxEnvelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pwm_output.RxEnvelope} RxEnvelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RxEnvelope.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RxEnvelope message.
         * @function verify
         * @memberof pwm_output.RxEnvelope
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RxEnvelope.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (!$util.isInteger(message.monotonicStampNs) && !(message.monotonicStampNs && $util.isInteger(message.monotonicStampNs.low) && $util.isInteger(message.monotonicStampNs.high)))
                    return "monotonicStampNs: integer|Long expected";
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (!$util.isInteger(message.localStampNs) && !(message.localStampNs && $util.isInteger(message.localStampNs.low) && $util.isInteger(message.localStampNs.high)))
                    return "localStampNs: integer|Long expected";
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (!$util.isInteger(message.appStartId) && !(message.appStartId && $util.isInteger(message.appStartId.low) && $util.isInteger(message.appStartId.high)))
                    return "appStartId: integer|Long expected";
            if (message.signalType != null && message.hasOwnProperty("signalType"))
                switch (message.signalType) {
                default:
                    return "signalType: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                case 5:
                case 6:
                    break;
                }
            if (message.device != null && message.hasOwnProperty("device")) {
                let error = $root.pwm_output.PwmOutputDevice.verify(message.device, long + 1);
                if (error)
                    return "device." + error;
            }
            if (message.state != null && message.hasOwnProperty("state")) {
                let error = $root.pwm_output.OutputState.verify(message.state, long + 1);
                if (error)
                    return "state." + error;
            }
            if (message.command != null && message.hasOwnProperty("command")) {
                let error = $root.pwm_output.TxEnvelope.verify(message.command, long + 1);
                if (error)
                    return "command." + error;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                if (!$util.isString(message.error))
                    return "error: string expected";
            return null;
        };

        /**
         * Creates a RxEnvelope message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pwm_output.RxEnvelope
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pwm_output.RxEnvelope} RxEnvelope
         */
        RxEnvelope.fromObject = function fromObject(object, long) {
            if (object instanceof $root.pwm_output.RxEnvelope)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.pwm_output.RxEnvelope();
            if (object.monotonicStampNs != null)
                if ($util.Long)
                    (message.monotonicStampNs = $util.Long.fromValue(object.monotonicStampNs)).unsigned = true;
                else if (typeof object.monotonicStampNs === "string")
                    message.monotonicStampNs = parseInt(object.monotonicStampNs, 10);
                else if (typeof object.monotonicStampNs === "number")
                    message.monotonicStampNs = object.monotonicStampNs;
                else if (typeof object.monotonicStampNs === "object")
                    message.monotonicStampNs = new $util.LongBits(object.monotonicStampNs.low >>> 0, object.monotonicStampNs.high >>> 0).toNumber(true);
            if (object.localStampNs != null)
                if ($util.Long)
                    (message.localStampNs = $util.Long.fromValue(object.localStampNs)).unsigned = true;
                else if (typeof object.localStampNs === "string")
                    message.localStampNs = parseInt(object.localStampNs, 10);
                else if (typeof object.localStampNs === "number")
                    message.localStampNs = object.localStampNs;
                else if (typeof object.localStampNs === "object")
                    message.localStampNs = new $util.LongBits(object.localStampNs.low >>> 0, object.localStampNs.high >>> 0).toNumber(true);
            if (object.appStartId != null)
                if ($util.Long)
                    (message.appStartId = $util.Long.fromValue(object.appStartId)).unsigned = true;
                else if (typeof object.appStartId === "string")
                    message.appStartId = parseInt(object.appStartId, 10);
                else if (typeof object.appStartId === "number")
                    message.appStartId = object.appStartId;
                else if (typeof object.appStartId === "object")
                    message.appStartId = new $util.LongBits(object.appStartId.low >>> 0, object.appStartId.high >>> 0).toNumber(true);
            switch (object.signalType) {
            default:
                if (typeof object.signalType === "number") {
                    message.signalType = object.signalType;
                    break;
                }
                break;
            case "PWM_OUTPUT_SIGNAL_TYPE_UNSPECIFIED":
            case 0:
                message.signalType = 0;
                break;
            case "PWM_OUTPUT_CONFIGURED":
            case 1:
                message.signalType = 1;
                break;
            case "PWM_OUTPUT_COMMAND":
            case 2:
                message.signalType = 2;
                break;
            case "PWM_OUTPUT_COMMAND_SUCCESS":
            case 3:
                message.signalType = 3;
                break;
            case "PWM_OUTPUT_COMMAND_REJECTED":
            case 4:
                message.signalType = 4;
                break;
            case "PWM_OUTPUT_COMMAND_FAILED":
            case 5:
                message.signalType = 5;
                break;
            case "PWM_OUTPUT_ERROR":
            case 6:
                message.signalType = 6;
                break;
            }
            if (object.device != null) {
                if (typeof object.device !== "object")
                    throw TypeError(".pwm_output.RxEnvelope.device: object expected");
                message.device = $root.pwm_output.PwmOutputDevice.fromObject(object.device, long + 1);
            }
            if (object.state != null) {
                if (typeof object.state !== "object")
                    throw TypeError(".pwm_output.RxEnvelope.state: object expected");
                message.state = $root.pwm_output.OutputState.fromObject(object.state, long + 1);
            }
            if (object.command != null) {
                if (typeof object.command !== "object")
                    throw TypeError(".pwm_output.RxEnvelope.command: object expected");
                message.command = $root.pwm_output.TxEnvelope.fromObject(object.command, long + 1);
            }
            if (object.error != null)
                message.error = String(object.error);
            return message;
        };

        /**
         * Creates a plain object from a RxEnvelope message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pwm_output.RxEnvelope
         * @static
         * @param {pwm_output.RxEnvelope} message RxEnvelope
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RxEnvelope.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.monotonicStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.monotonicStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.localStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.localStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.appStartId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.appStartId = options.longs === String ? "0" : 0;
                object.signalType = options.enums === String ? "PWM_OUTPUT_SIGNAL_TYPE_UNSPECIFIED" : 0;
                object.device = null;
                object.state = null;
                object.command = null;
                object.error = "";
            }
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (typeof message.monotonicStampNs === "number")
                    object.monotonicStampNs = options.longs === String ? String(message.monotonicStampNs) : message.monotonicStampNs;
                else
                    object.monotonicStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.monotonicStampNs) : options.longs === Number ? new $util.LongBits(message.monotonicStampNs.low >>> 0, message.monotonicStampNs.high >>> 0).toNumber(true) : message.monotonicStampNs;
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (typeof message.localStampNs === "number")
                    object.localStampNs = options.longs === String ? String(message.localStampNs) : message.localStampNs;
                else
                    object.localStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.localStampNs) : options.longs === Number ? new $util.LongBits(message.localStampNs.low >>> 0, message.localStampNs.high >>> 0).toNumber(true) : message.localStampNs;
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (typeof message.appStartId === "number")
                    object.appStartId = options.longs === String ? String(message.appStartId) : message.appStartId;
                else
                    object.appStartId = options.longs === String ? $util.Long.prototype.toString.call(message.appStartId) : options.longs === Number ? new $util.LongBits(message.appStartId.low >>> 0, message.appStartId.high >>> 0).toNumber(true) : message.appStartId;
            if (message.signalType != null && message.hasOwnProperty("signalType"))
                object.signalType = options.enums === String ? $root.pwm_output.PwmOutputSignalType[message.signalType] === undefined ? message.signalType : $root.pwm_output.PwmOutputSignalType[message.signalType] : message.signalType;
            if (message.device != null && message.hasOwnProperty("device"))
                object.device = $root.pwm_output.PwmOutputDevice.toObject(message.device, options);
            if (message.state != null && message.hasOwnProperty("state"))
                object.state = $root.pwm_output.OutputState.toObject(message.state, options);
            if (message.command != null && message.hasOwnProperty("command"))
                object.command = $root.pwm_output.TxEnvelope.toObject(message.command, options);
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = message.error;
            return object;
        };

        /**
         * Converts this RxEnvelope to JSON.
         * @function toJSON
         * @memberof pwm_output.RxEnvelope
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RxEnvelope.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RxEnvelope
         * @function getTypeUrl
         * @memberof pwm_output.RxEnvelope
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RxEnvelope.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pwm_output.RxEnvelope";
        };

        return RxEnvelope;
    })();

    return pwm_output;
})();

export const dmesg = $root.dmesg = (() => {

    /**
     * Namespace dmesg.
     * @exports dmesg
     * @namespace
     */
    const dmesg = {};

    /**
     * DmesgSignalType enum.
     * @name dmesg.DmesgSignalType
     * @enum {number}
     * @property {number} DMESG_SIGNAL_TYPE_UNSPECIFIED=0 DMESG_SIGNAL_TYPE_UNSPECIFIED value
     * @property {number} DMESG_STARTED=1 DMESG_STARTED value
     * @property {number} DMESG_MESSAGES=2 DMESG_MESSAGES value
     * @property {number} DMESG_BACKLOG_COMPLETE=3 DMESG_BACKLOG_COMPLETE value
     * @property {number} DMESG_GAP=4 DMESG_GAP value
     * @property {number} DMESG_SOURCE_UNAVAILABLE=5 DMESG_SOURCE_UNAVAILABLE value
     * @property {number} DMESG_ERROR=6 DMESG_ERROR value
     */
    dmesg.DmesgSignalType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "DMESG_SIGNAL_TYPE_UNSPECIFIED"] = 0;
        values[valuesById[1] = "DMESG_STARTED"] = 1;
        values[valuesById[2] = "DMESG_MESSAGES"] = 2;
        values[valuesById[3] = "DMESG_BACKLOG_COMPLETE"] = 3;
        values[valuesById[4] = "DMESG_GAP"] = 4;
        values[valuesById[5] = "DMESG_SOURCE_UNAVAILABLE"] = 5;
        values[valuesById[6] = "DMESG_ERROR"] = 6;
        return values;
    })();

    dmesg.RxEnvelope = (function() {

        /**
         * Properties of a RxEnvelope.
         * @memberof dmesg
         * @interface IRxEnvelope
         * @property {Long|null} [monotonicStampNs] RxEnvelope monotonicStampNs
         * @property {Long|null} [localStampNs] RxEnvelope localStampNs
         * @property {Long|null} [appStartId] RxEnvelope appStartId
         * @property {dmesg.DmesgSignalType|null} [signalType] RxEnvelope signalType
         * @property {Array.<string>|null} [records] RxEnvelope records
         * @property {boolean|null} [fromBacklog] RxEnvelope fromBacklog
         * @property {Long|null} [droppedRecords] RxEnvelope droppedRecords
         * @property {string|null} [error] RxEnvelope error
         */

        /**
         * Constructs a new RxEnvelope.
         * @memberof dmesg
         * @classdesc Represents a RxEnvelope.
         * @implements IRxEnvelope
         * @constructor
         * @param {dmesg.IRxEnvelope=} [properties] Properties to set
         */
        function RxEnvelope(properties) {
            this.records = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RxEnvelope monotonicStampNs.
         * @member {Long} monotonicStampNs
         * @memberof dmesg.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.monotonicStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * RxEnvelope localStampNs.
         * @member {Long} localStampNs
         * @memberof dmesg.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.localStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * RxEnvelope appStartId.
         * @member {Long} appStartId
         * @memberof dmesg.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.appStartId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * RxEnvelope signalType.
         * @member {dmesg.DmesgSignalType} signalType
         * @memberof dmesg.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.signalType = 0;

        /**
         * RxEnvelope records.
         * @member {Array.<string>} records
         * @memberof dmesg.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.records = $util.emptyArray;

        /**
         * RxEnvelope fromBacklog.
         * @member {boolean} fromBacklog
         * @memberof dmesg.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.fromBacklog = false;

        /**
         * RxEnvelope droppedRecords.
         * @member {Long} droppedRecords
         * @memberof dmesg.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.droppedRecords = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * RxEnvelope error.
         * @member {string} error
         * @memberof dmesg.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.error = "";

        /**
         * Creates a new RxEnvelope instance using the specified properties.
         * @function create
         * @memberof dmesg.RxEnvelope
         * @static
         * @param {dmesg.IRxEnvelope=} [properties] Properties to set
         * @returns {dmesg.RxEnvelope} RxEnvelope instance
         */
        RxEnvelope.create = function create(properties) {
            return new RxEnvelope(properties);
        };

        /**
         * Encodes the specified RxEnvelope message. Does not implicitly {@link dmesg.RxEnvelope.verify|verify} messages.
         * @function encode
         * @memberof dmesg.RxEnvelope
         * @static
         * @param {dmesg.IRxEnvelope} message RxEnvelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RxEnvelope.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.monotonicStampNs != null && Object.hasOwnProperty.call(message, "monotonicStampNs"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.monotonicStampNs);
            if (message.localStampNs != null && Object.hasOwnProperty.call(message, "localStampNs"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.localStampNs);
            if (message.appStartId != null && Object.hasOwnProperty.call(message, "appStartId"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.appStartId);
            if (message.signalType != null && Object.hasOwnProperty.call(message, "signalType"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.signalType);
            if (message.records != null && message.records.length)
                for (let i = 0; i < message.records.length; ++i)
                    writer.uint32(/* id 11, wireType 2 =*/90).string(message.records[i]);
            if (message.fromBacklog != null && Object.hasOwnProperty.call(message, "fromBacklog"))
                writer.uint32(/* id 12, wireType 0 =*/96).bool(message.fromBacklog);
            if (message.droppedRecords != null && Object.hasOwnProperty.call(message, "droppedRecords"))
                writer.uint32(/* id 13, wireType 0 =*/104).uint64(message.droppedRecords);
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                writer.uint32(/* id 50, wireType 2 =*/402).string(message.error);
            return writer;
        };

        /**
         * Encodes the specified RxEnvelope message, length delimited. Does not implicitly {@link dmesg.RxEnvelope.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dmesg.RxEnvelope
         * @static
         * @param {dmesg.IRxEnvelope} message RxEnvelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RxEnvelope.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RxEnvelope message from the specified reader or buffer.
         * @function decode
         * @memberof dmesg.RxEnvelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dmesg.RxEnvelope} RxEnvelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RxEnvelope.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dmesg.RxEnvelope();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.monotonicStampNs = reader.uint64();
                        break;
                    }
                case 2: {
                        message.localStampNs = reader.uint64();
                        break;
                    }
                case 3: {
                        message.appStartId = reader.uint64();
                        break;
                    }
                case 10: {
                        message.signalType = reader.int32();
                        break;
                    }
                case 11: {
                        if (!(message.records && message.records.length))
                            message.records = [];
                        message.records.push(reader.string());
                        break;
                    }
                case 12: {
                        message.fromBacklog = reader.bool();
                        break;
                    }
                case 13: {
                        message.droppedRecords = reader.uint64();
                        break;
                    }
                case 50: {
                        message.error = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RxEnvelope message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dmesg.RxEnvelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dmesg.RxEnvelope} RxEnvelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RxEnvelope.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RxEnvelope message.
         * @function verify
         * @memberof dmesg.RxEnvelope
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RxEnvelope.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (!$util.isInteger(message.monotonicStampNs) && !(message.monotonicStampNs && $util.isInteger(message.monotonicStampNs.low) && $util.isInteger(message.monotonicStampNs.high)))
                    return "monotonicStampNs: integer|Long expected";
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (!$util.isInteger(message.localStampNs) && !(message.localStampNs && $util.isInteger(message.localStampNs.low) && $util.isInteger(message.localStampNs.high)))
                    return "localStampNs: integer|Long expected";
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (!$util.isInteger(message.appStartId) && !(message.appStartId && $util.isInteger(message.appStartId.low) && $util.isInteger(message.appStartId.high)))
                    return "appStartId: integer|Long expected";
            if (message.signalType != null && message.hasOwnProperty("signalType"))
                switch (message.signalType) {
                default:
                    return "signalType: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                case 5:
                case 6:
                    break;
                }
            if (message.records != null && message.hasOwnProperty("records")) {
                if (!Array.isArray(message.records))
                    return "records: array expected";
                for (let i = 0; i < message.records.length; ++i)
                    if (!$util.isString(message.records[i]))
                        return "records: string[] expected";
            }
            if (message.fromBacklog != null && message.hasOwnProperty("fromBacklog"))
                if (typeof message.fromBacklog !== "boolean")
                    return "fromBacklog: boolean expected";
            if (message.droppedRecords != null && message.hasOwnProperty("droppedRecords"))
                if (!$util.isInteger(message.droppedRecords) && !(message.droppedRecords && $util.isInteger(message.droppedRecords.low) && $util.isInteger(message.droppedRecords.high)))
                    return "droppedRecords: integer|Long expected";
            if (message.error != null && message.hasOwnProperty("error"))
                if (!$util.isString(message.error))
                    return "error: string expected";
            return null;
        };

        /**
         * Creates a RxEnvelope message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dmesg.RxEnvelope
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dmesg.RxEnvelope} RxEnvelope
         */
        RxEnvelope.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dmesg.RxEnvelope)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dmesg.RxEnvelope();
            if (object.monotonicStampNs != null)
                if ($util.Long)
                    (message.monotonicStampNs = $util.Long.fromValue(object.monotonicStampNs)).unsigned = true;
                else if (typeof object.monotonicStampNs === "string")
                    message.monotonicStampNs = parseInt(object.monotonicStampNs, 10);
                else if (typeof object.monotonicStampNs === "number")
                    message.monotonicStampNs = object.monotonicStampNs;
                else if (typeof object.monotonicStampNs === "object")
                    message.monotonicStampNs = new $util.LongBits(object.monotonicStampNs.low >>> 0, object.monotonicStampNs.high >>> 0).toNumber(true);
            if (object.localStampNs != null)
                if ($util.Long)
                    (message.localStampNs = $util.Long.fromValue(object.localStampNs)).unsigned = true;
                else if (typeof object.localStampNs === "string")
                    message.localStampNs = parseInt(object.localStampNs, 10);
                else if (typeof object.localStampNs === "number")
                    message.localStampNs = object.localStampNs;
                else if (typeof object.localStampNs === "object")
                    message.localStampNs = new $util.LongBits(object.localStampNs.low >>> 0, object.localStampNs.high >>> 0).toNumber(true);
            if (object.appStartId != null)
                if ($util.Long)
                    (message.appStartId = $util.Long.fromValue(object.appStartId)).unsigned = true;
                else if (typeof object.appStartId === "string")
                    message.appStartId = parseInt(object.appStartId, 10);
                else if (typeof object.appStartId === "number")
                    message.appStartId = object.appStartId;
                else if (typeof object.appStartId === "object")
                    message.appStartId = new $util.LongBits(object.appStartId.low >>> 0, object.appStartId.high >>> 0).toNumber(true);
            switch (object.signalType) {
            default:
                if (typeof object.signalType === "number") {
                    message.signalType = object.signalType;
                    break;
                }
                break;
            case "DMESG_SIGNAL_TYPE_UNSPECIFIED":
            case 0:
                message.signalType = 0;
                break;
            case "DMESG_STARTED":
            case 1:
                message.signalType = 1;
                break;
            case "DMESG_MESSAGES":
            case 2:
                message.signalType = 2;
                break;
            case "DMESG_BACKLOG_COMPLETE":
            case 3:
                message.signalType = 3;
                break;
            case "DMESG_GAP":
            case 4:
                message.signalType = 4;
                break;
            case "DMESG_SOURCE_UNAVAILABLE":
            case 5:
                message.signalType = 5;
                break;
            case "DMESG_ERROR":
            case 6:
                message.signalType = 6;
                break;
            }
            if (object.records) {
                if (!Array.isArray(object.records))
                    throw TypeError(".dmesg.RxEnvelope.records: array expected");
                message.records = [];
                for (let i = 0; i < object.records.length; ++i)
                    message.records[i] = String(object.records[i]);
            }
            if (object.fromBacklog != null)
                message.fromBacklog = Boolean(object.fromBacklog);
            if (object.droppedRecords != null)
                if ($util.Long)
                    (message.droppedRecords = $util.Long.fromValue(object.droppedRecords)).unsigned = true;
                else if (typeof object.droppedRecords === "string")
                    message.droppedRecords = parseInt(object.droppedRecords, 10);
                else if (typeof object.droppedRecords === "number")
                    message.droppedRecords = object.droppedRecords;
                else if (typeof object.droppedRecords === "object")
                    message.droppedRecords = new $util.LongBits(object.droppedRecords.low >>> 0, object.droppedRecords.high >>> 0).toNumber(true);
            if (object.error != null)
                message.error = String(object.error);
            return message;
        };

        /**
         * Creates a plain object from a RxEnvelope message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dmesg.RxEnvelope
         * @static
         * @param {dmesg.RxEnvelope} message RxEnvelope
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RxEnvelope.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.records = [];
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.monotonicStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.monotonicStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.localStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.localStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.appStartId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.appStartId = options.longs === String ? "0" : 0;
                object.signalType = options.enums === String ? "DMESG_SIGNAL_TYPE_UNSPECIFIED" : 0;
                object.fromBacklog = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.droppedRecords = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.droppedRecords = options.longs === String ? "0" : 0;
                object.error = "";
            }
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (typeof message.monotonicStampNs === "number")
                    object.monotonicStampNs = options.longs === String ? String(message.monotonicStampNs) : message.monotonicStampNs;
                else
                    object.monotonicStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.monotonicStampNs) : options.longs === Number ? new $util.LongBits(message.monotonicStampNs.low >>> 0, message.monotonicStampNs.high >>> 0).toNumber(true) : message.monotonicStampNs;
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (typeof message.localStampNs === "number")
                    object.localStampNs = options.longs === String ? String(message.localStampNs) : message.localStampNs;
                else
                    object.localStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.localStampNs) : options.longs === Number ? new $util.LongBits(message.localStampNs.low >>> 0, message.localStampNs.high >>> 0).toNumber(true) : message.localStampNs;
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (typeof message.appStartId === "number")
                    object.appStartId = options.longs === String ? String(message.appStartId) : message.appStartId;
                else
                    object.appStartId = options.longs === String ? $util.Long.prototype.toString.call(message.appStartId) : options.longs === Number ? new $util.LongBits(message.appStartId.low >>> 0, message.appStartId.high >>> 0).toNumber(true) : message.appStartId;
            if (message.signalType != null && message.hasOwnProperty("signalType"))
                object.signalType = options.enums === String ? $root.dmesg.DmesgSignalType[message.signalType] === undefined ? message.signalType : $root.dmesg.DmesgSignalType[message.signalType] : message.signalType;
            if (message.records && message.records.length) {
                object.records = [];
                for (let j = 0; j < message.records.length; ++j)
                    object.records[j] = message.records[j];
            }
            if (message.fromBacklog != null && message.hasOwnProperty("fromBacklog"))
                object.fromBacklog = message.fromBacklog;
            if (message.droppedRecords != null && message.hasOwnProperty("droppedRecords"))
                if (typeof message.droppedRecords === "number")
                    object.droppedRecords = options.longs === String ? String(message.droppedRecords) : message.droppedRecords;
                else
                    object.droppedRecords = options.longs === String ? $util.Long.prototype.toString.call(message.droppedRecords) : options.longs === Number ? new $util.LongBits(message.droppedRecords.low >>> 0, message.droppedRecords.high >>> 0).toNumber(true) : message.droppedRecords;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = message.error;
            return object;
        };

        /**
         * Converts this RxEnvelope to JSON.
         * @function toJSON
         * @memberof dmesg.RxEnvelope
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RxEnvelope.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RxEnvelope
         * @function getTypeUrl
         * @memberof dmesg.RxEnvelope
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RxEnvelope.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dmesg.RxEnvelope";
        };

        return RxEnvelope;
    })();

    return dmesg;
})();

export const dfrobot_rs485 = $root.dfrobot_rs485 = (() => {

    /**
     * Namespace dfrobot_rs485.
     * @exports dfrobot_rs485
     * @namespace
     */
    const dfrobot_rs485 = {};

    /**
     * DfrobotSignalType enum.
     * @name dfrobot_rs485.DfrobotSignalType
     * @enum {number}
     * @property {number} DFROBOT_SIGNAL_TYPE_UNSPECIFIED=0 DFROBOT_SIGNAL_TYPE_UNSPECIFIED value
     * @property {number} DFROBOT_CONNECTED=1 DFROBOT_CONNECTED value
     * @property {number} DFROBOT_DISCONNECTED=2 DFROBOT_DISCONNECTED value
     * @property {number} DFROBOT_REGISTERS_SNAPSHOT=3 DFROBOT_REGISTERS_SNAPSHOT value
     * @property {number} DFROBOT_ERROR=4 DFROBOT_ERROR value
     * @property {number} DFROBOT_COMMAND_SUCCESS=5 DFROBOT_COMMAND_SUCCESS value
     * @property {number} DFROBOT_COMMAND_REJECTED=6 DFROBOT_COMMAND_REJECTED value
     * @property {number} DFROBOT_COMMAND_FAILED=7 DFROBOT_COMMAND_FAILED value
     * @property {number} DFROBOT_FORGOTTEN=8 DFROBOT_FORGOTTEN value
     */
    dfrobot_rs485.DfrobotSignalType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "DFROBOT_SIGNAL_TYPE_UNSPECIFIED"] = 0;
        values[valuesById[1] = "DFROBOT_CONNECTED"] = 1;
        values[valuesById[2] = "DFROBOT_DISCONNECTED"] = 2;
        values[valuesById[3] = "DFROBOT_REGISTERS_SNAPSHOT"] = 3;
        values[valuesById[4] = "DFROBOT_ERROR"] = 4;
        values[valuesById[5] = "DFROBOT_COMMAND_SUCCESS"] = 5;
        values[valuesById[6] = "DFROBOT_COMMAND_REJECTED"] = 6;
        values[valuesById[7] = "DFROBOT_COMMAND_FAILED"] = 7;
        values[valuesById[8] = "DFROBOT_FORGOTTEN"] = 8;
        return values;
    })();

    /**
     * DfrobotSensorModel enum.
     * @name dfrobot_rs485.DfrobotSensorModel
     * @enum {number}
     * @property {number} DFROBOT_MODEL_UNSPECIFIED=0 DFROBOT_MODEL_UNSPECIFIED value
     * @property {number} DFROBOT_SEN0640_IRRADIANCE=1 DFROBOT_SEN0640_IRRADIANCE value
     * @property {number} DFROBOT_SEN0641_PAR=2 DFROBOT_SEN0641_PAR value
     * @property {number} DFROBOT_SEN0642_UV=3 DFROBOT_SEN0642_UV value
     * @property {number} DFROBOT_SEN0644_LIGHT=4 DFROBOT_SEN0644_LIGHT value
     */
    dfrobot_rs485.DfrobotSensorModel = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "DFROBOT_MODEL_UNSPECIFIED"] = 0;
        values[valuesById[1] = "DFROBOT_SEN0640_IRRADIANCE"] = 1;
        values[valuesById[2] = "DFROBOT_SEN0641_PAR"] = 2;
        values[valuesById[3] = "DFROBOT_SEN0642_UV"] = 3;
        values[valuesById[4] = "DFROBOT_SEN0644_LIGHT"] = 4;
        return values;
    })();

    dfrobot_rs485.DfrobotDevice = (function() {

        /**
         * Properties of a DfrobotDevice.
         * @memberof dfrobot_rs485
         * @interface IDfrobotDevice
         * @property {string|null} [id] DfrobotDevice id
         * @property {dfrobot_rs485.DfrobotSensorModel|null} [model] DfrobotDevice model
         * @property {number|null} [modbusId] DfrobotDevice modbusId
         * @property {string|null} [portName] DfrobotDevice portName
         * @property {number|null} [baud] DfrobotDevice baud
         */

        /**
         * Constructs a new DfrobotDevice.
         * @memberof dfrobot_rs485
         * @classdesc Represents a DfrobotDevice.
         * @implements IDfrobotDevice
         * @constructor
         * @param {dfrobot_rs485.IDfrobotDevice=} [properties] Properties to set
         */
        function DfrobotDevice(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * DfrobotDevice id.
         * @member {string} id
         * @memberof dfrobot_rs485.DfrobotDevice
         * @instance
         */
        DfrobotDevice.prototype.id = "";

        /**
         * DfrobotDevice model.
         * @member {dfrobot_rs485.DfrobotSensorModel} model
         * @memberof dfrobot_rs485.DfrobotDevice
         * @instance
         */
        DfrobotDevice.prototype.model = 0;

        /**
         * DfrobotDevice modbusId.
         * @member {number} modbusId
         * @memberof dfrobot_rs485.DfrobotDevice
         * @instance
         */
        DfrobotDevice.prototype.modbusId = 0;

        /**
         * DfrobotDevice portName.
         * @member {string} portName
         * @memberof dfrobot_rs485.DfrobotDevice
         * @instance
         */
        DfrobotDevice.prototype.portName = "";

        /**
         * DfrobotDevice baud.
         * @member {number} baud
         * @memberof dfrobot_rs485.DfrobotDevice
         * @instance
         */
        DfrobotDevice.prototype.baud = 0;

        /**
         * Creates a new DfrobotDevice instance using the specified properties.
         * @function create
         * @memberof dfrobot_rs485.DfrobotDevice
         * @static
         * @param {dfrobot_rs485.IDfrobotDevice=} [properties] Properties to set
         * @returns {dfrobot_rs485.DfrobotDevice} DfrobotDevice instance
         */
        DfrobotDevice.create = function create(properties) {
            return new DfrobotDevice(properties);
        };

        /**
         * Encodes the specified DfrobotDevice message. Does not implicitly {@link dfrobot_rs485.DfrobotDevice.verify|verify} messages.
         * @function encode
         * @memberof dfrobot_rs485.DfrobotDevice
         * @static
         * @param {dfrobot_rs485.IDfrobotDevice} message DfrobotDevice message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DfrobotDevice.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.id);
            if (message.model != null && Object.hasOwnProperty.call(message, "model"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.model);
            if (message.modbusId != null && Object.hasOwnProperty.call(message, "modbusId"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.modbusId);
            if (message.portName != null && Object.hasOwnProperty.call(message, "portName"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.portName);
            if (message.baud != null && Object.hasOwnProperty.call(message, "baud"))
                writer.uint32(/* id 5, wireType 0 =*/40).uint32(message.baud);
            return writer;
        };

        /**
         * Encodes the specified DfrobotDevice message, length delimited. Does not implicitly {@link dfrobot_rs485.DfrobotDevice.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dfrobot_rs485.DfrobotDevice
         * @static
         * @param {dfrobot_rs485.IDfrobotDevice} message DfrobotDevice message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DfrobotDevice.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a DfrobotDevice message from the specified reader or buffer.
         * @function decode
         * @memberof dfrobot_rs485.DfrobotDevice
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dfrobot_rs485.DfrobotDevice} DfrobotDevice
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DfrobotDevice.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dfrobot_rs485.DfrobotDevice();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.id = reader.string();
                        break;
                    }
                case 2: {
                        message.model = reader.int32();
                        break;
                    }
                case 3: {
                        message.modbusId = reader.uint32();
                        break;
                    }
                case 4: {
                        message.portName = reader.string();
                        break;
                    }
                case 5: {
                        message.baud = reader.uint32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a DfrobotDevice message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dfrobot_rs485.DfrobotDevice
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dfrobot_rs485.DfrobotDevice} DfrobotDevice
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DfrobotDevice.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a DfrobotDevice message.
         * @function verify
         * @memberof dfrobot_rs485.DfrobotDevice
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        DfrobotDevice.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isString(message.id))
                    return "id: string expected";
            if (message.model != null && message.hasOwnProperty("model"))
                switch (message.model) {
                default:
                    return "model: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                    break;
                }
            if (message.modbusId != null && message.hasOwnProperty("modbusId"))
                if (!$util.isInteger(message.modbusId))
                    return "modbusId: integer expected";
            if (message.portName != null && message.hasOwnProperty("portName"))
                if (!$util.isString(message.portName))
                    return "portName: string expected";
            if (message.baud != null && message.hasOwnProperty("baud"))
                if (!$util.isInteger(message.baud))
                    return "baud: integer expected";
            return null;
        };

        /**
         * Creates a DfrobotDevice message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dfrobot_rs485.DfrobotDevice
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dfrobot_rs485.DfrobotDevice} DfrobotDevice
         */
        DfrobotDevice.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dfrobot_rs485.DfrobotDevice)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dfrobot_rs485.DfrobotDevice();
            if (object.id != null)
                message.id = String(object.id);
            switch (object.model) {
            default:
                if (typeof object.model === "number") {
                    message.model = object.model;
                    break;
                }
                break;
            case "DFROBOT_MODEL_UNSPECIFIED":
            case 0:
                message.model = 0;
                break;
            case "DFROBOT_SEN0640_IRRADIANCE":
            case 1:
                message.model = 1;
                break;
            case "DFROBOT_SEN0641_PAR":
            case 2:
                message.model = 2;
                break;
            case "DFROBOT_SEN0642_UV":
            case 3:
                message.model = 3;
                break;
            case "DFROBOT_SEN0644_LIGHT":
            case 4:
                message.model = 4;
                break;
            }
            if (object.modbusId != null)
                message.modbusId = object.modbusId >>> 0;
            if (object.portName != null)
                message.portName = String(object.portName);
            if (object.baud != null)
                message.baud = object.baud >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a DfrobotDevice message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dfrobot_rs485.DfrobotDevice
         * @static
         * @param {dfrobot_rs485.DfrobotDevice} message DfrobotDevice
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DfrobotDevice.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.id = "";
                object.model = options.enums === String ? "DFROBOT_MODEL_UNSPECIFIED" : 0;
                object.modbusId = 0;
                object.portName = "";
                object.baud = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.model != null && message.hasOwnProperty("model"))
                object.model = options.enums === String ? $root.dfrobot_rs485.DfrobotSensorModel[message.model] === undefined ? message.model : $root.dfrobot_rs485.DfrobotSensorModel[message.model] : message.model;
            if (message.modbusId != null && message.hasOwnProperty("modbusId"))
                object.modbusId = message.modbusId;
            if (message.portName != null && message.hasOwnProperty("portName"))
                object.portName = message.portName;
            if (message.baud != null && message.hasOwnProperty("baud"))
                object.baud = message.baud;
            return object;
        };

        /**
         * Converts this DfrobotDevice to JSON.
         * @function toJSON
         * @memberof dfrobot_rs485.DfrobotDevice
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DfrobotDevice.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for DfrobotDevice
         * @function getTypeUrl
         * @memberof dfrobot_rs485.DfrobotDevice
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        DfrobotDevice.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dfrobot_rs485.DfrobotDevice";
        };

        return DfrobotDevice;
    })();

    dfrobot_rs485.RegisterRange = (function() {

        /**
         * Properties of a RegisterRange.
         * @memberof dfrobot_rs485
         * @interface IRegisterRange
         * @property {number|null} [startRegister] RegisterRange startRegister
         * @property {Uint8Array|null} [data] RegisterRange data
         */

        /**
         * Constructs a new RegisterRange.
         * @memberof dfrobot_rs485
         * @classdesc Represents a RegisterRange.
         * @implements IRegisterRange
         * @constructor
         * @param {dfrobot_rs485.IRegisterRange=} [properties] Properties to set
         */
        function RegisterRange(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RegisterRange startRegister.
         * @member {number} startRegister
         * @memberof dfrobot_rs485.RegisterRange
         * @instance
         */
        RegisterRange.prototype.startRegister = 0;

        /**
         * RegisterRange data.
         * @member {Uint8Array} data
         * @memberof dfrobot_rs485.RegisterRange
         * @instance
         */
        RegisterRange.prototype.data = $util.newBuffer([]);

        /**
         * Creates a new RegisterRange instance using the specified properties.
         * @function create
         * @memberof dfrobot_rs485.RegisterRange
         * @static
         * @param {dfrobot_rs485.IRegisterRange=} [properties] Properties to set
         * @returns {dfrobot_rs485.RegisterRange} RegisterRange instance
         */
        RegisterRange.create = function create(properties) {
            return new RegisterRange(properties);
        };

        /**
         * Encodes the specified RegisterRange message. Does not implicitly {@link dfrobot_rs485.RegisterRange.verify|verify} messages.
         * @function encode
         * @memberof dfrobot_rs485.RegisterRange
         * @static
         * @param {dfrobot_rs485.IRegisterRange} message RegisterRange message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RegisterRange.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.startRegister != null && Object.hasOwnProperty.call(message, "startRegister"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.startRegister);
            if (message.data != null && Object.hasOwnProperty.call(message, "data"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.data);
            return writer;
        };

        /**
         * Encodes the specified RegisterRange message, length delimited. Does not implicitly {@link dfrobot_rs485.RegisterRange.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dfrobot_rs485.RegisterRange
         * @static
         * @param {dfrobot_rs485.IRegisterRange} message RegisterRange message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RegisterRange.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RegisterRange message from the specified reader or buffer.
         * @function decode
         * @memberof dfrobot_rs485.RegisterRange
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dfrobot_rs485.RegisterRange} RegisterRange
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RegisterRange.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dfrobot_rs485.RegisterRange();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.startRegister = reader.uint32();
                        break;
                    }
                case 2: {
                        message.data = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RegisterRange message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dfrobot_rs485.RegisterRange
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dfrobot_rs485.RegisterRange} RegisterRange
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RegisterRange.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RegisterRange message.
         * @function verify
         * @memberof dfrobot_rs485.RegisterRange
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RegisterRange.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.startRegister != null && message.hasOwnProperty("startRegister"))
                if (!$util.isInteger(message.startRegister))
                    return "startRegister: integer expected";
            if (message.data != null && message.hasOwnProperty("data"))
                if (!(message.data && typeof message.data.length === "number" || $util.isString(message.data)))
                    return "data: buffer expected";
            return null;
        };

        /**
         * Creates a RegisterRange message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dfrobot_rs485.RegisterRange
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dfrobot_rs485.RegisterRange} RegisterRange
         */
        RegisterRange.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dfrobot_rs485.RegisterRange)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dfrobot_rs485.RegisterRange();
            if (object.startRegister != null)
                message.startRegister = object.startRegister >>> 0;
            if (object.data != null)
                if (typeof object.data === "string")
                    $util.base64.decode(object.data, message.data = $util.newBuffer($util.base64.length(object.data)), 0);
                else if (object.data.length >= 0)
                    message.data = object.data;
            return message;
        };

        /**
         * Creates a plain object from a RegisterRange message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dfrobot_rs485.RegisterRange
         * @static
         * @param {dfrobot_rs485.RegisterRange} message RegisterRange
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RegisterRange.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.startRegister = 0;
                if (options.bytes === String)
                    object.data = "";
                else {
                    object.data = [];
                    if (options.bytes !== Array)
                        object.data = $util.newBuffer(object.data);
                }
            }
            if (message.startRegister != null && message.hasOwnProperty("startRegister"))
                object.startRegister = message.startRegister;
            if (message.data != null && message.hasOwnProperty("data"))
                object.data = options.bytes === String ? $util.base64.encode(message.data, 0, message.data.length) : options.bytes === Array ? Array.prototype.slice.call(message.data) : message.data;
            return object;
        };

        /**
         * Converts this RegisterRange to JSON.
         * @function toJSON
         * @memberof dfrobot_rs485.RegisterRange
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RegisterRange.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RegisterRange
         * @function getTypeUrl
         * @memberof dfrobot_rs485.RegisterRange
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RegisterRange.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dfrobot_rs485.RegisterRange";
        };

        return RegisterRange;
    })();

    dfrobot_rs485.WriteRegisterCommand = (function() {

        /**
         * Properties of a WriteRegisterCommand.
         * @memberof dfrobot_rs485
         * @interface IWriteRegisterCommand
         * @property {number|null} [modbusId] WriteRegisterCommand modbusId
         * @property {number|null} [register] WriteRegisterCommand register
         * @property {number|null} [value] WriteRegisterCommand value
         */

        /**
         * Constructs a new WriteRegisterCommand.
         * @memberof dfrobot_rs485
         * @classdesc Represents a WriteRegisterCommand.
         * @implements IWriteRegisterCommand
         * @constructor
         * @param {dfrobot_rs485.IWriteRegisterCommand=} [properties] Properties to set
         */
        function WriteRegisterCommand(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * WriteRegisterCommand modbusId.
         * @member {number} modbusId
         * @memberof dfrobot_rs485.WriteRegisterCommand
         * @instance
         */
        WriteRegisterCommand.prototype.modbusId = 0;

        /**
         * WriteRegisterCommand register.
         * @member {number} register
         * @memberof dfrobot_rs485.WriteRegisterCommand
         * @instance
         */
        WriteRegisterCommand.prototype.register = 0;

        /**
         * WriteRegisterCommand value.
         * @member {number} value
         * @memberof dfrobot_rs485.WriteRegisterCommand
         * @instance
         */
        WriteRegisterCommand.prototype.value = 0;

        /**
         * Creates a new WriteRegisterCommand instance using the specified properties.
         * @function create
         * @memberof dfrobot_rs485.WriteRegisterCommand
         * @static
         * @param {dfrobot_rs485.IWriteRegisterCommand=} [properties] Properties to set
         * @returns {dfrobot_rs485.WriteRegisterCommand} WriteRegisterCommand instance
         */
        WriteRegisterCommand.create = function create(properties) {
            return new WriteRegisterCommand(properties);
        };

        /**
         * Encodes the specified WriteRegisterCommand message. Does not implicitly {@link dfrobot_rs485.WriteRegisterCommand.verify|verify} messages.
         * @function encode
         * @memberof dfrobot_rs485.WriteRegisterCommand
         * @static
         * @param {dfrobot_rs485.IWriteRegisterCommand} message WriteRegisterCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WriteRegisterCommand.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.modbusId != null && Object.hasOwnProperty.call(message, "modbusId"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.modbusId);
            if (message.register != null && Object.hasOwnProperty.call(message, "register"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.register);
            if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.value);
            return writer;
        };

        /**
         * Encodes the specified WriteRegisterCommand message, length delimited. Does not implicitly {@link dfrobot_rs485.WriteRegisterCommand.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dfrobot_rs485.WriteRegisterCommand
         * @static
         * @param {dfrobot_rs485.IWriteRegisterCommand} message WriteRegisterCommand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WriteRegisterCommand.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a WriteRegisterCommand message from the specified reader or buffer.
         * @function decode
         * @memberof dfrobot_rs485.WriteRegisterCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dfrobot_rs485.WriteRegisterCommand} WriteRegisterCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WriteRegisterCommand.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dfrobot_rs485.WriteRegisterCommand();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.modbusId = reader.uint32();
                        break;
                    }
                case 2: {
                        message.register = reader.uint32();
                        break;
                    }
                case 3: {
                        message.value = reader.uint32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a WriteRegisterCommand message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dfrobot_rs485.WriteRegisterCommand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dfrobot_rs485.WriteRegisterCommand} WriteRegisterCommand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WriteRegisterCommand.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a WriteRegisterCommand message.
         * @function verify
         * @memberof dfrobot_rs485.WriteRegisterCommand
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        WriteRegisterCommand.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.modbusId != null && message.hasOwnProperty("modbusId"))
                if (!$util.isInteger(message.modbusId))
                    return "modbusId: integer expected";
            if (message.register != null && message.hasOwnProperty("register"))
                if (!$util.isInteger(message.register))
                    return "register: integer expected";
            if (message.value != null && message.hasOwnProperty("value"))
                if (!$util.isInteger(message.value))
                    return "value: integer expected";
            return null;
        };

        /**
         * Creates a WriteRegisterCommand message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dfrobot_rs485.WriteRegisterCommand
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dfrobot_rs485.WriteRegisterCommand} WriteRegisterCommand
         */
        WriteRegisterCommand.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dfrobot_rs485.WriteRegisterCommand)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dfrobot_rs485.WriteRegisterCommand();
            if (object.modbusId != null)
                message.modbusId = object.modbusId >>> 0;
            if (object.register != null)
                message.register = object.register >>> 0;
            if (object.value != null)
                message.value = object.value >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a WriteRegisterCommand message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dfrobot_rs485.WriteRegisterCommand
         * @static
         * @param {dfrobot_rs485.WriteRegisterCommand} message WriteRegisterCommand
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        WriteRegisterCommand.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.modbusId = 0;
                object.register = 0;
                object.value = 0;
            }
            if (message.modbusId != null && message.hasOwnProperty("modbusId"))
                object.modbusId = message.modbusId;
            if (message.register != null && message.hasOwnProperty("register"))
                object.register = message.register;
            if (message.value != null && message.hasOwnProperty("value"))
                object.value = message.value;
            return object;
        };

        /**
         * Converts this WriteRegisterCommand to JSON.
         * @function toJSON
         * @memberof dfrobot_rs485.WriteRegisterCommand
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        WriteRegisterCommand.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for WriteRegisterCommand
         * @function getTypeUrl
         * @memberof dfrobot_rs485.WriteRegisterCommand
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        WriteRegisterCommand.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dfrobot_rs485.WriteRegisterCommand";
        };

        return WriteRegisterCommand;
    })();

    dfrobot_rs485.Command = (function() {

        /**
         * Properties of a Command.
         * @memberof dfrobot_rs485
         * @interface ICommand
         * @property {dfrobot_rs485.IWriteRegisterCommand|null} [writeRegister] Command writeRegister
         */

        /**
         * Constructs a new Command.
         * @memberof dfrobot_rs485
         * @classdesc Represents a Command.
         * @implements ICommand
         * @constructor
         * @param {dfrobot_rs485.ICommand=} [properties] Properties to set
         */
        function Command(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Command writeRegister.
         * @member {dfrobot_rs485.IWriteRegisterCommand|null|undefined} writeRegister
         * @memberof dfrobot_rs485.Command
         * @instance
         */
        Command.prototype.writeRegister = null;

        /**
         * Creates a new Command instance using the specified properties.
         * @function create
         * @memberof dfrobot_rs485.Command
         * @static
         * @param {dfrobot_rs485.ICommand=} [properties] Properties to set
         * @returns {dfrobot_rs485.Command} Command instance
         */
        Command.create = function create(properties) {
            return new Command(properties);
        };

        /**
         * Encodes the specified Command message. Does not implicitly {@link dfrobot_rs485.Command.verify|verify} messages.
         * @function encode
         * @memberof dfrobot_rs485.Command
         * @static
         * @param {dfrobot_rs485.ICommand} message Command message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Command.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.writeRegister != null && Object.hasOwnProperty.call(message, "writeRegister"))
                $root.dfrobot_rs485.WriteRegisterCommand.encode(message.writeRegister, writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified Command message, length delimited. Does not implicitly {@link dfrobot_rs485.Command.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dfrobot_rs485.Command
         * @static
         * @param {dfrobot_rs485.ICommand} message Command message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Command.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Command message from the specified reader or buffer.
         * @function decode
         * @memberof dfrobot_rs485.Command
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dfrobot_rs485.Command} Command
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Command.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dfrobot_rs485.Command();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 10: {
                        message.writeRegister = $root.dfrobot_rs485.WriteRegisterCommand.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Command message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dfrobot_rs485.Command
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dfrobot_rs485.Command} Command
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Command.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Command message.
         * @function verify
         * @memberof dfrobot_rs485.Command
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Command.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.writeRegister != null && message.hasOwnProperty("writeRegister")) {
                let error = $root.dfrobot_rs485.WriteRegisterCommand.verify(message.writeRegister, long + 1);
                if (error)
                    return "writeRegister." + error;
            }
            return null;
        };

        /**
         * Creates a Command message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dfrobot_rs485.Command
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dfrobot_rs485.Command} Command
         */
        Command.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dfrobot_rs485.Command)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dfrobot_rs485.Command();
            if (object.writeRegister != null) {
                if (typeof object.writeRegister !== "object")
                    throw TypeError(".dfrobot_rs485.Command.writeRegister: object expected");
                message.writeRegister = $root.dfrobot_rs485.WriteRegisterCommand.fromObject(object.writeRegister, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a Command message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dfrobot_rs485.Command
         * @static
         * @param {dfrobot_rs485.Command} message Command
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Command.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.writeRegister = null;
            if (message.writeRegister != null && message.hasOwnProperty("writeRegister"))
                object.writeRegister = $root.dfrobot_rs485.WriteRegisterCommand.toObject(message.writeRegister, options);
            return object;
        };

        /**
         * Converts this Command to JSON.
         * @function toJSON
         * @memberof dfrobot_rs485.Command
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Command.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Command
         * @function getTypeUrl
         * @memberof dfrobot_rs485.Command
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Command.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dfrobot_rs485.Command";
        };

        return Command;
    })();

    dfrobot_rs485.CommandResult = (function() {

        /**
         * Properties of a CommandResult.
         * @memberof dfrobot_rs485
         * @interface ICommandResult
         * @property {Uint8Array|null} [commandId] CommandResult commandId
         * @property {string|null} [description] CommandResult description
         */

        /**
         * Constructs a new CommandResult.
         * @memberof dfrobot_rs485
         * @classdesc Represents a CommandResult.
         * @implements ICommandResult
         * @constructor
         * @param {dfrobot_rs485.ICommandResult=} [properties] Properties to set
         */
        function CommandResult(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CommandResult commandId.
         * @member {Uint8Array} commandId
         * @memberof dfrobot_rs485.CommandResult
         * @instance
         */
        CommandResult.prototype.commandId = $util.newBuffer([]);

        /**
         * CommandResult description.
         * @member {string} description
         * @memberof dfrobot_rs485.CommandResult
         * @instance
         */
        CommandResult.prototype.description = "";

        /**
         * Creates a new CommandResult instance using the specified properties.
         * @function create
         * @memberof dfrobot_rs485.CommandResult
         * @static
         * @param {dfrobot_rs485.ICommandResult=} [properties] Properties to set
         * @returns {dfrobot_rs485.CommandResult} CommandResult instance
         */
        CommandResult.create = function create(properties) {
            return new CommandResult(properties);
        };

        /**
         * Encodes the specified CommandResult message. Does not implicitly {@link dfrobot_rs485.CommandResult.verify|verify} messages.
         * @function encode
         * @memberof dfrobot_rs485.CommandResult
         * @static
         * @param {dfrobot_rs485.ICommandResult} message CommandResult message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CommandResult.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.commandId != null && Object.hasOwnProperty.call(message, "commandId"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.commandId);
            if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.description);
            return writer;
        };

        /**
         * Encodes the specified CommandResult message, length delimited. Does not implicitly {@link dfrobot_rs485.CommandResult.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dfrobot_rs485.CommandResult
         * @static
         * @param {dfrobot_rs485.ICommandResult} message CommandResult message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CommandResult.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CommandResult message from the specified reader or buffer.
         * @function decode
         * @memberof dfrobot_rs485.CommandResult
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dfrobot_rs485.CommandResult} CommandResult
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CommandResult.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dfrobot_rs485.CommandResult();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.commandId = reader.bytes();
                        break;
                    }
                case 2: {
                        message.description = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CommandResult message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dfrobot_rs485.CommandResult
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dfrobot_rs485.CommandResult} CommandResult
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CommandResult.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CommandResult message.
         * @function verify
         * @memberof dfrobot_rs485.CommandResult
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CommandResult.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.commandId != null && message.hasOwnProperty("commandId"))
                if (!(message.commandId && typeof message.commandId.length === "number" || $util.isString(message.commandId)))
                    return "commandId: buffer expected";
            if (message.description != null && message.hasOwnProperty("description"))
                if (!$util.isString(message.description))
                    return "description: string expected";
            return null;
        };

        /**
         * Creates a CommandResult message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dfrobot_rs485.CommandResult
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dfrobot_rs485.CommandResult} CommandResult
         */
        CommandResult.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dfrobot_rs485.CommandResult)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dfrobot_rs485.CommandResult();
            if (object.commandId != null)
                if (typeof object.commandId === "string")
                    $util.base64.decode(object.commandId, message.commandId = $util.newBuffer($util.base64.length(object.commandId)), 0);
                else if (object.commandId.length >= 0)
                    message.commandId = object.commandId;
            if (object.description != null)
                message.description = String(object.description);
            return message;
        };

        /**
         * Creates a plain object from a CommandResult message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dfrobot_rs485.CommandResult
         * @static
         * @param {dfrobot_rs485.CommandResult} message CommandResult
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CommandResult.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.commandId = "";
                else {
                    object.commandId = [];
                    if (options.bytes !== Array)
                        object.commandId = $util.newBuffer(object.commandId);
                }
                object.description = "";
            }
            if (message.commandId != null && message.hasOwnProperty("commandId"))
                object.commandId = options.bytes === String ? $util.base64.encode(message.commandId, 0, message.commandId.length) : options.bytes === Array ? Array.prototype.slice.call(message.commandId) : message.commandId;
            if (message.description != null && message.hasOwnProperty("description"))
                object.description = message.description;
            return object;
        };

        /**
         * Converts this CommandResult to JSON.
         * @function toJSON
         * @memberof dfrobot_rs485.CommandResult
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CommandResult.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CommandResult
         * @function getTypeUrl
         * @memberof dfrobot_rs485.CommandResult
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CommandResult.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dfrobot_rs485.CommandResult";
        };

        return CommandResult;
    })();

    dfrobot_rs485.RxEnvelope = (function() {

        /**
         * Properties of a RxEnvelope.
         * @memberof dfrobot_rs485
         * @interface IRxEnvelope
         * @property {Long|null} [monotonicStampNs] RxEnvelope monotonicStampNs
         * @property {Long|null} [localStampNs] RxEnvelope localStampNs
         * @property {Long|null} [appStartId] RxEnvelope appStartId
         * @property {dfrobot_rs485.DfrobotSignalType|null} [signalType] RxEnvelope signalType
         * @property {dfrobot_rs485.IDfrobotDevice|null} [device] RxEnvelope device
         * @property {Array.<dfrobot_rs485.IRegisterRange>|null} [ranges] RxEnvelope ranges
         * @property {dfrobot_rs485.ICommandResult|null} [command] RxEnvelope command
         * @property {string|null} [error] RxEnvelope error
         */

        /**
         * Constructs a new RxEnvelope.
         * @memberof dfrobot_rs485
         * @classdesc Represents a RxEnvelope.
         * @implements IRxEnvelope
         * @constructor
         * @param {dfrobot_rs485.IRxEnvelope=} [properties] Properties to set
         */
        function RxEnvelope(properties) {
            this.ranges = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RxEnvelope monotonicStampNs.
         * @member {Long} monotonicStampNs
         * @memberof dfrobot_rs485.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.monotonicStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * RxEnvelope localStampNs.
         * @member {Long} localStampNs
         * @memberof dfrobot_rs485.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.localStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * RxEnvelope appStartId.
         * @member {Long} appStartId
         * @memberof dfrobot_rs485.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.appStartId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * RxEnvelope signalType.
         * @member {dfrobot_rs485.DfrobotSignalType} signalType
         * @memberof dfrobot_rs485.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.signalType = 0;

        /**
         * RxEnvelope device.
         * @member {dfrobot_rs485.IDfrobotDevice|null|undefined} device
         * @memberof dfrobot_rs485.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.device = null;

        /**
         * RxEnvelope ranges.
         * @member {Array.<dfrobot_rs485.IRegisterRange>} ranges
         * @memberof dfrobot_rs485.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.ranges = $util.emptyArray;

        /**
         * RxEnvelope command.
         * @member {dfrobot_rs485.ICommandResult|null|undefined} command
         * @memberof dfrobot_rs485.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.command = null;

        /**
         * RxEnvelope error.
         * @member {string} error
         * @memberof dfrobot_rs485.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.error = "";

        /**
         * Creates a new RxEnvelope instance using the specified properties.
         * @function create
         * @memberof dfrobot_rs485.RxEnvelope
         * @static
         * @param {dfrobot_rs485.IRxEnvelope=} [properties] Properties to set
         * @returns {dfrobot_rs485.RxEnvelope} RxEnvelope instance
         */
        RxEnvelope.create = function create(properties) {
            return new RxEnvelope(properties);
        };

        /**
         * Encodes the specified RxEnvelope message. Does not implicitly {@link dfrobot_rs485.RxEnvelope.verify|verify} messages.
         * @function encode
         * @memberof dfrobot_rs485.RxEnvelope
         * @static
         * @param {dfrobot_rs485.IRxEnvelope} message RxEnvelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RxEnvelope.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.monotonicStampNs != null && Object.hasOwnProperty.call(message, "monotonicStampNs"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.monotonicStampNs);
            if (message.localStampNs != null && Object.hasOwnProperty.call(message, "localStampNs"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.localStampNs);
            if (message.appStartId != null && Object.hasOwnProperty.call(message, "appStartId"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.appStartId);
            if (message.signalType != null && Object.hasOwnProperty.call(message, "signalType"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.signalType);
            if (message.device != null && Object.hasOwnProperty.call(message, "device"))
                $root.dfrobot_rs485.DfrobotDevice.encode(message.device, writer.uint32(/* id 11, wireType 2 =*/90).fork()).ldelim();
            if (message.ranges != null && message.ranges.length)
                for (let i = 0; i < message.ranges.length; ++i)
                    $root.dfrobot_rs485.RegisterRange.encode(message.ranges[i], writer.uint32(/* id 20, wireType 2 =*/162).fork()).ldelim();
            if (message.command != null && Object.hasOwnProperty.call(message, "command"))
                $root.dfrobot_rs485.CommandResult.encode(message.command, writer.uint32(/* id 30, wireType 2 =*/242).fork()).ldelim();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                writer.uint32(/* id 50, wireType 2 =*/402).string(message.error);
            return writer;
        };

        /**
         * Encodes the specified RxEnvelope message, length delimited. Does not implicitly {@link dfrobot_rs485.RxEnvelope.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dfrobot_rs485.RxEnvelope
         * @static
         * @param {dfrobot_rs485.IRxEnvelope} message RxEnvelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RxEnvelope.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RxEnvelope message from the specified reader or buffer.
         * @function decode
         * @memberof dfrobot_rs485.RxEnvelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dfrobot_rs485.RxEnvelope} RxEnvelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RxEnvelope.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dfrobot_rs485.RxEnvelope();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.monotonicStampNs = reader.uint64();
                        break;
                    }
                case 2: {
                        message.localStampNs = reader.uint64();
                        break;
                    }
                case 3: {
                        message.appStartId = reader.uint64();
                        break;
                    }
                case 10: {
                        message.signalType = reader.int32();
                        break;
                    }
                case 11: {
                        message.device = $root.dfrobot_rs485.DfrobotDevice.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 20: {
                        if (!(message.ranges && message.ranges.length))
                            message.ranges = [];
                        message.ranges.push($root.dfrobot_rs485.RegisterRange.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 30: {
                        message.command = $root.dfrobot_rs485.CommandResult.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 50: {
                        message.error = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RxEnvelope message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dfrobot_rs485.RxEnvelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dfrobot_rs485.RxEnvelope} RxEnvelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RxEnvelope.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RxEnvelope message.
         * @function verify
         * @memberof dfrobot_rs485.RxEnvelope
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RxEnvelope.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (!$util.isInteger(message.monotonicStampNs) && !(message.monotonicStampNs && $util.isInteger(message.monotonicStampNs.low) && $util.isInteger(message.monotonicStampNs.high)))
                    return "monotonicStampNs: integer|Long expected";
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (!$util.isInteger(message.localStampNs) && !(message.localStampNs && $util.isInteger(message.localStampNs.low) && $util.isInteger(message.localStampNs.high)))
                    return "localStampNs: integer|Long expected";
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (!$util.isInteger(message.appStartId) && !(message.appStartId && $util.isInteger(message.appStartId.low) && $util.isInteger(message.appStartId.high)))
                    return "appStartId: integer|Long expected";
            if (message.signalType != null && message.hasOwnProperty("signalType"))
                switch (message.signalType) {
                default:
                    return "signalType: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                case 5:
                case 6:
                case 7:
                case 8:
                    break;
                }
            if (message.device != null && message.hasOwnProperty("device")) {
                let error = $root.dfrobot_rs485.DfrobotDevice.verify(message.device, long + 1);
                if (error)
                    return "device." + error;
            }
            if (message.ranges != null && message.hasOwnProperty("ranges")) {
                if (!Array.isArray(message.ranges))
                    return "ranges: array expected";
                for (let i = 0; i < message.ranges.length; ++i) {
                    let error = $root.dfrobot_rs485.RegisterRange.verify(message.ranges[i], long + 1);
                    if (error)
                        return "ranges." + error;
                }
            }
            if (message.command != null && message.hasOwnProperty("command")) {
                let error = $root.dfrobot_rs485.CommandResult.verify(message.command, long + 1);
                if (error)
                    return "command." + error;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                if (!$util.isString(message.error))
                    return "error: string expected";
            return null;
        };

        /**
         * Creates a RxEnvelope message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dfrobot_rs485.RxEnvelope
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dfrobot_rs485.RxEnvelope} RxEnvelope
         */
        RxEnvelope.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dfrobot_rs485.RxEnvelope)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dfrobot_rs485.RxEnvelope();
            if (object.monotonicStampNs != null)
                if ($util.Long)
                    (message.monotonicStampNs = $util.Long.fromValue(object.monotonicStampNs)).unsigned = true;
                else if (typeof object.monotonicStampNs === "string")
                    message.monotonicStampNs = parseInt(object.monotonicStampNs, 10);
                else if (typeof object.monotonicStampNs === "number")
                    message.monotonicStampNs = object.monotonicStampNs;
                else if (typeof object.monotonicStampNs === "object")
                    message.monotonicStampNs = new $util.LongBits(object.monotonicStampNs.low >>> 0, object.monotonicStampNs.high >>> 0).toNumber(true);
            if (object.localStampNs != null)
                if ($util.Long)
                    (message.localStampNs = $util.Long.fromValue(object.localStampNs)).unsigned = true;
                else if (typeof object.localStampNs === "string")
                    message.localStampNs = parseInt(object.localStampNs, 10);
                else if (typeof object.localStampNs === "number")
                    message.localStampNs = object.localStampNs;
                else if (typeof object.localStampNs === "object")
                    message.localStampNs = new $util.LongBits(object.localStampNs.low >>> 0, object.localStampNs.high >>> 0).toNumber(true);
            if (object.appStartId != null)
                if ($util.Long)
                    (message.appStartId = $util.Long.fromValue(object.appStartId)).unsigned = true;
                else if (typeof object.appStartId === "string")
                    message.appStartId = parseInt(object.appStartId, 10);
                else if (typeof object.appStartId === "number")
                    message.appStartId = object.appStartId;
                else if (typeof object.appStartId === "object")
                    message.appStartId = new $util.LongBits(object.appStartId.low >>> 0, object.appStartId.high >>> 0).toNumber(true);
            switch (object.signalType) {
            default:
                if (typeof object.signalType === "number") {
                    message.signalType = object.signalType;
                    break;
                }
                break;
            case "DFROBOT_SIGNAL_TYPE_UNSPECIFIED":
            case 0:
                message.signalType = 0;
                break;
            case "DFROBOT_CONNECTED":
            case 1:
                message.signalType = 1;
                break;
            case "DFROBOT_DISCONNECTED":
            case 2:
                message.signalType = 2;
                break;
            case "DFROBOT_REGISTERS_SNAPSHOT":
            case 3:
                message.signalType = 3;
                break;
            case "DFROBOT_ERROR":
            case 4:
                message.signalType = 4;
                break;
            case "DFROBOT_COMMAND_SUCCESS":
            case 5:
                message.signalType = 5;
                break;
            case "DFROBOT_COMMAND_REJECTED":
            case 6:
                message.signalType = 6;
                break;
            case "DFROBOT_COMMAND_FAILED":
            case 7:
                message.signalType = 7;
                break;
            case "DFROBOT_FORGOTTEN":
            case 8:
                message.signalType = 8;
                break;
            }
            if (object.device != null) {
                if (typeof object.device !== "object")
                    throw TypeError(".dfrobot_rs485.RxEnvelope.device: object expected");
                message.device = $root.dfrobot_rs485.DfrobotDevice.fromObject(object.device, long + 1);
            }
            if (object.ranges) {
                if (!Array.isArray(object.ranges))
                    throw TypeError(".dfrobot_rs485.RxEnvelope.ranges: array expected");
                message.ranges = [];
                for (let i = 0; i < object.ranges.length; ++i) {
                    if (typeof object.ranges[i] !== "object")
                        throw TypeError(".dfrobot_rs485.RxEnvelope.ranges: object expected");
                    message.ranges[i] = $root.dfrobot_rs485.RegisterRange.fromObject(object.ranges[i], long + 1);
                }
            }
            if (object.command != null) {
                if (typeof object.command !== "object")
                    throw TypeError(".dfrobot_rs485.RxEnvelope.command: object expected");
                message.command = $root.dfrobot_rs485.CommandResult.fromObject(object.command, long + 1);
            }
            if (object.error != null)
                message.error = String(object.error);
            return message;
        };

        /**
         * Creates a plain object from a RxEnvelope message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dfrobot_rs485.RxEnvelope
         * @static
         * @param {dfrobot_rs485.RxEnvelope} message RxEnvelope
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RxEnvelope.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.ranges = [];
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.monotonicStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.monotonicStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.localStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.localStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.appStartId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.appStartId = options.longs === String ? "0" : 0;
                object.signalType = options.enums === String ? "DFROBOT_SIGNAL_TYPE_UNSPECIFIED" : 0;
                object.device = null;
                object.command = null;
                object.error = "";
            }
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (typeof message.monotonicStampNs === "number")
                    object.monotonicStampNs = options.longs === String ? String(message.monotonicStampNs) : message.monotonicStampNs;
                else
                    object.monotonicStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.monotonicStampNs) : options.longs === Number ? new $util.LongBits(message.monotonicStampNs.low >>> 0, message.monotonicStampNs.high >>> 0).toNumber(true) : message.monotonicStampNs;
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (typeof message.localStampNs === "number")
                    object.localStampNs = options.longs === String ? String(message.localStampNs) : message.localStampNs;
                else
                    object.localStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.localStampNs) : options.longs === Number ? new $util.LongBits(message.localStampNs.low >>> 0, message.localStampNs.high >>> 0).toNumber(true) : message.localStampNs;
            if (message.appStartId != null && message.hasOwnProperty("appStartId"))
                if (typeof message.appStartId === "number")
                    object.appStartId = options.longs === String ? String(message.appStartId) : message.appStartId;
                else
                    object.appStartId = options.longs === String ? $util.Long.prototype.toString.call(message.appStartId) : options.longs === Number ? new $util.LongBits(message.appStartId.low >>> 0, message.appStartId.high >>> 0).toNumber(true) : message.appStartId;
            if (message.signalType != null && message.hasOwnProperty("signalType"))
                object.signalType = options.enums === String ? $root.dfrobot_rs485.DfrobotSignalType[message.signalType] === undefined ? message.signalType : $root.dfrobot_rs485.DfrobotSignalType[message.signalType] : message.signalType;
            if (message.device != null && message.hasOwnProperty("device"))
                object.device = $root.dfrobot_rs485.DfrobotDevice.toObject(message.device, options);
            if (message.ranges && message.ranges.length) {
                object.ranges = [];
                for (let j = 0; j < message.ranges.length; ++j)
                    object.ranges[j] = $root.dfrobot_rs485.RegisterRange.toObject(message.ranges[j], options);
            }
            if (message.command != null && message.hasOwnProperty("command"))
                object.command = $root.dfrobot_rs485.CommandResult.toObject(message.command, options);
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = message.error;
            return object;
        };

        /**
         * Converts this RxEnvelope to JSON.
         * @function toJSON
         * @memberof dfrobot_rs485.RxEnvelope
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RxEnvelope.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RxEnvelope
         * @function getTypeUrl
         * @memberof dfrobot_rs485.RxEnvelope
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RxEnvelope.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dfrobot_rs485.RxEnvelope";
        };

        return RxEnvelope;
    })();

    return dfrobot_rs485;
})();

export const normvla = $root.normvla = (() => {

    /**
     * Namespace normvla.
     * @exports normvla
     * @namespace
     */
    const normvla = {};

    normvla.Frame = (function() {

        /**
         * Properties of a Frame.
         * @memberof normvla
         * @interface IFrame
         * @property {Uint8Array|null} [globalFrameId] Frame globalFrameId
         * @property {Long|null} [monotonicStampNs] Frame monotonicStampNs
         * @property {Array.<normvla.IJoint>|null} [joints] Frame joints
         * @property {Array.<normvla.IImage>|null} [images] Frame images
         */

        /**
         * Constructs a new Frame.
         * @memberof normvla
         * @classdesc Represents a Frame.
         * @implements IFrame
         * @constructor
         * @param {normvla.IFrame=} [properties] Properties to set
         */
        function Frame(properties) {
            this.joints = [];
            this.images = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Frame globalFrameId.
         * @member {Uint8Array} globalFrameId
         * @memberof normvla.Frame
         * @instance
         */
        Frame.prototype.globalFrameId = $util.newBuffer([]);

        /**
         * Frame monotonicStampNs.
         * @member {Long} monotonicStampNs
         * @memberof normvla.Frame
         * @instance
         */
        Frame.prototype.monotonicStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Frame joints.
         * @member {Array.<normvla.IJoint>} joints
         * @memberof normvla.Frame
         * @instance
         */
        Frame.prototype.joints = $util.emptyArray;

        /**
         * Frame images.
         * @member {Array.<normvla.IImage>} images
         * @memberof normvla.Frame
         * @instance
         */
        Frame.prototype.images = $util.emptyArray;

        /**
         * Creates a new Frame instance using the specified properties.
         * @function create
         * @memberof normvla.Frame
         * @static
         * @param {normvla.IFrame=} [properties] Properties to set
         * @returns {normvla.Frame} Frame instance
         */
        Frame.create = function create(properties) {
            return new Frame(properties);
        };

        /**
         * Encodes the specified Frame message. Does not implicitly {@link normvla.Frame.verify|verify} messages.
         * @function encode
         * @memberof normvla.Frame
         * @static
         * @param {normvla.IFrame} message Frame message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Frame.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.globalFrameId != null && Object.hasOwnProperty.call(message, "globalFrameId"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.globalFrameId);
            if (message.monotonicStampNs != null && Object.hasOwnProperty.call(message, "monotonicStampNs"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.monotonicStampNs);
            if (message.joints != null && message.joints.length)
                for (let i = 0; i < message.joints.length; ++i)
                    $root.normvla.Joint.encode(message.joints[i], writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            if (message.images != null && message.images.length)
                for (let i = 0; i < message.images.length; ++i)
                    $root.normvla.Image.encode(message.images[i], writer.uint32(/* id 11, wireType 2 =*/90).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified Frame message, length delimited. Does not implicitly {@link normvla.Frame.verify|verify} messages.
         * @function encodeDelimited
         * @memberof normvla.Frame
         * @static
         * @param {normvla.IFrame} message Frame message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Frame.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Frame message from the specified reader or buffer.
         * @function decode
         * @memberof normvla.Frame
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {normvla.Frame} Frame
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Frame.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.normvla.Frame();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.globalFrameId = reader.bytes();
                        break;
                    }
                case 2: {
                        message.monotonicStampNs = reader.uint64();
                        break;
                    }
                case 10: {
                        if (!(message.joints && message.joints.length))
                            message.joints = [];
                        message.joints.push($root.normvla.Joint.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 11: {
                        if (!(message.images && message.images.length))
                            message.images = [];
                        message.images.push($root.normvla.Image.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Frame message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof normvla.Frame
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {normvla.Frame} Frame
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Frame.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Frame message.
         * @function verify
         * @memberof normvla.Frame
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Frame.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.globalFrameId != null && message.hasOwnProperty("globalFrameId"))
                if (!(message.globalFrameId && typeof message.globalFrameId.length === "number" || $util.isString(message.globalFrameId)))
                    return "globalFrameId: buffer expected";
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (!$util.isInteger(message.monotonicStampNs) && !(message.monotonicStampNs && $util.isInteger(message.monotonicStampNs.low) && $util.isInteger(message.monotonicStampNs.high)))
                    return "monotonicStampNs: integer|Long expected";
            if (message.joints != null && message.hasOwnProperty("joints")) {
                if (!Array.isArray(message.joints))
                    return "joints: array expected";
                for (let i = 0; i < message.joints.length; ++i) {
                    let error = $root.normvla.Joint.verify(message.joints[i], long + 1);
                    if (error)
                        return "joints." + error;
                }
            }
            if (message.images != null && message.hasOwnProperty("images")) {
                if (!Array.isArray(message.images))
                    return "images: array expected";
                for (let i = 0; i < message.images.length; ++i) {
                    let error = $root.normvla.Image.verify(message.images[i], long + 1);
                    if (error)
                        return "images." + error;
                }
            }
            return null;
        };

        /**
         * Creates a Frame message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof normvla.Frame
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {normvla.Frame} Frame
         */
        Frame.fromObject = function fromObject(object, long) {
            if (object instanceof $root.normvla.Frame)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.normvla.Frame();
            if (object.globalFrameId != null)
                if (typeof object.globalFrameId === "string")
                    $util.base64.decode(object.globalFrameId, message.globalFrameId = $util.newBuffer($util.base64.length(object.globalFrameId)), 0);
                else if (object.globalFrameId.length >= 0)
                    message.globalFrameId = object.globalFrameId;
            if (object.monotonicStampNs != null)
                if ($util.Long)
                    (message.monotonicStampNs = $util.Long.fromValue(object.monotonicStampNs)).unsigned = true;
                else if (typeof object.monotonicStampNs === "string")
                    message.monotonicStampNs = parseInt(object.monotonicStampNs, 10);
                else if (typeof object.monotonicStampNs === "number")
                    message.monotonicStampNs = object.monotonicStampNs;
                else if (typeof object.monotonicStampNs === "object")
                    message.monotonicStampNs = new $util.LongBits(object.monotonicStampNs.low >>> 0, object.monotonicStampNs.high >>> 0).toNumber(true);
            if (object.joints) {
                if (!Array.isArray(object.joints))
                    throw TypeError(".normvla.Frame.joints: array expected");
                message.joints = [];
                for (let i = 0; i < object.joints.length; ++i) {
                    if (typeof object.joints[i] !== "object")
                        throw TypeError(".normvla.Frame.joints: object expected");
                    message.joints[i] = $root.normvla.Joint.fromObject(object.joints[i], long + 1);
                }
            }
            if (object.images) {
                if (!Array.isArray(object.images))
                    throw TypeError(".normvla.Frame.images: array expected");
                message.images = [];
                for (let i = 0; i < object.images.length; ++i) {
                    if (typeof object.images[i] !== "object")
                        throw TypeError(".normvla.Frame.images: object expected");
                    message.images[i] = $root.normvla.Image.fromObject(object.images[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a Frame message. Also converts values to other types if specified.
         * @function toObject
         * @memberof normvla.Frame
         * @static
         * @param {normvla.Frame} message Frame
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Frame.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.joints = [];
                object.images = [];
            }
            if (options.defaults) {
                if (options.bytes === String)
                    object.globalFrameId = "";
                else {
                    object.globalFrameId = [];
                    if (options.bytes !== Array)
                        object.globalFrameId = $util.newBuffer(object.globalFrameId);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.monotonicStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.monotonicStampNs = options.longs === String ? "0" : 0;
            }
            if (message.globalFrameId != null && message.hasOwnProperty("globalFrameId"))
                object.globalFrameId = options.bytes === String ? $util.base64.encode(message.globalFrameId, 0, message.globalFrameId.length) : options.bytes === Array ? Array.prototype.slice.call(message.globalFrameId) : message.globalFrameId;
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (typeof message.monotonicStampNs === "number")
                    object.monotonicStampNs = options.longs === String ? String(message.monotonicStampNs) : message.monotonicStampNs;
                else
                    object.monotonicStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.monotonicStampNs) : options.longs === Number ? new $util.LongBits(message.monotonicStampNs.low >>> 0, message.monotonicStampNs.high >>> 0).toNumber(true) : message.monotonicStampNs;
            if (message.joints && message.joints.length) {
                object.joints = [];
                for (let j = 0; j < message.joints.length; ++j)
                    object.joints[j] = $root.normvla.Joint.toObject(message.joints[j], options);
            }
            if (message.images && message.images.length) {
                object.images = [];
                for (let j = 0; j < message.images.length; ++j)
                    object.images[j] = $root.normvla.Image.toObject(message.images[j], options);
            }
            return object;
        };

        /**
         * Converts this Frame to JSON.
         * @function toJSON
         * @memberof normvla.Frame
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Frame.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Frame
         * @function getTypeUrl
         * @memberof normvla.Frame
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Frame.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/normvla.Frame";
        };

        return Frame;
    })();

    normvla.Joint = (function() {

        /**
         * Properties of a Joint.
         * @memberof normvla
         * @interface IJoint
         * @property {number|null} [rangeMin] Joint rangeMin
         * @property {number|null} [rangeMax] Joint rangeMax
         * @property {number|null} [position] Joint position
         * @property {number|null} [positionNorm] Joint positionNorm
         * @property {number|null} [goal] Joint goal
         * @property {number|null} [goalNorm] Joint goalNorm
         * @property {number|null} [currentMa] Joint currentMa
         * @property {number|null} [velocity] Joint velocity
         * @property {Long|null} [monotonicStampNs] Joint monotonicStampNs
         */

        /**
         * Constructs a new Joint.
         * @memberof normvla
         * @classdesc Represents a Joint.
         * @implements IJoint
         * @constructor
         * @param {normvla.IJoint=} [properties] Properties to set
         */
        function Joint(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Joint rangeMin.
         * @member {number} rangeMin
         * @memberof normvla.Joint
         * @instance
         */
        Joint.prototype.rangeMin = 0;

        /**
         * Joint rangeMax.
         * @member {number} rangeMax
         * @memberof normvla.Joint
         * @instance
         */
        Joint.prototype.rangeMax = 0;

        /**
         * Joint position.
         * @member {number} position
         * @memberof normvla.Joint
         * @instance
         */
        Joint.prototype.position = 0;

        /**
         * Joint positionNorm.
         * @member {number} positionNorm
         * @memberof normvla.Joint
         * @instance
         */
        Joint.prototype.positionNorm = 0;

        /**
         * Joint goal.
         * @member {number} goal
         * @memberof normvla.Joint
         * @instance
         */
        Joint.prototype.goal = 0;

        /**
         * Joint goalNorm.
         * @member {number} goalNorm
         * @memberof normvla.Joint
         * @instance
         */
        Joint.prototype.goalNorm = 0;

        /**
         * Joint currentMa.
         * @member {number} currentMa
         * @memberof normvla.Joint
         * @instance
         */
        Joint.prototype.currentMa = 0;

        /**
         * Joint velocity.
         * @member {number} velocity
         * @memberof normvla.Joint
         * @instance
         */
        Joint.prototype.velocity = 0;

        /**
         * Joint monotonicStampNs.
         * @member {Long} monotonicStampNs
         * @memberof normvla.Joint
         * @instance
         */
        Joint.prototype.monotonicStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new Joint instance using the specified properties.
         * @function create
         * @memberof normvla.Joint
         * @static
         * @param {normvla.IJoint=} [properties] Properties to set
         * @returns {normvla.Joint} Joint instance
         */
        Joint.create = function create(properties) {
            return new Joint(properties);
        };

        /**
         * Encodes the specified Joint message. Does not implicitly {@link normvla.Joint.verify|verify} messages.
         * @function encode
         * @memberof normvla.Joint
         * @static
         * @param {normvla.IJoint} message Joint message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Joint.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.rangeMin != null && Object.hasOwnProperty.call(message, "rangeMin"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.rangeMin);
            if (message.rangeMax != null && Object.hasOwnProperty.call(message, "rangeMax"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.rangeMax);
            if (message.position != null && Object.hasOwnProperty.call(message, "position"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.position);
            if (message.positionNorm != null && Object.hasOwnProperty.call(message, "positionNorm"))
                writer.uint32(/* id 4, wireType 5 =*/37).float(message.positionNorm);
            if (message.goal != null && Object.hasOwnProperty.call(message, "goal"))
                writer.uint32(/* id 5, wireType 0 =*/40).uint32(message.goal);
            if (message.goalNorm != null && Object.hasOwnProperty.call(message, "goalNorm"))
                writer.uint32(/* id 6, wireType 5 =*/53).float(message.goalNorm);
            if (message.currentMa != null && Object.hasOwnProperty.call(message, "currentMa"))
                writer.uint32(/* id 7, wireType 0 =*/56).uint32(message.currentMa);
            if (message.velocity != null && Object.hasOwnProperty.call(message, "velocity"))
                writer.uint32(/* id 8, wireType 0 =*/64).uint32(message.velocity);
            if (message.monotonicStampNs != null && Object.hasOwnProperty.call(message, "monotonicStampNs"))
                writer.uint32(/* id 9, wireType 0 =*/72).uint64(message.monotonicStampNs);
            return writer;
        };

        /**
         * Encodes the specified Joint message, length delimited. Does not implicitly {@link normvla.Joint.verify|verify} messages.
         * @function encodeDelimited
         * @memberof normvla.Joint
         * @static
         * @param {normvla.IJoint} message Joint message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Joint.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Joint message from the specified reader or buffer.
         * @function decode
         * @memberof normvla.Joint
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {normvla.Joint} Joint
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Joint.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.normvla.Joint();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.rangeMin = reader.uint32();
                        break;
                    }
                case 2: {
                        message.rangeMax = reader.uint32();
                        break;
                    }
                case 3: {
                        message.position = reader.uint32();
                        break;
                    }
                case 4: {
                        message.positionNorm = reader.float();
                        break;
                    }
                case 5: {
                        message.goal = reader.uint32();
                        break;
                    }
                case 6: {
                        message.goalNorm = reader.float();
                        break;
                    }
                case 7: {
                        message.currentMa = reader.uint32();
                        break;
                    }
                case 8: {
                        message.velocity = reader.uint32();
                        break;
                    }
                case 9: {
                        message.monotonicStampNs = reader.uint64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Joint message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof normvla.Joint
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {normvla.Joint} Joint
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Joint.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Joint message.
         * @function verify
         * @memberof normvla.Joint
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Joint.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.rangeMin != null && message.hasOwnProperty("rangeMin"))
                if (!$util.isInteger(message.rangeMin))
                    return "rangeMin: integer expected";
            if (message.rangeMax != null && message.hasOwnProperty("rangeMax"))
                if (!$util.isInteger(message.rangeMax))
                    return "rangeMax: integer expected";
            if (message.position != null && message.hasOwnProperty("position"))
                if (!$util.isInteger(message.position))
                    return "position: integer expected";
            if (message.positionNorm != null && message.hasOwnProperty("positionNorm"))
                if (typeof message.positionNorm !== "number")
                    return "positionNorm: number expected";
            if (message.goal != null && message.hasOwnProperty("goal"))
                if (!$util.isInteger(message.goal))
                    return "goal: integer expected";
            if (message.goalNorm != null && message.hasOwnProperty("goalNorm"))
                if (typeof message.goalNorm !== "number")
                    return "goalNorm: number expected";
            if (message.currentMa != null && message.hasOwnProperty("currentMa"))
                if (!$util.isInteger(message.currentMa))
                    return "currentMa: integer expected";
            if (message.velocity != null && message.hasOwnProperty("velocity"))
                if (!$util.isInteger(message.velocity))
                    return "velocity: integer expected";
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (!$util.isInteger(message.monotonicStampNs) && !(message.monotonicStampNs && $util.isInteger(message.monotonicStampNs.low) && $util.isInteger(message.monotonicStampNs.high)))
                    return "monotonicStampNs: integer|Long expected";
            return null;
        };

        /**
         * Creates a Joint message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof normvla.Joint
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {normvla.Joint} Joint
         */
        Joint.fromObject = function fromObject(object, long) {
            if (object instanceof $root.normvla.Joint)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.normvla.Joint();
            if (object.rangeMin != null)
                message.rangeMin = object.rangeMin >>> 0;
            if (object.rangeMax != null)
                message.rangeMax = object.rangeMax >>> 0;
            if (object.position != null)
                message.position = object.position >>> 0;
            if (object.positionNorm != null)
                message.positionNorm = Number(object.positionNorm);
            if (object.goal != null)
                message.goal = object.goal >>> 0;
            if (object.goalNorm != null)
                message.goalNorm = Number(object.goalNorm);
            if (object.currentMa != null)
                message.currentMa = object.currentMa >>> 0;
            if (object.velocity != null)
                message.velocity = object.velocity >>> 0;
            if (object.monotonicStampNs != null)
                if ($util.Long)
                    (message.monotonicStampNs = $util.Long.fromValue(object.monotonicStampNs)).unsigned = true;
                else if (typeof object.monotonicStampNs === "string")
                    message.monotonicStampNs = parseInt(object.monotonicStampNs, 10);
                else if (typeof object.monotonicStampNs === "number")
                    message.monotonicStampNs = object.monotonicStampNs;
                else if (typeof object.monotonicStampNs === "object")
                    message.monotonicStampNs = new $util.LongBits(object.monotonicStampNs.low >>> 0, object.monotonicStampNs.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a Joint message. Also converts values to other types if specified.
         * @function toObject
         * @memberof normvla.Joint
         * @static
         * @param {normvla.Joint} message Joint
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Joint.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.rangeMin = 0;
                object.rangeMax = 0;
                object.position = 0;
                object.positionNorm = 0;
                object.goal = 0;
                object.goalNorm = 0;
                object.currentMa = 0;
                object.velocity = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.monotonicStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.monotonicStampNs = options.longs === String ? "0" : 0;
            }
            if (message.rangeMin != null && message.hasOwnProperty("rangeMin"))
                object.rangeMin = message.rangeMin;
            if (message.rangeMax != null && message.hasOwnProperty("rangeMax"))
                object.rangeMax = message.rangeMax;
            if (message.position != null && message.hasOwnProperty("position"))
                object.position = message.position;
            if (message.positionNorm != null && message.hasOwnProperty("positionNorm"))
                object.positionNorm = options.json && !isFinite(message.positionNorm) ? String(message.positionNorm) : message.positionNorm;
            if (message.goal != null && message.hasOwnProperty("goal"))
                object.goal = message.goal;
            if (message.goalNorm != null && message.hasOwnProperty("goalNorm"))
                object.goalNorm = options.json && !isFinite(message.goalNorm) ? String(message.goalNorm) : message.goalNorm;
            if (message.currentMa != null && message.hasOwnProperty("currentMa"))
                object.currentMa = message.currentMa;
            if (message.velocity != null && message.hasOwnProperty("velocity"))
                object.velocity = message.velocity;
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (typeof message.monotonicStampNs === "number")
                    object.monotonicStampNs = options.longs === String ? String(message.monotonicStampNs) : message.monotonicStampNs;
                else
                    object.monotonicStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.monotonicStampNs) : options.longs === Number ? new $util.LongBits(message.monotonicStampNs.low >>> 0, message.monotonicStampNs.high >>> 0).toNumber(true) : message.monotonicStampNs;
            return object;
        };

        /**
         * Converts this Joint to JSON.
         * @function toJSON
         * @memberof normvla.Joint
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Joint.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Joint
         * @function getTypeUrl
         * @memberof normvla.Joint
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Joint.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/normvla.Joint";
        };

        return Joint;
    })();

    normvla.Image = (function() {

        /**
         * Properties of an Image.
         * @memberof normvla
         * @interface IImage
         * @property {Uint8Array|null} [jpeg] Image jpeg
         * @property {Long|null} [monotonicStampNs] Image monotonicStampNs
         */

        /**
         * Constructs a new Image.
         * @memberof normvla
         * @classdesc Represents an Image.
         * @implements IImage
         * @constructor
         * @param {normvla.IImage=} [properties] Properties to set
         */
        function Image(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Image jpeg.
         * @member {Uint8Array} jpeg
         * @memberof normvla.Image
         * @instance
         */
        Image.prototype.jpeg = $util.newBuffer([]);

        /**
         * Image monotonicStampNs.
         * @member {Long} monotonicStampNs
         * @memberof normvla.Image
         * @instance
         */
        Image.prototype.monotonicStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new Image instance using the specified properties.
         * @function create
         * @memberof normvla.Image
         * @static
         * @param {normvla.IImage=} [properties] Properties to set
         * @returns {normvla.Image} Image instance
         */
        Image.create = function create(properties) {
            return new Image(properties);
        };

        /**
         * Encodes the specified Image message. Does not implicitly {@link normvla.Image.verify|verify} messages.
         * @function encode
         * @memberof normvla.Image
         * @static
         * @param {normvla.IImage} message Image message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Image.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.jpeg != null && Object.hasOwnProperty.call(message, "jpeg"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.jpeg);
            if (message.monotonicStampNs != null && Object.hasOwnProperty.call(message, "monotonicStampNs"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.monotonicStampNs);
            return writer;
        };

        /**
         * Encodes the specified Image message, length delimited. Does not implicitly {@link normvla.Image.verify|verify} messages.
         * @function encodeDelimited
         * @memberof normvla.Image
         * @static
         * @param {normvla.IImage} message Image message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Image.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an Image message from the specified reader or buffer.
         * @function decode
         * @memberof normvla.Image
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {normvla.Image} Image
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Image.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.normvla.Image();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.jpeg = reader.bytes();
                        break;
                    }
                case 2: {
                        message.monotonicStampNs = reader.uint64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an Image message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof normvla.Image
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {normvla.Image} Image
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Image.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an Image message.
         * @function verify
         * @memberof normvla.Image
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Image.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.jpeg != null && message.hasOwnProperty("jpeg"))
                if (!(message.jpeg && typeof message.jpeg.length === "number" || $util.isString(message.jpeg)))
                    return "jpeg: buffer expected";
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (!$util.isInteger(message.monotonicStampNs) && !(message.monotonicStampNs && $util.isInteger(message.monotonicStampNs.low) && $util.isInteger(message.monotonicStampNs.high)))
                    return "monotonicStampNs: integer|Long expected";
            return null;
        };

        /**
         * Creates an Image message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof normvla.Image
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {normvla.Image} Image
         */
        Image.fromObject = function fromObject(object, long) {
            if (object instanceof $root.normvla.Image)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.normvla.Image();
            if (object.jpeg != null)
                if (typeof object.jpeg === "string")
                    $util.base64.decode(object.jpeg, message.jpeg = $util.newBuffer($util.base64.length(object.jpeg)), 0);
                else if (object.jpeg.length >= 0)
                    message.jpeg = object.jpeg;
            if (object.monotonicStampNs != null)
                if ($util.Long)
                    (message.monotonicStampNs = $util.Long.fromValue(object.monotonicStampNs)).unsigned = true;
                else if (typeof object.monotonicStampNs === "string")
                    message.monotonicStampNs = parseInt(object.monotonicStampNs, 10);
                else if (typeof object.monotonicStampNs === "number")
                    message.monotonicStampNs = object.monotonicStampNs;
                else if (typeof object.monotonicStampNs === "object")
                    message.monotonicStampNs = new $util.LongBits(object.monotonicStampNs.low >>> 0, object.monotonicStampNs.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from an Image message. Also converts values to other types if specified.
         * @function toObject
         * @memberof normvla.Image
         * @static
         * @param {normvla.Image} message Image
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Image.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.jpeg = "";
                else {
                    object.jpeg = [];
                    if (options.bytes !== Array)
                        object.jpeg = $util.newBuffer(object.jpeg);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.monotonicStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.monotonicStampNs = options.longs === String ? "0" : 0;
            }
            if (message.jpeg != null && message.hasOwnProperty("jpeg"))
                object.jpeg = options.bytes === String ? $util.base64.encode(message.jpeg, 0, message.jpeg.length) : options.bytes === Array ? Array.prototype.slice.call(message.jpeg) : message.jpeg;
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (typeof message.monotonicStampNs === "number")
                    object.monotonicStampNs = options.longs === String ? String(message.monotonicStampNs) : message.monotonicStampNs;
                else
                    object.monotonicStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.monotonicStampNs) : options.longs === Number ? new $util.LongBits(message.monotonicStampNs.low >>> 0, message.monotonicStampNs.high >>> 0).toNumber(true) : message.monotonicStampNs;
            return object;
        };

        /**
         * Converts this Image to JSON.
         * @function toJSON
         * @memberof normvla.Image
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Image.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Image
         * @function getTypeUrl
         * @memberof normvla.Image
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Image.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/normvla.Image";
        };

        return Image;
    })();

    return normvla;
})();

export const hikmicro = $root.hikmicro = (() => {

    /**
     * Namespace hikmicro.
     * @exports hikmicro
     * @namespace
     */
    const hikmicro = {};

    hikmicro.UsbDeviceInfo = (function() {

        /**
         * Properties of a UsbDeviceInfo.
         * @memberof hikmicro
         * @interface IUsbDeviceInfo
         * @property {number|null} [vendorId] UsbDeviceInfo vendorId
         * @property {number|null} [productId] UsbDeviceInfo productId
         * @property {number|null} [busNumber] UsbDeviceInfo busNumber
         * @property {number|null} [deviceNumber] UsbDeviceInfo deviceNumber
         * @property {Array.<number>|null} [portNumbers] UsbDeviceInfo portNumbers
         * @property {string|null} [uniqueId] UsbDeviceInfo uniqueId
         * @property {string|null} [manufacturer] UsbDeviceInfo manufacturer
         * @property {string|null} [product] UsbDeviceInfo product
         * @property {string|null} [serialNumber] UsbDeviceInfo serialNumber
         * @property {number|null} [usbBcd] UsbDeviceInfo usbBcd
         * @property {number|null} [deviceBcd] UsbDeviceInfo deviceBcd
         * @property {number|null} [deviceClass] UsbDeviceInfo deviceClass
         * @property {number|null} [deviceSubclass] UsbDeviceInfo deviceSubclass
         * @property {number|null} [deviceProtocol] UsbDeviceInfo deviceProtocol
         * @property {number|null} [maxPacketSize0] UsbDeviceInfo maxPacketSize0
         * @property {number|null} [numConfigurations] UsbDeviceInfo numConfigurations
         * @property {Uint8Array|null} [deviceDescriptor] UsbDeviceInfo deviceDescriptor
         * @property {Array.<Uint8Array>|null} [configDescriptors] UsbDeviceInfo configDescriptors
         */

        /**
         * Constructs a new UsbDeviceInfo.
         * @memberof hikmicro
         * @classdesc Represents a UsbDeviceInfo.
         * @implements IUsbDeviceInfo
         * @constructor
         * @param {hikmicro.IUsbDeviceInfo=} [properties] Properties to set
         */
        function UsbDeviceInfo(properties) {
            this.portNumbers = [];
            this.configDescriptors = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * UsbDeviceInfo vendorId.
         * @member {number} vendorId
         * @memberof hikmicro.UsbDeviceInfo
         * @instance
         */
        UsbDeviceInfo.prototype.vendorId = 0;

        /**
         * UsbDeviceInfo productId.
         * @member {number} productId
         * @memberof hikmicro.UsbDeviceInfo
         * @instance
         */
        UsbDeviceInfo.prototype.productId = 0;

        /**
         * UsbDeviceInfo busNumber.
         * @member {number} busNumber
         * @memberof hikmicro.UsbDeviceInfo
         * @instance
         */
        UsbDeviceInfo.prototype.busNumber = 0;

        /**
         * UsbDeviceInfo deviceNumber.
         * @member {number} deviceNumber
         * @memberof hikmicro.UsbDeviceInfo
         * @instance
         */
        UsbDeviceInfo.prototype.deviceNumber = 0;

        /**
         * UsbDeviceInfo portNumbers.
         * @member {Array.<number>} portNumbers
         * @memberof hikmicro.UsbDeviceInfo
         * @instance
         */
        UsbDeviceInfo.prototype.portNumbers = $util.emptyArray;

        /**
         * UsbDeviceInfo uniqueId.
         * @member {string} uniqueId
         * @memberof hikmicro.UsbDeviceInfo
         * @instance
         */
        UsbDeviceInfo.prototype.uniqueId = "";

        /**
         * UsbDeviceInfo manufacturer.
         * @member {string} manufacturer
         * @memberof hikmicro.UsbDeviceInfo
         * @instance
         */
        UsbDeviceInfo.prototype.manufacturer = "";

        /**
         * UsbDeviceInfo product.
         * @member {string} product
         * @memberof hikmicro.UsbDeviceInfo
         * @instance
         */
        UsbDeviceInfo.prototype.product = "";

        /**
         * UsbDeviceInfo serialNumber.
         * @member {string} serialNumber
         * @memberof hikmicro.UsbDeviceInfo
         * @instance
         */
        UsbDeviceInfo.prototype.serialNumber = "";

        /**
         * UsbDeviceInfo usbBcd.
         * @member {number} usbBcd
         * @memberof hikmicro.UsbDeviceInfo
         * @instance
         */
        UsbDeviceInfo.prototype.usbBcd = 0;

        /**
         * UsbDeviceInfo deviceBcd.
         * @member {number} deviceBcd
         * @memberof hikmicro.UsbDeviceInfo
         * @instance
         */
        UsbDeviceInfo.prototype.deviceBcd = 0;

        /**
         * UsbDeviceInfo deviceClass.
         * @member {number} deviceClass
         * @memberof hikmicro.UsbDeviceInfo
         * @instance
         */
        UsbDeviceInfo.prototype.deviceClass = 0;

        /**
         * UsbDeviceInfo deviceSubclass.
         * @member {number} deviceSubclass
         * @memberof hikmicro.UsbDeviceInfo
         * @instance
         */
        UsbDeviceInfo.prototype.deviceSubclass = 0;

        /**
         * UsbDeviceInfo deviceProtocol.
         * @member {number} deviceProtocol
         * @memberof hikmicro.UsbDeviceInfo
         * @instance
         */
        UsbDeviceInfo.prototype.deviceProtocol = 0;

        /**
         * UsbDeviceInfo maxPacketSize0.
         * @member {number} maxPacketSize0
         * @memberof hikmicro.UsbDeviceInfo
         * @instance
         */
        UsbDeviceInfo.prototype.maxPacketSize0 = 0;

        /**
         * UsbDeviceInfo numConfigurations.
         * @member {number} numConfigurations
         * @memberof hikmicro.UsbDeviceInfo
         * @instance
         */
        UsbDeviceInfo.prototype.numConfigurations = 0;

        /**
         * UsbDeviceInfo deviceDescriptor.
         * @member {Uint8Array} deviceDescriptor
         * @memberof hikmicro.UsbDeviceInfo
         * @instance
         */
        UsbDeviceInfo.prototype.deviceDescriptor = $util.newBuffer([]);

        /**
         * UsbDeviceInfo configDescriptors.
         * @member {Array.<Uint8Array>} configDescriptors
         * @memberof hikmicro.UsbDeviceInfo
         * @instance
         */
        UsbDeviceInfo.prototype.configDescriptors = $util.emptyArray;

        /**
         * Creates a new UsbDeviceInfo instance using the specified properties.
         * @function create
         * @memberof hikmicro.UsbDeviceInfo
         * @static
         * @param {hikmicro.IUsbDeviceInfo=} [properties] Properties to set
         * @returns {hikmicro.UsbDeviceInfo} UsbDeviceInfo instance
         */
        UsbDeviceInfo.create = function create(properties) {
            return new UsbDeviceInfo(properties);
        };

        /**
         * Encodes the specified UsbDeviceInfo message. Does not implicitly {@link hikmicro.UsbDeviceInfo.verify|verify} messages.
         * @function encode
         * @memberof hikmicro.UsbDeviceInfo
         * @static
         * @param {hikmicro.IUsbDeviceInfo} message UsbDeviceInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UsbDeviceInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.vendorId != null && Object.hasOwnProperty.call(message, "vendorId"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.vendorId);
            if (message.productId != null && Object.hasOwnProperty.call(message, "productId"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.productId);
            if (message.busNumber != null && Object.hasOwnProperty.call(message, "busNumber"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.busNumber);
            if (message.deviceNumber != null && Object.hasOwnProperty.call(message, "deviceNumber"))
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.deviceNumber);
            if (message.portNumbers != null && message.portNumbers.length) {
                writer.uint32(/* id 5, wireType 2 =*/42).fork();
                for (let i = 0; i < message.portNumbers.length; ++i)
                    writer.uint32(message.portNumbers[i]);
                writer.ldelim();
            }
            if (message.uniqueId != null && Object.hasOwnProperty.call(message, "uniqueId"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.uniqueId);
            if (message.manufacturer != null && Object.hasOwnProperty.call(message, "manufacturer"))
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.manufacturer);
            if (message.product != null && Object.hasOwnProperty.call(message, "product"))
                writer.uint32(/* id 8, wireType 2 =*/66).string(message.product);
            if (message.serialNumber != null && Object.hasOwnProperty.call(message, "serialNumber"))
                writer.uint32(/* id 9, wireType 2 =*/74).string(message.serialNumber);
            if (message.usbBcd != null && Object.hasOwnProperty.call(message, "usbBcd"))
                writer.uint32(/* id 20, wireType 0 =*/160).uint32(message.usbBcd);
            if (message.deviceBcd != null && Object.hasOwnProperty.call(message, "deviceBcd"))
                writer.uint32(/* id 21, wireType 0 =*/168).uint32(message.deviceBcd);
            if (message.deviceClass != null && Object.hasOwnProperty.call(message, "deviceClass"))
                writer.uint32(/* id 22, wireType 0 =*/176).uint32(message.deviceClass);
            if (message.deviceSubclass != null && Object.hasOwnProperty.call(message, "deviceSubclass"))
                writer.uint32(/* id 23, wireType 0 =*/184).uint32(message.deviceSubclass);
            if (message.deviceProtocol != null && Object.hasOwnProperty.call(message, "deviceProtocol"))
                writer.uint32(/* id 24, wireType 0 =*/192).uint32(message.deviceProtocol);
            if (message.maxPacketSize0 != null && Object.hasOwnProperty.call(message, "maxPacketSize0"))
                writer.uint32(/* id 25, wireType 0 =*/200).uint32(message.maxPacketSize0);
            if (message.numConfigurations != null && Object.hasOwnProperty.call(message, "numConfigurations"))
                writer.uint32(/* id 26, wireType 0 =*/208).uint32(message.numConfigurations);
            if (message.deviceDescriptor != null && Object.hasOwnProperty.call(message, "deviceDescriptor"))
                writer.uint32(/* id 40, wireType 2 =*/322).bytes(message.deviceDescriptor);
            if (message.configDescriptors != null && message.configDescriptors.length)
                for (let i = 0; i < message.configDescriptors.length; ++i)
                    writer.uint32(/* id 41, wireType 2 =*/330).bytes(message.configDescriptors[i]);
            return writer;
        };

        /**
         * Encodes the specified UsbDeviceInfo message, length delimited. Does not implicitly {@link hikmicro.UsbDeviceInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof hikmicro.UsbDeviceInfo
         * @static
         * @param {hikmicro.IUsbDeviceInfo} message UsbDeviceInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UsbDeviceInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a UsbDeviceInfo message from the specified reader or buffer.
         * @function decode
         * @memberof hikmicro.UsbDeviceInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {hikmicro.UsbDeviceInfo} UsbDeviceInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UsbDeviceInfo.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.hikmicro.UsbDeviceInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.vendorId = reader.uint32();
                        break;
                    }
                case 2: {
                        message.productId = reader.uint32();
                        break;
                    }
                case 3: {
                        message.busNumber = reader.uint32();
                        break;
                    }
                case 4: {
                        message.deviceNumber = reader.uint32();
                        break;
                    }
                case 5: {
                        if (!(message.portNumbers && message.portNumbers.length))
                            message.portNumbers = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.portNumbers.push(reader.uint32());
                        } else
                            message.portNumbers.push(reader.uint32());
                        break;
                    }
                case 6: {
                        message.uniqueId = reader.string();
                        break;
                    }
                case 7: {
                        message.manufacturer = reader.string();
                        break;
                    }
                case 8: {
                        message.product = reader.string();
                        break;
                    }
                case 9: {
                        message.serialNumber = reader.string();
                        break;
                    }
                case 20: {
                        message.usbBcd = reader.uint32();
                        break;
                    }
                case 21: {
                        message.deviceBcd = reader.uint32();
                        break;
                    }
                case 22: {
                        message.deviceClass = reader.uint32();
                        break;
                    }
                case 23: {
                        message.deviceSubclass = reader.uint32();
                        break;
                    }
                case 24: {
                        message.deviceProtocol = reader.uint32();
                        break;
                    }
                case 25: {
                        message.maxPacketSize0 = reader.uint32();
                        break;
                    }
                case 26: {
                        message.numConfigurations = reader.uint32();
                        break;
                    }
                case 40: {
                        message.deviceDescriptor = reader.bytes();
                        break;
                    }
                case 41: {
                        if (!(message.configDescriptors && message.configDescriptors.length))
                            message.configDescriptors = [];
                        message.configDescriptors.push(reader.bytes());
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a UsbDeviceInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof hikmicro.UsbDeviceInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {hikmicro.UsbDeviceInfo} UsbDeviceInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UsbDeviceInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a UsbDeviceInfo message.
         * @function verify
         * @memberof hikmicro.UsbDeviceInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        UsbDeviceInfo.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.vendorId != null && message.hasOwnProperty("vendorId"))
                if (!$util.isInteger(message.vendorId))
                    return "vendorId: integer expected";
            if (message.productId != null && message.hasOwnProperty("productId"))
                if (!$util.isInteger(message.productId))
                    return "productId: integer expected";
            if (message.busNumber != null && message.hasOwnProperty("busNumber"))
                if (!$util.isInteger(message.busNumber))
                    return "busNumber: integer expected";
            if (message.deviceNumber != null && message.hasOwnProperty("deviceNumber"))
                if (!$util.isInteger(message.deviceNumber))
                    return "deviceNumber: integer expected";
            if (message.portNumbers != null && message.hasOwnProperty("portNumbers")) {
                if (!Array.isArray(message.portNumbers))
                    return "portNumbers: array expected";
                for (let i = 0; i < message.portNumbers.length; ++i)
                    if (!$util.isInteger(message.portNumbers[i]))
                        return "portNumbers: integer[] expected";
            }
            if (message.uniqueId != null && message.hasOwnProperty("uniqueId"))
                if (!$util.isString(message.uniqueId))
                    return "uniqueId: string expected";
            if (message.manufacturer != null && message.hasOwnProperty("manufacturer"))
                if (!$util.isString(message.manufacturer))
                    return "manufacturer: string expected";
            if (message.product != null && message.hasOwnProperty("product"))
                if (!$util.isString(message.product))
                    return "product: string expected";
            if (message.serialNumber != null && message.hasOwnProperty("serialNumber"))
                if (!$util.isString(message.serialNumber))
                    return "serialNumber: string expected";
            if (message.usbBcd != null && message.hasOwnProperty("usbBcd"))
                if (!$util.isInteger(message.usbBcd))
                    return "usbBcd: integer expected";
            if (message.deviceBcd != null && message.hasOwnProperty("deviceBcd"))
                if (!$util.isInteger(message.deviceBcd))
                    return "deviceBcd: integer expected";
            if (message.deviceClass != null && message.hasOwnProperty("deviceClass"))
                if (!$util.isInteger(message.deviceClass))
                    return "deviceClass: integer expected";
            if (message.deviceSubclass != null && message.hasOwnProperty("deviceSubclass"))
                if (!$util.isInteger(message.deviceSubclass))
                    return "deviceSubclass: integer expected";
            if (message.deviceProtocol != null && message.hasOwnProperty("deviceProtocol"))
                if (!$util.isInteger(message.deviceProtocol))
                    return "deviceProtocol: integer expected";
            if (message.maxPacketSize0 != null && message.hasOwnProperty("maxPacketSize0"))
                if (!$util.isInteger(message.maxPacketSize0))
                    return "maxPacketSize0: integer expected";
            if (message.numConfigurations != null && message.hasOwnProperty("numConfigurations"))
                if (!$util.isInteger(message.numConfigurations))
                    return "numConfigurations: integer expected";
            if (message.deviceDescriptor != null && message.hasOwnProperty("deviceDescriptor"))
                if (!(message.deviceDescriptor && typeof message.deviceDescriptor.length === "number" || $util.isString(message.deviceDescriptor)))
                    return "deviceDescriptor: buffer expected";
            if (message.configDescriptors != null && message.hasOwnProperty("configDescriptors")) {
                if (!Array.isArray(message.configDescriptors))
                    return "configDescriptors: array expected";
                for (let i = 0; i < message.configDescriptors.length; ++i)
                    if (!(message.configDescriptors[i] && typeof message.configDescriptors[i].length === "number" || $util.isString(message.configDescriptors[i])))
                        return "configDescriptors: buffer[] expected";
            }
            return null;
        };

        /**
         * Creates a UsbDeviceInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof hikmicro.UsbDeviceInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {hikmicro.UsbDeviceInfo} UsbDeviceInfo
         */
        UsbDeviceInfo.fromObject = function fromObject(object, long) {
            if (object instanceof $root.hikmicro.UsbDeviceInfo)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.hikmicro.UsbDeviceInfo();
            if (object.vendorId != null)
                message.vendorId = object.vendorId >>> 0;
            if (object.productId != null)
                message.productId = object.productId >>> 0;
            if (object.busNumber != null)
                message.busNumber = object.busNumber >>> 0;
            if (object.deviceNumber != null)
                message.deviceNumber = object.deviceNumber >>> 0;
            if (object.portNumbers) {
                if (!Array.isArray(object.portNumbers))
                    throw TypeError(".hikmicro.UsbDeviceInfo.portNumbers: array expected");
                message.portNumbers = [];
                for (let i = 0; i < object.portNumbers.length; ++i)
                    message.portNumbers[i] = object.portNumbers[i] >>> 0;
            }
            if (object.uniqueId != null)
                message.uniqueId = String(object.uniqueId);
            if (object.manufacturer != null)
                message.manufacturer = String(object.manufacturer);
            if (object.product != null)
                message.product = String(object.product);
            if (object.serialNumber != null)
                message.serialNumber = String(object.serialNumber);
            if (object.usbBcd != null)
                message.usbBcd = object.usbBcd >>> 0;
            if (object.deviceBcd != null)
                message.deviceBcd = object.deviceBcd >>> 0;
            if (object.deviceClass != null)
                message.deviceClass = object.deviceClass >>> 0;
            if (object.deviceSubclass != null)
                message.deviceSubclass = object.deviceSubclass >>> 0;
            if (object.deviceProtocol != null)
                message.deviceProtocol = object.deviceProtocol >>> 0;
            if (object.maxPacketSize0 != null)
                message.maxPacketSize0 = object.maxPacketSize0 >>> 0;
            if (object.numConfigurations != null)
                message.numConfigurations = object.numConfigurations >>> 0;
            if (object.deviceDescriptor != null)
                if (typeof object.deviceDescriptor === "string")
                    $util.base64.decode(object.deviceDescriptor, message.deviceDescriptor = $util.newBuffer($util.base64.length(object.deviceDescriptor)), 0);
                else if (object.deviceDescriptor.length >= 0)
                    message.deviceDescriptor = object.deviceDescriptor;
            if (object.configDescriptors) {
                if (!Array.isArray(object.configDescriptors))
                    throw TypeError(".hikmicro.UsbDeviceInfo.configDescriptors: array expected");
                message.configDescriptors = [];
                for (let i = 0; i < object.configDescriptors.length; ++i)
                    if (typeof object.configDescriptors[i] === "string")
                        $util.base64.decode(object.configDescriptors[i], message.configDescriptors[i] = $util.newBuffer($util.base64.length(object.configDescriptors[i])), 0);
                    else if (object.configDescriptors[i].length >= 0)
                        message.configDescriptors[i] = object.configDescriptors[i];
            }
            return message;
        };

        /**
         * Creates a plain object from a UsbDeviceInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof hikmicro.UsbDeviceInfo
         * @static
         * @param {hikmicro.UsbDeviceInfo} message UsbDeviceInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        UsbDeviceInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.portNumbers = [];
                object.configDescriptors = [];
            }
            if (options.defaults) {
                object.vendorId = 0;
                object.productId = 0;
                object.busNumber = 0;
                object.deviceNumber = 0;
                object.uniqueId = "";
                object.manufacturer = "";
                object.product = "";
                object.serialNumber = "";
                object.usbBcd = 0;
                object.deviceBcd = 0;
                object.deviceClass = 0;
                object.deviceSubclass = 0;
                object.deviceProtocol = 0;
                object.maxPacketSize0 = 0;
                object.numConfigurations = 0;
                if (options.bytes === String)
                    object.deviceDescriptor = "";
                else {
                    object.deviceDescriptor = [];
                    if (options.bytes !== Array)
                        object.deviceDescriptor = $util.newBuffer(object.deviceDescriptor);
                }
            }
            if (message.vendorId != null && message.hasOwnProperty("vendorId"))
                object.vendorId = message.vendorId;
            if (message.productId != null && message.hasOwnProperty("productId"))
                object.productId = message.productId;
            if (message.busNumber != null && message.hasOwnProperty("busNumber"))
                object.busNumber = message.busNumber;
            if (message.deviceNumber != null && message.hasOwnProperty("deviceNumber"))
                object.deviceNumber = message.deviceNumber;
            if (message.portNumbers && message.portNumbers.length) {
                object.portNumbers = [];
                for (let j = 0; j < message.portNumbers.length; ++j)
                    object.portNumbers[j] = message.portNumbers[j];
            }
            if (message.uniqueId != null && message.hasOwnProperty("uniqueId"))
                object.uniqueId = message.uniqueId;
            if (message.manufacturer != null && message.hasOwnProperty("manufacturer"))
                object.manufacturer = message.manufacturer;
            if (message.product != null && message.hasOwnProperty("product"))
                object.product = message.product;
            if (message.serialNumber != null && message.hasOwnProperty("serialNumber"))
                object.serialNumber = message.serialNumber;
            if (message.usbBcd != null && message.hasOwnProperty("usbBcd"))
                object.usbBcd = message.usbBcd;
            if (message.deviceBcd != null && message.hasOwnProperty("deviceBcd"))
                object.deviceBcd = message.deviceBcd;
            if (message.deviceClass != null && message.hasOwnProperty("deviceClass"))
                object.deviceClass = message.deviceClass;
            if (message.deviceSubclass != null && message.hasOwnProperty("deviceSubclass"))
                object.deviceSubclass = message.deviceSubclass;
            if (message.deviceProtocol != null && message.hasOwnProperty("deviceProtocol"))
                object.deviceProtocol = message.deviceProtocol;
            if (message.maxPacketSize0 != null && message.hasOwnProperty("maxPacketSize0"))
                object.maxPacketSize0 = message.maxPacketSize0;
            if (message.numConfigurations != null && message.hasOwnProperty("numConfigurations"))
                object.numConfigurations = message.numConfigurations;
            if (message.deviceDescriptor != null && message.hasOwnProperty("deviceDescriptor"))
                object.deviceDescriptor = options.bytes === String ? $util.base64.encode(message.deviceDescriptor, 0, message.deviceDescriptor.length) : options.bytes === Array ? Array.prototype.slice.call(message.deviceDescriptor) : message.deviceDescriptor;
            if (message.configDescriptors && message.configDescriptors.length) {
                object.configDescriptors = [];
                for (let j = 0; j < message.configDescriptors.length; ++j)
                    object.configDescriptors[j] = options.bytes === String ? $util.base64.encode(message.configDescriptors[j], 0, message.configDescriptors[j].length) : options.bytes === Array ? Array.prototype.slice.call(message.configDescriptors[j]) : message.configDescriptors[j];
            }
            return object;
        };

        /**
         * Converts this UsbDeviceInfo to JSON.
         * @function toJSON
         * @memberof hikmicro.UsbDeviceInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        UsbDeviceInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for UsbDeviceInfo
         * @function getTypeUrl
         * @memberof hikmicro.UsbDeviceInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        UsbDeviceInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/hikmicro.UsbDeviceInfo";
        };

        return UsbDeviceInfo;
    })();

    hikmicro.CompactStreamFormat = (function() {

        /**
         * Properties of a CompactStreamFormat.
         * @memberof hikmicro
         * @interface ICompactStreamFormat
         * @property {number|null} [fourcc] CompactStreamFormat fourcc
         * @property {number|null} [formatIndex] CompactStreamFormat formatIndex
         * @property {number|null} [frameIndex] CompactStreamFormat frameIndex
         * @property {number|null} [uvcWidth] CompactStreamFormat uvcWidth
         * @property {number|null} [uvcHeight] CompactStreamFormat uvcHeight
         * @property {number|null} [framesPerSecond] CompactStreamFormat framesPerSecond
         * @property {Uint8Array|null} [guid] CompactStreamFormat guid
         * @property {number|null} [sourceFormat] CompactStreamFormat sourceFormat
         */

        /**
         * Constructs a new CompactStreamFormat.
         * @memberof hikmicro
         * @classdesc Represents a CompactStreamFormat.
         * @implements ICompactStreamFormat
         * @constructor
         * @param {hikmicro.ICompactStreamFormat=} [properties] Properties to set
         */
        function CompactStreamFormat(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CompactStreamFormat fourcc.
         * @member {number} fourcc
         * @memberof hikmicro.CompactStreamFormat
         * @instance
         */
        CompactStreamFormat.prototype.fourcc = 0;

        /**
         * CompactStreamFormat formatIndex.
         * @member {number} formatIndex
         * @memberof hikmicro.CompactStreamFormat
         * @instance
         */
        CompactStreamFormat.prototype.formatIndex = 0;

        /**
         * CompactStreamFormat frameIndex.
         * @member {number} frameIndex
         * @memberof hikmicro.CompactStreamFormat
         * @instance
         */
        CompactStreamFormat.prototype.frameIndex = 0;

        /**
         * CompactStreamFormat uvcWidth.
         * @member {number} uvcWidth
         * @memberof hikmicro.CompactStreamFormat
         * @instance
         */
        CompactStreamFormat.prototype.uvcWidth = 0;

        /**
         * CompactStreamFormat uvcHeight.
         * @member {number} uvcHeight
         * @memberof hikmicro.CompactStreamFormat
         * @instance
         */
        CompactStreamFormat.prototype.uvcHeight = 0;

        /**
         * CompactStreamFormat framesPerSecond.
         * @member {number} framesPerSecond
         * @memberof hikmicro.CompactStreamFormat
         * @instance
         */
        CompactStreamFormat.prototype.framesPerSecond = 0;

        /**
         * CompactStreamFormat guid.
         * @member {Uint8Array} guid
         * @memberof hikmicro.CompactStreamFormat
         * @instance
         */
        CompactStreamFormat.prototype.guid = $util.newBuffer([]);

        /**
         * CompactStreamFormat sourceFormat.
         * @member {number} sourceFormat
         * @memberof hikmicro.CompactStreamFormat
         * @instance
         */
        CompactStreamFormat.prototype.sourceFormat = 0;

        /**
         * Creates a new CompactStreamFormat instance using the specified properties.
         * @function create
         * @memberof hikmicro.CompactStreamFormat
         * @static
         * @param {hikmicro.ICompactStreamFormat=} [properties] Properties to set
         * @returns {hikmicro.CompactStreamFormat} CompactStreamFormat instance
         */
        CompactStreamFormat.create = function create(properties) {
            return new CompactStreamFormat(properties);
        };

        /**
         * Encodes the specified CompactStreamFormat message. Does not implicitly {@link hikmicro.CompactStreamFormat.verify|verify} messages.
         * @function encode
         * @memberof hikmicro.CompactStreamFormat
         * @static
         * @param {hikmicro.ICompactStreamFormat} message CompactStreamFormat message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CompactStreamFormat.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.fourcc != null && Object.hasOwnProperty.call(message, "fourcc"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.fourcc);
            if (message.formatIndex != null && Object.hasOwnProperty.call(message, "formatIndex"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.formatIndex);
            if (message.frameIndex != null && Object.hasOwnProperty.call(message, "frameIndex"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.frameIndex);
            if (message.uvcWidth != null && Object.hasOwnProperty.call(message, "uvcWidth"))
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.uvcWidth);
            if (message.uvcHeight != null && Object.hasOwnProperty.call(message, "uvcHeight"))
                writer.uint32(/* id 5, wireType 0 =*/40).uint32(message.uvcHeight);
            if (message.framesPerSecond != null && Object.hasOwnProperty.call(message, "framesPerSecond"))
                writer.uint32(/* id 6, wireType 5 =*/53).float(message.framesPerSecond);
            if (message.guid != null && Object.hasOwnProperty.call(message, "guid"))
                writer.uint32(/* id 7, wireType 2 =*/58).bytes(message.guid);
            if (message.sourceFormat != null && Object.hasOwnProperty.call(message, "sourceFormat"))
                writer.uint32(/* id 8, wireType 0 =*/64).uint32(message.sourceFormat);
            return writer;
        };

        /**
         * Encodes the specified CompactStreamFormat message, length delimited. Does not implicitly {@link hikmicro.CompactStreamFormat.verify|verify} messages.
         * @function encodeDelimited
         * @memberof hikmicro.CompactStreamFormat
         * @static
         * @param {hikmicro.ICompactStreamFormat} message CompactStreamFormat message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CompactStreamFormat.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CompactStreamFormat message from the specified reader or buffer.
         * @function decode
         * @memberof hikmicro.CompactStreamFormat
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {hikmicro.CompactStreamFormat} CompactStreamFormat
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CompactStreamFormat.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.hikmicro.CompactStreamFormat();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.fourcc = reader.uint32();
                        break;
                    }
                case 2: {
                        message.formatIndex = reader.uint32();
                        break;
                    }
                case 3: {
                        message.frameIndex = reader.uint32();
                        break;
                    }
                case 4: {
                        message.uvcWidth = reader.uint32();
                        break;
                    }
                case 5: {
                        message.uvcHeight = reader.uint32();
                        break;
                    }
                case 6: {
                        message.framesPerSecond = reader.float();
                        break;
                    }
                case 7: {
                        message.guid = reader.bytes();
                        break;
                    }
                case 8: {
                        message.sourceFormat = reader.uint32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CompactStreamFormat message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof hikmicro.CompactStreamFormat
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {hikmicro.CompactStreamFormat} CompactStreamFormat
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CompactStreamFormat.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CompactStreamFormat message.
         * @function verify
         * @memberof hikmicro.CompactStreamFormat
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CompactStreamFormat.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.fourcc != null && message.hasOwnProperty("fourcc"))
                if (!$util.isInteger(message.fourcc))
                    return "fourcc: integer expected";
            if (message.formatIndex != null && message.hasOwnProperty("formatIndex"))
                if (!$util.isInteger(message.formatIndex))
                    return "formatIndex: integer expected";
            if (message.frameIndex != null && message.hasOwnProperty("frameIndex"))
                if (!$util.isInteger(message.frameIndex))
                    return "frameIndex: integer expected";
            if (message.uvcWidth != null && message.hasOwnProperty("uvcWidth"))
                if (!$util.isInteger(message.uvcWidth))
                    return "uvcWidth: integer expected";
            if (message.uvcHeight != null && message.hasOwnProperty("uvcHeight"))
                if (!$util.isInteger(message.uvcHeight))
                    return "uvcHeight: integer expected";
            if (message.framesPerSecond != null && message.hasOwnProperty("framesPerSecond"))
                if (typeof message.framesPerSecond !== "number")
                    return "framesPerSecond: number expected";
            if (message.guid != null && message.hasOwnProperty("guid"))
                if (!(message.guid && typeof message.guid.length === "number" || $util.isString(message.guid)))
                    return "guid: buffer expected";
            if (message.sourceFormat != null && message.hasOwnProperty("sourceFormat"))
                if (!$util.isInteger(message.sourceFormat))
                    return "sourceFormat: integer expected";
            return null;
        };

        /**
         * Creates a CompactStreamFormat message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof hikmicro.CompactStreamFormat
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {hikmicro.CompactStreamFormat} CompactStreamFormat
         */
        CompactStreamFormat.fromObject = function fromObject(object, long) {
            if (object instanceof $root.hikmicro.CompactStreamFormat)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.hikmicro.CompactStreamFormat();
            if (object.fourcc != null)
                message.fourcc = object.fourcc >>> 0;
            if (object.formatIndex != null)
                message.formatIndex = object.formatIndex >>> 0;
            if (object.frameIndex != null)
                message.frameIndex = object.frameIndex >>> 0;
            if (object.uvcWidth != null)
                message.uvcWidth = object.uvcWidth >>> 0;
            if (object.uvcHeight != null)
                message.uvcHeight = object.uvcHeight >>> 0;
            if (object.framesPerSecond != null)
                message.framesPerSecond = Number(object.framesPerSecond);
            if (object.guid != null)
                if (typeof object.guid === "string")
                    $util.base64.decode(object.guid, message.guid = $util.newBuffer($util.base64.length(object.guid)), 0);
                else if (object.guid.length >= 0)
                    message.guid = object.guid;
            if (object.sourceFormat != null)
                message.sourceFormat = object.sourceFormat >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a CompactStreamFormat message. Also converts values to other types if specified.
         * @function toObject
         * @memberof hikmicro.CompactStreamFormat
         * @static
         * @param {hikmicro.CompactStreamFormat} message CompactStreamFormat
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CompactStreamFormat.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.fourcc = 0;
                object.formatIndex = 0;
                object.frameIndex = 0;
                object.uvcWidth = 0;
                object.uvcHeight = 0;
                object.framesPerSecond = 0;
                if (options.bytes === String)
                    object.guid = "";
                else {
                    object.guid = [];
                    if (options.bytes !== Array)
                        object.guid = $util.newBuffer(object.guid);
                }
                object.sourceFormat = 0;
            }
            if (message.fourcc != null && message.hasOwnProperty("fourcc"))
                object.fourcc = message.fourcc;
            if (message.formatIndex != null && message.hasOwnProperty("formatIndex"))
                object.formatIndex = message.formatIndex;
            if (message.frameIndex != null && message.hasOwnProperty("frameIndex"))
                object.frameIndex = message.frameIndex;
            if (message.uvcWidth != null && message.hasOwnProperty("uvcWidth"))
                object.uvcWidth = message.uvcWidth;
            if (message.uvcHeight != null && message.hasOwnProperty("uvcHeight"))
                object.uvcHeight = message.uvcHeight;
            if (message.framesPerSecond != null && message.hasOwnProperty("framesPerSecond"))
                object.framesPerSecond = options.json && !isFinite(message.framesPerSecond) ? String(message.framesPerSecond) : message.framesPerSecond;
            if (message.guid != null && message.hasOwnProperty("guid"))
                object.guid = options.bytes === String ? $util.base64.encode(message.guid, 0, message.guid.length) : options.bytes === Array ? Array.prototype.slice.call(message.guid) : message.guid;
            if (message.sourceFormat != null && message.hasOwnProperty("sourceFormat"))
                object.sourceFormat = message.sourceFormat;
            return object;
        };

        /**
         * Converts this CompactStreamFormat to JSON.
         * @function toJSON
         * @memberof hikmicro.CompactStreamFormat
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CompactStreamFormat.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CompactStreamFormat
         * @function getTypeUrl
         * @memberof hikmicro.CompactStreamFormat
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CompactStreamFormat.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/hikmicro.CompactStreamFormat";
        };

        return CompactStreamFormat;
    })();

    hikmicro.CompactPayloadLayout = (function() {

        /**
         * Properties of a CompactPayloadLayout.
         * @memberof hikmicro
         * @interface ICompactPayloadLayout
         * @property {number|null} [sensorWidth] CompactPayloadLayout sensorWidth
         * @property {number|null} [sensorHeight] CompactPayloadLayout sensorHeight
         * @property {number|null} [payloadLength] CompactPayloadLayout payloadLength
         * @property {number|null} [thermalY16Offset] CompactPayloadLayout thermalY16Offset
         * @property {number|null} [thermalY16Length] CompactPayloadLayout thermalY16Length
         * @property {number|null} [runtimeBlockOffset] CompactPayloadLayout runtimeBlockOffset
         * @property {number|null} [runtimeBlockLength] CompactPayloadLayout runtimeBlockLength
         */

        /**
         * Constructs a new CompactPayloadLayout.
         * @memberof hikmicro
         * @classdesc Represents a CompactPayloadLayout.
         * @implements ICompactPayloadLayout
         * @constructor
         * @param {hikmicro.ICompactPayloadLayout=} [properties] Properties to set
         */
        function CompactPayloadLayout(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CompactPayloadLayout sensorWidth.
         * @member {number} sensorWidth
         * @memberof hikmicro.CompactPayloadLayout
         * @instance
         */
        CompactPayloadLayout.prototype.sensorWidth = 0;

        /**
         * CompactPayloadLayout sensorHeight.
         * @member {number} sensorHeight
         * @memberof hikmicro.CompactPayloadLayout
         * @instance
         */
        CompactPayloadLayout.prototype.sensorHeight = 0;

        /**
         * CompactPayloadLayout payloadLength.
         * @member {number} payloadLength
         * @memberof hikmicro.CompactPayloadLayout
         * @instance
         */
        CompactPayloadLayout.prototype.payloadLength = 0;

        /**
         * CompactPayloadLayout thermalY16Offset.
         * @member {number} thermalY16Offset
         * @memberof hikmicro.CompactPayloadLayout
         * @instance
         */
        CompactPayloadLayout.prototype.thermalY16Offset = 0;

        /**
         * CompactPayloadLayout thermalY16Length.
         * @member {number} thermalY16Length
         * @memberof hikmicro.CompactPayloadLayout
         * @instance
         */
        CompactPayloadLayout.prototype.thermalY16Length = 0;

        /**
         * CompactPayloadLayout runtimeBlockOffset.
         * @member {number} runtimeBlockOffset
         * @memberof hikmicro.CompactPayloadLayout
         * @instance
         */
        CompactPayloadLayout.prototype.runtimeBlockOffset = 0;

        /**
         * CompactPayloadLayout runtimeBlockLength.
         * @member {number} runtimeBlockLength
         * @memberof hikmicro.CompactPayloadLayout
         * @instance
         */
        CompactPayloadLayout.prototype.runtimeBlockLength = 0;

        /**
         * Creates a new CompactPayloadLayout instance using the specified properties.
         * @function create
         * @memberof hikmicro.CompactPayloadLayout
         * @static
         * @param {hikmicro.ICompactPayloadLayout=} [properties] Properties to set
         * @returns {hikmicro.CompactPayloadLayout} CompactPayloadLayout instance
         */
        CompactPayloadLayout.create = function create(properties) {
            return new CompactPayloadLayout(properties);
        };

        /**
         * Encodes the specified CompactPayloadLayout message. Does not implicitly {@link hikmicro.CompactPayloadLayout.verify|verify} messages.
         * @function encode
         * @memberof hikmicro.CompactPayloadLayout
         * @static
         * @param {hikmicro.ICompactPayloadLayout} message CompactPayloadLayout message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CompactPayloadLayout.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.sensorWidth != null && Object.hasOwnProperty.call(message, "sensorWidth"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.sensorWidth);
            if (message.sensorHeight != null && Object.hasOwnProperty.call(message, "sensorHeight"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.sensorHeight);
            if (message.payloadLength != null && Object.hasOwnProperty.call(message, "payloadLength"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.payloadLength);
            if (message.thermalY16Offset != null && Object.hasOwnProperty.call(message, "thermalY16Offset"))
                writer.uint32(/* id 10, wireType 0 =*/80).uint32(message.thermalY16Offset);
            if (message.thermalY16Length != null && Object.hasOwnProperty.call(message, "thermalY16Length"))
                writer.uint32(/* id 11, wireType 0 =*/88).uint32(message.thermalY16Length);
            if (message.runtimeBlockOffset != null && Object.hasOwnProperty.call(message, "runtimeBlockOffset"))
                writer.uint32(/* id 12, wireType 0 =*/96).uint32(message.runtimeBlockOffset);
            if (message.runtimeBlockLength != null && Object.hasOwnProperty.call(message, "runtimeBlockLength"))
                writer.uint32(/* id 13, wireType 0 =*/104).uint32(message.runtimeBlockLength);
            return writer;
        };

        /**
         * Encodes the specified CompactPayloadLayout message, length delimited. Does not implicitly {@link hikmicro.CompactPayloadLayout.verify|verify} messages.
         * @function encodeDelimited
         * @memberof hikmicro.CompactPayloadLayout
         * @static
         * @param {hikmicro.ICompactPayloadLayout} message CompactPayloadLayout message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CompactPayloadLayout.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CompactPayloadLayout message from the specified reader or buffer.
         * @function decode
         * @memberof hikmicro.CompactPayloadLayout
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {hikmicro.CompactPayloadLayout} CompactPayloadLayout
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CompactPayloadLayout.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.hikmicro.CompactPayloadLayout();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.sensorWidth = reader.uint32();
                        break;
                    }
                case 2: {
                        message.sensorHeight = reader.uint32();
                        break;
                    }
                case 3: {
                        message.payloadLength = reader.uint32();
                        break;
                    }
                case 10: {
                        message.thermalY16Offset = reader.uint32();
                        break;
                    }
                case 11: {
                        message.thermalY16Length = reader.uint32();
                        break;
                    }
                case 12: {
                        message.runtimeBlockOffset = reader.uint32();
                        break;
                    }
                case 13: {
                        message.runtimeBlockLength = reader.uint32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CompactPayloadLayout message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof hikmicro.CompactPayloadLayout
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {hikmicro.CompactPayloadLayout} CompactPayloadLayout
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CompactPayloadLayout.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CompactPayloadLayout message.
         * @function verify
         * @memberof hikmicro.CompactPayloadLayout
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CompactPayloadLayout.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.sensorWidth != null && message.hasOwnProperty("sensorWidth"))
                if (!$util.isInteger(message.sensorWidth))
                    return "sensorWidth: integer expected";
            if (message.sensorHeight != null && message.hasOwnProperty("sensorHeight"))
                if (!$util.isInteger(message.sensorHeight))
                    return "sensorHeight: integer expected";
            if (message.payloadLength != null && message.hasOwnProperty("payloadLength"))
                if (!$util.isInteger(message.payloadLength))
                    return "payloadLength: integer expected";
            if (message.thermalY16Offset != null && message.hasOwnProperty("thermalY16Offset"))
                if (!$util.isInteger(message.thermalY16Offset))
                    return "thermalY16Offset: integer expected";
            if (message.thermalY16Length != null && message.hasOwnProperty("thermalY16Length"))
                if (!$util.isInteger(message.thermalY16Length))
                    return "thermalY16Length: integer expected";
            if (message.runtimeBlockOffset != null && message.hasOwnProperty("runtimeBlockOffset"))
                if (!$util.isInteger(message.runtimeBlockOffset))
                    return "runtimeBlockOffset: integer expected";
            if (message.runtimeBlockLength != null && message.hasOwnProperty("runtimeBlockLength"))
                if (!$util.isInteger(message.runtimeBlockLength))
                    return "runtimeBlockLength: integer expected";
            return null;
        };

        /**
         * Creates a CompactPayloadLayout message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof hikmicro.CompactPayloadLayout
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {hikmicro.CompactPayloadLayout} CompactPayloadLayout
         */
        CompactPayloadLayout.fromObject = function fromObject(object, long) {
            if (object instanceof $root.hikmicro.CompactPayloadLayout)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.hikmicro.CompactPayloadLayout();
            if (object.sensorWidth != null)
                message.sensorWidth = object.sensorWidth >>> 0;
            if (object.sensorHeight != null)
                message.sensorHeight = object.sensorHeight >>> 0;
            if (object.payloadLength != null)
                message.payloadLength = object.payloadLength >>> 0;
            if (object.thermalY16Offset != null)
                message.thermalY16Offset = object.thermalY16Offset >>> 0;
            if (object.thermalY16Length != null)
                message.thermalY16Length = object.thermalY16Length >>> 0;
            if (object.runtimeBlockOffset != null)
                message.runtimeBlockOffset = object.runtimeBlockOffset >>> 0;
            if (object.runtimeBlockLength != null)
                message.runtimeBlockLength = object.runtimeBlockLength >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a CompactPayloadLayout message. Also converts values to other types if specified.
         * @function toObject
         * @memberof hikmicro.CompactPayloadLayout
         * @static
         * @param {hikmicro.CompactPayloadLayout} message CompactPayloadLayout
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CompactPayloadLayout.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.sensorWidth = 0;
                object.sensorHeight = 0;
                object.payloadLength = 0;
                object.thermalY16Offset = 0;
                object.thermalY16Length = 0;
                object.runtimeBlockOffset = 0;
                object.runtimeBlockLength = 0;
            }
            if (message.sensorWidth != null && message.hasOwnProperty("sensorWidth"))
                object.sensorWidth = message.sensorWidth;
            if (message.sensorHeight != null && message.hasOwnProperty("sensorHeight"))
                object.sensorHeight = message.sensorHeight;
            if (message.payloadLength != null && message.hasOwnProperty("payloadLength"))
                object.payloadLength = message.payloadLength;
            if (message.thermalY16Offset != null && message.hasOwnProperty("thermalY16Offset"))
                object.thermalY16Offset = message.thermalY16Offset;
            if (message.thermalY16Length != null && message.hasOwnProperty("thermalY16Length"))
                object.thermalY16Length = message.thermalY16Length;
            if (message.runtimeBlockOffset != null && message.hasOwnProperty("runtimeBlockOffset"))
                object.runtimeBlockOffset = message.runtimeBlockOffset;
            if (message.runtimeBlockLength != null && message.hasOwnProperty("runtimeBlockLength"))
                object.runtimeBlockLength = message.runtimeBlockLength;
            return object;
        };

        /**
         * Converts this CompactPayloadLayout to JSON.
         * @function toJSON
         * @memberof hikmicro.CompactPayloadLayout
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CompactPayloadLayout.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CompactPayloadLayout
         * @function getTypeUrl
         * @memberof hikmicro.CompactPayloadLayout
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CompactPayloadLayout.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/hikmicro.CompactPayloadLayout";
        };

        return CompactPayloadLayout;
    })();

    hikmicro.RuntimeBlockInfo = (function() {

        /**
         * Properties of a RuntimeBlockInfo.
         * @memberof hikmicro
         * @interface IRuntimeBlockInfo
         * @property {boolean|null} [markerOk] RuntimeBlockInfo markerOk
         * @property {number|null} [marker] RuntimeBlockInfo marker
         * @property {number|null} [rawSensor] RuntimeBlockInfo rawSensor
         * @property {number|null} [mtlibSensor] RuntimeBlockInfo mtlibSensor
         * @property {number|null} [interParam_0] RuntimeBlockInfo interParam_0
         * @property {number|null} [interParam_1] RuntimeBlockInfo interParam_1
         * @property {number|null} [interParam_2] RuntimeBlockInfo interParam_2
         * @property {number|null} [frameHeight] RuntimeBlockInfo frameHeight
         * @property {number|null} [frameWidth] RuntimeBlockInfo frameWidth
         * @property {number|null} [rawSensorHeader] RuntimeBlockInfo rawSensorHeader
         * @property {number|null} [mode] RuntimeBlockInfo mode
         * @property {number|null} [range] RuntimeBlockInfo range
         */

        /**
         * Constructs a new RuntimeBlockInfo.
         * @memberof hikmicro
         * @classdesc Represents a RuntimeBlockInfo.
         * @implements IRuntimeBlockInfo
         * @constructor
         * @param {hikmicro.IRuntimeBlockInfo=} [properties] Properties to set
         */
        function RuntimeBlockInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RuntimeBlockInfo markerOk.
         * @member {boolean} markerOk
         * @memberof hikmicro.RuntimeBlockInfo
         * @instance
         */
        RuntimeBlockInfo.prototype.markerOk = false;

        /**
         * RuntimeBlockInfo marker.
         * @member {number} marker
         * @memberof hikmicro.RuntimeBlockInfo
         * @instance
         */
        RuntimeBlockInfo.prototype.marker = 0;

        /**
         * RuntimeBlockInfo rawSensor.
         * @member {number} rawSensor
         * @memberof hikmicro.RuntimeBlockInfo
         * @instance
         */
        RuntimeBlockInfo.prototype.rawSensor = 0;

        /**
         * RuntimeBlockInfo mtlibSensor.
         * @member {number} mtlibSensor
         * @memberof hikmicro.RuntimeBlockInfo
         * @instance
         */
        RuntimeBlockInfo.prototype.mtlibSensor = 0;

        /**
         * RuntimeBlockInfo interParam_0.
         * @member {number} interParam_0
         * @memberof hikmicro.RuntimeBlockInfo
         * @instance
         */
        RuntimeBlockInfo.prototype.interParam_0 = 0;

        /**
         * RuntimeBlockInfo interParam_1.
         * @member {number} interParam_1
         * @memberof hikmicro.RuntimeBlockInfo
         * @instance
         */
        RuntimeBlockInfo.prototype.interParam_1 = 0;

        /**
         * RuntimeBlockInfo interParam_2.
         * @member {number} interParam_2
         * @memberof hikmicro.RuntimeBlockInfo
         * @instance
         */
        RuntimeBlockInfo.prototype.interParam_2 = 0;

        /**
         * RuntimeBlockInfo frameHeight.
         * @member {number} frameHeight
         * @memberof hikmicro.RuntimeBlockInfo
         * @instance
         */
        RuntimeBlockInfo.prototype.frameHeight = 0;

        /**
         * RuntimeBlockInfo frameWidth.
         * @member {number} frameWidth
         * @memberof hikmicro.RuntimeBlockInfo
         * @instance
         */
        RuntimeBlockInfo.prototype.frameWidth = 0;

        /**
         * RuntimeBlockInfo rawSensorHeader.
         * @member {number} rawSensorHeader
         * @memberof hikmicro.RuntimeBlockInfo
         * @instance
         */
        RuntimeBlockInfo.prototype.rawSensorHeader = 0;

        /**
         * RuntimeBlockInfo mode.
         * @member {number} mode
         * @memberof hikmicro.RuntimeBlockInfo
         * @instance
         */
        RuntimeBlockInfo.prototype.mode = 0;

        /**
         * RuntimeBlockInfo range.
         * @member {number} range
         * @memberof hikmicro.RuntimeBlockInfo
         * @instance
         */
        RuntimeBlockInfo.prototype.range = 0;

        /**
         * Creates a new RuntimeBlockInfo instance using the specified properties.
         * @function create
         * @memberof hikmicro.RuntimeBlockInfo
         * @static
         * @param {hikmicro.IRuntimeBlockInfo=} [properties] Properties to set
         * @returns {hikmicro.RuntimeBlockInfo} RuntimeBlockInfo instance
         */
        RuntimeBlockInfo.create = function create(properties) {
            return new RuntimeBlockInfo(properties);
        };

        /**
         * Encodes the specified RuntimeBlockInfo message. Does not implicitly {@link hikmicro.RuntimeBlockInfo.verify|verify} messages.
         * @function encode
         * @memberof hikmicro.RuntimeBlockInfo
         * @static
         * @param {hikmicro.IRuntimeBlockInfo} message RuntimeBlockInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RuntimeBlockInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.markerOk != null && Object.hasOwnProperty.call(message, "markerOk"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.markerOk);
            if (message.marker != null && Object.hasOwnProperty.call(message, "marker"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.marker);
            if (message.rawSensor != null && Object.hasOwnProperty.call(message, "rawSensor"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.rawSensor);
            if (message.mtlibSensor != null && Object.hasOwnProperty.call(message, "mtlibSensor"))
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.mtlibSensor);
            if (message.interParam_0 != null && Object.hasOwnProperty.call(message, "interParam_0"))
                writer.uint32(/* id 5, wireType 0 =*/40).uint32(message.interParam_0);
            if (message.interParam_1 != null && Object.hasOwnProperty.call(message, "interParam_1"))
                writer.uint32(/* id 6, wireType 0 =*/48).uint32(message.interParam_1);
            if (message.interParam_2 != null && Object.hasOwnProperty.call(message, "interParam_2"))
                writer.uint32(/* id 7, wireType 0 =*/56).uint32(message.interParam_2);
            if (message.frameHeight != null && Object.hasOwnProperty.call(message, "frameHeight"))
                writer.uint32(/* id 20, wireType 0 =*/160).uint32(message.frameHeight);
            if (message.frameWidth != null && Object.hasOwnProperty.call(message, "frameWidth"))
                writer.uint32(/* id 21, wireType 0 =*/168).uint32(message.frameWidth);
            if (message.rawSensorHeader != null && Object.hasOwnProperty.call(message, "rawSensorHeader"))
                writer.uint32(/* id 22, wireType 0 =*/176).uint32(message.rawSensorHeader);
            if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
                writer.uint32(/* id 23, wireType 0 =*/184).uint32(message.mode);
            if (message.range != null && Object.hasOwnProperty.call(message, "range"))
                writer.uint32(/* id 24, wireType 0 =*/192).uint32(message.range);
            return writer;
        };

        /**
         * Encodes the specified RuntimeBlockInfo message, length delimited. Does not implicitly {@link hikmicro.RuntimeBlockInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof hikmicro.RuntimeBlockInfo
         * @static
         * @param {hikmicro.IRuntimeBlockInfo} message RuntimeBlockInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RuntimeBlockInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RuntimeBlockInfo message from the specified reader or buffer.
         * @function decode
         * @memberof hikmicro.RuntimeBlockInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {hikmicro.RuntimeBlockInfo} RuntimeBlockInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RuntimeBlockInfo.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.hikmicro.RuntimeBlockInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.markerOk = reader.bool();
                        break;
                    }
                case 2: {
                        message.marker = reader.uint32();
                        break;
                    }
                case 3: {
                        message.rawSensor = reader.uint32();
                        break;
                    }
                case 4: {
                        message.mtlibSensor = reader.uint32();
                        break;
                    }
                case 5: {
                        message.interParam_0 = reader.uint32();
                        break;
                    }
                case 6: {
                        message.interParam_1 = reader.uint32();
                        break;
                    }
                case 7: {
                        message.interParam_2 = reader.uint32();
                        break;
                    }
                case 20: {
                        message.frameHeight = reader.uint32();
                        break;
                    }
                case 21: {
                        message.frameWidth = reader.uint32();
                        break;
                    }
                case 22: {
                        message.rawSensorHeader = reader.uint32();
                        break;
                    }
                case 23: {
                        message.mode = reader.uint32();
                        break;
                    }
                case 24: {
                        message.range = reader.uint32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RuntimeBlockInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof hikmicro.RuntimeBlockInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {hikmicro.RuntimeBlockInfo} RuntimeBlockInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RuntimeBlockInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RuntimeBlockInfo message.
         * @function verify
         * @memberof hikmicro.RuntimeBlockInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RuntimeBlockInfo.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.markerOk != null && message.hasOwnProperty("markerOk"))
                if (typeof message.markerOk !== "boolean")
                    return "markerOk: boolean expected";
            if (message.marker != null && message.hasOwnProperty("marker"))
                if (!$util.isInteger(message.marker))
                    return "marker: integer expected";
            if (message.rawSensor != null && message.hasOwnProperty("rawSensor"))
                if (!$util.isInteger(message.rawSensor))
                    return "rawSensor: integer expected";
            if (message.mtlibSensor != null && message.hasOwnProperty("mtlibSensor"))
                if (!$util.isInteger(message.mtlibSensor))
                    return "mtlibSensor: integer expected";
            if (message.interParam_0 != null && message.hasOwnProperty("interParam_0"))
                if (!$util.isInteger(message.interParam_0))
                    return "interParam_0: integer expected";
            if (message.interParam_1 != null && message.hasOwnProperty("interParam_1"))
                if (!$util.isInteger(message.interParam_1))
                    return "interParam_1: integer expected";
            if (message.interParam_2 != null && message.hasOwnProperty("interParam_2"))
                if (!$util.isInteger(message.interParam_2))
                    return "interParam_2: integer expected";
            if (message.frameHeight != null && message.hasOwnProperty("frameHeight"))
                if (!$util.isInteger(message.frameHeight))
                    return "frameHeight: integer expected";
            if (message.frameWidth != null && message.hasOwnProperty("frameWidth"))
                if (!$util.isInteger(message.frameWidth))
                    return "frameWidth: integer expected";
            if (message.rawSensorHeader != null && message.hasOwnProperty("rawSensorHeader"))
                if (!$util.isInteger(message.rawSensorHeader))
                    return "rawSensorHeader: integer expected";
            if (message.mode != null && message.hasOwnProperty("mode"))
                if (!$util.isInteger(message.mode))
                    return "mode: integer expected";
            if (message.range != null && message.hasOwnProperty("range"))
                if (!$util.isInteger(message.range))
                    return "range: integer expected";
            return null;
        };

        /**
         * Creates a RuntimeBlockInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof hikmicro.RuntimeBlockInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {hikmicro.RuntimeBlockInfo} RuntimeBlockInfo
         */
        RuntimeBlockInfo.fromObject = function fromObject(object, long) {
            if (object instanceof $root.hikmicro.RuntimeBlockInfo)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.hikmicro.RuntimeBlockInfo();
            if (object.markerOk != null)
                message.markerOk = Boolean(object.markerOk);
            if (object.marker != null)
                message.marker = object.marker >>> 0;
            if (object.rawSensor != null)
                message.rawSensor = object.rawSensor >>> 0;
            if (object.mtlibSensor != null)
                message.mtlibSensor = object.mtlibSensor >>> 0;
            if (object.interParam_0 != null)
                message.interParam_0 = object.interParam_0 >>> 0;
            if (object.interParam_1 != null)
                message.interParam_1 = object.interParam_1 >>> 0;
            if (object.interParam_2 != null)
                message.interParam_2 = object.interParam_2 >>> 0;
            if (object.frameHeight != null)
                message.frameHeight = object.frameHeight >>> 0;
            if (object.frameWidth != null)
                message.frameWidth = object.frameWidth >>> 0;
            if (object.rawSensorHeader != null)
                message.rawSensorHeader = object.rawSensorHeader >>> 0;
            if (object.mode != null)
                message.mode = object.mode >>> 0;
            if (object.range != null)
                message.range = object.range >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a RuntimeBlockInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof hikmicro.RuntimeBlockInfo
         * @static
         * @param {hikmicro.RuntimeBlockInfo} message RuntimeBlockInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RuntimeBlockInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.markerOk = false;
                object.marker = 0;
                object.rawSensor = 0;
                object.mtlibSensor = 0;
                object.interParam_0 = 0;
                object.interParam_1 = 0;
                object.interParam_2 = 0;
                object.frameHeight = 0;
                object.frameWidth = 0;
                object.rawSensorHeader = 0;
                object.mode = 0;
                object.range = 0;
            }
            if (message.markerOk != null && message.hasOwnProperty("markerOk"))
                object.markerOk = message.markerOk;
            if (message.marker != null && message.hasOwnProperty("marker"))
                object.marker = message.marker;
            if (message.rawSensor != null && message.hasOwnProperty("rawSensor"))
                object.rawSensor = message.rawSensor;
            if (message.mtlibSensor != null && message.hasOwnProperty("mtlibSensor"))
                object.mtlibSensor = message.mtlibSensor;
            if (message.interParam_0 != null && message.hasOwnProperty("interParam_0"))
                object.interParam_0 = message.interParam_0;
            if (message.interParam_1 != null && message.hasOwnProperty("interParam_1"))
                object.interParam_1 = message.interParam_1;
            if (message.interParam_2 != null && message.hasOwnProperty("interParam_2"))
                object.interParam_2 = message.interParam_2;
            if (message.frameHeight != null && message.hasOwnProperty("frameHeight"))
                object.frameHeight = message.frameHeight;
            if (message.frameWidth != null && message.hasOwnProperty("frameWidth"))
                object.frameWidth = message.frameWidth;
            if (message.rawSensorHeader != null && message.hasOwnProperty("rawSensorHeader"))
                object.rawSensorHeader = message.rawSensorHeader;
            if (message.mode != null && message.hasOwnProperty("mode"))
                object.mode = message.mode;
            if (message.range != null && message.hasOwnProperty("range"))
                object.range = message.range;
            return object;
        };

        /**
         * Converts this RuntimeBlockInfo to JSON.
         * @function toJSON
         * @memberof hikmicro.RuntimeBlockInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RuntimeBlockInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RuntimeBlockInfo
         * @function getTypeUrl
         * @memberof hikmicro.RuntimeBlockInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RuntimeBlockInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/hikmicro.RuntimeBlockInfo";
        };

        return RuntimeBlockInfo;
    })();

    hikmicro.CalibrationChunk = (function() {

        /**
         * Properties of a CalibrationChunk.
         * @memberof hikmicro
         * @interface ICalibrationChunk
         * @property {number|null} [index] CalibrationChunk index
         * @property {Uint8Array|null} [header] CalibrationChunk header
         * @property {Uint8Array|null} [payload] CalibrationChunk payload
         */

        /**
         * Constructs a new CalibrationChunk.
         * @memberof hikmicro
         * @classdesc Represents a CalibrationChunk.
         * @implements ICalibrationChunk
         * @constructor
         * @param {hikmicro.ICalibrationChunk=} [properties] Properties to set
         */
        function CalibrationChunk(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CalibrationChunk index.
         * @member {number} index
         * @memberof hikmicro.CalibrationChunk
         * @instance
         */
        CalibrationChunk.prototype.index = 0;

        /**
         * CalibrationChunk header.
         * @member {Uint8Array} header
         * @memberof hikmicro.CalibrationChunk
         * @instance
         */
        CalibrationChunk.prototype.header = $util.newBuffer([]);

        /**
         * CalibrationChunk payload.
         * @member {Uint8Array} payload
         * @memberof hikmicro.CalibrationChunk
         * @instance
         */
        CalibrationChunk.prototype.payload = $util.newBuffer([]);

        /**
         * Creates a new CalibrationChunk instance using the specified properties.
         * @function create
         * @memberof hikmicro.CalibrationChunk
         * @static
         * @param {hikmicro.ICalibrationChunk=} [properties] Properties to set
         * @returns {hikmicro.CalibrationChunk} CalibrationChunk instance
         */
        CalibrationChunk.create = function create(properties) {
            return new CalibrationChunk(properties);
        };

        /**
         * Encodes the specified CalibrationChunk message. Does not implicitly {@link hikmicro.CalibrationChunk.verify|verify} messages.
         * @function encode
         * @memberof hikmicro.CalibrationChunk
         * @static
         * @param {hikmicro.ICalibrationChunk} message CalibrationChunk message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CalibrationChunk.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.index != null && Object.hasOwnProperty.call(message, "index"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.index);
            if (message.header != null && Object.hasOwnProperty.call(message, "header"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.header);
            if (message.payload != null && Object.hasOwnProperty.call(message, "payload"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.payload);
            return writer;
        };

        /**
         * Encodes the specified CalibrationChunk message, length delimited. Does not implicitly {@link hikmicro.CalibrationChunk.verify|verify} messages.
         * @function encodeDelimited
         * @memberof hikmicro.CalibrationChunk
         * @static
         * @param {hikmicro.ICalibrationChunk} message CalibrationChunk message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CalibrationChunk.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CalibrationChunk message from the specified reader or buffer.
         * @function decode
         * @memberof hikmicro.CalibrationChunk
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {hikmicro.CalibrationChunk} CalibrationChunk
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CalibrationChunk.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.hikmicro.CalibrationChunk();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.index = reader.uint32();
                        break;
                    }
                case 2: {
                        message.header = reader.bytes();
                        break;
                    }
                case 3: {
                        message.payload = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CalibrationChunk message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof hikmicro.CalibrationChunk
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {hikmicro.CalibrationChunk} CalibrationChunk
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CalibrationChunk.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CalibrationChunk message.
         * @function verify
         * @memberof hikmicro.CalibrationChunk
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CalibrationChunk.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.index != null && message.hasOwnProperty("index"))
                if (!$util.isInteger(message.index))
                    return "index: integer expected";
            if (message.header != null && message.hasOwnProperty("header"))
                if (!(message.header && typeof message.header.length === "number" || $util.isString(message.header)))
                    return "header: buffer expected";
            if (message.payload != null && message.hasOwnProperty("payload"))
                if (!(message.payload && typeof message.payload.length === "number" || $util.isString(message.payload)))
                    return "payload: buffer expected";
            return null;
        };

        /**
         * Creates a CalibrationChunk message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof hikmicro.CalibrationChunk
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {hikmicro.CalibrationChunk} CalibrationChunk
         */
        CalibrationChunk.fromObject = function fromObject(object, long) {
            if (object instanceof $root.hikmicro.CalibrationChunk)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.hikmicro.CalibrationChunk();
            if (object.index != null)
                message.index = object.index >>> 0;
            if (object.header != null)
                if (typeof object.header === "string")
                    $util.base64.decode(object.header, message.header = $util.newBuffer($util.base64.length(object.header)), 0);
                else if (object.header.length >= 0)
                    message.header = object.header;
            if (object.payload != null)
                if (typeof object.payload === "string")
                    $util.base64.decode(object.payload, message.payload = $util.newBuffer($util.base64.length(object.payload)), 0);
                else if (object.payload.length >= 0)
                    message.payload = object.payload;
            return message;
        };

        /**
         * Creates a plain object from a CalibrationChunk message. Also converts values to other types if specified.
         * @function toObject
         * @memberof hikmicro.CalibrationChunk
         * @static
         * @param {hikmicro.CalibrationChunk} message CalibrationChunk
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CalibrationChunk.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.index = 0;
                if (options.bytes === String)
                    object.header = "";
                else {
                    object.header = [];
                    if (options.bytes !== Array)
                        object.header = $util.newBuffer(object.header);
                }
                if (options.bytes === String)
                    object.payload = "";
                else {
                    object.payload = [];
                    if (options.bytes !== Array)
                        object.payload = $util.newBuffer(object.payload);
                }
            }
            if (message.index != null && message.hasOwnProperty("index"))
                object.index = message.index;
            if (message.header != null && message.hasOwnProperty("header"))
                object.header = options.bytes === String ? $util.base64.encode(message.header, 0, message.header.length) : options.bytes === Array ? Array.prototype.slice.call(message.header) : message.header;
            if (message.payload != null && message.hasOwnProperty("payload"))
                object.payload = options.bytes === String ? $util.base64.encode(message.payload, 0, message.payload.length) : options.bytes === Array ? Array.prototype.slice.call(message.payload) : message.payload;
            return object;
        };

        /**
         * Converts this CalibrationChunk to JSON.
         * @function toJSON
         * @memberof hikmicro.CalibrationChunk
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CalibrationChunk.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CalibrationChunk
         * @function getTypeUrl
         * @memberof hikmicro.CalibrationChunk
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CalibrationChunk.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/hikmicro.CalibrationChunk";
        };

        return CalibrationChunk;
    })();

    hikmicro.CalibrationData = (function() {

        /**
         * Properties of a CalibrationData.
         * @memberof hikmicro
         * @interface ICalibrationData
         * @property {boolean|null} [attempted] CalibrationData attempted
         * @property {boolean|null} [ok] CalibrationData ok
         * @property {string|null} [error] CalibrationData error
         * @property {Uint8Array|null} [lengthHeader] CalibrationData lengthHeader
         * @property {number|null} [declaredLength] CalibrationData declaredLength
         * @property {Uint8Array|null} [container] CalibrationData container
         * @property {number|null} [factoryBlobOffset] CalibrationData factoryBlobOffset
         * @property {number|null} [factoryBlobLength] CalibrationData factoryBlobLength
         * @property {Array.<hikmicro.ICalibrationChunk>|null} [chunks] CalibrationData chunks
         */

        /**
         * Constructs a new CalibrationData.
         * @memberof hikmicro
         * @classdesc Represents a CalibrationData.
         * @implements ICalibrationData
         * @constructor
         * @param {hikmicro.ICalibrationData=} [properties] Properties to set
         */
        function CalibrationData(properties) {
            this.chunks = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CalibrationData attempted.
         * @member {boolean} attempted
         * @memberof hikmicro.CalibrationData
         * @instance
         */
        CalibrationData.prototype.attempted = false;

        /**
         * CalibrationData ok.
         * @member {boolean} ok
         * @memberof hikmicro.CalibrationData
         * @instance
         */
        CalibrationData.prototype.ok = false;

        /**
         * CalibrationData error.
         * @member {string} error
         * @memberof hikmicro.CalibrationData
         * @instance
         */
        CalibrationData.prototype.error = "";

        /**
         * CalibrationData lengthHeader.
         * @member {Uint8Array} lengthHeader
         * @memberof hikmicro.CalibrationData
         * @instance
         */
        CalibrationData.prototype.lengthHeader = $util.newBuffer([]);

        /**
         * CalibrationData declaredLength.
         * @member {number} declaredLength
         * @memberof hikmicro.CalibrationData
         * @instance
         */
        CalibrationData.prototype.declaredLength = 0;

        /**
         * CalibrationData container.
         * @member {Uint8Array} container
         * @memberof hikmicro.CalibrationData
         * @instance
         */
        CalibrationData.prototype.container = $util.newBuffer([]);

        /**
         * CalibrationData factoryBlobOffset.
         * @member {number} factoryBlobOffset
         * @memberof hikmicro.CalibrationData
         * @instance
         */
        CalibrationData.prototype.factoryBlobOffset = 0;

        /**
         * CalibrationData factoryBlobLength.
         * @member {number} factoryBlobLength
         * @memberof hikmicro.CalibrationData
         * @instance
         */
        CalibrationData.prototype.factoryBlobLength = 0;

        /**
         * CalibrationData chunks.
         * @member {Array.<hikmicro.ICalibrationChunk>} chunks
         * @memberof hikmicro.CalibrationData
         * @instance
         */
        CalibrationData.prototype.chunks = $util.emptyArray;

        /**
         * Creates a new CalibrationData instance using the specified properties.
         * @function create
         * @memberof hikmicro.CalibrationData
         * @static
         * @param {hikmicro.ICalibrationData=} [properties] Properties to set
         * @returns {hikmicro.CalibrationData} CalibrationData instance
         */
        CalibrationData.create = function create(properties) {
            return new CalibrationData(properties);
        };

        /**
         * Encodes the specified CalibrationData message. Does not implicitly {@link hikmicro.CalibrationData.verify|verify} messages.
         * @function encode
         * @memberof hikmicro.CalibrationData
         * @static
         * @param {hikmicro.ICalibrationData} message CalibrationData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CalibrationData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.attempted != null && Object.hasOwnProperty.call(message, "attempted"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.attempted);
            if (message.ok != null && Object.hasOwnProperty.call(message, "ok"))
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.ok);
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.error);
            if (message.lengthHeader != null && Object.hasOwnProperty.call(message, "lengthHeader"))
                writer.uint32(/* id 10, wireType 2 =*/82).bytes(message.lengthHeader);
            if (message.declaredLength != null && Object.hasOwnProperty.call(message, "declaredLength"))
                writer.uint32(/* id 11, wireType 0 =*/88).uint32(message.declaredLength);
            if (message.container != null && Object.hasOwnProperty.call(message, "container"))
                writer.uint32(/* id 12, wireType 2 =*/98).bytes(message.container);
            if (message.factoryBlobOffset != null && Object.hasOwnProperty.call(message, "factoryBlobOffset"))
                writer.uint32(/* id 14, wireType 0 =*/112).uint32(message.factoryBlobOffset);
            if (message.factoryBlobLength != null && Object.hasOwnProperty.call(message, "factoryBlobLength"))
                writer.uint32(/* id 15, wireType 0 =*/120).uint32(message.factoryBlobLength);
            if (message.chunks != null && message.chunks.length)
                for (let i = 0; i < message.chunks.length; ++i)
                    $root.hikmicro.CalibrationChunk.encode(message.chunks[i], writer.uint32(/* id 30, wireType 2 =*/242).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified CalibrationData message, length delimited. Does not implicitly {@link hikmicro.CalibrationData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof hikmicro.CalibrationData
         * @static
         * @param {hikmicro.ICalibrationData} message CalibrationData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CalibrationData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CalibrationData message from the specified reader or buffer.
         * @function decode
         * @memberof hikmicro.CalibrationData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {hikmicro.CalibrationData} CalibrationData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CalibrationData.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.hikmicro.CalibrationData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.attempted = reader.bool();
                        break;
                    }
                case 2: {
                        message.ok = reader.bool();
                        break;
                    }
                case 3: {
                        message.error = reader.string();
                        break;
                    }
                case 10: {
                        message.lengthHeader = reader.bytes();
                        break;
                    }
                case 11: {
                        message.declaredLength = reader.uint32();
                        break;
                    }
                case 12: {
                        message.container = reader.bytes();
                        break;
                    }
                case 14: {
                        message.factoryBlobOffset = reader.uint32();
                        break;
                    }
                case 15: {
                        message.factoryBlobLength = reader.uint32();
                        break;
                    }
                case 30: {
                        if (!(message.chunks && message.chunks.length))
                            message.chunks = [];
                        message.chunks.push($root.hikmicro.CalibrationChunk.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CalibrationData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof hikmicro.CalibrationData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {hikmicro.CalibrationData} CalibrationData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CalibrationData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CalibrationData message.
         * @function verify
         * @memberof hikmicro.CalibrationData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CalibrationData.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.attempted != null && message.hasOwnProperty("attempted"))
                if (typeof message.attempted !== "boolean")
                    return "attempted: boolean expected";
            if (message.ok != null && message.hasOwnProperty("ok"))
                if (typeof message.ok !== "boolean")
                    return "ok: boolean expected";
            if (message.error != null && message.hasOwnProperty("error"))
                if (!$util.isString(message.error))
                    return "error: string expected";
            if (message.lengthHeader != null && message.hasOwnProperty("lengthHeader"))
                if (!(message.lengthHeader && typeof message.lengthHeader.length === "number" || $util.isString(message.lengthHeader)))
                    return "lengthHeader: buffer expected";
            if (message.declaredLength != null && message.hasOwnProperty("declaredLength"))
                if (!$util.isInteger(message.declaredLength))
                    return "declaredLength: integer expected";
            if (message.container != null && message.hasOwnProperty("container"))
                if (!(message.container && typeof message.container.length === "number" || $util.isString(message.container)))
                    return "container: buffer expected";
            if (message.factoryBlobOffset != null && message.hasOwnProperty("factoryBlobOffset"))
                if (!$util.isInteger(message.factoryBlobOffset))
                    return "factoryBlobOffset: integer expected";
            if (message.factoryBlobLength != null && message.hasOwnProperty("factoryBlobLength"))
                if (!$util.isInteger(message.factoryBlobLength))
                    return "factoryBlobLength: integer expected";
            if (message.chunks != null && message.hasOwnProperty("chunks")) {
                if (!Array.isArray(message.chunks))
                    return "chunks: array expected";
                for (let i = 0; i < message.chunks.length; ++i) {
                    let error = $root.hikmicro.CalibrationChunk.verify(message.chunks[i], long + 1);
                    if (error)
                        return "chunks." + error;
                }
            }
            return null;
        };

        /**
         * Creates a CalibrationData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof hikmicro.CalibrationData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {hikmicro.CalibrationData} CalibrationData
         */
        CalibrationData.fromObject = function fromObject(object, long) {
            if (object instanceof $root.hikmicro.CalibrationData)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.hikmicro.CalibrationData();
            if (object.attempted != null)
                message.attempted = Boolean(object.attempted);
            if (object.ok != null)
                message.ok = Boolean(object.ok);
            if (object.error != null)
                message.error = String(object.error);
            if (object.lengthHeader != null)
                if (typeof object.lengthHeader === "string")
                    $util.base64.decode(object.lengthHeader, message.lengthHeader = $util.newBuffer($util.base64.length(object.lengthHeader)), 0);
                else if (object.lengthHeader.length >= 0)
                    message.lengthHeader = object.lengthHeader;
            if (object.declaredLength != null)
                message.declaredLength = object.declaredLength >>> 0;
            if (object.container != null)
                if (typeof object.container === "string")
                    $util.base64.decode(object.container, message.container = $util.newBuffer($util.base64.length(object.container)), 0);
                else if (object.container.length >= 0)
                    message.container = object.container;
            if (object.factoryBlobOffset != null)
                message.factoryBlobOffset = object.factoryBlobOffset >>> 0;
            if (object.factoryBlobLength != null)
                message.factoryBlobLength = object.factoryBlobLength >>> 0;
            if (object.chunks) {
                if (!Array.isArray(object.chunks))
                    throw TypeError(".hikmicro.CalibrationData.chunks: array expected");
                message.chunks = [];
                for (let i = 0; i < object.chunks.length; ++i) {
                    if (typeof object.chunks[i] !== "object")
                        throw TypeError(".hikmicro.CalibrationData.chunks: object expected");
                    message.chunks[i] = $root.hikmicro.CalibrationChunk.fromObject(object.chunks[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a CalibrationData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof hikmicro.CalibrationData
         * @static
         * @param {hikmicro.CalibrationData} message CalibrationData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CalibrationData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.chunks = [];
            if (options.defaults) {
                object.attempted = false;
                object.ok = false;
                object.error = "";
                if (options.bytes === String)
                    object.lengthHeader = "";
                else {
                    object.lengthHeader = [];
                    if (options.bytes !== Array)
                        object.lengthHeader = $util.newBuffer(object.lengthHeader);
                }
                object.declaredLength = 0;
                if (options.bytes === String)
                    object.container = "";
                else {
                    object.container = [];
                    if (options.bytes !== Array)
                        object.container = $util.newBuffer(object.container);
                }
                object.factoryBlobOffset = 0;
                object.factoryBlobLength = 0;
            }
            if (message.attempted != null && message.hasOwnProperty("attempted"))
                object.attempted = message.attempted;
            if (message.ok != null && message.hasOwnProperty("ok"))
                object.ok = message.ok;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = message.error;
            if (message.lengthHeader != null && message.hasOwnProperty("lengthHeader"))
                object.lengthHeader = options.bytes === String ? $util.base64.encode(message.lengthHeader, 0, message.lengthHeader.length) : options.bytes === Array ? Array.prototype.slice.call(message.lengthHeader) : message.lengthHeader;
            if (message.declaredLength != null && message.hasOwnProperty("declaredLength"))
                object.declaredLength = message.declaredLength;
            if (message.container != null && message.hasOwnProperty("container"))
                object.container = options.bytes === String ? $util.base64.encode(message.container, 0, message.container.length) : options.bytes === Array ? Array.prototype.slice.call(message.container) : message.container;
            if (message.factoryBlobOffset != null && message.hasOwnProperty("factoryBlobOffset"))
                object.factoryBlobOffset = message.factoryBlobOffset;
            if (message.factoryBlobLength != null && message.hasOwnProperty("factoryBlobLength"))
                object.factoryBlobLength = message.factoryBlobLength;
            if (message.chunks && message.chunks.length) {
                object.chunks = [];
                for (let j = 0; j < message.chunks.length; ++j)
                    object.chunks[j] = $root.hikmicro.CalibrationChunk.toObject(message.chunks[j], options);
            }
            return object;
        };

        /**
         * Converts this CalibrationData to JSON.
         * @function toJSON
         * @memberof hikmicro.CalibrationData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CalibrationData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CalibrationData
         * @function getTypeUrl
         * @memberof hikmicro.CalibrationData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CalibrationData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/hikmicro.CalibrationData";
        };

        return CalibrationData;
    })();

    hikmicro.DeviceInfo = (function() {

        /**
         * Properties of a DeviceInfo.
         * @memberof hikmicro
         * @interface IDeviceInfo
         * @property {string|null} [driver] DeviceInfo driver
         * @property {hikmicro.IUsbDeviceInfo|null} [usb] DeviceInfo usb
         * @property {hikmicro.ICompactStreamFormat|null} [streamFormat] DeviceInfo streamFormat
         * @property {hikmicro.ICompactPayloadLayout|null} [layout] DeviceInfo layout
         * @property {hikmicro.ICalibrationData|null} [calibration] DeviceInfo calibration
         */

        /**
         * Constructs a new DeviceInfo.
         * @memberof hikmicro
         * @classdesc Represents a DeviceInfo.
         * @implements IDeviceInfo
         * @constructor
         * @param {hikmicro.IDeviceInfo=} [properties] Properties to set
         */
        function DeviceInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * DeviceInfo driver.
         * @member {string} driver
         * @memberof hikmicro.DeviceInfo
         * @instance
         */
        DeviceInfo.prototype.driver = "";

        /**
         * DeviceInfo usb.
         * @member {hikmicro.IUsbDeviceInfo|null|undefined} usb
         * @memberof hikmicro.DeviceInfo
         * @instance
         */
        DeviceInfo.prototype.usb = null;

        /**
         * DeviceInfo streamFormat.
         * @member {hikmicro.ICompactStreamFormat|null|undefined} streamFormat
         * @memberof hikmicro.DeviceInfo
         * @instance
         */
        DeviceInfo.prototype.streamFormat = null;

        /**
         * DeviceInfo layout.
         * @member {hikmicro.ICompactPayloadLayout|null|undefined} layout
         * @memberof hikmicro.DeviceInfo
         * @instance
         */
        DeviceInfo.prototype.layout = null;

        /**
         * DeviceInfo calibration.
         * @member {hikmicro.ICalibrationData|null|undefined} calibration
         * @memberof hikmicro.DeviceInfo
         * @instance
         */
        DeviceInfo.prototype.calibration = null;

        /**
         * Creates a new DeviceInfo instance using the specified properties.
         * @function create
         * @memberof hikmicro.DeviceInfo
         * @static
         * @param {hikmicro.IDeviceInfo=} [properties] Properties to set
         * @returns {hikmicro.DeviceInfo} DeviceInfo instance
         */
        DeviceInfo.create = function create(properties) {
            return new DeviceInfo(properties);
        };

        /**
         * Encodes the specified DeviceInfo message. Does not implicitly {@link hikmicro.DeviceInfo.verify|verify} messages.
         * @function encode
         * @memberof hikmicro.DeviceInfo
         * @static
         * @param {hikmicro.IDeviceInfo} message DeviceInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeviceInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.driver != null && Object.hasOwnProperty.call(message, "driver"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.driver);
            if (message.usb != null && Object.hasOwnProperty.call(message, "usb"))
                $root.hikmicro.UsbDeviceInfo.encode(message.usb, writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.streamFormat != null && Object.hasOwnProperty.call(message, "streamFormat"))
                $root.hikmicro.CompactStreamFormat.encode(message.streamFormat, writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.layout != null && Object.hasOwnProperty.call(message, "layout"))
                $root.hikmicro.CompactPayloadLayout.encode(message.layout, writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.calibration != null && Object.hasOwnProperty.call(message, "calibration"))
                $root.hikmicro.CalibrationData.encode(message.calibration, writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified DeviceInfo message, length delimited. Does not implicitly {@link hikmicro.DeviceInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof hikmicro.DeviceInfo
         * @static
         * @param {hikmicro.IDeviceInfo} message DeviceInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeviceInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a DeviceInfo message from the specified reader or buffer.
         * @function decode
         * @memberof hikmicro.DeviceInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {hikmicro.DeviceInfo} DeviceInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeviceInfo.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.hikmicro.DeviceInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.driver = reader.string();
                        break;
                    }
                case 2: {
                        message.usb = $root.hikmicro.UsbDeviceInfo.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 3: {
                        message.streamFormat = $root.hikmicro.CompactStreamFormat.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 4: {
                        message.layout = $root.hikmicro.CompactPayloadLayout.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 5: {
                        message.calibration = $root.hikmicro.CalibrationData.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a DeviceInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof hikmicro.DeviceInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {hikmicro.DeviceInfo} DeviceInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeviceInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a DeviceInfo message.
         * @function verify
         * @memberof hikmicro.DeviceInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        DeviceInfo.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.driver != null && message.hasOwnProperty("driver"))
                if (!$util.isString(message.driver))
                    return "driver: string expected";
            if (message.usb != null && message.hasOwnProperty("usb")) {
                let error = $root.hikmicro.UsbDeviceInfo.verify(message.usb, long + 1);
                if (error)
                    return "usb." + error;
            }
            if (message.streamFormat != null && message.hasOwnProperty("streamFormat")) {
                let error = $root.hikmicro.CompactStreamFormat.verify(message.streamFormat, long + 1);
                if (error)
                    return "streamFormat." + error;
            }
            if (message.layout != null && message.hasOwnProperty("layout")) {
                let error = $root.hikmicro.CompactPayloadLayout.verify(message.layout, long + 1);
                if (error)
                    return "layout." + error;
            }
            if (message.calibration != null && message.hasOwnProperty("calibration")) {
                let error = $root.hikmicro.CalibrationData.verify(message.calibration, long + 1);
                if (error)
                    return "calibration." + error;
            }
            return null;
        };

        /**
         * Creates a DeviceInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof hikmicro.DeviceInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {hikmicro.DeviceInfo} DeviceInfo
         */
        DeviceInfo.fromObject = function fromObject(object, long) {
            if (object instanceof $root.hikmicro.DeviceInfo)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.hikmicro.DeviceInfo();
            if (object.driver != null)
                message.driver = String(object.driver);
            if (object.usb != null) {
                if (typeof object.usb !== "object")
                    throw TypeError(".hikmicro.DeviceInfo.usb: object expected");
                message.usb = $root.hikmicro.UsbDeviceInfo.fromObject(object.usb, long + 1);
            }
            if (object.streamFormat != null) {
                if (typeof object.streamFormat !== "object")
                    throw TypeError(".hikmicro.DeviceInfo.streamFormat: object expected");
                message.streamFormat = $root.hikmicro.CompactStreamFormat.fromObject(object.streamFormat, long + 1);
            }
            if (object.layout != null) {
                if (typeof object.layout !== "object")
                    throw TypeError(".hikmicro.DeviceInfo.layout: object expected");
                message.layout = $root.hikmicro.CompactPayloadLayout.fromObject(object.layout, long + 1);
            }
            if (object.calibration != null) {
                if (typeof object.calibration !== "object")
                    throw TypeError(".hikmicro.DeviceInfo.calibration: object expected");
                message.calibration = $root.hikmicro.CalibrationData.fromObject(object.calibration, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a DeviceInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof hikmicro.DeviceInfo
         * @static
         * @param {hikmicro.DeviceInfo} message DeviceInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DeviceInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.driver = "";
                object.usb = null;
                object.streamFormat = null;
                object.layout = null;
                object.calibration = null;
            }
            if (message.driver != null && message.hasOwnProperty("driver"))
                object.driver = message.driver;
            if (message.usb != null && message.hasOwnProperty("usb"))
                object.usb = $root.hikmicro.UsbDeviceInfo.toObject(message.usb, options);
            if (message.streamFormat != null && message.hasOwnProperty("streamFormat"))
                object.streamFormat = $root.hikmicro.CompactStreamFormat.toObject(message.streamFormat, options);
            if (message.layout != null && message.hasOwnProperty("layout"))
                object.layout = $root.hikmicro.CompactPayloadLayout.toObject(message.layout, options);
            if (message.calibration != null && message.hasOwnProperty("calibration"))
                object.calibration = $root.hikmicro.CalibrationData.toObject(message.calibration, options);
            return object;
        };

        /**
         * Converts this DeviceInfo to JSON.
         * @function toJSON
         * @memberof hikmicro.DeviceInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DeviceInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for DeviceInfo
         * @function getTypeUrl
         * @memberof hikmicro.DeviceInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        DeviceInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/hikmicro.DeviceInfo";
        };

        return DeviceInfo;
    })();

    hikmicro.ThermalFrame = (function() {

        /**
         * Properties of a ThermalFrame.
         * @memberof hikmicro
         * @interface IThermalFrame
         * @property {number|null} [sequence] ThermalFrame sequence
         * @property {Long|null} [monotonicStampNs] ThermalFrame monotonicStampNs
         * @property {Long|null} [localStampNs] ThermalFrame localStampNs
         * @property {hikmicro.IRuntimeBlockInfo|null} [runtime] ThermalFrame runtime
         * @property {Uint8Array|null} [payload] ThermalFrame payload
         */

        /**
         * Constructs a new ThermalFrame.
         * @memberof hikmicro
         * @classdesc Represents a ThermalFrame.
         * @implements IThermalFrame
         * @constructor
         * @param {hikmicro.IThermalFrame=} [properties] Properties to set
         */
        function ThermalFrame(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ThermalFrame sequence.
         * @member {number} sequence
         * @memberof hikmicro.ThermalFrame
         * @instance
         */
        ThermalFrame.prototype.sequence = 0;

        /**
         * ThermalFrame monotonicStampNs.
         * @member {Long} monotonicStampNs
         * @memberof hikmicro.ThermalFrame
         * @instance
         */
        ThermalFrame.prototype.monotonicStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ThermalFrame localStampNs.
         * @member {Long} localStampNs
         * @memberof hikmicro.ThermalFrame
         * @instance
         */
        ThermalFrame.prototype.localStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ThermalFrame runtime.
         * @member {hikmicro.IRuntimeBlockInfo|null|undefined} runtime
         * @memberof hikmicro.ThermalFrame
         * @instance
         */
        ThermalFrame.prototype.runtime = null;

        /**
         * ThermalFrame payload.
         * @member {Uint8Array} payload
         * @memberof hikmicro.ThermalFrame
         * @instance
         */
        ThermalFrame.prototype.payload = $util.newBuffer([]);

        /**
         * Creates a new ThermalFrame instance using the specified properties.
         * @function create
         * @memberof hikmicro.ThermalFrame
         * @static
         * @param {hikmicro.IThermalFrame=} [properties] Properties to set
         * @returns {hikmicro.ThermalFrame} ThermalFrame instance
         */
        ThermalFrame.create = function create(properties) {
            return new ThermalFrame(properties);
        };

        /**
         * Encodes the specified ThermalFrame message. Does not implicitly {@link hikmicro.ThermalFrame.verify|verify} messages.
         * @function encode
         * @memberof hikmicro.ThermalFrame
         * @static
         * @param {hikmicro.IThermalFrame} message ThermalFrame message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ThermalFrame.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.sequence != null && Object.hasOwnProperty.call(message, "sequence"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.sequence);
            if (message.monotonicStampNs != null && Object.hasOwnProperty.call(message, "monotonicStampNs"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.monotonicStampNs);
            if (message.localStampNs != null && Object.hasOwnProperty.call(message, "localStampNs"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.localStampNs);
            if (message.runtime != null && Object.hasOwnProperty.call(message, "runtime"))
                $root.hikmicro.RuntimeBlockInfo.encode(message.runtime, writer.uint32(/* id 6, wireType 2 =*/50).fork()).ldelim();
            if (message.payload != null && Object.hasOwnProperty.call(message, "payload"))
                writer.uint32(/* id 10, wireType 2 =*/82).bytes(message.payload);
            return writer;
        };

        /**
         * Encodes the specified ThermalFrame message, length delimited. Does not implicitly {@link hikmicro.ThermalFrame.verify|verify} messages.
         * @function encodeDelimited
         * @memberof hikmicro.ThermalFrame
         * @static
         * @param {hikmicro.IThermalFrame} message ThermalFrame message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ThermalFrame.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ThermalFrame message from the specified reader or buffer.
         * @function decode
         * @memberof hikmicro.ThermalFrame
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {hikmicro.ThermalFrame} ThermalFrame
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ThermalFrame.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.hikmicro.ThermalFrame();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.sequence = reader.uint32();
                        break;
                    }
                case 2: {
                        message.monotonicStampNs = reader.uint64();
                        break;
                    }
                case 3: {
                        message.localStampNs = reader.uint64();
                        break;
                    }
                case 6: {
                        message.runtime = $root.hikmicro.RuntimeBlockInfo.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 10: {
                        message.payload = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ThermalFrame message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof hikmicro.ThermalFrame
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {hikmicro.ThermalFrame} ThermalFrame
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ThermalFrame.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ThermalFrame message.
         * @function verify
         * @memberof hikmicro.ThermalFrame
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ThermalFrame.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.sequence != null && message.hasOwnProperty("sequence"))
                if (!$util.isInteger(message.sequence))
                    return "sequence: integer expected";
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (!$util.isInteger(message.monotonicStampNs) && !(message.monotonicStampNs && $util.isInteger(message.monotonicStampNs.low) && $util.isInteger(message.monotonicStampNs.high)))
                    return "monotonicStampNs: integer|Long expected";
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (!$util.isInteger(message.localStampNs) && !(message.localStampNs && $util.isInteger(message.localStampNs.low) && $util.isInteger(message.localStampNs.high)))
                    return "localStampNs: integer|Long expected";
            if (message.runtime != null && message.hasOwnProperty("runtime")) {
                let error = $root.hikmicro.RuntimeBlockInfo.verify(message.runtime, long + 1);
                if (error)
                    return "runtime." + error;
            }
            if (message.payload != null && message.hasOwnProperty("payload"))
                if (!(message.payload && typeof message.payload.length === "number" || $util.isString(message.payload)))
                    return "payload: buffer expected";
            return null;
        };

        /**
         * Creates a ThermalFrame message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof hikmicro.ThermalFrame
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {hikmicro.ThermalFrame} ThermalFrame
         */
        ThermalFrame.fromObject = function fromObject(object, long) {
            if (object instanceof $root.hikmicro.ThermalFrame)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.hikmicro.ThermalFrame();
            if (object.sequence != null)
                message.sequence = object.sequence >>> 0;
            if (object.monotonicStampNs != null)
                if ($util.Long)
                    (message.monotonicStampNs = $util.Long.fromValue(object.monotonicStampNs)).unsigned = true;
                else if (typeof object.monotonicStampNs === "string")
                    message.monotonicStampNs = parseInt(object.monotonicStampNs, 10);
                else if (typeof object.monotonicStampNs === "number")
                    message.monotonicStampNs = object.monotonicStampNs;
                else if (typeof object.monotonicStampNs === "object")
                    message.monotonicStampNs = new $util.LongBits(object.monotonicStampNs.low >>> 0, object.monotonicStampNs.high >>> 0).toNumber(true);
            if (object.localStampNs != null)
                if ($util.Long)
                    (message.localStampNs = $util.Long.fromValue(object.localStampNs)).unsigned = true;
                else if (typeof object.localStampNs === "string")
                    message.localStampNs = parseInt(object.localStampNs, 10);
                else if (typeof object.localStampNs === "number")
                    message.localStampNs = object.localStampNs;
                else if (typeof object.localStampNs === "object")
                    message.localStampNs = new $util.LongBits(object.localStampNs.low >>> 0, object.localStampNs.high >>> 0).toNumber(true);
            if (object.runtime != null) {
                if (typeof object.runtime !== "object")
                    throw TypeError(".hikmicro.ThermalFrame.runtime: object expected");
                message.runtime = $root.hikmicro.RuntimeBlockInfo.fromObject(object.runtime, long + 1);
            }
            if (object.payload != null)
                if (typeof object.payload === "string")
                    $util.base64.decode(object.payload, message.payload = $util.newBuffer($util.base64.length(object.payload)), 0);
                else if (object.payload.length >= 0)
                    message.payload = object.payload;
            return message;
        };

        /**
         * Creates a plain object from a ThermalFrame message. Also converts values to other types if specified.
         * @function toObject
         * @memberof hikmicro.ThermalFrame
         * @static
         * @param {hikmicro.ThermalFrame} message ThermalFrame
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ThermalFrame.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.sequence = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.monotonicStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.monotonicStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.localStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.localStampNs = options.longs === String ? "0" : 0;
                object.runtime = null;
                if (options.bytes === String)
                    object.payload = "";
                else {
                    object.payload = [];
                    if (options.bytes !== Array)
                        object.payload = $util.newBuffer(object.payload);
                }
            }
            if (message.sequence != null && message.hasOwnProperty("sequence"))
                object.sequence = message.sequence;
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (typeof message.monotonicStampNs === "number")
                    object.monotonicStampNs = options.longs === String ? String(message.monotonicStampNs) : message.monotonicStampNs;
                else
                    object.monotonicStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.monotonicStampNs) : options.longs === Number ? new $util.LongBits(message.monotonicStampNs.low >>> 0, message.monotonicStampNs.high >>> 0).toNumber(true) : message.monotonicStampNs;
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (typeof message.localStampNs === "number")
                    object.localStampNs = options.longs === String ? String(message.localStampNs) : message.localStampNs;
                else
                    object.localStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.localStampNs) : options.longs === Number ? new $util.LongBits(message.localStampNs.low >>> 0, message.localStampNs.high >>> 0).toNumber(true) : message.localStampNs;
            if (message.runtime != null && message.hasOwnProperty("runtime"))
                object.runtime = $root.hikmicro.RuntimeBlockInfo.toObject(message.runtime, options);
            if (message.payload != null && message.hasOwnProperty("payload"))
                object.payload = options.bytes === String ? $util.base64.encode(message.payload, 0, message.payload.length) : options.bytes === Array ? Array.prototype.slice.call(message.payload) : message.payload;
            return object;
        };

        /**
         * Converts this ThermalFrame to JSON.
         * @function toJSON
         * @memberof hikmicro.ThermalFrame
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ThermalFrame.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ThermalFrame
         * @function getTypeUrl
         * @memberof hikmicro.ThermalFrame
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ThermalFrame.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/hikmicro.ThermalFrame";
        };

        return ThermalFrame;
    })();

    hikmicro.ThermalFramesBlock = (function() {

        /**
         * Properties of a ThermalFramesBlock.
         * @memberof hikmicro
         * @interface IThermalFramesBlock
         * @property {number|null} [sequence] ThermalFramesBlock sequence
         * @property {number|null} [frameCount] ThermalFramesBlock frameCount
         * @property {Long|null} [monotonicStartNs] ThermalFramesBlock monotonicStartNs
         * @property {Long|null} [monotonicEndNs] ThermalFramesBlock monotonicEndNs
         * @property {Long|null} [localStartNs] ThermalFramesBlock localStartNs
         * @property {Long|null} [localEndNs] ThermalFramesBlock localEndNs
         * @property {hikmicro.ICompactStreamFormat|null} [streamFormat] ThermalFramesBlock streamFormat
         * @property {hikmicro.ICompactPayloadLayout|null} [layout] ThermalFramesBlock layout
         * @property {Array.<hikmicro.IThermalFrame>|null} [frames] ThermalFramesBlock frames
         */

        /**
         * Constructs a new ThermalFramesBlock.
         * @memberof hikmicro
         * @classdesc Represents a ThermalFramesBlock.
         * @implements IThermalFramesBlock
         * @constructor
         * @param {hikmicro.IThermalFramesBlock=} [properties] Properties to set
         */
        function ThermalFramesBlock(properties) {
            this.frames = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ThermalFramesBlock sequence.
         * @member {number} sequence
         * @memberof hikmicro.ThermalFramesBlock
         * @instance
         */
        ThermalFramesBlock.prototype.sequence = 0;

        /**
         * ThermalFramesBlock frameCount.
         * @member {number} frameCount
         * @memberof hikmicro.ThermalFramesBlock
         * @instance
         */
        ThermalFramesBlock.prototype.frameCount = 0;

        /**
         * ThermalFramesBlock monotonicStartNs.
         * @member {Long} monotonicStartNs
         * @memberof hikmicro.ThermalFramesBlock
         * @instance
         */
        ThermalFramesBlock.prototype.monotonicStartNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ThermalFramesBlock monotonicEndNs.
         * @member {Long} monotonicEndNs
         * @memberof hikmicro.ThermalFramesBlock
         * @instance
         */
        ThermalFramesBlock.prototype.monotonicEndNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ThermalFramesBlock localStartNs.
         * @member {Long} localStartNs
         * @memberof hikmicro.ThermalFramesBlock
         * @instance
         */
        ThermalFramesBlock.prototype.localStartNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ThermalFramesBlock localEndNs.
         * @member {Long} localEndNs
         * @memberof hikmicro.ThermalFramesBlock
         * @instance
         */
        ThermalFramesBlock.prototype.localEndNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ThermalFramesBlock streamFormat.
         * @member {hikmicro.ICompactStreamFormat|null|undefined} streamFormat
         * @memberof hikmicro.ThermalFramesBlock
         * @instance
         */
        ThermalFramesBlock.prototype.streamFormat = null;

        /**
         * ThermalFramesBlock layout.
         * @member {hikmicro.ICompactPayloadLayout|null|undefined} layout
         * @memberof hikmicro.ThermalFramesBlock
         * @instance
         */
        ThermalFramesBlock.prototype.layout = null;

        /**
         * ThermalFramesBlock frames.
         * @member {Array.<hikmicro.IThermalFrame>} frames
         * @memberof hikmicro.ThermalFramesBlock
         * @instance
         */
        ThermalFramesBlock.prototype.frames = $util.emptyArray;

        /**
         * Creates a new ThermalFramesBlock instance using the specified properties.
         * @function create
         * @memberof hikmicro.ThermalFramesBlock
         * @static
         * @param {hikmicro.IThermalFramesBlock=} [properties] Properties to set
         * @returns {hikmicro.ThermalFramesBlock} ThermalFramesBlock instance
         */
        ThermalFramesBlock.create = function create(properties) {
            return new ThermalFramesBlock(properties);
        };

        /**
         * Encodes the specified ThermalFramesBlock message. Does not implicitly {@link hikmicro.ThermalFramesBlock.verify|verify} messages.
         * @function encode
         * @memberof hikmicro.ThermalFramesBlock
         * @static
         * @param {hikmicro.IThermalFramesBlock} message ThermalFramesBlock message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ThermalFramesBlock.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.sequence != null && Object.hasOwnProperty.call(message, "sequence"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.sequence);
            if (message.frameCount != null && Object.hasOwnProperty.call(message, "frameCount"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.frameCount);
            if (message.monotonicStartNs != null && Object.hasOwnProperty.call(message, "monotonicStartNs"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.monotonicStartNs);
            if (message.monotonicEndNs != null && Object.hasOwnProperty.call(message, "monotonicEndNs"))
                writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.monotonicEndNs);
            if (message.localStartNs != null && Object.hasOwnProperty.call(message, "localStartNs"))
                writer.uint32(/* id 5, wireType 0 =*/40).uint64(message.localStartNs);
            if (message.localEndNs != null && Object.hasOwnProperty.call(message, "localEndNs"))
                writer.uint32(/* id 6, wireType 0 =*/48).uint64(message.localEndNs);
            if (message.streamFormat != null && Object.hasOwnProperty.call(message, "streamFormat"))
                $root.hikmicro.CompactStreamFormat.encode(message.streamFormat, writer.uint32(/* id 7, wireType 2 =*/58).fork()).ldelim();
            if (message.layout != null && Object.hasOwnProperty.call(message, "layout"))
                $root.hikmicro.CompactPayloadLayout.encode(message.layout, writer.uint32(/* id 8, wireType 2 =*/66).fork()).ldelim();
            if (message.frames != null && message.frames.length)
                for (let i = 0; i < message.frames.length; ++i)
                    $root.hikmicro.ThermalFrame.encode(message.frames[i], writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified ThermalFramesBlock message, length delimited. Does not implicitly {@link hikmicro.ThermalFramesBlock.verify|verify} messages.
         * @function encodeDelimited
         * @memberof hikmicro.ThermalFramesBlock
         * @static
         * @param {hikmicro.IThermalFramesBlock} message ThermalFramesBlock message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ThermalFramesBlock.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ThermalFramesBlock message from the specified reader or buffer.
         * @function decode
         * @memberof hikmicro.ThermalFramesBlock
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {hikmicro.ThermalFramesBlock} ThermalFramesBlock
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ThermalFramesBlock.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.hikmicro.ThermalFramesBlock();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.sequence = reader.uint32();
                        break;
                    }
                case 2: {
                        message.frameCount = reader.uint32();
                        break;
                    }
                case 3: {
                        message.monotonicStartNs = reader.uint64();
                        break;
                    }
                case 4: {
                        message.monotonicEndNs = reader.uint64();
                        break;
                    }
                case 5: {
                        message.localStartNs = reader.uint64();
                        break;
                    }
                case 6: {
                        message.localEndNs = reader.uint64();
                        break;
                    }
                case 7: {
                        message.streamFormat = $root.hikmicro.CompactStreamFormat.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 8: {
                        message.layout = $root.hikmicro.CompactPayloadLayout.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 10: {
                        if (!(message.frames && message.frames.length))
                            message.frames = [];
                        message.frames.push($root.hikmicro.ThermalFrame.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ThermalFramesBlock message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof hikmicro.ThermalFramesBlock
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {hikmicro.ThermalFramesBlock} ThermalFramesBlock
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ThermalFramesBlock.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ThermalFramesBlock message.
         * @function verify
         * @memberof hikmicro.ThermalFramesBlock
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ThermalFramesBlock.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.sequence != null && message.hasOwnProperty("sequence"))
                if (!$util.isInteger(message.sequence))
                    return "sequence: integer expected";
            if (message.frameCount != null && message.hasOwnProperty("frameCount"))
                if (!$util.isInteger(message.frameCount))
                    return "frameCount: integer expected";
            if (message.monotonicStartNs != null && message.hasOwnProperty("monotonicStartNs"))
                if (!$util.isInteger(message.monotonicStartNs) && !(message.monotonicStartNs && $util.isInteger(message.monotonicStartNs.low) && $util.isInteger(message.monotonicStartNs.high)))
                    return "monotonicStartNs: integer|Long expected";
            if (message.monotonicEndNs != null && message.hasOwnProperty("monotonicEndNs"))
                if (!$util.isInteger(message.monotonicEndNs) && !(message.monotonicEndNs && $util.isInteger(message.monotonicEndNs.low) && $util.isInteger(message.monotonicEndNs.high)))
                    return "monotonicEndNs: integer|Long expected";
            if (message.localStartNs != null && message.hasOwnProperty("localStartNs"))
                if (!$util.isInteger(message.localStartNs) && !(message.localStartNs && $util.isInteger(message.localStartNs.low) && $util.isInteger(message.localStartNs.high)))
                    return "localStartNs: integer|Long expected";
            if (message.localEndNs != null && message.hasOwnProperty("localEndNs"))
                if (!$util.isInteger(message.localEndNs) && !(message.localEndNs && $util.isInteger(message.localEndNs.low) && $util.isInteger(message.localEndNs.high)))
                    return "localEndNs: integer|Long expected";
            if (message.streamFormat != null && message.hasOwnProperty("streamFormat")) {
                let error = $root.hikmicro.CompactStreamFormat.verify(message.streamFormat, long + 1);
                if (error)
                    return "streamFormat." + error;
            }
            if (message.layout != null && message.hasOwnProperty("layout")) {
                let error = $root.hikmicro.CompactPayloadLayout.verify(message.layout, long + 1);
                if (error)
                    return "layout." + error;
            }
            if (message.frames != null && message.hasOwnProperty("frames")) {
                if (!Array.isArray(message.frames))
                    return "frames: array expected";
                for (let i = 0; i < message.frames.length; ++i) {
                    let error = $root.hikmicro.ThermalFrame.verify(message.frames[i], long + 1);
                    if (error)
                        return "frames." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ThermalFramesBlock message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof hikmicro.ThermalFramesBlock
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {hikmicro.ThermalFramesBlock} ThermalFramesBlock
         */
        ThermalFramesBlock.fromObject = function fromObject(object, long) {
            if (object instanceof $root.hikmicro.ThermalFramesBlock)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.hikmicro.ThermalFramesBlock();
            if (object.sequence != null)
                message.sequence = object.sequence >>> 0;
            if (object.frameCount != null)
                message.frameCount = object.frameCount >>> 0;
            if (object.monotonicStartNs != null)
                if ($util.Long)
                    (message.monotonicStartNs = $util.Long.fromValue(object.monotonicStartNs)).unsigned = true;
                else if (typeof object.monotonicStartNs === "string")
                    message.monotonicStartNs = parseInt(object.monotonicStartNs, 10);
                else if (typeof object.monotonicStartNs === "number")
                    message.monotonicStartNs = object.monotonicStartNs;
                else if (typeof object.monotonicStartNs === "object")
                    message.monotonicStartNs = new $util.LongBits(object.monotonicStartNs.low >>> 0, object.monotonicStartNs.high >>> 0).toNumber(true);
            if (object.monotonicEndNs != null)
                if ($util.Long)
                    (message.monotonicEndNs = $util.Long.fromValue(object.monotonicEndNs)).unsigned = true;
                else if (typeof object.monotonicEndNs === "string")
                    message.monotonicEndNs = parseInt(object.monotonicEndNs, 10);
                else if (typeof object.monotonicEndNs === "number")
                    message.monotonicEndNs = object.monotonicEndNs;
                else if (typeof object.monotonicEndNs === "object")
                    message.monotonicEndNs = new $util.LongBits(object.monotonicEndNs.low >>> 0, object.monotonicEndNs.high >>> 0).toNumber(true);
            if (object.localStartNs != null)
                if ($util.Long)
                    (message.localStartNs = $util.Long.fromValue(object.localStartNs)).unsigned = true;
                else if (typeof object.localStartNs === "string")
                    message.localStartNs = parseInt(object.localStartNs, 10);
                else if (typeof object.localStartNs === "number")
                    message.localStartNs = object.localStartNs;
                else if (typeof object.localStartNs === "object")
                    message.localStartNs = new $util.LongBits(object.localStartNs.low >>> 0, object.localStartNs.high >>> 0).toNumber(true);
            if (object.localEndNs != null)
                if ($util.Long)
                    (message.localEndNs = $util.Long.fromValue(object.localEndNs)).unsigned = true;
                else if (typeof object.localEndNs === "string")
                    message.localEndNs = parseInt(object.localEndNs, 10);
                else if (typeof object.localEndNs === "number")
                    message.localEndNs = object.localEndNs;
                else if (typeof object.localEndNs === "object")
                    message.localEndNs = new $util.LongBits(object.localEndNs.low >>> 0, object.localEndNs.high >>> 0).toNumber(true);
            if (object.streamFormat != null) {
                if (typeof object.streamFormat !== "object")
                    throw TypeError(".hikmicro.ThermalFramesBlock.streamFormat: object expected");
                message.streamFormat = $root.hikmicro.CompactStreamFormat.fromObject(object.streamFormat, long + 1);
            }
            if (object.layout != null) {
                if (typeof object.layout !== "object")
                    throw TypeError(".hikmicro.ThermalFramesBlock.layout: object expected");
                message.layout = $root.hikmicro.CompactPayloadLayout.fromObject(object.layout, long + 1);
            }
            if (object.frames) {
                if (!Array.isArray(object.frames))
                    throw TypeError(".hikmicro.ThermalFramesBlock.frames: array expected");
                message.frames = [];
                for (let i = 0; i < object.frames.length; ++i) {
                    if (typeof object.frames[i] !== "object")
                        throw TypeError(".hikmicro.ThermalFramesBlock.frames: object expected");
                    message.frames[i] = $root.hikmicro.ThermalFrame.fromObject(object.frames[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ThermalFramesBlock message. Also converts values to other types if specified.
         * @function toObject
         * @memberof hikmicro.ThermalFramesBlock
         * @static
         * @param {hikmicro.ThermalFramesBlock} message ThermalFramesBlock
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ThermalFramesBlock.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.frames = [];
            if (options.defaults) {
                object.sequence = 0;
                object.frameCount = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.monotonicStartNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.monotonicStartNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.monotonicEndNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.monotonicEndNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.localStartNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.localStartNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.localEndNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.localEndNs = options.longs === String ? "0" : 0;
                object.streamFormat = null;
                object.layout = null;
            }
            if (message.sequence != null && message.hasOwnProperty("sequence"))
                object.sequence = message.sequence;
            if (message.frameCount != null && message.hasOwnProperty("frameCount"))
                object.frameCount = message.frameCount;
            if (message.monotonicStartNs != null && message.hasOwnProperty("monotonicStartNs"))
                if (typeof message.monotonicStartNs === "number")
                    object.monotonicStartNs = options.longs === String ? String(message.monotonicStartNs) : message.monotonicStartNs;
                else
                    object.monotonicStartNs = options.longs === String ? $util.Long.prototype.toString.call(message.monotonicStartNs) : options.longs === Number ? new $util.LongBits(message.monotonicStartNs.low >>> 0, message.monotonicStartNs.high >>> 0).toNumber(true) : message.monotonicStartNs;
            if (message.monotonicEndNs != null && message.hasOwnProperty("monotonicEndNs"))
                if (typeof message.monotonicEndNs === "number")
                    object.monotonicEndNs = options.longs === String ? String(message.monotonicEndNs) : message.monotonicEndNs;
                else
                    object.monotonicEndNs = options.longs === String ? $util.Long.prototype.toString.call(message.monotonicEndNs) : options.longs === Number ? new $util.LongBits(message.monotonicEndNs.low >>> 0, message.monotonicEndNs.high >>> 0).toNumber(true) : message.monotonicEndNs;
            if (message.localStartNs != null && message.hasOwnProperty("localStartNs"))
                if (typeof message.localStartNs === "number")
                    object.localStartNs = options.longs === String ? String(message.localStartNs) : message.localStartNs;
                else
                    object.localStartNs = options.longs === String ? $util.Long.prototype.toString.call(message.localStartNs) : options.longs === Number ? new $util.LongBits(message.localStartNs.low >>> 0, message.localStartNs.high >>> 0).toNumber(true) : message.localStartNs;
            if (message.localEndNs != null && message.hasOwnProperty("localEndNs"))
                if (typeof message.localEndNs === "number")
                    object.localEndNs = options.longs === String ? String(message.localEndNs) : message.localEndNs;
                else
                    object.localEndNs = options.longs === String ? $util.Long.prototype.toString.call(message.localEndNs) : options.longs === Number ? new $util.LongBits(message.localEndNs.low >>> 0, message.localEndNs.high >>> 0).toNumber(true) : message.localEndNs;
            if (message.streamFormat != null && message.hasOwnProperty("streamFormat"))
                object.streamFormat = $root.hikmicro.CompactStreamFormat.toObject(message.streamFormat, options);
            if (message.layout != null && message.hasOwnProperty("layout"))
                object.layout = $root.hikmicro.CompactPayloadLayout.toObject(message.layout, options);
            if (message.frames && message.frames.length) {
                object.frames = [];
                for (let j = 0; j < message.frames.length; ++j)
                    object.frames[j] = $root.hikmicro.ThermalFrame.toObject(message.frames[j], options);
            }
            return object;
        };

        /**
         * Converts this ThermalFramesBlock to JSON.
         * @function toJSON
         * @memberof hikmicro.ThermalFramesBlock
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ThermalFramesBlock.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ThermalFramesBlock
         * @function getTypeUrl
         * @memberof hikmicro.ThermalFramesBlock
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ThermalFramesBlock.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/hikmicro.ThermalFramesBlock";
        };

        return ThermalFramesBlock;
    })();

    hikmicro.RxEnvelope = (function() {

        /**
         * Properties of a RxEnvelope.
         * @memberof hikmicro
         * @interface IRxEnvelope
         * @property {hikmicro.IDeviceInfo|null} [deviceInfo] RxEnvelope deviceInfo
         * @property {hikmicro.IThermalFramesBlock|null} [frames] RxEnvelope frames
         */

        /**
         * Constructs a new RxEnvelope.
         * @memberof hikmicro
         * @classdesc Represents a RxEnvelope.
         * @implements IRxEnvelope
         * @constructor
         * @param {hikmicro.IRxEnvelope=} [properties] Properties to set
         */
        function RxEnvelope(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RxEnvelope deviceInfo.
         * @member {hikmicro.IDeviceInfo|null|undefined} deviceInfo
         * @memberof hikmicro.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.deviceInfo = null;

        /**
         * RxEnvelope frames.
         * @member {hikmicro.IThermalFramesBlock|null|undefined} frames
         * @memberof hikmicro.RxEnvelope
         * @instance
         */
        RxEnvelope.prototype.frames = null;

        /**
         * Creates a new RxEnvelope instance using the specified properties.
         * @function create
         * @memberof hikmicro.RxEnvelope
         * @static
         * @param {hikmicro.IRxEnvelope=} [properties] Properties to set
         * @returns {hikmicro.RxEnvelope} RxEnvelope instance
         */
        RxEnvelope.create = function create(properties) {
            return new RxEnvelope(properties);
        };

        /**
         * Encodes the specified RxEnvelope message. Does not implicitly {@link hikmicro.RxEnvelope.verify|verify} messages.
         * @function encode
         * @memberof hikmicro.RxEnvelope
         * @static
         * @param {hikmicro.IRxEnvelope} message RxEnvelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RxEnvelope.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.deviceInfo != null && Object.hasOwnProperty.call(message, "deviceInfo"))
                $root.hikmicro.DeviceInfo.encode(message.deviceInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.frames != null && Object.hasOwnProperty.call(message, "frames"))
                $root.hikmicro.ThermalFramesBlock.encode(message.frames, writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified RxEnvelope message, length delimited. Does not implicitly {@link hikmicro.RxEnvelope.verify|verify} messages.
         * @function encodeDelimited
         * @memberof hikmicro.RxEnvelope
         * @static
         * @param {hikmicro.IRxEnvelope} message RxEnvelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RxEnvelope.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RxEnvelope message from the specified reader or buffer.
         * @function decode
         * @memberof hikmicro.RxEnvelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {hikmicro.RxEnvelope} RxEnvelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RxEnvelope.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.hikmicro.RxEnvelope();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.deviceInfo = $root.hikmicro.DeviceInfo.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 2: {
                        message.frames = $root.hikmicro.ThermalFramesBlock.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RxEnvelope message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof hikmicro.RxEnvelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {hikmicro.RxEnvelope} RxEnvelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RxEnvelope.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RxEnvelope message.
         * @function verify
         * @memberof hikmicro.RxEnvelope
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RxEnvelope.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.deviceInfo != null && message.hasOwnProperty("deviceInfo")) {
                let error = $root.hikmicro.DeviceInfo.verify(message.deviceInfo, long + 1);
                if (error)
                    return "deviceInfo." + error;
            }
            if (message.frames != null && message.hasOwnProperty("frames")) {
                let error = $root.hikmicro.ThermalFramesBlock.verify(message.frames, long + 1);
                if (error)
                    return "frames." + error;
            }
            return null;
        };

        /**
         * Creates a RxEnvelope message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof hikmicro.RxEnvelope
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {hikmicro.RxEnvelope} RxEnvelope
         */
        RxEnvelope.fromObject = function fromObject(object, long) {
            if (object instanceof $root.hikmicro.RxEnvelope)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.hikmicro.RxEnvelope();
            if (object.deviceInfo != null) {
                if (typeof object.deviceInfo !== "object")
                    throw TypeError(".hikmicro.RxEnvelope.deviceInfo: object expected");
                message.deviceInfo = $root.hikmicro.DeviceInfo.fromObject(object.deviceInfo, long + 1);
            }
            if (object.frames != null) {
                if (typeof object.frames !== "object")
                    throw TypeError(".hikmicro.RxEnvelope.frames: object expected");
                message.frames = $root.hikmicro.ThermalFramesBlock.fromObject(object.frames, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a RxEnvelope message. Also converts values to other types if specified.
         * @function toObject
         * @memberof hikmicro.RxEnvelope
         * @static
         * @param {hikmicro.RxEnvelope} message RxEnvelope
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RxEnvelope.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.deviceInfo = null;
                object.frames = null;
            }
            if (message.deviceInfo != null && message.hasOwnProperty("deviceInfo"))
                object.deviceInfo = $root.hikmicro.DeviceInfo.toObject(message.deviceInfo, options);
            if (message.frames != null && message.hasOwnProperty("frames"))
                object.frames = $root.hikmicro.ThermalFramesBlock.toObject(message.frames, options);
            return object;
        };

        /**
         * Converts this RxEnvelope to JSON.
         * @function toJSON
         * @memberof hikmicro.RxEnvelope
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RxEnvelope.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RxEnvelope
         * @function getTypeUrl
         * @memberof hikmicro.RxEnvelope
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RxEnvelope.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/hikmicro.RxEnvelope";
        };

        return RxEnvelope;
    })();

    return hikmicro;
})();

export const normfs = $root.normfs = (() => {

    /**
     * Namespace normfs.
     * @exports normfs
     * @namespace
     */
    const normfs = {};

    normfs.ClientRequest = (function() {

        /**
         * Properties of a ClientRequest.
         * @memberof normfs
         * @interface IClientRequest
         * @property {normfs.ISetupRequest|null} [setup] ClientRequest setup
         * @property {normfs.IPingRequest|null} [ping] ClientRequest ping
         * @property {normfs.IWriteRequest|null} [write] ClientRequest write
         * @property {normfs.IReadRequest|null} [read] ClientRequest read
         */

        /**
         * Constructs a new ClientRequest.
         * @memberof normfs
         * @classdesc Represents a ClientRequest.
         * @implements IClientRequest
         * @constructor
         * @param {normfs.IClientRequest=} [properties] Properties to set
         */
        function ClientRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ClientRequest setup.
         * @member {normfs.ISetupRequest|null|undefined} setup
         * @memberof normfs.ClientRequest
         * @instance
         */
        ClientRequest.prototype.setup = null;

        /**
         * ClientRequest ping.
         * @member {normfs.IPingRequest|null|undefined} ping
         * @memberof normfs.ClientRequest
         * @instance
         */
        ClientRequest.prototype.ping = null;

        /**
         * ClientRequest write.
         * @member {normfs.IWriteRequest|null|undefined} write
         * @memberof normfs.ClientRequest
         * @instance
         */
        ClientRequest.prototype.write = null;

        /**
         * ClientRequest read.
         * @member {normfs.IReadRequest|null|undefined} read
         * @memberof normfs.ClientRequest
         * @instance
         */
        ClientRequest.prototype.read = null;

        /**
         * Creates a new ClientRequest instance using the specified properties.
         * @function create
         * @memberof normfs.ClientRequest
         * @static
         * @param {normfs.IClientRequest=} [properties] Properties to set
         * @returns {normfs.ClientRequest} ClientRequest instance
         */
        ClientRequest.create = function create(properties) {
            return new ClientRequest(properties);
        };

        /**
         * Encodes the specified ClientRequest message. Does not implicitly {@link normfs.ClientRequest.verify|verify} messages.
         * @function encode
         * @memberof normfs.ClientRequest
         * @static
         * @param {normfs.IClientRequest} message ClientRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ClientRequest.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.setup != null && Object.hasOwnProperty.call(message, "setup"))
                $root.normfs.SetupRequest.encode(message.setup, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.ping != null && Object.hasOwnProperty.call(message, "ping"))
                $root.normfs.PingRequest.encode(message.ping, writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.write != null && Object.hasOwnProperty.call(message, "write"))
                $root.normfs.WriteRequest.encode(message.write, writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            if (message.read != null && Object.hasOwnProperty.call(message, "read"))
                $root.normfs.ReadRequest.encode(message.read, writer.uint32(/* id 11, wireType 2 =*/90).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified ClientRequest message, length delimited. Does not implicitly {@link normfs.ClientRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof normfs.ClientRequest
         * @static
         * @param {normfs.IClientRequest} message ClientRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ClientRequest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ClientRequest message from the specified reader or buffer.
         * @function decode
         * @memberof normfs.ClientRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {normfs.ClientRequest} ClientRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ClientRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.normfs.ClientRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.setup = $root.normfs.SetupRequest.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 2: {
                        message.ping = $root.normfs.PingRequest.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 10: {
                        message.write = $root.normfs.WriteRequest.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 11: {
                        message.read = $root.normfs.ReadRequest.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ClientRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof normfs.ClientRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {normfs.ClientRequest} ClientRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ClientRequest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ClientRequest message.
         * @function verify
         * @memberof normfs.ClientRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ClientRequest.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.setup != null && message.hasOwnProperty("setup")) {
                let error = $root.normfs.SetupRequest.verify(message.setup, long + 1);
                if (error)
                    return "setup." + error;
            }
            if (message.ping != null && message.hasOwnProperty("ping")) {
                let error = $root.normfs.PingRequest.verify(message.ping, long + 1);
                if (error)
                    return "ping." + error;
            }
            if (message.write != null && message.hasOwnProperty("write")) {
                let error = $root.normfs.WriteRequest.verify(message.write, long + 1);
                if (error)
                    return "write." + error;
            }
            if (message.read != null && message.hasOwnProperty("read")) {
                let error = $root.normfs.ReadRequest.verify(message.read, long + 1);
                if (error)
                    return "read." + error;
            }
            return null;
        };

        /**
         * Creates a ClientRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof normfs.ClientRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {normfs.ClientRequest} ClientRequest
         */
        ClientRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.normfs.ClientRequest)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.normfs.ClientRequest();
            if (object.setup != null) {
                if (typeof object.setup !== "object")
                    throw TypeError(".normfs.ClientRequest.setup: object expected");
                message.setup = $root.normfs.SetupRequest.fromObject(object.setup, long + 1);
            }
            if (object.ping != null) {
                if (typeof object.ping !== "object")
                    throw TypeError(".normfs.ClientRequest.ping: object expected");
                message.ping = $root.normfs.PingRequest.fromObject(object.ping, long + 1);
            }
            if (object.write != null) {
                if (typeof object.write !== "object")
                    throw TypeError(".normfs.ClientRequest.write: object expected");
                message.write = $root.normfs.WriteRequest.fromObject(object.write, long + 1);
            }
            if (object.read != null) {
                if (typeof object.read !== "object")
                    throw TypeError(".normfs.ClientRequest.read: object expected");
                message.read = $root.normfs.ReadRequest.fromObject(object.read, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a ClientRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof normfs.ClientRequest
         * @static
         * @param {normfs.ClientRequest} message ClientRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ClientRequest.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.setup = null;
                object.ping = null;
                object.write = null;
                object.read = null;
            }
            if (message.setup != null && message.hasOwnProperty("setup"))
                object.setup = $root.normfs.SetupRequest.toObject(message.setup, options);
            if (message.ping != null && message.hasOwnProperty("ping"))
                object.ping = $root.normfs.PingRequest.toObject(message.ping, options);
            if (message.write != null && message.hasOwnProperty("write"))
                object.write = $root.normfs.WriteRequest.toObject(message.write, options);
            if (message.read != null && message.hasOwnProperty("read"))
                object.read = $root.normfs.ReadRequest.toObject(message.read, options);
            return object;
        };

        /**
         * Converts this ClientRequest to JSON.
         * @function toJSON
         * @memberof normfs.ClientRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ClientRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ClientRequest
         * @function getTypeUrl
         * @memberof normfs.ClientRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ClientRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/normfs.ClientRequest";
        };

        return ClientRequest;
    })();

    normfs.ServerResponse = (function() {

        /**
         * Properties of a ServerResponse.
         * @memberof normfs
         * @interface IServerResponse
         * @property {normfs.ISetupResponse|null} [setup] ServerResponse setup
         * @property {normfs.IPingResponse|null} [ping] ServerResponse ping
         * @property {normfs.IWriteResponse|null} [write] ServerResponse write
         * @property {normfs.IReadResponse|null} [read] ServerResponse read
         */

        /**
         * Constructs a new ServerResponse.
         * @memberof normfs
         * @classdesc Represents a ServerResponse.
         * @implements IServerResponse
         * @constructor
         * @param {normfs.IServerResponse=} [properties] Properties to set
         */
        function ServerResponse(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ServerResponse setup.
         * @member {normfs.ISetupResponse|null|undefined} setup
         * @memberof normfs.ServerResponse
         * @instance
         */
        ServerResponse.prototype.setup = null;

        /**
         * ServerResponse ping.
         * @member {normfs.IPingResponse|null|undefined} ping
         * @memberof normfs.ServerResponse
         * @instance
         */
        ServerResponse.prototype.ping = null;

        /**
         * ServerResponse write.
         * @member {normfs.IWriteResponse|null|undefined} write
         * @memberof normfs.ServerResponse
         * @instance
         */
        ServerResponse.prototype.write = null;

        /**
         * ServerResponse read.
         * @member {normfs.IReadResponse|null|undefined} read
         * @memberof normfs.ServerResponse
         * @instance
         */
        ServerResponse.prototype.read = null;

        /**
         * Creates a new ServerResponse instance using the specified properties.
         * @function create
         * @memberof normfs.ServerResponse
         * @static
         * @param {normfs.IServerResponse=} [properties] Properties to set
         * @returns {normfs.ServerResponse} ServerResponse instance
         */
        ServerResponse.create = function create(properties) {
            return new ServerResponse(properties);
        };

        /**
         * Encodes the specified ServerResponse message. Does not implicitly {@link normfs.ServerResponse.verify|verify} messages.
         * @function encode
         * @memberof normfs.ServerResponse
         * @static
         * @param {normfs.IServerResponse} message ServerResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ServerResponse.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.setup != null && Object.hasOwnProperty.call(message, "setup"))
                $root.normfs.SetupResponse.encode(message.setup, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.ping != null && Object.hasOwnProperty.call(message, "ping"))
                $root.normfs.PingResponse.encode(message.ping, writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.write != null && Object.hasOwnProperty.call(message, "write"))
                $root.normfs.WriteResponse.encode(message.write, writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            if (message.read != null && Object.hasOwnProperty.call(message, "read"))
                $root.normfs.ReadResponse.encode(message.read, writer.uint32(/* id 11, wireType 2 =*/90).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified ServerResponse message, length delimited. Does not implicitly {@link normfs.ServerResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof normfs.ServerResponse
         * @static
         * @param {normfs.IServerResponse} message ServerResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ServerResponse.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ServerResponse message from the specified reader or buffer.
         * @function decode
         * @memberof normfs.ServerResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {normfs.ServerResponse} ServerResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ServerResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.normfs.ServerResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.setup = $root.normfs.SetupResponse.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 2: {
                        message.ping = $root.normfs.PingResponse.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 10: {
                        message.write = $root.normfs.WriteResponse.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 11: {
                        message.read = $root.normfs.ReadResponse.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ServerResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof normfs.ServerResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {normfs.ServerResponse} ServerResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ServerResponse.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ServerResponse message.
         * @function verify
         * @memberof normfs.ServerResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ServerResponse.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.setup != null && message.hasOwnProperty("setup")) {
                let error = $root.normfs.SetupResponse.verify(message.setup, long + 1);
                if (error)
                    return "setup." + error;
            }
            if (message.ping != null && message.hasOwnProperty("ping")) {
                let error = $root.normfs.PingResponse.verify(message.ping, long + 1);
                if (error)
                    return "ping." + error;
            }
            if (message.write != null && message.hasOwnProperty("write")) {
                let error = $root.normfs.WriteResponse.verify(message.write, long + 1);
                if (error)
                    return "write." + error;
            }
            if (message.read != null && message.hasOwnProperty("read")) {
                let error = $root.normfs.ReadResponse.verify(message.read, long + 1);
                if (error)
                    return "read." + error;
            }
            return null;
        };

        /**
         * Creates a ServerResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof normfs.ServerResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {normfs.ServerResponse} ServerResponse
         */
        ServerResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.normfs.ServerResponse)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.normfs.ServerResponse();
            if (object.setup != null) {
                if (typeof object.setup !== "object")
                    throw TypeError(".normfs.ServerResponse.setup: object expected");
                message.setup = $root.normfs.SetupResponse.fromObject(object.setup, long + 1);
            }
            if (object.ping != null) {
                if (typeof object.ping !== "object")
                    throw TypeError(".normfs.ServerResponse.ping: object expected");
                message.ping = $root.normfs.PingResponse.fromObject(object.ping, long + 1);
            }
            if (object.write != null) {
                if (typeof object.write !== "object")
                    throw TypeError(".normfs.ServerResponse.write: object expected");
                message.write = $root.normfs.WriteResponse.fromObject(object.write, long + 1);
            }
            if (object.read != null) {
                if (typeof object.read !== "object")
                    throw TypeError(".normfs.ServerResponse.read: object expected");
                message.read = $root.normfs.ReadResponse.fromObject(object.read, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a ServerResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof normfs.ServerResponse
         * @static
         * @param {normfs.ServerResponse} message ServerResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ServerResponse.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.setup = null;
                object.ping = null;
                object.write = null;
                object.read = null;
            }
            if (message.setup != null && message.hasOwnProperty("setup"))
                object.setup = $root.normfs.SetupResponse.toObject(message.setup, options);
            if (message.ping != null && message.hasOwnProperty("ping"))
                object.ping = $root.normfs.PingResponse.toObject(message.ping, options);
            if (message.write != null && message.hasOwnProperty("write"))
                object.write = $root.normfs.WriteResponse.toObject(message.write, options);
            if (message.read != null && message.hasOwnProperty("read"))
                object.read = $root.normfs.ReadResponse.toObject(message.read, options);
            return object;
        };

        /**
         * Converts this ServerResponse to JSON.
         * @function toJSON
         * @memberof normfs.ServerResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ServerResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ServerResponse
         * @function getTypeUrl
         * @memberof normfs.ServerResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ServerResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/normfs.ServerResponse";
        };

        return ServerResponse;
    })();

    normfs.Id = (function() {

        /**
         * Properties of an Id.
         * @memberof normfs
         * @interface IId
         * @property {Uint8Array|null} [raw] Id raw
         */

        /**
         * Constructs a new Id.
         * @memberof normfs
         * @classdesc Represents an Id.
         * @implements IId
         * @constructor
         * @param {normfs.IId=} [properties] Properties to set
         */
        function Id(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Id raw.
         * @member {Uint8Array} raw
         * @memberof normfs.Id
         * @instance
         */
        Id.prototype.raw = $util.newBuffer([]);

        /**
         * Creates a new Id instance using the specified properties.
         * @function create
         * @memberof normfs.Id
         * @static
         * @param {normfs.IId=} [properties] Properties to set
         * @returns {normfs.Id} Id instance
         */
        Id.create = function create(properties) {
            return new Id(properties);
        };

        /**
         * Encodes the specified Id message. Does not implicitly {@link normfs.Id.verify|verify} messages.
         * @function encode
         * @memberof normfs.Id
         * @static
         * @param {normfs.IId} message Id message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Id.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.raw != null && Object.hasOwnProperty.call(message, "raw"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.raw);
            return writer;
        };

        /**
         * Encodes the specified Id message, length delimited. Does not implicitly {@link normfs.Id.verify|verify} messages.
         * @function encodeDelimited
         * @memberof normfs.Id
         * @static
         * @param {normfs.IId} message Id message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Id.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an Id message from the specified reader or buffer.
         * @function decode
         * @memberof normfs.Id
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {normfs.Id} Id
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Id.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.normfs.Id();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.raw = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an Id message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof normfs.Id
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {normfs.Id} Id
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Id.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an Id message.
         * @function verify
         * @memberof normfs.Id
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Id.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.raw != null && message.hasOwnProperty("raw"))
                if (!(message.raw && typeof message.raw.length === "number" || $util.isString(message.raw)))
                    return "raw: buffer expected";
            return null;
        };

        /**
         * Creates an Id message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof normfs.Id
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {normfs.Id} Id
         */
        Id.fromObject = function fromObject(object, long) {
            if (object instanceof $root.normfs.Id)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.normfs.Id();
            if (object.raw != null)
                if (typeof object.raw === "string")
                    $util.base64.decode(object.raw, message.raw = $util.newBuffer($util.base64.length(object.raw)), 0);
                else if (object.raw.length >= 0)
                    message.raw = object.raw;
            return message;
        };

        /**
         * Creates a plain object from an Id message. Also converts values to other types if specified.
         * @function toObject
         * @memberof normfs.Id
         * @static
         * @param {normfs.Id} message Id
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Id.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                if (options.bytes === String)
                    object.raw = "";
                else {
                    object.raw = [];
                    if (options.bytes !== Array)
                        object.raw = $util.newBuffer(object.raw);
                }
            if (message.raw != null && message.hasOwnProperty("raw"))
                object.raw = options.bytes === String ? $util.base64.encode(message.raw, 0, message.raw.length) : options.bytes === Array ? Array.prototype.slice.call(message.raw) : message.raw;
            return object;
        };

        /**
         * Converts this Id to JSON.
         * @function toJSON
         * @memberof normfs.Id
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Id.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Id
         * @function getTypeUrl
         * @memberof normfs.Id
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Id.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/normfs.Id";
        };

        return Id;
    })();

    /**
     * OffsetType enum.
     * @name normfs.OffsetType
     * @enum {number}
     * @property {number} OT_ABSOLUTE=0 OT_ABSOLUTE value
     * @property {number} OT_SHIFT_FROM_TAIL=1 OT_SHIFT_FROM_TAIL value
     */
    normfs.OffsetType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "OT_ABSOLUTE"] = 0;
        values[valuesById[1] = "OT_SHIFT_FROM_TAIL"] = 1;
        return values;
    })();

    normfs.Offset = (function() {

        /**
         * Properties of an Offset.
         * @memberof normfs
         * @interface IOffset
         * @property {normfs.IId|null} [id] Offset id
         * @property {normfs.OffsetType|null} [type] Offset type
         */

        /**
         * Constructs a new Offset.
         * @memberof normfs
         * @classdesc Represents an Offset.
         * @implements IOffset
         * @constructor
         * @param {normfs.IOffset=} [properties] Properties to set
         */
        function Offset(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Offset id.
         * @member {normfs.IId|null|undefined} id
         * @memberof normfs.Offset
         * @instance
         */
        Offset.prototype.id = null;

        /**
         * Offset type.
         * @member {normfs.OffsetType} type
         * @memberof normfs.Offset
         * @instance
         */
        Offset.prototype.type = 0;

        /**
         * Creates a new Offset instance using the specified properties.
         * @function create
         * @memberof normfs.Offset
         * @static
         * @param {normfs.IOffset=} [properties] Properties to set
         * @returns {normfs.Offset} Offset instance
         */
        Offset.create = function create(properties) {
            return new Offset(properties);
        };

        /**
         * Encodes the specified Offset message. Does not implicitly {@link normfs.Offset.verify|verify} messages.
         * @function encode
         * @memberof normfs.Offset
         * @static
         * @param {normfs.IOffset} message Offset message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Offset.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                $root.normfs.Id.encode(message.id, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.type);
            return writer;
        };

        /**
         * Encodes the specified Offset message, length delimited. Does not implicitly {@link normfs.Offset.verify|verify} messages.
         * @function encodeDelimited
         * @memberof normfs.Offset
         * @static
         * @param {normfs.IOffset} message Offset message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Offset.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an Offset message from the specified reader or buffer.
         * @function decode
         * @memberof normfs.Offset
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {normfs.Offset} Offset
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Offset.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.normfs.Offset();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.id = $root.normfs.Id.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 2: {
                        message.type = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an Offset message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof normfs.Offset
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {normfs.Offset} Offset
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Offset.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an Offset message.
         * @function verify
         * @memberof normfs.Offset
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Offset.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.id != null && message.hasOwnProperty("id")) {
                let error = $root.normfs.Id.verify(message.id, long + 1);
                if (error)
                    return "id." + error;
            }
            if (message.type != null && message.hasOwnProperty("type"))
                switch (message.type) {
                default:
                    return "type: enum value expected";
                case 0:
                case 1:
                    break;
                }
            return null;
        };

        /**
         * Creates an Offset message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof normfs.Offset
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {normfs.Offset} Offset
         */
        Offset.fromObject = function fromObject(object, long) {
            if (object instanceof $root.normfs.Offset)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.normfs.Offset();
            if (object.id != null) {
                if (typeof object.id !== "object")
                    throw TypeError(".normfs.Offset.id: object expected");
                message.id = $root.normfs.Id.fromObject(object.id, long + 1);
            }
            switch (object.type) {
            default:
                if (typeof object.type === "number") {
                    message.type = object.type;
                    break;
                }
                break;
            case "OT_ABSOLUTE":
            case 0:
                message.type = 0;
                break;
            case "OT_SHIFT_FROM_TAIL":
            case 1:
                message.type = 1;
                break;
            }
            return message;
        };

        /**
         * Creates a plain object from an Offset message. Also converts values to other types if specified.
         * @function toObject
         * @memberof normfs.Offset
         * @static
         * @param {normfs.Offset} message Offset
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Offset.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.id = null;
                object.type = options.enums === String ? "OT_ABSOLUTE" : 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = $root.normfs.Id.toObject(message.id, options);
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = options.enums === String ? $root.normfs.OffsetType[message.type] === undefined ? message.type : $root.normfs.OffsetType[message.type] : message.type;
            return object;
        };

        /**
         * Converts this Offset to JSON.
         * @function toJSON
         * @memberof normfs.Offset
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Offset.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Offset
         * @function getTypeUrl
         * @memberof normfs.Offset
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Offset.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/normfs.Offset";
        };

        return Offset;
    })();

    normfs.SetupRequest = (function() {

        /**
         * Properties of a SetupRequest.
         * @memberof normfs
         * @interface ISetupRequest
         * @property {Long|null} [version] SetupRequest version
         */

        /**
         * Constructs a new SetupRequest.
         * @memberof normfs
         * @classdesc Represents a SetupRequest.
         * @implements ISetupRequest
         * @constructor
         * @param {normfs.ISetupRequest=} [properties] Properties to set
         */
        function SetupRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SetupRequest version.
         * @member {Long} version
         * @memberof normfs.SetupRequest
         * @instance
         */
        SetupRequest.prototype.version = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new SetupRequest instance using the specified properties.
         * @function create
         * @memberof normfs.SetupRequest
         * @static
         * @param {normfs.ISetupRequest=} [properties] Properties to set
         * @returns {normfs.SetupRequest} SetupRequest instance
         */
        SetupRequest.create = function create(properties) {
            return new SetupRequest(properties);
        };

        /**
         * Encodes the specified SetupRequest message. Does not implicitly {@link normfs.SetupRequest.verify|verify} messages.
         * @function encode
         * @memberof normfs.SetupRequest
         * @static
         * @param {normfs.ISetupRequest} message SetupRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SetupRequest.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.version);
            return writer;
        };

        /**
         * Encodes the specified SetupRequest message, length delimited. Does not implicitly {@link normfs.SetupRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof normfs.SetupRequest
         * @static
         * @param {normfs.ISetupRequest} message SetupRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SetupRequest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SetupRequest message from the specified reader or buffer.
         * @function decode
         * @memberof normfs.SetupRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {normfs.SetupRequest} SetupRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SetupRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.normfs.SetupRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.version = reader.uint64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SetupRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof normfs.SetupRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {normfs.SetupRequest} SetupRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SetupRequest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SetupRequest message.
         * @function verify
         * @memberof normfs.SetupRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SetupRequest.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.version != null && message.hasOwnProperty("version"))
                if (!$util.isInteger(message.version) && !(message.version && $util.isInteger(message.version.low) && $util.isInteger(message.version.high)))
                    return "version: integer|Long expected";
            return null;
        };

        /**
         * Creates a SetupRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof normfs.SetupRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {normfs.SetupRequest} SetupRequest
         */
        SetupRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.normfs.SetupRequest)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.normfs.SetupRequest();
            if (object.version != null)
                if ($util.Long)
                    (message.version = $util.Long.fromValue(object.version)).unsigned = true;
                else if (typeof object.version === "string")
                    message.version = parseInt(object.version, 10);
                else if (typeof object.version === "number")
                    message.version = object.version;
                else if (typeof object.version === "object")
                    message.version = new $util.LongBits(object.version.low >>> 0, object.version.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a SetupRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof normfs.SetupRequest
         * @static
         * @param {normfs.SetupRequest} message SetupRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SetupRequest.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.version = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.version = options.longs === String ? "0" : 0;
            if (message.version != null && message.hasOwnProperty("version"))
                if (typeof message.version === "number")
                    object.version = options.longs === String ? String(message.version) : message.version;
                else
                    object.version = options.longs === String ? $util.Long.prototype.toString.call(message.version) : options.longs === Number ? new $util.LongBits(message.version.low >>> 0, message.version.high >>> 0).toNumber(true) : message.version;
            return object;
        };

        /**
         * Converts this SetupRequest to JSON.
         * @function toJSON
         * @memberof normfs.SetupRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SetupRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SetupRequest
         * @function getTypeUrl
         * @memberof normfs.SetupRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SetupRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/normfs.SetupRequest";
        };

        return SetupRequest;
    })();

    normfs.SetupResponse = (function() {

        /**
         * Properties of a SetupResponse.
         * @memberof normfs
         * @interface ISetupResponse
         * @property {Long|null} [version] SetupResponse version
         * @property {Uint8Array|null} [instanceIdBytes] SetupResponse instanceIdBytes
         * @property {string|null} [instanceId] SetupResponse instanceId
         */

        /**
         * Constructs a new SetupResponse.
         * @memberof normfs
         * @classdesc Represents a SetupResponse.
         * @implements ISetupResponse
         * @constructor
         * @param {normfs.ISetupResponse=} [properties] Properties to set
         */
        function SetupResponse(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SetupResponse version.
         * @member {Long} version
         * @memberof normfs.SetupResponse
         * @instance
         */
        SetupResponse.prototype.version = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * SetupResponse instanceIdBytes.
         * @member {Uint8Array} instanceIdBytes
         * @memberof normfs.SetupResponse
         * @instance
         */
        SetupResponse.prototype.instanceIdBytes = $util.newBuffer([]);

        /**
         * SetupResponse instanceId.
         * @member {string} instanceId
         * @memberof normfs.SetupResponse
         * @instance
         */
        SetupResponse.prototype.instanceId = "";

        /**
         * Creates a new SetupResponse instance using the specified properties.
         * @function create
         * @memberof normfs.SetupResponse
         * @static
         * @param {normfs.ISetupResponse=} [properties] Properties to set
         * @returns {normfs.SetupResponse} SetupResponse instance
         */
        SetupResponse.create = function create(properties) {
            return new SetupResponse(properties);
        };

        /**
         * Encodes the specified SetupResponse message. Does not implicitly {@link normfs.SetupResponse.verify|verify} messages.
         * @function encode
         * @memberof normfs.SetupResponse
         * @static
         * @param {normfs.ISetupResponse} message SetupResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SetupResponse.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.version);
            if (message.instanceIdBytes != null && Object.hasOwnProperty.call(message, "instanceIdBytes"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.instanceIdBytes);
            if (message.instanceId != null && Object.hasOwnProperty.call(message, "instanceId"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.instanceId);
            return writer;
        };

        /**
         * Encodes the specified SetupResponse message, length delimited. Does not implicitly {@link normfs.SetupResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof normfs.SetupResponse
         * @static
         * @param {normfs.ISetupResponse} message SetupResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SetupResponse.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SetupResponse message from the specified reader or buffer.
         * @function decode
         * @memberof normfs.SetupResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {normfs.SetupResponse} SetupResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SetupResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.normfs.SetupResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.version = reader.uint64();
                        break;
                    }
                case 2: {
                        message.instanceIdBytes = reader.bytes();
                        break;
                    }
                case 3: {
                        message.instanceId = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SetupResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof normfs.SetupResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {normfs.SetupResponse} SetupResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SetupResponse.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SetupResponse message.
         * @function verify
         * @memberof normfs.SetupResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SetupResponse.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.version != null && message.hasOwnProperty("version"))
                if (!$util.isInteger(message.version) && !(message.version && $util.isInteger(message.version.low) && $util.isInteger(message.version.high)))
                    return "version: integer|Long expected";
            if (message.instanceIdBytes != null && message.hasOwnProperty("instanceIdBytes"))
                if (!(message.instanceIdBytes && typeof message.instanceIdBytes.length === "number" || $util.isString(message.instanceIdBytes)))
                    return "instanceIdBytes: buffer expected";
            if (message.instanceId != null && message.hasOwnProperty("instanceId"))
                if (!$util.isString(message.instanceId))
                    return "instanceId: string expected";
            return null;
        };

        /**
         * Creates a SetupResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof normfs.SetupResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {normfs.SetupResponse} SetupResponse
         */
        SetupResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.normfs.SetupResponse)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.normfs.SetupResponse();
            if (object.version != null)
                if ($util.Long)
                    (message.version = $util.Long.fromValue(object.version)).unsigned = true;
                else if (typeof object.version === "string")
                    message.version = parseInt(object.version, 10);
                else if (typeof object.version === "number")
                    message.version = object.version;
                else if (typeof object.version === "object")
                    message.version = new $util.LongBits(object.version.low >>> 0, object.version.high >>> 0).toNumber(true);
            if (object.instanceIdBytes != null)
                if (typeof object.instanceIdBytes === "string")
                    $util.base64.decode(object.instanceIdBytes, message.instanceIdBytes = $util.newBuffer($util.base64.length(object.instanceIdBytes)), 0);
                else if (object.instanceIdBytes.length >= 0)
                    message.instanceIdBytes = object.instanceIdBytes;
            if (object.instanceId != null)
                message.instanceId = String(object.instanceId);
            return message;
        };

        /**
         * Creates a plain object from a SetupResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof normfs.SetupResponse
         * @static
         * @param {normfs.SetupResponse} message SetupResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SetupResponse.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.version = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.version = options.longs === String ? "0" : 0;
                if (options.bytes === String)
                    object.instanceIdBytes = "";
                else {
                    object.instanceIdBytes = [];
                    if (options.bytes !== Array)
                        object.instanceIdBytes = $util.newBuffer(object.instanceIdBytes);
                }
                object.instanceId = "";
            }
            if (message.version != null && message.hasOwnProperty("version"))
                if (typeof message.version === "number")
                    object.version = options.longs === String ? String(message.version) : message.version;
                else
                    object.version = options.longs === String ? $util.Long.prototype.toString.call(message.version) : options.longs === Number ? new $util.LongBits(message.version.low >>> 0, message.version.high >>> 0).toNumber(true) : message.version;
            if (message.instanceIdBytes != null && message.hasOwnProperty("instanceIdBytes"))
                object.instanceIdBytes = options.bytes === String ? $util.base64.encode(message.instanceIdBytes, 0, message.instanceIdBytes.length) : options.bytes === Array ? Array.prototype.slice.call(message.instanceIdBytes) : message.instanceIdBytes;
            if (message.instanceId != null && message.hasOwnProperty("instanceId"))
                object.instanceId = message.instanceId;
            return object;
        };

        /**
         * Converts this SetupResponse to JSON.
         * @function toJSON
         * @memberof normfs.SetupResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SetupResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SetupResponse
         * @function getTypeUrl
         * @memberof normfs.SetupResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SetupResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/normfs.SetupResponse";
        };

        return SetupResponse;
    })();

    normfs.PingRequest = (function() {

        /**
         * Properties of a PingRequest.
         * @memberof normfs
         * @interface IPingRequest
         * @property {Long|null} [sequence] PingRequest sequence
         * @property {Long|null} [clientTimestampNs] PingRequest clientTimestampNs
         */

        /**
         * Constructs a new PingRequest.
         * @memberof normfs
         * @classdesc Represents a PingRequest.
         * @implements IPingRequest
         * @constructor
         * @param {normfs.IPingRequest=} [properties] Properties to set
         */
        function PingRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PingRequest sequence.
         * @member {Long} sequence
         * @memberof normfs.PingRequest
         * @instance
         */
        PingRequest.prototype.sequence = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * PingRequest clientTimestampNs.
         * @member {Long} clientTimestampNs
         * @memberof normfs.PingRequest
         * @instance
         */
        PingRequest.prototype.clientTimestampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new PingRequest instance using the specified properties.
         * @function create
         * @memberof normfs.PingRequest
         * @static
         * @param {normfs.IPingRequest=} [properties] Properties to set
         * @returns {normfs.PingRequest} PingRequest instance
         */
        PingRequest.create = function create(properties) {
            return new PingRequest(properties);
        };

        /**
         * Encodes the specified PingRequest message. Does not implicitly {@link normfs.PingRequest.verify|verify} messages.
         * @function encode
         * @memberof normfs.PingRequest
         * @static
         * @param {normfs.IPingRequest} message PingRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PingRequest.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.sequence != null && Object.hasOwnProperty.call(message, "sequence"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.sequence);
            if (message.clientTimestampNs != null && Object.hasOwnProperty.call(message, "clientTimestampNs"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.clientTimestampNs);
            return writer;
        };

        /**
         * Encodes the specified PingRequest message, length delimited. Does not implicitly {@link normfs.PingRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof normfs.PingRequest
         * @static
         * @param {normfs.IPingRequest} message PingRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PingRequest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a PingRequest message from the specified reader or buffer.
         * @function decode
         * @memberof normfs.PingRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {normfs.PingRequest} PingRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PingRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.normfs.PingRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.sequence = reader.uint64();
                        break;
                    }
                case 2: {
                        message.clientTimestampNs = reader.uint64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a PingRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof normfs.PingRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {normfs.PingRequest} PingRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PingRequest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PingRequest message.
         * @function verify
         * @memberof normfs.PingRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PingRequest.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.sequence != null && message.hasOwnProperty("sequence"))
                if (!$util.isInteger(message.sequence) && !(message.sequence && $util.isInteger(message.sequence.low) && $util.isInteger(message.sequence.high)))
                    return "sequence: integer|Long expected";
            if (message.clientTimestampNs != null && message.hasOwnProperty("clientTimestampNs"))
                if (!$util.isInteger(message.clientTimestampNs) && !(message.clientTimestampNs && $util.isInteger(message.clientTimestampNs.low) && $util.isInteger(message.clientTimestampNs.high)))
                    return "clientTimestampNs: integer|Long expected";
            return null;
        };

        /**
         * Creates a PingRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof normfs.PingRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {normfs.PingRequest} PingRequest
         */
        PingRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.normfs.PingRequest)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.normfs.PingRequest();
            if (object.sequence != null)
                if ($util.Long)
                    (message.sequence = $util.Long.fromValue(object.sequence)).unsigned = true;
                else if (typeof object.sequence === "string")
                    message.sequence = parseInt(object.sequence, 10);
                else if (typeof object.sequence === "number")
                    message.sequence = object.sequence;
                else if (typeof object.sequence === "object")
                    message.sequence = new $util.LongBits(object.sequence.low >>> 0, object.sequence.high >>> 0).toNumber(true);
            if (object.clientTimestampNs != null)
                if ($util.Long)
                    (message.clientTimestampNs = $util.Long.fromValue(object.clientTimestampNs)).unsigned = true;
                else if (typeof object.clientTimestampNs === "string")
                    message.clientTimestampNs = parseInt(object.clientTimestampNs, 10);
                else if (typeof object.clientTimestampNs === "number")
                    message.clientTimestampNs = object.clientTimestampNs;
                else if (typeof object.clientTimestampNs === "object")
                    message.clientTimestampNs = new $util.LongBits(object.clientTimestampNs.low >>> 0, object.clientTimestampNs.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a PingRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof normfs.PingRequest
         * @static
         * @param {normfs.PingRequest} message PingRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PingRequest.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.sequence = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.sequence = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.clientTimestampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.clientTimestampNs = options.longs === String ? "0" : 0;
            }
            if (message.sequence != null && message.hasOwnProperty("sequence"))
                if (typeof message.sequence === "number")
                    object.sequence = options.longs === String ? String(message.sequence) : message.sequence;
                else
                    object.sequence = options.longs === String ? $util.Long.prototype.toString.call(message.sequence) : options.longs === Number ? new $util.LongBits(message.sequence.low >>> 0, message.sequence.high >>> 0).toNumber(true) : message.sequence;
            if (message.clientTimestampNs != null && message.hasOwnProperty("clientTimestampNs"))
                if (typeof message.clientTimestampNs === "number")
                    object.clientTimestampNs = options.longs === String ? String(message.clientTimestampNs) : message.clientTimestampNs;
                else
                    object.clientTimestampNs = options.longs === String ? $util.Long.prototype.toString.call(message.clientTimestampNs) : options.longs === Number ? new $util.LongBits(message.clientTimestampNs.low >>> 0, message.clientTimestampNs.high >>> 0).toNumber(true) : message.clientTimestampNs;
            return object;
        };

        /**
         * Converts this PingRequest to JSON.
         * @function toJSON
         * @memberof normfs.PingRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PingRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PingRequest
         * @function getTypeUrl
         * @memberof normfs.PingRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PingRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/normfs.PingRequest";
        };

        return PingRequest;
    })();

    normfs.PingResponse = (function() {

        /**
         * Properties of a PingResponse.
         * @memberof normfs
         * @interface IPingResponse
         * @property {Long|null} [localStampNs] PingResponse localStampNs
         * @property {Long|null} [monotonicStampNs] PingResponse monotonicStampNs
         * @property {normfs.IPingRequest|null} [request] PingResponse request
         */

        /**
         * Constructs a new PingResponse.
         * @memberof normfs
         * @classdesc Represents a PingResponse.
         * @implements IPingResponse
         * @constructor
         * @param {normfs.IPingResponse=} [properties] Properties to set
         */
        function PingResponse(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PingResponse localStampNs.
         * @member {Long} localStampNs
         * @memberof normfs.PingResponse
         * @instance
         */
        PingResponse.prototype.localStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * PingResponse monotonicStampNs.
         * @member {Long} monotonicStampNs
         * @memberof normfs.PingResponse
         * @instance
         */
        PingResponse.prototype.monotonicStampNs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * PingResponse request.
         * @member {normfs.IPingRequest|null|undefined} request
         * @memberof normfs.PingResponse
         * @instance
         */
        PingResponse.prototype.request = null;

        /**
         * Creates a new PingResponse instance using the specified properties.
         * @function create
         * @memberof normfs.PingResponse
         * @static
         * @param {normfs.IPingResponse=} [properties] Properties to set
         * @returns {normfs.PingResponse} PingResponse instance
         */
        PingResponse.create = function create(properties) {
            return new PingResponse(properties);
        };

        /**
         * Encodes the specified PingResponse message. Does not implicitly {@link normfs.PingResponse.verify|verify} messages.
         * @function encode
         * @memberof normfs.PingResponse
         * @static
         * @param {normfs.IPingResponse} message PingResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PingResponse.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.localStampNs != null && Object.hasOwnProperty.call(message, "localStampNs"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.localStampNs);
            if (message.monotonicStampNs != null && Object.hasOwnProperty.call(message, "monotonicStampNs"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.monotonicStampNs);
            if (message.request != null && Object.hasOwnProperty.call(message, "request"))
                $root.normfs.PingRequest.encode(message.request, writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified PingResponse message, length delimited. Does not implicitly {@link normfs.PingResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof normfs.PingResponse
         * @static
         * @param {normfs.IPingResponse} message PingResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PingResponse.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a PingResponse message from the specified reader or buffer.
         * @function decode
         * @memberof normfs.PingResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {normfs.PingResponse} PingResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PingResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.normfs.PingResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 2: {
                        message.localStampNs = reader.uint64();
                        break;
                    }
                case 3: {
                        message.monotonicStampNs = reader.uint64();
                        break;
                    }
                case 4: {
                        message.request = $root.normfs.PingRequest.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a PingResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof normfs.PingResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {normfs.PingResponse} PingResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PingResponse.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PingResponse message.
         * @function verify
         * @memberof normfs.PingResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PingResponse.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (!$util.isInteger(message.localStampNs) && !(message.localStampNs && $util.isInteger(message.localStampNs.low) && $util.isInteger(message.localStampNs.high)))
                    return "localStampNs: integer|Long expected";
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (!$util.isInteger(message.monotonicStampNs) && !(message.monotonicStampNs && $util.isInteger(message.monotonicStampNs.low) && $util.isInteger(message.monotonicStampNs.high)))
                    return "monotonicStampNs: integer|Long expected";
            if (message.request != null && message.hasOwnProperty("request")) {
                let error = $root.normfs.PingRequest.verify(message.request, long + 1);
                if (error)
                    return "request." + error;
            }
            return null;
        };

        /**
         * Creates a PingResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof normfs.PingResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {normfs.PingResponse} PingResponse
         */
        PingResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.normfs.PingResponse)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.normfs.PingResponse();
            if (object.localStampNs != null)
                if ($util.Long)
                    (message.localStampNs = $util.Long.fromValue(object.localStampNs)).unsigned = true;
                else if (typeof object.localStampNs === "string")
                    message.localStampNs = parseInt(object.localStampNs, 10);
                else if (typeof object.localStampNs === "number")
                    message.localStampNs = object.localStampNs;
                else if (typeof object.localStampNs === "object")
                    message.localStampNs = new $util.LongBits(object.localStampNs.low >>> 0, object.localStampNs.high >>> 0).toNumber(true);
            if (object.monotonicStampNs != null)
                if ($util.Long)
                    (message.monotonicStampNs = $util.Long.fromValue(object.monotonicStampNs)).unsigned = true;
                else if (typeof object.monotonicStampNs === "string")
                    message.monotonicStampNs = parseInt(object.monotonicStampNs, 10);
                else if (typeof object.monotonicStampNs === "number")
                    message.monotonicStampNs = object.monotonicStampNs;
                else if (typeof object.monotonicStampNs === "object")
                    message.monotonicStampNs = new $util.LongBits(object.monotonicStampNs.low >>> 0, object.monotonicStampNs.high >>> 0).toNumber(true);
            if (object.request != null) {
                if (typeof object.request !== "object")
                    throw TypeError(".normfs.PingResponse.request: object expected");
                message.request = $root.normfs.PingRequest.fromObject(object.request, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a PingResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof normfs.PingResponse
         * @static
         * @param {normfs.PingResponse} message PingResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PingResponse.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.localStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.localStampNs = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.monotonicStampNs = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.monotonicStampNs = options.longs === String ? "0" : 0;
                object.request = null;
            }
            if (message.localStampNs != null && message.hasOwnProperty("localStampNs"))
                if (typeof message.localStampNs === "number")
                    object.localStampNs = options.longs === String ? String(message.localStampNs) : message.localStampNs;
                else
                    object.localStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.localStampNs) : options.longs === Number ? new $util.LongBits(message.localStampNs.low >>> 0, message.localStampNs.high >>> 0).toNumber(true) : message.localStampNs;
            if (message.monotonicStampNs != null && message.hasOwnProperty("monotonicStampNs"))
                if (typeof message.monotonicStampNs === "number")
                    object.monotonicStampNs = options.longs === String ? String(message.monotonicStampNs) : message.monotonicStampNs;
                else
                    object.monotonicStampNs = options.longs === String ? $util.Long.prototype.toString.call(message.monotonicStampNs) : options.longs === Number ? new $util.LongBits(message.monotonicStampNs.low >>> 0, message.monotonicStampNs.high >>> 0).toNumber(true) : message.monotonicStampNs;
            if (message.request != null && message.hasOwnProperty("request"))
                object.request = $root.normfs.PingRequest.toObject(message.request, options);
            return object;
        };

        /**
         * Converts this PingResponse to JSON.
         * @function toJSON
         * @memberof normfs.PingResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PingResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PingResponse
         * @function getTypeUrl
         * @memberof normfs.PingResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PingResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/normfs.PingResponse";
        };

        return PingResponse;
    })();

    normfs.WriteRequest = (function() {

        /**
         * Properties of a WriteRequest.
         * @memberof normfs
         * @interface IWriteRequest
         * @property {Long|null} [writeId] WriteRequest writeId
         * @property {string|null} [queueId] WriteRequest queueId
         * @property {Array.<Uint8Array>|null} [packets] WriteRequest packets
         */

        /**
         * Constructs a new WriteRequest.
         * @memberof normfs
         * @classdesc Represents a WriteRequest.
         * @implements IWriteRequest
         * @constructor
         * @param {normfs.IWriteRequest=} [properties] Properties to set
         */
        function WriteRequest(properties) {
            this.packets = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * WriteRequest writeId.
         * @member {Long} writeId
         * @memberof normfs.WriteRequest
         * @instance
         */
        WriteRequest.prototype.writeId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * WriteRequest queueId.
         * @member {string} queueId
         * @memberof normfs.WriteRequest
         * @instance
         */
        WriteRequest.prototype.queueId = "";

        /**
         * WriteRequest packets.
         * @member {Array.<Uint8Array>} packets
         * @memberof normfs.WriteRequest
         * @instance
         */
        WriteRequest.prototype.packets = $util.emptyArray;

        /**
         * Creates a new WriteRequest instance using the specified properties.
         * @function create
         * @memberof normfs.WriteRequest
         * @static
         * @param {normfs.IWriteRequest=} [properties] Properties to set
         * @returns {normfs.WriteRequest} WriteRequest instance
         */
        WriteRequest.create = function create(properties) {
            return new WriteRequest(properties);
        };

        /**
         * Encodes the specified WriteRequest message. Does not implicitly {@link normfs.WriteRequest.verify|verify} messages.
         * @function encode
         * @memberof normfs.WriteRequest
         * @static
         * @param {normfs.IWriteRequest} message WriteRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WriteRequest.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.writeId != null && Object.hasOwnProperty.call(message, "writeId"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.writeId);
            if (message.queueId != null && Object.hasOwnProperty.call(message, "queueId"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.queueId);
            if (message.packets != null && message.packets.length)
                for (let i = 0; i < message.packets.length; ++i)
                    writer.uint32(/* id 10, wireType 2 =*/82).bytes(message.packets[i]);
            return writer;
        };

        /**
         * Encodes the specified WriteRequest message, length delimited. Does not implicitly {@link normfs.WriteRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof normfs.WriteRequest
         * @static
         * @param {normfs.IWriteRequest} message WriteRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WriteRequest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a WriteRequest message from the specified reader or buffer.
         * @function decode
         * @memberof normfs.WriteRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {normfs.WriteRequest} WriteRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WriteRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.normfs.WriteRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.writeId = reader.uint64();
                        break;
                    }
                case 2: {
                        message.queueId = reader.string();
                        break;
                    }
                case 10: {
                        if (!(message.packets && message.packets.length))
                            message.packets = [];
                        message.packets.push(reader.bytes());
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a WriteRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof normfs.WriteRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {normfs.WriteRequest} WriteRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WriteRequest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a WriteRequest message.
         * @function verify
         * @memberof normfs.WriteRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        WriteRequest.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.writeId != null && message.hasOwnProperty("writeId"))
                if (!$util.isInteger(message.writeId) && !(message.writeId && $util.isInteger(message.writeId.low) && $util.isInteger(message.writeId.high)))
                    return "writeId: integer|Long expected";
            if (message.queueId != null && message.hasOwnProperty("queueId"))
                if (!$util.isString(message.queueId))
                    return "queueId: string expected";
            if (message.packets != null && message.hasOwnProperty("packets")) {
                if (!Array.isArray(message.packets))
                    return "packets: array expected";
                for (let i = 0; i < message.packets.length; ++i)
                    if (!(message.packets[i] && typeof message.packets[i].length === "number" || $util.isString(message.packets[i])))
                        return "packets: buffer[] expected";
            }
            return null;
        };

        /**
         * Creates a WriteRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof normfs.WriteRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {normfs.WriteRequest} WriteRequest
         */
        WriteRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.normfs.WriteRequest)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.normfs.WriteRequest();
            if (object.writeId != null)
                if ($util.Long)
                    (message.writeId = $util.Long.fromValue(object.writeId)).unsigned = true;
                else if (typeof object.writeId === "string")
                    message.writeId = parseInt(object.writeId, 10);
                else if (typeof object.writeId === "number")
                    message.writeId = object.writeId;
                else if (typeof object.writeId === "object")
                    message.writeId = new $util.LongBits(object.writeId.low >>> 0, object.writeId.high >>> 0).toNumber(true);
            if (object.queueId != null)
                message.queueId = String(object.queueId);
            if (object.packets) {
                if (!Array.isArray(object.packets))
                    throw TypeError(".normfs.WriteRequest.packets: array expected");
                message.packets = [];
                for (let i = 0; i < object.packets.length; ++i)
                    if (typeof object.packets[i] === "string")
                        $util.base64.decode(object.packets[i], message.packets[i] = $util.newBuffer($util.base64.length(object.packets[i])), 0);
                    else if (object.packets[i].length >= 0)
                        message.packets[i] = object.packets[i];
            }
            return message;
        };

        /**
         * Creates a plain object from a WriteRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof normfs.WriteRequest
         * @static
         * @param {normfs.WriteRequest} message WriteRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        WriteRequest.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.packets = [];
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.writeId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.writeId = options.longs === String ? "0" : 0;
                object.queueId = "";
            }
            if (message.writeId != null && message.hasOwnProperty("writeId"))
                if (typeof message.writeId === "number")
                    object.writeId = options.longs === String ? String(message.writeId) : message.writeId;
                else
                    object.writeId = options.longs === String ? $util.Long.prototype.toString.call(message.writeId) : options.longs === Number ? new $util.LongBits(message.writeId.low >>> 0, message.writeId.high >>> 0).toNumber(true) : message.writeId;
            if (message.queueId != null && message.hasOwnProperty("queueId"))
                object.queueId = message.queueId;
            if (message.packets && message.packets.length) {
                object.packets = [];
                for (let j = 0; j < message.packets.length; ++j)
                    object.packets[j] = options.bytes === String ? $util.base64.encode(message.packets[j], 0, message.packets[j].length) : options.bytes === Array ? Array.prototype.slice.call(message.packets[j]) : message.packets[j];
            }
            return object;
        };

        /**
         * Converts this WriteRequest to JSON.
         * @function toJSON
         * @memberof normfs.WriteRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        WriteRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for WriteRequest
         * @function getTypeUrl
         * @memberof normfs.WriteRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        WriteRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/normfs.WriteRequest";
        };

        return WriteRequest;
    })();

    normfs.WriteResponse = (function() {

        /**
         * Properties of a WriteResponse.
         * @memberof normfs
         * @interface IWriteResponse
         * @property {Long|null} [writeId] WriteResponse writeId
         * @property {normfs.WriteResponse.Result|null} [result] WriteResponse result
         * @property {Array.<normfs.IId>|null} [ids] WriteResponse ids
         */

        /**
         * Constructs a new WriteResponse.
         * @memberof normfs
         * @classdesc Represents a WriteResponse.
         * @implements IWriteResponse
         * @constructor
         * @param {normfs.IWriteResponse=} [properties] Properties to set
         */
        function WriteResponse(properties) {
            this.ids = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * WriteResponse writeId.
         * @member {Long} writeId
         * @memberof normfs.WriteResponse
         * @instance
         */
        WriteResponse.prototype.writeId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * WriteResponse result.
         * @member {normfs.WriteResponse.Result} result
         * @memberof normfs.WriteResponse
         * @instance
         */
        WriteResponse.prototype.result = 0;

        /**
         * WriteResponse ids.
         * @member {Array.<normfs.IId>} ids
         * @memberof normfs.WriteResponse
         * @instance
         */
        WriteResponse.prototype.ids = $util.emptyArray;

        /**
         * Creates a new WriteResponse instance using the specified properties.
         * @function create
         * @memberof normfs.WriteResponse
         * @static
         * @param {normfs.IWriteResponse=} [properties] Properties to set
         * @returns {normfs.WriteResponse} WriteResponse instance
         */
        WriteResponse.create = function create(properties) {
            return new WriteResponse(properties);
        };

        /**
         * Encodes the specified WriteResponse message. Does not implicitly {@link normfs.WriteResponse.verify|verify} messages.
         * @function encode
         * @memberof normfs.WriteResponse
         * @static
         * @param {normfs.IWriteResponse} message WriteResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WriteResponse.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.writeId != null && Object.hasOwnProperty.call(message, "writeId"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.writeId);
            if (message.result != null && Object.hasOwnProperty.call(message, "result"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.result);
            if (message.ids != null && message.ids.length)
                for (let i = 0; i < message.ids.length; ++i)
                    $root.normfs.Id.encode(message.ids[i], writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified WriteResponse message, length delimited. Does not implicitly {@link normfs.WriteResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof normfs.WriteResponse
         * @static
         * @param {normfs.IWriteResponse} message WriteResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WriteResponse.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a WriteResponse message from the specified reader or buffer.
         * @function decode
         * @memberof normfs.WriteResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {normfs.WriteResponse} WriteResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WriteResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.normfs.WriteResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.writeId = reader.uint64();
                        break;
                    }
                case 2: {
                        message.result = reader.int32();
                        break;
                    }
                case 10: {
                        if (!(message.ids && message.ids.length))
                            message.ids = [];
                        message.ids.push($root.normfs.Id.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a WriteResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof normfs.WriteResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {normfs.WriteResponse} WriteResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WriteResponse.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a WriteResponse message.
         * @function verify
         * @memberof normfs.WriteResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        WriteResponse.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.writeId != null && message.hasOwnProperty("writeId"))
                if (!$util.isInteger(message.writeId) && !(message.writeId && $util.isInteger(message.writeId.low) && $util.isInteger(message.writeId.high)))
                    return "writeId: integer|Long expected";
            if (message.result != null && message.hasOwnProperty("result"))
                switch (message.result) {
                default:
                    return "result: enum value expected";
                case 0:
                case 1:
                    break;
                }
            if (message.ids != null && message.hasOwnProperty("ids")) {
                if (!Array.isArray(message.ids))
                    return "ids: array expected";
                for (let i = 0; i < message.ids.length; ++i) {
                    let error = $root.normfs.Id.verify(message.ids[i], long + 1);
                    if (error)
                        return "ids." + error;
                }
            }
            return null;
        };

        /**
         * Creates a WriteResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof normfs.WriteResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {normfs.WriteResponse} WriteResponse
         */
        WriteResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.normfs.WriteResponse)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.normfs.WriteResponse();
            if (object.writeId != null)
                if ($util.Long)
                    (message.writeId = $util.Long.fromValue(object.writeId)).unsigned = true;
                else if (typeof object.writeId === "string")
                    message.writeId = parseInt(object.writeId, 10);
                else if (typeof object.writeId === "number")
                    message.writeId = object.writeId;
                else if (typeof object.writeId === "object")
                    message.writeId = new $util.LongBits(object.writeId.low >>> 0, object.writeId.high >>> 0).toNumber(true);
            switch (object.result) {
            default:
                if (typeof object.result === "number") {
                    message.result = object.result;
                    break;
                }
                break;
            case "WR_DONE":
            case 0:
                message.result = 0;
                break;
            case "WR_SERVER_ERROR":
            case 1:
                message.result = 1;
                break;
            }
            if (object.ids) {
                if (!Array.isArray(object.ids))
                    throw TypeError(".normfs.WriteResponse.ids: array expected");
                message.ids = [];
                for (let i = 0; i < object.ids.length; ++i) {
                    if (typeof object.ids[i] !== "object")
                        throw TypeError(".normfs.WriteResponse.ids: object expected");
                    message.ids[i] = $root.normfs.Id.fromObject(object.ids[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a WriteResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof normfs.WriteResponse
         * @static
         * @param {normfs.WriteResponse} message WriteResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        WriteResponse.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.ids = [];
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.writeId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.writeId = options.longs === String ? "0" : 0;
                object.result = options.enums === String ? "WR_DONE" : 0;
            }
            if (message.writeId != null && message.hasOwnProperty("writeId"))
                if (typeof message.writeId === "number")
                    object.writeId = options.longs === String ? String(message.writeId) : message.writeId;
                else
                    object.writeId = options.longs === String ? $util.Long.prototype.toString.call(message.writeId) : options.longs === Number ? new $util.LongBits(message.writeId.low >>> 0, message.writeId.high >>> 0).toNumber(true) : message.writeId;
            if (message.result != null && message.hasOwnProperty("result"))
                object.result = options.enums === String ? $root.normfs.WriteResponse.Result[message.result] === undefined ? message.result : $root.normfs.WriteResponse.Result[message.result] : message.result;
            if (message.ids && message.ids.length) {
                object.ids = [];
                for (let j = 0; j < message.ids.length; ++j)
                    object.ids[j] = $root.normfs.Id.toObject(message.ids[j], options);
            }
            return object;
        };

        /**
         * Converts this WriteResponse to JSON.
         * @function toJSON
         * @memberof normfs.WriteResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        WriteResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for WriteResponse
         * @function getTypeUrl
         * @memberof normfs.WriteResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        WriteResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/normfs.WriteResponse";
        };

        /**
         * Result enum.
         * @name normfs.WriteResponse.Result
         * @enum {number}
         * @property {number} WR_DONE=0 WR_DONE value
         * @property {number} WR_SERVER_ERROR=1 WR_SERVER_ERROR value
         */
        WriteResponse.Result = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "WR_DONE"] = 0;
            values[valuesById[1] = "WR_SERVER_ERROR"] = 1;
            return values;
        })();

        return WriteResponse;
    })();

    normfs.ReadRequest = (function() {

        /**
         * Properties of a ReadRequest.
         * @memberof normfs
         * @interface IReadRequest
         * @property {Long|null} [readId] ReadRequest readId
         * @property {string|null} [queueId] ReadRequest queueId
         * @property {Long|null} [step] ReadRequest step
         * @property {normfs.IOffset|null} [offset] ReadRequest offset
         * @property {Long|null} [limit] ReadRequest limit
         */

        /**
         * Constructs a new ReadRequest.
         * @memberof normfs
         * @classdesc Represents a ReadRequest.
         * @implements IReadRequest
         * @constructor
         * @param {normfs.IReadRequest=} [properties] Properties to set
         */
        function ReadRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ReadRequest readId.
         * @member {Long} readId
         * @memberof normfs.ReadRequest
         * @instance
         */
        ReadRequest.prototype.readId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ReadRequest queueId.
         * @member {string} queueId
         * @memberof normfs.ReadRequest
         * @instance
         */
        ReadRequest.prototype.queueId = "";

        /**
         * ReadRequest step.
         * @member {Long} step
         * @memberof normfs.ReadRequest
         * @instance
         */
        ReadRequest.prototype.step = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ReadRequest offset.
         * @member {normfs.IOffset|null|undefined} offset
         * @memberof normfs.ReadRequest
         * @instance
         */
        ReadRequest.prototype.offset = null;

        /**
         * ReadRequest limit.
         * @member {Long} limit
         * @memberof normfs.ReadRequest
         * @instance
         */
        ReadRequest.prototype.limit = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new ReadRequest instance using the specified properties.
         * @function create
         * @memberof normfs.ReadRequest
         * @static
         * @param {normfs.IReadRequest=} [properties] Properties to set
         * @returns {normfs.ReadRequest} ReadRequest instance
         */
        ReadRequest.create = function create(properties) {
            return new ReadRequest(properties);
        };

        /**
         * Encodes the specified ReadRequest message. Does not implicitly {@link normfs.ReadRequest.verify|verify} messages.
         * @function encode
         * @memberof normfs.ReadRequest
         * @static
         * @param {normfs.IReadRequest} message ReadRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReadRequest.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.readId != null && Object.hasOwnProperty.call(message, "readId"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.readId);
            if (message.queueId != null && Object.hasOwnProperty.call(message, "queueId"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.queueId);
            if (message.step != null && Object.hasOwnProperty.call(message, "step"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.step);
            if (message.offset != null && Object.hasOwnProperty.call(message, "offset"))
                $root.normfs.Offset.encode(message.offset, writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            if (message.limit != null && Object.hasOwnProperty.call(message, "limit"))
                writer.uint32(/* id 11, wireType 0 =*/88).uint64(message.limit);
            return writer;
        };

        /**
         * Encodes the specified ReadRequest message, length delimited. Does not implicitly {@link normfs.ReadRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof normfs.ReadRequest
         * @static
         * @param {normfs.IReadRequest} message ReadRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReadRequest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ReadRequest message from the specified reader or buffer.
         * @function decode
         * @memberof normfs.ReadRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {normfs.ReadRequest} ReadRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReadRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.normfs.ReadRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.readId = reader.uint64();
                        break;
                    }
                case 2: {
                        message.queueId = reader.string();
                        break;
                    }
                case 3: {
                        message.step = reader.uint64();
                        break;
                    }
                case 10: {
                        message.offset = $root.normfs.Offset.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 11: {
                        message.limit = reader.uint64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ReadRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof normfs.ReadRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {normfs.ReadRequest} ReadRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReadRequest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ReadRequest message.
         * @function verify
         * @memberof normfs.ReadRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ReadRequest.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.readId != null && message.hasOwnProperty("readId"))
                if (!$util.isInteger(message.readId) && !(message.readId && $util.isInteger(message.readId.low) && $util.isInteger(message.readId.high)))
                    return "readId: integer|Long expected";
            if (message.queueId != null && message.hasOwnProperty("queueId"))
                if (!$util.isString(message.queueId))
                    return "queueId: string expected";
            if (message.step != null && message.hasOwnProperty("step"))
                if (!$util.isInteger(message.step) && !(message.step && $util.isInteger(message.step.low) && $util.isInteger(message.step.high)))
                    return "step: integer|Long expected";
            if (message.offset != null && message.hasOwnProperty("offset")) {
                let error = $root.normfs.Offset.verify(message.offset, long + 1);
                if (error)
                    return "offset." + error;
            }
            if (message.limit != null && message.hasOwnProperty("limit"))
                if (!$util.isInteger(message.limit) && !(message.limit && $util.isInteger(message.limit.low) && $util.isInteger(message.limit.high)))
                    return "limit: integer|Long expected";
            return null;
        };

        /**
         * Creates a ReadRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof normfs.ReadRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {normfs.ReadRequest} ReadRequest
         */
        ReadRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.normfs.ReadRequest)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.normfs.ReadRequest();
            if (object.readId != null)
                if ($util.Long)
                    (message.readId = $util.Long.fromValue(object.readId)).unsigned = true;
                else if (typeof object.readId === "string")
                    message.readId = parseInt(object.readId, 10);
                else if (typeof object.readId === "number")
                    message.readId = object.readId;
                else if (typeof object.readId === "object")
                    message.readId = new $util.LongBits(object.readId.low >>> 0, object.readId.high >>> 0).toNumber(true);
            if (object.queueId != null)
                message.queueId = String(object.queueId);
            if (object.step != null)
                if ($util.Long)
                    (message.step = $util.Long.fromValue(object.step)).unsigned = true;
                else if (typeof object.step === "string")
                    message.step = parseInt(object.step, 10);
                else if (typeof object.step === "number")
                    message.step = object.step;
                else if (typeof object.step === "object")
                    message.step = new $util.LongBits(object.step.low >>> 0, object.step.high >>> 0).toNumber(true);
            if (object.offset != null) {
                if (typeof object.offset !== "object")
                    throw TypeError(".normfs.ReadRequest.offset: object expected");
                message.offset = $root.normfs.Offset.fromObject(object.offset, long + 1);
            }
            if (object.limit != null)
                if ($util.Long)
                    (message.limit = $util.Long.fromValue(object.limit)).unsigned = true;
                else if (typeof object.limit === "string")
                    message.limit = parseInt(object.limit, 10);
                else if (typeof object.limit === "number")
                    message.limit = object.limit;
                else if (typeof object.limit === "object")
                    message.limit = new $util.LongBits(object.limit.low >>> 0, object.limit.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a ReadRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof normfs.ReadRequest
         * @static
         * @param {normfs.ReadRequest} message ReadRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ReadRequest.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.readId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.readId = options.longs === String ? "0" : 0;
                object.queueId = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.step = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.step = options.longs === String ? "0" : 0;
                object.offset = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.limit = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.limit = options.longs === String ? "0" : 0;
            }
            if (message.readId != null && message.hasOwnProperty("readId"))
                if (typeof message.readId === "number")
                    object.readId = options.longs === String ? String(message.readId) : message.readId;
                else
                    object.readId = options.longs === String ? $util.Long.prototype.toString.call(message.readId) : options.longs === Number ? new $util.LongBits(message.readId.low >>> 0, message.readId.high >>> 0).toNumber(true) : message.readId;
            if (message.queueId != null && message.hasOwnProperty("queueId"))
                object.queueId = message.queueId;
            if (message.step != null && message.hasOwnProperty("step"))
                if (typeof message.step === "number")
                    object.step = options.longs === String ? String(message.step) : message.step;
                else
                    object.step = options.longs === String ? $util.Long.prototype.toString.call(message.step) : options.longs === Number ? new $util.LongBits(message.step.low >>> 0, message.step.high >>> 0).toNumber(true) : message.step;
            if (message.offset != null && message.hasOwnProperty("offset"))
                object.offset = $root.normfs.Offset.toObject(message.offset, options);
            if (message.limit != null && message.hasOwnProperty("limit"))
                if (typeof message.limit === "number")
                    object.limit = options.longs === String ? String(message.limit) : message.limit;
                else
                    object.limit = options.longs === String ? $util.Long.prototype.toString.call(message.limit) : options.longs === Number ? new $util.LongBits(message.limit.low >>> 0, message.limit.high >>> 0).toNumber(true) : message.limit;
            return object;
        };

        /**
         * Converts this ReadRequest to JSON.
         * @function toJSON
         * @memberof normfs.ReadRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ReadRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ReadRequest
         * @function getTypeUrl
         * @memberof normfs.ReadRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ReadRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/normfs.ReadRequest";
        };

        return ReadRequest;
    })();

    normfs.ReadResponse = (function() {

        /**
         * Properties of a ReadResponse.
         * @memberof normfs
         * @interface IReadResponse
         * @property {Long|null} [readId] ReadResponse readId
         * @property {normfs.ReadResponse.Result|null} [result] ReadResponse result
         * @property {normfs.IId|null} [id] ReadResponse id
         * @property {Uint8Array|null} [data] ReadResponse data
         * @property {normfs.ReadResponse.DataSource|null} [dataSource] ReadResponse dataSource
         */

        /**
         * Constructs a new ReadResponse.
         * @memberof normfs
         * @classdesc Represents a ReadResponse.
         * @implements IReadResponse
         * @constructor
         * @param {normfs.IReadResponse=} [properties] Properties to set
         */
        function ReadResponse(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ReadResponse readId.
         * @member {Long} readId
         * @memberof normfs.ReadResponse
         * @instance
         */
        ReadResponse.prototype.readId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ReadResponse result.
         * @member {normfs.ReadResponse.Result} result
         * @memberof normfs.ReadResponse
         * @instance
         */
        ReadResponse.prototype.result = 0;

        /**
         * ReadResponse id.
         * @member {normfs.IId|null|undefined} id
         * @memberof normfs.ReadResponse
         * @instance
         */
        ReadResponse.prototype.id = null;

        /**
         * ReadResponse data.
         * @member {Uint8Array} data
         * @memberof normfs.ReadResponse
         * @instance
         */
        ReadResponse.prototype.data = $util.newBuffer([]);

        /**
         * ReadResponse dataSource.
         * @member {normfs.ReadResponse.DataSource} dataSource
         * @memberof normfs.ReadResponse
         * @instance
         */
        ReadResponse.prototype.dataSource = 0;

        /**
         * Creates a new ReadResponse instance using the specified properties.
         * @function create
         * @memberof normfs.ReadResponse
         * @static
         * @param {normfs.IReadResponse=} [properties] Properties to set
         * @returns {normfs.ReadResponse} ReadResponse instance
         */
        ReadResponse.create = function create(properties) {
            return new ReadResponse(properties);
        };

        /**
         * Encodes the specified ReadResponse message. Does not implicitly {@link normfs.ReadResponse.verify|verify} messages.
         * @function encode
         * @memberof normfs.ReadResponse
         * @static
         * @param {normfs.IReadResponse} message ReadResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReadResponse.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.readId != null && Object.hasOwnProperty.call(message, "readId"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.readId);
            if (message.result != null && Object.hasOwnProperty.call(message, "result"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.result);
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                $root.normfs.Id.encode(message.id, writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            if (message.data != null && Object.hasOwnProperty.call(message, "data"))
                writer.uint32(/* id 11, wireType 2 =*/90).bytes(message.data);
            if (message.dataSource != null && Object.hasOwnProperty.call(message, "dataSource"))
                writer.uint32(/* id 12, wireType 0 =*/96).int32(message.dataSource);
            return writer;
        };

        /**
         * Encodes the specified ReadResponse message, length delimited. Does not implicitly {@link normfs.ReadResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof normfs.ReadResponse
         * @static
         * @param {normfs.IReadResponse} message ReadResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReadResponse.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ReadResponse message from the specified reader or buffer.
         * @function decode
         * @memberof normfs.ReadResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {normfs.ReadResponse} ReadResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReadResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.normfs.ReadResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.readId = reader.uint64();
                        break;
                    }
                case 2: {
                        message.result = reader.int32();
                        break;
                    }
                case 10: {
                        message.id = $root.normfs.Id.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 11: {
                        message.data = reader.bytes();
                        break;
                    }
                case 12: {
                        message.dataSource = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ReadResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof normfs.ReadResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {normfs.ReadResponse} ReadResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReadResponse.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ReadResponse message.
         * @function verify
         * @memberof normfs.ReadResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ReadResponse.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.readId != null && message.hasOwnProperty("readId"))
                if (!$util.isInteger(message.readId) && !(message.readId && $util.isInteger(message.readId.low) && $util.isInteger(message.readId.high)))
                    return "readId: integer|Long expected";
            if (message.result != null && message.hasOwnProperty("result"))
                switch (message.result) {
                default:
                    return "result: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                case 5:
                    break;
                }
            if (message.id != null && message.hasOwnProperty("id")) {
                let error = $root.normfs.Id.verify(message.id, long + 1);
                if (error)
                    return "id." + error;
            }
            if (message.data != null && message.hasOwnProperty("data"))
                if (!(message.data && typeof message.data.length === "number" || $util.isString(message.data)))
                    return "data: buffer expected";
            if (message.dataSource != null && message.hasOwnProperty("dataSource"))
                switch (message.dataSource) {
                default:
                    return "dataSource: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                    break;
                }
            return null;
        };

        /**
         * Creates a ReadResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof normfs.ReadResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {normfs.ReadResponse} ReadResponse
         */
        ReadResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.normfs.ReadResponse)
                return object;
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.normfs.ReadResponse();
            if (object.readId != null)
                if ($util.Long)
                    (message.readId = $util.Long.fromValue(object.readId)).unsigned = true;
                else if (typeof object.readId === "string")
                    message.readId = parseInt(object.readId, 10);
                else if (typeof object.readId === "number")
                    message.readId = object.readId;
                else if (typeof object.readId === "object")
                    message.readId = new $util.LongBits(object.readId.low >>> 0, object.readId.high >>> 0).toNumber(true);
            switch (object.result) {
            default:
                if (typeof object.result === "number") {
                    message.result = object.result;
                    break;
                }
                break;
            case "RR_START":
            case 0:
                message.result = 0;
                break;
            case "RR_ENTRY":
            case 1:
                message.result = 1;
                break;
            case "RR_END":
            case 2:
                message.result = 2;
                break;
            case "RR_QUEUE_NOT_FOUND":
            case 3:
                message.result = 3;
                break;
            case "RR_NOT_FOUND":
            case 4:
                message.result = 4;
                break;
            case "RR_SERVER_ERROR":
            case 5:
                message.result = 5;
                break;
            }
            if (object.id != null) {
                if (typeof object.id !== "object")
                    throw TypeError(".normfs.ReadResponse.id: object expected");
                message.id = $root.normfs.Id.fromObject(object.id, long + 1);
            }
            if (object.data != null)
                if (typeof object.data === "string")
                    $util.base64.decode(object.data, message.data = $util.newBuffer($util.base64.length(object.data)), 0);
                else if (object.data.length >= 0)
                    message.data = object.data;
            switch (object.dataSource) {
            default:
                if (typeof object.dataSource === "number") {
                    message.dataSource = object.dataSource;
                    break;
                }
                break;
            case "DS_NONE":
            case 0:
                message.dataSource = 0;
                break;
            case "DS_CLOUD":
            case 1:
                message.dataSource = 1;
                break;
            case "DS_DISK_STORE":
            case 2:
                message.dataSource = 2;
                break;
            case "DS_DISK_WAL":
            case 3:
                message.dataSource = 3;
                break;
            case "DS_MEMORY":
            case 4:
                message.dataSource = 4;
                break;
            }
            return message;
        };

        /**
         * Creates a plain object from a ReadResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof normfs.ReadResponse
         * @static
         * @param {normfs.ReadResponse} message ReadResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ReadResponse.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.readId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.readId = options.longs === String ? "0" : 0;
                object.result = options.enums === String ? "RR_START" : 0;
                object.id = null;
                if (options.bytes === String)
                    object.data = "";
                else {
                    object.data = [];
                    if (options.bytes !== Array)
                        object.data = $util.newBuffer(object.data);
                }
                object.dataSource = options.enums === String ? "DS_NONE" : 0;
            }
            if (message.readId != null && message.hasOwnProperty("readId"))
                if (typeof message.readId === "number")
                    object.readId = options.longs === String ? String(message.readId) : message.readId;
                else
                    object.readId = options.longs === String ? $util.Long.prototype.toString.call(message.readId) : options.longs === Number ? new $util.LongBits(message.readId.low >>> 0, message.readId.high >>> 0).toNumber(true) : message.readId;
            if (message.result != null && message.hasOwnProperty("result"))
                object.result = options.enums === String ? $root.normfs.ReadResponse.Result[message.result] === undefined ? message.result : $root.normfs.ReadResponse.Result[message.result] : message.result;
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = $root.normfs.Id.toObject(message.id, options);
            if (message.data != null && message.hasOwnProperty("data"))
                object.data = options.bytes === String ? $util.base64.encode(message.data, 0, message.data.length) : options.bytes === Array ? Array.prototype.slice.call(message.data) : message.data;
            if (message.dataSource != null && message.hasOwnProperty("dataSource"))
                object.dataSource = options.enums === String ? $root.normfs.ReadResponse.DataSource[message.dataSource] === undefined ? message.dataSource : $root.normfs.ReadResponse.DataSource[message.dataSource] : message.dataSource;
            return object;
        };

        /**
         * Converts this ReadResponse to JSON.
         * @function toJSON
         * @memberof normfs.ReadResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ReadResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ReadResponse
         * @function getTypeUrl
         * @memberof normfs.ReadResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ReadResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/normfs.ReadResponse";
        };

        /**
         * Result enum.
         * @name normfs.ReadResponse.Result
         * @enum {number}
         * @property {number} RR_START=0 RR_START value
         * @property {number} RR_ENTRY=1 RR_ENTRY value
         * @property {number} RR_END=2 RR_END value
         * @property {number} RR_QUEUE_NOT_FOUND=3 RR_QUEUE_NOT_FOUND value
         * @property {number} RR_NOT_FOUND=4 RR_NOT_FOUND value
         * @property {number} RR_SERVER_ERROR=5 RR_SERVER_ERROR value
         */
        ReadResponse.Result = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "RR_START"] = 0;
            values[valuesById[1] = "RR_ENTRY"] = 1;
            values[valuesById[2] = "RR_END"] = 2;
            values[valuesById[3] = "RR_QUEUE_NOT_FOUND"] = 3;
            values[valuesById[4] = "RR_NOT_FOUND"] = 4;
            values[valuesById[5] = "RR_SERVER_ERROR"] = 5;
            return values;
        })();

        /**
         * DataSource enum.
         * @name normfs.ReadResponse.DataSource
         * @enum {number}
         * @property {number} DS_NONE=0 DS_NONE value
         * @property {number} DS_CLOUD=1 DS_CLOUD value
         * @property {number} DS_DISK_STORE=2 DS_DISK_STORE value
         * @property {number} DS_DISK_WAL=3 DS_DISK_WAL value
         * @property {number} DS_MEMORY=4 DS_MEMORY value
         */
        ReadResponse.DataSource = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "DS_NONE"] = 0;
            values[valuesById[1] = "DS_CLOUD"] = 1;
            values[valuesById[2] = "DS_DISK_STORE"] = 2;
            values[valuesById[3] = "DS_DISK_WAL"] = 3;
            values[valuesById[4] = "DS_MEMORY"] = 4;
            return values;
        })();

        return ReadResponse;
    })();

    return normfs;
})();

export { $root as default };
