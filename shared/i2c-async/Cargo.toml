[package]
name = "i2c-async"
version = "0.1.0"
edition = "2024"

[dependencies]
bytes = { workspace = true }
tokio = { workspace = true }

[target.'cfg(target_os = "linux")'.dependencies]
i2cdev = "0.6.2"
libc = "0.2"
